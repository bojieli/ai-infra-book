 Sami Alabed, Daniel Belov, Bart Chrzaszcz, Juliana Franco, Dominik Grewe, Dougal Maclaurin, James Molloy, Tom Natan, Tamara Norman, Xiaoyue Pan,
                          Adam Paszke, Norman A. Rink, Michael Schaarschmidt, Timur Sitdikov, Agnieszka Swietlik, Dimitrios Vytiniotis, and Joel Wee

 API                                   Inputs                                               Description
 ManualPartition (tactic)              inputs: Dictionary                                   Specifies the sharding dimension for each input
                                       - keys: function input names                         along the given axis. For example:
                                       - values: dimension to shard                         ({"D": 0, "w0": 1}, axis="x")
                                       axis: String (e.g., "batch", "model")                Shards D’s 0th and w0 1st’s dimensions on the "x" axis.
 AutomaticPartition (tactic)           axes: List of Strings (e.g., ["x", "y"])             Automatically determines the program sharding
                                       options: Dictionary (passed to AUTO)                 over the specified axes.
 PartIR.jit                            func: The tensor program to partition                The entry point to PartIR. It returns:
                                       schedule: List of PartIR’s tactics                   - The partitioned program.
                                       kwargs: Dictionary                                   - Analytical performance estimates after each tactic.
                                       - Passed to jax.jit[4].                              - Inserted collectives after each tactic.
                                                               Table 1. PartIR Python API.



system is the StableHLO dialect, making PartIR a frontend-                          of type tensor<64x8xf32>. The range argument %rB plays
and backend-agnostic tool.3                                                         the role of a loop index that has a PartIR-specific range type.
   We have formally proven the correctness of the most com-                            The slice ops consume these loop indices. The meaning
plex part of the PartIR stack, the lowering to PartIR:SPMD,                         of slice 0 %x[%rB] is that it extracts the %rB-th chunk of
and described the process in detail in Appendix C. The rest                         the tensor %x along dimension 0. The tiling here perfectly
of the stack is compartmentalized and tested extensively.                           partitions the tensor dimension into 4 equally-sized, contigu-
                                                                                    ous chunks since axis "B" has size 4. Hence, the result of
5     PartIR:Core                                                                   slice has shape 64x8. Furthermore, the value tiling action
PartIR:Core introduces two operations on top of StableHLO:                          has replaced %x of type tensor<256x8xf32> with value %xt
1) a loop op that expresses pure (parallel) loops performing                        of the same type. Value tiling is a type-preserving, and also
tiling or reductions, and 2) a slice op that extracts a tensor                      semantics-preserving local rewrite.
slice based on a loop index. We will present these constructs
                                                                                    5.2    Propagation action
and their semantics as part of describing the PartIR:Core
compiler actions.                                                                   Value tiling actions create fairly trivial loops, making them
                                                                                    not particularly interesting. However, they help bootstrap a
5.1    Value tiling action                                                          powerful propagation pass that subsequently creates loops
A tiling action tile<%value, dim, axis> creates a loop                              around operations consuming or producing these values and
that in each iteration yields a slice of %value along dimension                     further slices other function arguments. This propagation is
dim. For example, value tiling %x along dimension 0 and axis                        justified by program equivalences.
"B" from Listing 1 - tile<%x, 0, B> - produces:                                     5.2.1 Program equivalences. PartIR propagation is built
func @f ( %x : tensor <256 x8xf32 > ,                                               around program equivalences that involve loop and slice in-
          %w1 : tensor <8 x16xf32 > ,                                               structions. Figure 4 presents three admissible program equiv-
          %w2 : tensor <16 x8xf32 >) {                                              alences for a matrix multiplication. The first two rewrite a
  %xt = loop " B " [ #tile <0 >] ( %rB : range <4 >) {                              matrix multiplication as a tiling loop with a smaller mul-
    yield ( slice 0 %x [ %rB ]) : tensor <64 x8xf32 >                               tiplication inside. The last one introduces the #sum action
  } : tensor <256 x8xf32 >                                                          attribute accompanying the loop. This signifies that the re-
  %x1 = matmul ( %xt, %w1 )
                                                                                    sults of each iteration of the loop should be reduced, as the
  %x2 = matmul ( %x1, %w2 )
                                                                                    operands are sliced on their contracting dimension.4
  return %x2 : tensor <256 x8xf32 > }
                                                                                       To allow us to implement the rewriting code once for all
   The loop operation contains two static attributes: (i) a                         operators, we use a tile-mapping registry (TMR). The TMR
mesh axis ("B") and (ii) an action attribute (#tile<0>). It                         contains, for every tensor operation with 𝑛 inputs, a set of
also accepts a single-argument closure (a region in the MLIR                        specifications of the form
jargon) that represents the loop body: (%rB: range<4>)
{ ... }. The closure takes as an argument a range value                                                   𝑡 1⊥, . . . , 𝑡𝑛⊥ ↩→ 𝜎1, . . . , 𝜎𝑘
(%rB) and performs a tensor computation returning a value                           where 𝑡 ⊥ stands for an optional tiling action, while 𝜎 stands
                                                                                    for an arbitrary action (including #sum). StableHLO ops and
3We only present APIs for JAX since it is our users’ frontend tool of choice.

Typical choices for backends are XLA, CUDA or OpenXLA.                              4 PartIR supports custom reductions for any associative reduction function.

                                                                                6
