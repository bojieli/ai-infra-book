PartIR: Composing SPMD Partitioning Strategies for Machine Learning


user wants to replicate. For example, by partitioning the                  or SPMD collectives in the case of shard_map, our users
optimizer state, propagation also shards the parameters,                   define their partitioning strategies using schedules, which
but for Z2 the parameters must be replicated (Section 2.3).                resemble ideas found in kernel-generating schedule-based
Hence, PartIR exposes an explicit atomic<value, axis> ac-                  systems [9, 16, 29, 47, 71]. Although we took inspiration
tion, which creates a trivial loop whose sole purpose is to                from Halide schedules [47], the schedules in PartIR differ in
block propagation and keep a value replicated. For example                 that they are applied to entire programs that span multiple
atomic<%x, "M"> replaces %x with this loop:                                devices rather than generating small program kernels for
%xr = loop " M " [ any ] ( %r : range < ... >) { yield %x ; }              a single device. Similar to Lift [59] and RISE [17], PartIR
                                                                           is inspired by functional-style IRs [60] that transform pro-
The [any] ensures all devices compute the same value.                      grams by equality-based rewriting [65] and similar to the
  Model-internal annotations. Section 7.4 argues that se-                  functional array representation in [43]. This is in contrast to
quentialization of decisions lets us handle most conflicts.                DaCe [3] that applies graph transformations interactively by
However, we are still left with corner cases. For example,                 employing a representation based on data flow graphs. Dis-
consider a component of matrix diagonalization, where a                    tIR [53] exposes a Python API with explicit device placement
matrix is multiplied by its transpose:                                     and point-to-point communication to program the distribu-
                                                                           tion directly. PartIR:Core abstracts away distribution and
func @main ( %x : tensor <(256 x256xf32 >) {
  %tx = transpose %x { dims =[1 ,0 ]} : ...
                                                                           deals with it during SPMD lowering. Finally, PartIR allows
  %y = matmul ( %x, %tx ) ...                                              for an automatic partitioner to be used as tactic, making it
                                                                           orthogonal to Alpa [73], AutoMap [2, 54] and others.
If we shard %x on dimension 0 along a given mesh axis "M",
then %tx (its transpose) is sharded on dimension 1. That is a                 A newer generation of StableHLO partitioners. Ad-
propagation “conflict” and prevents sharding of the matmul.                dressing some PartIR limitations (e.g. limited reshape sup-
To resolve this conflict, users must replicate the intermediate            port, relying on examining the loop structure for propaga-
tensor %tx before partitioning it, which is done by naming                 tion), the newly introduced Shardy propagation system [39]
the tensor using a primitive called tag, then applying an                  employs rewriting-free sharding annotation propagation
atomic action on it:                                                       (like GSPMD), but based on mesh axes (like PartIR). It extends
func @main ( %x : tensor <(256 x256xf32 >) {
                                                                           the PartIR TMR idea (Section 5.2.1) with sharding factors that
  %tx = transpose %x { dims =[1 ,0 ]}                                      allow for implicit mesh splitting. Currently, Shardy relies
  %tag_tx = tag " transposed " %tx                                         on a separate code generation/collectives insertion pass, the
  %atomic = loop " M " [ any ] ( %r : range < ... >) {                     same one GSPMD uses. Which complicates the process of
    yield %tag_tx ;                                                        ensuring consistency between the two passes and to obtain
  }                                                                        a cost model at the MLIR level prior to entering the XLA
  %y = matmul ( %x, %atomic ) ...                                          compiler. However, there exists ongoing work to address
By this forcing of replication of the intermediate %tx/%tag_x              these issues. Shardy supports incremental propagation based
 an all_gather is inserted on the second operand of the                    on priority annotations in sharding specifications, and fea-
matmul in the final partitioned function:                                  tures an elaborate hierarchy for conflict resolution. Finally
                                                                           the Mesh MLIR dialect[45] is an attempt to introduce mesh-
func @main ( %x : tensor <(16 x256xf32 >) {
  %tx = transpose { dims =[1 ,0 ]} %x :
                                                                           based operations at the StableHLO MLIR level that can be
    tensor <256 x16xf32 >                                                  used to define a complete partitioner.
  %gx = all_gather [{} , { " M " }] %tx :
    tensor <256 x256xf32 >                                                 10    Conclusion and Future Work
  %y = matmul ( %x, %gx ) : tensor <16 x256xf32 >                          PartIR is an MLIR-based compiler that enables effortless
  ...                                                                      partitioning of tensor programs by decoupling partitioning
                                                                           strategy from model code and instead expresses them using
9    Related work                                                          tactics. Each tactic desugars into a series of compiler rewrite
The need for NN scaling motivated new partitioning tools [49,              actions. PartIR’s incremental design enables a powerful (yet
55], as frameworks like TensorFlow [1] and PyTorch [42]                    simple) propagation system that automatically predictably
support only data and limited model parallelism. JAX [4] pro-              partitions programs without relying on cost-based heuristics
vides two main partitioning APIs: jax.jit, which surfaces                  to handle conflicts. In the future, we want to support training
GSPMD [70] (that builds on GShard [32]); and shard_map,                    over heterogeneous device clusters using MPMD [22, 33, 34],
which disables GSPMD and lets users manually perform com-                  which will require new partitioning tactics and IR extensions.
munication across devices. PartIR builds on top of some of
JAX’s partitioning infrastructure (e.g., meshes), but instead of
users annotating their NN code with sharding annotations,
                                                                      13
