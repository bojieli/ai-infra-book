PartIR: Composing SPMD Partitioning Strategies for Machine Learning



                              params                   Device 1


           batch[0]                                               new
                        fwd   bwd                    update
                                                                  params

                                    AllReduce    ⊕
                                     gradients
           batch[1]                                               new
                        fwd   bwd                    update
                                                                  params


                              params
                                                       Device 2




                              params[0]               Device 1
                                                                                  Figure 2. Batch (N) and model (M) parallelism. On the left, the
           batch[0]
                        fwd   bwd
                                                                  new             communication along the M axis (e.g., Megatron’s [56] activation
                                                     update
                                                                  params[0]
                                                                                  reductions). On the right, the communication along the N axis (e.g.,
            AllGather          ReduceScatter
             params                 gradients
                                                 ⊕                                gradient reductions). Each device parameter shard is color-coded;
          batch[1]                                                new
                        fwd   bwd                    update
                                                                  params[1]
                                                                                  all devices along N hold the same shard.

                              params[1]               Device 2
                                                                                  shows a model partitioned over a 2D mesh with BP on one
                                                                                  axis and MP over the other. There exist many other strate-
Figure 1. Top: batch parallelism, the gradients are AllRe-                        gies, such as activation sharding after MP [30, 66], or Trans-
duced to update the parameters. Bottom: Z3/FSDP, note the                         former’s multi-query sharding [44]. Many of these combina-
parameters are sharded and only AllGathered before their                          tions were implemented and verified in PartIR.
use, highlighted in thick blue bars in the figure. The gradi-
ents are ReducedScattered before updating the parameters.                         2.4    Strategies as program transforms
                                                                                  To demonstrate these partitioning strategies, consider a JAX
                                                                                  program composed of two matrix multiplications1 :
2.3   Parallelism strategies                                                      def f (x , w1 , w2 ) :
  Batch parallelism (BP). The input batch is sharded across                         return ( x @ w1 ) @ w2
the devices while the model parameters and optimizer state                        y = jax . jit ( f ) ( input , w1 , w2 ) # Trace, compile, run.
are replicated. We expect to see one AllReduce operation per                      The jax.jit() function traces the Python function into Sta-
parameter in the backward pass, cf. Figure 1.                                     bleHLO [31] MLIR, the array IR encoding of XLA HLO [15],
   Model parallelism (MP). Model parameters are sharded                           before compiling the function for a specific backend. The
across the devices – for example, in Megatron sharding for                        StableHLO for the program above looks as follows:2
Transformer models [30, 35, 56], 2 AllReduce operations (on                       func @main ( %x : tensor <256 x8xf32 > ,
activation tensors) are introduced per Transformer layer                                       %w1 : tensor <8 x16xf32 > ,
(and 2 more for its backward pass).                                                            %w2 : tensor <16 x8xf32 >) {
                                                                                    %x1 = matmul ( %x, %w1 ) : tensor <256 x16xf32 >
  Optimizer sharding. The optimizer state is sharded to                             %x2 = matmul ( %x1, %w2 ) : tensor <256 x8xf32 >
reduce peak memory. Two variants exist: ZeRO2 (Z2) that                             return %x2 : tensor <256 x8xf32 >
additionally partitions the gradients, and ZeRO3 (Z3) that                        }
further partitions the parameters [48, 50, 69]. This paper uses                   Listing 1. An unpartitioned matmul chain, where each value
an SPMD variant that shards all parameters, known as fully-                       is annotated with a type, e.g., %x1 is an array of shape 256x8
sharded data parallelism (FSDP) [72]. Z3/FSDP (illustrated in                     and a float32 element type.
Figure 1) reduce peak memory usage as the optimizer uses
gradient shards and parameter/optimizer shards to update                            Assuming this program executes on a 2D mesh {𝐵:4, 𝑀:2},
the parameter shards. These shards are distributed and gath-                      what kind of parallelization strategy should we use?
ered back when they are needed. From the perspective of
                                                                                    Batch (data) parallelism. One strategy is to partition
SPMD collectives, every parameter is gathered once in the
                                                                                  the first (256-sized) dimension of input %x across axis 𝐵 -
forward and once in the backward pass, using AllGathers.
                                                                                  resulting in a pure “map” over that dimension:
The gradients are gathered during the backward pass only
using ReduceScatter – a cheaper operation than AllReduce.                         1 For simplicity, we do not show a full training step with back-propagation,

                                                                                  just a feed-forward function. We even skip the elementwise non-linear
  Combining strategies. Training large models efficiently                         operators, as they are trivial to partition.
requires combining multiple partitioning strategies across                        2 StableHLO uses dot_general to represent matrix multiplication. We use

several axes, e.g., BP+MP+Z3 [35, 46]. For example, Figure 2                      matmul as syntactic sugar in the paper for easier presentation.
                                                                              3
