           A Software-defined Tensor Streaming Multiprocessor for
                       Large-scale Machine Learning
              Dennis Abts                               Garrin Kimmell                           Andrew Ling                          John Kim
                  Groq Inc.                                    Groq Inc.                             Groq Inc.                     KAIST/Groq Inc.

               Matt Boyd                                  Andrew Bitar                           Sahil Parmar                    Ibrahim Ahmed
                  Groq Inc.                                    Groq Inc.                            Groq Inc.                          Groq Inc.

         Roberto DiCecco                                    David Han                           John Thompson                       Michael Bye
                  Groq Inc.                                    Groq Inc.                            Groq Inc.                          Groq Inc.

          Jennifer Hwang                                Jeremy Fowers                             Peter Lillian                  Ashwin Murthy
                  Groq Inc.                                    Groq Inc.                            Groq Inc.                          Groq Inc.

       Elyas Mehtabuddin                                  Chetan Tekur                          Thomas Sohmers                       Kris Kang
                  Groq Inc.                                    Groq Inc.                            Groq Inc.                          Groq Inc.

                                                       Stephen Maresh                            Jonathan Ross
                                                               Groq Inc.                            Groq Inc.

ABSTRACT                                                                                    CCS CONCEPTS
We describe our novel commercial software-defined approach for                              • Computer systems organization → Interconnection archi-
large-scale interconnection networks of tensor streaming process-                           tectures.
ing (TSP) elements. The system architecture includes packaging,
routing, and flow control of the interconnection network of TSPs.                           KEYWORDS
We describe the communication and synchronization primitives of
                                                                                            Machine Learning, Tensor Streaming Processor, Dragonfly, Soft-
a bandwidth-rich substrate for global communication. This scalable
                                                                                            ware Scheduling
communication fabric provides the backbone for large-scale sys-
tems based on a software-defined Dragonfly topology, ultimately
yielding a parallel machine learning system with elasticity to sup-
port a variety of workloads, both training and inference. We extend                         1    INTRODUCTION
the TSP’s producer-consumer stream programming model to in-                                 Historically, high-performance computing (HPC) systems were
clude global memory which is implemented as logically shared, but                           broadly categorized as capability or capacity systems. This di-
physically distributed SRAM on-chip memory. Each TSP contributes                            chotomy arises because of communication latency and bandwidth
220 MiBytes to the global memory capacity, with the maximum                                 trade-offs when we apply more processing elements (PEs) to a
capacity limited only by the network’s scale — the maximum num-                             fixed-size problem (strong scaling) with the goal of minimizing the
ber of endpoints in the system. The TSP acts as both a processing                           program’s execution time. Alternatively, we can deploy more PEs
element (endpoint) and network switch for moving tensors across                             to increase throughput (i.e. weak scaling). This duality requires
the communication links. We describe a novel software-controlled                            both novel chip architectures [7][1][36] in the underlying PEs and
networking approach that avoids the latency variation introduced                            a scalable system architecture with high throughput (bisection
by dynamic contention for network links. We describe the topol-                             bandwidth) and low end-to-end latency (low network diameter) for
ogy, routing and flow control to characterize the performance of                            fine-grained communication necessary to efficiently handle both
the network that serves as the fabric for a large-scale parallel ma-                        strong and weak scaling. For example, the network demand for
chine learning system with up to 10,440 TSPs and more than 2                                training an ML model, often requiring data parallelism (weak scal-
TeraBytes of global memory accessible in less than 3 microseconds                           ing), differs from inference on that same model using (pipelined)
of end-to-end system latency.                                                               model parallelism (ie. strong scaling). The multiprocessor system,
                                                                                            interconnection network, and the programming model of the indi-
Permission to make digital or hard copies of part or all of this work for personal or       vidual processing elements work in unison to collectively execute
classroom use is granted without fee provided that copies are not made or distributed       the different “layers” of a deep learning network. It is this set of
for profit or commercial advantage and that copies bear this notice and the full citation
on the first page. Copyrights for third-party components of this work must be honored.      sub-tasks, expressed as individual PE programs, that are distributed
For all other uses, contact the owner/author(s).                                            among the computing elements and responsible for carrying out,
ISCA ’22, June 18–22, 2022, New York, NY, USA                                               or executing, the specifics of the machine learning model.
© 2022 Copyright held by the owner/author(s).
ACM ISBN 978-1-4503-8610-4/22/06.                                                              The burgeoning parameter space of natural language processing
https://doi.org/10.1145/3470496.3527405                                                     (NLP) models like GPT-3 [6] use 100s-of-billions of parameters to
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                           Abts and Kimmell, et al.




Figure 1: A conventional manycore chip multiprocessor                          Figure 2: The global bandwidth profile per TSP.
with sources of non-determinism (highlighted in red) which
can reorder memory and network references.                              about 14 GB/sec of global bandwidth per TSP endpoint. The Drag-
                                                                        onfly network delivers flat global bandwidth up to the maximum
                                                                        system configuration of 145 cabinets for a total of 10,440 TSPs.
achieve state-of-the-art accuracy for a variety of inference applica-      In the remainder of this paper, we describe the system architec-
tions. These models require significant compute resources for both      ture that enables a synchronous communication model across the
training [31] [39] and inference to spread the model across multiple    network fabric of TSP elements, in particular:
processing elements. The computational demands of large models              • We propose a software-scheduled high-radix interconnection
are twofold: requiring memory resources to store model parame-                network that enables deterministic communication among
ters, constants, and gradients to fit into the available memory of            TSPs and eliminates latency variance. We also describe the
each processing element; and load balance the computation (flops)             design and implementation of a software-scheduled Dragon-
across the processing elements. To efficiently train these models,            fly topology for scale-out TSP system.
a variety of techniques have been used to exploit both pipelined            • We describe the hardware-software interface implementa-
(model) parallelism [30][19] and data (mini-batch) parallelism.               tion including instruction level support to “deskew” and
   In a conventional CPU or GPU multiprocessor, the memory and                align a set of plesiochronous links to enable a synchronous
network resources are dynamically shared among the processing                 distributed programming model and a tensor-based commu-
elements (Fig 1) and are a source of non-determinism. To make                 nication protocol that eliminates packet headers/footers.
an ML model amenable to execution on a parallel computer, we                • We propose and describe a novel source-based routing and
must first decompose the model into sub-tasks that can be mapped              flow control that enables a software-scheduled network with
to the underlying processing elements of the system. The com-                 explicit software control of the traffic pattern and its total
munication cost among the PEs is tightly coupled to the system’s              ordering of packets across the network.
packaging hierarchy which seeks to exploit “packaging locality”             • We evaluate system performance on representative work-
wherein proximal compute resources are densely connected provid-              loads: 1) distributed matrix multiplication, 2) Cholesky fac-
ing more bandwidth among these highly-connected components.                   torization, and 3) latency-sensitive collectives such as All-
As a result, the parallel decomposition strategy is aligned with the          Reduce that are essential for scaled ML or converged HPC
system packaging hierarchy in terms of racks, nodes, and eventually           applications.
the PEs carrying out the execution of each sub-task.
   This paper describes the novel software-defined system architec-     2   SCALE-OUT SYSTEM ORGANIZATION
ture of a commercial, scale-out tensor streaming processing (TSP)       The programming model of the TSP is based upon a statically sched-
multiprocessor. The software-defined multi-TSP system extends the       uled, deterministic execution with 220 MiBytes of local storage for
determinism of a single TSP using software-scheduled high-radix         parameters and instruction text [1]. Each TSP is programmed using
Dragonfly network and ISA “runtime deskew” instruction support          a producer-consumer stream programming model that allows func-
to maintain the illusion of a synchronous, lock-step system. We         tional units to be chained together. The TSP’s functional units are
describe the system architecture in terms of TSP endpoints [1],         organized as 320-element SIMD (single instruction multiple data)
network topology, routing, flow control, and fault tolerance. The       instruction execution into functional slices consisting of a group of
scalability or scale-out bandwidth is determined from the system        20 tiles, each of which perform a 16-way SIMD computation on the
packaging constraints which in turn drive the system’s “bandwidth       data. To enable seamless scalability and preserve that programming
profile” (Fig 2). The bandwidth profile expresses the relationship      model across multiple TSP processing elements, the network also
between system scale (number of endpoints) and global bandwidth         needs to be deterministic. In this section, we describe the Dragonfly
and illustrates the bandwidth cliffs at each system packaging bound-    topology that is exploited for the software-defined system archi-
ary. As Fig 2 shows, small systems with fewer than 16 TSPs can take     tecture to provide scalability. We detail the core topology design
advantage of abundant wire density within the node, and larger          objectives to achieve the desired scale-out application demands,
systems up to several hundred TSPs can take advantage of about          the hierarchical organization of the system based on the Dragonfly
50 GB/sec of global (bisection) bandwidth per TSP. As system size       topology, and the fundamental properties of the chip-to-chip (C2C)
grows beyond 264 TSPs, the available global bandwidth flattens to       links between processing elements.
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                    ISCA ’22, June 18–22, 2022, New York, NY, USA




                                                                                         Figure 5: Four levels of packaging hierarchy, where each
                                                                                         chip (a) is fitted with a heat-sink and packaged on a PCIe
                                                                                         card (b) and eight TSP cards are co-located within the a
                                                                                         shared memory node (c) occupying four rack units (4RU) in
Figure 3: The global shared address space is physically dis-                             the rack (d). An expanded view of node shows the abundant
tributed among the TSPs in the system.                                                   intra-node, low-profile cables within the chassis to exploit
                                                                                         packaging locality.

                                                                                         eliminating the non-determinism that can be induced by the in-
                                                                                         termediate routers, software-scheduled network organization also
                                                                                         enables high injection bandwidth as the router node bandwidth
                                                                                         effectively becomes processing endpoint injection bandwidth.
                                                                                         Hierarchical Packaging-aware Topology: The topology of the
                                                                                         interconnection network is driven primarily by the packaging con-
                                                                                         straints [11] imposed by system packaging hierarchy. In particular,
                                                                                         while high-radix routers can achieve low network diameter, en-
                                                                                         abling high-radix can be a challenge in the scale-out systems pack-
Figure 4: High-level organization of (a) indirect network, (b)
                                                                                         aging (pin) constraints. The packaging constraints encountered
direct networks, and (c) proposed software-scheduled (“di-
                                                                                         in designing our system vary across levels of the hierarchy: from
rect”) scale-out organization (R: router, P: processing end-
                                                                                         pin-count constraints on the ASIC die level; area and form-factor
point).
                                                                                         constraints at the PCIe card level; general-purpose CPU (non-TSP)
2.1      Topology Objectives                                                             compute, power, cooling, and cabling routing constraints at the
                                                                                         chassis level; to cable length and power at the rack level.
The objectives of the scale-out multi-TSP system topology are the                           To achieve the objectives described above, the Dragonfly [25]
following.                                                                               topology was leveraged in the scale-out system. While high-radix
Low network diameter: The total observed communication la-                               topologies such as Flattened Butterfly [24] or HyperX [3] can be
tency and variance increases with the number of hops in the the                          leveraged, their scalability is limited when the router or node radix
network. Thus, reducing the network diameter is known to reduce                          is small. However, by using a collection of routers as a group, a
network latency (hop count) as well as lower the network cost [26].                      virtual high-radix network can be created with the Dragonfly and
Direct Network: System topologies can be classified as either indi-                      enables scalable topology with lower radix node while not requiring
rect or direct networks1 (Fig 4). While indirect networks such as fat-                   intermediate switches that is required for topologies such as the
tree have been commonly used for large-scale systems, they require                       fat-tree.
intermediate switches or routers that introduce non-determinism
from dynamic arbitration and queuing. Instead, a direct network                          2.2    System packaging hierarchy
(Fig 4(b)) removes intermediate routers as all routers have end-
points connected to them. In the scale-out TSP system, the direct                        The basic building block of the system packaging is a 4U chassis
network is extended to create a “glueless” multiprocessor by com-                        enclosure which houses eight TSPs as shown in Fig 5 that is referred
bining the processing endpoints and the routers (Fig 4(c)) – thus,                       to as a “node.” The pin-bandwidth from each TSP is partitioned
directly connect TSPs to create their communication fabric. While                        into 7 “local” links and 4 “global” links. The 7 local links are used
                                                                                         to provide full connectivity between the group of 8 TSPs that are
1 Indirect networks are defined as systems where endpoints and switches are considered
                                                                                         within the same SMP coherence domain and can communicate
“separate” while direct networks can be represented as endpoints and switches being a    synchronously with other TSPs in the node (Fig 6). All of the global
single “node.” [11]                                                                      links for each TSP within a node can be combined to create a
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                                                    Abts and Kimmell, et al.


                                                                                             28 internal C2C cables to fully-connect the eight (8) TSPs with their
                                                                                             7 peers within the node. This allows us to keep 73% of the cables
                                                                                             (44 of 60 cables used by each node) short and inexpensive using
                                                                                             electrical signaling.
                                                                                                As a consequence of the deterministic network design, the hard-
                                                                                             ware is disallowed from asserting back pressure which would dis-
                                                                                             rupt deterministic operation of the network. Instead, software ex-
                                                                                             plicitly schedules vectors on each physical link in the system taking
                                                                                             into account the channel bandwidth and latency of each channel to
                                                                                             ensure we never overflow the transmitter or underflow the receiver.
                                                                                             Specifically, as a tensor flows hop by hop through the network,
            Figure 6: Scale-out topology block diagram.                                      we use the local SRAM storage on each TSP to provide interme-
                                                                                             diate buffering of the tensor’s individual vectors and in this way,
32 “virtual” port high-radix router to effectively be a “group” that                         a vector is the flow control unit (flit). Since the network path is
is used as a building block of the Dragonfly topology. Each C2C                              fully deterministic, a tensor consisting of one or more vector flits
link consist of four (4) lanes and thus in theory, the radix can be                          can be scheduled using virtual cut through [23] flow control since
increased to 11 × 4 = 44 ports. However, the packaging constraints                           the receiving TSP can immediately begin sending vectors to the
of a PCIe card limits the number of links that can be exposed for                            next-hop of the tensor’s path.
inter-node communication. Instead, we exploit the abundant wire
density within the 4U node chassis level of the packaging hierarchy,                         3     MAINTAINING DETERMINISM IN A
to enable full connectivity between the 8 TSPs while reducing the                                  DISTRIBUTED SYSTEM
amount of bandwidth for global communication.                                                The collection of functional units on each TSP act as a single logical
   Using a node as a building block with 32-port virtual router, the                         core which is scheduled statically (at compile-time). We extend the
TSP system can scale out up to 33 nodes for total of 33×8 = 264 TSPs                         single-chip TSP determinism to a multi-chip distributed system so
by providing full connectivity between all of the nodes that results                         that we can efficiently share the global SRAM without requiring a
in a three hop topology with minimal routing. Thus, it allows fine-                          mutex to guarantee atomic access to the global memory — instead,
grained communication across the 264 TSP system and efficient                                we communicate with explicit send or receive instructions at spe-
access to its combined 56 GiBytes of global SRAM (Fig 3). At the                             cific times so we can reason about program correctness from this
next higher layer of the packaging hierarchy is the rack, consisting                         total ordering. In order to achieve this predictable behavior, the
of nine (9) nodes, each with eight (8) TSPs, interconnected via the                          TSP hardware-software interface exposes all architecturally-visible
four (4) “global” links per TSP for a total of 32 × 9 = 288 ports                            state (all SRAM, and stream registers) so that the static computation
of global bandwidth. To scale to even larger systems, we can use                             graph can be expressed as a series of dependencies that impose
the rack as the “local group” and partition half of the 288-ports to                         temporal deadlines on the operand arrival times of tensors being
doubly-connect the set of nine (9) nodes within each rack using half                         communicated. We express these dependencies as a DAG (directed
(144) of the ports so that we have the proper 2× “internal speedup”                          acyclic graph) to explicitly schedule the communication traffic. Sum-
within the local group required to route traffic among the global                            mary of ISA support to enable determinism across multiple nodes
links. The remaining 144 ports are used to connect to other racks in                         is shown in Table 1.
the system and delivers up to 145 racks in the maximally configured                              A multi-TSP system relies on three mechanisms to establish and
system or 145 (racks) ×72 (TSPs per rack) for a total of 10,440 TSPs                         maintain synchrony: i) a per-TSP collection of hardware-aligned
in the largest system with at-most 5 hop diameter using minimal                              counters that are continuously (every 256 cycles) exchanged to
routing (two in the source-rack, one global hop, and two in the                              maintain a global consensus time; ii) a procedure for initial pro-
destination-rack).                                                                           gram alignment that utilizes the links to ensure every TSP begins
                                                                                             executing its instructions simultaneously; iii) a runtime resyn-
2.3      Chip-to-chip (C2C) links and flow control                                           chronization process to account for individual TSP clock drift
The chip-to-chip (C2C) links connect TSPs both within and be-                                during long-running computations.
tween nodes. The C2C links use low-swing differential signaling
operating up to 30 Gbps2 over 34 AWG cables, with the longest                                3.1     Hardware aligned counters (HAC)
cable being 0.75 meter within the node. This low-profile cabling is                          Synchronizing a network of TSPs involves a combination of hard-
designed to lay flat underneath the shroud used on the 4U enclosure.                         ware and software mechanisms. Each TSP maintains a free-running
Likewise, we can exploit packaging locality and keep cables within                           internal hardware aligned counter (HAC) with a low (< 256 cy-
the rack relatively short (<2m) and use low-cost QSFP (quad-small                            cle) overflow period. 3 When two TSPs are connected via a point-to-
form package) electrical cables limiting the more expensive active                           point C2C link, each TSP will transmit its internal HAC value to its
optical cables for longer rack-to-rack connections. Each node has                            peer. This provides a mechanism for characterizing link latency by
2 The C2C links support a variety of operating speeds, however, in general we operate        having a TSP transmit its current HAC value, then its peer reflect
all the links at the same data rate of 25 Gbps for both electrical cables within racks and   that value back. When the reflected HAC value is returned to the
active-optical transceivers between racks, with a combined bandwidth of 100 Gbps
across the four lanes in a link.                                                             3 The HAC period is also referred to as an epoch.
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                         ISCA ’22, June 18–22, 2022, New York, NY, USA


 Table 1: ISA support for a deterministic scale-out system.                           Table 2: HAC latency characterization of seven (7) intra-node C2C
                                                                                      links based on 100K iterations.
              Name              Description
               HAC              hardware aligned counter
                                                                                                           link     min       mean       max        std
               SAC              software aligned couner                                                       A     209      216.87      228       2.93
                                                                                                              B     210      216.87      227       2.88
               SYNC             intra-chip pause instruction
                                                                                                              C     209      216.41      226       2.66
              NOTIFY            intra-chip global signal to functional
                                units to restart execution                                                    D     210      216.47      226       2.71
              DESKEW            pause instruction until HAC over-                                             E     209      216.27      226       2.78
                                flows                                                                         F     209      216.48      225       2.63
            TRANSMIT            instruction to send notification to                                           G     211      217.35      228       2.84
                                child through C2C link
       RUNTIME_DESKEW 𝑡         delay TSP for 𝑡 ± 𝛿𝑡
                                                                                      common (periodic) HAC reference can be combined to provide the
                                                                                      illusion of a shared clock across the entire system in the following
originating TSP, it is compared with the internal free-running HAC,                   manner. First, we introduce a DESKEW instruction that allows us to
with the difference being the link latency (modulo a multiple of the                  align program execution with the (local) HAC. When a functional
HAC period) (Fig 7(a)). This procedure is repeated until we have                      unit executes a DESKEW, it will pause issuing subsequent instruc-
an acceptable confidence in the estimate of mean latency and vari-                    tions on that functional unit until the next time the HAC overflows
ance within the system tolerance. Table 2 summarizes the results                      or the epoch boundary. This allows us to ensure that distributed
of performing this operation 100K times within a 8-TSP node and                       computation can begin relative to a HAC epoch boundary that is a
latency for 7 of the intra-node C2C links are shown.                                  shared reference time among TSPs in the network.
    After a link latency is characterized, two peer TSPs 𝑇0 and 𝑇1 ,                      Fig 7(b) shows the HAC-based synchronization procedure using
with an observed latency with mean 𝐿, are configured in a paren-                      DESKEW instructions in preparation for invoking a multi-TSP pro-
t/child relationship such that the HAC maintained within 𝑇0 (i.e.,                    gram. At time 𝑡 1 , the child device is locally placed into a polling
HAC0 ) serves as the reference and is periodically transmitted to                     synchronization loop. In the polling loop, at each epoch boundary
𝑇1 . When the instantaneous value of HAC0 is received by 𝑇1 , its                     (shown as HAC=0), the child device will wait to receive a vector from
value plus the latency 𝐿 is compared to HAC1 . The difference rep-                    the parent device. If that vector has not been transmitted yet, the
resents initial misalignment from continual clock drift. The value                    loop will continue. At time 𝑡 2 a parent program is invoked that per-
of HAC1 is adjusted to reduce the difference (the maximum adjust-                     forms a DESKEW followed by a TRANSMIT. Though the events 𝑡 1 and
ment rate is configurable). After a sufficient number of iterations                   𝑡 2 are unsynchronized, the use of DESKEW forces alignment of the
of this process (approximately bounded by the period of the HAC                       subsequent instruction with the shared HAC value. The TRANSMIT
counters), the two counters HAC0 and HAC1 will converge within                        from the parent happens at an epoch boundary and the vector ar-
a neighborhood determined by the jitter of the link latency. Thus,                    rives at the child at some time 𝑡 3 . This vector will be consumed
this protocol determines a common periodic reference for two TSPs.                    by the RECV instruction that will issue following the next epoch
To expand this protocol to a multi-hop network of TSPs, a spanning                    boundary following 𝑡 3 . This will cause the child device to exit the
tree of parent/child HAC relationships is established to maintain a                   synchronization loop at ⌊𝐿/𝑝𝑒𝑟𝑖𝑜𝑑⌋ +1 clock epochs 4 following the
common HAC reference time distributed across the network.                             transmit from the parent to child device, at time 𝑡 4 . Finally, both the
    The HAC alignment procedure serves as the foundation upon                         parent and child device will issue a (chip-local) NOTIFY instruction
which the synchronous distributed system is programmed. We                            at time 𝑡 4 to begin synchronized computation.
build upon this foundation with software-controlled ISA support                           To support scaling to multi-hop networks, the system incorpo-
for a common software clock reference. We recall the foundations                      rates the DESKEW-based synchronization process repeatedly along
for deterministic scheduling [1] of multiple functional units on a                    each hop of the HAC spanning tree, with an overall synchronization
single TSP depends on a SYNC instruction to “pause” issue from the                    overhead of (⌊𝐿/𝑝𝑒𝑟𝑖𝑜𝑑⌋ + 1) ∗ℎ total cycles, where 𝐿 the maximum
independent instruction streams for the various units, followed by                    single-link latency and ℎ the height of the spanning tree. Note that
a single functional unit issuing a NOTIFY instruction. This NOTIFY                    this overhead occurs only at the start of a distributed inference. A
instruction in turn results in a global control signal (with known                    lighter-weight (approximately 1 epoch) runtime resynchronization
latency) to be delivered to the paused functional units, causing                      process described in the following section is used to adjust for clock
them to restart execution on the same clock cycle. This NOTIFY                        skew during program execution.
thus serves as the software-controlled time reference upon which
all other static scheduling is derived.                                               3.3      Runtime deskew for resynchronization
                                                                                      The HAC-based scheme described in the previous section estab-
3.2     Initial program alignment                                                     lishes a common starting time reference for a multi-TSP system, but
                                                                                      the inter-TSP drift due to frequency uncertainty of the independent
The SYNC and NOTIFY instructions provide a chip-wide synchroniza-
tion mechanism that relies on a shared clock and fully-deterministic                  4 𝐿 is the latency of the parent to child link and the period is the epoch length or 252
control propagation path which does not exist in a multi-TSP sys-                     clock cycles. The HAC is an 8-bit counter, but 4 values are reserved for special control
tem since each TSP has an independent clock source. However, the                      codes.
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                              Abts and Kimmell, et al.


                                                                                        Conventional
                                                                                                                  Software-Scheduled
                                                                                     (Non-Deterministic)
                                                                                                                       Network
                                                                                          Network
                                                                                           backpressure 2

                                                                                      A                   B        A             B
                                                                                          re-route
                                                                                                      1
                                                                                       3


                                                                                      C                   D        C             D
                                                                                                (a)                       (b)


                                                                          Figure 8: Comparison of (a) conventional network and
                                                                          (b) software-scheduled network (SSN) when routing con-
Figure 7: Sequence diagram overview of synchronization for (a)            tention can occur. SSN resolves all contention via software
HAC exchange and adjustment and (b) initial synchronization.              at compile-time and does not require adaptive hardware or
                                                                          congestion sensing to assert back-pressure.
clocks across multiple TSPs must also be tolerated. The TSP pro-
vides an additional software aligned counter (SAC) to allow the
TSPs to re-synchronize during computation to keep accumulated             in the network, with scheduling tensors at compile time. With our
drift within allowable tolerances. The SAC is a free-running counter      SSN approach, all contention for both functional units and network
with the same period as the HAC. However, unlike the HAC, it is           links are resolved at compile-time. In the remainder of this section,
not updated to correspond with upstream HAC peers, but rather             we discuss the design of the interconnection network of the TSP
continues to freely count local clock cycles. The delta between a         multiprocessor system that enables software-scheduled network,
TSP’s SAC and HAC, thus represents the accumulated drift of the           including:
“local” view of time (i.e. SAC) since the last synchronization and the      (A) Deterministic traffic pattern from static graph analysis
“global” view of time (i.e., HAC).                                          (B) Scheduling of communication, not routing
    To reconcile the local vs global time, a RUNTIME_DESKEW instruc-        (C) Deterministic routing, including non-minimal routes
tion is provided in the ISA. The instruction takes a single parameter:      (D) No hardware arbitration and explicit (software) flow control
a target number of clock cycles to stall. When executed, the TSP will       (E) Forward-error-correction (FEC) to avoid reactive link-layer
delay for the target number of cycles plus or minus 𝛿𝑡 , the signed             replay
difference of the HAC and SAC. In the event that 𝛿𝑡 is positive, the
“local” time is faster than the “global” time represented by the HAC,     4.1    Traffic pattern known a-priori
and the TSP will stall for the target number of cycles plus 𝛿𝑡 , and
                                                                          To enable SSN, the communication or the network traffic pattern
vice-versa if the difference is negative. In this manner the “local”
                                                                          needs to be known. For most of the workloads that multi-TSP
time is re-aligned with the “global” time, the multi-TSP system
                                                                          targets, the communication pattern, both in space and in time, are
is re-synchronized, and the accumulated global error is reduced
                                                                          known a-priori to the communication itself during the compile
to the link jitter. This RUNTIME_DESKEW instruction is scheduled
                                                                          time. As a result, the optimal routing or “scheduling” decision can
to be executed on each TSP within the network at the same time.
                                                                          be made based on the the traffic pattern. In particular, SSN takes
While it is executing, the other functional units on each TSP are
                                                                          advantage of a ML model’s static computation graph and a priori
quiesced by performing a SYNC instruction to park each ICUs, until
                                                                          knowledge of the traffic pattern to enable an alternative scheme
it is subsequently awakened via a NOTIFY instruction.
                                                                          for routing tensors as a collection of back-to-back vectors which are
                                                                          the flow control units (flits) within the network. This provides fine-
4    SOFTWARE-SCHEDULED NETWORKING                                        grained (320 byte) communications between TSPs by extending
     (SSN)                                                                this single-chip determinism to the entire network of TSPs and lays
A foundational characteristic of the TSP architecture is its deter-       the foundation for scheduling tensors precisely to the clock cycle
ministic data paths. Execution latency of all instructions is known       across the network links.
statically (at compile time) and therefore exposed to the compiler           The traffic pattern is an emergent property of the underlying
via the ISA (instruction set architecture). As described earlier in the   ML model, and how the model is partitioned across processing
previous section, the hardware extends the guarantee of determin-         elements allowing for both model parallelism (i.e., distributing dif-
istic execution from a single TSP to a multi-TSP system. Achieving        ferent layers across TSPs) and data parallelism to exploit mini-batch
this determinism across the full network allows the compiler to not       parallelism across the cluster. Model decomposition is automated
only have cycle-accurate knowledge of all data movement within a          by the compiler to auto-scale the workload across a desired number
single TSP, but also across the links connecting the network of TSP       of TSP elements. The compiler partitions the workload into smaller
processing elements. The exact timing (in cycles) to inject a vector      sub-tasks and maps them to individual TSPs responsible for execut-
at the source TSP, as well as the exact cycles data will arrive at a      ing them. This mapping process induces the traffic pattern for the
destination TSP, can all be resolved at compile time. We refer to this    parallel workload. For pipelined model parallelism [30][19] [31] we
networking paradigm as software-scheduled networking (SSN) since          compute the precise execution time of each pipe stage’s sub-task
it replaces the notion of dynamically routing packets as they flow        and exchange activations between the layers of each stage. This
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                    ISCA ’22, June 18–22, 2022, New York, NY, USA


                                                                                         The scheduling of communication enables an alternative com-
                                                                                      munication model, compared to conventional networks. Conven-
                                                                                      tional networks provide a simple abstraction for communication
                                                                                      between a pair of processors, A and B, in the system (e.g., Infini-
                                                                                      band’s queue-pair abstraction). As an example of the transactional
                                                                                      nature of the network, consider a simple remote transaction where
                                                                                      processor A sends a message like “read the value of address X from
                                                                                      processor B and reply to processor A.” Upon receipt, processor B,
                                                                                      issues a DRAM read and sends the reply back to processor A in-
                                                                                      curring one round-trip network latency (Fig 9(a)). With a software-
                                                                                      scheduled networking, we only incur half of the network requests
                                                                                      since we know when to send the reply(X) message to the expectant
                                                                                      processor A, eliminating the “request” leg of the protocol traffic
                                                                                      (Fig 9(b)). In this model, data is “pushed” toward the TSP that will be
Figure 9: Communication model for (a) a conventional network
                                                                                      consuming it, and from a programming model perspective, where
using remote DMA and (b) the TSP communication model which
eliminates the request leg of the transaction.
                                                                                      the tensor comes from (local versus remote memory) is irrelevant.


                                                                                      4.3    Deterministic Load-balancing
process is fundamentally different than the conventional approach
since we know the exact execution time of each stage (to the clock                    The multi-TSP system exploits the path diversity of the Dragonfly
cycle) and therefore do not require dynamic profiling to extract the                  by deterministically spreading the offered traffic across the available
run-time characteristics of the sub-tasks. This makes the parallel de-                links. In the same way that more conventional networks spread
composition step precise and explicitly under control of the compiler.                packets within a message across the available up links of a fat-tree
This flexibility, in turn, allows the compiler’s scheduling algorithm                 [38] network, we are spreading the 320-byte vectors of a larger
to explicitly make computation and communication trade-offs to                        tensor across the C2C links in the path between the source and
change the ratio of compute to communication time and control                         destination TSP. The abundant path diversity of the Dragonfly hi-
the surface-to-volume characteristics of the parallel workload. The                   erarchical topology is unlocked using “non-minimal” routing to
compiler will overlap as much compute and communication to                            spread the offered traffic across multiple injection links in each TSP.
effectively hide the C2C link latency.                                                This “load balances” the global network links based on the offered
                                                                                      load (tensor size) and precise scheduling of the individual vectors
                                                                                      (flits) to enable deterministic load-balancing. While providing
4.2     Scheduled, Not Routed                                                         deterministic load-balancing, this also avoids any end-to-end re-
The routing algorithm determines the path a message takes through                     ordering of hardware-based adaptive routing that strives for the
the network and is commonly implemented in hardware using                             same goal.
lookup-tables to provide a simple output-port mapping for each                            One key difference in the network topology, compared with
incoming packet by inspecting the packet’s destination node. In                       hardware-based topologies, is that the “routers” are effectively “end-
comparison, the multi-TSP system explicitly controls and sched-                       points” that are injecting traffic (Fig 4(c)). This is a critical difference
ules the hop-by-hop path through the network by orchestrating a                       since the amount of bandwidth injected is no longer limited by the
sequence of send and receive instructions on the source and des-                      injection bandwidth but by the switch router bandwidth. It is also
tination nodes, respectively. Given that all data movement can be                     critical as it requires non-minimal routing to be bandwidth-aware
statically inferred, the compiler orchestrates data movement based                    non-minimal routing. In a hardware-defined Dragonfly, if there is
on global information across time and space to eliminate conflicts                    no congestion (or contention) for the minimal path, all packets are
for shared output ports.                                                              routed minimally. When network congestion is sensed, routing may
   To highlight the advantages of SSN, an example is shown in                         avoid minimal paths and route non-minimally to use an otherwise
Fig 8 with four TSPs. Assume both TSPs A and B sends traffic to                       under-utilized path with the goal of reducing latency and improving
D, creating contention for the shared link from B to D. TSP A has                     overall throughput. As a result, in a hardware-defined Dragonfly, if
two minimal routes to TSP D — routing through TSP B or routing                        only a single source and a single destination are communicating,
through TSP C. Without global information on B’s downstream                           non-minimal is not necessary since an injection channel cannot
congestion, TSP A’s may route through TSP B. This routing can re-                     oversubscribe a single network channel. However, in SSN, non-
sult in congestion at TSP B and create back-pressure towards TSP A                    minimal routing can be exploited to provide higher amount of
(2). Once the back-pressure is sensed, TSP A can route through TSP                    bandwidth between the source and the destination TSP.
C (3). This reactivity resulting from arbitration and backpressure                        Software-scheduled routing still entails a “decision” to determine
not only adds complexity to the hardware but also adds latency                        whether packets should be routed minimally or non-minimally. In
and non-determinism. In comparison, SSN moves this decision-                          prior works utilizing hardware-based routing, the decisions are
making from the hardware into the compiler where it can schedule                      made dynamically on a per-packet basis using local congestion
data movement across the network to avoid contention and enable                       information (e.g. FIFO depth, or transmit credits) available in the
deterministic communication.                                                          router. Making the routing decision at compile-time like all other
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                                        Abts and Kimmell, et al.


                                                                                tail flit (8B)   body flit (8B)   body flit (8B)        header flit (8B)
                                                                                                                                   DATA[4:0]
                                                                            DATA[319:317]
                                                                                                                                   Type (data, CSR, Sync, Ctrl)
                                                                            IDLE, IDLE, IDLE     DATA[20:13]       DATA[12:5]
                                                                                                                                   Sync
                                                                            End
                                                                                                                                   Start



                                                                          Figure 11: Packet format of a 320-byte vector has an encoding effi-
                                                                          ciency of 97.5% (320/328 bytes).



                                                                          group topology, we triple-connect physical links within the torus to
                                                                          increase the nearest-neighbor throughput. Our software-scheduled
Figure 10: Performance benefit of non-minimal routing as the mes-         approach avoids the complexity and cost of additional virtual (or
sage size increases and the number of non-minimal path increases.         physical) channels to avoid deadlock.
                                                                              One benefit of scheduling and the lack of hardware flow control
functional units on the TSP allows the compiler’s scheduling algo-        is that data no longer needs to be encapsulated into packets and
rithm to schedule the network links optimally. We use the tensor’s        messages in order to traverse the network; instead, all information
physical data volume (i.e., the product of a tensor’s dimensions          about data movement has been encoded in the instruction streams
H×W×C) as the data volume being communicated, and based on                produced by the compiler with only 2.5% encoding overhead (Fig
the tensor size we select the number of links to spread the traffic       11) of each vector.
across. Fig 10 provides an analysis on the benefit of non-minimal
routing as the message size varies and the number of non-minimal          4.5        Forward error correction (FEC)
paths within a TSP chassis is modified. The analysis assume 8-node        Point-to-point networks like PCIe typically employ a link-layer
TSP that are fully connected and thus, one minimal path and seven         retry (e.g. a sliding-window retransmission protocol) to replay pack-
non-minimal paths for each source-destination pair. The analysis          ets at the link-layer so that errors are not observed by the network
provides an optimal distribution of minimally- and non-minimally          or application layers. Unfortunately, this link-level retry mecha-
routed messages such that overall latency for communication is            nism also introduces non-deterministic behavior since it changes
minimized. The analysis shows that for a message size smaller             the expected arrival time of the retransmitted packets and thus
than 8kB, there is no benefit of non-minimal routing. However, for        interferes with the global synchronization of the system. Instead,
larger message sizes, the benefit of non-minimal routing gradually        to maintain determinism in the face of transmission errors, we
increases and the benefit of more bandwidth (or more non-minimal          use forward error correction (FEC) on every link to correct sim-
paths) provide higher benefit for larger message size. The actual         ple transmission errors and detect uncorrectable burst errors. This
crossover point on the benefit of non-minimal routing is a function       keeps the collection of point-to-point physical links deterministic
of the message size, the number of non-minimal paths, and per-hop         between any source-destination TSP pair in the system. We
latency (not shown).                                                      route packets hop by hop through the network, correcting any
                                                                          transmission errors in situ, and flag any critical errors that require
4.4     Flow Control                                                      the runtime system to “replay” the inference (i.e. a software replay)
To enable SSN, the interconnection disallows dynamic arbitration          to determine if the fault is transient and disappears after replaying
since it would result in non-determinism and make it impossible           the inference, or persists after a retry and requires physical inter-
for the compiler to explicitly schedule and pace every link. There        vention (e.g., to replace a marginal cable, power supply unit, or TSP
is also no conventional “queuing” or buffers that are commonly            card) to remedy the fault.
found in datacenter networks, aside from very shallow buffers at             The scale of a parallel computer — the maximum number of
the interface between the C2C logic and TSP core clock bound-             processing elements in the system — is in a very practical sense
aries. In addition, virtual channels (VCs) [10] are commonly used to      limited by the reliability of the system. The TSP processing elements
avoid routing deadlock with non-minimal global adaptive routing           use a deterministic datapath and error correction of all single-bit
and guarantee circular dependencies do not occur. With software-          errors (SBEs) which are corrected in situ by the TSP hardware, and
scheduled routing, circular dependencies between packets can still        detect all multi-bit errors (MBEs) so that the runtime software can
occur; however, routing deadlock is fundamentally caused when             replay the inference on a set of known good hardware if and when
packets hold on to a resource (e.g., buffer or VC) while requesting an-   a critical error is identified.
other resource (e.g., downstream buffer). With software-scheduled            The system reliability strategy uses N+1 redundancy by pro-
networking, packets or messages are scheduled in advance; thus,           visioning a hot spare node (Fig 6) in every deployed rack. The
the packets do not hold on to the resource while requesting an-           Dragonfly topology is both edge and node symmetric so the net-
other resource and routing deadlock cannot occur – and VCs are            work remains fully-connected (ie. there is a path between every
not needed. Similarly, toroidal deadlock scenarios arise in torus         source-destination pair in the system) and is key to making this N+1
networks due to overlapping VC dependencies around the torus              resiliency practicable. This strategy allows the runtime to monitor
links (Fig 6b). The local group radix-8 torus topology enables ef-        system health and replace any unusable nodes with the spare node
ficient nearest-neighbor communication with adjacent TSPs for             as the runtime layer marshals resources for invoking the parallel
inference using pipelined model parallelism. With a radix-8 local         program’s execution. This overhead can be reduced by provisioning
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                 ISCA ’22, June 18–22, 2022, New York, NY, USA




                                                                                      Figure 13: Matrix multiplication on an Nvidia A100 and the
                                                                                      TSP for various Matrix sizes 𝑁 on a single chip.
Figure 12: The abstraction layers of the hardware-software inter-
face and software stack.



a redundant node per system such that, for example, a 33 node sys-
tem with four racks would have 1 of 33 nodes as the spare (reducing
the overhead from 11% to 3%, leaving 32 nodes (256 TSPs) for exe-
cuting the parallel program. By provisioning a hot-spare node and
providing a software-replay mechanism we can gracefully recover                       Figure 14: Performance of a matrix-matrix computation of
from a critical fault by having the runtime replay the inference. We                  size [800×32576]×[32576×8192] (left) latency vs number of
protect against these critical errors with FEC on the network links                   TSPs, and (right) throughput and utilization vs number of
and single-error correction and double-error detection (SECDED)                       TSPs.
extensively throughout the TSP’s memory system, data paths, and
instruction buffers.                                                                  5.2    Distributed matrix multiplication
                                                                                      Matrix operations like vector-matrix and matrix-matrix multipli-
5     EVALUATION AND DISCUSSION                                                       cation are workhorses of ML models. To map matrix workloads
In this section we evaluate the latency and bandwidth character-                      (i.e. [M×N] × [N×L]) onto multiple TSPs, we take two approaches:
istics of several workloads from both HPC and machine learn-                          column-wise weight splits where the second matrix ([N×L]) is split
ing including: distributed matrix multiplication, BERT-Large, All-                    equally column-wise across multiple TSPs and the final results are
Reduce collective communications, and Cholesky factorization. The                     then concatenated together. Alternatively, row-wise weight splits
Cholesky workload in particular is difficult to efficiently parallelize               where the second matrix is split equally ([N×L] row-wise) across
due to a loop-carried dependence of a vector-matrix multiplication                    multiple TSPs and the first matrix ([M×N]) is split column-wise;
on the inner-loop. The matrix-matrix, vector-matrix, and matrix                       the final result is the reduction of all the partial product matrices
transpose operations are representative of and commonly used by                       produced by each TSP. For single chip, the compiler decomposes
many machine learning models, like sequence-to-sequence models                        a matrix multiply into [1× K]×[K × 320] sub-operations, where
(e.g. LSTMs) and transformers.                                                        K=[160,320] i.e. the vector lengths of the hardware for FP16 and
                                                                                      int8 respectively. Additionally, a TSP can run two FP16 or four
                                                                                      int8 sub-operations each cycle. Results are shown in Fig 13 and
5.1     Software stack                                                                compares the achievable utilization of the TSP and Nvidia’s A100
Fig 12 illustrates the different layers of the software stack with                    when computing the matrix operation [2304×4096]×[4096×N], for
two primary design entry points either as PyTorch or TensorFlow                       N=[1376..3500] as described in [33]. As Fig 13 highlights, we are
inputs, or a custom application on top of a bare-metal programming                    able to achieve at least 80% utilization consistently at different
interface. Both bare-metal API and the compiler share the same                        matrix sizes on the TSP, which contrasts with conventional archi-
assembler and runtime stack where the scheduled program is passed                     tectures such as GPUs. Using a combination of column-wise and
to the assembler to generate a machine-code binary that is then                       row-wise weight splits, we can further decompose large matrices
run on the TSP. When targeting multiple TSPs, the input model                         and run them on multiple TSPs to minimize the overall latency of
is either automatically partitioned by the compiler or manually                       the operation.
by the programmer, and an individual TSP binary is compiled and                           To demonstrate our matrix decomposition approach, we decom-
prepared for each TSP in the system. The runtime system then                          pose the [800×32576]×[32576×8192] operation amongst several
emplaces all program collateral on the TSPs and synchronizes all                      TSPs using column-wise and row-wise weight splits. We first di-
programs (as described earlier in Section 3) so that we launch the                    vide the operation into eight (8) sub-operations using column-wise
inference simultaneously across all cooperating TSPs.                                 splits (i.e. eight [800×32576] × [32576×(8192/8)] operations). We
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                           Abts and Kimmell, et al.


                                                                           The input bandwidth requirements vary drastically depending on
                                                                        the data ordering of the computation. Decomposing a matrix-matrix
                                                                        operation on a single TSP involves decomposing the operations
                                                                        into a sequence of [1×K] × [K×320] operations, where K=160 for
                                                                        FP16 or K=320 for int8 data values. Depending on the sequence
                                                                        of K×320 tiles we load into the matrix multiply unit on the TSP,
                                                                        the bandwidth requirements can change drastically. For example,
                                                                        for a [100000×100000] secondary weight matrix, if K×320 tiles are
                                                                        loaded in column-major order (i.e. load rows 0-159, followed by
                                                                        rows 160-320, and so on) this requires approximately 570 GB/s of in-
                                                                        coming bandwidth to the chip in order to maintain the computation
                                                                        throughput. However, traversing the secondary weight matrix in
Figure 15: Throughput measured in FP16 TFlops for a                     row-major order reduces demand on incoming bandwidth to only
matrix-matrix multiplication of size N, for cluster size of             3.7 GB/s which is well within the channel capacity of a 16-lane PCIe
100, 200, and 300 TSPs.                                                 Gen4 link. In our results shown in Fig 15, we are assuming input
                                                                        matrices are streamed over PCIe in the order (row, or column-order)
                                                                        that minimizes the injected data volume transferred across PCIe.
further decompose the operations onto more TSPs by dividing into
even smaller using row-wise splits of the form [800×(32576/N)]          5.3    All-Reduce Bandwidth
× [(32576/N)×(8192/8)] where N=[1..13], where we try to cluster         Collective operations are used to provide global communica-
row-wise splits in a single node to leverage the Dragonfly topology.    tion among cooperating processing elements. These performance-
A reduction is applied within a node on all the partial results (with   critical operations often bottleneck the overall system performance
8 TSPs) to create one [800×(8192/8)] result per node. Finally, if       because they are constrained by the slowest link — the network
needed, the result on each node is reduced and transferred with one     link having highest channel load — making it critical to “load bal-
of its neighboring nodes over C2C until eight 800×(8192/8) results      ance” the physical links of the network to avoid variation in the
are produced. These are concatenated together to form the final         communication latency among the communicating TSPs. Fig 16
[800×8192] result.                                                      shows the effective (realized) bandwidth performing an 8-way All-
   The results of distributed matrix multiplication are shown in        Reduce operation across different tensor sizes, with a zoomed-in
Fig 14 where we assume a Dragonfly topology described previously,       region showing the performance for different message sizes [27].
with each TSP operating at 900MHz with PCIe Gen4 ×16 host CPU           The combination of synchronous communication over a direct net-
interface. As Fig 14 shows, latency reduces as we perform more          work with very low overhead (Fig 11) allows the All-Reduce to
row-wise splits and add more TSPs since adding more TSPs adds           quickly saturate the available network capacity.
both compute processing elements and communication (C2C) links              A GPU or CPU system with shared-memory semantics will com-
allowing the system performance to grow proportionally as we add        municate results via shared DRAM, and requires a flag (mutex)
more TSPs.                                                              to indicate when the data is produced (ie. globally visible) and
   Column-wise splits are used for large matrix operations on a         can safely be consumed. After writing the data, but before writing
cluster of TSPs to avoid any large reductions of partial products,      the flag, a memory fence is required to ensure sequential consis-
and reduce IO bandwidth requirements that are required in row-          tency between the producer and the consumer. This “lock-based”
wise splits (Fig 15 i.e. [N×N] × [N×N] is decomposed to X [N×N]         shared memory mailbox is flexible [27], but it requires an additional
× [N×(N/X)] operations run on X TSPs ). When compared to [17]           semaphore for coordination (the mutex, or flag variable) which is
which uses a cluster of Nvidia V100s, we can achieve over 100×          unnecessary on the Groq system since the compiler tracks the to-
more FP16 throughput compared to the peak performance on 432            tal ordering of memory references and global time of reference,
GPUs achieving approximately 2800 (fp64) TFlops on matrix sizes         it does not require the added flag to signal the consumer. Instead,
of 650000×650000. Unfortunately, V100 GPU cluster results using
FP16 data are not publicly known, however, despite differences in
precision the TSP speedup is significant.
                                                                                            zoomed in, linear scale
   The results in Fig 14 highlight how using column-wise and row-
wise weight splits complement each other and can yield extremely
low-latency matrix-matrix operations. The split scheme produces
good results for the matrix dimensions we used. However, depend-
ing on the ML model or workload, these dimensions can vary dras-
tically - yielding different split schemes. For example, in scenarios
where the resultant matrix is relatively small, row-wise splits out-
perform column-wise splits, since communication bandwidth needs
are limited. Additionally, with the Dragonfly topology, reductions
within a node are extremely efficient since each TSP within the
node has direct access to the other 7 TSPs within that node.              Figure 16: Realized throughput of an 8-way AllReduce.
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                  ISCA ’22, June 18–22, 2022, New York, NY, USA




                                                                                      Figure 18: BERT encoders executed on 1, 4, 8, and 16 TSPs,
                                                                                      and normalized to the realized throughput (TOPs) on a sin-
Figure 17: BERT-Large latency histogram across 24,240 runs.                           gle TSP.


the consumer will respect the data dependence and only be issued                      model size from 6, to 24, 48, and 96 encoders and run it on 1, 4, 8 and
after the data is updated, ensuring a sequentially consistent view of                 16 TSPs respectively. Due to linear scaling, we are able to scale the
global memory. We see the effect of this shared-memory overhead                       realized throughput proportionally to the number of devices. For
in Fig 16 (zoomed in) as the Groq system outperforms competitive                      example by increasing the compute capacity to 4 TSPs, we are able
systems on fine-grained communication 5 , despite the A100 having                     to realize 4× more throughput (realized TeraOps) when compared
more pin bandwidth. The normalized results for A100 shows the                         to the execution of BERT on a single TSP.
performance of A100 if the total pin bandwidth was normalized to
the pin bandwidth of a TSP. When normalized, the performance (or                      5.5    Cholesky factorization on TSPs
throughput) of TSP AllReduce matches A100 at large tensor size                        The matrix multiplication unit on the TSP consumes two matrices
while significantly outperforming A100 at smaller tensor size.                        𝐴 and 𝐵, containing floating-point values with dimensions at most
                                                                                      160×320, and generates the output 𝐴𝐵𝑇 . The inputs are read from
5.4     Transformers on TSPs                                                          stream registers flowing outward, away from the vector processing
In order to illustrate the predictable and deterministic nature of our                unit in the center of the chip, and the output is written to stream
TSP at scale, we execute a single inference of BERT-Large running                     registers flowing inward, toward the vector processing unit. After
on four TSPs within a GroqNode 24,240 times, using SQuAD1.1 dev                       generating an update vector 𝑆 with the matrix multiplication unit,
Dataset, and measure the latency of the inference which includes                      each iteration of Cholesky modifies it with these vector operations:
the input read and write time over PCIe. Once measured, we bin
each time into 5𝜇s bins which is plotted in Fig 17, which shows the
zoomed in histogram plot. The results show that 99% of inferences
return in under 1225 𝜇s, with all of them returning by 1300𝜇s. The
dotted line at 100% highlights the estimated latency returned by
our compiler, and shows that it is within 2% of the actual measured
latency in the majority of cases. The deviation and variance between
estimated and measured is due to the extended invocation time
of the PCIe data transfer for the input and output transfers when
running BERT-Large on 4 TSPs. When executing BERT-Base on a
single TSP, we see a similar relationship between the estimated and
measured latency, where their results are within 2% of each other.
   As we add processing elements (TSPs) to the system, we are
simultaneously adding both compute resources (vector and matrix
ALUs) as well as communication links to the (direct) interconnec-
tion network. In addition the the predictable nature of the TSP at
scale, Groq TSPs are able to achieve linear scaling as you add more
TSPs to the system. This is illustrated in Fig 18 which shows the
realized TOPs when executing transformer models, normalized to
the execution time on a single TSP. In this run, we scale a BERT
5 Results for A100 were measured on an 8 A100 GPU system with 300 GB/s of NVlink
bandwidth per GPU connected through NVSwitch. The measurements are from
nccl-tests [34] and results of bus bw is shown.                                         Figure 19: Cholesky factorization across multiple TSPs.
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                                Abts and Kimmell, et al.


def c h o l e s k y _ v e c t o r _ o p s ( S , U , i ) :
  ( n , m) = np . s h a p e ( S )

    I = S[ i :n , [ i ]] − U[0: n − i , [0]]
    splat = rsqrt ( I [0][0])
    updates = I [ 0 : n − i , [ 0 ] ] ∗ s p l a t
    return ( u p d a t e s )
   Here, 𝑆 is the input matrix, 𝑖 is the index of the current iteration,
and 𝑟𝑠𝑞𝑟𝑡 is a custom approximation of the reciprocal square root
function. All of these operations are mapped onto the ALUs in
the vector processing unit in order to modify the data in a single
fly-by. We implement Cholesky by channeling the data through the                                            (a)
matrix and vector units alternately. The matrix is partitioned across
multiple TSPs as shown in Fig 19(a) and (b), we use a block-cyclic
distribution of 320 rows on each TSP. The overall parallel execution
                                                            𝑝3
time for Cholesky factorization is proportional to 3 for a 𝑝 × 𝑝
input matrix, that when executed on multiple TSPs yields a net
speedup of 1.2×, 1.4×, and 1.5× for 2, 4, and 8 TSPs, respectively as
shown in Fig 19(c). We show good scaling from 14.9 FP16 TFlops on
4 TSPs to 22.4 FP16 TFlops on 8 TSPs, with 3× realized throughput
compared to the best known results [16].

5.6     Traffic patterns
The TSPs participating in model parallel workload will exchange                                             (b)
global results using an all-reduce across the system to distribute         Figure 20: Performance breakdown of BERT-Large on a 4-
the result. This “collective” operation decomposes to many fine-           TSP system with (a) an un-optimized and (b) an optimized
grained vector reductions that are accumulated and then broadcast          compiler.
across all participants. We use non-minimal routing and per-device
compute capacity to efficiently distribute and accumulate partial
reductions bidirectionally. The worst-case latency in a 256-TSP
Dragonfly uses only three (3) hops and thus an all-reduce across all       uses proprietary interconnect to scale out TPUs using a 2D torus
TSPs has a pipelined network latency of 722 ns per hop ×3 hops =           topology. Habana Gaudi [29] training chips uses commodity eth-
2,166 ns, or ≈2.1 𝜇sec. A three-stage, hierarchical all-reduce uses        ernet to provide full connectivity within a server while enabling
the 8-way fully connected TSPs within each node as the first stage,        scale-out through ethernet as well. Nvidia DGX Pod [32] scales
the four global links between nodes for the second stage, and final        out using NVlink within a DGX system while Infiniband is used to
8-way fully connected network within each node to complete the             scale-out the systems. Tesla [22] outlined a high-radix topology to
hierarchical all-reduce.                                                   scale-out their training system. However, all of these systems are
   In Fig 20, we show the performance breakdown of a 4-TSP sys-            fundamentally based on hardware-defined networks; in compari-
tem running BERT-Large, including the communication time (C2C)             son, this work addresses how software-scheduled network can be
and compute time. To illustrate the importance of load-balancing           leveraged to enable deterministic communication.
the functional units, we show results for both (a) our initial (un-        High-Radix Networks: Dragonfly topology [25] has been pro-
optimized) compiler implementation which partitioned the work-             posed for scalable large-scale systems in high-performance comput-
load by balancing only FLOPs (floating point operations), whereas          ing. Different variations of Dragonfly have been proposed including
(b) the optimized compiler carefully considers data movements to           Dragonfly+ [40] and MegaFly [14]. Cray/HPE have implemented
exploit the spatial organization of the TSP. The optimized imple-          a Dragonfly topology with different group topology in their XC
mentation results in approximately 26% improvement in realized             systems [4] and their Slingshot network [37] while providing all-
throughput.                                                                to-all connectivity between the groups. To exploit the path diver-
                                                                           sity of the Dragonfly topology, different global adaptive routing
6     RELATED WORK                                                         algorithms [20, 41] have been proposed to enable higher perfor-
Scale-out Machine Learning Systems: State of the art NLP work-             mance across different traffic patterns. However, all prior work
loads have billions of model parameters requiring both memory              have focused on hardware-based Dragonfly network; in compari-
capacity and bandwidth to move those parameters into the PEs               son, this work proposes a software-scheduled Dragonfly network
where they are computed on. The software-defined Dragonfly net-            that presents different opportunities as well as challenges. Spread-
work allows the TSPs in the system to communicate synchronously            ing the offered traffic across available physical links to increase the
with no wasted execution incurred for barriers. Different scale-           throughput and reduce the observed message latency is the goal
out systems have been proposed, including Google TPU [42] that             of adaptive routing [15][20][2]; however, prior adaptive routing
A Software-defined Tensor Streaming Multiprocessor for Large-scale Machine Learning                                          ISCA ’22, June 18–22, 2022, New York, NY, USA


was done in hardware while this work proposes a software sched-                            York, NY, USA, Article 5, 11 pages. https://doi.org/10.1145/1362622.1362630
uled routing. To minimize performance variations in a multi-hop                        [3] Jung Ho Ahn, Nathan Binkert, Al Davis, Moray McLaren, and Robert S. Schreiber.
                                                                                           2009. HyperX: Topology, Routing, and Packaging of Efficient Large-Scale Net-
network, globally fair arbitration have been proposed. Age-based                           works. In Conference on High Performance Computing Networking, Storage and
arbitration [2] has been implemented to enable global fairness and                         Analysis (SC). 1–11.
                                                                                       [4] Bob Alverson, Edwin Froese, Larry Kaplan, and Duncan Roweth. 2012. The Cray
techniques such as equality-of-service [28] has been proposed to ap-                       XC Scaleable System. In Cray Inc. White Paper.
proximate global fairness. However, while globally fair arbitration                    [5] Tom Bergan, Owen Anderson, Joseph Devietti, Luis Ceze, and Dan Grossman.
can improve fairness, it does not provide determinism.                                     2010. CoreDet: A Compiler and Runtime System for Deterministic Multithreaded
                                                                                           Execution. In Proceedings of the Fifteenth International Conference on Architectural
Deterministic Execution: Many hardware/software techniques                                 Support for Programming Languages and Operating Systems (Pittsburgh, Pennsyl-
have been proposed to enable determinism in general-purpose                                vania, USA) (ASPLOS XV). Association for Computing Machinery, New York, NY,
multi-core processors [5, 13, 18, 35]. Recently, similar determin-                         USA, 53–64. https://doi.org/10.1145/1736020.1736029
                                                                                       [6] Tom B Brown, Benjamin Mann, Nick Ryder, Melanie Subbiah, Jared Kaplan,
istic execution have been proposed for GPUs [8, 21] as well as                             Prafulla Dhariwal, Arvind Neelakantan, Pranav Shyam, Girish Sastry, Amanda
data-parallel environments [12] and high-performance computing                             Askell, et al. 2020. Language models are few-shot learners. arXiv preprint
                                                                                           arXiv:2005.14165 (2020).
 [9] to enable reproducible execution. While prior work have pro-                      [7] Cerebras CS-1. 2021. http://cerebras.net. http://cerebras.net (2021).
posed deterministic execution across different processor architec-                     [8] Yuan Hsi Chou, Christopher Ng, Shaylin Cattell, Jeremy Intan, Matthew D. Sin-
tures, with support from both software and hardware, to the best                           clair, Joseph Devietti, Timothy G. Rogers, and Tor M. Aamodt. 2020. Deterministic
                                                                                           Atomic Buffering. In 2020 53rd Annual IEEE/ACM International Symposium on
of our knowledge, no prior work have explored how determinis-                              Microarchitecture (MICRO). 981–995. https://doi.org/10.1109/MICRO50266.2020.
tic execution can be achieved across multiple nodes of a scale-out                         00083
system.                                                                                [9] Caroline Collange, David Defour, Stef Graillat, and Roman Iakymchuk. 2014. A
                                                                                           Reproducible Accurate Summation Algorithm for High-Performance Computing.
                                                                                           In Technical Report HAL-00949355, INRIA,.
7    CONCLUSION                                                                       [10] William J. Dally. 1992. Virtual-Channel Flow Control. IEEE Transactions on
                                                                                           Parallel and Distributed Systems 3, 2 (1992), 194–205.
We describe the system architecture of a novel, purpose-built com-                    [11] W. J. Dally and B. Towles. 2004. Principles and Practices of Interconnection Networks.
mercial system for scalable ML and converged HPC applications.                             Morgan Kaufmann, San Francisco, CA.
                                                                                      [12] David Defour and Sylvain Collange. 2015. Reproducible floating-point atomic
The novel source-based, software-scheduled routing algorithm al-                           addition in data-parallel environment.. In FedCSIS (Annals of Computer Science
lows the automatic parallelizing compiler to load balance the global                       and Information Systems, Vol. 5). IEEE, 721–728. http://dblp.uni-trier.de/db/conf/
links of the Dragonfly network. This deterministic load balancing                          fedcsis/fedcsis2015.html#DefourC15
                                                                                      [13] Joseph Devietti, Brandon Lucia, Luis Ceze, and Mark Oskin. 2009. DMP: Deter-
allows the compiler to schedule the physical network channels                              ministic Shared Memory Multiprocessing. In Proceedings of the 14th International
by spreading a large tensor across multiple non-minimal paths                              Conference on Architectural Support for Programming Languages and Operat-
to maximize throughput, or use minimal routing to accomplish a                             ing Systems (Washington, DC, USA) (ASPLOS XIV). Association for Computing
                                                                                           Machinery, New York, NY, USA, 85–96. https://doi.org/10.1145/1508244.1508255
barrier-free all-reduce with minimal end to end latency. We demon-                    [14] Mario Flajslik, Eric Borch, and Mike A. Parker. 2018. Megafly: a topology for
strate and discuss system performance on representative workloads                          exascale systems. In High Performance Computing, Rio Yokota, Michèle Weiland,
                                                                                           David Keyes, and Carsten Trinitis (Eds.). Springer International Publishing, Cham,
like distributed matrix multiplication, All-Reduce, BERT-Large, and                        289–310.
Cholesky factorization. We extend the Groq’s TSP stream program-                      [15] Christopher J. Glass and Lionel M. Ni. 1992. The turn model for adaptive routing.
ming model from a single-chip to large scale system-wide deter-                            In Proceedings of the International Symposium on Computer Architecture.
                                                                                      [16] Azzam Haidar, Ahmad Abdelfatah, Stanimire Tomov, and Jack Dongarra. 2017.
minism using a combination of hardware-alignment counters and                              High-performance Cholesky factorization for GPU-only execution. In Proceedings
ISA support to facilitate runtime deskew operations to provide the                         of the General Purpose GPUs. ACM New York, NY, USA, 42–52.
illusion of a globally synchronous distributed system.                                [17] Thomas Herault, Yves Robert, George Bosilca, and Jack Dongarra. 2019. Generic
                                                                                           Matrix Multiplication for Multi-GPU Accelerated Distributed-Memory Platforms
                                                                                           over PaRSEC. In 2019 IEEE/ACM 10th Workshop on Latest Advances in Scal-
ACKNOWLEDGEMENTS                                                                           able Algorithms for Large-Scale Systems (ScalA). 33–41. https://doi.org/10.1109/
                                                                                           ScalA49573.2019.00010
With any new endeavor where the starting point is simply an idea, a                   [18] Derek R. Hower, Polina Dudnik, Mark D. Hill, and David A. Wood. 2011.
lot of people and effort goes into synthesizing that idea and bringing                     Calvin: Deterministic or not? Free will to choose. In 2011 IEEE 17th Interna-
                                                                                           tional Symposium on High Performance Computer Architecture. 333–334. https:
it to fruition. We would like to thank the anonymous reviewers for                         //doi.org/10.1109/HPCA.2011.5749741
their helpful comments. We would like to thank Greg Thorson for                       [19] Yanping Huang, Youlong Cheng, Ankur Bapna, Orhan Firat, Dehao Chen, Mia
early contributions to this work. Special thanks to Mike Cherba                            Chen, HyoukJoong Lee, Jiquan Ngiam, Quoc V Le, Yonghui Wu, et al. 2019. Gpipe:
                                                                                           Efficient training of giant neural networks using pipeline parallelism. Advances
and Sarah Massengill for detailed comments on early drafts of the                          in neural information processing systems 32 (2019), 103–112.
manuscript.                                                                           [20] Nan Jiang, John Kim, and William J. Dally. 2009. Indirect Adaptive Routing on
                                                                                           Large Scale Interconnection Networks. In Proceedings of ISCA’09. Austin, TX,
                                                                                           220–231.
REFERENCES                                                                            [21] Hadi Jooybar, Wilson W.L. Fung, Mike O’Connor, Joseph Devietti, and Tor M.
 [1] Dennis Abts, Jonathan Ross, Jonathan Sparling, Mark Wong-VanHaren, Max                Aamodt. 2013. GPUDet: A Deterministic GPU Architecture. In Proceedings of
     Baker, Tom Hawkins, Andrew Bell, John Thompson, Temesghen Kahsai, Garrin              the Eighteenth International Conference on Architectural Support for Program-
     Kimmell, Jennifer Hwang, Rebekah Leslie-Hurd, Michael Bye, E.R. Creswick,             ming Languages and Operating Systems (Houston, Texas, USA) (ASPLOS ’13).
     Matthew Boyd, Mahitha Venigalla, Evan Laforge, Jon Purdy, Purushotham Ka-             Association for Computing Machinery, New York, NY, USA, 1–12. https:
     math, Dinesh Maheshwari, Michael Beidler, Geert Rosseel, Omar Ahmad, Gleb             //doi.org/10.1145/2451116.2451118
     Gagarin, Richard Czekalski, Ashay Rane, Sahil Parmar, Jeff Werner, Jim Sproch,   [22] Andrej Karpathy. 2021. Keynote at Workshop on Autonomous Driving.
     Adrian Macias, and Brian Kurtz. 2020. Think Fast: A Tensor Streaming Pro-        [23] Parviz Kermani and Leonard Kleinrock. 1979. Virtual cut-through: A new com-
     cessor (TSP) for Accelerating Deep Learning Workloads. In 2020 ACM/IEEE               puter communication switching technique. Computer Networks (1976) 3, 4 (1979),
     47th Annual International Symposium on Computer Architecture (ISCA). 145–158.         267–286.
     https://doi.org/10.1109/ISCA45697.2020.00023                                     [24] John Kim, James Balfour, and William Dally. 2007. Flattened butterfly topology
 [2] Dennis Abts and Deborah Weisser. 2007. Age-Based Packet Arbitration in Large-         for on-chip networks. In Proceedings of the 40th Annual IEEE/ACM International
     Radix k-ary n-cubes. In Proceedings of the 2007 ACM/IEEE Conference on Super-         Symposium on Microarchitecture. IEEE Computer Society, 172–182.
     computing (Reno, Nevada) (SC ’07). Association for Computing Machinery, New
ISCA ’22, June 18–22, 2022, New York, NY, USA                                                                                                         Abts and Kimmell, et al.


[25] John Kim, William J Dally, Steve Scott, and Dennis Abts. 2008. Technology-driven,   [34] NVIDIA. 2021. NCCL Tests. https://github.com/NVIDIA/nccl-tests/
     highly-scalable dragonfly topology. In ACM SIGARCH Computer Architecture            [35] Marek Olszewski, Jason Ansel, and Saman Amarasinghe. 2009. Kendo: Efficient
     News, Vol. 36:3. IEEE Computer Society, 77–88.                                           Deterministic Multithreading in Software. In Proceedings of the 14th International
[26] John Kim, William J. Dally, Brian Towles, and Amit K. Gupta. 2005. Microar-              Conference on Architectural Support for Programming Languages and Operating
     chitecture of a High-Radix Router. In ISCA ’05: Proceedings of the 32nd Annual           Systems (Washington, DC, USA) (ASPLOS XIV). Association for Computing Ma-
     International Symposium on Computer Architecture. IEEE Computer Society, Madi-           chinery, New York, NY, USA, 97–108. https://doi.org/10.1145/1508244.1508256
     son, WI, USA, 420–431.                                                              [36] Raghu Prabhakar, Yaqi Zhang, David Koeplinger, Matt Feldman, Tian Zhao,
[27] Benjamin Klenk, Nan Jiang, Greg Thorson, and Larry Dennison. 2020. An In-                Stefan Hadjis, Ardavan Pedram, Christos Kozyrakis, and Kunle Olukotun. 2017.
     Network Architecture for Accelerating Shared-Memory Multiprocessor Col-                  Plasticine: A Reconfigurable Architecture For Parallel Paterns. In International
     lectives. In 2020 ACM/IEEE 47th Annual International Symposium on Computer               Symposium on Computer Architecture (ISCA). 389–402.
     Architecture (ISCA). 996–1009. https://doi.org/10.1109/ISCA45697.2020.00085         [37] Steve Scott. 2019. Rosetta: A 64-port Switch for Cray’s Slingshot Interconnect.
[28] Michael M. Lee, John Kim, Dennis Abts, Michael Marty, and Jae W. Lee. 2010. Prob-        Keynote 2, HOTI.
     abilistic Distance-Based Arbitration: Providing Equality of Service for Many-Core   [38] Steve Scott, Dennis Abts, John Kim, and William J. Dally. 2006. The BlackWidow
     CMPs. In Proceedings of the 2010 43rd Annual IEEE/ACM International Sympo-               High-Radix Clos Network. In Proceedings of the 33rd Annual International Sympo-
     sium on Microarchitecture (MICRO ’43). IEEE Computer Society, USA, 509–519.              sium on Computer Architecture (ISCA ’06). IEEE Computer Society, USA, 16–28.
     https://doi.org/10.1109/MICRO.2010.18                                                    https://doi.org/10.1109/ISCA.2006.40
[29] Eitan Medina. 2019. Habana Labs Approach to Scaling AI Training. In Hot Chips       [39] Mohammad Shoeybi, Mostofa Patwary, Raul Puri, Patrick LeGresley, Jared Casper,
     31 Symposium, Palo Alto, CA, USA.                                                        and Bryan Catanzaro. 2019. Megatron-lm: Training multi-billion parameter
[30] Deepak Narayanan, Aaron Harlap, Amar Phanishayee, Vivek Seshadri, Nikhil R               language models using model parallelism. arXiv preprint arXiv:1909.08053 (2019).
     Devanur, Gregory R Ganger, Phillip B Gibbons, and Matei Zaharia. 2019.              [40] A. Shpiner, Z. Haramaty, S. Eliad, V. Zdornov, B. Gafni, and E. Zahavi. 2017.
     PipeDream: generalized pipeline parallelism for DNN training. In Proceedings of          Dragonfly+: low cost topology for scaling datacenters. In 2017 IEEE 3rd Interna-
     the 27th ACM Symposium on Operating Systems Principles. 1–15.                            tional Workshop on High-Performance Interconnection Networks in the Exascale
[31] Deepak Narayanan, Mohammad Shoeybi, Jared Casper, Patrick LeGres-                        and Big-Data Era (HiPINEB). 1–8. https://doi.org/10.1109/HiPINEB.2017.11
     ley, Mostofa Patwary, Vijay Anand Korthikanti, Dmitri Vainbrand, Prethvi            [41] Jongmin Won, Gwangsun Kim, John Kim, Ted Jiang, Mike Parker, and Steve Scott.
     Kashinkunti, Julie Bernauer, Bryan Catanzaro, et al. 2021. Efficient large-scale         2015. Overcoming Far-end Congestion in Large-Scale Networks. In IEEE 21st
     language model training on gpu clusters. arXiv preprint arXiv:2104.04473 (2021).         International Symposium on High Performance Computer Architecture (HPCA).
[32] NVIDIA. 2011. NVidia DGX POD. https://www.nvidia.com/en-us/data-center/             [42] Cliff Young. 2017. Evaluation of the Tensor Processing Unit: A Deep Neural
     dgx-pod-reference-architecture/                                                          Network Accelerator for the Datacenter. In Hot Chips 29 Symposium, Palo Alto,
[33] Nvidia. 2021. Matrix Multiplication Background, User Guide. Technical Report.            CA, USA.
     NVIDIA.
