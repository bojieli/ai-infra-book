    DistRS: Disaggregated Reward Service for RLVR
              with Batch-Level Constraint
   Ruidong Zhu, School of Computer Science, Peking University; Mingcong Han,
  ByteDance Seed; Yinmin Zhong, School of Computer Science, Peking University;
Wencong Xiao, ByteDance Seed; Xuanzhe Liu and Xin Jin, School of Computer Science,
                               Peking University
            https://www.usenix.org/conference/nsdi26/presentation/zhu-ruidong




  This paper is included in the Proceedings of the 23rd USENIX Symposium
            on Networked Systems Design and Implementation.
                         May 4–6, 2026 • Renton, WA, USA
                                 ISBN 978-1-939133-54-0


             Open access to the Proceedings of the 23rd USENIX Symposium
           on Networked Systems Design and Implementation is sponsored by
    DistRS: Disaggregated Reward Service for RLVR with Batch-Level Constraint
        Ruidong Zhu1 Mingcong Han2 Yinmin Zhong1 Wencong Xiao2 Xuanzhe Liu1 Xin Jin1
                  1 School of Computer Science, Peking University 2 ByteDance Seed

                         Abstract
                                                                                             Verifiable
   Reinforcement Learning with Verifiable Rewards (RLVR)               Rollout                Reward                   Train
has emerged as a key post-training paradigm for enhancing                                (math, code, game…)
the capabilities of large language models (LLMs). As the com-
plexity increases and resource consumption grows, reward
computation is becoming a critical workload in the RLVR                            Figure 1: Workflow of RLVR.
training process.
   We present DistRS, a disaggregated reward service frame-        verifiable reward computation in RLVR can be considerably
work designed to provide resource-efficient reward computa-        more complex and resource-intensive. For example, when
tion for RLVR training. Through the analysis of a real RLVR        training models to generate CUDA code [8], reward computa-
training task, we observe that the reward service faces a highly   tion requires compiling the code on CPUs and executing the
dynamic workload, motivating the need for elasticity and           code on GPUs to verify correctness if compilation succeeds
multi-tenancy. DistRS leverages request-level flexibility from     (Figure 4(a)). In production training tasks, this process can
the request-in, batch-out characteristic of reward computation     use thousands of CPUs and hundreds of GPUs, with each
to design more resource-efficient scaling and scheduling poli-     request taking up to several minutes to complete. For more
cies. Specifically, DistRS establishes a batch-level constraint    CPU-intensive workloads, reward computation can simultane-
for each training task that relaxes latency requirements at the    ously occupy hundreds of thousands of CPUs during training,
request level. Building on this foundation, we design a history-   making its overall cost comparable to that of training itself.
based resource scaling policy and a batch-level priority-based        As reward computation grows increasingly intensive, its
request scheduling policy. In addition, DistRS incorporates        underlying paradigm is undergoing rapid evolution. Early
a timeout-aware mechanism to adjust resource allocation,           frameworks for RLVR [9, 10] adopted an approach similar
thereby mitigating the impact of deviations between history        to that of RLHF, performing reward computation collectively
and actual execution. We evaluate DistRS with real-world           after rollout (Figure 2(b)). However, the substantial complex-
RLVR training tasks and the results demonstrate that DistRS        ity of reward computation introduces a range of issues, in-
reduces resource consumption by up to 3.79× while incurring        cluding mismatches of resource types, resource contention,
minimal overhead on training progress.                             and limited computational concurrency. Consequently, recent
                                                                   works [11–13], as well as our internal practice, have decou-
1   Introduction                                                   pled reward computation from the RL training framework and
Reinforcement learning (RL) has become an indispensable            deployed it as an independent service. The disaggregated re-
step in the post-training process of large language models         ward service further enables request-level reward computation.
(LLMs). Early applications of RL to LLMs primarily focused         As shown in Figure 2(c), once an input sequence completes
on aligning models with human preferences by introducing           its rollout, the corresponding reward computation request can
a reward model to score outputs, a paradigm known as Re-           be sent to the service and processed in real time.
inforcement Learning from Human Feedback (RLHF) [1, 2].               In this paper, we aim at developing a disaggregated reward
More recent studies [3–7] demonstrate that in domains where        service that minimizes resource consumption while intro-
correctness can be automatically verified—such as mathe-           ducing negligible disruption to training tasks. However, our
matics and code—rewards can be directly computed through           analysis of the reward service workload in a real RL train-
rule-based methods, substantially improving both reasoning         ing task (§3) reveals that achieving this objective remains
ability and task performance. This paradigm is referred to as      highly challenging. We observe that the reward service faces
Reinforcement Learning with Verifiable Rewards (RLVR).             a highly dynamic workload. In terms of arrival patterns, re-
   The key distinction between RLVR and RLHF lies in the           ward computation requests are generated only after an output
reward computation process. As shown in Figure 1, the work-        sequence is fully generated. Consequently, no reward request
flow of RLVR largely mirrors that of RLHF, with each it-           arises in the early rollout steps or during training phases when
eration consisting of a rollout phase, reward computation,         collocated training architecture (§2.1) is employed. Further-
and a training phase. In RLHF, rewards are computed via a          more, the arrival pattern exhibits a long-tail characteristic, as
forward pass of the reward model, and this computation is          a small fraction of input sequences generate relatively long
typically executed in batches on the same resources used for       outputs [14–16], resulting in reward computation requests
rollout and training, as illustrated in Figure 2(a). However,      arriving later. In terms of resource demands, they vary signif-



USENIX Association                         23rd USENIX Symposium on Networked Systems Design and Implementation                1517
                                                               Rollout                 Reward                                flexibility




         (a) Batched and colocated       (b) Colocated reward computation       (c) Standalone reward service.   (d) DistRS. (The critical
        reward computation in RLHF.                  in RLVR.                                                      request is textured.)

                                      Figure 2: Comparison of four types of reward computation.

icantly as training progresses, owing to the evolving output                dynamically adjusts resource allocation based on predefined
behavior of the LLM during the process. Different outputs                   timeout thresholds, thereby ensuring efficient resource utiliza-
may require vastly different amounts of resources and compu-                tion while preserving training progress.
tation time for reward evaluation. The dynamic nature of the                    We then extend the algorithm to the multi-tenant setting,
workload renders static resource allocation inefficient, result-            where scaling decisions are still made at batch boundaries.
ing in either wasted resources or increased training delays.                To further improve the effectiveness of resource sharing, we
   The highly dynamic workload motivates us to adopt an                     incorporate the batch-level constraint into the request schedul-
elastic and multi-tenant service. However, the objective of                 ing policy. In particular, we assign priorities to batches based
traditional elastic multi-tenant services is to satisfy the ser-            on the estimated completion time of the batch for each task,
vice level objective (SLO) of individual requests [17, 18].                 and schedule request execution according to these priorities.
Our further analysis shows that, due to the unique request-in,                  We implement DistRS and integrate it with an internal RL
batch-out nature of reward service, the objective for reward                training framework. Experimental results on real-world train-
service can be relaxed to a batch-level constraint. Conse-                  ing tasks demonstrate that DistRS reduces GPU consumption
quently, compared to traditional elastic multi-tenant services,             in the reward service by up to 3.79× and CPU consumption
we can exploit greater flexibility. The request-in property orig-           by up to 2.16×, while imposing minimal impact on training.
inates from the autoregressive generation of LLMs, where                        In summary, our contributions are as follows:
requests are generated and dispatched to the reward service in              • We present a systematic workload analysis of reward com-
a sequential manner. The batch-out property arises because                     putation in RLVR, revealing its highly dynamic nature and
the training phase cannot advance until the reward service                     the unique request-in, batch-out characteristic.
has returned responses for all requests within the batch. As                • We propose DistRS, a disaggregated reward service frame-
illustrated in Figure 2(d), the critical request is defined as                 work that leverages request-level flexibility and batch-level
the last request in a batch to complete if no queuing occurs.                  constraints to design resource scaling and request schedul-
To minimize the impact on training, only the critical request                  ing policies that minimize resource consumption while pre-
must be executed immediately. In contrast, the other requests                  serving training progress.
can be deferred and scheduled flexibly at any point between                 • We conduct experiments on real RL training tasks and the
their arrival and the completion of the critical request.                      results demonstrate up to 3.79× reduction in resource con-
   To this end, we propose DistRS, a disaggregated reward                      sumption with minimal impact on training.
service framework aiming at both resource efficiency and
minimal impact on training. In addition to elasticity and multi-            2      Background and Motivation
tenancy, we build a batch-level constraint that relaxes the                 In this section, we first introduce the process and system
latency requirement at the request level, and take this as the              architecture of RLVR in §2.1, followed by a discussion of the
foundation for designing our scaling and scheduling policies.               emerging paradigm of reward-as-a-service §2.2. Finally, we
   We propose a history-based resource scaling algorithm for                present our motivation and objectives in §2.3.
individual tasks that operates at the batch granularity. By lever-
aging the history of previous batches, the algorithm predicts               2.1     Reinforcement Learning with Verifiable Rewards
the arrival times and execution times of requests in the upcom-             RLHF heavily relies on a reward model to evaluate LLM out-
ing batch, and accordingly determines the minimum resources                 puts. However, the reward model’s judgments are not always
required to complete all requests under the batch-level con-                reliable, as it can be susceptible to reward hacking [19, 20].
straint. The main challenge lies in handling requests whose                 To address this limitation, recent works [3, 5, 6] propose Re-
execution times are significantly underestimated, which may                 inforcement Learning with Verifiable Rewards (RLVR) for
lead to incorrect queuing decisions and extra delays. To mit-               LLM training. In domains such as math, code, and knowledge-
igate this issue, we design a timeout-aware mechanism that                  based question answering, simple rule-based methods can



1518    23rd USENIX Symposium on Networked Systems Design and Implementation                                              USENIX Association
                   Rollout      Train     Rollout       Train                 Code
                                                                                          Compile           Execute
                      (a) Colocated architecture.

                   Rollout    Rollout     Rollout      Rollout
                                                                                         (a) CUDA reward computation.
                               Train       Train        Train
                                                                                          Compile
        (b) Disaggregated architecture with one-step staleness.
                                                                              Code
Figure 3: Two architectures of LLM RL training frameworks.                                                   Execute
                                                                                            Fetch
provide reward scores that accurately reflect the correctness                             test case
of LLM outputs. RLVR has been widely applied in these areas
to enhance LLMs’ reasoning ability, tool-use capabilities, and
task-specific performance.
                                                                                           (b) OJ reward computation.
   Figure 1 illustrates a training iteration in RLVR. First, given
a batch of inputs, the model performs autoregressive token                  Figure 4: Examples of verifiable reward computation.
generation, a phase commonly referred to as the rollout in RL
algorithms. After the rollout, relevant portions of the model’s             The main difference between RLVR and RLHF is the re-
outputs are extracted for verifiable reward computation. For             ward computation. In RLVR, reward computation may involve
instance, for a math problem, the model first performs rea-              a workflow, with each request potentially requiring different
soning and thinking, and then outputs an answer; the reward              inputs and traversing multiple stages. Furthermore, it con-
is computed by comparing the generated answer with the                   sumes a wide variety of resources [8, 25, 26], including CPU,
reference solution. Through format matching and answer ver-              GPU, network, and storage, which may differ from those pri-
ification, a final score is assigned to the output. This score           marily used in RL training. We illustrate the process of verifi-
is then transformed on the training side and used to guide               able rewards with two concrete examples. Figure 4(a) shows
model parameter updates via algorithms such as PPO and                   the reward computation for RL tasks targeting CUDA code
GRPO [6, 21–23].                                                         generation [8]: once an input sequence completes its roll-
   In terms of architecture for LLM RL training frameworks,              out, the generated CUDA code is extracted via simple string
there are two main paradigms: colocated and disaggregated.               matching, compiled on the CPU, and, if compilation succeeds,
We present them in Figure 3, where the weight update and                 executed on the GPU. Figure 4(b) depicts the reward com-
context switching are omitted for simplicity. In the colocated           putation for RL training on online judge (OJ) programming
architecture (Figure 3(a)), the rollout and training phases share        problems [13], which require code to be automatically tested
the same set of resources and execute alternately. In contrast,          against predefined inputs and outputs [27, 28]. The generated
the disaggregated architecture (Figure 3(b)) allocates sepa-             code is first extracted. If compilation is required (e.g., C++),
rate resources for rollout and training, with the two phases             the code is compiled, whereas for interpreted languages (e.g.,
communicating over the network for data transfer and pa-                 Python), this step is omitted. The system then retrieves the
rameter synchronization. To optimize resource utilization in             corresponding test cases from remote storage, executes the
disaggregated setups, techniques such as one-step staleness              code, and verifies its correctness. Colocating reward compu-
and streaming [15, 24] have been proposed to enable pipeline             tation with rollout and training can cause resource contention
execution of rollout and training.                                       and limit concurrency, making the reward computation phase
                                                                         a bottleneck.
2.2   Reward-as-a-Service                                                   To support resource-intensive reward computations and im-
Since reward computation in RLHF is essentially a forward                prove their scalability, recent works and our internal practice
pass of a model, its computational load remains relatively               have started deploying reward computation as a standalone
fixed across different training tasks, and it primarily relies           service, i.e., reward-as-a-service. We collectively refer to the
on GPUs, similar to other training phases. Consequently, re-             rollout and training phases and their resources as the trainer,
ward computation in RLHF often shares resources with other               while a disaggregated reward service receives reward compu-
phases, utilizing GPUs freed after the rollout phase to perform          tation requests from the trainer and returns the results. Decou-
computations collectively. RLHFuse [14] fuses the rollout                pled from the trainer, the reward service can leverage a ded-
and reward computation phases, but they still share the same             icated resource pool and conveniently provide request-level
set of resources. When RL frameworks such as verl [9] and                reward computation, as depicted in Figure 2(c). As model
OpenRLHF [10] began supporting RLVR, they adopted a                      capabilities continue to expand and application domains diver-
similar approach to reward computation, placing it after the             sify, reward computation will become increasingly complex.
entire rollout phase completes and performing it in batches,             Consequently, we expect disaggregated reward-as-a-service
as illustrated in Figure 2(b).                                           to become the mainstream approach for RLVR.



USENIX Association                                  23rd USENIX Symposium on Networked Systems Design and Implementation           1519
                 200                                                             80                                                                  300
                                                                                                                                                                Iter 0        Iter 2          Iter 4
                 160                                                                                                                                            Iter 1        Iter 3          Iter 5




                                                                 Request Count
 Request Count




                                                                                 60
                                                                                                                                                     200




                                                                                                                                          Time (s)
                 120
                                                                                 40
                  80
                                                                                                                                                     100
                                                                                 20
                  40

                   0                                                              0                                                                    0
                       0   1000   2000      3000   4000   5000                        0   100   200   300   400   500   600   700   800                    0      500       1000       1500        2000
                                  Time Bin (10s)                                                       Time Bin (5s)                                                       Request

(a) Arrival rate during 50 iterations with a bin of 10s. (b) Arrival rate during 5 iterations with a bin of 5s.                                        (c) Request arrival time for 6 iterations.

                                                     Figure 5: Arrival pattern of reward service during training.

2.3                Motivation and Objectives                                                                  3.1       Workload Characteristics
As reward computation becomes more complex, the resource                                                      Characteristics of arrival pattern. First, we analyze the
demands of the reward service also grow. For instance, in                                                     arrival pattern of reward computation requests during train-
our company’s RL tasks that train models to generate CUDA                                                     ing. Figure 5(a) shows the overall arrival rate of requests
kernels, each reward computation requires tens of seconds for                                                 throughout the training process, using a bin size of 10 sec-
CPU compilation and several additional seconds for GPU ex-                                                    onds. The arrival rate exhibits substantial variation, peaking at
ecution. Furthermore, to evaluate each kernel accurately and                                                  over 180 requests per 10 seconds while also dropping to zero.
fairly, it must exclusively occupy a GPU during execution. As                                                 Figure 5(b) zooms in on five iterations with finer temporal
a result, the resource demand for reward service is approach-                                                 granularity. Because this task adopts a colocated architec-
ing that of the trainer. It is foreseeable that training LLMs                                                 ture, no reward computation requests are generated during
to handle more complex tasks—such as building distributed                                                     the training phase. Furthermore, at the beginning of each
systems or generating project-level code—will require even                                                    rollout, reward requests are sparse or absent. As a result, as
greater resources and longer computation times. Fundamen-                                                     shown in Figure 5(b), roughly half of the time within each
tally, under the RLVR paradigm, a task can only be effectively                                                iteration sees no reward requests arriving. Once the rollout
trained if it can be evaluated efficiently and accurately, with                                               progresses, the number of requests rapidly rises to a peak
timely reward feedback [29].                                                                                  and then gradually decays, forming a long tail. Figure 5(c)
   As reward computation grows into a critical component of                                                   normalizes each iteration by aligning the first reward request
the overall training workload, attention must be directed not                                                 to the same start time and plotting subsequent request arrivals
only to the optimization of the trainer but also to the resource                                              in order. It demonstrates the similarity in the arrival patterns
management of the reward service. In this work, our objective                                                 of reward requests across adjacent iterations, as their curves
is to design a disaggregated reward service that (i) imposes                                                  overlap closely. The reason for this similarity is that the dis-
minimal disruption to training progress and (ii) consumes as                                                  tribution of output lengths during the rollout phase evolves
few resources as possible.                                                                                    relatively slowly across adjacent iterations [6, 30]. The figure
   To achieve this goal, we first conduct an in-depth analy-                                                  also reveals the long-tail property of the rollout phase, as the
sis of the reward computation workload generated by a real                                                    arrival interval of the final requests, i.e., the slope of the curve,
RL task in §3. It shows that the reward service faces highly                                                  increases significantly.
dynamic workloads. The cyclic execution of training phases
leads to dynamic variations in the request arrival pattern, while                                             Characteristics of resource consumption. Next, we char-
the evolving output behavior of the model during training                                                     acterize the resource demand of reward computation during
further causes the resource requirements for reward compu-                                                    training. We measure the total CPU node time and GPU node
tation to change accordingly. Consequently, static resource                                                   time (defined as the number of CPUs/GPUs multiplied by
management either slows down training significantly due to                                                    their running time) consumed per iteration. Figure 6(a) re-
insufficient allocation or causes severe resource waste due to                                                ports the results of each iteration, where CPU node time cor-
overallocation.                                                                                               responds to compilation and GPU node time to execution. As
                                                                                                              training progresses, both CPU and GPU node times per itera-
                                                                                                              tion exhibit an increasing trend. To explain this phenomenon,
3                 Workload Analysis and Insight
                                                                                                              we further analyze the distribution of requests across different
In this section, we analyze the workload of reward computa-                                                   states in each iteration, as shown in Figure 6(b). The request
tion in a real RL training task for CUDA code generation and                                                  states are categorized as follows: Generation failed: the model
present our key insights. The task adopts a colocated archi-                                                  fails to produce CUDA code that passes simple text-based
tecture [9], runs for 50 iterations with a batch size of 2048                                                 checks; Compile failed: the generated code cannot be com-
per iteration, and is trained using GRPO [22]. The maximum                                                    piled; Execute error: the code compiles successfully but fails
rollout generation length is set to 16K tokens.                                                               during execution or produces incorrect results; Success: the



1520                   23rd USENIX Symposium on Networked Systems Design and Implementation                                                                                  USENIX Association
                                                                                                                                     Gen Failed                        Execute Error
                                      Compilation Time               Execution Time                                                  Compile Failed                    Success
 Compilation Time (×104 s)




                             12                                                        8                                 1500                                                                                                  Compilation Time          Execution Time




                                                                                       Execution Time (×103 s)
                                                                                                                                                                                                                   120                                                    60




                                                                                                                                                                                            Compilation Time (s)




                                                                                                                                                                                                                                                                              Execution Time (s)
                             10
                                                                                       6                                 1000                                                                                       90
                                                                                                                                                                                                                                                                          40




                                                                                                                 Count
                              8
                                                                                                                                                                                                                    60
                                                                                       4                                  500                                                                                                                                             20
                              6                                                                                                                                                                                     30

                              4                                                        2                                    0                                                                                        0                                                    0
                                  0      10       20            30       40       50                                            0     10       20            30             40         50                                0         400    800     1200    1600     2000
                                                         Iter                                                                                         Iter                                                                                   Request

(a) Total compilation and execution time of each iter-                                                                      (b) State distribution of each iteration.                       (c) Compilation and execution time of each request
ation.                                                                                                                                                                                      in an iteration.
                                                                       Figure 6: Resource consumption of reward service during training.

code compiles and executes successfully, and produces cor-                                                                                                             2400
rect outputs. As the LLM gradually learns to generate valid                                                                                                            2000
CUDA code during training, the likelihood that the gener-                                                                                                              1600




                                                                                                                                                             Request
ated code can be compiled and executed increases. Therefore,                                                                                                           1200
the proportion of requests in the Generation failed and Com-                                                                                                            800
pile failed categories steadily decreases. Meanwhile, Execute                                                                                                           400
error requests first increase and then decline, and Success
                                                                                                                                                                           0
requests grow significantly. Therefore, the observed increase                                                                                                                    0                                           100                  200
in resource consumption in Figure 6(a) stems from the LLM’s                                                                                                                                                                        Time (s)
evolving output behavior during training. Although we fo-
                                                                                                                                                             Figure 7: Request-in, batch-out nature of reward service.
cus here on the CUDA code generation task, we argue that
this workload variation is representative of RLVR in general.                                                                                           service not only incurs significant resource idleness when
Many reward computations involve multiple stages, where                                                                                                 no requests are present, but also fails to adapt to changing
later stages are only reached if earlier ones succeed, and LLMs                                                                                         demands, leading to either wasted resources or stalled training
typically learn to progress through these stages gradually.                                                                                             progress. To address this, we envision an elastic, multi-tenant
   We also analyze the compilation and execution times of                                                                                               reward service. Elasticity allows the service to dynamically
individual requests within a batch, as shown in Figure 6(c).                                                                                            adjust resource allocation in response to workload fluctua-
Since some requests fail to generate code or pass compila-                                                                                              tions, while multi-tenancy further promotes resource sharing
tion, their compilation or execution times are recorded as zero.                                                                                        and reuse. Despite the promise of these mechanisms, a key
For requests that successfully enter compilation or execution,                                                                                          challenge remains: determining what kind of policy should
most exhibit relatively consistent runtimes, although a small                                                                                           govern their application to effectively achieve our goals.
fraction take significantly longer to complete. In fact, many of
these long-running requests terminate only upon reaching the                                                                                            3.2            Key Insight
timeout thresholds set during training. This occurs because the
LLM may output arbitrary content—for example, code con-                                                                                                 A deeper analysis of the interaction between the reward ser-
taining infinite loops—that fails to terminate during execution.                                                                                        vice and the trainer yields our key insight: the reward service
Unlike data analytics and similar workloads, where execution                                                                                            interacts with the trainer in a request-in, batch-out manner.
time can be accurately estimated by profiling [31, 32], in our                                                                                          Figure 7 illustrates this property. Each request is represented
setting, it is infeasible to reliably predict request runtime prior                                                                                     by a horizontal blue line, where the left endpoint marks its
to execution. However, the reward service cannot allow un-                                                                                              arrival time at the service. Requests arrive at different times,
bounded execution. It terminates the reward computation if                                                                                              reflecting the request-in aspect, since during rollout the LLM
the running time exceeds a timeout threshold and assigns the                                                                                            generates outputs autoregressively, leading to staggered com-
request a negative reward. In the CUDA reward service we                                                                                                pletion times for each input. For each request, we measure
studied, normal requests typically require around 50 seconds                                                                                            its compilation time and execution time to obtain its com-
for compilation and a few seconds for execution, while the                                                                                              pletion time under the assumption of no queuing (i.e., the
timeout thresholds are set to 120 seconds for compilation and                                                                                           earliest possible completion time). This time is indicated by
60 seconds for execution.                                                                                                                               the right endpoint of the corresponding line. We then take
                                                                                                                                                        the maximum of these earliest completion times across all
Summary and outlook. The above analysis demonstrates                                                                                                    requests, which represents the earliest possible completion
that during RLVR training, both the workload arrival patterns                                                                                           time for the entire batch, shown as the red vertical line. The
and the resource demands of the reward service vary dynam-                                                                                              batch-out aspect arises because the trainer consumes reward
ically over time. Therefore, a statically configured reward                                                                                             results in batches. Consequently, the trainer can only proceed



USENIX Association                                                                               23rd USENIX Symposium on Networked Systems Design and Implementation                                                                                                     1521
                    Data plane              Control plane             microservice manner. It hosts workers that execute the differ-
                       Proxy                                          ent stages of reward computation, and workers of different
                                          Resource Pool
                                                                      stages may use different types of resources. Each worker can
                                                                      process one request at a time. We denote the worker executing
                                            Worker 1
       RL                                                             the i-th stage as worker i. In the scenario of RL training for
    Training
                        ……
                                            Worker 2
                                                                      CUDA code generation, reward computation involves two
     Tasks         Priority Queue                                     stages: compiling the generated code and executing the com-
                   Batch-Level              Worker 1                  piled code. Therefore, the resource pool contains two types
                  Scaling Planner                                     of workers: worker 1 with CPUs for compilation and worker
                                            Worker 2                  2 with GPUs for execution. This architecture can be easily
                    Task Profiles
                                                                      extended to support more complex reward computations in-
                                                                      volving additional stages and resource types. The resource
            Figure 8: System overview of DistRS.                      pool supports dynamic scaling of workers for each stage.
to the training phase once the reward service has finished            Proxy. The proxy serves as the entry point for training tasks
computing rewards for the entire batch, i.e., after the red line.     to access the reward service. The proxy maintains a request
   The key insight from the request-in, batch-out character-          queue for each stage. Requests from the training tasks or the
istic is that each batch has a critical request—the request           previous stage are stored in the queue, waiting for the cor-
that finishes last under the assumption of no queuing. The            responding stage workers to fetch and process them. Each
completion time of this request determines the completion             request in the queue is assigned a batch-level priority, reflect-
time of the entire batch. To avoid delaying training progress,        ing the urgency determined by the estimated completion time
the critical request must be completed without queuing. In            of the batch it belongs to. Requests with the same priority,
contrast, other requests only need to finish before the criti-        i.e., from the same batch, are processed in a first-come, first-
cal request; as long as this condition is met, they impose no         served (FCFS) manner. In addition, the proxy incorporates
additional impact on training progress. Even though prior             a batch-level resource scaling planner, which periodically
work such as StreamRL [15] enables streaming data trans-              makes scaling decisions based on the historical request pro-
fer between inference and training to reduce synchronization          files it maintains. Specifically, when a new batch arrives or an
granularity, training still requires at least one mini-batch to       existing batch completes, it estimates each batch’s completion
proceed. Moreover, reinforcement learning algorithms such             time as well as the expected arrival and running times of fu-
as GRPO [22] require all samples within a group to complete           ture requests, and then makes scaling decisions with the goal
reward computation before training can begin. Consequently,           of minimizing resource consumption while preserving the
the request-in, batch-out characteristic fundamentally persists.      training process. Moreover, the trainer can provide additional
This insight provides greater flexibility in scheduling request       hints: the start of a new rollout batch indicates that a new
execution (Figure 2(d)) and motivates the design of more              batch of reward computation requests will arrive shortly. This
resource-efficient scaling and scheduling policies that operate       enables us to proactively trigger resource scaling decisions
under batch-level constraints.                                        and warm up new workers during the interval between the on-
                                                                      set of rollout and the arrival of the first reward request, thereby
4    Overview                                                         effectively hiding the overhead of resource scheduling and
Inspired by the above insight, we propose DistRS, a disag-            cold starts.
gregated reward service framework with elasticity and multi-          5     Policy Design
tenancy to provide efficient reward computation for RLVR
workloads. Any RL training task requiring the reward com-             In this section, we present the design of DistRS, with a focus
putation can connect to DistRS as a client. DistRS aims to            on the policies for resource scaling and request scheduling.
minimize resource consumption while ensuring the progress             We begin by modeling the scaling objective in the single-task
of all training tasks. It fully leverages the request-in, batch-out   setting and introducing our scaling policy in Section 5.1. We
characteristic of reward computation during training to define        then extend the design to the multi-task setting and describe
the Service Level Objective (SLO) at the batch level, which           the request scheduling strategies required to enable efficient
relaxes the latency requirement at the request level. Based on        multi-tenancy in Section 5.2.
the batch-level constraint, DistRS designs efficient resource         5.1   Scaling Policy for Single Task
scaling and request scheduling policies. As illustrated in Fig-
ure 8, DistRS mainly consists of two modules: the proxy and           Objective and constraint formulation. We begin with the
the resource pool.                                                    simpler case in which only a single training task utilizes the
                                                                      reward service. We model the resource scaling problem at
Elastic resource pool. The resource pool provides the com-            the granularity of batches, meaning that resource allocation
putational resources required for reward computation in a             decisions are made only when the first requests of a new batch



1522    23rd USENIX Symposium on Networked Systems Design and Implementation                                     USENIX Association
    Symbol       Description                                               Algorithm 1 Simulation-Based Searching for Scaling
    B            Batch size                                                  1: Input: Information of the previous batch, i.e.,
    M            Number of stages in reward computation                      2:          R = {ri } and S = {si j }, where 0 ≤ i < B and 0 ≤ j < M.
    Rt           Total resource consumption of trainer                       3:          Cost of each worker at each stage C1 ,C2 , . . . ,CM
    Cj           Cost of a worker at stage j                                 4:          the maximum tolerable extra delay D
    ri           Rollout completion time of request i                        5: Output: The number of workers at each stage N1 , N2 , . . . , NM
    si j         Running time of reward request i at stage j                 6: Initialize output list Num[M] = [B] ∗ M
    ti           Earliest possible completion time of request i              7: sort stage j by C j in descending order
    T            Earliest possible completion time of the batch              8: for each stage j in the sorted order do
                                                                                                 high
    d            Extra delay                                                 9:     N low
                                                                                      j   = 1, N j      =B
    Nj           Number of workers at stage j                               10:     while N low
                                                                                                    high
                                                                                                < N j do
                                                                                            j
    Rr           Total resource consumption of reward service                                             high
                                                                            11:        Num[ j] = (N low
                                                                                                     j + Nj    )/2
Table 1: Boldface variables denote inputs with known val-                   12:         sat = SIMULATE(Num, R, S, D)
                                                                            13:         if sat == True then
ues; underlined variables denote inputs that are unpredictable                                 high
                                                                            14:              N j = Num[ j]
in practice; all other variables represent either outputs deter-
                                                                            15:         else
mined by the policy or variables affected by these outputs.                 16:              N low  = Num[ j] + 1
                                                                                               j
arrive. Once decided, the allocated resources remain fixed un-              17:     Num[ j] = N low
                                                                                                j
til all requests in the batch complete, after which the resources
                                                                            18: return Num
are released. Table 1 summarizes the key notations used in
this formulation. Our scaling decision determines the num-
ber of workers N j allocated to each stage. Given the rollout               In practical RL training, the rollout phase typically lasts sev-
completion time ri for each request and the execution time si j             eral hundred seconds, placing T on the order of hundreds of
of each stage in the corresponding reward computation, the                  seconds or more. Consequently, the additional delay d per-
earliest possible completion time of request i is                           mitted by the constraint amounts to at most a few seconds. In
                                                                            summary, we formulate an objective that minimizes resource
                              ti = ri + ∑ si j .                    (1)
                                                                            consumption under a batch-level delay constraint, which nat-
                                            j
                                                                            urally aligns with the request-in, batch-out characteristic of
Therefore, the earliest completion time of the entire batch is              reward computation.
                                 T = max ti .                       (2)    Solving the problem. Ideally, we would express d as a func-
                                        i
                                                                           tion of the number of workers at each stage, N j , to directly
We denote the actual completion time of the entire batch
                                                                           solve the optimization problem. Unfortunately, while d de-
as T + d (d ≥ 0), where d represents the additional delay
                                                                           pends on the rollout completion time ri of each request and
introduced by queuing in reward computation beyond the
                                                                           the execution time si j at each stage—and is monotonically
theoretical earliest completion time T . For each batch, the
                                                                           decreasing with respect to N j —a general closed-form expres-
objective function can be formulated as:
                                                                           sion is not available. Moreover, for a future batch, ri and si j
             min (Rt + Rr) · (T + d),               s.t. d ≪ T.     (3)    cannot be predicted in advance. However, we observe that
           N1 ,...,NM
                                                                           the arrival patterns of requests in adjacent batches are often
This objective function aims to minimize the total resources               similar (Figure 5(c)). Based on this observation, we adopt
consumed by the trainer and the reward service during rollout              a simulation-based sequential binary search algorithm that
and reward computation, while ensuring that the impact of                  leverages historical information to determine the number of
queuing in reward computation is negligible. Here, Rt denotes              workers at each stage.
the trainer-side configuration, and T depends solely on the                   Algorithm 1 presents the pseudocode of our scaling deci-
characteristics of rollout (ri ) and reward computation requests           sion procedure. It takes as input the historical request infor-
(si j ), both of which are independent of the reward service.              mation ri and si j , the cost of each worker at each stage C j , and
Combined with the constraint d ≪ T , the objective can be                  the maximum tolerable delay D, and outputs the number of
simplified to:                                                             workers N j for each stage. Initially, each N j is set to a maxi-
                                                                           mum value B (Line 6), ensuring that every request in the batch
                          min Rr,       s.t. d ≪ T.                 (4)
                        N1 ,...,NM                                         can be immediately served by an available worker, thereby
Expanding Rr as ∑ j N j · C j , we obtain the final form of the            satisfying the delay constraint. The stages are then sorted in
objective:                                                                 descending order of worker cost C j (Line 7). For each stage, a
                                                                           binary search between 1 and B determines the minimal num-
                   min
                 N1 ,...,NM j
                             ∑ N j ·C j ,       s.t. d ≪ T.         (5)    ber of workers that still satisfies the delay constraint (Lines
                                                                           9-17). At each step, the current allocation is evaluated using



USENIX Association                                    23rd USENIX Symposium on Networked Systems Design and Implementation                 1523
                                                         queue                 Execute              DDL

                                       4                                               4                                       4
                                  3                                        3                                            3
          Req                 2                                                  2                                      2
                     1                                         1                                           1
                     0                                         0                                           0




       Worker                                                                                                           2
                     1        2        4                       1                 2                         1            3
                     0            3                            0           3           4                   0                   4

                            Time                                       Time                                         Time
                  (a) History-based allocation.       (b) Violation due to long-running requests.           (c) Ideal allocation.

                                      Figure 9: Constraint violation due to long-running requests.

the simulation function SIMULATE(Num, R, S, D), which re-                  Consequently, purely reactive approaches are insufficient, and
turns true if the batch’s extra delay d remains below D. In the            a proactive strategy is required.
simulation, requests are processed in an FCFS manner, with                    Based on the above analysis, we propose a more conser-
each worker handling one request at a time. The algorithm fi-              vative, timeout-aware, proactive adjustment scheme. Within
nally outputs a worker allocation that satisfies the batch-level           the SIMULATE function, we not only check whether the over-
constraint. While this approach cannot theoretically guarantee             all batch completion time satisfies the constraint, but also
the global optimum, experiments in §6 show that it achieves                introduce an additional constraint to control whether certain
satisfactory results on the CUDA reward service with a short               requests can be queued. During the SIMULATE process, if
runtime, demonstrating its practical effectiveness.                        a request i is found to be queued at stage k, we compute its
    At this stage, our algorithm can find a near-optimal solution          latest possible completion time as
under the assumption that future batches closely resemble his-                                                     M
torical ones. However, in most cases, historical information is                                       LT = ts + ∑ T j .
reliable only at the level of aggregate distributions, such as the                                                j=k
overall arrival pattern, while the execution time of individual
requests is difficult to predict accurately. Figure 9 illustrates         Where ts is the current timestamp and T j is the timeout thresh-
such an example. For simplicity, we consider a reward compu-              old for each subsequent stage. If LT > T + D, then this re-
tation scenario with a single stage and five requests per batch,          quest may cause the overall batch to violate the extra delay
with a delay requirement of D = 0. In Figure 9(a), the running            constraint due to queuing. In this case, function SIMULATE
time of each request is derived from the data of the previ-               immediately returns False, indicating that the current number
ous batch. Algorithm 1 directly uses historical information               of workers is insufficient. The timeout-aware constraint effec-
to determine that two workers suffice to meet the constraint.             tively prevents potentially long-running requests from being
Requests 2 and 3 are queued, starting execution after requests            queued, resulting in a slightly higher allocation of workers.
0 and 1 complete. However, as shown in Figure 9(b), request               Conceptually, this trades additional resources for reduced ex-
2 in this batch has a longer-than-expected execution time, and            tra delay. As demonstrated in §6, the extra resource cost is
with only two workers, queuing causes its completion time to              modest, while the reduction in delay is substantial.
violate the constraint. To satisfy the requirement, three work-            5.2       Scaling and Scheduling Policy for Multiple Tasks
ers are needed, as illustrated in Figure 9(c), where request 2
                                                                          We next consider the more complex scenario in which mul-
runs without queuing. As shown in Figure 6(c), in the CUDA
                                                                          tiple training tasks share a single reward service, with each
reward service and similar code-execution services, unexpect-
                                                                          task subject to the batch-level constraint described in §5.1.
edly long execution times are common. To bound the runtime
of each request, such services typically set a timeout thresh-             Scaling policy. Regarding the scaling policy, the key differ-
old. When long-running requests occur, particularly those                  ence between multi-tenant and single-tenant scenarios lies
that only terminate upon reaching the timeout, the amount of               in the independence of batches. In the single-tenant case,
queuing they can tolerate is far less than estimated. Critically,          each batch can be treated independently. In contrast, in the
it is impossible to predict in advance which requests will be              multi-tenant scenario, requests from different tasks are inter-
long-running. These cases can only be detected once the re-                leaved. Consequently, when a batch from one task arrives or
quest starts execution, at which point no corrective action can            completes, it is highly likely that batches from other tasks
be taken. Furthermore, long-running requests exhibit little cor-           are still in progress. When continuing to make scaling deci-
relation with any observable features or system metrics prior              sions at the start or end of a batch, we categorize requests into
to execution. As a result, general-purpose autoscalers (e.g.,              three types: Future, requests that are expected to arrive at
utilization-driven approaches) are unaware of the batch-level              some point in the future; Queuingk , requests that have already
constraint and therefore cannot effectively address this issue.            arrived at the service but are waiting in the queue at stage



1524    23rd USENIX Symposium on Networked Systems Design and Implementation                                                USENIX Association
                    Ideal           ZQ-Dedicated         DistRS                       Ideal           ZQ-Dedicated       DistRS              1.2
          280                                                              3200

          210                                                              2400                                                              0.8




                                                                                                                                  Time (s)
  # GPU




                                                                   # CPU
          140                                                              1600
                                                                                                                                             0.4
           70                                                               800

            0                                                                 0                                                              0.0
                0           10000       20000       30000                         0           10000       20000      30000                         0   1     2      3     4   5
                                      Time                                                              Time                                                  Job ID

                      (a) Number of GPU nodes.                                         (b) Number of CPU nodes.                                        (c) Extra delay.

                                                       Figure 10: Performance under colocated RL training.

Algorithm 2 Request Information Collection                                                            quests are unknown, an FCFS scheduling policy is already
 1: Input: Information of the previous batch, i.e.,                                                   sufficiently effective. In contrast, in a multi-task scenario, the
 2:          R = {ri } and S = {si j }, where 0 ≤ i < B and 0 ≤ j < M.                                expected earliest completion times of the current batches vary
 3:          Current batch’s arrival time AT                                                          across tasks, so requests originating from different batches
 4:          Elapsed time of running requests at each stage eik                                       are subject to different constraints. However, FCFS cannot ac-
 5: Output: Estimated request information                                                             count for this difference. As a result, under the same amount
 6: Initialize output list Reqs = []
                                                                                                      of resources, requests with tighter constraints may be blocked
 7: // Collect Future requests
                                                                                                      by earlier-arriving requests with looser constraints, leading to
 8: for each request i in the batch do
 9:     if AT + ri > cur_ts then                                                                      increased queuing delays for the latter. To this end, we pro-
10:          Reqs.append((AT + ri , {si1 , si2 , ..., siM }))                                         pose a simple yet effective scheduling policy called Earliest
11: for each stage k do
                                                                                                      Batch First (EBF). Specifically, we manage a priority queue
12:     // Collect Queuingk requests                                                                  for each stage, where requests are prioritized based on the
13:     for each queuing request i at stage k do                                                      expected completion time of their corresponding batches. Re-
14:         Sample a request i′ from historical data                                                  quests with the same priority are sorted by their arrival time.
15:         Reqs.append((cur_ts, {si′ k , si′ (k+1) , . . . , si′ M }))                               When a worker at stage k becomes available, it selects from
16:             // Collect Runningk requests                                                          the queue the request whose associated batch has the earliest
17:             for each running request i at stage k do                                              estimated completion time. The expected completion time
18:                 Sample a request i′ with si′ k > eik from historical data                         of a batch is estimated using Equation 1 and 2. It is worth
19:                 Reqs.append((cur_ts, {si′ k − eik , si′ (k+1) , . . . , si′ M }))                 noting that we also apply EBF within the SIMULATE func-
20: return Reqs                                                                                       tion, enabling the scaling policy to be aware of the scheduling
                                                                                                      policy and thereby make more informed scaling decisions.

k; and Runningk , requests that are currently being executed                                          6     Evaluation
at stage k. Algorithm 2 describes how requests from each
                                                                                                      In this section, we evaluate DistRS using the CUDA reward
currently active batch are selected or sampled and how their
                                                                                                      service illustrated in Figure 4(a), focusing on both its resource
associated information is determined. For Future requests,
                                                                                                      allocation efficiency and its impact on training progress. We
the only requirement is that their estimated arrival time occurs
                                                                                                      first present an end-to-end evaluation of DistRS in a multi-
after the current timestamp. Historical requests satisfying this
                                                                                                      tenant setting (§6.2), and then highlight the effectiveness of
condition are selected, and their corresponding information
                                                                                                      its scaling and scheduling policies (§6.3).
can be directly used (Line 8-10). For Queuingk and Runningk
requests, since they have already arrived at the service, their                                       Implementation. The implementation of DistRS involves
arrival time is set to the current timestamp. Their running time                                      modifications to the trainer and the development of a disag-
at each stage is sampled from historical data. For Runningk                                           gregated reward service. The trainer is a production-scale RL
requests specifically, both the sampling and the estimation of                                        training framework. We trigger real-time reward computation
remaining execution time must account for the time already                                            requests via a hook attached to the end of the rollout phase.
spent executing in the current stage. Whenever a batch arrives                                        Specifically, when an input sequence completes its rollout,
or completes, we collect information for all active batches                                           a function annotated with @ray.remote is invoked to extract
using Algorithm 2 and feed it into Algorithm 1 to determine                                           the generated code and send a request to the reward service.
the resource allocation for the next time window.                                                     This design allows requests to be issued immediately with-
                                                                                                      out interfering with ongoing rollouts. In the reward service,
Scheduling policy. In the single-task scenario, since our con-                                        each worker runs in a container and is managed centrally by
straint is defined at the batch level, all requests share the                                         Kubernetes. The proxy, implemented as an HTTP server, han-
same constraint. When the execution times of individual re-                                           dles incoming reward computation requests from trainers and



USENIX Association                                           23rd USENIX Symposium on Networked Systems Design and Implementation                                             1525
                    Ideal      ZQ-Dedicated      DistRS                       Ideal       ZQ-Dedicated                      DistRS               2.0
          400                                                      4800
                                                                                                                                                 1.6
          300                                                      3600




                                                                                                                                      Time (s)
                                                                                                                                                 1.2
  # GPU




                                                           # CPU
          200                                                      2400
                                                                                                                                                 0.8
          100                                                      1200                                                                          0.4

            0                                                         0                                                                          0.0
                0                10000             20000                  0                 10000                             20000                    0   1       2    3        4   5
                                 Time                                                       Time                                                                   Job ID

                      (a) Number of GPU nodes.                                  (b) Number of CPU nodes.                                                   (c) Extra delay.

                                 Figure 11: Performance under disaggregated RL training with one-step staleness.

fetch requests from workers. The scaling planner executes in                                                                  ZQ-Dedicated                 Ideal            DistRS
a separate process, allowing it to run concurrently with the                                                        4.4

HTTP server.




                                                                                                    GPU Node Time
                                                                                                                    3.4
6.1         Methodology
                                                                                                                    2.4
Testbed. We deploy the CUDA reward service on a cluster
comprising both CPU and GPU nodes. Each compiler is allo-                                                           1.4
cated 4 CPU cores and 16 GB of memory, while each executor
is equipped with an NVIDIA GPU for executing CUDA ker-                                                              0.4
                                                                                                                            Colocated                           Disaggregated
nels. The proxy runs on a CPU node provisioned with 80                                                                    Figure 12: Normalized GPU time.
cores and 900 GB of memory.
                                                                                           and measure the total GPU/CPU time consumed during the
Workload. We evaluate DistRS using a trace collected from                                  entire training process. To assess the impact on training, we
a real CUDA code generation RL training task, which is the                                 primarily measure the extra delay of each batch (denoted
same trace analyzed in §3. For each batch, we record the                                   as d in Table 1), which captures the additional time caused
arrival time of each request along with the corresponding gen-                             by queuing in reward computation beyond the theoretical
erated code. For end-to-end evaluation, we concurrently run                                minimum required to complete all requests in the batch.
six RL training tasks using this trace, with their start times
staggered within a short time window. The original trace cor-                              6.2      End-to-end Performance
responds to a colocated training architecture. To generate the
                                                                                           We first evaluate the end-to-end performance of DistRS with
arrival pattern for a disaggregated one-step staleness training
                                                                                           six concurrent RL training tasks under both colocated and
architecture, we adjust the idle gaps between adjacent batches,
                                                                                           disaggregated RL training architectures.
reflecting the continuous rollout performed by dedicated re-
sources in a disaggregated setup.                                                          Colocated RL training. Figure 10 presents the performance
                                                                                           of DistRS under colocated RL training. As shown in Fig-
Baseline. Since no existing baseline is specifically designed
                                                                                           ures 10(a) and 10(b), DistRS significantly reduces resource
for the reward service, and a fully static resource configura-
                                                                                           consumption compared to the ZQ-Dedicated baseline, lower-
tion cannot guarantee that training tasks remain unblocked,
                                                                                           ing total GPU time by 3.79× and CPU time by 1.98×. This
we adopt a baseline resource allocation strategy based on his-
                                                                                           improvement stems from leveraging the request-in, batch-out
torical information. This strategy targets zero request queuing
                                                                                           property to flexibly queue requests, enabling all requests to
and allocates resources independently for each training task.
                                                                                           be completed under the batch-level constraint using fewer re-
We refer to this baseline as ZQ-Dedicated (Zero-Queue and
                                                                                           sources. Additionally, DistRS supports multi-tenancy and em-
Dedicated Resource). Additionally, we consider an idealized
                                                                                           ploys EBF to prioritize requests from batches with more strict
configuration in which all training tasks share a single reward
                                                                                           constraints, further enhancing resource utilization. In contrast,
service. In this setup, the service makes scaling decisions
                                                                                           the ZQ-Dedicated baseline must allocate sufficient resources
with perfect knowledge of the arrival times for all requests in
                                                                                           for each individual training task to avoid queuing, resulting in
the current batch and the execution time of each stage, while
                                                                                           substantial resource waste. Moreover, DistRS’s resource allo-
scheduling requests using EBF.
                                                                                           cation closely tracks the ideal configuration, demonstrating its
Metrics. Following the objective in Equation 5, we evaluate                                effectiveness in adapting to dynamic workloads without prior
the performance of DistRS from two perspectives: resource                                  knowledge of future requests. Figure 10(c) shows that DistRS
allocation and its impact on training progress. For resource                               incurs negligible additional latency, averaging 0.62 seconds,
allocation, we record the number of GPUs and CPUs allocated                                which is insignificant compared to the over 600 seconds re-
by each scaling decision, visualize the allocation over time,                              quired for a full training iteration. Overall, DistRS substan-



1526            23rd USENIX Symposium on Networked Systems Design and Implementation                                                                               USENIX Association
                       ZQ        History-based         DistRS                        History-based           DistRS                                         FCFS              DistRS                               FCFS.Task1       DistRS.Task1     Task1.T
              80                                                           90                                                          160                                                                         FCFS.Task2       DistRS.Task2     Task2.T
                                                                                                                                                                                                            2400


              60                                                                                                                       120
                                                                                                                                                                                                            1800
                                                                           60
 # GPU Node




                                                                                                                            Time (s)
                                                                Time (s)




                                                                                                                                                                                                  Request
              40                                                                                                                        80
                                                                                                                                                                                                            1200

                                                                           30
                                                                                                                                        40
              20                                                                                                                                                                                             600


                                                                                                                                         0                                                                     0
               0                                                            0                                                                16   18   20   22    24   26     28   30   32   34                    0        100     200        300   400
                   0        10   20          30   40       50                   0   10      20          30     40     50
                                                                                                                                                                 # GPU Node                                                       Time (s)
                                      Iter                                                       Iter


                   (a) Allocated GPU nodes.                                         (b) Extra delay.                                         (a) Allocated GPU nodes.                                                     (b) Extra delay.

                        Figure 13: Effectiveness of resource scaling.                                                                             Figure 14: Effectiveness of request scheduling.

tially reduces resource consumption while having minimal                                                                   The history-based method allocates the fewest resources, as it
impact on training progress.                                                                                               relies solely on historical information and cannot anticipate
                                                                                                                           long-running requests in new batches. In contrast, DistRS
Disaggregated RL training. Figure 11 presents the perfor-                                                                  incorporates a timeout-aware adjustment strategy under the
mance of DistRS under disaggregated RL training with one-                                                                  batch-level constraint, which allocates additional resources
step staleness. With reduced idle time between batches, the                                                                to handle potential long-running requests. Consequently, Dis-
workload becomes more continuous, leading to higher over-                                                                  tRS provisions slightly more resources than the history-based
all resource demand. Nevertheless, similar to the colocated                                                                approach while significantly reducing the impact on training.
training scenario, DistRS effectively reduces resource con-                                                                Figure 13(b) presents the extra delay incurred by each method,
sumption compared to the ZQ-Dedicated baseline, achieving                                                                  where the values are sorted by extra delay for clarity. The ZQ
3.49× reduction in total GPU time and 2.16× reduction in                                                                   method is not plotted, as it always incurs zero delay. The
total CPU time, as shown in Figures 11(a) and 11(b). DistRS’s                                                              history-based method experiences a high average extra delay
resource allocation closely follows the ideal configuration,                                                               of 14.3 seconds due to its inability to handle long-running re-
demonstrating its ability to adapt to different training archi-                                                            quests effectively. In contrast, DistRS maintains a low average
tectures while maintaining high resource efficiency. The extra                                                             extra delay of only 2.1 seconds, demonstrating the effective-
delay introduced remains low, averaging 0.85 seconds (Fig-                                                                 ness of the timeout-aware mechanism in balancing resource
ure 11(c)), indicating minimal impact on training progress                                                                 efficiency and training performance.
even under a more continuous workload.
   Figure 12 compares the normalized total GPU time con-                                                                   Effectiveness of request scheduling. We then evaluate the
sumed by DistRS against the ZQ-Dedicated and ideal con-                                                                    effectiveness of DistRS’s request scheduling policy using
figurations under both colocated and disaggregated training                                                                two concurrent batches, with the second batch starting 100
architectures. The normalization is based on the total GPU                                                                 seconds after the first. We compare DistRS, which employs
time actually required to execute all requests, so values closer                                                           the EBF scheduling policy, with a variant using the FCFS
to 1 indicate lower resource waste. The results show that the                                                              policy. To isolate the impact of scheduling, we fix the number
values achieved by DistRS are very close to 1, indicating that                                                             of CPU nodes at a large value to ensure CPU resources are not
it attains near-ideal resource efficiency in both training archi-                                                          a bottleneck and vary the number of GPU nodes. Figure 14(a)
tectures, with only a minor increase in resource consumption.                                                              shows the average extra delay incurred by the two tasks under
                                                                                                                           different numbers of GPU nodes. With a small number of
6.3                    Design Choices                                                                                      GPU nodes, both methods experience high extra delays due
We further evaluate the design choices of DistRS, focusing on                                                              to queuing. As the number of GPU nodes increases, the extra
the effectiveness of the resource scaling policy and the request                                                           delay for DistRS decreases rapidly, while the FCFS method
scheduling policy. For brevity, we focus on GPU resource                                                                   remains relatively high. Using EBF, DistRS requires 1.5×
allocation, since CPU allocation follows similar trends.                                                                   fewer GPU nodes than FCFS to achieve the same low extra
Effectiveness of resource scaling. We first evaluate the ef-                                                               delay of just a few seconds. This improvement arises because
fectiveness of the resource scaling policy in DistRS using                                                                 EBF prioritizes requests from the first batch, which has a
a single RL training task. Since the scaling policy operates                                                               more strict constraint, enabling more requests from this batch
at the granularity of batches, we use the iteration ID as the                                                              to complete sooner and thereby reducing overall extra delay.
x-axis in the figures. We compare DistRS with two variants:                                                                In contrast, FCFS processes requests strictly in arrival order,
(1) ZQ, which allocates resources with the objective of zero                                                               which can delay the first batch when resources are limited.
queuing; and (2) History-Based, which allocates resources                                                                  Figure 14(b) provides a detailed view of the finish times of
purely based on historical workload patterns. Figure 13(a)                                                                 requests from both batches when 24 GPUs are allocated. It
shows the number of GPU nodes allocated over time for each                                                                 shows that DistRS completes requests from the first batch
method. The ZQ approach consistently allocates a large num-                                                                earlier than FCFS, allowing the entire batch to finish sooner
ber of GPU nodes, resulting in substantial resource waste.                                                                 and further reducing the extra delay.



USENIX Association                                                                       23rd USENIX Symposium on Networked Systems Design and Implementation                                                                                        1527
                                       Stage 1         Stage 2            cient resource utilization and performance guarantees [40–45]
                  1.6
                                                                          Approaches can be broadly categorized into reactive, proac-
                  1.2                                                     tive [42, 46], and hybrid approaches. Recent work has also
                                                                          explored autoscaling in LLM serving systems, such as BlitzS-
       Time (s)




                  0.8
                                                                          cale [47] and LambdaScale [48], to accommodate dynamic
                  0.4                                                     workloads. Compared to general cloud applications, reward
                                                                          computation workloads in RL training exhibit stronger peri-
                  0.0
                        1000    2000         4000          8000   16000   odicity, making workload prediction less of a challenge for
                                          # Requests                      autoscaling. Instead, the key difficulty lies in defining ap-
                        Figure 15: Algorithm running time.                propriate autoscaling objectives tailored to the request-in,
                                                                          batch-out nature of reward computation.
6.4    System Overhead
Figure 15 shows the running time of the scaling algorithm                 SLO-targeted system. Service-level objectives (SLOs) are
in DistRS under varying numbers of estimated requests. Al-                widely employed in cloud systems to define performance tar-
gorithm 1 determines the resource allocation for each stage               gets. Prior work [17, 18, 49, 50] has focused on satisfying
sequentially. The reward computation for CUDA code gen-                   SLOs while improving resource efficiency or reducing cost
eration consists of two stages: compilation (Stage 1) and                 in microservice and serverless environments. In the context
execution (Stage 2), so the algorithm’s running time can be di-           of model serving, systems such as ClockWork [51], SHEP-
vided accordingly. As shown in the figure, both stages require            HERD [52], and DistServe [53] optimize scheduling and re-
more time as the number of requests increases. However, even              source allocation to meet latency SLOs and maximize good-
with 16,000 requests, the total running time of the algorithm             put. Our analysis in §3 reveals that reward computation in
remains around 2 seconds. Since the algorithm executes only               RLVR training exhibits a unique request-in, batch-out char-
at the start or completion of each batch, and batch durations             acteristic, meaning that the effective SLO of each individual
are typically on the order of hundreds of seconds or more,                request is determined by the batch to which it belongs. Guided
this overhead is negligible. Furthermore, the scaling planner             by this insight, we design a reward service that improves re-
runs asynchronously in a separate process, ensuring it does               source efficiency while ensuring seamless integration of the
not interfere with the normal operation of the reward service.            reward computation phase with the RLVR training pipeline.

7     Related Work                                                        8   Conclusion
System optimization for LLM RL. As research increasingly                  We present DistRS, a disaggregated reward service framework
focuses on improving LLM performance through reinforce-                   designed to provide efficient reward computation for RLVR
ment learning, a growing body of work [9, 10, 14–16, 33–39]               training tasks. By leveraging the request-in, batch-out char-
has explored system-level optimizations for LLM RL frame-                 acteristic of reward computation, DistRS relaxes the latency
works. The disaggregated architecture is first introduced in              requirements at the request level to a batch-level constraint.
OpenRLHF [10] and NeMo-Aligner [38], which decouples                      Based on this, we design resource scaling and request schedul-
the rollout and training phases to run on separate resources.             ing policies that minimize resource consumption while min-
In contrast, systems such as verl [9], ReaL [34], and RLH-                imizing the impact on training progress, thereby achieving
Fuse [14] propose colocated architectures, colocating rollout             a reduction of up to 3.79× in resource consumption. We
and training on the same resources to mitigate idle periods               analyze the workload and explore system optimization for
in disaggregated designs. More recently, variants of disaggre-            the reward computation phase in LLM RL training, and we
gated architectures with pipeline, streaming, or asynchronous             believe this work will prompt a revisit of the importance of
execution have been proposed [15, 16, 35, 39]. However, these             cloud infrastructure beyond training frameworks in advancing
efforts focus primarily on optimizing the rollout and train-              the frontier of AI capabilities.
ing phases, while the reward computation phase has received
                                                                          Acknowledgements. We thank our shepherd, Rachee Singh,
little attention. Our work is orthogonal to these RL frame-
                                                                          and the anonymous reviewers for their valuable feedback. We
work optimizations and focuses on system optimizations for
                                                                          thank Weiqiang Lou for implementing the basic CUDA sand-
the reward computation phase in LLM RL training. We char-
                                                                          box. This work was supported in part by the National Natural
acterize the unique workload properties of reward computa-
                                                                          Science Foundation of China under Grant 62325201 and the
tion and design a disaggregated reward service with elasticity
                                                                          Scientific Research Innovation Capability Support Project for
and multi-tenancy, thereby improving the resource efficiency
                                                                          Young Faculty under Grant ZYGXQNJSKYCXNLZCXM-
while minimizing its impact on RL training progress.
                                                                          I1. Xin Jin and Xuanzhe Liu are the corresponding authors.
Resource autoscaling. Autoscaling is a widely adopted tech-               Ruidong Zhu, Yinmin Zhong, Xuanzhe Liu, and Xin Jin are
nique in cloud computing, enabling dynamic resource alloca-               also with the Key Laboratory of High Confidence Software
tion in response to workload variations to achieve both effi-             Technologies (Peking University), Ministry of Education.



1528          23rd USENIX Symposium on Networked Systems Design and Implementation                                USENIX Association
References                                                      [12] P. Intellect, “PRIME-RL.” https://github.com/
 [1] L. Ouyang, J. Wu, X. Jiang, D. Almeida, C. Wainwright,          PrimeIntellect-ai/prime-rl, 2025.
     P. Mishkin, C. Zhang, S. Agarwal, K. Slama, A. Ray,        [13] Y. Cheng, J. Chen, J. Chen, L. Chen, L. Chen, W. Chen,
     et al., “Training language models to follow instructions        Z. Chen, S. Geng, A. Li, B. Li, et al., “Fullstack bench:
     with human feedback,” Advances in Neural Information            Evaluating LLMs as full stack coders,” arXiv preprint
     Processing Systems, 2022.                                       arXiv:2412.00535, 2024.
 [2] Y. Bai, A. Jones, K. Ndousse, A. Askell, A. Chen,          [14] Y. Zhong, Z. Zhang, B. Wu, S. Liu, Y. Chen, C. Wan,
     N. DasSarma, D. Drain, S. Fort, D. Ganguli,                     H. Hu, L. Xia, R. Ming, Y. Zhu, et al., “Optimizing
     T. Henighan, et al., “Training a helpful and harmless           RLHF training for large language models with stage
     assistant with reinforcement learning from human                fusion,” in USENIX NSDI, 2025.
     feedback,” arXiv preprint arXiv:2204.05862, 2022.
                                                                [15] Y. Zhong, Z. Zhang, X. Song, H. Hu, C. Jin, B. Wu,
 [3] D. Guo, D. Yang, H. Zhang, J. Song, R. Zhang, R. Xu,            N. Chen, Y. Chen, Y. Zhou, C. Wan, et al., “StreamRL:
     Q. Zhu, S. Ma, P. Wang, X. Bi, et al., “Deepseek-r1:            Scalable, heterogeneous, and elastic RL for LLMs
     Incentivizing reasoning capability in LLMs via rein-            with disaggregated stream generation,” arXiv preprint
     forcement learning,” arXiv preprint arXiv:2501.12948,           arXiv:2504.15930, 2025.
     2025.
                                                                [16] W. Fu, J. Gao, X. Shen, C. Zhu, Z. Mei, C. He, S. Xu,
 [4] J. Pan, J. Zhang, X. Wang, L. Yuan, H. Peng,                    G. Wei, J. Mei, J. Wang, et al., “AReaL: A large-scale
     and A. Suhr, “TinyZero.” https://github.com/                    asynchronous reinforcement learning system for lan-
     Jiayi-Pan/TinyZero, 2025.                                       guage reasoning,” arXiv preprint arXiv:2505.24298,
 [5] B. Jin, H. Zeng, Z. Yue, J. Yoon, S. Arik, D. Wang,             2025.
     H. Zamani, and J. Han, “Search-r1: Training LLMs to        [17] Z. Wang, P. Li, C.-J. M. Liang, F. Wu, and F. Y. Yan, “Au-
     reason and leverage search engines with reinforcement           tothrottle: A practical bi-level approach to resource man-
     learning,” arXiv preprint arXiv:2503.09516, 2025.               agement for SLO-targeted microservices,” in USENIX
 [6] Q. Yu, Z. Zhang, R. Zhu, Y. Yuan, X. Zuo, Y. Yue,               NSDI, 2024.
     W. Dai, T. Fan, G. Liu, L. Liu, et al., “Dapo: An open-    [18] Z. Wu, W.-L. Chiang, Z. Mao, Z. Yang, E. Friedman,
     source LLM reinforcement learning system at scale,”             S. Shenker, and I. Stoica, “Can’t be late: optimizing spot
     arXiv preprint arXiv:2503.14476, 2025.                          instance savings under deadlines,” in USENIX NSDI,
 [7] Y. Zheng, J. Lu, S. Wang, Z. Feng, D. Kuang, and                2024.
     Y. Xiong, “EasyR1: An efficient, scalable, multi-          [19] L. Weng, “Reward hacking in reinforcement learning,”
     modality RL training framework.” https://github.                lilianweng.github.io, Nov 2024.
     com/hiyouga/EasyR1, 2025.
                                                                [20] Y. Miao, S. Zhang, L. Ding, R. Bao, L. Zhang, and
 [8] X. Li, X. Sun, A. Wang, J. Li, and C. Shum, “Cuda-l1:           D. Tao, “Inform: Mitigating reward hacking in RLHF
     Improving cuda optimization via contrastive reinforce-          via information-theoretic reward modeling,” Advances
     ment learning,” arXiv preprint arXiv:2507.14111, 2025.          in Neural Information Processing Systems, 2024.
 [9] G. Sheng, C. Zhang, Z. Ye, X. Wu, W. Zhang, R. Zhang,      [21] J. Schulman, F. Wolski, P. Dhariwal, A. Radford, and
     Y. Peng, H. Lin, and C. Wu, “HybridFlow: A flexible             O. Klimov, “Proximal policy optimization algorithms,”
     and efficient RLHF framework,” in EuroSys, 2025.                arXiv preprint arXiv:1707.06347, 2017.
[10] J. Hu, X. Wu, W. Shen, J. K. Liu, Z. Zhu, W. Wang,         [22] Z. Shao, P. Wang, Q. Zhu, R. Xu, J. Song, X. Bi,
     S. Jiang, H. Wang, H. Chen, B. Chen, et al., “Open-             H. Zhang, M. Zhang, Y. Li, Y. Wu, et al., “Deepseek-
     RLHF: An easy-to-use, scalable and high-performance             math: Pushing the limits of mathematical reason-
     RLHF framework,” arXiv preprint arXiv:2405.11143,               ing in open language models,” arXiv preprint
     2024.                                                           arXiv:2402.03300, 2024.
[11] T. Griggs, S. Hegde, E. Tang, S. Liu, S. Cao, D. Li,       [23] C. Zheng, S. Liu, M. Li, X.-H. Chen, B. Yu,
     C. Ruan, P. Moritz, K. Hakhamaneshi, R. Liaw, A. Malik,         C. Gao, K. Dang, Y. Liu, R. Men, A. Yang, et al.,
     M. Zaharia, J. E. Gonzalez, and I. Stoica, “Evolving            “Group sequence policy optimization,” arXiv preprint
     SkyRL into a highly-modular RL framework,” 2025.                arXiv:2507.18071, 2025.
     Notion Blog.




USENIX Association                       23rd USENIX Symposium on Networked Systems Design and Implementation            1529
[24] M. Luo, S. Tan, R. Huang, A. Patel, A. Ariyak, Q. Wu,       [37] K. Lei, Y. Jin, M. Zhai, K. Huang, H. Ye, and J. Zhai,
     X. Shi, R. Xin, C. Cai, M. Weber, et al., “Deepcoder:            “PUZZLE: Efficiently aligning large language models
     A fully open-source 14b coder at o3-mini level,” 2025.           through Light-Weight context switch,” in USENIX ATC,
     Notion Blog.                                                     2024.
[25] C. Baronio, P. Marsella, B. Pan, S. Guo, and S. Alberti,    [38] G. Shen, Z. Wang, O. Delalleau, J. Zeng, Y. Dong,
     “Kevin: Multi-turn RL for generating CUDA kernels,”              D. Egert, S. Sun, J. Zhang, S. Jain, A. Taghibakhshi,
     arXiv preprint arXiv:2507.11948, 2025.                           et al., “Nemo-aligner: Scalable toolkit for efficient
[26] S. Liu, S. Hegde, S. Cao, A. Zhu, D. Li, T. Griggs,              model alignment,” arXiv preprint arXiv:2405.01481,
     E. Tang, A. Malik, K. Hakhamaneshi, R. Liaw, P. Moritz,          2024.
     M. Zaharia, J. E. Gonzalez, and I. Stoica, “SkyRL-SQL:      [39] Z. Han, A. You, H. Wang, K. Luo, G. Yang, W. Shi,
     Matching GPT-4o and o4-mini on text2SQL with multi-              M. Chen, S. Zhang, Z. Lan, C. Deng, et al., “AsyncFlow:
     turn RL,” 2025. Notion Blog.                                     An asynchronous streaming RL framework for efficient
[27] S. Wasik, M. Antczak, J. Badura, A. Laskowski, and               LLM post-training,” arXiv preprint arXiv:2507.01663,
     T. Sternal, “A survey on online judge systems and their          2025.
     applications,” ACM Computing Surveys (CSUR), 2018.          [40] K. Rzadca, P. Findeisen, J. Swiderski, P. Zych,
[28] H. Wu, Y. Liu, L. Qiu, and Y. Liu, “Online judge system          P. Broniek, J. Kusmierek, P. Nowak, B. Strack, P. Witu-
     and its applications in c language teaching,” in Interna-        sowski, S. Hand, et al., “Autopilot: workload autoscaling
     tional Symposium on Educational Technology (ISET),               at google,” in EuroSys, 2020.
     2016.
                                                                 [41] N. Roy, A. Dubey, and A. Gokhale, “Efficient autoscal-
[29] S. Yao, “The second half.” https://ysymyth.github.               ing in the cloud using predictive models for workload
     io/The-Second-Half, 2025.                                        forecasting,” in IEEE 4th international conference on
                                                                      cloud computing, 2011.
[30] Y. Yue, Y. Yuan, Q. Yu, X. Zuo, R. Zhu, W. Xu, J. Chen,
     C. Wang, T. Fan, Z. Du, et al., “Vapo: Efficient and        [42] Z. Pan, Y. Wang, Y. Zhang, S. B. Yang, Y. Cheng,
     reliable reinforcement learning for advanced reasoning           P. Chen, C. Guo, Q. Wen, X. Tian, Y. Dou, et al.,
     tasks,” arXiv preprint arXiv:2504.05118, 2025.                   “Magicscaler: Uncertainty-aware, predictive autoscal-
[31] H. Zhang, Y. Tang, A. Khandelwal, J. Chen, and I. Sto-           ing,” VLDB, 2023.
     ica, “Caerus: NIMBLE task scheduling for serverless         [43] H. Qian, Q. Wen, L. Sun, J. Gu, Q. Niu, and Z. Tang,
     analytics,” in USENIX NSDI, 2021.                                “Robustscaler: Qos-aware autoscaling for complex work-
[32] C. Jin, Z. Zhang, X. Xiang, S. Zou, G. Huang, X. Liu,            loads,” in IEEE ICDE, 2022.
     and X. Jin, “Ditto: Efficient serverless analytics with     [44] A. U. Gias, G. Casale, and M. Woodside, “Atom: Model-
     elastic parallelism,” in ACM SIGCOMM, 2023.                      driven autoscaling for microservices,” in IEEE ICDCS,
[33] Z. Yao, R. Y. Aminabadi, O. Ruwase, S. Rajbhan-                  2019.
     dari, X. Wu, A. A. Awan, J. Rasley, M. Zhang, C. Li,
                                                                 [45] D. Zou, W. Lu, Z. Zhu, X. Lu, J. Zhou, X. Wang, K. Liu,
     C. Holmes, et al., “DeepSpeed-Chat: Easy, fast and af-
                                                                      K. Wang, R. Sun, and H. Wang, “Optscaler: A collab-
     fordable RLHF training of chatgpt-like models at all
                                                                      orative framework for robust autoscaling in the cloud,”
     scales,” arXiv preprint arXiv:2308.01320, 2023.
                                                                      VLDB, 2024.
[34] Z. Mei, W. Fu, K. Li, G. Wang, H. Zhang, and Y. Wu,
     “Real: Efficient RLHF training of large language            [46] Z. Zhou, C. Zhang, L. Ma, J. Gu, H. Qian, Q. Wen,
     models with parameter reallocation,” arXiv preprint              L. Sun, P. Li, and Z. Tang, “AHPA: adaptive horizon-
     arXiv:2406.14088, 2024.                                          tal pod autoscaling systems on alibaba cloud container
                                                                      service for kubernetes,” in Proceedings of the AAAI con-
[35] J. He, T. Li, E. Feng, D. Du, Q. Liu, T. Liu, Y. Xia,            ference on artificial intelligence, 2023.
     and H. Chen, “History Rhymes: Accelerating LLM re-
     inforcement learning with RhymeRL,” arXiv preprint          [47] D. Zhang, H. Wang, Y. Liu, X. Wei, Y. Shan, R. Chen,
     arXiv:2508.18588, 2025.                                          and H. Chen, “BlitzScale: Fast and live large model
                                                                      autoscaling with O (1) host caching,” in USENIX OSDI,
[36] Z. Wang, T. Zhou, L. Liu, A. Li, J. Hu, D. Yang, J. Hou,         2025.
     S. Feng, Y. Cheng, and Y. Qi, “DistFlow: A fully dis-
     tributed RL framework for scalable and efficient LLM
     post-training,” arXiv preprint arXiv:2507.13833, 2025.



1530   23rd USENIX Symposium on Networked Systems Design and Implementation                              USENIX Association
[48] M. Yu, R. Yang, C. Jia, Z. Su, S. Yao, T. Lan, Y. Yang,    [51] A. Gujarati, R. Karimi, S. Alzayat, W. Hao, A. Kauf-
     Y. Cheng, W. Wang, A. Wang, et al., “λ Scale: Enabling          mann, Y. Vigfusson, and J. Mace, “Serving DNNs like
     fast scaling for serverless large language model infer-         clockwork: Performance predictability from the bottom
     ence,” arXiv preprint arXiv:2502.09922, 2025.                   up,” in USENIX OSDI, 2020.
[49] Z. Zhang, C. Jin, and X. Jin, “Jolteon: Unleashing         [52] H. Zhang, Y. Tang, A. Khandelwal, and I. Stoica, “SHEP-
     the promise of serverless for serverless workflows,” in         HERD: Serving DNNs in the wild,” in USENIX NSDI,
     USENIX NSDI, 2024.                                              2023.
[50] A. Mahgoub, E. B. Yi, K. Shankar, S. Elnikety,             [53] Y. Zhong, S. Liu, J. Chen, J. Hu, Y. Zhu, X. Liu, X. Jin,
     S. Chaterji, and S. Bagchi, “ORION and the three rights:        and H. Zhang, “DistServe: Disaggregating prefill and
     Sizing, bundling, and prewarming for serverless DAGs,”          decoding for goodput-optimized large language model
     in USENIX OSDI, 2022.                                           serving,” in USENIX OSDI, 2024.




USENIX Association                       23rd USENIX Symposium on Networked Systems Design and Implementation           1531
