study, closely matching the Mess benchmark design. Third, the                 benchmark [48], [49] executed on one CPU core or one
framework closely couples the memory-related profiling of                     GPU Stream Multiprocessor (SM). This determines the y-axis
hardware platforms and applications. Similar to the Mess                      position of the measurement. The pointer-chase is selected
simulator, the Mess application profiling itself is uncomplicated,            because it is simple and portable to different actual hardware
and its real value comes from the application analysis in the                 platforms and simulators. Optionally, memory latency could
context of the memory system characteristic. The quality of                   be measured with instruction-based sampling approach [50]
the memory characterization therefore directly impacts the the                available in some current architectures [28], [51], [52].
quality of the overall analysis. For this reason, the analysis has               Running pointer-chase alone measures the unloaded memory
to be performed with detailed Mess-like memory performance                    access latency. To measure the latency of the loaded memory
description.                                                                  system, concurrently with the pointer-chase, on the remaining
   The Mess benchmark is released as open source and it                       CPU cores or GPU SMs, we run a memory traffic generator.
is ready to be used in x86, Power, ARM, and RISC-V                            The memory access latency is still measured only for the
CPUs and NVIDIA GPUs [32]. The release also contains                          pointer-chase benchmark. The purpose of the traffic generator
all bandwidth–latency measurements shown in the paper,                        is to create memory accesses that will collide with the pointer-
including the CXL expander curves provided by the memory                      chase in the loaded memory system. The generator is designed
manufacturer. We also release as open source the ZSim, gem5                   to create memory traffic with configurable memory bandwidth
and OpenPiton Metro-MPI integrated with the Mess simulator                    utilization and read/write ratio. Each CPU core and GPU SM
supporting DDR4, DDR5, Optane, HBM2, HBM2E and CXL                            traverses two separate arrays, one with load and one with store
expanders [41]. Also, the public releases of the production                   operations. Therefore, the overall memory traffic is complex,
HPC performance analysis tools already include the Mess                       determined by a sequential accesses within each array, but
application profiling extension [42]. The released tools are                  also by the interleaving between memory request from distinct
ready to be used by the community for better understanding                    arrays. The Mess benchmark covers a large range of row-buffer
of the current and exploration of future memory systems.                      hit/empty/miss rates, e.g. between 35/43/22% and 84/13/3% in
                                                                              state-of-the-art Intel architectures (Section IV-D).
                         II. M ESS BENCHMARK                                     Both, pointer-chase and traffic generator are implemented in
    This section describes the Mess benchmark use, and analyzes assembly to minimize any compiler intervention. The detailed
its characterization of actual platforms and hardware simulators. implementations of pointer- chase and traffic generator is
                                                                              presented in Appendix A. To minimize the latency penalties
A. Mess benchmark: Memory bandwidth–latency curves                            introduced by the TLB misses and the page walk, the Mess data-
    The Mess memory characterization comprises tens of structures are allocated in huge memory pages. Additionally, at
bandwidth–latency curves, each corresponding to a specific runtime, these overheads are monitored with hardware counters
ratio between the read and write memory traffic. The and subtracted from the memory latency measurements. The
Mess benchmark kernels cover the whole range of memory Mess benchmark release [32] includes the source code and its
operations, from 100%-loads to 100%-stores. The 100%- detailed description.
load kernel generates a 100%-read memory traffic, while the
                                                                              B. Validation
100%-store kernel creates 50%-read/50%-write traffic. This
is because the contemporary CPUs deploy the write-allocate                       The Mess benchmark exceeds the coverage of all existing
cache policy [43]. With this policy, each store instruction first memory benchmarks and tools. Still, these tools can validate
reads data from the main memory to the cache, then modifies it, some of the Mess measurements.
and finally writes it to the main memory once the cache line is                  The unloaded memory system latency can be measured with
evicted. Each store instruction, therefore, does not correspond               LMbench     and Google multichase in CPU platforms, and P-
to a single memory write, but to one read and one write.                 1    chase  [53]  in GPUs. We used these benchmarks to validate the
    The Mess bandwidth–latency curves are illustrated in Fig-                 Mess   unloaded  latency measurements in all hardware platforms
ure 1 (middle). The curves with different composition of read                 under   study.  In all experiments, Mess closely matches the
and write traffic are plotted with different shades of blue.                  LMbench,     Google  multichase and P-chase results.
Each curve is constructed based on tens of measurement                           In Intel systems, the maximum sustained memory bandwidth
points that cover the whole range of memory-traffic intensity.                can  be  measured   with  the Intel Advisor [4]. In the Skylake,
Figure 1 (top) illustrates the construction process for one of                Cascade    Lake  and  Sapphire  Rapids servers under study, the
the curves. The x-axis of the chart corresponds to memory                     Mess    benchmark    matches  the  Advisor measurements, with
bandwidth, monitored with hardware counters [44]–[47]. The                    a difference   below  1%.
memory access latency is measured with a pointer-chase                           The Intel Memory Latency Checker (MLC) [11], can
                                                                              measure the memory latency for a selected memory traffic
   1 Memory traffic with more than 50% of writes can be generated with        intensity, i.e. memory bandwidth. The memory bandwidth
streaming stores that directly write data to the main memory. The public Mess can be fine-tuned, but the tool provides a sparse analysis
repository already includes the benchmark with x86 streaming (non-temporal)
stores. We are working on the equivalent benchmark version for ARM and        of different traffic compositions, i.e. read and write memory
Power CPUs (zero cache line fill instructions), and NVIDIA GPUs.              operations. We compare the Intel MLC results and the



                                                                      3
