                             Polymorphic Error Correction
                                        Evgeny Manzhosov and Simha Sethumadhavan
                                      Department of Computer Science, Columbia University
                                                   New York, New York, USA
                                            Email: {evgeny, simha}@cs.columbia.edu


   Abstract—In this paper, we propose a new memory error               aligned eight-bit symbols, and more, again, at the cost of no
correction scheme, Polymorphic ECC, based on a novel idea of           additional storage. In contrast, a state-of-the-art Reed-Solomon
redundancy polymorphism for error correction. With redundancy          code, for the same storage budget as Polymorphic ECC, can
polymorphism, we can use the check bits, i.e., parity bits in
traditional ECC, to correct errors from different fault models.        only correct symbol-aligned errors [61], with a ∼7% chance of
For example, the error correction procedure will use the same          miscorrecting other corruptions. This limitation becomes even
redundancy value for single-bit errors, double-bit errors, Chip-       more pronounced when ECC bits used for traditional error
Kill, and others. As a result, Polymorphic ECC corrects more           correction mechanisms are shared for security features such
errors than traditional codes, which typically target a single fault   as memory safety tags [58], [74], further reducing the efficacy
model or require multiple redundancies for multi-fault model
support, leading to higher storage overheads. Our construction         of conventional codes [51].
is very compact, allowing us to embed an inlined cryptographic            Traditional constructions of ECC have redundancy infor-
message authentication code (MAC) with each cacheline, ensuring        mation crafted (mathematically) to work with only one fault
data integrity and near 100% error detection without needing           model. However, our construction uses residue codes, where
any extra storage. The MAC, further permits iterative correction       the redundancy information is the remainder (residue) left over
among the many supported fault models. In the paper, we show
that the novel combination of redundancy polymorphism with             after dividing the data word by a known constant [26]. With
iterative correction, corrects errors due to fault models not          this approach, we gain two advantages on parity-based codes.
covered by traditional codes and guarantees data integrity with        First, we gain redundancy polymorphism, i.e., the ability to
up to 60-bit MACs while using 64-byte cachelines and standard          map the same residue value to errors due to multiple fault
40-bit DDR5 memory channels.                                           models. Second, as we discovered, the cardinality of those
   Index Terms—Memory Error Correction, Security, Reliability,
Polymorphism.                                                          mappings is much smaller than that of those made with
                                                                       traditional parity, making the search space for errors much
                                                                       smaller and error correction much faster.
                       I. I NTRODUCTION
                                                                          For example, consider a 32-bit word protected by 4-bit
   Today, Error Correction Codes (ECC) are critical for offer-         parity, where one bit protects 8 bits of data, i.e., the first parity
ing acceptable levels of system reliability. Their effectiveness,      bit protects bits 0 to 7, the second parity bit protects bits 8 to
however, is increasingly challenged by emerging security               15, etc. Suppose we know there was a two-bit error in the data,
threats [14] and modern defense mechanisms [74]. Specifi-              and we need to correct it through search. When we recompute
cally, rowhammer faults [64], [71] and extremely tight design          the parity bits, we can have two outcomes: (1) two parity bits
margins exacerbate the reliability concerns of modern memory           do not match, which will indicate corrupted bytes, bounding
systems. At the same time, new security defenses, such as              the search space to 64 error candidates, and (2) parity bits
memory encryption [4], [11], [40], further strain program              match, which means that we have to try all possible 2-bit errors
reliability since faults that escape traditional ECC mechanisms        that are in the same byte or 4 × 82 = 112 error candidates.
result in errors that diffuse and propagate more widely. Thus,         In the same settings, Polymorphic ECC, as we show in the
in this paper, we present a novel ECC mechanism designed               paper, needs at most 18 and 10 error candidates, respectively,
to address the dual challenges of ensuring both the reliability        resulting in a significant reduction of search space.
and security of modern memory systems.                                    Conceptually, Polymorphic ECC works in two steps: (1)
   We describe a novel error correction scheme, Polymorphic            error detection via MAC check and (2) error correction via
ECC, which significantly enhances error detection and cor-             iterations. The error detection is done by comparing an inlined
rection capabilities compared to current state-of-the-art codes        MAC with a new one recomputed from the data, and if those
while staying within the bits budgeted to support traditional          MACs do not match, we declare an error. In this case, we
ECC. Because of how it is constructed, Polymorphic ECC                 begin the iterative correction process. First, we use codeword
permits at least a 40-bit MAC to be stored with each 64-               residue to derive probable errors for the first fault model, say,
byte cache line to detect any pattern of bit corrections with          1-bit errors. Then, for each iteration, we use one of those
a probability of 1 − 2−40 with no additional area overhead.            error candidates to correct the error, and the outcome of each
Further, Polymorphic ECC also permits storing 11 redundancy            correction trial is verified by MAC check against inlined MAC.
bits per 64 data bits, allowing it to correct any single-bit           Suppose none of the error candidates of the first fault model
error, two random bit errors, eight-bit errors within naturally        corrected the error and passed MAC verification. In that case,
