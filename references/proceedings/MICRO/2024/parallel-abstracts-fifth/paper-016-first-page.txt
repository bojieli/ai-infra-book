                         2024 57th IEEE/ACM International Symposium on Microarchitecture (MICRO)



LightWSP: Whole-System Persistence on the Cheap
                    Yuchen Zhou                             Jianping Zeng                         Changhee Jung
                  Purdue University                       Purdue University                     Purdue Univerisity
                West Lafayette, USA                     West Lafayette, USA                    West Lafayette, USA
                zhou1166@purdue.edu                     zeng207@purdue.edu                      chjung@purdue.edu




   Abstract—Whole-system persistence (WSP) has recently at-           NVM is used as persistent heap—to delineate a specific code
tracted more interest thanks to its transparency and performance      region and persist its data with crash consistency ensured. This
benefits over partial-system persistence where users are not only     is so-called partial-system persistence (PSP) in that only the
burdened by complex persistent programming but also incapable
of using DRAM as LLC. Nevertheless, existing WSP work either          stores of such user-defined code regions are to be persisted in
introduces high hardware cost or causes non-trivial performance       NVM.
overhead. To this end, this paper presents LightWSP, a com-              While PSP looks promising for achieving data persistence
piler/architecture co-design scheme that can achieve WSP in a         and crash consistency, it faces several challenges. First, to
lightweight yet performant manner. LightWSP compiler parti-           ensure persistence, PSP either uses expensive persist barriers—
tions program into a series of recoverable regions (epochs) with
their live-out registers checkpointed, while LightWSP hardware        e.g., clwb and sfence in x86 processors—or relies on
persists the stores of the regions—whose boundary serves as           dedicated hardware structures [51], [60], [80], [96], thus being
a power failure recovery point—enforcing crash consistency;           unable to achieve low-cost data persistence. Second, in PSP,
LightWSP leverages the battery-backed write pending queue             application-specific recovery code—tailored to data structures
(WPQ) of a memory controller as a redo buffer, i.e., all stores are   being persisted—is often desired for performance reasons,
first buffered in WPQ and then persisted together in non-volatile
memory (NVM) at each region end. In this way, no matter when          which places a significant programming burden on users [7],
power failure happens, NVM is never corrupted by the stores           [37], [91], [93], [111]. Third, PSP requires users to explicitly
of the power-interrupted region, facilitating correct recovery.       manipulate the persistent heap with special memory allocator
In particular, LightWSP supports multiple memory controllers          and deallocator such as pmalloc/pfree [44], exposing
on the cheap without costly speculation/misspeculation handling       them to potential memory leaks. This further complicates
mechanisms used by prior work. The experimental results with
38 applications show that LightWSP incurs only an average of          persistent programming and increases the risk of bugs [14],
9.0% run-time overhead. This is on par with the state-of-the-art      [25], [29], [32]–[34], [58], [71]–[73], [76], [77], [82].
work, that complicates the core microarchitecture significantly          More importantly, it is quite challenging for PSP to exploit
with its intrusive design for memory controller speculation, yet      DRAM as last-level cache (LLC) atop non-volatile main
the hardware cost of LightWSP is near zero (0.5B per core).           memory. The reason is twofold: (1) existing technologies,
                       I. I NTRODUCTION                               e.g., Intel eADR [41], cannot afford to keep volatile states of
                                                                      terabyte DRAM from power failure; (2) DRAM is still used
   The emergence of non-volatile memory (NVM) technolo-
                                                                      as main memory in PSP—unlike Optane’s memory mode that
gies, exemplified by Intel’s Optane Persistent Memory [42],
                                                                      allows DRAM cache yet uses PM as volatile main memory
has paved a new way for data persistence thanks to their irre-
                                                                      losing persistence. Consequently, PSP forfeits the benefit of
sistible benefits, e.g., nonvolatility, high areal density, DRAM-
                                                                      leveraging DRAM as LLC, though it is essential for high
comparable speed, and byte-addressability1 [1], [2], [56], [61].
                                                                      performance; an ideal PSP scheme underperforms LightWSP,
The upshot is that the core pipeline can directly persist the
                                                                      that enables DRAM cache, by 46.8% on average for memory-
data being stored to persistent memory (PM) with regular store
                                                                      intensive applications and up to 160% for libquantum of
instructions. This allows program to achieve high-performance
                                                                      CPU2006 as shown in Figure 9.
data persistence without having to rely on slow block devices.
                                                                         As an alternative to PSP, whole-system persistence (WSP)
   However, care must be taken to avoid crash inconsistency
                                                                      has recently attracted growing interest among researchers [15],
across power failure. During program execution, a younger
                                                                      [19], [27], [53], [81], [98], [105], [108], [110]. Two unique
store could be evicted (persisted) from cache to NVM before
                                                                      features of WSP drive this trend, i.e., transparency and per-
an older one; if power failure occurs while the older is yet
                                                                      formance. WSP ensures persistence for all data of every kind
to be persisted, it can lead to inconsistent NVM states in the
                                                                      of program—including OS and libraries—with no source code
wake of the power failure. With that in mind, users typically
                                                                      modification. Moreover, WSP can enable DRAM cache over
leverage transactions [20], [51], [59], [65], [95], [113] or
                                                                      PM by using it as non-volatile main memory without com-
failure-atomic-sections (FASEs) [13], [21], [38], [48], [50],
                                                                      promising data persistence or crash consistency. Nevertheless,
[66], [96]—atop Intel Optane’s app-direct mode [45] where
                                                                      existing WSP work is far from practical due to intrusive
  1 NVM DIMMs are attached to the regular memory bus as with DRAM     hardware modification and/or high run-time overhead. For
DIMMs.                                                                example, Capri [53] resorts to over-complex redo+undo log-


2379-315/24/$31.00 ©2024 IEEE                                     215
DOI 10.1109/MICRO61859.2024.00025
