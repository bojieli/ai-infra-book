                                              HgPCN: A Heterogeneous Architecture for E2E
                                                   Embedded Point Cloud Inference
                                                            1st Yiming Gao                            2nd Chao Jiang                        3rd Wesley Piard
                                                          University of Florida                     University of Florida                 University of Florida
                                                           gaoyiming@ufl.edu                         jc19chaoj@ufl.edu                     wespiard@ufl.edu

                                                          4th Xiangru Chen                           5th Bhavesh Patel                       6th Herman Lam*
                                                         University of Florida                          Dell EMC                            University of Florida
                                                         cxr1994816@ufl.edu                      Bhavesh.A.Patel@dell.com                      hlam@ufl.edu
arXiv:2501.07767v1 [cs.AR] 14 Jan 2025




                                            Abstract—Point cloud is an important type of geometric data             Index Terms—Heterogeneous Computing, Edge AI Service,
                                         structure for many embedded applications such as autonomous              Point Cloud Network Inference
                                         driving and augmented reality. Current Point Cloud Networks
                                         (PCNs) have proven to achieve great success in using inference to                             I. I NTRODUCTION
                                         perform point cloud analysis, including object part segmentation,
                                         shape classification, and so on. However, point cloud applications          With the development of 3D sensors, such as LiDARs,
                                         on the computing edge require more than just the inference               and RGB-D cameras, there are increased interests in research
                                         step. They require an end-to-end (E2E) processing of the point           and development of processing point cloud data. Point Cloud
                                         cloud workloads: pre-processing of raw data, input preparation,          Networks (PCNs) have proven to achieve great success in
                                         and inference to perform point cloud analysis. Current PCN
                                         approaches to support end-to-end processing of point cloud               different tasks of point cloud analysis, including object part
                                         workload cannot meet the real-time latency requirement on the            segmentation, shape classification, and so on. Point cloud ap-
                                         edge, i.e., the ability of the AI service to keep up with the speed      plications on the computing edge, such as autonomous robotics
                                         of raw data generation by 3D sensors.                                    and drones, have stringent real-time requirements. Many PCNs
                                            Latency for end-to-end processing of the point cloud workloads        for point cloud AI tasks have been proposed [9], including
                                         stems from two reasons: memory-intensive down-sampling in the            DSA (Domain-Specific Architecture) PCN accelerators such
                                         pre-processing phase and the data structuring step for input
                                         preparation in the inference phase. In this paper, we present            as Mesorasi [6], PointACC [16], and Crescent [5], which have
                                         HgPCN, an end-to-end heterogeneous architecture for real-time            been proposed to accelerate PCN inference. However, point
                                         embedded point cloud applications. In HgPCN, we introduce                cloud applications on the computing edge require more than
                                         two novel methodologies based on spatial indexing to address             just the inference step. They require an end-to-end processing
                                         the two identified bottlenecks. In the Pre-processing Engine             of the point cloud workloads. A recent paper [23] indicated
                                         of HgPCN, an Octree-Indexed-Sampling method is used to
                                         optimize the memory-intensive down-sampling bottleneck of the            that the processing of AI services for edge computing suffer
                                         pre-processing phase. In the Inference Engine, HgPCN extends             the problem of high “AI tax”, which includes the supporting
                                         a commercial DLA with a customized Data Structuring Unit                 steps for AI workload such as pre-processing of raw data,
                                         which is based on a Voxel-Expanded Gathering method to                   input preparation, and offloading communication overhead.
                                         fundamentally reduce the workload of the data structuring step           These steps contribute to a major part of overall latency in
                                         in the inference phase.
                                                                                                                  edge AI services. Unfortunately, this high “AI tax” problem is
                                            The initial prototype of HgPCN has been implemented on
                                         an Intel PAC (Xeon+FPGA) platform. Four commonly available               more serious for point cloud applications. Due to the inherent
                                         point cloud datasets were used for comparison, running on                irregularity and large size of raw point cloud data, the pre-
                                         three baseline devices: Intel Xeon W-2255, Nvidia Xavier NX              processing and communication overhead of point cloud data
                                         Jetson GPU, and Nvidia 4060ti GPU. These point cloud datasets            are far more expensive than traditional data types, such as
                                         were also run on two existing PCN accelerators for compar-
                                                                                                                  image or video. Due to the high AI-tax, the current PCN
                                         ison: PointACC and Mesorasi. Our results show that for the
                                         inference phase, depending on the dataset size, HgPCN achieves           approaches to support end-to-end processing of point cloud
                                         speedup from 1.3× to 10.2× vs. PointACC, 2.2× to 16.5× vs.               workload cannot meet the real-time requirement [10], i.e., the
                                         Mesorasi, and 6.4× to 21× vs. Jetson NX GPU. Along with                  ability of the AI service to keep up with the speed of raw data
                                         optimization of the memory-intensive down-sampling bottleneck            generation by 3D sensors. In PCN edge services, the AI tax
                                         in pre-processing phase, the overall latency shows that HgPCN
                                                                                                                  is mainly stemmed from two reasons: the expensive down-
                                         can reach the real-time requirement by providing end-to-end
                                         service with keeping up with the raw data generation rate.               sampling pre-processing phase and the data structuring step
                                                                                                                  for input preparation in the inference phase.
                                                                                                                     As shown in Figure 1(a), an end-to-end point cloud AI
                                            This research was supported in part by the National Science Founda-
                                         tion (NSF) Center for Space, High-Performance, and Resilient Computing   service based on PCNs comprise of two major phases after
                                         (SHREC) through the IUCRC Program under Grant No. CNS-1738420.           raw data generation: pre-processing of the point cloud data
                                                                                  services for real-time applications. For example, the most com-
                                                                                  monly used down-sampling pre-processing method, farthest-
                                                                                  point sampling (FPS), takes over 200 seconds to sample 10%
                                                                                  of 1 million points [10] on GPUs. For CPUs, such enormous
                                                                                  amount of computation is rather slow because their limited
                                                                                  parallel capability. We will show in Section III that the down-
                                                                                  sampling pre-processing phase occupies a major part of the
                                                                                  end-to-end latency, which greatly exacerbating the problem of
                                                                                  high “AI tax” [23].
                                                                                     Currently, there is a very limited number of hardware-based
                                                                                  designs for point cloud accelerators [6], [16]. Furthermore,
                                                                                  these works do not provide a complete solution since they do
                                                                                  not fully consider the two bottlenecks. They focus solely on the
                                                                                  AI (inference) phase, based on the assumption that the input is
                                                                                  already down-sampled (pre-processed) and restructured. More
                                                                                  details on these previous works will be given in Section II.
Fig. 1. (a) Two phases of an end-to-end point clouds AI service (classification      In this paper, we first identify the workload characteristics
task), (b) Overall architecture to process the two phases.
                                                                                  and bottlenecks of the two major phases of end-to-end point
                                                                                  cloud edge applications. The frontend pre-processing phase
                                                                                  is a memory-intensive task, whereas the backend inference
and PCN inference. After obtaining the raw point cloud data                       phase is a computationally intensive task. The workload char-
from the sensor, the size of the raw data is enormous: e.g.,                      acteristics are discussed in Section III. An overall architecture
for every frame, a LiDAR sensor produces approximately 2                          for HgPCN to support end-to-end processing of point-cloud
million points. Also, the number of points in each frame                          applications is presented in Section IV.
is highly irregular because different objects have different                         To enable the real-time PCN process in HgPCN, we intro-
reflectivity to the laser. Thus, point cloud applications on the                  duce two novel methodologies to replace the brute-force tradi-
edge require an effective front-end pre-processing phase to                       tional pre-processing and data structuring methods (introduced
deal with this kind of irregularity before feeding the data                       in Section II) by converting them into efficient Octree-based
into the PCN for inference. For each frame, it has to be                          spatial query. To address the memory-intensive bottleneck
down-sampled from million-level, variable-number points into                      in the pre-processing phase, we develop an Octree-Indexed-
thousands-level, fixed number points (e.g., 4096 points per                       Sampling (OIS) method. The OIS method uses an Octree
frame to feed into the input layer of a PCN). Because of                          data structure as a spatial index [25] to organize the point
the large amounts of points in raw point cloud data and                           cloud data in memory. With an Octree pre-organization, down-
expensive nature of down-sampling methods, down-sampling                          sampling can be done with only reading out the desired after-
pre-processing is extremely memory intensive and is a major                       sampled points among raw point cloud data from the memory,
bottleneck in a real-time point cloud application.                                eliminating the need for repetitive access of input and interme-
   As a type of Deep Neural Network (DNN), the backend                            diate data from memory. In the HgPCN architecture, the OIS
PCN inference phase is a computationally intensive task. In                       method is implemented in the Pre-processing Engine shown
recent years, there have been much advance in Deep Learning                       in Figure 1(b), which comprised of the Octree-Build Unit in
Accelerators (DLAs), including commercially available DLAs                        the CPU and the Down-sampling Unit in the accelerator (e.g.,
[2], which perform well in satisfying the computationally in-                     FPGA). The design and implementation of the Pre-processing
tensive inference requirements of traditional DNNs. However,                      Engine are described in detail in Section V.
current DLAs cannot be directly applied to PCN inference.                            For the Inference phase, we develop an Octree-based Voxel-
The reason is, unlike traditional dense data types, points in                     Expanded Gathering (VEG) method to optimize the data
a point cloud are sparsely distributed in a 3D space [9],                         structuring step. In our prototype implementation, the In-
[28]. Thus, before the actual feature computation with MVM                        ference Engine (shown in Figure 1(b)) will make use of
(matrix-vector multiplication), an additional step, data struc-                   a commercially available DLA. It will be enhanced by a
turing, is necessary to adapt the spatial sparsity of the point                   Data Structuring Unit (DSU) to form the PCN Inference
cloud and prepare the input for the following convolutional                       Accelerator. As described in more detail in Section VI, the
layers. The data structuring step is not supported by current                     Data Structuring Unit is based on the VEG method and
DLAs. However, if unaccelerated, this step results in a non-                      fundamentally reduces the workload of the data structuring
trivial part of the total computation of PCN inference. Data                      step as compared against current methods.
structuring before feature computation is the second major                           The initial prototype of HgPCN has been implemented on
bottleneck in a real-time point cloud application.                                an Intel PAC (Xeon+FPGA) platform. The evaluation of the
   Current general-purpose architectures fail to effectively                      HgPCN prototype is performed using four modern point cloud
solve these two bottlenecks of end-to-end point cloud AI                          datasets: ModelNet40 [27], ShapeNet [1], S3DIS [15], and
                    Fig. 2. Illustration of the steps of an end-to-end PCN inference (toy-valued pedestrian classification task).



KITTI [8]. The comparing baseline includes CPU/GPUs, and                       The feature computation step for inference of PCNs in
existing DSA PCN accelerators: PointACC [16], Mesorasi [6].                 Figure 2 makes use of traditional DNNs outputted from the
The evaluation of HgPCN is first performed independently                    data structuring step. The weight will be applied to the input
for the two major phases (from engine-level), then evaluate                 points subset in the MLP hidden layer and the final outputs are
the E2E latency from the HgPCN system-level in the edge                     obtained from the output layer. The feature computation step
computing scenario. The description of the prototype HgPCN                  can be decomposed into MVM, and can be directly accelerated
and its evaluation are detailed in Section VII.                             by existing commercially avaibable DLAs such as NPU [4].
   In Section VIII, we provide the conclusions of our work                     In summary, an end-to-end PCN inference process contains
and a discussion of our future directions.                                  three major steps: down-sampling pre-processing of the raw
                                                                            point cloud data, data structuring, and feature computation.
          II. BACKGROUND AND R ELATED W ORK                                 Among these operations, down-sampling and data structuring
                                                                            are point-cloud-specific operations and have not been fully
A. Point Cloud Data and PCNs
                                                                            optimized. However, these two operations are quite expensive
   A point cloud is a set of points x = {(pk , fk )}, where                 and become major bottlenecks for end-to-end PCN inference.
pk = (xk , yk , zk ) is the coordinate of the k th point, and fk            Expensive downsampling pre-processing. In real-world edge
is the corresponding 1-D feature vector. Unlike a traditional               computing applications, pre-processing is a necessary step in
image, which is a dense 2D pixel-matrix, a point cloud is                   the AI pipeline [23]. Furthermore, for point clouds, the pre-
comprised of numerous spatially distributed points in a 3D                  processing is far more expensive than traditional data types
space. Point clouds are commonly generated by 3D data                       and contributes to a major part of the latency. There are two
acquisition devices (such as LiDAR) and generally are massive               main causes of the high latency. The first is the enormous size
and ever-changing. For example, in the KITTI dataset [8],                   of raw point cloud data. The number of points in raw point
each frame includes approximately (N ∼106 ) to (N ∼107 )                    cloud data is from N ∼105 to N ∼106 . The second reason is
points; and the number of points varies widely between frames.              the expensive down-sampling methods used. For example, the
Current DLAs processing DNNs cannot consume such large                      FPS method samples the raw point cloud iteratively. It starts
and irregular input datasets. Thus, a pre-processing (down-                 by randomly selecting a seed point from the raw point cloud
sampling) step is necessary. As shown in Figure 2, the down-                set (denoted as set C) and putting it in the sampled points set
sampling step is used to decimate the original point cloud                  (denoted as set S). In each iteration, it picks a point from the
into a fixed number of points, while maintaining the spatial                unpicked point set C − S that is the farthest from the sampled
information structure.                                                      point set S and adds it into the sampled point set S, until S
   The down-sampled input point cloud is the input to the                   contains the predefined number of points K. In this way, the
backend PCN inference phase, as shown in Figure 2. Similar                  FPS method will return a subset p1 , · · ·, pk , · · ·, pK from
to the concept of a “stride window” in convolution, weight                  raw points C, such that each pk is the farthest point from the
kernels in PCN will only be applied to a subset of input points             first k − 1 points. K is a selected fixed number (e.g., 4096).
while moving over the entire point set. Because point clouds                We will show later in Section III that the down-sampling pre-
are sparsely scattered in a 3D space, forming the input subset              processing phase is a memory-intensive task and is a huge
of points is far more difficult than traditional dense input                computational workload. In addition, the down-sampling pre-
like images. Unlike the pixels in images, there is no direct                processing phase does not use memory bandwidth efficiently.
neighbor-indexing method in point clouds. Thus, as shown in                 In the FPS method, over 99% of memory accesses are wasted
Figure 2, before the feature computation step, an extra data                because most of these points will be filtered out and not
structuring step is required to form the subset of points as the            be used again after down-sampling. Among all the existing
“input feature map” by using neighbor-gathering methods.                    sampling methods, only the Random Sampling (RS) method
is possibly fast enough to satisfy real-time requirements on        backend, Pointnet++ [22] was used to perform inference on
general-purpose architectures. The RS method simply picks           the Modelnet40 [27], ShapeNet [1], S3DIS [15], and KITTI
K points from the raw point cloud randomly. As a result,            [8] datasets. The end-to-end PCN application was run to
the accuracy of random sampling is low and cannot be fully          obtain a breakdown of the latency. Then, we quantitatively
trusted [10], especially in some safety-oriented applications.      analyze the detailed operations in the two phases to identify
Expensive Data Structuring. As mentioned, in order to use           the bottlenecks which motivate the HgPCN design.
commercially available DLAs, PCNs need to gather point-
subsets, which are in the neighboring set N (xk ) of selected
central points (red points in the Figure 2), to form the
“input feature map” for feature computation. (For example,
in Figure 2, central point A gathers nearest points B, C, D, E,
and F to form the point-subset A.) Unlike pixels in images,
the points in point clouds are scattered over a 3D space in an
irregular manner. There is no direct neighbor-indexing method
to gathering the neighboring set N (xk ). The gathering process
can be achieved by expensive nearest neighbor gathering,
such as KNN (K-nearest-neighbors) and BQ (Ball query). In
traditional methods, neighbor points gathering for each central
                                                                      Fig. 3. End-to-end execution time breakdown (actual time not shown).
point needs to search over the entire input point cloud. Let’s
assume the size of the input point cloud is n, and neighbor         Overall Latency Analysis. Figure 3 shows the percentage of
gathering size is k. For neighbor gathering of each central         the total end-to-end latency spent on the pre-processing phase
point, it is necessary to compute the distances from this central   and inference phase for each of the four example datasets. As
point to every other n−1 points, and pick the top k points with     expected, datasets with larger raw point clouds require more
nearest distances. Each neighbor gathering process of a central     time for pre-processing. From Figure 3, it can be observed that
point replicates these steps and results in a high workload.        the latency of pre-processing is far greater than the latency of
B. Current PCN Accelerators                                         the actual inference. The results confirm that the high cost
                                                                    of the AI tax for point cloud applications is far more serious
   Existing PCN accelerators mainly focus on optimizing the
                                                                    than traditional AI applications [23]. Optimizing only the PCN
expensive data structuring step (second bottleneck), with the       inference phase, as done in existing PCN Accelerators [13], is
assumption the point cloud data is already pre-processed            far from enough to meet the real-time requirements of point
(first bottleneck). Unlike these PCN accelerators, the proposed     cloud applications on the computing edge.
HgPCN will not assume the point cloud data has been pre-
processed. The HgPCN architecture supports efficient online         A. Analysis of Frontend Pre-processing
pre-processing, together with an Inference Engine to supports
efficient end-to-end PCN inference service.                         Bottleneck Identification. The bottleneck in the pre-
   To perform the data structuring step in the inference phase,     processing phase is caused by large data movement, result-
existing PCN accelerators can be divided into two types. The        ing from the large memory footprint of the down-sampling
first type [6], [16] performs data structuring by using accurate    process. One reason is the large input size. In our baseline
neighbor search methods, which returns the same result as           datasets, for every frame of raw point cloud data, KITTI con-
traditional data structuring methods. The second type is to         tains (N ∼106 ), Modelnet40 contains (N ∼105 ), and S3DIS
improve the latency of the data structuring step by using some      contains (N ∼105 ) points, respectively. The second reason is
tree-based methods [5], [20], [29], to perform an approximate       that an excessive amount of intermediate data is generated
neighbor search. As a result, these approximation methods           during the down-sampling phase. As we discussed in Section
require some adaptation in the model training phase [5]. The        II, the FPS algorithm samples the raw point cloud by itera-
data structuring method used in HgPCN results in accurate           tively picking the farthest point. For each iteration, we have to
(not approximate) data structuring, which is compatible with        compute the distances between every point in unpicked points
current training method. Thus, we will compare our inference        set C −S to picked points set S and rank these distances. This
results (in Section VII) to the first type of PCN accelerators.     process exhibits low data locality because all of the computed
                                                                    distances (intermediate data) are written into the memory, and
                      III. M OTIVATION                              then read again after all distances are calculated. For these
  In this section, we identify the key performance bottle-          reasons, the down-sampling phase is a very time-consuming
necks of an end-to-end PCN in general (not considering              memory-intensive task, and the major resource of AI tax.
accelerators). The evaluation was performed using general-
purpose platforms: Intel® Xeon® W-2255 CPU and 4060Ti               B. Analysis of Backend PCN Inference
GPU. For down-sampling in the pre-processing phase, the                As shown in Figure 2, PCN inference contains two separate
most commonly used FPS [3] method was used. For the                 steps: data structuring (DS) and feature computation (FC). The
DS step is used as input preparation for the FC step, which is
the actual convolution (CONV) step.
Bottleneck Identification. The data structuring step consists
of the neighbor search of central points, which can be solved
by algorithms such as k-nearest-neighbors (KNN) or Ball
query (BQ). Even though the neighbor search operations them-
selves do not involve complicated computations, it is still time-
consuming because the neighbor search algorithms operate
over the entire input point cloud. For the data structuring step
of a point-subset of PCNs, we need to compute the distance
from every point to the central point and pick the top k nearest
points by ranking these distances (using the KNN example).
And this data structuring step is replicated for every central
point in the input point cloud. As a result, a large amount of
computation is required by the data structuring steps for each                    Fig. 4. Architecture overview of HgPCN.
feature computation step. A recent work [16] shows that in
PointNet++, approximately 50% of total computation of the           by traversing points in the raw point cloud in a single pass of
inference phase is consumed by the data structuring step and        the data. Additionally, the point cloud data in the Host Memory
becomes another source of AI tax.                                   will be configured (i.e., reorganized) according to the Octree-
   The feature computation in PCNs (i.e., actual inference)         based sequence. The Octree-Table will record the addresses
shares the same requirements as the traditional DNNs and            of the re-arranged points. More details of this process and
can be directly optimized by using DLAs [13]. But the data          how it greatly reduces Host Memory access will be given in
structuring step is unique for PCNs and are not supported           Section V. After the Octree-Table is built, it is transferred to
by existing commercial accelerators. To take advantage of           the Down-sampling Unit in the FPGA.
commercial DLAs to accelerating the feature computation             Preprocessing: downsampling. In Section V, we will intro-
step, the data structuring bottleneck needs to be addressed.        duce an Octree-Indexed-Sampling (OIS) method which will
                                                                    make use of the Octree-Table [17] to perform the down-
                IV. H G PCN A RCHITECTURE
                                                                    sampling process on memory addresses without having to ac-
   Shown in Figure 4 is an overview of the architecture of          cess the Host Memory, greatly reducing the memory-intensive.
HgPCN, which is based on a CPU-FPGA shared memory                   As shown in Figure 4, this down-sampling step is performed
platform (e.g., Intel PAC card [12]). The major system compo-       in the FPGA in the Down-sampling Unit. The output of
nents of the architecture are: (1) CPU, (2) Host Memory, and        the down-sampling is recorded in the Sampled-Points-Table
(3) FPGA. The major architectural components of HgPCN are           (SPT), which contains the addresses of after-sampled points.
(1) Pre-processing Engine (Octree-build Unit in the CPU and         Using these addresses in the SPT, the Down-sampling Unit
Down-sampling Unit in the FPGA) and (2) Inference Engine            can read the after-sampled points directly from Host Memory
(Data Structuring Unit and Feature Computation Unit) in the         and these points provide the input for the inference phase.
FPGA. A key feature of the HgPCN architecture is that both          Inference Engine. The Inference Engine consists of two main
the CPU and FPGA can access the data in the shared Host             modules, the Data Structuring Unit (DSU) and the Feature
Memory. For each frame of the point cloud, we assume the raw        Computation Unit (FCU), both of which are implemented
data from the data sensor (e.g., LiDAR) is collected and stored     in the FPGA. In our prototype implementation, the FCU is
in the Host Memory. Recall from Section I (Figure 1), an end-       a commercially available Deep Learning Accelerator (DLA),
to-end point cloud AI service based on PCNs comprise of two         like the Intel NPU [26]. The DLA will perform inference on
major phases after raw data generation: pre-processing of the       the point-subset as the “input feature map”, which is the output
point cloud data and PCN inference. As will be described in         from the DSU.
more detail later, for the HgPCN, the pre-processing phase             The Data Structuring Unit (DSU) performs a preparation
consists of two steps; (1) an Octree construction & memory          step to produce an input feature map that can be used by
pre-configuration step which is performed using the CPU             existing DLAs. The implementation of the DSU is based
and Host Memory, and (2) a down-sampling step which is              on a Voxel-Expanded Gathering method and fundamentally
performed in the Down-sampling Unit in the FPGA. The PCN            reduces the workload of the data structuring step as compared
inference phase is performed in the Inference Engine in the         to current methods. The details of the DSU and the Voxel-
FPGA. As shown in Figure 4, the Inference Engine consists           Expanded Gathering method will be presented in Section VI.
of a Data Structuring Unit and a Feature Computation Unit.
Pre-processing: Octree construction & memory pre-
configuration For each frame of the point cloud, an Octree is                       V. P RE - PROCESSING E NGINE
constructed in the CPU according to the raw point cloud data           As we discussed in Section III, the pre-processing of a point
and configured into an Octree-Table. The Octree can be built        cloud frame using standard methods is a memory-intensive
Fig. 5. Octree-Indexed-Sampling method overview: (a) A point cloud character “A” (black and color points). Note, for simplicity, that it is a 2D Quadtree
illustration of our Octree-Index-Sampling (OIS) method. An Octree contains two horizontal levels of Quadtrees, having an extra Z dimension. (b) Corresponding
Quadtree representation. For simplicity, only node “11” is fully expanded. As shown, the content of a Quad-tree is stored in a Quadtree-Table in the Down-
sampling Unit; and the raw points corresponding to the Quadtree are pre-configurated in the Host Memory. (c) An example of OIS steps to create the
Sampled-Point-Table, which contains the corresponding Host Memory addresses of the K picked points, where K is a pre-defined number, e.g., 4096.



task. An extremely large amount of input points and inter-                      plus three levels in this example). For each division, the level
mediate data need to be accessed repetitively from memory;                      of the Octree is increased by one and the m-codes [18] of the
and as a result, the computational workload for pre-processing                  subdivided nodes add two more bits at the end (three more
is also extremely heavy. In this section, we introduce an                       bits in the Octree). In these newly added bits, the first bit
Octree-Indexed-Sampling (OIS) method to greatly reduce the                      represents the X-axis, and the second bit represents the Y-axis
memory-intensive bottleneck in the down-sampling process.                       (for an Octree, the third bit represents the Z-axis).
With the OIS method, the HgPCN builds the Octree Table in                          As shown in Figure 5(a), these new-added bits are based
the Octree-build Unit in the CPU and transfer it to the FPGA                    on the Space-Filling Curve (SFC) traversal order [24]. For the
via MMIO. Based on the Octree Table, the down-sampling                          sub-voxels (child-nodes) generated by each subdivision, the
process is performed in the Down-sampling Unit in the FPGA,                     new-added code of each sub-voxel, when compared with its
which directly access the desired after-sampled points from                     parent voxel, is based on its relative position inside the parent
memory. Also in this section, we present how the Down-                          voxel: i.e., the bottom-left quadrant is 00; the top-left is 01;
sampling Unit accelerates the large computational workload                      the bottom-right is 10, and the top-right is 11.
through hardware parallelism.                                                      Shown in Figure 5(b) is a partial representation of the
Octree-Index-Sampling (OIS) Our OIS method is based on                          resulting Quadtree. The root and three levels are color-coded,
a key observation: down-sampling can be based on the relative                   corresponding to Figure 5(a). Every non-empty voxel from
position of a point in a 3D space. For example, most of                         Figure 5(a) will be represented by a node in the Quadtree
the down-sampling methods normalize the point cloud data                        in Figure 5(b). In the final generated Quadtree, each non-leaf
(to produce the relative positions) before the actual down-                     node includes up to four child-nodes (sub-voxels). Each leaf
sampling process. In the OIS method, we will describe how                       node includes the actual points within that node. For example,
we can obtain the relative position information beforehand by                   the leaf node 110011 shown in Figure 5(a) includes one point
using a spatial indexing method. Then we can directly use the                   (11001100) and the leaf node 001101 contains four points.
index to perform the down-sampling without having to access                        As described in [25], Octree can be used as an indexing
the memory to read out the absolute XYZ coordinates.                            method in a spatial database to optimize spatial queries.
                                                                                In HgPCN, the spatial information of each point is stored
A. Octree-build Unit in the CPU                                                 in Host Memory and can be obtained efficiently through an
Octree Construction. The Octree structure is a way to reg-                      Octree-Table lookup. To do so, the point cloud data has to be
ularize the point cloud using voxels. It decomposes the point                   reorganized as described next.
cloud distributed in a 3D space into a 1D-array, where the                      Octree-based Organization in Host Memory. The Octree
leaf level of the generated Octree is the resulting 1D-array.                   construction (as described in the previous section) is a process
Figure 5(a) illustrates the steps of creating a Quadtree (note                  of mapping the voxels (nodes) from a higher dimension (2D in
that a Quadtree, a simplified 2D-version of an Octree, is used                  Quadtree; 3D in Octree) to 1-D linear ordering. This resulting
for illustration). In the beginning, we put the point cloud (e.g.,              1D linear ordering can be naturally mapped to consecutive
point cloud character “A”) into a root-level voxel (the outer                   memory addresses, which is also a 1D array. In Figure 5(b),
black bordered “box” in Figure 5(a)). Then we continuously                      connecting the leaf nodes together from the left-most leaf node
divide each non-empty voxel into sub-voxels (4 “blue” sub-                      (110001) to the right-most leaf node (110101) is the 1D linear
voxels for the example Quadtree in Figure 5(a); 8 sub-voxels                    ordering based on SFC traversal. Based on this 1D order, we
in an actual Octree) until it reaches a pre-defined depth (root                 can construct a 1D array comprised of the points inside the leaf
                                                                          the spatial information (xk , yk , zk ) and feature information
                                                                          (fk ) of that point, as described in the following example.
                                                                          Octree-Indexed-Sampling Algorithm and an Example. Fig-
                                                                          ure 5(c) shows an example of the steps in using the OIS
                                                                          method to achieve the same function as the commonly-used
                                                                          FPS method, but without incurring the cost of repetitive
                                                                          memory accesses. As illustrated in Algorithm 1 in Figure 6, for
                                                                          picking the farthest point as the next picked point, the common
                                                                          FPS method searches every point in unpicked point set (C −S)
                                                                          and compares distances. The OIS method substitutes the
                                                                          operations of Algorithm 1, which work on sparse points, with
                                                                          Octree-Table lookup operations. In the OIS method (Algorithm
                                                                          2 in Figure 6), the step that picks the farthest point is first
                                                                          approximated by picking the farthest voxel. Then within that
                                                                          voxel, pick the farthest of the points inside this voxel according
Fig. 6. Pseudocodes of 1) common method for FPS. 2) OIS method for FPS.
                                                                          to the SFC (Space-Filling Curve) traversal order. In Algorithm
                                                                          1, the for-loop for finding the next point (Line 4 to Line 6)
nodes. In cases when a leaf node contains multiple points (such           iterates N times (N is the number of points in an up-to-million
as Node 110010 or 110100), the intra-node point arrangement               point raw point cloud frame). In contrast, the while-loop in
also follows the SFC traversal. Note again that Figure 5(b)               the Algorithm 2 for finding the next point (Line 3 to Line 11)
is only a partial representation of the example Quadtree from             iterates at most a number equal to the depth of the Octree,
Figure 5(a), thus the 1D array [P1 , P2 , . . . , P8 ] shown at the       which is quite limited.
bottom of Figure 5(b) represents only a part of the complete                 For example, assume that we pick the red point (left bottom-
1D array for this example. The key point is that this 1D-array            most point in Figure 5(a)) as the seed point. As the first
represents the same points in the raw point cloud, but are in             picked point, the seed point is written into the first entry of the
a reorganized sequence.                                                   Sampled-Point-Table, as shown in Figure 5(c). Since this seed
   Next, the resulting 1D array is used to pre-configure the              point belongs to voxel 000000, we want to find the farthest
point cloud data in Host Memory by creating a reorganized                 voxel from 000000. The process is explained as follows.
copy in the memory. Using the simplified example in Fig-                     In the Octree (Figure 5(c)), the seed voxel 000000 belongs
ure 5(b), the points [P1 , P2 , . . . , P8 ] are initially distributed    to voxel 00 in the first level; 0000 in the second level; and
“irregularly” throughout the million-point dataset in Host                000000 in the third (leaf) level. We begin to search for the
Memory. After the Octree-based reorganization process, those              desired farthest voxel of the seed voxel at the first level.
points [P1 , P2 , . . . , P8 ] are stored in consecutive addresses in     At the first level, voxel 000000 belongs to voxel 00 and its
the Host Memory.                                                          farthest first-level voxel is 11. (Note that the distance between
   In summary, the Octree construction and the point cloud                two voxels can be determined by the Hamming distance [19]
data reorganization are performed at the same time by a single            between m-code [11]). Voxel 11 has two child-voxels in the
pass of raw point cloud data. As will be described in the next            second level, 1101 and 1100, of which 1101 is the farther voxel
subsection, through an Octree-Table lookup, we can obtain the             from the 0000. Continuing, voxel 1101 has two child-voxels,
memory address to directly access the spatial information (or             110100 and 110101, of which 110101 is the farther voxel from
characteristics) of the desired point. In contrast, the commonly          000000. Now we reached the leaf and find voxel 110101 as
used FPS method has to read out every point in the raw point              our desired farthest voxel from the seed voxel 000000. For
cloud in Host Memory and searches for the desired next point              the points inside voxel 110101, we pick the farthest point
by a ranking operation.                                                   according to SFC traversal sequence and insert it into the next
                                                                          entry of the Sampled-Point-Table, as shown in Figure 5(c).
B. Down-sampling Unit in the FPGA                                            Continuing, in the standard FPS method [3], when the
Configuration and use of Octree-Table. As shown in Fig-                   picked points set S contains more than one point (two points
ure 5(b), the generated Octree will be configured into an                 in this example thus far), the seed point for the next iteration
equivalent Octree-Table, to be transferred to and used by                 will be the Euclidean norm of S (i.e., a virtual summary point
the Down-sampling Unit in the FPGA. In the Octree, the                    to represent S), denoted by ||S||2 . The OIS method follows the
leaf nodes contain the address (or address range) of the                  same approach and repeats the steps of picking the subsequent
contained point(s). In the Down-sampling Unit, the Sampling               farthest points from ||S||2 until the size of the point set N(S)
Modules perform (in parallel) the down-sampling task by                   reaches the pre-defined number K.
performing Octree-Table lookup operations. When a Sampling                Hardware Acceleration Figure 7(a) shows the details the
Module reaches a leaf node during an Octree-Table lookup, the             Sampling Modules of the Down-sampling Unit in HgPCN.
Down-sampling Unit can determine the address containing the               Each Sampling Module has two inputs: m-codes of the as-
desired point and access the Host Memory directly to obtain               signed voxel and m-codes of the seed voxel. A Sampling
                                                                   an input feature map; without which the inference cannot be
                                                                   supported by current commercially available DLAs. Thus, as
                                                                   shown in Figure 8, the HgPCN Inference Engine consists of
                                                                   a Data Structuring Unit (DSU) and Feature Computation Unit
                                                                   (FCU). The FCU is a commercially available Deep Learning
                                                                   Accelerator (DLA) which implements a classic systolic array
                                                                   design. The Data Structuring Unit (DSU) is a custom-designed
                                                                   module to optimize the expensive data structuring step and will
                                                                   be described below.
                                                                   Data Structuring Unit Most existing PCN accelerators [6],
                                                                   [16] accelerate this step mainly from a hardware perspective,
                                                                   e.g., using parallel execution. In this paper, we propose a
          Fig. 7. Hardware design of voxel-level parallelism.      Voxel-Expanded Gathering (VEG) method, in which opti-
                                                                   mization is achieved through algorithm and hardware co-
                                                                   design. The VEG method utilize the spatial indexing nature of
Module calculates the Hamming distance between the two             Octree to algorithmically minimize the required computational
m-codes using an efficient XOR operation [19]. The outputs         workload in the data structuring (DS) step, and then further
from the Sampling Modules are simultaneously inputted into         accelerate through parallel execution. Furthermore, the VEG
a bitonic sorter (not shown) to select the node with the largest   method can efficiently support commonly used DS methods,
Hamming distance as the next searched node.                        e.g., KNN (K-Nearest-Neighbors) and BQ (Ball Query). In
   To further accelerate the OIS-based down-sampling phase,        this section, we will use KNN as the example.
the Down-sampling Unit deploys multiple Sampling Modules           Traditional Method for Input Feature Map. After the down-
based on voxel-level parallelism. Figure 7(b) shows the steps      sampling step, current PCNs typically pick a fixed number
of OIS-based FPS using voxel-level parallelism. At any given       of central points (e.g., red points in the input point cloud of
time, eight Sampling Modules are each assigned one of these        Figure 2) from the down-sampled input point cloud. The data
child-nodes to find the farthest node in parallel.                 structuring step is used to form the subset of points as the
Summary: In summary, to efficiently perform point                  “input feature map” by gathering nearby points of these central
cloud down-sampling, HgPCN employs an Octree-Indexed-              points. For the data structuring step of each central point,
Sampling (OIS) method by utilizing the Octree-based spatial        current PCN accelerators search all the points from a (down
queries to directly access the target points in Host Memory. To    sampled) input point cloud, and then calculate the distance
do so, HgPCN first constructs an Octree-Table and performs a       from every other point to the center point. Next, they sort
pre-configuration step by reorganizing the point cloud data        and pick out the nearest K neighbor points from input point
in the Host Memory. In our prototype HgPCN, these two              cloud. For example, if we assume the down-sampled input
processes are performed simultaneously in the Octree-build         point cloud (after the processing phase) has 4096 points, and
Unit (in the CPU) with a single pass of the raw point cloud        KNN needs to gather the K = 32 nearest points from the
data. After that, the Down-sampling Unit of HgPCN (in the          central point. With traditional methods, they need to compute
FPGA) will perform the down-sampling by directly obtaining         4095 distances from the central point to every other point, and
the memory addresses of the desired after-sampled points           select the top K = 32 points with shortest distances. In this
from the Octree-Table. In this manner, HgPCN greatly reduces       process, most of the 4095 distance calculation (except for the
the memory-intensive bottleneck of the pre-processing phase        32 nearest neighbors) can be regarded as wasted.
by indexing the 3D relative position of points with the 1D         Voxel-Expanded Gathering (VEG) Method. In the VEG
memory address. To further accelerate the OIS-based down-          method, before the actual sorting step, we first narrow the
sampling phase, HgPCN exploits voxel-level parallelism by          range of nearest point search by adjacent-indexing through
using multiple Sampling Modules in the Down-sampling Unit.         the use of an Octree. The standard Octree neighbor-search
   Finally, the OIS method also provides a significant saving      operation [7] is used to search the voxels adjacent to a central
of on-chip memory in the FPGA. This is important because it        point’s voxel. More details of the steps of VEG method
allows the FPGA to have enough on-chip memory to support           are discussed below. The HgPCN Data Structure Unit can
the accelerators for the two phases in HgPCN (down-sampling        execute multiple Octree neighbor search operations in parallel
and inference) within one device. In Section VII, we will          to search all adjacent voxels at the same time.
evaluate and analyze of the on-chip memory-saving benefit          Architectural Support In HgPCN. As shown in Figure 8,
provided by the OIS method.                                        the design of the HgPCN Data Structuring Unit consists of
                                                                   six pipeline stages:
                    VI. I NFERENCE ENGINE
                                                                      • 1) Fetch Central Points (FP): Fetch a central point and
  As we discussed in Section III, for point cloud inference,             the corresponding m-code [18] of this point.
before the actual feature computation step in the Inference           • 2) Locate Central Voxel (LV): Locate the voxel that
Engine, an expensive data structuring step is required to form           contains the central point Vseed (yellow voxel shown in
                                                 Fig. 8. Inference Engine overview.



    Figure 8 (2 and 3)).                                           they are definitely among the 32 nearest neighbors. Existing
  • 3) Voxel Expansion (VE): At the first level of voxel           methods usually compute the distance from the central point
    expansion (V 1), neighboring voxels touching the central       to every other point in the 4096 input points, and pick the
    voxel Vseed are included (the gray voxels in Figure 8 (3)).    32 nearest points. With the VEG method, the computation
    Voxel expansion continues outward and include the next         workload (of sorting and picking the nearest points) is reduced
    level of touching voxels (the green voxels are included        from 4095 to N2 .
    for the second expansion V2 ). Voxel expansion continues
                                                                                                VII. E VALUATION
    until the total number of points included in the expanded
    voxels is at least K.                                          A. Evaluation Setup
  • 4) Gather Points (GP): Let’s assume that there are             Implementation Method A prototype of HgPCN has been
    N0 points in the initial voxel Vseed and that we need          implemented on an Intel PAC, a shared memory CPU-FPGA
    n expansions to collect enough points (>= K). Also,            (Intel Arria 10 GX) platform. The accelerators are imple-
    assume there are N1 points in gathered voxels by first         mented on the FPGA side with SystemVerilog and VHDL,
    expansion V1 ; and Nn points in gathered voxels by the         and the software implemented on the CPU side with C++.
    n-th expansion Vn . Gather the points from Vseed to Vn−1       Benchmark datasets As shown in Table I, four common
    (i.e., total of N0 + N1 ... + Nn−1 points).                    point cloud datasets (for four applications), with different raw-
  • 5) Sort (ST): Sort the points in last voxel set Vn and pick    dataset frame sizes, were selected as our benchmarks.
    the K - sum(N0 to Nn−1 ) points with nearest distances
    from the central point. Together with the N0 + N1 ... +                                           TABLE I
    Nn−1 points, the K nearest points are found.                                              E VALUATION B ENCHMARKS
  • 6) Buffering (BF): Output the gathered K points, includ-              Application             Dataset    input Size      PCN Model
    ing the coordinates (pk ) and feature information (fk ), to       Object Classification     ModelNet40      1024      Pointnet++(c) [22]
                                                                       Part Segmentation         ShapeNet       2048      Pointnet++(ps) [22]
    the input buffer for the feature computation step.                Indoor Segmentation         S3DIS         4096      Pointnet++(s) [22]
   The above steps are repeated for each central point.               Outdoor Segmentation        KITTI        16384      Pointnet++(s) [21]
Example: Assume we need to gather the 32 nearest neighbors
for the central point (red point) in Figure 8.                     Baseline devices and accelerators for comparison General-
                                                                   purpose devices used in our comparison include the Intel®
  • The voxel (yellow) that contains the red point is the seed
                                                                   Xeon® W-2255, Nvidia Jetson GPU, and 4060ti GPU. These
    voxel Vseed . Let’s assume there are N0 points gathered
                                                                   general-purpose devices are used for end-to-end comparison,
    in Vseed and that N0 < 32.
                                                                   including the pre-processing phase and the inference phase.
  • The voxel search is expanded outward from Vseed and
                                                                      The second type of baseline hardware for comparison is
    includes first-level neighbor voxels of V1 (grey voxels in
                                                                   against existing PCN accelerators, Mesorasi [6] and PointACC
    Figure 8). Assume there are N1 points in the set of grey
                                                                   [16], with 16 × 16 systolic arrays for the feature computation
    voxels and that N0 + N1 < 32. These N1 points are
                                                                   step. Because these PCN accelerators do not include the pre-
    gathered in V1 .
                                                                   processing phase (i.e., not end-to-end), we will perform the
  • Since there are still < 32 in the gathered voxels, we
                                                                   comparison only on the PCN inference phase.
    continue to include the second-level neighbor voxels
    (green voxels). Let’s assume there are N2 points in the        B. Analysis of the OIS method on CPU
    set of green voxels. These N2 points are gathered in V2 .        As stated, for the FPS down-sampling algorithm, the OIS
  • Let’s assume that N0 + N1 + N2 > 32, then pick the top         method converts original operations (Algorithm 1 in Figure 6)
    nearest (32 − N0 − N1 ) points in V2 and combine them          on sparse points into efficient Octree lookup operations (Al-
    with N0 and N1 to form the 32 nearest neighbors.               gorithm 2), optimizing the memory-intensive problem of the
  Note that it is unnecessary to sort or compute the distances     pre-processing phase. To demonstrate the inherent advantage
of points in N0 (from Vseed ) and N1 (from V1 ), because           of the OIS method against the common FPS method, we first
           Fig. 9. Memory-access saving from OIS-method.                        Fig. 10. latency speedup from OIS-method on CPU.



evaluated these algorithms running on the same CPU (for OIS,         among the raw point cloud data, OIS can greatly reduce the
both Octree-build Unit and Down-sampling Unit are on CPU).           amount of memory access (as demonstrated in Figure 9).
Benefits of the OIS method. Both Algorithm 1 (common                 Furthermore in HgPCN, the VEG method (for DS step) can
FPS-method) and Algorithm 2 (OIS method) were run on an              reuse the built Octree to amortize the overhead.
Intel® Xeon® W-2255 CPU, using point cloud frames with
different sizes. The results are shown in Figure 9 and 10. In
both figures, the x-axis shows the different point cloud frames.
Labels beginning with “MN.” are frames from the ModelNet40
dataset and the label “kitti.avg” represent a frame from the
KITTI database of the average size. Note that for Shapenet,
the raw data size is smaller than 4096 points, so it doesn’t
include a column for down-sampled to 4096 points. For the
y-axis, 6000× means the OIS method requires 6000 times less
memory accesses than FPS method.                                               Fig. 11. Octree-build overhead of OIS-based sampling.
   As shown in Figure 9, memory-access saving for these
benchmarks ranges from 1700× to 7900× for point cloud
frames of different sizes. This result is consistent with the the-   C. Pre-processing Engine Performance
oretical analysis of memory-access saving between Algorithm            In Figure 12, we first compare the OIS-on-CPU (software
1 and Algorithm 2 in Figure 6.                                       implementation on CPU) vs OIS-on-HgPCN to illustrate the
   Figure 10 shows the overall measured latency improvement          speedup resulting from the hardware Down-sampling Unit. As
of the OIS-based method, as a result of the memory-access            seen from the first two columns in Figure 12, compared with
savings. The OIS-based sampling outperforms the common               OIS-on-CPU, the OIS-on-HgPCN can provide a from 1.2×
FPS method with 800× to 7500× speedup.                               to 4.1× speedup. This speedup stems from the reason that
Overhead from Octree Build. As discussed in Section V                the hardware Down-sampling Unit (described in Section V-B)
(Pre-processing Engine section) and shown in Algorithm 2, the        can achieve a 5.95× to 6.24× speedup compared to the CPU-
Octree-indexed Sampling method requires an initial overhead          implemented Down-sampling Unit.
of building the Octree. Figure 11 details the overhead for
different point cloud datasets and frame sizes. The Octree-
construction overhead can range from 0.25 to 0.8 of the total
latency of the OIS method when implemented only on CPU.
In OIS-based sampling (on CPU), most of the latency and
memory accesses are from the Octree-construction, because
the OIS method must go through the entire raw point cloud
data once to build the Octree. Note also, that the latency of OIS
is impacted by two factors: the number of points in the point
cloud and the depth of the Octree. The Octree-construction           Fig. 12. Latency comparison of Pre-processing Engine against the baselines
latency is determined by the number of points in a point cloud,
and the speed of Octree-search is linearly related to the Octree        In the remainder of Figure 12, we compared the runtime
depth. The Octree depth is influenced by the non-uniformity          latency of HgPCN’s Pre-processing Engine with three existing
of a point cloud [14]. In Figure 11, even though MN.piano            sampling methods on the baseline devices where they can
and MN.plant contain almost the same number of points, the           get the best performance (CPU or GPU). (To our best of
spatial distribution of MN.piano is more non-uniform than            knowledge, there is no existing DSA for point cloud pre-
that of MN.plant, resulting in a deeper Octree for MN.piano.         processing). Among these sampling methods, FPS is the most
Unlike traditional methods, which require repetitive searching       commonly used because it results in the least information loss.
However, it has the highest runtime latency. Random sampling       not end-to-end); thus we can only perform the comparison on
(RS) has the lowest runtime latency, but is not generally          the PCN inference phase. Also, because the Mesorasi PCN
used because it has the highest information loss. In some          accelerator uses a random picking method to generate central
existing methods [10], a reinforcement process uses an encoder     points for the data structuring step, we will do the same for
architecture to enhance RS (as RS+reinforce). But this method      the GPU, PointACC, and HgPCN to ensure a fair comparison.
is not universal. It can only be applied to PCNs with encoder-     The evaluation results are shown in Figure 14, in which the
decoder architectures.                                             x-axis shows the four PCN tasks from Table I and the y-axis
   In Figure 12, the total latency of the Octree-indexed-          shows the speedup of HgPCN against the baselines.
sampling (OIS) method on HgPCN (light-blue columns) is                As shown in Figure 14, when compared to Nvidia Jetson
shown. This latency includes the CPU-end (Octree-build Unit)       NX GPU (blue columns), HgPCN achieves from 6.4× to
and the FPGA-accelerator-end (Down-sampling Unit). The             21× speedup. When compared to Mesorasi (grey columns),
results show that the OIS method on HgPCN possesses the            based on the same systolic array architecture for the feature
best advantages of the tested sampling methods. Although the       computation step, HgPCN achieves 2.2× to 16.5× speedup.
performance of OIS on HgPCN is a little slower than random         We noted that Mesorasi uses a GPU to perform the data struc-
sampling, it can achieve the same accuracy as the FPS method.      turing step, which still occupies a major part of the latency.
Unlike RS+reinforce, it is universal to all types of point cloud   Even though Mesorasi tries to overlap the data structuring and
networks. Furthermore, compared to the FPS method, HgPCN           feature computation, the inference speed is still largely limited
offers a more consistent latency for different sizes of point      by the latency of the data structuring step.
cloud frames, providing better tail latency for edge computing.
On-chip memory-saving with the OIS method. Reducing
on-chip memory usage in an FPGA is important: 1) to free
up resources to implement other parts of the application and
2) to have the opportunity to keep an entire implementation
on a single FPGA. Figure 13 shows the memory-consumption
comparison between the FPS method and the OIS method:
OIS can provide from 12× to 22× memory-saving.


                                                                            Fig. 14. Speedup of HgPCN over baseline hardware.

                                                                      When compared to PointACC (orange columns), HgPCN
                                                                   achieves 1.3× to 10.2× speedup. Like HgPCN, PointACC
                                                                   also developed a customized unit (Mapping Unit) for the data
                                                                   structuring step. Like HgPCN’s DSU, the Mapping Unit of
                                                                   PointACC also uses a bitonic sorter to accelerate the ranking
                                                                   process. However, while based on the same bitonic sorter
     Fig. 13. On-chip memory-saving benefit from the OIS method.   approach to select the top K nearest points, the HgPCN can
                                                                   outperform PointACC because of the fundamentally reduced
   In our prototype implementation of HgPCN, the device used
                                                                   workload resulting from the VEG method. Through the VEG
is the Intel Arria 10 GX 1150 FPGA, which has 65Mb total on-
                                                                   method, HgPCN narrows the range for searching the nearest
chip RAM. From our evaluation, if the raw point cloud frame
                                                                   points. In Figure 15, we compare HgPCN against PointACC
contains more than ∼5×105 points and is stored in the FPGA
                                                                   to show the benefits of the VEG method. Like other current
memory (as in the case of FPS), the input points and generated
                                                                   existing methods, the searched range of PointACC’s bitonic
intermediate data during the pre-processing phase will exceed
                                                                   sorter is over the entire input point cloud. In contrast, the
the total capacity (65Mb) of on-chip memory. In this case,
                                                                   required workload of the bitonic sorter in HgPCN’s Data
there will be no space for the Inference Engine. With the OIS
                                                                   Structuring Unit only sorts the points in Nn (from the last
method, only the Octree-Table needs to be stored in the on-
                                                                   expansion Vn in Figure 8). Thus, the required workload of the
chip memory, along with minimum amount of intermediate
                                                                   bitonic sorter in HgPCN’s DSU is fundamentally less than that
data. Even if raw point cloud frames contain ∼1 × 106 points,
                                                                   of PointACC’s Mapping Unit. Figure 15 shows the workload
the OIS method only consumes approximate 10 Mb of memory
                                                                   reduction due to the benefits of the VEG method. Note that
in the pre-processing phase.
                                                                   for PCN tasks with larger input sizes, the VEG method can
D. Evaluation of Inference Engine                                  achieve an even greater level of workload reduction. Figure 16
                                                                   shows the latency breakdown of the VEG method.
  In this section, we will present the evaluation results of the
HgPCN Inference Engine against a GPU accelerator and two           E. Evaluation of System-level HgPCN
PCN accelerators: PointACC and Mesorasi. Recall that these            In previous subsections, we analyzed separately the de-
PCN accelerators do not include the pre-processing phase (i.e.,    tails of the performance of HgPCN in the pre-processing
                                                                  A. Discussion and Future Directions
                                                                  Practical Implications Note that even though both the OIS
                                                                  method in the pre-processing phase and the VEG method in
                                                                  the inference phase are based on the Octree structure, they
                                                                  are independent methods and can be used independently. For
                                                                  example, the HgPCN Pre-processing Engine can be a plug-
      Fig. 15. VEG benefit.         Fig. 16. VEG breakdown.       in to other PCN inference accelerators (not using the VEG
                                                                  method) to perform the end-to-end PCN inference. Similarly,
                                                                  the HgPCN Inference Engine (with VEG method) can be used
phase (heterogeneously implemented on the CPU side and the        as an independent PCN inference accelerator. In addition, the
FPGA-accelerator side) and the inference phase (on FPGA           OIS and VEG are not only limited to PCN tasks. OIS is appli-
side). Here, we combine these two phases to evaluate the E2E      cable to other non-AI point cloud applications (e.g., AR/VR)
system-level performance of HgPCN in an edge computing            which require down-sampling of the raw point cloud data.
scenario. For this evaluation, the KITTI dataset is used as our   VEG can be used for other point cloud applications [29] which
benchmark dataset because each frame of KITTI is associated       requires neighbor gathering. Also note that the implementation
with a time-label of frame generation, from which we can          of these two methods for point cloud nets is not limited to a
determine the maximum generation rate of KITTI data frames.       CPU-FPGA platform (as in this paper). The proposed methods
   Let’s assume the point cloud frames are generated in real-     are also can be implemented on other accelerators, such GPU
time frame by frame. Our definition of meeting real-time re-      and CGRA (Coarse-Grained Reconfigurable Arrays).
quirement is the end-to-end processing of each frame can keep        Finally, compared to images, the analysis and processing of
up with the sampling (data generation) rate. For the KITTI        point cloud data is a relatively unexplored area. The arrange-
dataset, it was shown that HgPCN can process 16 average           ment methods of image pixels (e.g., row-major or column-
frames per second (FPS). For the KITTI dataset, according         major) are well-developed, with which a 1D-address can be
to the time label of frame-generation, we can determine the       used as the index to a 2D-coordinate of a pixel. However, to
maximum generation rate of KITTI data frames is less than         our best of Knowledge, OIS is the first method that can be used
16 frames per second. Thus, we can conclude that HgPCN can        to index the 3D-coordinate with 1D-address (by arranging the
keep up with the data generation rate of KITTI to meet the        points). In HgPCN, we demonstrated the benefit of the OIS
real-time requirement of such an application.                     method and believe it can inspire future works to propose other
                                                                  efficient arranging methods for point cloud.
          VIII. C ONCLUSIONS AND D ISCUSSION
                                                                  Future Direction of OIS: Approximate OIS-based FPS.
   Latency for end-to-end processing of the point cloud           Based on OIS, the current prototyped HgPCN accelerates
workloads stems from two reasons: memory-intensive down-          the commonly used down-sampling method, farthest-point
sampling in the pre-processing phase and the data structuring     sampling (FPS). Considering the approximate nature of DNNs,
step for input preparation in the inference phase. In this        an approximate OIS-based FPS method can be explored to
paper, we presented HgPCN, an end-to-end heterogeneous            further enhance the speed while potentially causing only
architecture for real-time embedded point cloud application. In   marginal information loss. In the current OIS-based FPS
HgPCN, we utilize the space-indexing function of the Octree       method, the accurate farthest point is identified and added to
structure to develop two novel methodologies to address the       the sampled point set S by first finding the farthest leaf node
two identified bottlenecks and accelerate them by using a het-    and then selecting the farthest point within that node. With
erogeneous CPU-FPGA implementation. In the Pre-processing         the approximate FPS method, instead of finding the accurate
Engine of HgPCN, an OIS method is used to optimize                farthest point, we can randomly pick a point contained by
the memory-intensive down-sampling bottleneck of the pre-         current accessed node once Octree search is near leaf level.
processing phase. In the Inference Engine, HgPCN extends          Because the randomly picked point belongs to the same node
a commercial DLA with a customized Data Structuring Unit          as the actual farthest point, it is spatially adjacent to the actual
which is based on a VEG method to fundamentally reduce the        farthest point and can serve as an approximate substitute being
workload of the data structuring step in the inference phase.     added to the sampled points set S. We will explore the tradeoff
   Our results from the previous section showed that for          between the enhance performance of OIS (by reducing the
the inference phase, depending on the dataset size, HgPCN         number of Octree search operations) vs. the lost in accuracy.
achieves speedup from 1.3× to 10.2× vs. PointACC, 2.2×            Future Direction of VEG: Semi-approximate Data Struc-
to 16.5× vs. Mesorasi, and 6.4× to 21× vs. Jetson Xavier          turing. As we introduced in Section II, unlike the accu-
NX GPU. Along with optimization of the memory-intensive           rate Data Structuring (DS) method used by HgPCN, some
down-sampling bottleneck in pre-processing phase, the overall     PCN accelerators [5], [20] opt for the approximate method
latency shows that HgPCN can provide the capability of            to accelerate the PCN data structuring step. which requires
satisfying real-time requirement by keeping up with the raw       extra adaption in training. Based on the VEG method, a
data generation rate of a benchmark application.                  semi-approximate VEG method can be explored as a future
direction, positioned in the middle between accurate and                           [14] S. Laine and T. Karras, “Efficient sparse voxel octrees-analysis, ex-
approximate data structuring methods. This semi-approximate                             tensions,” and Implementation. Technical report, NVIDIA, Tech. Rep.,
                                                                                        2010.
VEG method aims to integrate the benefits of both methods.                         [15] L. Landrieu and M. Simonovsky, “Large-scale point cloud semantic
   In the current VEG method, the majority of nearest points                            segmentation with superpoint graphs,” in Proceedings of the IEEE
are gathered directly from the points included in voxel ex-                             conference on computer vision and pattern recognition, 2018, pp. 4558–
                                                                                        4567.
pansions, except for the final voxel expansion. The points                         [16] Y. Lin, Z. Zhang, H. Tang, H. Wang, and S. Han, “Pointacc: Efficient
collected in the final voxel expansion need to be sorted to                             point cloud accelerator,” in MICRO-54: 54th Annual IEEE/ACM Inter-
select the rest of the nearest points. This sorting process                             national Symposium on Microarchitecture, 2021, pp. 449–461.
                                                                                   [17] D. Madeira, A. Montenegro, E. Clua, and T. Lewiner, “Gpu octrees
contributes to most of the computational workload for VEG.                              and optimized search,” in Proceedings of VIII Brazilian Symposium on
With a semi-approximate VEG, the rest of nearest points                                 Games and Digital Entertainment, 2009, pp. 73–76.
gathered in the final voxel expansion can be approximated                          [18] G. M. Morton, “A computer oriented geodetic data base and a new
                                                                                        technique in file sequencing,” 1966.
by randomly picking points. Among the results of the semi-                         [19] M. Norouzi, D. J. Fleet, and R. R. Salakhutdinov, “Hamming distance
approximate VEG, most of the gathered points are accurate,                              metric learning,” Advances in neural information processing systems,
and the points from the final expansion can serve as substitutes                        vol. 25, 2012.
                                                                                   [20] R. Pinkham, S. Zeng, and Z. Zhang, “Quicknn: Memory and perfor-
for the accurate points because they are spatially adjacent.                            mance optimization of kd tree based nearest neighbor search for 3d point
This way, the semi-approximate VEG can further accelerate                               clouds,” in 2020 IEEE International symposium on high performance
the current VEG and potentially without the need for training                           computer architecture (HPCA). IEEE, 2020, pp. 180–192.
                                                                                   [21] C. R. Qi, W. Liu, C. Wu, H. Su, and L. J. Guibas, “Frustum pointnets
adaptation.                                                                             for 3d object detection from rgb-d data,” in Proceedings of the IEEE
                                                                                        conference on computer vision and pattern recognition, 2018, pp. 918–
                                                                                        927.
                               R EFERENCES                                         [22] C. R. Qi, L. Yi, H. Su, and L. J. Guibas, “Pointnet++: Deep hierarchical
                                                                                        feature learning on point sets in a metric space,” Advances in neural
 [1] A. X. Chang, T. Funkhouser, L. Guibas, P. Hanrahan, Q. Huang, Z. Li,               information processing systems, vol. 30, 2017.
     S. Savarese, M. Savva, S. Song, H. Su et al., “Shapenet: An information-      [23] D. Richins, D. Doshi, M. Blackmore, A. Thulaseedharan Nair, N. Patha-
     rich 3d model repository,” arXiv preprint arXiv:1512.03012, 2015.                  pati, A. Patel, B. Daguman, D. Dobrijalowski, R. Illikkal, K. Long,
 [2] Y. Chen, Y. Xie, L. Song, F. Chen, and T. Tang, “A survey of accelerator           D. Zimmerman, and V. Janapa Reddi, “Missing the forest for the
     architectures for deep neural networks,” Engineering, vol. 6, no. 3, pp.           trees: End-to-end ai application performance in edge data centers,” in
     264–274, 2020.                                                                     2020 IEEE International Symposium on High Performance Computer
 [3] Y. Eldar, M. Lindenbaum, M. Porat, and Y. Y. Zeevi, “The farthest point            Architecture (HPCA), 2020, pp. 515–528.
     strategy for progressive image sampling,” IEEE Transactions on Image          [24] U. Sakoglu, L. Bhupati, N. Beheshti, N. Tsekos, and L. Johnsson, “An
     Processing, vol. 6, no. 9, pp. 1305–1315, 1997.                                    adaptive space-filling curve trajectory for ordering 3d datasets to 1d:
 [4] H. Esmaeilzadeh, A. Sampson, L. Ceze, and D. Burger, “Neural accel-                Application to brain magnetic resonance imaging data for classification,”
     eration for general-purpose approximate programs,” Communications of               in Computational Science–ICCS 2020: 20th International Conference,
     the ACM, vol. 58, no. 1, pp. 105–115, 2014.                                        Amsterdam, The Netherlands, June 3–5, 2020, Proceedings, Part III 20.
 [5] Y. Feng, G. Hammonds, Y. Gan, and Y. Zhu, “Crescent: taming memory                 Springer, 2020, pp. 635–646.
     irregularities for accelerating deep point cloud analytics,” in Proceedings   [25] B. Schön, A. S. M. Mosa, D. F. Laefer, and M. Bertolotto, “Octree-based
     of the 49th Annual International Symposium on Computer Architecture,               indexing for 3d pointclouds within an oracle spatial dbms,” Computers
     2022, pp. 962–977.                                                                 & Geosciences, vol. 51, pp. 430–438, 2013.
 [6] Y. Feng, B. Tian, T. Xu, P. Whatmough, and Y. Zhu, “Mesorasi:                 [26] J. Song, Y. Cho, J.-S. Park, J.-W. Jang, S. Lee, J.-H. Song, J.-G. Lee,
     Architecture support for point cloud analytics via delayed-aggregation,”           and I. Kang, “7.1 an 11.5 tops/w 1024-mac butterfly structure dual-
     in 2020 53rd Annual IEEE/ACM International Symposium on Microar-                   core sparsity-aware neural processing unit in 8nm flagship mobile soc,”
     chitecture (MICRO), 2020, pp. 1037–1050.                                           in 2019 IEEE International Solid-State Circuits Conference-(ISSCC).
 [7] S. F. Frisken and R. N. Perry, “Simple and efficient traversal methods             IEEE, 2019, pp. 130–132.
     for quadtrees and octrees,” Journal of Graphics Tools, vol. 7, no. 3, pp.     [27] J. Sun, Q. Zhang, B. Kailkhura, Z. Yu, C. Xiao, and Z. M. Mao,
     1–11, 2002.                                                                        “Modelnet40-c: Arobustness benchmark for 3d point cloud recognition
 [8] A. Geiger, P. Lenz, C. Stiller, and R. Urtasun, “Vision meets robotics:            under corruption.”
     The kitti dataset,” The International Journal of Robotics Research,           [28] Q. Xu, X. Sun, C.-Y. Wu, P. Wang, and U. Neumann, “Grid-gcn for fast
     vol. 32, no. 11, pp. 1231–1237, 2013.                                              and scalable point cloud learning,” in Proceedings of the IEEE/CVF
 [9] Y. Guo, H. Wang, Q. Hu, H. Liu, L. Liu, and M. Bennamoun, “Deep                    Conference on Computer Vision and Pattern Recognition, 2020, pp.
     learning for 3d point clouds: A survey,” IEEE transactions on pattern              5661–5670.
     analysis and machine intelligence, vol. 43, no. 12, pp. 4338–4364, 2020.      [29] T. Xu, B. Tian, and Y. Zhu, “Tigris: Architecture and algorithms for
[10] Q. Hu, B. Yang, L. Xie, S. Rosa, Y. Guo, Z. Wang, N. Trigoni, and                  3d perception in point clouds,” in Proceedings of the 52nd Annual
     A. Markham, “Randla-net: Efficient semantic segmentation of large-                 IEEE/ACM International Symposium on Microarchitecture, 2019, pp.
     scale point clouds,” in Proceedings of the IEEE/CVF conference on                  629–642.
     computer vision and pattern recognition, 2020, pp. 11 108–11 117.
[11] R. H. Hunter, B. C. White, R. R. Patel, and J. R. Ballard, “Using morton
     codes to partition faceted geometry: an architecture for terabyte-scale
     geometry models,” 2020.
[12] C. Jiang, D. Ojika, T. Kurth, S. Vallecorsa, B. Patel, and H. Lam, “Accel-
     eration of scientific deep learning models on heterogeneous computing
     platform with intel® fpgas,” in High Performance Computing: ISC High
     Performance 2019 International Workshops, Frankfurt, Germany, June
     16-20, 2019, Revised Selected Papers. Springer, 2019, pp. 587–600.
[13] N. P. Jouppi, C. Young, N. Patil, D. Patterson, G. Agrawal, R. Bajwa,
     S. Bates, S. Bhatia, N. Boden, A. Borchers et al., “In-datacenter
     performance analysis of a tensor processing unit,” in Proceedings of the
     44th annual international symposium on computer architecture, 2017,
     pp. 1–12.
