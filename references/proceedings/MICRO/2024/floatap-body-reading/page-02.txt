different FP formats while maintaining high utilization and                   Value               Description                         Representation
integer compatibility. Moreover, FloatAP remains highly pro-                  Normal Value        1.M×2E−127 × (-1)S                  0<E<255, M ̸= 0
grammable using RISC-V ‘V’ vector extensions and easily in-                   ±∞                  Infinity, e.g., -7÷0, ∞+5           E = 255, M = 0
tegrates with domain-specific languages and ML frameworks.
                                                                              NaN                 Not a number, e.g., 0÷0, ∞×0        E = 255, M ̸= 0
   We evaluate FloatAP using contemporary ML workloads,
                                                                              ±0                  Zero with sign                      E = 0, M = 0
including CNN-, RNN-, and LLM-based parallel codes.
The results show that, on average, FloatAP achieves 2.7×                      Denormal Value      0.M×2−126 × (-1)S                   E = 0, M ̸= 0
higher inference throughput compared to an area-equivalent                       TABLE I: IEEE 754 single-precision FP values and representation.
NVIDIA A100 Tensor Core GPU for single-precision FP
(1.5× for bfloat16) and 2.4× higher energy efficiency (1.6×
for bfloat16).                                                               encoding [42]1 . From low- to high-order bits, it consists
   The contributions of this paper are as follows:                           of 23 bits of mantissa, 8 bits of exponent, and the sign
   • To the best of our knowledge, this is the first published               bit. Conventionally, the mantissa only physically stores the
     work that discusses associative FP arithmetic in detail,                fractional part to the right of the radix point; the 0/1 value to
     including how to realize FP vector operations using CAM                 the left of the radix point always equals ‘1’ and is implicit.
     arrays, including addition, maximum, reduction, mul-                    Therefore, the 23-bit mantissa actually represents 24 bits. In
     tiplication, dot-product, and conversion between two’s                  Figure 1, the mantissa field is ‘101’, so the mantissa equals to
     complement and signed format (needed to support IEEE                    1.101 in base 2. Note that the mantissa itself is positive and
     754). Our approach does not rely on precalculated LuTs,                 not represented using two’s complement.
     approximation, or quantization.                                            The 8-bit exponent field is an unsigned integer; the actual
   • We leverage existing bit-sliced AP designs to realize                   index value equals the exponent minus an implicit constant
     fast mantissa alignment, which helps reduce the time                    bias. The bias equals (2e − 1) if the exponent field has e bits
     complexity of FP add and reduction from O(m2 ) to                       (e.g., 127 for an 8-bit exponent). In this way, the unsigned
     O(m), where m is the mantissa bit width.                                exponent field can represent a symmetric range of signed
   • We design novel AP multiplication and dot-product op-                   integers centered at zero, making it possible to scale mantissa
     erations, which utilize the CAM and reduction tree col-                 to both very large and close-to-zero values. Meanwhile, the
     laboratively to deliver high performance.                               unsigned format makes the comparison between two values
   • We propose architectural support to allow FloatAP to                    easier than the two’s complement. In Figure 1, the exponent
     accommodate different FP formats while maintaining                      field is ‘10’, so the index equals −125, and the exponentiation
     high utilization. We also show mechanisms to enable                     factor of this number is 2−125 . The value of the sample equals
     concurrency between exponent and mantissa fields.                       −1.101 × 2−125 .
                                                                                Importantly, when the exponent field is all zeros and the
              II. BACKGROUND AND M OTIVATION                                 mantissa is not zero, the implicit value to the left of the radix
A. Floating-Point Basics                                                     point is assumed to be 0 instead of 1. In addition, the scaling
                                                                             factor will equal 21−bias instead of 2−bias . Those FP numbers
                                                                             are called denormalized or subnormal values. The purpose is
             H                              L                                to be able to represent very small values. Moreover, IEEE
  1-bit Sign 10000001010100000000000000000000                                754 standard reserves certain encodings for special values,
                                                                             including ±0, ±∞, NaN (not-a-number). Table I summarizes
                                                                             all the possible value types and their representation. Specif-
              8-bit Exponent             23-bit Mantissa                     ically, the division by zero of a non-zero value will lead to
              value = 1.M x 2(E-127) x (-1)S                                 infinity, whereas mathematically undefined behavior, e.g., zero
                                                                             divided by zero, will result in a Not-a-Number (NaN). The
              -1.101 x 2-125 in the example above                            special values can be propagated quietly through arithmetic
                                                                             operations, although some systems and standards may choose
Fig. 1. An illustrative example of an IEEE754 [42] 32-bit single-precision   to signal an exception for certain types of NaNs, for example.
floating-point number.
                                                                                FP multiplication involves adding the exponents, multiply-
                                                                             ing the mantissas, and XORing the sign bits. FP addition
   Floating-point representation (FP) is a high-precision, high-             requires two steps in series: before adding the mantissas, the
range data type used to encode real numbers in a computer                    operand with the smaller exponent must first right-shift its
system. The absolute value of an FP number equals a variable                 mantissa and increment its exponent as many times as needed
fractional number called mantissa multiplied by a power-of-                  to equalize both exponents, after which the mantissas can be
two scaling factor. The index of the scaling factor is given by              added. As a result of this normalization, some precision may
an integer exponent. The number’s sign is typically represented
by a separate bit. Figure 1 shows an illustrative example                      1 Without loss of generality, in this paper, we use 32-bit single-precision
of a single-precision (32-bit) FP number using IEEE 754                      IEEE 754 FP encoding in all the examples unless otherwise specified.
