update. Finally, the multiply result is calculated in the same        D. Handling Special Values
way as Fig. 5 for both the upper and lower m bits ( 4 and                FloatAP can efficiently handle all the special values listed
5 in Fig. 6). In total, the latency of vfmul is only (5m+4e+4)        in Table I. Since FloatAP can search a pattern across a vector
cycles, including the time of adding the exponent field.              in one cycle, it can quickly locate special values and handle
   Since a needs to be duplicated and occupies m different            them before performing computation on normal values. This
rows in a chain, the next element in the input vector will            entire handling process is done using parallel search/update,
occupy a different chain. Thus, the total vector length of the        eliminating the need for additional exception-handling logic.
entire memory array is reduced by m times. Yet even in terms             Specifically, the denormal values are handled during the
of throughput, our vfmul is still 20% better than the bit-serial      mantissa-integer conversion discussed in Sec. III-B. FloatAP
approach in [12], as the latter spends more than 6m2 cycles for       first searches all zeros across the exponent field to locate
FP values. Moreover, when the physical vector length of the           denormal values in a vector. These values are then masked
memory array is much longer than the vector length of the data        out when resolving the hidden-1 for normal FP numbers.
structure, our vfmul can achieve more than m× throughput              After that, both normal and denormal values’ mantissas are
improvement.                                                          converted to two’s complement. Finally, FloatAP updates the
   Dot-Product (vfdot.vv) – Similar as vfmul, FloatAP can             exponent of denormals from zero to one in parallel so that they
realize the vfdot.vv, the most critical operation for tensor-         can be interpreted as normal values in future computations.
based applications, by simultaneously leveraging the asso-            The overall overhead is merely several cycles.
ciative memory and the adder tree. We do not adopt the                   FloatAP supports both quiet and signaling modes for Infs
straightforward implementation of vfdot with back-to-back             and NaNs. Before an arithmetic operation, FloatAP first
vfmul and vfredsum. Instead, we exploit the commutativity of          searches for and locates them. In signaling mode, it will
addition to reorder the sum in vfredsum and in vfmul based on         directly trigger an invalid operation exception. In quiet mode,
Equation (2) when calculating the dot-product of the mantissa         FloatAP will visit all the propagation rules (e.g., ∞+x=∞,
vectors, like in other tree-based dot-product proposals for           ∞*0=NaN), search for the input combinations across input
integers [15], [49]. In this way, FloatAP does not need to            vectors, and update the output of the matched lanes according
replicate and shift the mantissa of v1 like in vfmul. Instead,        to the rules. Even if 0, Inf, and NaN all exist, the worst-
every element in v1 and v2 occupies a different row in a chain        case overhead is merely 8 cycles. Moreover, if the arithmetic
like in other vector operations such as vfadd. As a result, the       behavior and dataset are both trustworthy, FloatAP can disable
vfdot can achieve much higher FLOPS than vfmul.                       the special value handling to avoid the overhead or perform
                                                                      sanity checks periodically using search operations.
   Listing 2 shows the pseudo-code of vfdot. Before calculating
the dot-product of the mantissa fields, FloatAP first needs           E. Other Functionalities
to add v1 and v2’s exponent vectors (line 5 in Listing 2),
                                                                         Using addition and multiplication as basic operations,
and align M1 based on this Esum vector (line 8). Those
                                                                      FloatAP can efficiently implement other more complex func-
two steps require (5e + 4m + 6) cycles. Then, it calculates
                                                                      tionalities, such as MAC, division, and non-linear activation
the dot-product of the mantissa fields Pvlen in m iterations. In      functions (e.g., GELU or Sigmoid), with the Newton-Raphson
each iteration, FloatAP calculates       i=0 M1 [i] ∗ M2 [i].bitj     method or Taylor-Maclaurin series. This way, FloatAP can
for a different j value. Specifically, the M1 [i] ∗ M2 [i].bitj in
                                                                      perform any operations without assistance from other compute
Equation (2) is realized by a vector AND operation (line 13).
                                                                      engines. Any frequently used function can be implemented
FloatAP can efficiently implement it using one search
                                                    Pvlen and two     as a single vector instruction by directly microprogramming
updates, similar to 3 in Fig. 6. The inner sum i=0 is then
                                                                      its AA into a single instruction and hard-coding it into the
calculated using the same method as Fig. 5,Ptaking m cycles
                                                  m−1                 VCU’s AA storage. In our evaluation, however, the non-
(line 14 in Listing 2). As for the outer sum j=0 and the 2j
                                                                      linear functions are implemented as a RISC-V program using
item, FloatAP amortizes them across the m iterations. In each
                                                                      multiple vfmul and vfadd instructions to avoid introducing non-
iteration, it only needs to calculate a scalar add-accumulation
                                                                      standard instructions to the ISA. Note that while FloatAP
(line 14) and multiply-accumulation (line 15) using the ×2 and
                                                                      is compatible with a lookup table (LuT)-based activation
accumulate units at the root of the adder tree. The latency is
                                                                      approximation [32], [33] that other PIM designs tend to use,
completely hidden behind the vredsum operation. In addition,
                                                                      we choose not to do so because storing the pre-calculated
as Fig. 5 shows that, during vredsum, the chain does not
                                                                      values in SRAM consumes too much area, whereas if in
perform any search/update in all the m cycles except for the
                                                                      DRAM it entails extra data movement.
first one. Therefore, FloatAP can overlap line 14 in iteration
(j − 1) with line 13 in iteration j. Thus, the entire step 3                         IV. F LOATAP A RCHITECTURE
in Listing 2 only takes (m2 + 6) cycles, including a five-
cycle latency to fill the pipelined adder tree. In total, the vfdot   A. Propagation Chain Microarchitecture
operation takes (5e+m2 +4m+12) cycles. Compared to a back-               We build FloatAP on top of the CAPE framework [11],
to-back vfmul and vfredsum, our vfdot algorithm achieves more         [12], [35], [50] in Fig. 3, the state-of-the-art SRAM-based bit-
than 4× throughput improvement.                                       sliced AP. Originally, CAPE’s chain was designed for 32-bit
