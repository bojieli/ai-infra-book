           III. F LOATAP A SSOCIATIVE A LGORITHMS                                       After this step, if a particular cell in the column stores 1, the
   In this section, we introduce FP associative algorithms                              corresponding row in the tag is marked as 1. Essentially, this
(AAs) that can fully harness the bit-sliced subarray chain                              search copies the target column into the tag column. In the
to maximize performance. We use m and e to denote the                                   second cycle, all subarrays overwrite the original value in the
bit width of the mantissa and exponent fields, respectively.                            target column as 0 using a bulk update. Last, the subarrays use
We assume that, within each chain, the sign is stored in                                the left neighbor’s tag bits as masks to selectively update 1 to
the most significant subarray, the next e subarrays store the                           the target column (shown as the red path in Fig. 4). Only the
exponent, and the last m subarrays store the mantissa. First,                           rows marked as 1 by the left neighbor’s tag bits will be updated
we introduce an efficient AA that can align the mantissa                                as 1, and the remaining rows will stay 0. Finally, the 1brsh
field of an FP vector based on the exponent in O(m) cycles.                             of vector a completes. We also implement 1blsh in the same
Then, we discuss how FloatAP implements reduction sum                                   fashion, which is necessary for our hybrid multiplication AA.
and addition using this AA, which results in m times fewer                              Using 1brsh and 1blsh, FloatAP can realize any n-bit shift one
cycles than the existing approach [18], [28]. Next, we show a                           hop after another in 3n cycles. Note that the example above
hybrid AA that realizes multiplication within O(m) cycles by                            shows a destructive implementation–i.e., the original value is
a shift-and-accumulate approach. It performs in-situ shifting                           overwritten. FloatAP also supports a non-destructive version
via search/update and then calculates the accumulation with                             with the same latency by allocating either a new vector register
the adder tree. Finally, by combining the insights of FloatAP                           or metadata column for the result. Additionally, rotation and
mantissa alignment and hybrid multiplication, we introduce                              bit-width upgrading are both supported with the help of the
a fast dot-product that provides 6× higher throughput than                              tag bus in Fig. 3(d). Using the left shift with bit-width
back-to-back multiplication and reduction.                                              upgrading as an example. The overflowed most significant
                                                                                        bit will be stored in a metadata column (invisible to the
A. Mantissa Alignment                                                                   programmer) in the rightmost subarray. This twist over 1blsh
                                                                                        does not impact cycle count. Specifically, in cycle two, the
                                                                                        rightmost subarray will update 0 to both target and metadata
  int* a            a.bit3            a.bit2          a.bit1             a.bit0   1     columns. In cycle three, while other subarrays selectively
   0101    a[0] 0     0           1     1         0     0            1     1
                                                                                        update the target column based on the right neighbor’s tag,
   1101    a[1] 1     1           1     1         0     0            1     1
   0110    a[2] 0     0           1     1         1     1            0     0
                                                                                        the rightmost subarray updates the metadata column based
   1010    a[3] 1     1           0     0         1     1            0     0            on the leftmost subarray’s tag, transmitted by the tag bus.
                                                                                        Moreover, for vectors in two’s complement representation,
           search(1)           search(1)       search(1)       search(1)
                                                                                        FloatAP also supports the arithmetic right shift. The only
                                                                                  2     difference is that, in cycle 2, the leftmost subarray selectively
                 a.bit3            a.bit2             a.bit1             a.bit0
           a[0] 0 0               1 0             0     0            1     0
                                                                                        updates 1 to its target column based on its own tag bits instead
           a[1] 1 0               1 0             0     0            1     0            of unconditionally updating 0. Similarly, for the implicit-one
           a[2] 0 0               1 0             1     0            0     0            representation in Fig. 1, the leftmost subarray in cycle two
           a[3] 1 0               0 0             1     0            0     0            unconditionally updates 1 instead of 0.
           update(0)           update(0)       update(0)       update(0)                   Mantissa Alignment (align) – To align the mantissas of
                                                                                        an FP vector, we right-shift each element’s mantissa so that its
                                                      a.bit1                      3     exponent matches the maximum exponent value in the vector,
  int* a         a.bit3            a.bit2                                a.bit0
   0010    a[0] 0 0               1 0             0     1            1     0            Emax . Specifically, an element with an exponent E right-shifts
   0110    a[1] 1 0               1 1             0     1            1     0            its mantissa by Emax − E bits as shown below:
   0011    a[2] 0 0               1 0             1     1            0     1
   0101    a[3] 1 0               0 1             1     0            0     1             2E · M = 2Emax · M · 2E−Emax = 2Emax · M >> (Emax − E)
                             selective                   selective          selective
                             update(1)                   update(1)          update(1)   Because the mantissa bit-width is m, if E is less than Emax −
Fig. 4. A word-parallel, bit-parallel, one-bit right shift (1brsh) takes three          m + 1, the mantissa will become zero after shifting. Thus, the
cycles. The four subarrays store bit3 to bit0 from left to right, respectively.         align algorithm focuses on elements with the top m largest
                                                                                        exponent values, from Emax − m + 1 to Emax . Accordingly,
   One-Bit Shift (1bsh) – FloatAP implements mantissa                                   align takes m iterations. In iteration i, the CSB searches for
alignment using one-bit right shift (1brsh) operations. We                              the elements whose exponents fall into the range of (Emax −
propose a bit-parallel 1brsh, illustrated in Fig. 4, which                              m, Emax − m + i], and right-shifts their mantissa by one bit
completes in three cycles. In Fig. 4, a is a mantissa vector                            using 1brsh. In this way, those whose exponents are equal to
with four elements ( each element in a different color), and                            E will only appear in the search results of the last Emax −
each element has four bits sliced across four subarrays from                            E iterations. And the CSB will right-shift their mantissa by
left to right. When stored in this way, 1brsh is essentially                            exactly Emax − E bits. FloatAP relies on the existing OR gate
moving the data in a particular column to the same column                               at each tag bit [12], [55] to complete the range search in each
in the right neighboring subarray. To do so, all subarrays first                        iteration within one cycle. Initially, it searches Emax − m + 1
simultaneously search for 1 in the column where a is stored.                            across the exponent field and saves the result in the tag bits of
