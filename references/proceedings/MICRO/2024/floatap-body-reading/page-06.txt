the exponent subarray. From the second iteration, it searches significant). Every time, the tree will reduce the tag bits into an
Emax −m+i, combines the search result with the saved tag bits integer population count (popcnt). Meanwhile, the sum from
from the previous iteration using the OR gates, and saves the the previous cycle is multiplied by 2 and accumulated with
new results. Therefore, the tag bits of the exponent subarray the new popcnt. This process is pipelined so after exactly
keep the accumulative results of all the searches in the previous m cycles, we get the reduction sum of the mantissa vector.
iterations, which is exactly (Emax − m, Emax − m + i]. At last, And the result’s exponent equals the Emax found during align
the tag bits mark all the elements with an exponent in (Emax − algorithm. In total, the FP vfredsum takes (e+5m+9) cycles,
m, Emax ]. We can then utilize them to update all the unselected including a 5-cycle adder tree latency when the vector length
rows’ mantissa to zero as their exponents are smaller than is large.
Emax −m+1. Using our approach, each iteration takes 4 cycles 1 # to-do: v0[i] = v1[i] + v2[i]
(1 for search and 3 for 1brsh). And the align takes (4m + 4) 2 def vfadd.vv(v0, v1, v2):
cycles in total. Note that searching for specific values across a 3   # step1: calculate exponent difference
                                                                      ∆E = vsub.vv(v1.Exp, v2.Exp)
vector’s exponent field requires all the exponent bits stored in 45
the same subarray, which contradicts bit-slicing. We address 6        # step2: swap some operand pairs so that v2[i]
this challenge in IV-A. Also, before align, the AP must first 7       # always have a smaller exponent than v1[i]
                                                                      for i in range(n):
find Emax . This vector maximum operation involves searching 89           if ∆E[i] < 0:
for 1 from the most to the least significant bit, which finishes 10            swap(v1[i], v2[i])
in e cycles for e-bit wide exponent field.                       11
                                                                                    12       # step3: align v2’s mantissa based on ∆E
B. Addition and Reduction Sum                                                       13       ∆E = |∆E|
                                                                                    14       align(v2.Man, ∆E)
     With the help of the align, FloatAP can efficiently imple- 15
                                                                                                      # step4: add the mantissa and copy v1’s exponent
ment addition instructions. Nevertheless, FP mantissa uses a 16                              17       vadd.vv(v0.Man, v1.Man, v2.Man)
sign bit and has an implicit one, whereas integers adopt two’s 18                                     v0.Exp = v1.Exp
complement representation, so FloatAP first needs to convert
                                                                                                Listing 1. Pseudocode of FloatAP’s vfadd.vv algorithm inspired by [18],
mantissas into integers before performing addition.                                             [28]. With bit-sliced data layout and our align algorithm, FloatAP achieves
     Mantissa-Integer Conversion – The subarrays first per- linear time complexity, whereas in both [18], [28] vfadd requires quadratic
form a 1brsh to all the mantissa and set the most significant bit complexity.
to 1 (the implicit one). The least significant bit is temporarily
rotated to a metadata column in the most significant subarray.                                     Vector-Vector Addition (vfadd.vv) – Other than vfredsum,
In the meantime, all the exponents are increased by one due FloatAP also supports element-wise vfadd.vv. By exploiting
to the right shift of the mantissa. Next, all negative mantissa the bit-sliced data layout and the align algorithm, FloatAP
are flipped and increased by one from the least significant significantly optimizes the latency of FP vfadd of other digital
bit. At this point, all mantissa are in integer. The rotated PIM architectures [18], [28]. As shown in Listing 1, vfadd
least significant bit can now be dropped so that the width follows four steps. First, it calculates the difference between
of the mantissa remains as m. In total, the conversion takes the exponent fields of the two input vectors. Next, it swaps
(4e+4m+7) cycles. Note that in practice, we convert mantissa every pair of v1[i] and v2[i] if v1[i] has smaller exponent.
to integer as soon as a vector is loaded into CSB, and they After this step, v1 always stores the values with the larger
remain in integer form until stored back in the main memory exponent in every pair of v1[i] and v2[i]. FloatAP can
so that the conversion overhead is minimized.                                                   efficiently achieve this step using two vmerge.vv operations
                                                                                                that are already supported by CAPE [12]. In step 3, align is
  Sum    Pop
         Cnt     bit1
                      Tag bus 1
                          bit0
                                Sum     Pop
                                        Cnt     bit1
                                                     Tag bus 2
                                                         bit0
                                                               Sum    Pop
                                                                      Cnt     bit1
                                                                                   Tag bus 3
                                                                                       bit0
                                                                                                performed     to v2’s mantissas so that every pair of v2[i] and
    0                             10                             110
      +
         0     0 0
             + 1 1
                      1 1
                      0 0            +
                                        10    0 0
                                            + 1 1
                                                     1 1
                                                     0 0            +
                                                                      10    0 0
                                                                          + 1 1
                                                                                   1 1
                                                                                   0 0
                                                                                                v1[i] have the same exponent. Unlike in vfredsum, the align
      x2 0                           x2 0                          x2 100 1 1
  0
               1 1    1 1
        search(1) search(1)
                                0
                                              1 1    1 1
                                                               10
                                                                                   1 1
                                                                                                in vfadd uses the ∆E calculated in step1 as the reference
                                                                                                exponent vector instead of the exponent of v2 itself. Finally, in
Fig. 5. Mantissa reduction sum using the bit-serial adder tree proposed by
CAPE [12] takes O(m) cycles. The input vector in this example is (1,2,3) step 4, the result’s mantissa vector is calculated by adding v1
and adopts a bit-sliced layout (2-bit wide). The sum equals 6.                                  and v2’s mantissa using integer vadd. The result’s exponent
                                                                                                equals v1’s exponent.
     Reduction Sum (vfredsum.vs) – FloatAP utilizes the bit-                                       FloatAP vfadd is extremely efficient (O(e + m)) because all
serial integer adder tree proposed by CAPE [12] to calculate four steps only require linear or constant time complexity. In
the reduction sum of the mantissa. Fig. 5 shows how it works comparison, other digital PIM architectures can only achieve
under the bit-sliced design. First, all the mantissa subarrays O(e + m2 ) [18], [28] because they adopt a contiguous data
simultaneously search 1 across the column where the mantissa layout and, therefore, cannot support bit-parallel align. Thus,
vector is stored. This will copy the vector operand into the their vfadd has to iterate step 4 by m times, resulting in
tag bits ( 1 ). Then, at every cycle, one of the subarrays sends quadratic time complexity. In addition, we also optimize the
its tag bits to the tree via the tag bus ( 2 to 3 ). This happens original AP integer vadd to further accelerate steps 1 and 4.
from the leftmost subarray (most significant) to the right (least The original vadd does not take advantage of bit-slicing and
