            Tag Bus                                                                                          in ML inference. Thus, FloatAP supports chain splitting to
                                                                                                             utilize the second half of the chain for FP16 and BF16.
 Exponents             Mantissa22       …             Mantissa2             Mantissa1           Mantissa0    Specifically, the CSB’s maximum vector length is doubled for
                                                                                                             16-bit formats. The second half of the vector will be bit-sliced
                                                  …                                                          and stored in the second half of the chain. Both half-chains
                                                                                                             can then simultaneously perform the same operation on the
                           (a) The one-hop backward paths (red dashed) to support right-shift
                                                                                                             first and second half of the vector. To support chain splitting,
          Tag Bus
                                4 extra columns
                                                                                                             Subarray14 needs four extra columns for the exponent search
                    Sign              Exp7            …           Exp1         Mts22        …       Mts0
                                                                                                             in the second half-chain. Subarray25 to Subarray23 , which
                                                                                                             store the exponent for FP32 and BF16, are repurposed for
  Propagation Bus
                                                                                                …            mantissa for FP16. Thus, they need the one-hop rightward tag
  Comparand Bus
        (b) The 32-bit FP propagation chain: four extra columns in the most significant exponent subarray;
                                                                                                             path as well. Tri-state buffers are needed to separate the tag
        exponent and mantissa can operate in parallel thanks to the separate comparand and propagation bus   bus at the midpoint so that both half-chains can utilize the tag
         Fig. 7. FloatAP’s modifications to the chain microarchitecture.                                     bus simultaneously. However, there is no need to duplicate
                                                                                                             chain controllers or command buses since the two half-chains
TABLE II: The time complexity of FloatAP vs. CAPE. Original CAPE does                                        perform exactly the same operation at every cycle. Last, we
not support FP, so we map the best existing PIM-FP algorithm [28] to it.                                     incorporate an extra reduction tree for the second half-chain
             RISC-V Inst.                       FloatAP                       CAPE-FP
                                                                                                             to maximize the vfdot and vfmul performance.
             vfredsum.vs                        e+4m+10                    e+1.5m2 +0.5m+9                      Exponent-Mantissa Concurrency – As discussed in III-A,
             vfadd.vv                          4e+7m+18                   4e+1.5m2 +8.5m+4                   each iteration in align searches a specific value across ex-
             vfmul.vv                             5m+2                       4e+6m2 +11m                     ponent and performs 1brsh to mantissa. Thus, the exponent
             vfdot.vv                        5e+m2 +3m+13                 9e+7.5m2 +11.5m+9
                                                                                                             search can be overlapped with the last mantissa update in the
                                                                                                             previous iteration. As we store the contiguous copy of the
                                                                                                             exponent in metadata columns, this enhancement involves no
integers and every chain contains 32 subarrays. Thus, single-                                                hardware cost because, as Fig. 7(b) shows, the original CAPE
precision FP values (FP32) can be naturally bit-sliced and                                                   chain already separates the command buses for the metadata
stored in the CAPE chain. The sign will be stored in Bit31 ,                                                 (blue) and the data columns (brown). This optimization can
the 8-bit exponent in Bit30 to Bit23 , and mantissa in Bit22                                                 reduce the mantissa alignment time complexity to O(3m)
to Bit0 . 16-bit formats such as half-precision (FP16, 5-bit                                                 cycles. Table II shows that after this optimization, the time
exponent, 10-bit mantissa) and bfloat16 (BF16, 8-bit exponent,                                               complexity of vfredsum, vfadd, and vfdot becomes (e+4m+10),
7-bit mantissa) [48] can also reside in the chain despite only                                               (4e+7m+18), and (5e+m2 +3m+13), respectively.
utilizing the second half of the chain (we address this later).
For double-precision (FP64), FloatAP can accommodate it                                                      B. FloatAP Architecture
using two vector registers, each storing the higher and lower
32 bits. We adopt the following modifications to the integer                                                    A FloatAP core adopts the same organization as CAPE,
chain to support the FP operations described in Sec. III.                                                    shown in Fig. 3(a). One can instantiate FloatAP cores and
                                                                                                             integrate them into a system in various ways. In our evaluation,
   1) Mantissa Subarrays: 1brsh requires a subarray to selec-
                                                                                                             we envision FloatAP as a loosely coupled accelerator with a
tively update rows based on the tag bits of its left neighbor. So
                                                                                                             large number of cores and its own DRAM system. Each AP
we add an extra one-hop rightward tag path for every mantissa
                                                                                                             core is an independent PE and works on an exclusive partition
subarray, shown as red dashed lines in Fig. 7(a), which feeds
                                                                                                             of the problem. FloatAP cores do not have caches for the
the tag bits of a subarray to its right neighbor’s mux.
                                                                                                             CSBs, nor do CSBs act like caches.4 Instead, they are the
   2) Exponent Subarrays: align entails searching a specific                                                 end consumers and producers of the data and access DRAM
8-bit key across the entire exponent field. For each row, the tag                                            directly using vector loads/stores. For the frequently-used data,
bit is marked as 1 only if all bits match. However, since the                                                such as weights, the program can load them into CSB once
exponent is bit-sliced, this operation will necessitate one of the                                           and keep them there for an extended period of time.
exponent subarrays temporarily storing a copy of the searched
                                                                                                                ISA and Software Stack – We program FloatAP using the
exponent in a contiguous data layout. Thus, we widen one
                                                                                                             FP instructions in RISC-V’s standard vector extension [14].
of the exponent subarrays by several columns, i.e., the green
                                                                                                             Table II summarizes the corresponding RISC-V vector in-
region in Fig. 7(b). Every subarray already has 4 metadata
                                                                                                             structions of the operations we introduced in Sec. III. As
columns [12], all free during exponent search. So we only
                                                                                                             we mentioned, the mantissa-to-integer conversion is performed
need 4 extra columns because we focus on FP32 or narrower
                                                                                                             right after a vector is loaded. Note that, apart from the ones
formats in this work. For FP64 it will need 7 extra columns.
                                                                                                             in the table, FloatAP also supports all the non-FP vector
   Apart from the above two mandatory modifications, we                                                      instructions of CAPE. In this work, we manually implement
propose two optional enhancements that can fully exploit
the concurrency among subarrays to boost the performance.                                                       4 The CP of each FloatAP core does have a tiny private I/D cache. However,
Chain Splitting for 16 bits – BF16 and FP16 are prevalent                                                    it does not share data with CSB.
