                                                    FuseMax: Leveraging Extended Einsums to
                                                     Optimize Attention Accelerator Design
                                                      Nandeeka Nayak                              Xinrui Wu                     Toluwanimi O. Odemuyiwa
                                               University of California, Berkeley            Tsinghua University                University of California, Davis
                                                    Berkeley, CA, USA                          Beijing, China                        Davis, CA, USA
                                                   nandeeka@berkeley.edu                xr-wu20@mails.tsinghua.edu.cn            todemuyiwa@ucdavis.edu

                                               Michael Pellauer                             Joel S. Emer                                 Christopher W. Fletcher
                                                     NVIDIA                Massachusetts Institute of Technology / NVIDIA            University of California, Berkeley
                                              Westford, MA, USA                      Cambridge, MA, USA                                   Berkeley, CA, USA
arXiv:2406.10491v3 [cs.AR] 31 Oct 2024




                                              mpellaer@nvidia.com                         jsemer@mit.edu                                cwfletcher@berkeley.edu



                                            Abstract—Attention for transformers is a critical workload          Fortunately, many of the layers (projections, fully con-
                                         that has recently received significant ‘attention’ as a target      nected layers, etc.) used by transformers look very similar to
                                         for custom acceleration. Yet, while prior work succeeds in          prior generations of machine learning models. Their resource-
                                         reducing attention’s memory-bandwidth requirements, it creates
                                         load imbalance between operators that comprise the attention        intensive tensor products can be described and evaluated with
                                         computation (resulting in severe compute under-utilization) and     existing tensor algebra accelerator modeling tools [29], [35],
                                         requires on-chip memory that scales with sequence length (which     [41], and many of the other layers (e.g., layer normalization)
                                         is expected to grow over time).                                     have negligible impact on performance and can be safely
                                            This paper ameliorates these issues, enabling attention with     ignored.
                                         nearly 100% compute utilization, no off-chip memory traffic
                                         bottlenecks, and on-chip buffer size requirements that are inde-       However, the attention layer [52]—usually described as a
                                         pendent of sequence length. The main conceptual contribution is     matrix multiplication, a softmax, and then another matrix
                                         to use a recently proposed abstraction—the cascade of Einsums—      multiplication—does not fit into either of these boxes. For
                                         to describe, formalize, and taxonomize the space of attention       example, the softmax is both memory intensive (featuring low
                                         algorithms that appear in the literature. In particular, we show    algorithmic reuse) and compute intensive (featuring exponen-
                                         how Einsum cascades can be used to infer non-trivial lower
                                         bounds on the number of passes a kernel must take through its       tiation and division). Furthermore, attention’s characteristics
                                         input data, which has implications for either required on-chip      preclude many “free lunches” often used to improve efficiency
                                         buffer capacity or memory traffic. We show how this notion can      in other DNN models. For example, because all tensors are
                                         be used to meaningfully divide the space of attention algorithms    a function of the model inputs, there is no opportunity to
                                         into several categories and use these categories to inform our      amortize memory access costs with an increased batch size.
                                         design process.
                                            Based on the above characterization, we propose FuseMax—a        Additionally, since none of the operands can be computed
                                         novel mapping and binding of attention onto a spatial array-style   before the inputs are given, compression/strength reduction
                                         architecture. On attention, in an iso-area comparison, FuseMax      techniques (e.g., quantization [22], [60], sparsity [34], [46],
                                         achieves an average 6.7× speedup over the prior state-of-the-art,   [53], etc.) must be applied dynamically, leading to more
                                         FLAT, while using 79% of the energy. Similarly, on full end-to-     complicated algorithms and hardware designs.
                                         end transformer inference, FuseMax achieves an average 5.3×
                                         speedup over FLAT using 83% of the energy.                             To illustrate the difficulty in accelerating attention, consider
                                            Index Terms—Tensor algebra, Extended Einsums, Spatial ar-        the state-of-the-art accelerator for attention: FLAT [28]. FLAT
                                         chitectures, Attention                                              uses fusion to reduce attention memory bandwidth bottlenecks
                                                                                                             on a spatial architecture (e.g., a TPU [27]). Specifically, FLAT
                                                               I. I NTRODUCTION                              maps attention’s matrix multiplications to the 2D spatial array
                                            Over the past few years, transformers [52] have emerged as       and softmax operations to a separate 1D array. While FLAT’s
                                         the model architecture of choice for a wide range of machine        design does make attention compute bound, it becomes com-
                                         learning applications, from natural language processing [13],       pute bottlenecked in the 1D array (the softmax), causing
                                         [17], [48], [49] to computer vision [18], [33] to speech            severe under utilization of the 2D array. While one could add
                                         recognition [4], [26]. This rise has been accompanied by a          additional PEs to the 1D array, this results in corresponding
                                         corresponding wave of proposals for accelerating transformers       area costs.
                                         in both software [12], [14], [15] and hardware [28], [62].             Making matters worse, FLAT requires that the entire vector
                                                                                                             over which the softmax is performed be buffered on chip.
                                           This work was partially funded by NSF grants CNS-1954521, CNS-
                                         1942888, CNS-2154183, CCF-8191902, and CCF-2217099; as well as by   This vector is proportional to the sequence length, which is
                                         an Intel gift and a Microsoft Research PhD fellowship.              growing rapidly with time (e.g., Google reports 10 million
length sequences in research, which would require 100s of           denominator and only after that can one revisit and scale each
MegaBytes to buffer [44]). When the vector/sequence length          input (softmax numerator) by the denominator. Because this
grows beyond allowable buffer capacity, FLAT is forced              analysis is performed on the cascade of Einsums, our lower
to spill, which contributes significantly to attention energy       bounds on passes hold for any choice of mapping, including
consumption and can even make attention memory-bandwidth            applications of fusion. For example, despite using fusion,
bound.                                                              FLAT employs a 3-pass cascade and its reliance on large on-
   This paper. We address the above challenges by propos-           chip buffering is a symptom of trying to avoid three passes-
ing a novel spatial architecture – FuseMax – to accelerate          worth of DRAM traffic. We, then, show how transforming the
attention, with particular emphasis on removing bottlenecks         attention cascade reduces the number of passes required.
imposed by the softmax. Our architecture addresses all of the          Additionally, we find that expressing attention as a cascade
aforementioned issues associated with FLAT. Namely:                 of Einsums reveals that optimizations that were previously
   • FuseMax is compute bound, but provides almost 100%             conflated can actually be applied separately. We specifically
      utilization of both the 2D and 1D arrays throughout the       call out one that is used by 1-pass algorithms to eliminate the
      attention layer, without adding additional PEs to the 1D      need for a second pass after the final softmax denominator
      array.                                                        has been calculated. We recognize that this optimization has
   • FuseMax’s on-chip memory requirements are invariant            the added benefit of decreasing the required divisions, which
      to sequence length and require no extra spills to memory      is not only useful for but can be applied to 2- and 3-pass
      regardless of sequence length.                                cascades as well.
   The paper’s technical core is split into three parts.               Finally, Section V uses the insights from Section IV as a
   First, Section III demonstrates a novel analysis on kernels      starting point to develop a novel mapping and binding for
that uses the recently proposed cascade of Einsums abstrac-         attention that can be lowered to a spatial architecture. We
tion [35]. In a nutshell, an Einsum defines an iteration space      call our architecture FuseMax. FuseMax adopts the 1-pass
over tensors and what computation is done on and between            attention cascade used in FlashAttention-2 [14]. However,
tensors at each point in the iteration space. A cascade of          despite using the cascade from FlashAttention-2, binding this
Einsums is a sequence of dependent Einsums that can be used         cascade to a spatial architecture is non-trivial. In particular,
to describe and specify a larger kernel.                            FlashAttention-2 binds the cascade onto a GPU, an archi-
   While prior work [35], [39] provides a precise definition        tecture that features homogeneous PEs, each with relatively
for Einsums, a major contribution in our work is to show how        large per-PE storage, and expensive inter-PE communication.
this definition can be leveraged to inform accelerator design.      Spatial architectures feature opposite characteristics: heteroge-
Specifically, we recognize that the cascade makes explicit pre-     neous PEs, each with smaller per-PE storage, and cheap (but
cisely what dependencies there are between Einsums. We show         restricted) inter-PE communication. Specifically, the networks
how this can be used to make non-trivial deductions about a         that connect the PEs within the 2D array allow efficient
kernel’s allowed fusion granularity and algorithmic minimum         communication primarily between neighbors. We overcome
per-tensor live footprint. The relationship between the live        these differences and demonstrate a novel mapping and bind-
footprint and the buffer capacity, in turn, has implications for    ing for the 1-pass cascade that achieves high utilization
the required data movement.                                         for entire transformer layers. Our architecture requires only
   In more detail, this analysis provides insight into the number   minimal changes to a standard spatial architecture and is
of passes an algorithm performs, i.e., the number of times a        performance/energy robust to long sequence lengths (e.g., 1M
given element of an input must be revisited after visiting every    tokens and beyond).
other element of the input. Normally, one strives to choose
                                                                       To summarize, we make the following contributions:
a dataflow that exploits maximal reuse in a given element
(or tile of elements) to avoid having frequently reload it.           • We show how cascades of Einsums can be used to inform
However, some algorithms preclude this strategy. In this work,          accelerator design, both in terms of reasoning about
we describe how to count the number of passes a cascade                 compute requirements and per-tensor live footprints. We
requires and present two methods for reducing the number                formalize lower bounds on the number of passes a cas-
of passes. In general, fewer passes is preferable; although,            cade imposes given any possible mapping of the cascade
interestingly, we find that decreasing the number of passes can         onto hardware.
increase the required compute. Given that an Einsum cascade           • We use cascades of Einsums, and the observation about
is mapping/scheduling agnostic, this analysis provides insight          pass lower bounds, to provide a taxonomy and precise
given any possible scheduling of the cascade onto hardware.             specification of numerically stable attention algorithms
   Next, Section IV applies the cascade of Einsums abstraction          in the literature. Orthogonally, we show how previously
to describe and formalize the attention kernel. Using the notion        entangled attention optimizations can be applied across
of passes introduced in Section III, we taxonomize the space            attention algorithms.
of numerically stable attention proposals that appear in the          • We propose a novel mapping and binding for attention
literature. For example, in a naı̈ve implementation of attention,       for a spatial architecture—which we call FuseMax—that
one must traverse the entire softmax input to build the softmax         achieves high utilization for both 2D and 1D array PEs,
    and has memory traffic requirements that are independent           We also note that we can view the iteration space itself as
    of sequence length.                                             a tensor. In the example above, this tensor has shape K ×
  • We evaluate FuseMax on BERT [17], TrXL [13], T5 [49],           M × N . Therefore, we define a special fibertree—called the
    and XLM [13] and demonstrate a 6.7× speedup on                  iteration space fibertree or is-fibertree—that is the fibertree
    attention with 79% of the energy and a 5.3× speedup on          representation of this iteration space tensor.
    full end-to-end inference with 83% of the energy relative
    to FLAT.                                                        C. Extended Einsums
                                                                       Traditional Einsums sufficiently express standard tensor
                      II. BACKGROUND
                                                                    algebra, including those supported in Basic Linear Algebra
  In this section, we describe the concepts and terminology         Subprograms (BLAS) [19], [30] and tensor network con-
used in the remainder of the paper.                                 tractions [1]. However, they cannot handle more complex
                                                                    computations. The recently proposed Extended General Ein-
A. Tensors                                                          sums notation (EDGE) [39], extends Einsums to handle graph
   This paper focuses on algebraic computations on tensors,         algorithm computations. We find this abstraction useful for
where a tensor is a multidimensional array. A tensor’s rank         also expressing complex tensor algebra computations and use
refers to a specific dimension of the tensor, while the tensor’s    its notation throughout the paper. We now briefly summarize
shape is the set of valid coordinates for each of the tensor’s      the portions of EDGE that we leverage.
ranks. We use the notation N -tensor to denote a tensor with           1) User-Defined Computations: V EDGE separates
                                                                                                                  W       computa-
N ranks, where a 0-tensor is a scalar, a 1-tensor is a vector,      tions into three “actions”: map ( ), reduce ( ), and populate
a 2-tensor is a matrix, etc.                                        (=) [39]. Map specifies the pair-wise computation between the
   We adopt the format-agnostic fibertree abstraction of ten-       shared ranks of two tensors, reduce specifies the computation
sors, where a tensor is represented as a tree of fibers, as         for the reduction step of an Einsum, and default populate (=)
detailed in prior work [25], [35], [38], [43], [51], [55], [57],    places a computed value from the right-hand side (RHS) of
[58], using the specific version described in TeAAL [35,            the Einsum to its location on the left-hand side (LHS).
Section 2.1]. In this abstraction, a fiber consists of the set of      Each map and reduce action contains two operations: merge
coordinates for a given rank with common coordinates for all        and compute. Compute defines the operation to apply between
higher-level ranks. Each coordinate is coupled with a payload.      two data values, and can be any user-defined function. Merge
The payload may contain a reference to a fiber in the next          specifies which regions of the iteration space to touch; execu-
lower rank, or to a leaf data value.                                tion will not need to access the data space corresponding to
                                                                    culled points. Together, merge and compute precisely define
B. Traditional Einsums                                              the computations in an Einsum. Common merge operations
   An Einsum expression defines a computation on a set of           include intersection (∩), which touches points with non-zero
tensor operands using an iteration space that specifies the set     values in both operands; and union (∪), which touches points
of points where the computations are performed [35], [39]. For      where at least one of the operands is non-zero.
example, we describe matrix-matrix multiplication (GEMM)               The full EDGE specification for GEMM is then:
with the following Einsum:                                                                             ^        _
                                                                               Zm,n = Ak,m · Bk,n ::      ×(∩) +(∪),            (2)
                     Zm,n = Ak,m × Bk,n                      (1)                                      k         k
                                                                           V
where A and B are input 2-tensors of shape K × M and                where k specifies a map action between A and B on the
K×N , respectively. Z is an output 2-tensor with shape M ×N .       k rank and the intersection merge operator (∩) culls k points
Throughout this paper, we use the same symbol for both the          where at least one operand is zero. The compute operator (×)
shape and name of a rank (e.g., rank K in A has a shape of          multiplies the data values
                                                                                         W of coordinates surviving intersection.
K).                                                                 The reduce action ( k ) on the k rank gathers all non-empty
   The iteration space of this Einsum is [0, K) × [0, M ) ×         points in the k rank and reduces them using addition (+).
[0, N ). An evaluation of this Einsum must: (1) walk every            In this work, we use three user-defined computations:
(k, m, n) point in the iteration space; and, at each point (2)        1) Maximum (max(∪)) takes the maximum of two values.
project into the data space of all input tensors, (3) multiply           SupposeV we have the following expression: Zm = Am ·
the corresponding data values, and (4) place the result at the           Bm :: m max(∪). The union merge operator (∪) filters
corresponding data point in Z. If a value already exists at an           out any m coordinates where both operands contain 0
(m, n) point in Z (due to computation at the same (m, n) point           (and places 0 in the output). The max compute operator
for a different k in the iteration space), reduce the two values         then returns the maximum of the two operands.
together using addition. Note that the Einsum specifies what          2) Divide (÷(←)) divides two data values. V    Given the
to compute; it does not indicate the order in which one walks            following expression, Zm = Am · Bm :: m ÷(←),
the iteration space. These aspects are left to the mapping [9],          the merge operator (←) only touches m points where
[35], [41].                                                              there is a non-zero value in the B operand (see [39,
      Appendix]), and the compute operator divides the data          5) Cascades of Einsums: TeAAL [35] introduces the con-
      value in A with the data value in B.                         cept of cascades of Einsums, which expresses directed acyclic
  3) Subtraction and Exponentiation: To apply the expo-            graphs (DAGs) of Einsum expressions as a sequence of sub-
      nential to an expression that subtracts two tensors,         Einsums. One can view the unrolled iterative expression in
      we useV the following expression: Zm = Am ·                  Einsum 3 as a cascade:
      Bm :: m sub-then-exp(1). The user-defined operator                                 S1 = S0 + A0
      (sub-then-exp) performs Am minus Bm then applies                                    S2 = S1 + A1
      the exponential to the result. The merge operator, 1, is
                                                                                          ...
      EDGE’s “pass-through” operator, which touches all m
      points in the iteration space.                                                     SK = SK−1 + AK
  In addition to map and reduce, EDGE enables the expression
of user-defined unary operations on tensors. For example, we          Finally, we use the EDGE Initialization label to specify
can express the application of the non-linear, sigmoid function    computations that initialize tensors, which occur once. We use
(σ) on each element of a tensor A as Zm = σ(Am ).                  the EDGE Extended Einsum(s) label to specify the computa-
  2) Shorthand Notation: Throughout this paper, we take            tion that occurs on each iteration of a cascade of Einsums [39].
advantage of EDGE’s shorthand notation [39] in the following       For example, see (Einsum) Cascade 5.
ways:
                                                                   D. Mapping and Binding
  • We drop all reduce actions that consist of add and
     union   in the compute and merge operator,     respectively      While the cascade of Einsums specifies what computation
      W
     ( +(∪)). Thus, Zm = Ak,m ::
                                            W                      is required, the mapping and binding describe how it should
                                               k +(∪) becomes
     Zm = Ak,m .                                                   occur [9], [35], [41], [51]. We use the concept of logical tasks
  • We express all map actions using infix notation; that is,
                                                                   to define these terms. A logical task is a grouping of points
                     V
     Ak,m · Bk,n :: k ×(∩) becomes Ak,m × Bk,n .                   in the iteration spaces of all Einsums. Tasks are defined such
  • When max is part of a map action (Am · Bm ::
                                                                   that each point in the iteration spaces is assigned to exactly
     V                                                             one task. Logical tasks can be as small as a single point or as
       m max(∪)), we replace it with the following shorthand:
     max(Am , Bm ).                                                large as an entire iteration space. In the final schedule, each
  • When ÷ is part of a map action (Am · Bm ::
                                                   V               logical task must be assigned to exactly one compute unit that
                                                      m ÷(←)),
     we replace it with the following: Am /Bm .                    finishes the given task before moving onto the next task.
  • When sub-then-exp is part of the map action (Am · Bm ::
                                                                      The mapping, therefore, describes a task graph, a directed,
       m sub-then-exp(1)), we replace it with the shorthand
     V                                                             acyclic graph whose nodes are logical tasks and edges are
     eAm −Bm .                                                     dependencies between the tasks. Mapping specifications typ-
  • We can express rank variable expressions with only one
                                                                   ically include aspects such as loop order, partitioning, and
     valid coordinate (e.g., Si:i=2 ) using just the coordinate    work scheduling (sequential vs. parallel operations) [35]. Thus,
     (in this case, S2 ).                                          the dependencies in the task graph can be true dependencies
                                                                   (enforced by the cascade) or additional ordering constraints
  3) Filtering Rank Expressions: EDGE also enables express-
                                                                   imposed by the mapping specification.
ing Einsums that touch only a subset of the data space of their
constituent tensors. For example, we may express the prefix           The binding describes how the tasks are bound to the
sum of a tensor Ak with the following Einsum:                      actual hardware, including which compute unit each task is
                                                                   associated with, when that task will be executed, and where
                        Si+1 = Ak:k≤i                              the inputs and outputs are stored in the memory hierarchy.
                                                                   This binding must obey the dependencies present in the task
For each coordinate i, Si+1 is built by reducing together the
                                                                   graph and the physical limitations of the architecture but is
subset of A whose coordinates are ≤ i. Note that this definition
                                                                   otherwise unconstrained.
of prefix sum computes the entire sum for a given i without
iteratively reusing the previous sum.                              E. Tensor Algebra Accelerators
   4) Expressing Iterative Computations: EDGE expresses
recursion and iteration through generative/iterative ranks. We        In recent years, the popularity of domain-specific tensor
use the term standard ranks to differentiate non-iterative ranks   algebra accelerators has increased. A typical accelerator based
from iterative ranks. We can express the iterative prefix sum      on a spatial architecture consists of off-chip main memory,
as follows:                                                        an on-chip shared global buffer, various scratchpads, and
                        Si+1 = Si + Ai                       (3)   a 1D and/or 2D processing engine (PE) array where each
                                                                   PE contains compute units [9], [27], [28], [38], [62]. This
                            ⋄:i≥K                            (4)
                                                                   design minimizes memory transfer latency while maximizing
Here, S is a tensor with the iterative rank, I, ranging from 0     compute utilization [7]–[9], [11], [27]. Various tools enable the
to K (inclusive). Statement 4 indicates the stopping condition     quick modeling and design space exploration of tensor algebra
for the iterative expression (when i is greater than or equal to   accelerators, including Timeloop [41] and Accelergy [56],
K).                                                                GAMMA [61], and DOSA [23].
  III. PASSES P ERFORMED BY A C ASCADE OF E INSUMS                  pass can be fused at will, producing and consuming a tile of the
   Our first contribution is to demonstrate a novel analysis that   intermediate at a time. Einsums in different passes cannot be
can be applied to a cascade of Einsums. The key insight is          fused. Revisiting Cascade 1, Einsums 5 and 6 cannot be fused
that cascades of Einsums provide a precise description of the       on the K rank. Any implementation must visit all elements of
iteration space for each Einsum and the data space for each         the K fiber of A to produce Y before it can visit any of the
constituent tensor, enabling us to derive the algorithmic min-      elements of that fiber to produce Z.
imum live footprint for each tensor, with implications for the         This analysis also provides a non-trivial lower bound on the
allowed fusion schedules and required buffer capacity/memory        tensors’ live footprints. For example, the algorithmic minimum
traffic. Because this analysis relies only on the cascade of        live footprint for tensor A is a fiber of shape K. In other words,
Einsums, it holds for any choice of mapping and binding.            an architecture must either have enough buffer space to hold
                                                                    an entire K fiber of A or spill and reload that fiber, incurring
A. Calculating the Number of Passes                                 memory traffic proportional to the shape of K. We note that
  We will apply our analysis to attention in Section IV.            this analysis is mapping independent. There is no dataflow for
To illustrate ideas, we first start with a simple pedagogical       this cascade that enables a smaller live footprint.
example, shown in Cascade 1.
                                                                    C. Reducing the Number of Passes via Reassociation
                                                                       Given the restrictions that multi-pass cascades place on
                         Y = Ak × Bk                       (5)      the allowed dataflows and tensor live footprints, it can be
                         Z = Y × Ak                        (6)      beneficial to manipulate the cascade to reduce the number of
                                                                    passes required. Crucially, these manipulations are functionally
                                                                    equivalent and only change how Z is computed. In this section,
               Cascade 1: An example 2-pass cascade.
                                                                    we will present two methods for doing so, though we leave
   Einsum 5 performs a dot product between Ak and Bk , and          a full analysis of the space of pass-reduction approaches to
Einsum 6 multiplies the first Einsum’s result Y by Ak again         future work.
to produce Z. If we want to minimize data traffic of Ak , we           1) Deferring the Multiplication by Y : First, we recognize
need to choose a dataflow for each Einsum that keeps Ak             that, by the distributive property, Einsum 6 can be factored to
stationary and fuses the two Einsums together. In other words,      perform the reduction of Ak first, before multiplying the result
the dataflow must finish using the first element of Ak before       by Y . Doing so, we get the following cascade:
moving onto the next. However, such a dataflow does not exist
for this cascade. Any implementation must visit every element
of Ak to compute Y before it can revisit any element of Ak                                     Y = Ak × B k                          (7)
to compute Z.                                                                                  X = Ak                                (8)
   We define a pass that a cascade performs over a particular
fiber of a particular rank and tensor to be a traversal of every                               Z =Y ×X                               (9)
element of that fiber. Each time an element must be revisited
after visiting every other element of that fiber, there is an       Cascade 2: A reassociation of Cascade 1 that defers the Y × to compute Z
                                                                    with 1-pass of the K rank.
additional pass. For example, Cascade 1 performs two passes
over the K rank of Ak .                                                Now, because there is no read-after-write dependency be-
   Since an Einsum’s iteration space can also be represented as     tween Einsums 7 and 8, both Einsums can be included in
a fibertree (i.e., an is-fibertree – see Section II-B), we extend   the same pass. In fact, because Einsum 8 reduces away the
our definition of an iteration space for a cascade of Einsums       K rank, Cascade 2 is a 1-pass cascade with respect to this
by considering its iteration space to be the sequence of the        rank. This reassociation actually provides a second benefit over
is-fibertrees for each Einsum. Now, in a scenario where fibers      Cascade 1: Einsum 9 now only requires one multiplication (as
for a particular rank exist in multiple is-fibertrees; in each,     opposed to K multiplications in Einsum 6).
they project to the same tensor; and there is a dependency             2) Iteratively Constructing Y and Z: Alternatively, we can
such that all of the elements of the earlier is-fibertree’s fiber   iteratively construct Y and Z as we perform the pass through
must be read before any element can be read again by the            Ak . To do so, we will take a similar approach to the prefix-
later is-fibertree (for all mappings of the cascade), we refer to   sum (see Sections II-C3-II-C4) and build intermediate Y s and
that read-read sequence as creating an additional pass. When        Zs.
there is a sequence of N such read-read dependencies, we
say the cascade is an (N + 1)-pass cascade. For our example,                            RYi+1 = Ak:k≤i × Bk:k≤i                        (16)
Cascade 1 requires two passes of the K rank.                                            RZi+1 = RYi+1 × Ak:k≤i                         (17)
B. Implications of the Number of Passes                             Just like with the prefix sum, this version requires a lot of extra
  The number of passes a cascade performs is relevant be-           compute, but, because Y = RYK and therefore Z = RZK ,
cause it restricts possible fusion schedules. Einsums within a      the final result is the same.
                                                                                                                                                      1.0                          Attn
   Initialization:




                                                                                                                              Proportion of Compute
                                                                                                     Add & Norm
                                                                                                                                                      0.8                          Linear
                              RYi:i=0 = 0                          (10)
                                                                                                          FFN
                                                                                                                                                                                   Other
                                                                                                     Add & Norm
                                                                                                                                                      0.6
                             RZi:i=0 = 0                           (11)            MatMul
                                                                                                     Deprojection                                     0.4
                                                                                                                        ...
                                                                               Softmax
   Extended Einsums:                                                                                  Attention
                                                                                                                                                      0.2
                                                                               MatMul
                                                                                                   Proj   Proj   Proj

             RYi+1 = RYi + Ai × Bi                                 (12)        Q     K      V                                                         0.0 1K   4K 16K 64K 256K 1M
                                                                                                                                                                Sequence Length
                           RYi+1
             RZi+1 = RZi ×        + RYi+1 × Ai                     (13)                     (a) Encoder architecture                                        (b) Required compute
                            RYi
                 Z = RZK                                           (14)                          Fig. 1: Overview of transformer encoder inference.

                     ⋄:i≥K                                         (15)
                                                                               IV. TAXONOMIZING ATTENTION AS E INSUM C ASCADES

Cascade 3: A reassociation of Cascade 1 that iteratively constructs Y and Z
with 1-pass of the K rank.                                                       Our second contribution is to apply the cascade of Einsums
                                                                              abstraction and the notion of passes to transformer models to
                                                                              describe, taxonomize, and highlight trade-offs in the space of
  We remove this extra work by making the I ranks of RYi+1                    attention implementations. This section first looks at the trans-
and RZi+1 iterative. This is shown in Cascade 3. Iterative                    former model as a whole, identifying attention as an important
RYi+1 (Einsum 12) looks very similar to the iterative prefix-                 kernel (Section IV-A). We then give an overview of attention
sum. However, computing RZi+1 is a little more complicated.                   and a “straightforward” (but inefficient) algorithm for softmax
  To derive the expression for RZi+1 , we start by introducing                by writing them as cascades of Einsums (Sections IV-B-
one more intermediate Si , which is the prefix sum for Ak :                   IV-C). Finally, we show how optimizations to softmax can be
                                                                              described by modifying the cascades and provide a taxonomy
                            Si = Ak:k≤i−1                             (18)    of the space using the number of passes required by each
                                                                              cascade (Sections IV-D-IV-E).
Now, we can combine Einsums 17 and 18 to write RZi in
terms of this prefix-sum:
                                                                              A. Transformers
                           RZi = RYi × Si                             (19)
                                                                                 Transformer models generally follow the architecture de-
Dividing both sides by RYi , we derive an alternate definition                fined in [52]. Our work, which addresses the impact of long
for Si :                                                                      sequence lengths during self-attention, focuses on the encoder
                                                                              architecture.1 Figure 1a gives an overview. The transformer
                                      RZi                                     first projects the input (by multiplying it by weight tensors) to
                               Si =                                           form a query, key, and value. Self-attention is made up of three
                                      RYi
                                                                              operations: a matrix multiplication of the query and key, a
Si+1 can also be written using this alternative definition:                   softmax on the result, and another matrix multiplication, which
                                                                              combines the softmax output with the value. The attention
                                    RZi                                       output is then deprojected (again, multiplying by a weight
                          Si+1 =        + Ai                          (20)
                                    RYi                                       tensor), normalized, passed through a two-layer feed-forward
                                                                              neural network (FFN), and normalized once more.
We can combine Einsums 19 and 20 to compute RZi+1 in
terms of RZi (i.e., iteratively):                                                As the sequence length grows, the relative importance of the
                                                                              different operations changes. Figure 1b shows that at shorter
                                                                              sequence lengths, the weight-times-activation “linear” layers
                                            
                                    RZi
            RZi+1 = RYi+1 ×             + Ai                                  are a larger fraction of the total required compute, while at
                                    RYi
                                                                              long sequence lengths, the attention operation dominates. In
Distributing RYi+1 and performing some reassociation, we get                  all cases, the additional non-linearities (e.g., the normalization,
Einsum 13.                                                                    the ReLU between the FFN layers, etc.) have negligible
   Cascade 3 is also a 1-pass cascade, performing one pass of                 impact. In the next section, we focus on describing attention
the K rank of Ak (indexed with the variable i) and iteratively                more precisely, and use our analysis to understand prior work
building RYi+1 and RZi+1 . Unfortunately, unlike Cascade 2,                   on efficient implementations.
Cascade 3 does require extra compute over the original Cas-
cade 1. However, memory bandwidth-limited workloads can                         1 During the decoder phase, inference is severely bottlenecked on the
afford to trade off extra compute for reduced memory traffic,                 memory traffic required to read the KV cache [24], and therefore the on-
and Cascade 3 may still provide benefit.                                      chip accelerator design has less impact on performance.
B. Redefining Attention’s “Matrix Multiplications”                                 In the context of attention, this operation becomes two dimen-
                                                                                   sional and can be expressed using the following cascade with
  In the original transformer paper [52], the kernel was                           input QKm,p :
described with the following equation:
                                                                                                          SNm,p = eQKm,p                                  (26)
                                                           T
                                                              
                                                       QK                                                    SDp = SNm,p                                  (27)
       Attention(Q, K, V ) = sof tmax                  √           V      (21)
                                                         dk                                                  Am,p = SNm,p /SDp                            (28)
   However, this equation says almost nothing about what the                       For each point in the iteration space (m, p), we exponentiate
inputs Q, K, and V look like or what iteration space needs                         QKm,p to generate the softmax numerator (SNm,p in Ein-
to be traversed. We clarify these points by rewriting the above                    sum 26), reduce SNm,p with addition to produce the softmax
as a cascade of Einsums, with the exception of the softmax,                        denominator (SDp in Einsum 27), and finally, divide the
whose cascade we will explore in Section IV-C. The first step                      numerator and denominator to produce the final result (Am,p
is to give each of the ranks names: M and P are the sequence                       in Einsum 28).
lengths for Q and K/V , respectively, and E and F are the                             1) Improving Numerical Stability: Because eQKm,p can
embeddings for Q/K and V , respectively.                                           easily become extremely large, the above formulation suffers
                                                                                   from overflow. Therefore, practical implementations [2], [42]
                                                                                   often prefer the numerically stable variant that replaces Ein-
                            1                                                      sum 26 with:
                   QKm,p = √ × Qe,p × Ke,m                                (22)
                             E                                                                                         _
                                                                                                    GMp = QKm,p ::        max(∪)             (29)
                    Am,p = sof tmax(QKm,p )                               (23)
                                                                                                                            m
                     AVf,p = Am,p × Vf,m                                  (24)                       SNm,p = e      QKm,p −GMp
                                                                                                                                                          (30)

   Here, Einsums 222,3 and 24 look like matrix multiplications.                    and drop the √1E term when computing QKm,p .4 To compute
Taking Einsum 24 as an example, for each point in the iteration                    the global maximum5 GMp , we reduce QKm,p with the op-
space F ×M ×P , we perform a multiplication using elements                         erator max (instead of +). Notice that subtracting GMp from
from two 2-tensors (Am,p and Vf,m ) to produce a 2-tensor                          QKm,p in the exponent is equivalent to dividing by eGMp , and
                                                                                                 1
output (AVf,p ), which requires reducing across the inputs’                        because the eGM p
                                                                                                      term appears in both the numerator (SNm,p
shared rank M . Einsums 22-24 can be modified to refer to                          via Einsum 30) and denominator (SDp via Einsum 27), the
the full batched, multi-head self attention [52] by adding the                     result (Am,p ) stays the same. This construction improves
batch (B) and head (H) ranks to all tensors. This changes                          numerical stability by bounding the values of the softmax
the characteristics of the kernel. Adding the B and H ranks                        numerator SNm,p to the range (0, 1].
means that Einsums 22 and 24 behave like many independent
                                                                                   D. Optimizing Softmax Compute
matrix multiplications instead of one monolithic matrix multi-
plication. The challenges with attention, described in Section I,                     We now describe an optimization to attention that reduces
still follow clearly from this modification. Because all tensors                   compute requirements, specifically division. This optimization
contain a B rank, the matrix multiplications are all unique to                     was used in FlashAttention-2 [14]. We point out that it can
the specific batch’s inputs. Therefore, none of these tensors can                  be applied more broadly, i.e., to any cascade we discuss in
be computed before the inputs are given, and there is no data                      Section IV-E. Einsum 28 requires M ×P divisions. While this
sharing between the different elements in the batch. Hence, to                     is the best we can do for an independent softmax, we note that
simplify notation, we assume the presence of the B and H                           attention does not use the softmax in isolation [52]. Instead, it
ranks but omit writing them throughout the rest of paper.                          subsequently multiplies the result, Am,p , and another tensor,
                                                                                   Vf,m , per Einsum 24, reproduced here:

C. Softmax as a Cascade of Einsums                                                                         AVf,p = Am,p × Vf,m

   We now apply the same precise notation to the softmax. A                        To optimize the full attention cascade, we can refactor Ein-
softmax [5] over a 1-tensor is traditionally expressed with the                    sums 28 and 24 by, instead, first combining SNm,p and
following equation:                                                                Vf,m (Einsum 31) and reducing across the M rank and then
                                                                                   performing the division (Einsum 32), as follows:
                                    e Im                                                                 SN Vf,p = SNm,p × Vf,m                           (31)
                              Am = P I                                    (25)
                                     ke
                                         k
                                                                                                           AVf,p = SN Vf,p /SDp                           (32)

   2 Einsums do not require the transpose, since this information is implicit in     4 The √1 term was introduced to bound the magnitude of SN
                                                                                                                                                     m,p [52].
                                                                                              E
the indices.                                                                       Because the numerically stable softmax variant already accomplishes this, the
   3 In Einsum 22, we also substitute E for d following the notation defined       scaling is often omitted [12], [14], [15].
                                              k
in Section II-B, where the shape of a rank is also its rank name.                    5 “Global” here refers to over the entire M fiber.
           3-pass            2-pass               1-pass
        PyTorch [42]     TileFlow [62]      FlashAttention [15]
                                                                          in the 3-pass cascade), the 2-pass cascade first partitions the
       TensorFlow [2]   Choi et al. [12]   FlashAttention-2 [14]          input, computes a per-partition local max and applies it to form
         FLAT [28]                         Rabe and Staats [47]           a variant of SNm,p whose elements are likewise partitioned
          E.T. [6]
                                                                          and adjusted by the local max. Analogously, each partition gets
           TABLE I: Classifying prior attention algorithms.               a local denominator (also adjusted by the same local max).
This reassociation does F × P divisions instead of M × P                  While this is occurring, it builds the global max from the
divisions. Since M is the sequence length and F is an em-                 local max values. Next, in a second pass, it uses the global
bedding dimension (i.e., M ≫ F ), this reassociation reduces              max to correct the per-partition numerators and denominators
the required divisions (by a factor of M                                  and compute the softmax output.
                                       F ).
                                                                             3) 1-Pass Attention Cascades: While prior work proposes
E. Optimizing Softmax Live Footprint and Memory Traffic                   multiple different 1-pass cascades [14], [15], [47], the main
   We now apply the analysis described in Section III to                  ideas are the same in each. Rather than using the per-partition
analyze attention’s live footprint and memory traffic. We                 local max to compute the local numerator and denominator,
consider the exact attention literature, omitting works that              instead keep a running max that represents the max value seen
either do not model/evaluate the softmax or include approx-               so far. Each time a new running max is computed, also adjust
imation strategies that improve performance at the cost of                previous results (e.g., numerator-times-V , denominator, etc.)
reduced accuracy (increased perplexity). We discuss the latter            with this max.
in Section VII.                                                              This transformation can be described more precisely using
   We find that existing approaches to attention can be clas-             the reassociations presented in Section III-C. First, we modify
sified as either 3-pass, 2-pass, or 1-pass cascades, where an             Cascade 4 to multiply the softmax numerator-times-V and
N -pass cascade performs N passes of a given M fiber. See                 then compute the division (as described in Section IV-D).
Table I. Next, we describe the key ideas of each.                         This reassociation combines the second and third passes of
   1) 3-Pass Attention Cascades: The 3-pass cascade is the                Cascade 4 (see Section III-C1). To ensure numerical stability,
straightforward, numerically stable cascade that we already               we cannot use the same strategy to combine the first and
discussed in Section IV-C1, namely Einsums 29-30 followed                 second passes. So we instead use the iterative approach (see
by Einsums 27-28, reproduced in Cascade 4 for clarity.                    Section III-C2).
                                                                             We are now ready to describe FlashAttention-2’s 1-pass
                                                                          cascade (shown as Cascade 5). We later use it to build
                                                                          FuseMax. Note the evidently increased compute relative to
    QKm,p = Qe,p × Ke,m        /* Pass 1 */ (33)
                     _                                                    the 3-pass cascade. We will carefully design the binding in
     GMp = QKm,p ::     max(∪)              (34)                          Section V to hide these overheads on a spatial architecture.
                             m                                               We will start by expressing the partitioning of both of
     SNm,p = eQKm,p −GMp                     /* Pass 2 */ (35)            the inputs Ke,m and Vf,m into M1 chunks of M0 elements
       SDp = SNm,p                                                 (36)   each (Einsums 39-40). After computing BQKm1,m0,p , this
                                                                          allows us to perform operations like maximum on individual
       Am,p = SNm,p /SDp                     /* Pass 3 */ (37)
                                                                          M 0 fibers, rather than on the whole tensor (Einsum 45).
      AVf,p = Am,p × Vf,m                                          (38)   The problem is, of course, that the local maximum is not
                                                                          necessarily the same for all M 0 fibers and so will not just
               Cascade 4: The 3-pass attention cascade.                   cancel nicely like the global maximum.
                                                                             We resolve this by instead using the running maximum
   In Pass 1, we compute QKm,p and GMp ; in Pass 2, we                    (RMm1,p )—the global maximum of all inputs seen so far—
compute SNm,p and SDp ; and in Pass 3, we compute Am,p                    instead of the local maximum. We recognize that M 1 can also
and AVf,p . Notice that we must finish an entire M fiber of               serve as an iterative rank, and iteratively build up RMm1,p .
Einsum 34 (reading an entire M fiber of QKm,p ) before                    After initializing RMm1:m1=0,p to −∞ (Einsum 41), we com-
GMp is ready to start Einsum 35 (where we must read the                   pute a new running maximum RMm1+1,p using the running
same M fiber of QKm,p again). Similarly, we must finish an                maximum computed in the previous iteration RMm1,p and the
entire M fiber of Einsum 36 (reading an entire M fiber of                 new local maximum LMm1,p (Einsum 46).
SNm,p ) before SDp is ready to start Einsum 37 (where we                     We can now use the running maximum to compute a local
must read the same M fiber of SNm,p again). To summarize,                 numerator SLNm1,m0,p (Einsum 47), a local denominator
as a consequence of the dependencies between Einsums, this                SLDm1,p (Einsum 48), and even the softmax numerator-
cascade must perform three passes over each M fiber. This                 times-V SLN Vf,m1,p (Einsum 49) using the partitioned
holds for any choice of mapping (including ones that perform              BVf,m1,m0 (Einsum 40).
fusion).                                                                     Now consider the softmax denominator. Eventually, we
   2) 2-Pass Attention Cascades: We now briefly summarize                 would like to reduce SLDm1,p into a 1-tensor, but because
the 2-pass cascade, deferring details due to space. Rather than           its values may have been computed with different maximums,
computing the global max and then starting the softmax (as                we cannot simply use addition. Instead, by introducing a
                                                                                         V. M APPING AND B INDING ATTENTION
   Initialization:
                                                                                  Based on the framework from Section IV, we now describe
                     BKe,m1,m0 = Ke,m1×M 0+m0                      (39)
                                                                               FuseMax, an efficient mapping and binding of an attention
                      BVf,m1,m0 = Vf,m1×M 0+m0                     (40)        algorithm (specifically the 1-pass cascade in Cascade 5) to a
                  RMm1:m1=0,p = −∞                                 (41)        spatial array-style architecture. To enable maximum flexibility
                  RDm1:m1=0,p = 0                                  (42)        while binding, FuseMax’s mapping places each iteration space
                                                                               point in its own logical task.
                RN Vm1:m1=0,p = 0                                  (43)
                                                                                  The goal when binding a cascade onto hardware is to fully
   Extended Einsums:                                                           utilize all available compute units. In our evaluation of prior
                                                                               work (Figure 6 and Section VI-B), we observe that at short
       BQKm1,m0,p = Qe,p × BKe,m1,m0                               (44)
                                  _                                            sequence lengths, the 2D PE array is under-utilized because
          LMm1,p = BQKm1,m0,p ::     max(∪)                        (45)        it must wait for the 1D PE array to compute the softmax. At
                                                m0                             longer sequence lengths, both arrays are under-utilized since
          RMm1+1,p = max(RMm1,p , LMm1,p )                         (46)        the workload becomes memory-bandwidth limited.
                             BQKm1,m0,p −RMm1+1,p                                 FuseMax’s binding addresses these issues to achieve full
        SLNm1,m0,p = e                                             (47)
            SLDm1,p = SLNm1,m0,p                                   (48)        utilization on both the 1D and 2D PE arrays. First, we decrease
                                                                               the compute performed by the 1D array by (1) applying
        SLN Vf,m1,p = SLNm1,m0,p × BVf,m1,m0                       (49)        the division reduction optimization (Section IV-D) and (2)
                             RMm1,p −RMm1+1,p
           P RMm1,p = e                                            (50)        sharing the other operations (sum/max/exp) between the 1D
           SP Dm1,p = RDm1,p × P RMm1,p                            (51)        and 2D arrays. Similarly, we ensure that the workload is never
                                                                               memory-bandwidth limited by deeply fusing all Einsums in
          RDm1+1,p = SLDm1,p + SP Dm1,p                            (52)
                                                                               the cascade to restrict the live footprint to only what can be
       SP N Vf,m1,p = RN Vf,m1,p × P RMm1,p                        (53)        buffered on-chip. No matter the sequence length, our dataflow
      RN Vf,m1+1,p = SLN Vf,m1,p + SP N Vf,m1,p                    (54)        is never forced to spill any of its intermediates off-chip.
                AVf,p = RN Vf,M 1,p /RDM 1,p                       (55)           Architecture. FuseMax is a spatial array architecture based
                        ⋄ : m1 ≥ M 1                               (56)        on the TPUv2/TPUv3 [37, Figure 1(e)]. The off-chip DRAM
                                                                               and a large global buffer both feed data to connected 2D and
                                                                               1D arrays (see Figure 2). We set parameters to match the cloud
Cascade 5: A 1-pass attention cascade. Note that M 1 is used as a standard
rank (e.g., to access BQKm1,m0,p ) and as an iterative rank (e.g., to access
                                                                               configuration in prior work [28].
RMm1,p ). The stopping condition for all iterative ranks is m1 ≥ M 1
(Statement 56).
                                                                                                                          256

                                                                                         400
                                                                                        GB/sec      Global
                                                                                                                      2D PE Array




                                                                                                                                         256
                                                                                                    buffer
                                                                                                                       (+, x, max)
                                                                                                   (16 MB)
new running denominator RDm1,p with iterative rank M 1,
we can correct the old denominator RDm1,p to the new
running maximum RMm1+1,p and then perform the addition.
                                                                                                                      1D PE Array
We start by initializing the running denominator at the start
                                                                                                                      (+, x, max, /)
of the computation to 0 (Einsum 42). Then, at each point
m1, the correction factor P RMm1,p allows us to correct the
previous running denominator RDm1,p with the new maxi-                                 Fig. 2: Spatial array architecture assumed for FuseMax.
mum (Einsum 51). In other words, RDm1,p is downscaled
by eRMm1,p . SP Dm1,p “switches” the downscaling factor on                        Figure 3 shows the evolution of the 2D PE array archi-
                                                   eRMm1,p
RDm1,p to eRMm1+1,p by multiplying RDm1,p by eRM      m1+1,p
                                                                               tecture, from a fixed-dataflow multiply-accumulate TPU PE
(P RMm1,p ). Once SLDm1,p and SP Dm1,p have the same                           (Figure 3a) to a flexible-dataflow multiply-accumulate PE
maximum, they can be combined to produce the new running                       (Figure 3b) to a FuseMax PE (Figure 3c). Note, although both
denominator RDm1+1,p (Einsum 52). We can do the same to                        the 1D and 2D PE arrays in FuseMax perform exponentiation,
compute the running numerator-times-V (Einsums 43, 53-54).                     we implement exponentiation with 6 sequential multiply-
                                                                               accumulate operations [36], [53] and therefore do not require
   Finally, AVf,p can be computed by dividing the final                        a dedicated exponentiation unit.
numerator-times-V by the final denominator. By construction,                      Mapping. Prior attention accelerators [28], [62] explore
at this point, RN Vf,M 1,p and RDM 1,p are both downscaled                     fusing many of attention’s loop nests together. However,
by the same maximum RMM 1,p (conveniently, also the global                     because these accelerators all use multi-pass cascades, the
maximum) and can be correctly combined.                                        algorithmic minimum live footprint of some tensors (e.g.,
                                                                    Register File
                                                                                    pipeline parallelism to achieve high utilization of both the 1D
                                                                    (10 entries)    and 2D spatial arrays. Figure 4 shows the waterfall diagram
                                     +, x
                                                                                    for FuseMax in the steady state. Time is broken into epochs.
+,xx
+,            +,xx                   +, x
                                            +, x                       +, x, max
              +,                            +, x
                                                                                    Each epoch performs the same set of tile-granular operations
                                                                                    at specific tile-relative coordinates (given by a, b, c, d in the
                                                                                    figure). Across all epochs, the kernel evaluates all tiles and
       (a) TPU [27] PE             (b) FLAT [28] PE              (c) FuseMax PE     each Einsum in Cascade 5 is mapped to either the 2D or 1D
                         Fig. 3: 2D PE architecture evolution.                      array for all epochs (as shown in the figure).
                                                                                       A major design consideration when binding the attention
                                                                                    kernel is how to overcome the latency of fills and drains
QKm,p ) is O(M ), meaning that for long sequence lengths,                           to/from the spatial array. Consider a tile of QKm,p of shape
intermediates cannot be buffered on chip.                                           M 0 × P 0. Per Einsum 22, the iteration space to evaluate
   FuseMax leverages fusion in conjunction with the 1-pass                          this tile is E × M 0 × P 0 which becomes E cycles on the
cascade to eliminate the memory traffic of these tensors,                           spatial array. For the networks we evaluate, E = 64 or 128.
regardless of the sequence length. Specifically, we partition                       Assume E = 64. Using an output stationary dataflow, while
on both M and P (forming M 1, M 0 and P 2, P 1, P 0), and                           each PE performs 64 MACCs, it takes ∼ 256 cycles to both fill
maximally fuse all levels in the attention loopnest as shown                        and drain the spatial array. Without careful interleaving, this
in Mapping 1. That is, all Einsums in Cascade 5 are fused                           combination of parameters causes low utilization because, for
except for the last (which is fused to the rest only on P 2).                       example, the running max RMm1+1,p1,: cannot be computed
                                                                                    until a tile of QKm1,:,p1,: is completed and spatially reduced
   for p2 ...:                                                                      (drained) to form the local max LMm1,p1,: (Einsums 45-46).
     for m1 ...:                                                                       Our binding address the above issues with two levels of
       for p1 ...:                                                                  interleaving. First, we interleave the construction of dependent
         parallel_for p0 ...:                                                       tiles across epochs. This is reminiscent of software pipelining.
           parallel_for m0 ...:
                                                                                    For example, in Figure 4 the d-th tile of BQK and LM are
             (RNV[:, m1 + 1, p2, p1, p0],
              RD[m1 + 1, p2, p1, p0]) =                                             completed in Epoch i (as they correspond to a fill followed
                 ComputeRNVTile(                                                    by a drain and can be easily pipelined). The RM (which has
                   Q[:, p2, p1, p0],                                                to wait for the drain) for tile d takes place in a later epoch.
                   K[:, m1, m0], V[:, m1, m0])                                      Instead, Epoch i computes an earlier tile’s running maximum
     for p1 ...:
                                                                                    RM [c].
       parallel_for p0 ...:
         AV[:, p2, p1, p0] =                                                           Second, we interleave the construction of certain tiles within
           ComputeAVTile(                                                           an epoch at a fine (e.g., cycle-by-cycle) granularity. See the
             RNV[:, m1 + 1, p2, p1, p0],                                            notation ‘A|B’ in Figure 4. This is to ensure high utilization
             RD[m1 + 1, p2, p1, p0])                                                of both the 2D and 1D PE arrays at all times. To make
                                                                                    this more clear, Figure 5 shows the start up and steady-state
                                                                                    interleaving of SLN V and BQK in the 2D array and SP N V
Mapping 1: The FuseMax mapping as a loopnest. We partition on both
M and P and map the innermost ranks M 0 and P 0 to the spatial                      and RN V in the 1D array. In each cycle, a given PE in the
array PEs. ComputeRNVTile performs Einsums 44-54 from Cascade 5.                    2D array computes a value for either BQK or SLN V and
ComputeAVTile performs Einsum 55. Note that each Einsum represents                  this alternates cycle by cycle. Each neighbor-neighbor link in
a loopnest: by writing all Einsums in ComputeRNVTile under a single
loopnest, we mean that we are maximally fusing those loopnests. Outer loops         the array is active in every cycle—carrying data for one of the
over B and H (if performing batched multihead attention) are not shown.             two operation types. By interleaving SLN V with BQK, the
                                                                                    1D PEs can concurrently compute SP N V and RN V .
   While prior work implementing attention in hardware [28],                           Putting everything together, as Section VI-B will show, the
[62] does utilize the 2D spatial array for the tensor products, it                  above enables high utilization of all 2D and 1D array PEs.
fails to do so for the softmax, choosing instead to use the 1D                         FuseMax on GPUs. FuseMax’s mapping and binding
array. Because there are far fewer total PEs in the 1D array                        cannot be directly applied to GPUs. FuseMax’s architec-
than the 2D array, the softmax becomes a bottleneck. FuseMax                        ture features heterogeneous PEs, each with smaller per-PE
improves utilization of the 2D spatial array by using it for                        storage, and cheap (but restricted) inter-PE communication.
both the tensor products and the exponentiation operator in the                     Specifically, the networks that connect the PEs within the 2D
softmax. FuseMax parallelizes across the M 0 and P 0 ranks                          array allow efficient, fixed-latency communication primarily
throughout the attention kernel (see Mapping 1). We set M 0×                        between neighbors, including between the bottom of the
P 0 = # 2D Array PEs. The large spatial reductions required                         2D array and the 1D array. However, the GPU architecture
when parallelizing across the M 0 rank are easily handled by                        features opposite characteristics: homogeneous PEs, each with
the low-cost inter-PE communication network.                                        relatively large per-PE storage, and expensive, loosely coupled
   Binding. The dependencies between different Einsums in                           inter-PE communication. While concurrent work [50] has
our cascade necessitate a binding that implements fine-grain                        explored using the GPU’s Tensor Cores to compute BQK and
                                                                                  Epoch i



                2D Array                  ("#$% & | )*+ , ) → #/ , → "#$ 0 → "#1 0


                1D Array                                               ("2$% 3    4$% 3      4/ 0 ) → 41 3 → 24/ 0 → "21[0]


                                                                       Time
                                Epoch i-1                                                                                        Epoch i+1

Fig. 4: FuseMax pipelining at a glance. Each tensor name (e.g., SLN V ) corresponds to the Einsum used to compute that tensor (see Cascade 5). a, b, c
and d denote tile-relative coordinates where a < b < c < d. If Epoch i produces tiles with coordinates a, b, c, d, Epoch i + 1 produces tiles with identifiers
a + 1, b + 1, c + 1, d + 1. And so on. ‘A|B’ denotes ‘computing tile A is interleaved with computing tile B.’ ‘A → B’ denotes ‘computing tile A is done
before computing tile B.’ Computing AVf,p is not shown. The green and blue time periods making up an epoch take almost the same number of cycles.



                                                                              0                                              0            0

                                                                          0                                              0            1



                                                                                                                             1
                                                                                                                         0

                 2D Array                                   2D Array                                   2D Array
                 1D Array                                   1D Array                                   1D Array
                                                                                                                             0

                                                                                                                         0


                               t=0                                        t=1                                            t=2

                               t=3                                        t=4

                                                                                                           Legend
                                                                                                                  BV             BK
                                              0
                                   2                                          2          2
                               2          1                               2          3                            SLNV           PRM
                                                                                                                  Q              SPNV
                                   1          1                               3          1
                               0          1                               2          1


                                                                                                                  =
                                                                                                                                  … RF …
                 2D Array                                   2D Array
                 1D Array                                   1D Array                                                               FUs
                                   0
                                              1                               2          1
                               0          1                               2          1


Fig. 5: Intial pipeline fill (t = 0 to t = 2) and steady-state (t = 3 and t = 4) for the intra-epoch interleaving of SLN V |BQK and SP N V |RN V to
maximize 2D and 1D PE utilization, respectively, on a toy 2x2 array. Each color indicates a tensor and each number indicates a point in that tensor (e.g., the
point BV0 moves from the top left PE at t = 1 to the top right PE at t = 2). To reason about signal timing, we use input (but not output) latches for data
in each PE, so moving data appears on output wires. Some stationary tensors (e.g., BQK) and Einsums (e.g., SLD) are omitted for clarity.



SLN V and using software pipelining to hide the latency of the                    A. Experimental Set-Up
other compute, the GPU’s loosely coupled threads require fre-
quent synchronization to maintain correctness. FuseMax takes                         First, we present the experimental setup details common to
advantage of the tight coupling between the 2D and 1D arrays                      all following subsections.
to statically schedule compute between the arrays, enabling                          Workloads. We evaluate all accelerators and configurations
high utilization across the board without sychronization.                         using the same transformer models used by FLAT [28]: BERT-
                                                                                  Base [17] (BERT), TrXL-wt103 [13] (TrXL), T5-small [49]
                            VI. E VALUATION                                       (T5), and XLM [13]. We omit FlauBERT [31] because it uses
   In this section, we demonstrate how FuseMax’s cascade, ar-                     the same hyperparameters as TrXL. We also note that though
chitecture, and binding work together to achieve improvements                     T5 is an encoder-decoder model, we only evaluate the encoder
in both performance and energy relative to the state of the art,                  in this work. Following FLAT, we use a batch size B = 64
for both attention and end-to-end transformer inference.                          for all evaluations.
                    1.0                                                                                                                                           Unfused
                                                                                                                                                                  FLAT
Utilization 1D

                                                                                                                                                                  FuseMax
                    0.5                                                                                                                                           +Cascade
                                                                                                                                                                  +Architecture
                                                                                                                                                                  +Binding
                    0.0 1K 4K 16K 64K 256K 1M                1K 4K 16K 64K 256K 1M            1K 4K 16K 64K 256K 1M         1K 4K 16K 64K 256K 1M
                                BERT                                 TrXL                              T5                           XLM
                                                                                       (a) 1D PE array utilization
                    1.0                                                                                                                                           Unfused
                                                                                                                                                                  FLAT
Utilization 2D




                                                                                                                                                                  FuseMax
                    0.5                                                                                                                                           +Cascade
                                                                                                                                                                  +Architecture
                                                                                                                                                                  +Binding
                    0.0 1K 4K 16K 64K 256K 1M                1K 4K 16K 64K 256K 1M            1K 4K 16K 64K 256K 1M         1K 4K 16K 64K 256K 1M
                                BERT                                 TrXL                              T5                           XLM
                                                                                       (b) 2D PE array utilization

                                    Fig. 6: Utilization of the different PE arrays on the unfused baseline, FLAT, and three configurations building up FuseMax.

                    1.0
Proportion Active




                    0.8                                                                                                                                                QK
                    0.6                                                                                                                                                LM
                                                                                                                                                                       SLN
                    0.4                                                                                                                                                SLD
                    0.2                                                                                                                                                SLNV/AV
                    0.0
                          FL
                               +C
                                     +A
                                          +B

                                                  FL
                                                       +C
                                                             +A
                                                                  +B

                                                                        FL
                                                                             +C
                                                                                  +A
                                                                                       +B

                                                                                                 FL
                                                                                                      +C
                                                                                                            +A
                                                                                                                 +B

                                                                                                                      FL
                                                                                                                           +C
                                                                                                                                +A
                                                                                                                                     +B

                                                                                                                                           FL
                                                                                                                                                +C
                                                                                                                                                     +A
                                                                                                                                                          +B
                                    1K                      4K                 16K                        64K               256K                   1M
   Fig. 7: 2D array utilization by Einsum across different configurations—FLAT (FL), +Cascade (+C), +Architecture (+A), and +Binding (+B)—and sequence
   lengths on BERT.


                                                                                                                                                                  Unfused
                    10                                                                                                                                            FLAT
Speedup




                                                                                                                                                                  FuseMax
                     5                                                                                                                                            +Cascade
                                                                                                                                                                  +Architecture
                                                                                                                                                                  +Binding
                     0 1K 4K 16K 64K 256K 1M                 1K 4K 16K 64K 256K 1M            1K 4K 16K 64K 256K 1M         1K 4K 16K 64K 256K 1M
                               BERT                                  TrXL                              T5                           XLM
                                          Fig. 8: Speedup of attention for FLAT and three configurations building up FuseMax over an unfused baseline.

                    1.0                                                                                                                                           Unfused
                                                                                                                                                                  FLAT
Energy Use




                                                                                                                                                                  FuseMax
                    0.5                                                                                                                                           +Cascade
                                                                                                                                                                  +Architecture
                                                                                                                                                                  +Binding
                    0.0 1K 4K 16K 64K 256K 1M                1K 4K 16K 64K 256K 1M            1K 4K 16K 64K 256K 1M         1K 4K 16K 64K 256K 1M
                                BERT                                 TrXL                              T5                           XLM
                                    Fig. 9: Energy consumption of attention for FLAT and three configurations building up FuseMax over an unfused baseline.


                    10                                                                                                                                            Unfused
                                                                                                                                                                  FLAT
Speedup




                                                                                                                                                                  FuseMax
                     5                                                                                                                                            +Cascade
                                                                                                                                                                  +Architecture
                                                                                                                                                                  +Binding
                     0 1K 4K 16K 64K 256K 1M                 1K 4K 16K 64K 256K 1M            1K 4K 16K 64K 256K 1M         1K 4K 16K 64K 256K 1M
                               BERT                                  TrXL                              T5                           XLM
                                Fig. 10: Speedup of transformer inference on FLAT and three configurations building up FuseMax over an unfused baseline.
             1.0                                                                                                                                 Unfused
                                                                                                                                                 FLAT
Energy Use

                                                                                                                                                 FuseMax
             0.5                                                                                                                                 +Cascade
                                                                                                                                                 +Architecture
                                                                                                                                                 +Binding
             0.0 1K 4K 16K 64K 256K 1M          1K 4K 16K 64K 256K 1M          1K 4K 16K 64K 256K 1M          1K 4K 16K 64K 256K 1M
                         BERT                           TrXL                            T5                            XLM
                   Fig. 11: Energy consumption of transformer inference on FLAT and three configurations building up FuseMax over an unfused baseline.



     Modeling with Timeloop and Accelergy. We perform                                 implements a binding that fully produces and consumes one
  our evaluation using two tools for tensor algebra accelerator                       M 0 × P 0 tile of BQK before starting the next, and +Binding
  modeling and design space exploration: Timeloop [41] and                            adds FuseMax’s pipelined/interleaved binding.
  Accelergy [56]. We use these tools to build models of the                             Hardware parameters. Figure 2 shows the selected hard-
  accelerator architectures at a 45nm technology node and                             ware parameters. We chose the PE array dimension to match
  evaluate each Einsum individually. Results from individual                          FLAT’s cloud accelerator and then set the global buffer ca-
  Einsums are combined using heuristics presented in prior work                       pacity so that the overall chip area was as close to FLAT’s as
  for evaluating full cascades [35]. Together, these tools allow us                   possible. Also following FLAT, we use a 940 MHz frequency.
  to evaluate execution time, energy, and area for all our designs.                   We use Accelergy to model the area of both designs and find
  We perform floating-point division using the design in Xia et                       that FuseMax is 6.4% smaller.
  al. [59], scaled down to a 45nm technology node [56].
     Unfused Baseline. We build the unfused baseline by com-                          B. Evaluating Attention
  bining the costs of three phases: QK (Einsum 22), the 3-pass                           We now evaluate FuseMax to demonstrate the benefits it
  softmax (Cascade 4), and AV (Einsum 24). Because this base-                         provides on the attention kernel by comparing it to the two
  line is unfused, each phase can be scheduled independently,                         baselines.
  but proceed sequentially and require outputs to be written                             Utilization. Figure 6a shows the utilization of the 1D PE
  to memory between phases. We use Timeloop to search for                             array when performing attention. FLAT’s utilization drops for
  efficient mappings to perform QK and AV . Additionally, we                          sequence lengths ≥ 256K—it becomes memory bandwidth
  model the softmax for the unfused baseline by allowing the                          limited because it must spill the QK and A tensors to memory.
  accelerator to load the M fibers of the input on-chip one-by-                       By using a 1-pass cascade (+Cascade), FuseMax’s utilization
  one (spilling if there is not enough space) before performing                       becomes independent of sequence length. We also note that
  the compute. We model the memory traffic, compute, and                              without the FuseMax binding (+Architecture), the 1D array is
  energy required to perform all Einsums required for attention.                      forced to stall and utilization drops. Adding in this binding
     FLAT Baseline. Our main baseline is the state-of-the-                            (+Binding) enables FuseMax to fully utilize the 1D array
  art attention accelerator FLAT [28]. Though we started with                         again.
  the FLAT authors’ original code, we found and corrected a                              Similarly, Figure 6b shows the utilization of the 2D array.
  number of bugs. Through private correspondence with the                             Because of the large amount of compute required for the
  FLAT authors, we verified the bugs were indeed bugs. We                             softmax, most configurations achieve poor utilization of this
  also discovered a couple of larger conceptual errors, which                         array. In fact, because the 1-pass cascade increases the com-
  the authors told us to avoid by restricting FLAT to only search                     pute required, +Cascade’s 2D array utilization is lower than
  through configurations without these issues.                                        FLAT’s at short sequence lengths. On the other hand, FuseMax
     Beyond correcting the FLAT codebase, we created and                              (+Binding) achieves high utilization across the board and, at
  validated a Timeloop model that reproduces the FLAT authors’                        long sequence lengths, reaches almost 100% utilization. Both
  (corrected) code to within < 1% error. However, the FLAT                            baselines achieve slightly higher utilization on XLM, which
  codebase does not model the cost to perform the softmax.                            can be attributed to the higher intensity caused by a larger
  Specifically, their model ignores the cost of the data transfers                    embedding dimension (E/F ).
  required for the softmax (between any levels of the memory                             Figure 7 explores this phenomenon in more detail, breaking
  hierarchy) and uses 230 1D PEs for compute. When comparing                          down the utilization by Einsum. FuseMax effectively hides
  FuseMax and FLAT in this work, we augment our Timeloop                              both the costs of the memory traffic and softmax compute,
  model to model softmax correctly per the 3-pass cascade                             allowing it to achieve high 2D array utilization while spending
  implicitly assumed by FLAT using only 256 1D PEs.                                   most of the cycles on the tensor products.
     FuseMax Configurations. To demonstrate the sources of                               Speedup. Figure 8 shows that FuseMax achieves an average
  the improvements achieved by FuseMax, we present three                              speedup of 10× over the unfused baseline and 6.7× over
  configurations, one associated with each of the major changes                       FLAT. We note FuseMax achieves lower speedup on XLM
  we propose: +Cascade uses the 1-pass cascade on the FLAT                            only because the baselines are able to achieve higher utilization
  architecture, +Architecture adds the FuseMax architecture but                       of the 2D array on this transformer (Figure 6b).
                        105                                                             PE buffers to accommodate the resulting pipelined/interleaved
                                                                           Model        binding, we generate a family of designs for efficient trans-
                                                                               BERT
                                                                               TrXL     former inference.
                        104                                                    T5
                                                                               XLM                          VII. R ELATED W ORK
Attention Latency (s)




                                                                                           Spatial architectures have been applied successfully to a
                                                                                        variety of domains in academia [9], [10], [40], [45] and
                        103                                                             industry [3], [27]. Beyond FLAT [28] (discussed in the main
                                                                                        body of the paper), TileFlow [62] is a framework for modeling
                                                                                        and searching for efficient fused dataflows (including for atten-
                        102                                                             tion) on spatial architectures. Though TileFlow does explore
                                                                                        a broader space of dataflows than FLAT, even implementing
                                                                                        the 2-pass softmax cascade (Section IV-E2), its dataflows
                                                                                        remain softmax-compute limited. Recent work has explored
                              10 1               100                101                 the scheduling/compilation of a multi-Einsum kernels [21],
                                                 Area (cm2)                             [54], [62]. However, these works explore a limited set of
                  Fig. 12: Pareto-optimal curves for FuseMax at sequence length 256K.   transformations, making FuseMax’s inter-Einsum interleaving
                                                                                        not discoverable.
                                                                                           Quantization and sparsity have also been successfully ap-
    Energy. Figure 9 shows that FuseMax uses 77% the energy                             plied to reduce the transformer inference compute and live
 of the unfused baseline and 79% the energy of FLAT.6 The                               footprint. We view these schemes as complementary to our
 energy use of the unfused baseline and FLAT are dominated                              work. GPTQ [20], AWQ [32], and LLM.int8() [16] quan-
 by the DRAM access energy, the global buffer access energy,                            tize model weights to 4 or 8 bits without significant ac-
 and the QK and AV (Einsums 22 and 24) compute energy.                                  curacy degradation. Outlier-aware quantization schemes like
 FuseMax achieves its energy savings by significantly reducing                          GOBO [60] and OliVe [22] quantize both weights and ac-
 the DRAM and global buffer access energies. In fact, ≥ 95%                             tivations to a low-bit precision on specific hardware designs.
 of the energy used by FuseMax across all models and se-                                SpAtten [53] prunes entire tokens and heads, while Sanger [34]
 quence lengths goes to the compute performed by the MACC                               and DOTA [46] use quantized or low-rank projected Q and K
 functional units in the 2D array.                                                      tensors to estimate which values of QK and A can be safely
                                                                                        pruned. All of these algorithms are expressible as cascades of
 C. Evaluating Transformer Inference
                                                                                        Einsums, and therefore, may be combined with FuseMax to
    To evaluate the benefits of FuseMax on end-to-end trans-                            improve performance and energy efficiency, though we leave
 former inference, we include the other required linear lay-                            their specification and implementation to future work.
 ers (Section IV-A). We use Timeloop to search for optimal
 mappings for these linear layers and use the same mappings                                                  VIII. C ONCLUSION
 for all three accelerator configurations. The attention modeling                          This paper advanced the state of the art in spatial accelerator
 remains the same as Section VI-B.                                                      design for transformer inference. To do so, we expressed
    Speedup. Figure 10 shows the performance improvement                                attention and its variants as cascades of Einsums. We used
 achieved by FuseMax. Across the sequence lengths tested,                               these cascades to reason about attention’s characteristics, in-
 FuseMax achieves an average speedup of 7.6× over the                                   dependent of its mapping/scheduling. Using these principles,
 unfused baseline and 5.3× over FLAT. As discussed in Sec-                              we proposed FuseMax—an accelerator that uses deep fusion
 tion IV-A, as sequence length grows, attention becomes a                               and fine-grain pipelining to map attention onto a spatial
 larger fraction of the total required compute. Therefore, at 1M                        architecture. FuseMax achieves ∼ 100% utilization of both
 tokens, FuseMax achieves an average 10× speedup over the                               PE arrays, demonstrating 6.7× speedup over the prior state-
 unfused baseline and 7.5× speedup over FLAT.                                           of-the-art (FLAT) using 79% of the energy on attention and
    Energy. Figure 11 shows the energy reduction achieved by                            5.3× speedup over FLAT using 83% of the energy on end-to-
 FuseMax. Here, we see similar results: as attention becomes                            end inference.
 a larger fraction of the kernel, the energy reduction increases.                          Our work shows that cascades of Einsums provide a
 FuseMax uses 82% of the unfused baseline and 83% of                                    powerful abstraction for representing and analyzing domain-
 FLAT’s energy during end-to-end inference.                                             specific kernels. Future work may explore their application
 D. Pareto-Optimality of FuseMax                                                        to other attention variants (e.g., those exploiting quantization
                                                                                        and sparsity) or even other domains (e.g., fully homomorphic
   We further observe that by varying the size of the PE array
                                                                                        encryption, scientific computing, relational algebra, etc.). Do-
 (between 16×16 and 512×512) and setting the global and per-
                                                                                        ing so enables mapping-agnostic analysis and may elucidate
   6 FLAT reports larger energy savings over the unfused baseline because it            previously undiscovered cascades and schedules for these
 only reports energy associated with DRAM traffic during the tensor products.           algorithms.
                     ACKNOWLEDGMENT                                  F. Expected Results
   We thank the anonymous MICRO and MLArchSys review-                  Graphs        will       be      displayed       within
ers for their feedback on submitted versions of the work; Ab-        the     Jupyter      notebook    and/or      found     in
himanyu Bambhaniya, Sheng-Chun Kao and Tushar Krishna                workspace/outputs/generated/<timestamp
for discussions on FLAT; and finally Tanner Andrulis and             or default>/figs/. They can be compared with
Angshuman Parashar for help with Timeloop. We would also             Figures 6-12 in the paper or the corresponding figures in
like to thank Nafea Bshara, Ron Diamant, Serina Tan, Stephen         workspace/outputs/pregenerated/figs/.
Neuendorffer, Yakun Sophia Shao, Hongbin Zheng, and others           G. Methodology
at Amazon, AMD, and NVIDIA for helpful discussions about
                                                                        Submission, reviewing and badging methodology:
the work as it matured.
                                                                        • https://www.acm.org/publications/policies/

                          A PPENDIX                                       artifact-review-and-badging-current
                                                                        • https://cTuning.org/ae
A. Abstract
                                                                                                    R EFERENCES
   In this artifact, we provide Timeloop and Accelergy models
                                                                      [1] “Tensor network contractions,” ser. Lecture Notes in Physics, vol. 964.
of the accelerator FuseMax, an accelerator for encoder-style              Springer Cham, 2020.
transformer inference. For ease-of-use, we provide a Docker           [2] M. Abadi, P. Barham, J. Chen, Z. Chen, A. Davis, J. Dean, M. Devin,
container and a set of Jupyter notebooks through which to run             S. Ghemawat, G. Irving, M. Isard et al., “Tensorflow: a system for large-
                                                                          scale machine learning,” in OSDI’16.
the experiments. This artifact can be evaluated on an x86-84          [3] AWS.        (2024)     Trainium     architecture.      [Online].     Avail-
machine with 5 GB of disk space.                                          able: https://awsdocs-neuron.readthedocs-hosted.com/en/latest/general/
                                                                          arch/neuron-hardware/trainium.html
                                                                      [4] A. Baevski, H. Zhou, A. Mohamed, and M. Auli, “wav2vec 2.0: a
B. Artifact check-list (meta-information)                                 framework for self-supervised learning of speech representations,” in
  • Algorithm: Timeloop/Accelergy model of the FuseMax accel-             NIPS ’20.
    erator and the baselines it was evaluated against                 [5] J. S. Bridle, “Probabilistic interpretation of feedforward classification
                                                                          network outputs, with relationships to statistical pattern recognition,” in
  • Program: Python, Timeloop, Accelergy
                                                                          NATO Neurocomputing, 1989.
  • Run-time environment: Docker, Jupyter
                                                                      [6] S. Chen, S. Huang, S. Pandey, B. Li, G. R. Gao, L. Zheng, C. Ding,
  • Hardware: x86-64 machine                                              and H. Liu, “E.t.: Re-thinking self-attention for transformer models on
  • Output: Plots generated from scripts                                  gpus,” in SC’21.
  • Experiments: Modeling of the five different accelerator design    [7] T. Chen, Z. Du, N. Sun, J. Wang, C. Wu, Y. Chen, and O. Temam,
    points via Timeloop and Accelergy models                              “Diannao: A small-footprint high-throughput accelerator for ubiquitous
  • How much disk space required (approximately)?: 5GB                    machine-learning,” ACM Sigplan Notices, 2014.
  • How much time is needed to prepare workflow (approxi-             [8] Y. Chen, Y. Xie, L. Song, F. Chen, and T. Tang, “A survey of accelerator
    mately)?: 20 minutes                                                  architectures for deep neural networks,” Engineering, 2020.
                                                                      [9] Y.-H. Chen, J. Emer, and V. Sze, “Eyeriss: A spatial architecture for
  • How much time is needed to complete experiments (approx-
                                                                          energy-efficient dataflow for convolutional neural networks,” in ISCA’16.
    imately)?: 9 hours                                               [10] ——, “Eyeriss v2: A flexible and high-performance accelerator for
  • Publicly available?: Yes                                              emerging deep neural networks,” in ArXiv, 2018.
  • Archived (provide DOI)?: Provided after evaluation               [11] Y. Chen, T. Luo, S. Liu, S. Zhang, L. He, J. Wang, L. Li, T. Chen,
                                                                          Z. Xu, N. Sun, and O. Temam, “Dadiannao: A machine-learning
                                                                          supercomputer,” in MICRO’14.
C. Description - How to access                                       [12] J. Choi, H. Li, B. Kim, S. Hwang, and J. H. Ahn, “Accelerating trans-
   The artifact is hosted on Github at https://github.com/                former networks through recomposing softmax layers,” in IISWC’22.
                                                                     [13] A. Conneau and G. Lample, “Cross-lingual language model pretraining,”
FPSG-UIUC/micro24-fusemax-artifact. Following the in-                     in NIPS’19.
structions in this repository will allow you to install              [14] T. Dao, “Flashattention-2: Faster attention with better parallelism and
the relevant dependences, run the experiments, and dis-                   work partitioning,” in ArXiv, 2023.
                                                                     [15] T. Dao, D. Y. Fu, S. Ermon, A. Rudra, and C. Ré, “Flashattention: Fast
play the graphs. System requirements can be found                         and memory-efficient exact attention with io-awareness,” in ArXiv, 2022.
at https://github.com/FPSG-UIUC/micro24-fusemax-artifact/            [16] T. Dettmers, M. Lewis, Y. Belkada, and L. Zettlemoyer, “Llm.int8():
blob/main/README.md#system-requirements.                                  8-bit matrix multiplication for transformers at scale,” in ArXiv, 2022.
                                                                     [17] J. Devlin, M.-W. Chang, K. Lee, and K. Toutanova, “Bert: Pre-training
                                                                          of deep bidirectional transformers for language understanding,” in
D. Installation                                                           NAACL’19.
                                                                     [18] A. Dosovitskiy, L. Beyer, A. Kolesnikov, D. Weissenborn, X. Zhai,
   Installation instructions can be found at https:                       T. Unterthiner, M. Dehghani, M. Minderer, G. Heigold, S. Gelly,
//github.com/FPSG-UIUC/micro24-fusemax-artifact/blob/                     J. Uszkoreit, and N. Houlsby, “An image is worth 16x16 words:
                                                                          Transformers for image recognition at scale,” in ICLR’21.
main/README.md#installation.                                         [19] I. S. Duff, M. A. Heroux, and R. Pozo, “An overview of the sparse basic
                                                                          linear algebra subprograms: The new standard from the BLAS technical
E. Evaluation                                                             forum,” in TOMS’02.
                                                                     [20] E. Frantar, S. Ashkboos, T. Hoefler, and D. Alistarh, “GPTQ: Accurate
   Evaluation instructions can be found at https:                         post-training compression for generative pretrained transformers,” in
                                                                          ArXiv, 2022.
//github.com/FPSG-UIUC/micro24-fusemax-artifact/blob/                [21] M. Gilbert, Y. N. Wu, A. Parashar, V. Sze, and J. S. Emer, “Looptree: En-
main/README.md#run-experiments.                                           abling exploration of fused-layer dataflow accelerators,” in ISPASS’23.
[22] C. Guo, J. Tang, W. Hu, J. Leng, C. Zhang, F. Yang, Y. Liu, M. Guo,               L. Fang, J. Bai, and S. Chintala, “Pytorch: an imperative style, high-
     and Y. Zhu, “Olive: Accelerating large language models via hardware-              performance deep learning library,” in NIPS’19.
     friendly outlier-victim pair quantization,” in ISCA’23.                      [43] M. Pellauer, J. Clemons, V. Balaji, N. C. Crago, A. Jaleel, D. Lee,
[23] C. Hong, Q. Huang, G. Dinh, M. Subedar, and Y. S. Shao, “DOSA:                    M. O’Connor, A. Parashar, S. Treichler, P. Tsai, S. W. Keckler, and
     Differentiable model-based one-loop search for DNN accelerators,” in              J. S. Emer, “Symphony: Orchestrating sparse and dense tensors with
     MICRO’23.                                                                         hierarchical heterogeneous processing,” in TOCS’23.
[24] C. Hooper, S. Kim, H. Mohammadzadeh, M. W. Mahoney, Y. S. Shao,              [44] S. Pichai and D. Hassabis. (2024) Our next-generation model:
     K. Keutzer, and A. Gholami, “Kvquant: Towards 10 million context                  Gemini 1.5. [Online]. Available: https://blog.google/technology/ai/
     length llm inference with kv cache quantization,” in ArXiv, 2024.                 google-gemini-next-generation-model-february-2024/#context-window
[25] O. Hsu, M. Strange, R. Sharma, J. Won, K. Olukotun, J. S. Emer,              [45] R. Prabhakar, Y. Zhang, D. Koeplinger, M. Feldman, T. Zhao, S. Hadjis,
     M. A. Horowitz, and F. Kjølstad, “The sparse abstract machine,” in                A. Pedram, C. Kozyrakis, and K. Olukotun, “Plasticine: A reconfigurable
     ASPLOS’23.                                                                        architecture for parallel paterns,” in SIGARCH Computer Architecture
[26] W.-N. Hsu, B. Bolte, Y.-H. H. Tsai, K. Lakhotia, R. Salakhutdinov, and            News, 2017.
     A. Mohamed, “Hubert: Self-supervised speech representation learning          [46] Z. Qu, L. Liu, F. Tu, Z. Chen, Y. Ding, and Y. Xie, “Dota: detect and
     by masked prediction of hidden units,” TASLP’21.                                  omit weak attentions for scalable transformer acceleration,” in ASPLOS
[27] N. P. Jouppi, C. Young, N. Patil, D. Patterson, G. Agrawal, R. Bajwa,             ’22.
     S. Bates, S. Bhatia, N. Boden, A. Borchers, R. Boyle, P.-l. Cantin,          [47] M. N. Rabe and C. Staats, “Self-attention does not need o(n2 ) memory,”
     C. Chao, C. Clark, J. Coriell, M. Daley, M. Dau, J. Dean, B. Gelb,                in ArXiv, 2022.
     T. V. Ghaemmaghami, R. Gottipati, W. Gulland, R. Hagmann, C. R.              [48] A. Radford and K. Narasimhan, “Improving language understanding
     Ho, D. Hogberg, J. Hu, R. Hundt, D. Hurt, J. Ibarz, A. Jaffey,                    by generative pre-training,” 2018. [Online]. Available: https://api.
     A. Jaworski, A. Kaplan, H. Khaitan, D. Killebrew, A. Koch, N. Kumar,              semanticscholar.org/CorpusID:49313245
     S. Lacy, J. Laudon, J. Law, D. Le, C. Leary, Z. Liu, K. Lucke,               [49] C. Raffel, N. Shazeer, A. Roberts, K. Lee, S. Narang, M. Matena,
     A. Lundin, G. MacKean, A. Maggiore, M. Mahony, K. Miller, R. Na-                  Y. Zhou, W. Li, and P. J. Liu, “Exploring the limits of transfer learning
     garajan, R. Narayanaswami, R. Ni, K. Nix, T. Norrie, M. Omernick,                 with a unified text-to-text transformer,” in JMLR’20.
     N. Penukonda, A. Phelps, J. Ross, M. Ross, A. Salek, E. Samadiani,           [50] J. Shah, G. Bikshandi, Y. Zhang, V. Thakkar, P. Ramani, and T. Dao,
     C. Severn, G. Sizikov, M. Snelham, J. Souter, D. Steinberg, A. Swing,             “Flashattention-3: Fast and accurate attention with asynchrony and low-
     M. Tan, G. Thorson, B. Tian, H. Toma, E. Tuttle, V. Vasudevan, R. Wal-            precision,” in ArXiv, 2024.
     ter, W. Wang, E. Wilcox, and D. H. Yoon, “In-datacenter performance          [51] V. Sze, Y. Chen, T. Yang, and J. S. Emer, Efficient Processing of Deep
     analysis of a tensor processing unit,” in ISCA’17.                                Neural Networks, ser. Synthesis Lectures on Computer Architecture.
[28] S.-C. Kao, S. Subramanian, G. Agrawal, A. Yazdanbakhsh, and T. Kr-                Springer, 2020.
     ishna, “Flat: An optimized dataflow for mitigating attention bottlenecks,”   [52] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez,
     in ASPLOS’23.                                                                     L. Kaiser, and I. Polosukhin, “Attention is all you need,” in NIPS’17.
[29] H. Kwon, P. Chatarasi, M. Pellauer, A. Parashar, V. Sarkar, and T. Kr-       [53] H. Wang, Z. Zhang, and S. Han, “Spatten: Efficient sparse attention
     ishna, “Understanding reuse, performance, and hardware cost of DNN                architecture with cascade token and head pruning,” in HPCA’21.
     dataflow: A data-centric approach,” in MICRO’19.                             [54] J. Wang, L. Guo, and J. Cong, “Autosa: A polyhedral compiler for high-
[30] C. L. Lawson, R. J. Hanson, D. R. Kincaid, and F. T. Krogh, “Basic                performance systolic arrays on fpga,” in FPGA’21.
     linear algebra subprograms for fortran usage,” in TOMS’79.                   [55] J. Won, C. Hong, C. Mendis, J. Emer, and S. Amarasinghe, “Unified
[31] H. Le, L. Vial, J. Frej, V. Segonne, M. Coavoux, B. Lecouteux, A. Al-             convolution framework: A compiler-based approach to support sparse
     lauzen, B. Crabbé, L. Besacier, and D. Schwab, “Flaubert: Unsupervised           convolutions,” in MLSys’23.
     language model pre-training for french,” in ArXiv, 2019.                     [56] Y. N. Wu, J. S. Emer, and V. Sze, “Accelergy: An architecture-level
[32] J. Lin, J. Tang, H. Tang, S. Yang, W.-M. Chen, W.-C. Wang, G. Xiao,               energy estimation methodology for accelerator designs,” in ICCAD’19.
     X. Dang, C. Gan, and S. Han, “Awq: Activation-aware weight quanti-           [57] Y. N. Wu, P. Tsai, S. Muralidharan, A. Parashar, V. Sze, and J. S. Emer,
     zation for llm compression and acceleration,” in MLSys’24.                        “HighLight: Efficient and flexible DNN acceleration with hierarchical
[33] Z. Liu, Y. Lin, Y. Cao, H. Hu, Y. Wei, Z. Zhang, S. Lin, and                      structured sparsity,” in MICRO’23.
     B. Guo, “Swin transformer: Hierarchical vision transformer using shifted     [58] Y. N. Wu, P.-A. Tsai, A. Parashar, V. Sze, and J. S. Emer, “Sparseloop:
     windows,” in ICCV’21.                                                             An analytical approach to sparse tensor accelerator modeling,” in MI-
[34] L. Lu, Y. Jin, H. Bi, Z. Luo, P. Li, T. Wang, and Y. Liang, “Sanger: A            CRO’22.
     co-design framework for enabling sparse attention using reconfigurable       [59] J. Xia, W. Fu, M. Liu, and M. Wang, “Low-latency bit-accurate ar-
     architecture,” in MICRO’21.                                                       chitecture for configurable precision floating-point division,” in Applied
[35] N. Nayak, T. Odemuyiwa, S. Ugare, C. W. Fletcher, M. Pellauer, and                Sciences, 2021.
     J. Emer, “Teaal: A declarative framework for modeling sparse tensor          [60] A. H. Zadeh, I. Edo, O. M. Awad, and A. Moshovos, “Gobo: Quan-
     accelerators,” in MICRO’23.                                                       tizing attention-based nlp models for low latency and energy efficient
[36] P. Nilsson, A. U. R. Shaik, R. Gangarajaiah, and E. Hertz, “Hardware              inference,” in MICRO’20.
     implementation of the exponential function using taylor series,” in          [61] G. Zhang, N. Attaluri, J. S. Emer, and D. Sanchez, “Gamma: Leveraging
     NORCHIP’14.                                                                       gustavson’s algorithm to accelerate sparse matrix multiplication,” in
[37] T. Norrie, N. Patil, D. H. Yoon, G. Kurian, S. Li, J. Laudon, C. Young,           ASPLOS’21.
     N. Jouppi, and D. Patterson, “The design process for google’s training       [62] S. Zheng, S. Chen, S. Gao, L. Jia, G. Sun, R. Wang, and Y. Liang,
     chips: Tpuv2 and tpuv3,” IEEE Micro, 2021.                                        “Tileflow: A framework for modeling fusion dataflow via tree-based
[38] T. O. Odemuyiwa, H. Asghari-Moghaddam, M. Pellauer, K. Hegde, P.-                 analysis,” in MICRO’23.
     A. Tsai, N. Crago, A. Jaleel, J. D. Owens, E. Solomonik, J. Emer,
     and C. Fletcher, “Accelerating sparse data orchestration via dynamic
     reflexive tiling,” in ASPLOS’23.
[39] T. O. Odemuyiwa, J. S. Emer, and J. D. Owens, “The EDGE language:
     Extended general einsums for graph algorithms,” in ArXiv, 2024.
[40] A. Parashar, M. Pellauer, M. Adler, B. Ahsan, N. Crago, D. Lustig,
     V. Pavlov, A. Zhai, M. Gambhir, A. Jaleel, R. Allmon, R. Rayess,
     S. Maresh, and J. Emer, “Efficient spatial processing element control
     via triggered instructions,” IEEE Micro, 2014.
[41] A. Parashar, P. Raina, Y. S. Shao, Y.-H. Chen, V. A. Ying, A. Mukkara,
     R. Venkatesan, B. Khailany, S. W. Keckler, and J. Emer, “Timeloop: A
     systematic approach to dnn accelerator evaluation,” in ISPASS’19.
[42] A. Paszke, S. Gross, F. Massa, A. Lerer, J. Bradbury, G. Chanan,
     T. Killeen, Z. Lin, N. Gimelshein, L. Antiga, A. Desmaison, A. Köpf,
     E. Yang, Z. DeVito, M. Raison, A. Tejani, S. Chilamkurthy, B. Steiner,
