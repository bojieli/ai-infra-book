   Unleashing CPU Potential for Executing GPU
 Programs through Compiler/Runtime Optimizations
                 Ruobing Han                                 Jisheng Zhao                               Hyesoon Kim
        Georgia Institute of Technology            Georgia Institute of Technology             Georgia Institute of Technology
                Atlanta, USA                               Atlanta, USA                                Atlanta, USA
           hanruobing@gatech.edu                    jisheng.zhao@cc.gatech.edu                    hyesoon@cc.gatech.edu



   Abstract—Although modern CPUs deliver high performance,             threads. MCUDA [49] proposes a compiler transformation that
they are often under-utilized in CPU-GPU heterogeneous systems.        wraps the workload of an entire GPU block and executes it
Enabling CPUs to execute GPU programs facilitates workload             using a single CPU thread. This transformation, named ’flat
sharing between CPUs and GPUs, which can increase CPU device
utilization and overall benefit heterogeneous systems.                 collapsing transformation’ in [23], enhances per-
   The flat collapsing transformation represents a state-of-the-       formance compared to directly mapping one GPU thread to
art solution for GPU-to-CPU migration and is widely accepted           one CPU thread by reducing the overhead of thread context
in both academic and commercial projects. However, in this             switching and better utilizing the vector units in CPU cores. An
paper, we identify that CPU programs transformed by flat               example of flat collapsing transformation is demonstrated in
collapsing transformation are not compatible with standard
CPU compiler optimizations and runtime environments, which             Figure 1. The flat collapsing transformation can be implemented
leads to suboptimal performance. Based on the observation, we          using either a source-to-source translator [9], [49] or low-
propose four compiler/runtime optimizations. These optimizations       level Intermediate Representation (IR) transformation [12],
complement flat collapsing transformation and help generate high-      [21], [23], [40]. We use a CUDA-to-OpenMP example to
performance programs. Our evaluations demonstrate an average           demonstrate how flat collapsing transformation aims to utilize
performance improvement of 20.84% over the state-of-the-art
framework on an x86 CPU and 16.10% on an ARM CPU.                      CPU computational resources for executing GPU programs.

                        I. I NTRODUCTION
   For the stereotype of CPU-GPU heterogeneous systems,
GPUs are the predominant devices for executing massively
computing applications, while CPUs are primarily required to
provide more accessible options to users. However, it is often
overlooked that CPUs also provide a substantial amount of
computational resources. For example, the latest Intel Gold
6423N [6] and AMD EPYC 9654P [5] CPUs feature 28 and 96
CPU cores, respectively, along with AVX support, providing
up to 6.5 and 10.9 TFLOPS/sec of computational power.
Similarly, ARM-based CPUs like Fujitsu’s A64FX deliver a
peak performance of 5.4 TFLOPS/sec [2]. These CPUs achieve
peak data processing performance at a magnitude similar to that
of NVIDIA A100 GPUs, which offer 19.5 TFLOPS [4]. This                 Fig. 1: An example of a CUDA program and the corresponding
implies that, with full utilization of the performance capabilities,   CPU program transformed by flat collapsing transformation.
CPUs might achieve performance comparable to GPUs.
   Although CPUs provide a substantial amount of com-                     The workflow for existing GPU-to-CPU solutions [3], [21],
putational resources, they are consistently underutilized in           [23], [27], [49] is illustrated by the solid black line in Figure 2.
heterogeneous systems. It is not surprising to find users waiting      Initially, GPU programs are transformed into CPU programs
for hours to have GPU devices scheduled while many CPUs                using flat collapsing transformation. These generated CPU
remain idle. This observation raises an important question:            programs are then subjected to standard compiler optimizations.
"Can CPUs be effectively utilized to run GPU applications?"            Finally, runtime libraries that implement GPU APIs (e.g.,
   GPU programs typically consist of a large number of                 cudaMalloc, cudaMemcpy, kernel launch) with equivalent CPU
lightweight parallel tasks, whereas CPUs are designed to handle        functions are linked to produce executable CPU files.
a relatively small number of heavy workloads. Running GPU                 However, in this paper, we highlight that this pipeline
programs on CPUs requires transforming programs designed for           does not effectively utilize the computational potential of
the bulk-synchronous parallel model into traditional program-          CPUs for two main reasons. First, the CPU programs are
ming models, which do not support a high number of active              transformed from GPU programs, which are optimized for GPU
                                                                                          II. BACKGROUND
                                                                    A. Flat Collapsing Transformation
                                                                       GPUs are designed to handle the efficient execution of a large
                                                                    number of lightweight tasks, while CPUs are optimized for a
                                                                    smaller quantity of heavy tasks. To reconcile the disparity in
                                                                    parallelism and workload granularity, MCUDA [49] introduces
                                                                    a compiler transformation referred to as the flat collapsing
                                                                    transformation. The transformation aims to migrate GPU
                                                                    programs to CPUs in an efficient manner. Essentially, the
                                                                    flat collapsing transformation wraps the workload in a CUDA
                                                                    block into a CPU function to be executed by a CPU thread.
                                                                    Figure 1 showcases a CUDA application and the CPU program
Fig. 2: The existing GPU-to-CPU migration process is depicted       transformed by the flat collapsing transformation.
in black text. We propose four optimizations, highlighted in           There are several important points to note in this example.
green text, which can be integrated into this workflow.             Firstly, all CUDA blocks are transformed into iterations within
                                                                    a loop in the main function of the transformed CPU program,
                                                                    with length equals to the grid size, allowing parallelization
architectures. Some GPU optimizations do not benefit CPU            across multiple CPU threads. Secondly, the CUDA threads
architectures. Second, the existing pipeline treats transformed     are also transformed into iterations within loops, with lengths
CPU programs as ordinary CPU programs and optimizes them            equivalent to the block size. These loops can be vectorized
with standard compiler optimizations. However, CPU programs         using SIMD instructions. We call these loops flat loops in
transformed by flat collapsing transformation are usually more      this paper. Thirdly, the CUDA shared memory variable s is
complex than manually written CPU programs. Therefore, the          mapped to CPU thread-local memory. Fourthly, a barrier is
existing compiler optimizations are too general to effectively      present in the CUDA program (line 5th), causing the flat
optimize these programs.                                            collapsing transformation to generate two sequential flat loops
                                                                    that wrap the CUDA code before (line 3-4th) and after the
   Based on these observations, we propose four compiler/run-
                                                                    barrier (line 6th). Lastly, the local variable idx is extended to
time optimizations designed to enhance the performance of
                                                                    arrays with lengths equal to the CUDA block size, as they are
transformed CPU programs. These optimizations, which are
                                                                    referenced in both flat loops. This transformation is critical for
compatible with flat collapsing transformation and highlighted
                                                                    maintaining the correctness of flat collapsing transformation,
in green in Figure 2, specifically address key inefficiencies.
                                                                    which is called ’selective replication’ in MCUDA [49]. The
The anti-coalescing transformation and tail block adaptive
                                                                    insight is that, after migrating to a CPU program, where a
synchronization are compiler transformations applied to GPU
                                                                    CPU thread needs to execute a GPU block workload, the CPU
programs, while block size invariant analysis is a transformation
                                                                    thread should create arrays to store local variables of each
applied to CPU programs. GPU-block dynamic tiling is a
                                                                    GPU thread in the block.
runtime optimization that targets the kernel launch function.
                                                                       In general, flat collapsing transformation is a compiler
   This paper offers several noteworthy contributions:              transformation that groups multiple parallel fine-grained work-
                                                                    loads into a single coarse-grained task. It reduces the number
  • Identification of unresolved efficiency challenges in exist-
                                                                    of parallel tasks by increasing the workload of each task,
    ing GPU-to-CPU migration solutions.
                                                                    thereby bridging the gap between the number of concurrent
  • Introduction of three compiler optimizations designed to
                                                                    threads required by software developers and the concurrent
    enhance the performance of generated CPU programs.
                                                                    cores supported by hardware. Similar transformations are
  • Introduction of a runtime optimization that dynamically
                                                                    utilized to improve the runtime of GPU programs [8] or to
    adjusts the workload distribution to optimally align with
                                                                    support performance portability of SPMD languages on devices
    CPU computational resources.
                                                                    with less parallelism [23], [27], [31]. Thus, we believe the
  • Integration of the proposed optimizations into a state-
                                                                    optimizations proposed in this paper can also be extended for
    of-the-art GPU-to-CPU solution, resulting in significant
                                                                    GPU-to-GPU transformations and projects aimed at SPMD
    performance improvements on both x86 (20.84%) and
                                                                    performance portability.
    ARM CPUs (16.10%).

   In this paper, we focus on migrating CUDA applications to        B. Challenges for Existing Solutions
CPUs. However, the proposed optimizations are not limited             Although flat collapsing transformation is expected to
to any specific programming languages. Consequently, the            generate programs suitable for CPUs, we find that the generated
technical contributions are also applicable to other parallel       CPU programs consistently exhibit suboptimal performance.
programming models (e.g., HIP [1], OpenCL [41]) and libraries       The low performance stems from two main reasons. First,
(e.g., OCCA [38], Kokkos [51]).                                     the original GPU programs are designed for utilizing GPU
computation resource, which may not perform optimally on                                                       GPU-to-CPU solutions
                                                                              Metrics        NV-GPU
CPU architectures. Second, flat collapsing transformation is not                                           DPC++       MCUDA        CuPBoP
compatible with existing compiler optimizations. Specifically,                # of LLC ld    4,578,831    9,086,165   11,034,617    8,899,486
the CPU programs generated by flat collapsing transformation                  # of LLC st    4,179,010    6,989,671    414,866       637,932
are too complex to be effectively analyzed and transformed by                 LLC ld Hit     90.83%2        72.2%       63.4%         72.6%
                                                                              LLC st Hit     90.83%2        76.1%       76.5%         83.7%
standard compiler optimizations. Consequently, these programs
cannot be optimized significantly.                                        TABLE I: The profiling result for the CUDA program and
   We illustrate a CUDA program and the corresponding                     CPU programs transformed by three CUDA-to-CPU solutions.
CPU program generated by flat collapsing transformation in
Figure 1. Although the original CUDA program is simple, the
transformed CPU program is more complex: The CPU program                  B. Flat Loop Optimization
includes indirect memory accesses (lines 5-6th) introduced by
flat collapsing transformation. These indirect memory accesses                      All transformed CPU programs are loop-intensive because
bring challenges for compiler optimization, as the compiler                      flat collapsing transformation introduces flat loops to wrap the
cannot clearly determine memory access dependencies. An                          original CUDA code. Therefore, optimizing these flat loops
attempt to optimize this code section using LLVM Polly [19],                     is crucial. Each flat loop has a uniform length equivalent to
a popular polyhedral model compiler, results in the warning                      the CUDA block size, which is typically a runtime variable.
message "The array subscript of ’s’ is not affine," preventing                   Consequently, all flat loops are classified as dynamic loops.
Polly from optimizing this section. Additionally, flat collapsing                   There are two common strategies for loop optimization: loop
transformation generates a significant number of loops where                     vectorization and reduction of the loop body. However, both
the length is equal to the CUDA block size. Since the CUDA                       approaches face significant challenges. Dynamic flat loops
block size is typically a runtime variable, these loops are                      present greater complexity for vectorization compared to static
dynamic1 and challenging to optimize because the compiler                        loops. Similarly, optimizing the loop body is challenging due to
has limited knowledge about their properties.                                    the great amount of instructions dependent on runtime variables,
                                                                                 which hinders effective analysis and optimization. Moreover,
                                                                                 flat collapsing transformation often introduces indirect memory
                    III. P ROBLEM S TATEMENTS
                                                                                 access, which poses challenges for memory access dependency
    We provide detailed discussions about the limitations of analysis and impedes the application of compiler optimizations.
existing GPU-to-CPU solutions in this section and introduce                         It is important to note that even for CUDA programs that
four corresponding optimizations in Section IV.                                  use  a constant value as the block size, it is still challenging
                                                                                 for flat collapsing transformation to generate static loops. This
                                                                                 limitation arises because flat collapsing transformation is a
A. Divergent Memory Access Preferences                                           module-level transformation, which cannot utilize cross-module
    GPU and CPU architectures have different memory access information. A CUDA program contains two modules: host
preferences. CUDA optimizes memory bandwidth utilization and kernel, executed on the CPU and GPU respectively. The
by grouping interleaved global memory access among threads block size value is specified in the host module. The kernel
into a single request, a technique known as memory coalescing. module gets the block size by reading from NVIDIA GPU
In contrast, CPUs favor sequential memory access patterns to intrinsic registers at runtime. Thus, it is challenging for flat
achieve high spatial locality. Consequently, directly transform- collapsing transformation to retrieve the static block size across
ing CUDA’s interleaved access pattern into CPU code could the module boundary since it is a module-level transformation.
result in poor cache utilization on the CPU. An example that
derived from Hetero-Mark [50] is shown in Listing 1. The C. Uneven Workload Configuration
CUDA program contains an interleaved access pattern (line 7th)
to apply global memory coalescing. However, the transformed                         The third challenge arises from the mapping strategy
CPU program has poor spatial locality (line 20th).                               employed     by flat collapsing transformation, which assigns
                                                                                 a GPU block to a CPU thread. From the hardware perspective,
    We profile this program on both an NVIDIA GTX TITAN
                                                                                 this approach maps a GPU streaming multiprocessor (SM) to
X GPU and Intel CPUs using three GPU-to-CPU migration
                                                                                 a CPU core. Workloads optimized for a GPU SM may not
solutions [21], [26], [49]. The comparison of Last-Level-Cache
                                                                                 perform equally well on a CPU core. For instance, an SM in
(LLC) performance, as shown in Table I, reveals that the CPU
                                                                                 the NVIDIA A100 GPU is equipped with 192KB of L1 cache
programs transformed from CUDA do not utilize the CPU cache
                                                                                 and shared memory, and it provides a computational capacity
as efficiently as the original programs on NVIDIA GPUs.
                                                                                 of 180 GFLOPs. In contrast, a core in the Intel 6424N CPU
                                                                                 comes with a 2MB L2 cache and has a computational capacity
   1 Dynamic loops are loops with unknown number of iterations at compile
                                                                                 of 230 GFLOPs. Consequently, direct mapping of a CUDA
time.
   2 The GPU hit rate is not split into load and store, as the profiling toolkit block to a CPU thread can lead to suboptimal utilization of
does not distinguish between load and store hit rates.                           the CPU’s computational resources.
                          IV. O PTIMIZATIONS                        effectively circumvents the complications arising from flat
                                                                    collapsing transformation.
     To address the issues outlined in Section III, we propose four
                                                                       The CPU program transformed by flat collapsing transforma-
  compiler/runtime optimizations: In response to the disparities
                                                                    tion (the bottom part of Listing 1) contains nested loops. The
  in memory access patterns, we introduce anti-coalescing trans-
                                                                    outer loop is generated by the flat collapsing transformation
  formation, a novel GPU-to-GPU transformation that eliminates
                                                                    (refer to f lat_loop), and the inner loop is the loop that
  the negative effects of memory coalescing. We propose block
                                                                    contains the global memory coalescing in the original CUDA
  size invariant analysis and tail block adaptive synchronization
                                                                    program (refer to original_loop). The high locality can be
  to tackle the challenges associated with flat loops. Furthermore,
                                                                    achieved by exchanging the order of outer/inner loops, as the
  to effectively cater to the specific requirements of CPU
                                                                    memory access index (index) is linearly increased with the
  architectures regarding workload configuration, we propose
                                                                    f lat_loop’s induction variable (tid). However, since there is
  GPU-block dynamic tiling, which redistributes workloads to
                                                                    a data dependency (tid->index) between these loops, the loop
  ensure optimal utilization of CPU resources.
                                                                    reordering cannot be applied.
     These optimizations can be integrated into the existing
                                                                       We attempt to use LLVM optimization and the Polly
  GPU-to-CPU workflow (highlighted in green in Figure 2).
                                                                    compiler [19] to optimize the cpu_Histogram function. The
  The anti-coalescing transformation and tail block adaptive
                                                                    LLVM loop-interchange optimization reports a dependency
  synchronization are GPU-to-GPU transformations, while block
                                                                    between the flat loop and the original loop and cannot
  size invariant analysis optimizes the transformed CPU programs.
                                                                    transform the nested loops. Polly detects indirect memory
  GPU-block dynamic tiling is a runtime optimization that targets
                                                                    access when accessing the priv_hist array and leaves the
  the kernel launch function within the runtime library.
                                                                    code section unchanged. This evaluation demonstrates the
 1 // original CUDA program
 2 __global__ void cuda_Histogram(uint32_t *pixels,
                                                                    challenges existing compiler optimizations face when analyzing
         uint32_t num_pixels) {                                     programs generated by flat collapsing transformation.
 3     uint32_t priv_hist[256];
 4     uint32_t gsize = blockDim.x * gridDim.x;                        1   // incorrect CUDA program
 5     uint32_t index = blockDim.x * blockIdx.x+threadIdx.x;           2   __global__ void transformed_cuda_Histogram(uint32_t *
 6     while (index < num_pixels) {                                             pixels, uint32_t num_pixels) {
 7       uint32_t color = pixels[index]; // global memory              3     uint32_t priv_hist[256];
          coalescing                                                   4     uint32_t gsize = blockDim.x * gridDim.x;
 8       priv_hist[color]++;                                           5     uint32_t index = blockDim.x * blockIdx.x+threadIdx.x;
 9       index += gsize;                                               6     while (index < num_pixels) {
10     }                                                               7       uint32_t color = pixels[index];
11     ...                                                             8       priv_hist[color]++;
12   }                                                                 9       index += gsize;
13   // CPU program transformed by the flat collapsing                10       __syncthreads(); // incorrect barrier usage, as not
14   void cpu_Histogram(uint32_t *pixels, uint32_t                               all threads can access it
          num_pixels) {                                               11     }
15     for (int tid = 0; tid < BLOCK_SIZE; tid++) { // flat           12   }
          loop                                                        13   // correct CUDA program, generated by the anti-
16       uint32_t priv_hist[256];                                               coalescing transformation
17       uint32_t gsize = BLOCK_SIZE * GRID_SIZE;                     14   __global__ void transformed_cuda_Histogram(uint32_t *
18       uint32_t index = BLOCK_SIZE * BLOCK_ID + tid;                          pixels, uint32_t num_pixels) {
19       while (index < num_pixels) { // original loop                15       uint32_t priv_hist[256];
20         uint32_t color = pixels[index]; // poor locality           16       uint32_t gsize = blockDim.x * gridDim.x;
21         priv_hist[color]++;                                        17       uint32_t index = blockDim.x * blockIdx.x+threadIdx.
22         index += gsize;                                                      x;
23       }                                                            18       __shared__ bool has_activated_thread;
24       ...                                                          19       bool activated = true;
25     }                                                              20       do {
26   }                                                                21           has_activated_thread = false;
                                                                      22           __syncthreads(); // correct barrier usage, all
        Listing 1: The Histogram benchmark in Hetero-Mark.                      threads can access it
                                                                      23           activated &= (index < num_pixels);
                                                                      24           has_activated_thread |= activated;
     A. Anti-Coalescing Transformation                                25           if (activated) {
                                                                      26             uint32_t color = pixels[index];
                                                                                     priv_hist[color]++;
        1) Insight: As noted in Section III, one of the most common 2728             index += gsize;
     CUDA memory access patterns, global memory coalescing, 29                     }
                                                                               } while (has_activated_thread);
     can lead to poor performance in transformed CPU programs. 30     31   }
     This is because transformed CPU programs exhibit low spatial
     locality. An example of global memory access coalescing is Listing 2: The incorrect and correct solutions for inserting a
     shown in Listing 1, where the transformed CPU program has barrier into the loop of the CUDA program in Listing 1.
     a large stride (gsize) between each memory access of array            Instead of trying to reordering the generated loops, the anti-
     pixels, resulting in the low spatial locality. To address the       coalescing transformation applies transformation on the CUDA
     slowdown caused by the global memory coalescing, we propose         program  to  guide the flat collapsing transformation directly
     the anti-coalescing transformation, a transformation applied        generate f lat loop inside original loop.
     before the flat collapsing transformation (i.e. operates at CUDA      The anti-coalescing transformation utilizes an internal prop-
     program). This policy makes the anti-coalescing transformation erty of the flat collapsing transformation. Specifically, as
   discussed in [23], [27], when there is a barrier inside the Our compiler analysis takes the loops in the CUDA function
   original loop, the flat collapsing transformation will then to check if they satisfy the 2nd and 3rd conditions listed above.
   generate the flat loop inside the original loop. Thus, we could For example, the code in Figure 3 contains a global memory
   insert a barrier in the original loop to make the f lat loop coalescing pattern and satisfies all three conditions. It includes
   the inner loop in the transformed CPU program. However, a for-loop construct (condition 1), and its stride (gsize) has
   directly inserting a barrier inside the original loop may generate a linear relationship with the block dimension (condition 2).
   incorrect CUDA programs. For example, at the top of Listing 2, Additionally, there is a global memory access inside the loop,
   the inserted barriers may not be reached by all threads. and the index has a linear relationship with the loop induction
   In CUDA, it results in undefined behavior. Thus, the anti- variable and thread index (condition 3). Therefore, the anti-
   coalescing transformation has to apply extra transformations to coalescing transformation identifies this code section as a global
   transform the original loop to a do-while loop with auxiliary memory coalescing code section.
   instructions to maintain correctness. The CUDA program
   transformed by the anti-coalescing transformation is shown
   at the bottom of Listing 2. The transformed CPU program
   is shown in Listing 3: with a barrier inside the transformed
   original loop, the flat collapsing transformation generates the
   f lat loop (line 13th) inside the original loop (line 11th). In
   this order, the array index index[tid] (line 17th) is linearly
   increased with the induction variable (tid) of the inner loop,
   which achieves high spatial locality.
      2) Implementation: The anti-coalescing transformation is a
   pattern matching and rewriting method: it first scans the CUDA Fig. 3: The code sections that satisfy all conditions are identified
   program to identify code sections that contain global memory as global memory coalescing code sections.
   coalescing patterns. Then, it applies compiler transformations
   to these sections.
                                                                      Applying the transformation: In the second phase, the anti-
 1 bool has_activated_thread;
 2 void cpu_Histogram(uint32_t *pixels, uint32_t                      coalescing transformation applies code transformation to those
         num_pixels) {                                                detected code sections, i.e. loop nest. The actual transformation
 3    uint32_t priv_hist[BLOCK_SIZE][256];
 4    uint32_t gsize = BLOCK_SIZE * GRID_SIZE;                        is to visit the loop nest in a top-down manner and rewrite
 5    uint32_t index[BLOCK_SIZE];                                     each level of the loop by substituting loop-iterator related
 6    for (int tid = 0; tid < BLOCK_SIZE; tid++)
 7      index[tid] = BLOCK_SIZE * BLOCK_ID + tid;                     operations and loop body into a new loop template. As shown
 8    bool activated[BLOCK_SIZE];                                     in Figure 4, the anti-coalescing transformation extracts loop-
 9    for (int tid = 0; tid < BLOCK_SIZE; tid++)
10      activated[tid] = true;                                        body (red), loop-cond (blue), loop-initialization (green), and
11    do { // original loop                                           loop stride (orange) information from the original loop structure,
12      has_activated_thread = false;
13      for (int tid = 0; tid < BLOCK_SIZE; tid++) { //               and fills them into a template to get the new loop. The
         flat loop                                                    template provides auxiliary variables and related operations that
14         activated[tid] &= (index[tid] < num_pixels);
15         has_activated_thread |= activated[tid];                    record whether a CUDA thread is still activated (activated)
16         if (activated[tid]) {                                      and whether there are any activated threads in the CUDA
17           uint32_t color = pixels[index[tid]]; // high
         locality                                                     block (has_activated_thread). The former is required for
18           priv_hist[tid][color]++;                                 each thread, and thus, is defined as a CUDA local variable,
19           index[tid] += gsize;
20         }                                                          while the latter is shared within a block and defined as a CUDA
21      }                                                             shared variable. To guide the flat collapsing transformation
22    } while (has_activated_thread);
23 }                                                                  generates the f lat loop as the inner loop, a barrier operation
                                                                      is applied before executing the original loop body.
   Listing 3: The CPU program generated from the bottom of
   Listing 2, which achieves high spatial locality.                      3) Proof: In this section, we provide a theoretical proof of
   Detecting global memory coalescing: Although there are             the  correctness of anti-coalescing transformation. To guarantee
   multiple ways to implement global memory coalescing, after         correctness,   the transformed program must preserve identical
   analyzing CUDA programs in real applications, we have              semantics   in  terms of data access. Considering CUDA’s SPMD
   identified a common code pattern that satisfies three conditions:  model,   the  correctness    of data access is assured by ensuring
      1) global memory coalescing is always implemented in loop       the  accuracy    of  the  workload   distribution. Specifically, anti-
          constructs;                                                 coalescing   transformation    applies transformations only to loops
      2) the stride value in that loop has a linear relationship with that  contain   global   memory    coalescing.    Therefore, the most
          the GPU block dimension value;                              critical aspect   is  to ensure  that the  loop   bodies are executed
      3) there is a global memory access inside the loop, and the     the  same   number      of times  and  that   the  induction variables
          index used to access has a linear relationship with the     maintain   original    values across  all iterations.
          loop induction variable and the thread index.                  For the original loop (left of Figure 4), each thread may
                                                                                               l                           m
                                                                     remains true for the first LOOP_BOUNDARY−init_iter(t)
                                                                                                        stride_value         iterations.
                                                                     Consequently, the loop body in the transformed loop is executed
                                                                     the same number of times as in the original loops (Eq 1).
                                                                     B. Flat Loop Optimization
                                                                         All transformed CPU programs are loop intensive. As
                                                                      illustrated in Figure 1, flat collapsing transformation typically
                                                                      generates a significant number of flat loops. Each iteration
                                                                      within these loops represents a CUDA thread. These loops
                                                                      have a desirable characteristic — they are parallelizable. Since
                                                                      GPU programs generally lack dependencies among threads, the
                                                                      corresponding transformed flat loops should similarly exhibit
                                                                      no inter-iteration dependencies. Therefore, optimizing these
Fig. 4: The anti-coalescing transformation rewrites the original
                                                                      flat loops is crucial for achieving high performance. While
loop by extracting its iterator information and loop body, filling
                                                                      loop optimization is a well-explored area for traditional CPU
them into a template to construct the new loop.
                                                                      programs, our evaluation reveals that modern compilers’ loop
                                                                      optimization strategies are often too generic and conservative
have a different loop length. For thread t, the loop length is        for these specific flat loops. In response, we propose two
                                                                    compiler transformations that convert the programs into formats
                         LOOP _BOU N DARY − init_iter(t) that enable more compiler optimizations.
original_length =
                                       stride_value                      1) Block Size Invariant Analysis: All flat loops have the GPU
                                                                (1) block size as their length. The block size is a variable initialized
   In the transformed loop (right of Figure 4), let iterit denote in the host program and passed to the kernel program during
the value of the iter variable for thread t in iteration i. Then, kernel launch. Since the host and kernel programs are distinct
the following relationship can be established:                        modules that do not share information during compilation, flat
                                                                      collapsing transformation cannot access the block size from the
                     t
                 iter0 = init_iter(t)                           (2) host program while transforming the kernel program. Therefore,
                     t         t                                      flat collapsing transformation makes GPU block size a runtime
                 iteri = iteri−1 + stride_value                 (3)
                                                                      variable. Consequently, all flat loops are dynamic loops, which
   In the transformed loop, the original loop body resides within are difficult to optimize.
the generated do-while loops and is wrapped by an if-statement           Another critical observation is that, in real-world GPU
with the conditional variable activated. The do-while loop applications, block sizes are often determinable through static
is governed by the shared variable has_activated_thread, analysis. Developers commonly choose constant block sizes
indicating that all threads possess the same loop length. Let for two main reasons. First, it facilitates efficient utilization
has_activated_threadi represent the value of the shared of shared memory and synchronization within a CUDA SM.
variable has_activated_thread in the i th iteration, and Second, GPUs traditionally impose a limit on the maximum
activatedti denote the value of activated in the i th iteration number of threads per block to align with hardware constraints.
for thread t. The following relationships are then established:          Based on these observations, we propose block size invariant
                                                                      analysis, which analyzes the host program and utilizes the
                                      n−1
                                      _                               results to optimize the kernel program, thereby converting
       has_activated_threadi =            activatedti           (4) dynamic loops to static loops. The block size invariant analysis
                                      t=0                             represents an inter-module optimization, which is challenging
                       ^ i                                            to implement within modern compiler infrastructures.
       activatedti =       (iterpt < LOOP_BOUNDARY) (5)                  The block size invariant analysis analyzes the original GPU
                       p=0                                            program and applies transformations to the CPU program.
   Combing Eq 4, Eq 5 and Eq 3, we get Eq 6.                          Specifically, it analyzes kernel launch instructions in CUDA
                                                                      host programs to determine block sizes for a CUDA kernel fk .
                  (                  l                          m     It then generates a wrapper function kw that contains multiple
             t      true,     if i < LOOP_BOUNDARY−init_iter(t)
                                              stride_value            versions   of fk ’s body, each tailored to different block sizes.
  activatedi =
                    f alse, otherwise                                 The block size invariant analysis also replaces the original call
                                                                (6) sites of fk with kw . The process is illustrated in Figure 5.
   We understand that for each iteration i, the loop body                The block size invariant analysis initially applies a compiler
is executed only when both has_activated_threadi and analysis that constructs pointer alias information and a call
activatedti are true. From Eq 4, it is evident that when graph. It then performs inter-procedural constant propagation,
activatedti is true, has_activated_threadi is invariably true which helps identify constant block sizes from the host
as well. Additionally, according to Eq 6, the activated variable programs and passes these to the kernel programs. The block
                                                                 also applied to other blocks. In the transformed CPU program,
                                                                 shown at the bottom-left, masked load/store operations are
                                                                 required when vectorizing these flat loops. For CPUs that have
                                                                 no or limited support for masked instructions, this will hinder
                                                                 the utilization of SIMD instructions.
                                                                    Based on the observation, we propose tail block adaptive
                                                                 synchronization. Similar to block size invariant analysis, it
                                                                 is a cross-module analysis. It analyzes the grid/block size
                                                                 information from the host program and uses this information
                                                                 to detect condition statements in the kernel program that only
                                                                 diverge at the tail block. For example, in the top-left CUDA
             Fig. 5: Wrapper function generation.                program in Figure 6, the condition if (gid < N ) will be true
                                                                 for all blocks except the tail block. Thus, the if statement can
                                                                 be eliminated in these blocks.
size invariant analysis subsequently synthesizes a wrapper          The tail block adaptive synchronization is applied to the
function composed of several versions of the kernel function. CUDA program, resulting in the modified CUDA program
This is achieved by substituting the runtime block size variable (top-right) that retains the if-statement only in the tail block,
with constants detected during the analysis phase. Ultimately, while eliminating it from all other blocks. Consequently, the
the original call sites are modified by replacing the kernel transformed CPU programs, depicted at the bottom-right, have
function with this wrapper function.                             a reduced requirement for masked load/store operations. For
   The block size invariant analysis always maintains correct- those architectures that do not support masked load/store
ness, even in cases where the block size cannot be analyzed instructions, the tail block adaptive synchronization brings
at compile time. As demonstrated in Figure 5, the generated higher performance.
switch instruction contains a default branch for cases where the    It is important to note that existing compiler optimiza-
block size cannot be predicted. In such cases, the block size tions cannot achieve the same transformation. For compiler
will be a runtime variable, and the original program generated optimizations designed for GPU execution, this kind of
by flat collapsing transformation will be executed.              transformation (from Figure 6 top-left to top-right) will not
                                                                 achieve any speedup on GPU execution since it introduces
                                                                 extra synchronization and does not decrease the total number
                                                                 of if-statements for each thread. Similarly, CPU optimizations
                                                                 (e.g., loop peeling, loop unswitching) cannot translate the CPU
                                                                 program from bottom-left to bottom-right in Figure 6 either.
                                                                 This is because the branch condition depends not only on
                                                                 the loop induction variable (tid), but also on the block size
                                                                 and grid size, which are runtime variables passed from the
                                                                 host module. By analyzing host programs, tail block adaptive
                                                                 synchronization can apply code transformations that cannot be
                                                                 done by existing compiler optimizations.

                                                                   C. GPU-Block Dynamic Tiling
                                                                       1) Insight: As introduced in Section III-C, directly mapping
                                                                   CUDA blocks to CPU threads often yields suboptimal results.
                                                                   This is because, from the hardware perspective, GPU SMs and
                                                                   CPU cores have different capacities.
                                                                       To mitigate the mismatch, we propose GPU-block dynamic
                                                                   tiling, enabling runtime redistribution of the CUDA workload
 Fig. 6: An example of tail block adaptive synchronization.        across CPU cores to accommodate architectural differences
                                                                   and improve computational efficiency. Instead of mapping a
   2) Tail Block Adaptive Synchronization: GPU programs            single GPU block to a CPU thread, the GPU-block dynamic
exhibit parallelism at two levels: grid and block. Consequently,   tiling maps N GPU blocks to a CPU thread. The optimal
tail blocks are commonly required to manage edge cases, as         N depends on both the characteristics of the CUDA kernels
illustrated at the top-left of Figure 6. When the block size is    and the capacity of the CPU. The GPU-block dynamic tiling
not a divisor of N , certain threads in the last block should      initially tiles GPU blocks such that the total number of tiled
not activate, necessitating an if-statement branch to filter out   blocks matches the number of CPU cores, with the tiled block
                                                                                    grid_size×block_size
these inactive threads. However, this conditional branch is        size equal to number_of   _CP U _cores . This approach ensures full
CPU utilization and avoids unnecessary context switches as
the number of thread is same as the number of CPU cores.
    Furthermore, we find that for some CUDA programs,
particularly those with lightweight kernels, further tiling
the GPU blocks can enhance performance. This seemingly
counterintuitive phenomenon arises from the fact that additional
tiling, although it does not utilize all CPU cores, reduces the
overhead associated with CPU synchronization. The execution
of lightweight kernels primarily suffers high CPU synchroniza-
tion overhead; hence any reduction in this aspect can improve
overall performance. Additionally, further tiling of GPU blocks
enables more effective use of CPU computation resources, such
as increasing cache locality by aggregating more CUDA blocks
be executed by the same CPU cores.
    Based on the observation, we propose GPU-block dynamic                 Fig. 7: The process of GPU-block dynamic tiling.
tiling to dynamically adjust the tile size. This optimization is
motivated by the observation that for most CUDA programs, the
                                                                    D. Scope of Proposed Optimizations
same kernel is launched multiple times with similar workloads,
e.g., the same kernels are launched iteratively to process             We summarize the scope of GPU programs that benefit from
different batches in batch processing programs. GPU-block each proposed optimization: anti-coalescing transformation is
dynamic tiling is a hill-climbing-like approach: it iteratively used to optimize GPU kernels that contain global memory
increases the tile size and measures the achieved speedup for coalescing code sections; block size invariant analysis is
kernel execution until no further performance improvement is suitable for GPU programs whose block sizes are static values
observed. Therefore, this dynamic approach provides a more in host programs; tail block adaptive synchronization can be
efficient mapping of CUDA blocks onto CPU cores, particularly used to optimize GPU kernels that contain branch instructions
for lightweight kernels. Compared with a normal kernel launch, which diverge only for the last block; GPU-block dynamic
the tuning process only includes extra simple calculations tiling is a runtime optimization and requires that kernels are
and time measurements, resulting in negligible overhead. In launched multiple times with similar workloads.
our evaluation, we include the tuning overhead in the kernel                               V. E VALUATION
execution time to demonstrate the overall runtime improvement.
                                                                       To illustrate the compatibility of the proposed optimiza-
    2) Implementation: The GPU-block dynamic tiling is inte- tions with flat collapsing transformation, we integrate these
grated into the kernel launch function as a runtime optimization. optimizations into CuPBoP [21], [22] (commit: fd5681), a GPU-
Upon kernel launch, the optimization first determines if the to-CPU framework that utilizes flat collapsing transformation.
kernel has been executed previously. If not, the tiled block To execute CUDA source code on CPUs, the source code
                grid_size×block_size
size is set to number_of _CP U _cores , which matches the number    is first compiled to LLVM IR. Then, the proposed compiler
of CPU cores. Otherwise, if the kernel has been executed optimizations and flat collapsing transformation are applied
before, the method doubles the last tiled block size to further as LLVM-IR transformations. The transformed IR is finally
grouping the blocks into CPU cores. The execution time with compiled to CPU executable files.
the new tile size is then measured and compared with the               Additionally, two other GPU-to-CPU solution, DPC++ [26]
runtime of previous tile size. If the current tile size exhibits (version 2024.0.2) and MCUDA [49] (version 1.0.1), are used
superior efficiency, the optimal solution is updated accordingly. as the baselines.
Conversely, the tile size is set back to the previous one and          Two platforms are utilized for evaluation: one is equipped
remains invariant for subsequent kernel launches.                   with two Intel Gold 6226R CPUs (x86), and the other features
    An example of the GPU-block dynamic tiling process is a single ARM A64FX CPU (AArch64). To avoid randomness
demonstrated in Figure 7. For a GPU kernel with 128 blocks, in runtime evaluation, we run each evaluation 7 times and
when executed on a 4-core CPU, at the first launch, GPU-block report the median as the final result.
dynamic tiling assigns blocks to four threads to fully utilize         We use benchmarks from different areas: Parboil [48], Ro-
all CPU cores. At the second launch, GPU-block dynamic dinia [10], and Hetero-mark [50] offer CUDA implementations
tiling tries to double the tile size. Thus, only two threads are of conventional HPC applications. Polybench [18] comprises
required, each executing 64 blocks. After execution, GPU- linear algebra operations implemented in various languages.
block dynamic tiling detects a speedup compared to the first We do not evaluate all applications in these benchmarks, since
iteration. Therefore, it tries to further increase the tile size in some applications are not supported by DPC++, CuPBoP and
the third kernel launch but detects a slowdown. Consequently, MCUDA: MCUDA does not support C++ syntax, dynamic
GPU-block dynamic tiling rolls back to the best-evaluated tile shared memory, or using integer types for grid/block sizes.
size for all remaining kernel launches.                             Therefore, for almost all benchmarks, we must apply manual
preprocessing and post-processing to the source code, resulting
in a significant workload. Similarly, DPC++ and CuPBoP do
not support texture memory and atomic instructions. In general,
these GPU-to-CPU solutions are error-prone; therefore, we only
evaluate CUDA benchmarks that can be successfully migrated.
    For all evaluations, we apply compiler O3 pipeline to the
transformed CPU programs.
    We use CuPBoP as the baseline, and demonstrate the runtime
improvement achieved by applying all proposed optimizations
in Figure 8. While all optimizations are applied simultaneously,
it is observed that different applications experience varying
degrees of speed-up from each optimization. Consequently,
applications are categorized based on the optimization that
delivers the most significant impact on runtime. For the x86
(Figure 8a), out of all 16 applications, 4 demonstrate a notable                      (a) Intel x86 CPU evaluation.
speed-up due to anti-coalescing transformation, 9 due to block
size invariant analysis, and 3 owing to GPU-block dynamic
tiling. On average, the proposed optimizations contribute to a
20.84% speed-up. Analyzing each optimization individually,
anti-coalescing transformation contributes to a 24.46% speed-up
for applications that benefit from it, while block size invariant
analysis and GPU-block dynamic tiling result in speed-ups of
14.18% and 53.46%, respectively. Since x86 supports masked
ld/st, applying tail block adaptive synchronization does not
yield significant improvement.
    A similar trend is observed for ARM CPUs, as depicted in
Figure 8b. Since ARM CPUs do not support masked load/store,
four applications benefit from tail block adaptive synchro-
nization, which can be optimized using SIMD instructions to
achieve a speed-up of 25.23%. The cumulative effect of all                        (b) ARM AArch64 CPU evaluation.
optimizations results in a 16.10% speed-up.                       Fig. 8: The speed-up achieved by the proposed optimizations.
    Comparing the runtime with other GPU-to-CPU solutions Although all four optimizations are applied concurrently,
(Figure 9), our solution achieves the highest performance across programs exhibit varied improvements from each. Consequently,
all benchmarks, indicating that the observed speed-up from the programs are categorized based on the optimization that yields
proposed optimizations is not due to potential implementation the most significant speed-up.
drawbacks in CuPBoP. Our solution is 47.39%, 51.46%, 20.84%
faster than MCUDA, DPC++, and CuPBoP on average.
    In the following sections, we evaluate the effect of each instructions in the transformed code. To assess this overhead,
optimization individually. The anti-coalescing transformation, we present the results at the bottom of Figure 10. Despite the
block size invariant analysis, and GPU-block dynamic tiling extra branch instructions, the overhead is negligible compared
optimizations are evaluated using the Intel CPU, which offers to the achieved runtime improvement.
mature profiling toolkits. The tail block adaptive synchroniza-      In addition to enhanced cache utilization, we also observe
tion is assessed on the ARM CPU which does not support that the CPU program optimized by anti-coalescing trans-
masked load/store operations.                                     formation provides more opportunities for leveraging SIMD
                                                                  instructions, as illustrated in Table II. This is due to the fact that
A. Anti-Coalescing Transformation                                 the optimized programs’ inner loops access memory addresses
    We evaluate the effectiveness of anti-coalescing transfor- sequentially, a pattern that compilers are more likely to optimize
mation on five CUDA kernels that leverage global memory through vectorization transformations. Nevertheless, the most
coalescing. The performance of the transformed CPU kernels, significant speed-up is attributed to improved cache utilization.
both with and without anti-coalescing transformation, is com- When evaluating with loop-vectorization settings turned off,
pared in Figure 10. The results demonstrate that anti-coalescing all five applications still achieve the same speed-up.
transformation significantly reduces the kernel execution time
for all kernels. This reduction is attributable to the improved B. Block Size Invariant Analysis
cache locality, as evidenced by the decrease in the number of        The block size invariant analysis enables the generation
LLC misses (middle of Figure 10). It is important to recognize of flat loops with constant lengths and loop bodies with
that the anti-coalescing transformation introduces extra branch constant block sizes, which are more amenable to optimization
                                                           by standard compiler techniques. The block size invariant
                                                           analysis achieves a significant speed-up (over 30%) for
                                                           2DCONV, 3DCONV, JACOB1, and JACOB2D applications,
                                                           as it enables loop vectorization to generate programs that
                                                           utilize SIMD instructions effectively. All these applications
                                                           employ CUDA blocks with two dimensions. Since flat col-
                                                           lapsing transformation generates a single for-loop to sim-
                                                           ulate all threads, it incurs additional instructions such as
                                                           threadIdx.y = tid%block_size.y to calculate the thread
                                                           index in all dimensions. These instructions are associated
                                                           with DIV and REM operations. For the LLVM compiler, its
                                                           cost function is affected by these instructions, leading to an
                                                           estimated overhead for vectorization exceeds that of the scalar
                                                           version, thereby preventing the application of loop vectorization
                                                           to these programs. With block size invariant analysis, the block
                                                           size variables are replaced with constants. Notably, as most
                                                           block size values are powers of two, these expensive DIV/REM
                                                           operators can be replaced by SHIFT and AND operators.
Fig. 9: The runtime result comparision with three CUDA-to- Consequently, the compiler opts to apply vectorization on
CPU solutions: DPC++(commercial), MCUDA (academic), and the transformed programs, thereby enhancing the utilization
CuPBoP (academic).                                         of SIMD instructions. The utilization of AVX instructions is
                                                           documented in Table III.

                                                                  App.         invariant             AVX Instructions
                                                                                                  128           256            512
                                                                               w/          23,134,208             0     78,249,984
                                                                  2DCONV
                                                                               w/o                  0             0              0
                                                                               w/          28,285,440             0    102,737,920
                                                                  3DCONV
                                                                               w/o                  0             0              0
                                                                               w/          41,120,000    30,720,000    100,800,000
                                                                  JACOB1D
                                                                               w/o                  0             0              0
                                                                               w/           5,200,000             0     26,880,000
                                                                  JACOB2D
                                                                               w/o                  0             0              0

                                                                  TABLE III: The number of executed AVX instructions with-
                                                                  /without the block size invariant analysis.

                                                                     In addition to loop vectorization, the constant block size
Fig. 10: The runtime, number of LLC misses and executed
                                                                  further facilitates other compiler optimizations, including con-
branch instructions on transformed CPU programs.
                                                                  stant propagation and constant folding. As a result, applications
                                                                  not optimized by loop vectorization still attain improvements.
                                                                  Owing to the ubiquity of flat loops and instructions that utilize
App.     Anti                    AVX Instructions                 block size variables as operands in the transformed CPU
                                                                  programs, almost all programs benefit from block size invariant
                     128                256              512
                                                                  analysis to varying extents.
         w/      190,063,921              0           93,068,672
Hist
         w/o     190,067,557              0           13,483,904 C. Tail Block Adaptive Synchronization
         w/     22,884,123,056     11,475,615,858   23,085,449,918
Col2im                                                                The tail block adaptive synchronization is specifically
         w/o          0                   0               0
         w/       33,554,561          4,194,320       22,020,182 designed for CPUs lacking support for masked load/store
Sad_8
         w/o          0                   0               0        instructions. Therefore, its effect is evaluated on the ARM
         w/           5                  16           1,048,620 CPU. Four applications — 2DCONV, 3DCONV, JACOB1D,
Sad_16
         w/o          1                   0               3
         w/      213,961,856         58,722,304       71,574,784 and JACOB2D — are transformed using tail block adaptive
Vecadd                                                             synchronization, and each experiences a significant speed-up
         w/o          0                   0               0
                                                                  of 13.63%, 26.81%, 62.92%, and 17.60% respectively.
TABLE II: Number of executed AVX instructions with and               This speed-up is attributed to the utilization of SIMD
without anti-coalescing transformation.                           instructions, as detailed in Table IV. Without tail block adaptive
                                                                  synchronization, all load/store instructions reside within the
if-statements, necessitating masked load/store for vectorization.    memory access. As a result, the transformed CPU program
However, with tail block adaptive synchronization, most              could not fully utilize the available CPU resources. In its
load/store instructions, except those executed by the last blocks,   default configuration, each kernel launch involved 128 CUDA
are moved out of the conditional statement, thereby allowing         blocks, translating to 128 CPU threads in the transformed
optimization using general vectorization instructions provided       CPU program. This setup led to a substantial synchronization
by ARM CPUs. This substantial amount of SIMD computation             overhead relative to the kernel workload. With the application
results in significant speed-up,                                     of GPU-block dynamic tiling, each CPU core is tasked with
                                                                     executing 16 CUDA blocks, reducing the number of required
     App.        Tail   simd_inst     ase_spec    ase_sve_int        CPU threads to eight and thus diminishing the kernel launch
     2DCONV       w/    37 483 230   33 541 230   31 398 389         and synchronization overhead. Such an adjustment enables the
                  w/o    4 222 977        2172         1724          FIR application to substantially benefit from the optimization.
     3DCONV       w/    55 305 575   48 205 721   34 415 726
                  w/o    7 087 320        2438         1744
                                                                                          VI. R ELATED WORK
     JACOB1D      w/    92 277 566   91 866 604   85 218 510
                  w/o       24 408       22 046        1693         For more than a decade, researchers have sought to achieve
      JACOB2D       w/ 124 677 629 123 423 652   98 905 416      high  performance for GPU programs on CPUs. To bridge the
                   w/o     565 787        1964        1690       parallelism gap between these two architectures, the researchers
                                                                 in MCUDA [49] propose a compiler transformation, flat
TABLE IV: The number of executed SIMD instructions
                                                                 collapsing transformation, that generates loops to wrap CUDA
with/without the tail block adaptive synchronization.
                                                                 kernels. This transformation enables the use of fewer CPU
                                                                 threads, with each thread handling a heavier workload that is
D. GPU-Block Dynamic Tiling                                      better suited for CPU architectures. The transformation has
   The GPU-block dynamic tiling is tailored for GPU programs been widely used for GPU-to-CPU migration. For example,
that iteratively launch the same kernels. We evaluate the tiling POCL [27] and SYCL [24] use it to support CPU backends
processes of four CUDA benchmarks that iteratively launch for OpenCL and SYCL languages.
same kernels at least 10 times, with results presented in           This transformation is widely used [15], [21], [29], [47] with
Figure 11. In the initial phase, the tiled block size is set to  different   implementations: Moses et al. [40] implement the
match the number of CPU cores. The tiling size is gradually      transformation      on MLIR [39]; Ocelot [12] and CuPBoP [21]
increased during execution, until further increases do not yield implement      it as LLVM IR transformations; and Cumulus [9]
any additional speed-up.                                         implements      it as a CUDA-to-C++ transformation. We imple-
   We compared the execution time with the workload config-      ment   the  proposed    optimizations as LLVM IR transformations
urations in CuPBoP, which equally distribute CUDA blocks         to make    them    compatible    with CuPBoP.
to all CPU cores. Three programs (BS, FIR, and GA) benefit          Some    researchers    expand   the flat collapsing transformation:
from the GPU-block dynamic tiling, achieving speed-ups           COX     [23]    proposes    the  hierarchical  collapsing to support
of 29.67%, 75.98%, and 67.78%, respectively. For the Hist        CUDA      warp-level     functions,  and    [20]  proposes additional
applications, optimal CPU performance is already achieved by     compiler   analysis   to  detect implicit barriers in CUDA programs
evenly distributing blocks across all CPU cores                  to ensure    the  correctness   of the flat collapsing transformation.
                                                                    The flat collapsing transformation focuses solely on barrier
                                                                 instructions. To ensure program correctness, it generates
                                                                 separate loops to wrap instructions before and after these
                                                                 barriers. For instructions that are not barriers, flat collapsing
                                                                 transformation simply wraps them into loops. Therefore, there
                                                                 is potential for further optimization, as discussed in this paper.
                                                                    OCCA [38] introduced a library-based approach that utilizes
                                                                 APIs to abstract backend and kernel languages, making it
                                                                 compatible with various parallel programming models such as
                                                                 OpenMP and OpenCL. The library employs Just-In-Time com-
                                                                 pilation to compile kernel functions and offers minor extensions
                                                                 to C to facilitate programming for each backend. Similarly,
                                                                 Kokkos [51] is designed to provide an easy-to-program solution
                                                                 for various types of devices, such as CPU, GPU, and FPGA,
   Fig. 11: The process of the GPU-block dynamic tiling.         through its C++ library. It offers an abstraction layer that
                                                                 separates user algorithms from execution details, enabling
   The FIR benchmark experiences the most significant speed- performance optimization across different architectures. The
up. Initially, each CUDA block contains only 32 threads, each optimizations introduced in this paper are also applicable to
has a lightweight workload of just 32 FLOPs and minimal these two library-based approaches.
   There are several projects [14], [35]–[37], [44], [45], [53] coalescing optimization, which intentionally makes threads
that address the differences in high-performance programs access interleaving memory addresses. However, this reversal is
on CPUs and GPUs. Majeti et al. [35] propose a source- necessary to provide portability for real-world GPU programs.
to-source translation that converts data structures (e.g., AOS All GPU-to-CPU solutions aim to migrate unchanged GPU
to SOA) based on the target backend to optimize CPU and programs, and most off-the-shelf GPU programs are optimized
GPU preferences. In [53], tunable factors like caching, tiling, specifically for GPU architectures. Thus, reversing GPU
and prefetching are manually tuned for OpenCL programs optimizations is necessary to achieve performance portability.
to improve performance on both CPUs and GPUs. On the
other hand, Falch et al. [14] present an auto-tuning approach C. Why Run GPU Programs on CPUs?
based on machine learning. These factors also influence              There are multiple benefits to executing GPU programs
energy efficiency, as explored in [45]. These methods require on CPUs. First, it allows workload sharing between GPUs
developers to provide explicit tuning knobs in programs to and CPUs, which can address the imbalance of utilization in
achieve performance portability across different hardware. In CPU-GPU clusters, thereby reducing runtime [30], [32] and
contrast, our approach can be applied to general GPU programs lowering energy consumption [46]. Second, the CPU offers a
without the need for explicit tuning knobs.                       more mature development environment, allowing developers to
                                                                  use CPU toolkits to debug GPU programs [7], [9], [13], [43].
                       VII. D ISCUSSIONS                          Lastly, it provides a platform for education and research for
A. Why Existing Compiler Optimizations Cannot Help?               those who do not have access to GPUs and wish to explore
   In Section V, for the baseline, we apply LLVM’s O3 proof-of-concept GPU programming.
optimization on all CPU programs. Despite this, it does not          We acknowledge that supporting the translation of CPU
achieve the same performance as our proposed optimizations. programs to GPUs is also important. Some researchers focus on
   We also attempt to use Polly [19], a popular Polyhedral executing OpenMP programs written for CPUs on GPUs [11],
model compiler, to optimize the CPU programs generated [25], [33], [34], [42]. PPCG [52] is a source-to-source translator
by flat collapsing transformation. However, we find that it based on polyhedral analysis, which translates sequential CPU
cannot analyze most of the programs. To apply Polyhedral programs to GPU programs.
model optimization effectively, the loop sections must be
                                                                                        VIII. C ONCLUSION
static control parts (SCoPs) [16], [17], where loop bounds
and memory dependencies are amenable to static analysis.             The flat collapsing transformation is widely used for GPU-to-
Unfortunately, the CPU programs generated by flat collapsing CPU migration solutions. Nevertheless, there is untapped poten-
transformation typically contain indirect memory accesses, tial to achieve superior performance since the transformed CPU
which pose significant challenges for memory dependency programs do not fully utilize CPU computational resources.
analysis and prevent the polyhedral compiler from functioning We have identified three unaddressed challenges in current
effectively. Specifically, flat collapsing transformation expands solutions. Accordingly, we introduce four compiler/runtime
local variables into arrays, transforming scalars into array optimizations to facilitate the generation of more performance-
elements. Consequently, a CUDA program that accesses efficient CPU programs. We integrate these optimizations into
memory using a scalar index might be transformed into one an off-the-shelf GPU-to-CPU solution and demonstrate the
involving indirect memory access. For example, flat collapsing achieved speedup through experiments. We evaluate the speed-
transformation converts idx to idx[tid] in Figure 1. When we ups of CPU programs on both x86 and ARM architectures. On
attempt to optimize this CPU program using Polly, it reports average, the optimized applications yield speed-ups of 20.84%
the warning message "The array subscript of ’s’ is not affine," on x86 CPUs and 16.10% on ARM CPUs.
leaving the code section unchanged.                                  These optimizations pave the way for further advancements
   Similarly, the complexities introduced by flat collapsing in the performance portability of GPU programs to CPUs,
transformation also prevent other general compiler optimiza- thereby making them more efficient and effective in heteroge-
tions. Thus, in this paper, we summarize the common patterns neous computing environments.
in CPU programs generated by flat collapsing transformation
                                                                                       ACKNOWLEDGEMENTS
and propose four optimizations accordingly, which can be used
complementarily with standard compiler optimizations.                This research was partially supported by NSF PPOSS
                                                                  2119523, Intel, AMD and BAH. We also want to acknowledge
B. Do These Optimizations Nullify CUDA Optimizations?             the research infrastructure and services provided by the Rogues
   The tail block adaptive synchronization, block size invari- Gallery testbed [28], hosted by the Center for Research
ant analysis, and GPU-block dynamic tiling can be applied into Novel Computing Hierarchies (CRNCH) at Georgia
alongside general CUDA programs and are not related to any Tech. The Rogues Gallery testbed is primarily supported by
CUDA-specific optimizations.                                      the National Science Foundation (NSF) under NSF Award
   It is true that anti-coalescing transformation undoes what Number #2016701. Any opinions, findings, conclusions, or
programmers have intentionally implemented. Specifically, recommendations expressed in this material are those of the
anti-coalescing transformation reverses the global memory author(s) and do not necessarily reflect the views of the NSF.
                              A PPENDIX
                                                                     1   cd CuPBoP-MICRO-AE
                                                                     2   export CuPBoP_PATH=$PWD
    A. Abstract                                                      3   mkdir build && cd build
                                                                     4   # $CUDA_PATH should be set
       The artifact integrates the proposed compiler optimizations 5     CC=gcc CXX=g++ cmake ..
    into CuPBoP [22] to provide a solution for executing GPU 6           make
                                                                   7     export CuPBoP_BUILD_PATH=$PWD
    programs on CPUs. The artifact also provides three examples 8        export PATH=$CuPBoP_BUILD_PATH/compilation:$PATH
    containing GPU programs that can be transformed by each of 9         export LD_LIBRARY_PATH=$CuPBoP_BUILD_PATH/runtime:
                                                                              $CuPBoP_BUILD_PATH/runtime/threadPool:
    the proposed optimizations.                                               $LD_LIBRARY_PATH
       The LLVM IR-to-IR transformations in the artifact are
    the optimizations (anti-coalescing transformation, block size
                                                                     E. Experiment workflow
    invariant analysis, and tail block adaptive synchronization)
    proposed in the paper.                                              All evaluations follow the same workflow. First, the LLVM
                                                                     IR-to-IR translator applies the proposed optimizations. Then,
    B. Artifact check-list (meta-information)                        the transformed IR is used to generate CPU executable files.
       • Compilation: CUDA, LLVM and GCC.                            Finally, the executable files are executed to verify correctness.
       • Run-time environment: Linux (Ubuntu 20.04).                    We provide the script run.sh for each evaluation, which
       • Hardware: x86 CPU.
                                                                     integrates the above workflow. Users can directly execute
       • Output: CPU executable file.
       • How much disk space required (approximately)?: 5 GB.
                                                                     run.sh to run the evaluation.
      • How much time is needed to prepare workflow (approxi-
        mately)?: 20 minutes.
                                                                     F. Evaluation and expected results
      • How much time is needed to complete experiments (approx-
        imately)?: 5 minutes.                                           There are three independent evaluations for the three
      • Publicly available?: Yes.
      • Code licenses (if publicly available)?: Apache-2.0 license.
                                                                     proposed  compiler optimizations, respectively.
      • Archived (provide DOI)?: 10.5281/zenodo.13250264.           1 cd $CuPBoP_PATH/MICRO_AE_example/anti-coalescing-
                                                                              transformation
                                                                     2   bash run.sh
    C. Description                                                   3
                                                                     4   cd $CuPBoP_PATH/MICRO_AE_example/block-size-invariant-
       1) How to access: The artifact can be downloaded from           analysis
    https://zenodo.org/doi/10.5281/zenodo.13250263. It is also 56 bash run.sh
    available on GitHub at https://github.com/drcut/CuPBoP- 7 cd $CuPBoP_PATH/MICRO_AE_example/tail-block-adaptive-
                                                                       synchronization
    MICRO-AE/tree/v1.0.0.                                      8 bash run.sh
       2) Hardware dependencies: x86 CPU.
       3) Software dependencies:                                 Anti-Coalescing: The input CUDA program contains a global
                                                                 memory coalescing access pattern. The translator is expected
       • Ubuntu 20.04;
                                                                 to detect the code section and apply the anti-coalescing
       • GCC-8;
                                                                 transformation. The translator is expected to output:
       • LLVM-14;
                                                                1 Find global memory coalescing
       • CUDA-10.1 toolkit;
                                                                2 Loop at depth 1 containing: %37<header><exiting>,%41<
       • moodycamel::ConcurrentQueue (C++ library);                    latch>


    D. Installation                                                      Block Size Invariant Analysis: The input CUDA program
                                                                         contains three different kernel launches, with block sizes of
    Download artifact: We recommend downloading the artifact             16, 32, and 42. The translator is expected to collect all block
    from GitHub, which can automatically download the third-party        sizes. Thus, it is expected to output:
    libraries required for execution.
                                                                     1   possible block sizes:
1   git clone --recursive --branch v1.0.0 --depth 1 https            2   x: 16 y: 1 z: 1
         ://github.com/drcut/CuPBoP-MICRO-AE.git                     3   x: 32 y: 1 z: 1
                                                                     4   x: 42 y: 1 z: 1
    (Optional) Download LLVM: The artifact requires LLVM-
    14. For users who do not have it installed, we recommend                Tail Block Adaptive Synchronization: The input CUDA
    downloading the pre-built LLVM.                                      kernel contains an if condition that will only diverge at the
1   wget https://github.com/llvm/llvm-project/releases/
                                                                         last block (with block index equal to grid size - 1). Thus, the
         download/llvmorg-14.0.0/clang+llvm-14.0.0-x86_64-               translator should apply tail block adaptive synchronization to
         linux-gnu-ubuntu-18.04.tar.xz                                   the kernel. The translator should output:
2   tar -xvf clang+llvm-14.0.0-x86_64-linux-gnu-ubuntu
         -18.04.tar.xz                                               1   Condition is always true (without tail block) with
3   export PATH=$PWD/clang+llvm-14.0.0-x86_64-linux-gnu-                      threshold 512 and global index ranges: [0, 496)
         ubuntu-18.04/bin:$PATH                                      2   The function can be optimized by tail block adaptive
                                                                              sync
    Build artifact:
G. Notes                                                                       [20] Z. Guo, E. Z. Zhang, and X. Shen, “Correctly treating synchronizations
                                                                                    in compiling fine-grained spmd-threaded programs for cpu,” in 2011
   The workflow of executing CUDA source code on CPUs first                         International Conference on Parallel Architectures and Compilation
requires compiling CUDA source code to LLVM IR, which                               Techniques. IEEE, 2011, pp. 310–319.
necessitates the CUDA toolkit. In the artifact, we choose to                   [21] R. Han, J. Chen, B. Garg, J. Young, J. Sim, and H. Kim, “Cupbop: A
                                                                                    framework to make cuda portable,” in Proceedings of the 28th ACM
directly provide the generated LLVM IR and focus on the                             SIGPLAN Annual Symposium on Principles and Practice of Parallel
evaluation of the IR-to-IR transformation. However, we also                         Programming, 2023, pp. 444–446.
provide the CUDA source code (i.e., hist.cu and vecadd.cu).                    [22] R. Han, J. Chen, B. Garg, X. Zhou, J. Lu, J. Young, J. Sim, and H. Kim,
                                                                                    “Cupbop: Making cuda a portable language,” ACM Transactions on
Although this source code is not directly used for artifact                         Design Automation of Electronic Systems, vol. 29, no. 4, pp. 1–25, 2024.
evaluation, it provides a more comprehensive understanding                     [23] R. Han, J. Lee, J. Sim, and H. Kim, “Cox: Exposing cuda warp-
of the LLVM IRs used for evaluation.                                                level functions to cpus,” ACM Transactions on Architecture and Code
                                                                                    Optimization (TACO), 2022.
                                                                               [24] L. Howes and M. Rovatsou, “Sycl integrates opencl devices with modern
                             R EFERENCES                                            c++,” Khronos Group, 2015.
 [1] “Hip,” 2016. [Online]. Available: https://github.com/ROCm-Developer-      [25] J. Huber, M. Cornelius, G. Georgakoudis, S. Tian, J. M. M. Diaz, K. Dinel,
     Tools/HIP                                                                      B. Chapman, and J. Doerfert, “Efficient execution of openmp on gpus,”
 [2] “A64fx specifications,” 2018. [Online]. Available: https://www.fujitsu.        in 2022 IEEE/ACM International Symposium on Code Generation and
     com/global/about/resources/news/press-releases/2018/0822-02.html               Optimization (CGO). IEEE, 2022, pp. 41–52.
 [3] “Hip-cpu,” 2020. [Online]. Available: https://github.com/ROCm-            [26] Intel, “Dpct,” 2020. [Online]. Available: https://www.intel.com/content/
     Developer-Tools/HIP-CPU                                                        www/us/en/developer/tools/oneapi/dpc-compatibility-tool.html
 [4] “Nvidia a100 specifications,” 2021. [Online]. Available: https:           [27] P. Jääskeläinen, C. S. de La Lama, E. Schnetter, K. Raiskila, J. Takala,
     //ieeexplore.ieee.org/abstract/document/9361255                                and H. Berg, “pocl: A performance-portable opencl implementation,”
 [5] “Amd epyc™ 9654,” 2023. [Online]. Available: https://www.amd.com/              International Journal of Parallel Programming, vol. 43, no. 5, pp. 752–
     en/products/cpu/amd-epyc-9654                                                  785, 2015.
 [6] “Intel     xeon     gold     6423,”    2023.     [Online].   Available:   [28] A. Jezghani, J. Young, W. Powell, R. Rahaman, and J. E. Coulter, “Future
     https://www.intel.com/content/www/us/en/products/sku/236591/intel-             computing with the rogues gallery,” in 2023 IEEE International Parallel
     xeon-gold-6423n-processor-52-5m-cache-2-00-ghz/specifications.html             and Distributed Processing Symposium Workshops (IPDPSW). IEEE,
 [7] T. M. Aamodt, W. W. Fung, I. Singh, A. El-Shafiey, J. Kwa, T. Hether-          2023, pp. 262–269.
     ington, A. Gubran, A. Boktor, T. Rogers, and A. B. et al., “Gpgpu-sim     [29] R. Karrenberg and S. Hack, “Improving performance of opencl on cpus,”
     3.x manual,” 2012.                                                             in International Conference on Compiler Construction. Springer, 2012,
 [8] P. Barua, J. Shirako, and V. Sarkar, “Cost-driven thread coarsening for        pp. 1–20.
     gpu kernels,” in Proceedings of the 27th International Conference on      [30] C. Lee, W. W. Ro, and J.-L. Gaudiot, “Boosting cuda applications
     Parallel Architectures and Compilation Techniques, 2018, pp. 1–14.             with cpu–gpu hybrid computing,” International Journal of Parallel
 [9] V. Blomkvist Karlsson, “Cumulus-translating cuda to sequential c++:            Programming, vol. 42, no. 2, pp. 384–404, 2014.
     Simplifying the process of debugging cuda programs,” 2021.                [31] J. Lee, J. Kim, S. Seo, S. Kim, J. Park, H. Kim, T. T. Dao, Y. Cho,
[10] S. Che, M. Boyer, J. Meng, D. Tarjan, J. W. Sheaffer, S.-H. Lee, and           S. J. Seo, S. H. Lee et al., “An opencl framework for heterogeneous
     K. Skadron, “Rodinia: A benchmark suite for heterogeneous computing,”          multicores with local memory,” in Proceedings of the 19th international
     in 2009 IEEE international symposium on workload characterization              conference on Parallel architectures and compilation techniques, 2010,
     (IISWC). Ieee, 2009, pp. 44–54.                                                pp. 193–204.
[11] A. Chikin, T. Lloyd, J. N. Amaral, E. Tiotto, and M. Usman, “Memory-      [32] J. Lee, M. Samadi, Y. Park, and S. Mahlke, “Skmd: Single kernel on mul-
     access-aware safety and profitability analysis for transformation of           tiple devices for transparent cpu-gpu collaboration,” ACM Transactions
     accelerator-bound openmp loops,” ACM Transactions on Architecture              on Computer Systems (TOCS), vol. 33, no. 3, pp. 1–27, 2015.
     and Code Optimization (TACO), vol. 16, no. 3, pp. 1–26, 2019.             [33] S. Lee and R. Eigenmann, “Openmpc: Extended openmp programming
[12] G. Diamos, A. Kerr, S. Yalamanchili, and N. Clark, “Ocelot: a dynamic          and tuning for gpus,” in SC’10: Proceedings of the 2010 ACM/IEEE
     optimization framework for bulk-synchronous applications in hetero-            International Conference for High Performance Computing, Networking,
     geneous systems,” in 2010 19th International Conference on Parallel            Storage and Analysis. IEEE, 2010, pp. 1–11.
     Architectures and Compilation Techniques (PACT). IEEE, pp. 353–364.       [34] S. Lee, S.-J. Min, and R. Eigenmann, “Openmp to gpgpu: a compiler
[13] A. S. Elhelw and S. Pai, “Horus: A modular gpu emulator framework,” in         framework for automatic translation and optimization,” ACM Sigplan
     2020 IEEE International Symposium on Performance Analysis of Systems           Notices, vol. 44, no. 4, pp. 101–110, 2009.
     and Software (ISPASS). IEEE, 2020, pp. 104–106.                           [35] D. Majeti, R. Barik, J. Zhao, M. Grossman, and V. Sarkar, “Compiler-
[14] T. L. Falch and A. C. Elster, “Machine learning based auto-tuning for          driven data layout transformation for heterogeneous platforms,” in Euro-
     enhanced opencl performance portability,” in 2015 IEEE International           Par 2013: Parallel Processing Workshops: HeteroPar. Springer, 2014,
     Parallel and Distributed Processing Symposium Workshop. IEEE, 2015,            pp. 188–197.
     pp. 1231–1240.                                                            [36] D. Majeti, K. S. Meel, R. Barik, and V. Sarkar, “Automatic data
[15] J. Fang, P. Zhang, T. Tang, C. Huang, and C. Yang, “Implementing               layout generation and kernel mapping for cpu + gpu architectures,”
     and evaluating opencl on an armv8 multi-core cpu,” in 2017 IEEE                in Proceedings of the 25th International Conference on Compiler
     International Symposium on Parallel and Distributed Processing with            Construction, 2016, pp. 240–250.
     Applications and International Conference on Ubiquitous Computing         [37] D. Majeti and V. Sarkar, “Heterogeneous habanero-c (h2c): a portable
     and Communications (ISPA/IUCC). IEEE, 2017, pp. 860–867.                       programming model for heterogeneous processors,” in 2015 IEEE
[16] P. Feautrier, “Parametric integer programming,” RAIRO-Operations               International Parallel and Distributed Processing Symposium Workshop.
     Research, vol. 22, no. 3, pp. 243–268, 1988.                                   IEEE, 2015, pp. 708–717.
[17] S. Girbal, N. Vasilache, C. Bastoul, A. Cohen, D. Parello, M. Sigler,     [38] D. S. Medina, A. St-Cyr, and T. Warburton, “Occa: A unified approach
     and O. Temam, “Semi-automatic composition of loop transformations              to multi-threading languages,” arXiv preprint arXiv:1403.0968, 2014.
     for deep parallelism and memory hierarchies,” International Journal of    [39] W. S. Moses, L. Chelini, R. Zhao, and O. Zinenko, “Polygeist: Raising
     Parallel Programming, vol. 34, pp. 261–317, 2006.                              c to polyhedral mlir,” in 2021 30th International Conference on Parallel
[18] S. Grauer-Gray, L. Xu, R. Searles, S. Ayalasomayajula, and J. Cavazos,         Architectures and Compilation Techniques (PACT). IEEE, 2021, pp.
     “Auto-tuning a high-level language targeted to gpu codes,” in 2012             45–59.
     innovative parallel computing (InPar). Ieee, 2012, pp. 1–10.              [40] W. S. Moses, I. R. Ivanov, J. Domke, T. Endo, J. Doerfert, and O. Zinenko,
[19] T. Grosser, H. Zheng, R. Aloor, A. Simbürger, A. Größlinger, and L.-           “High-performance gpu-to-cpu transpilation and optimization via high-
     N. Pouchet, “Polly-polyhedral optimization in llvm,” in Proceedings of         level parallel constructs,” in Proceedings of the 28th ACM SIGPLAN
     the First International Workshop on Polyhedral Compilation Techniques          Annual Symposium on Principles and Practice of Parallel Programming,
     (IMPACT), vol. 2011, 2011, p. 1.                                               2023, pp. 119–134.
[41] A. Munshi, “The opencl specification,” in 2009 IEEE Hot Chips 21
     Symposium (HCS). IEEE, 2009, pp. 1–314.
[42] G. Ozen and M. Wolfe, “Performant portable openmp,” in Proceedings
     of the 31st ACM SIGPLAN International Conference on Compiler
     Construction, 2022, pp. 156–168.
[43] A. Patel, S. Tian, J. Doerfert, and B. Chapman, “A virtual gpu
     as developer-friendly openmp offload target,” in 50th International
     Conference on Parallel Processing Workshop, 2021, pp. 1–7.
[44] A. Qasem, A. M. Aji, and M. L. Chu, “Investigating data layout
     transformations in chapel,” in 2018 IEEE International Parallel and
     Distributed Processing Symposium Workshops (IPDPSW). IEEE, 2018,
     pp. 915–924.
[45] A. Qasem and S. Teich, “Evaluating the impact of data layout and
     placement on the energy efficiency of heterogeneous applications,” in
     2017 Eighth International Green and Sustainable Computing Conference
     (IGSC). IEEE, 2017, pp. 1–8.
[46] E. Stafford, B. Pérez, J. L. Bosque, R. Beivide, and M. Valero, “To
     distribute or not to distribute: the question of load balancing for
     performance or energy,” in Euro-Par 2017. Springer, 2017, pp. 710–722.
[47] J. A. Stratton, V. Grover, J. Marathe, B. Aarts, M. Murphy, Z. Hu,
     and W.-m. W. Hwu, “Efficient compilation of fine-grained spmd-
     threaded programs for multicore cpus,” in Proceedings of the 8th
     annual IEEE/ACM international symposium on Code generation and
     optimization, 2010, pp. 111–119.
[48] J. A. Stratton, C. Rodrigues, I.-J. Sung, N. Obeid, L.-W. Chang,
     N. Anssari, G. D. Liu, and W.-m. W. Hwu, “Parboil: A revised benchmark
     suite for scientific and commercial throughput computing,” Center for
     Reliable and High-Performance Computing, vol. 127, p. 27, 2012.
[49] J. A. Stratton, S. S. Stone, and W.-M. W. Hwu, “Mcuda: An efficient
     implementation of cuda kernels for multi-core cpus,” in International
     Workshop on Languages and Compilers for Parallel Computing. Springer,
     2008, pp. 16–30.
[50] Y. Sun, X. Gong, A. K. Ziabari, L. Yu, X. Li, S. Mukherjee, C. McCard-
     well, A. Villegas, and D. Kaeli, “Hetero-mark, a benchmark suite for
     cpu-gpu collaborative computing,” in 2016 IEEE International Symposium
     on Workload Characterization (IISWC). IEEE, 2016, pp. 1–10.
[51] C. R. Trott et al., “Kokkos 3: Programming model extensions for the
     exascale era,” IEEE Transactions on Parallel and Distributed Systems,
     vol. 33, no. 4, 2022.
[52] S. Verdoolaege, J. Carlos Juega, A. Cohen, J. Ignacio Gomez, C. Tenllado,
     and F. Catthoor, “Polyhedral parallel code generation for cuda,” ACM
     Transactions on Architecture and Code Optimization (TACO), vol. 9,
     no. 4, pp. 1–23, 2013.
[53] Y. Zhang, M. Sinclair, and A. A. Chien, “Improving performance
     portability in opencl programs,” in Supercomputing: 28th International
     Supercomputing Conference, ISC 2013, Leipzig, Germany, June 16-20,
     2013. Proceedings 28. Springer, 2013, pp. 136–150.
