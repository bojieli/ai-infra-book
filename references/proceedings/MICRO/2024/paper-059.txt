                Over-synchronization in GPU Programs
                                  Ajay Nayak                                            Arkaprava Basu
                           Indian Institute of Science                             Indian Institute of Science
                                Bengaluru, India                                       Bengaluru, India
                              ajaynayak@iisc.ac.in                                   arkapravab@iisc.ac.in




   Abstract—The performance of GPU (Graphics Processing                that the effect of the synchronization is visible only within
Unit)-accelerated functions affects a large spectrum of modern         a subset of threads (i.e., its scope). Those with a narrower
software. Efficiently synchronizing across thousands of concur-        scope that synchronizes a smaller subset of threads are faster.
rent threads is critical to the performance of GPU programs.
GPU vendors have introduced advanced programming con-                  For example, on an NVIDIA RTX 3090 GPU, the block-
structs, e.g., scopes, for efficiently synchronizing within a chosen   scope fence that guarantees visibility in the issuing thread’s
subset of threads. However, programmers must explicitly employ         threadblock is 21× faster than the device-scope fence that
them, where applicable, to benefit from such features.                 ensures visibility across all threads of a kernel.
   We demonstrate how GPU programs can leave performance
on the table by failing to fully harness advanced synchronization         The default scope in both CUDA and OpenCL is the
features in modern GPUs – leading to over-synchronization. We          device scope. Programmers must explicitly use a narrower
discover three different variants of over-synchronization observed     scope (e.g., block), where applicable, for good performance.
in real-world applications. We then build a tool, ScopeAdvice, to      However, a data race can manifest if the scope is narrower than
find cases of over-synchronization in CUDA programs. Avoid-            needed for synchronizing threads [14]–[16]. Unsurprisingly,
ing reported over-synchronization improves the performance of
several GPU applications by up to 55%.                                 programmers often use a wider scope than necessary, priori-
   Index Terms—GPU, synchronization, performance bugs.                 tizing correctness over performance, even in popular CUDA
                                                                       libraries written by experts (e.g., cuML [17]). A programmer
                       I. I NTRODUCTION                                could have used a narrower-scoped operation to improve
   A broad class of today’s software, from deep learning, graph        performance without altering the program’s behavior. We call
processing, and data analytics to scientific computing, rely on        such occurrences over-synchronization.
GPU’s massively parallel computing [1]–[3]. Consequently,                 We discover three subtle variants of over-synchronization
the efficiency of GPU programs has an overwhelming bearing             and redundant synchronization in CUDA programs [18] that
on the performance of a large swath of software.                       may leave significant performance on the table. Their subtlety
   GPUs significantly contribute to a server’s capital and             arises from the interaction of CUDA’s scoped synchronization
operational costs. We estimate that 87% of the cost of a DGX           model and the GPU micro-architecture.
server with four NVIDIA A100 GPUs is due to the GPUs                      First, we find that a fence can have a wider scope than
only [4], [5]. They contribute significantly to the power budget,      necessary due to the nuanced semantics of related memory
too [6]. Unfortunately, GPUs are often left under-utilized by          operations. Assume that a variable is written to (store) and read
the software, significantly but unnecessarily increasing the cost      from (load) by threads from different threadblocks. It is natural
of computing [7]–[11].                                                 to assume that synchronization (e.g., fence) of device scope
   To the best of our knowledge, we are the first to demonstrate       is needed after the write and before the read to ensure that
how sub-optimal synchronization in GPU programs can cause              the latter observes data produced by the first thread. However,
significant performance inefficiency. Many key use cases of            we observe that a narrower-scoped fence will suffice if the
GPU, e.g., graph processing, need fine-grain synchronization,          instruction performing the read skips the GPU’s L1 cache to
e.g., fences. The efficiency of synchronizing across thousands         directly look up the GPU’s L2 cache.1 For example, a load
of concurrent GPU threads can significantly affect a GPU               to a variable declared ‘volatile’ is guaranteed to skip the L1
program’s overall performance. However, GPU’s hierarchi-               cache and return the value from the coherent L2 cache or
cal programming paradigm and its nuanced synchronization               memory (§14.5.3.3 of [18]). Similarly, the memory operations
model [12], [13] make writing correct GPU programs with                in a device-scoped atomic read-modify-write instruction also
minimal synchronization challenging.                                   skip the L1 cache. In such scenarios, the fence is needed
   To keep thousands of concurrent GPU threads tractable,              only to ensure ordering amongst the loads/stores and not for
they are organized as groups of up to 1024 threads called a            ensuring visibility of updates across threadblocks. As a fence
threadblock. Synchronizing across all the threads of a kernel          of any scope guarantees ordering, using a narrower-scoped
is slow and often unnecessary due to GPU’s hierarchical
programming paradigm. Programming languages like CUDA                    1 Unlike CPUs, GPU hardware, by design, eschews cache coherence for its
and OpenCL support scoped synchronization to write ef-                 L1 caches to avoid overheads of maintaining coherence across thousands of
ficiently synchronized code. The scope qualifiers guarantee            concurrent loads/stores. The L2 cache, however, is coherent.
fence, e.g., a block-scoped fence, before reading the variable,   • We demonstrate that removing over-synchronization from
would have preserved the program semantics (Section III-1).       CUDA programs does not alter the program’s semantics.
   In another incarnation of over-synchronization, a fence        • We built a runtime tool, ScopeAdvice, to detect and report
could be entirely redundant. We notice that the threadblock       suspected cases of over-synchronization to the programmer.
barrier in CUDA ( syncthreads) encompasses the semantics          Following the tool’s advice, programs sped up by up to 55%.
of a block-scoped fence beyond acting as an execution barrier
                                                                                        II. BACKGROUND
across threads in a threadblock. Thus, using a block-scoped
fence immediately following a barrier is redundant. Finally, we   GPU hierarchy: GPUs arrange thousands of concurrent
notice that common lock/unlock routines may unnecessarily         threads in a hierarchy. The top of the hardware hierarchy
synchronize across all threads of a kernel, even when the         consists of Streaming Multiprocessors (SM). Each SM sports
communicating threads fall within the same threadblock.           multiple Single-Instruction-Multiple-Data (SIMD) units fur-
   We find evidence of these variants of over-synchronizations    ther divided into execution lanes. All SIMD units in an SM
across popular GPU-accelerated libraries and open-source          share a private L1 cache and scratchpad (shared memory). All
programs, including cuML [17], cudpp [19], cuBLAS [20],           the SMs share a larger L2 cache. The GPU’s global memory
and gpufilter [21]. In each case, modifying only three lines      (on-board device memory) is accessible to all GPU threads.
of code on average, i.e., either by changing a fence’s scope      The hardware caches contents of global memory in L1 and
or removing one, improves the performance by up to 55%            L2 caches. GPU hardware does not keep its L1 cache contents
without affecting program behavior.                               coherent to avoid excessive overheads across hundreds of SMs.
   We demonstrate how removing these over-synchronizations        However, L2 cache contents are always coherent.
retains the original program’s semantics. NVIDIA publishes           In the software hierarchy, the smallest execution unit is
only a programming optimization guide for CUDA but does           a thread mapped to an execution lane in a SIMD unit. A
not formally specify its memory consistency model. However,       group of threads (typically 32) forms a warp, the smallest
it formally specifies the memory model for PTX [22]. All          hardware-scheduled unit of work that executes on SIMD units.
programs written in CUDA code are lowered to PTX. We              A block (threadblock) is a group of warps that executes on the
demonstrate that the removal of over-synchronizations is also     same SM. Threads in a threadblock can communicate amongst
valid against the formal PTX memory consistency model.            themselves via scratchpad. Finally, a grid comprises all the
                                                                  threadblocks used for executing a GPU kernel.
   Unfortunately, given the subtleties of these over-
                                                                  Scoped Operations on GPUs: Global synchronization across
synchronizations, it could be challenging to manually notice
                                                                  all the threads on a GPU is often unnecessary. Since the
these, as evidenced by their existence in libraries written by
                                                                  execution is naturally divided into threadblocks, synchroniza-
experts. Our final contribution thus is ScopeAdvice, a tool
                                                                  tion within a threadblock is often sufficient. NVIDIA, AMD
that detects over-synchronization in CUDA programs. It uses
                                                                  provides a programming construct called scopes that enables
NVIDIA’s NVBit library [23] to trace a kernel’s execution
                                                                  specifying the subset of the threads that should synchronize.
and gather relevant memory access and synchronization traces
                                                                     CUDA supports scope qualifiers for fence and atomic oper-
for analysis. ScopeAdvice reports the line number, source
                                                                  ations. Fences in CUDA provide both ordering and visibility
file, and the type of over-synchronization (if present). Given
                                                                  guarantees. A fence of any scope ensures that memory in-
concrete, actionable advice, the programmer can enhance
                                                                  structions in the issuing thread cannot be reordered across the
their code to improve performance.
                                                                  fence. The scope qualifier of the fence ensures the visibility
   Beyond its usefulness to existing programs, ScopeAdvice        of operations across a subset of threads. For example, a block-
eases programming too. Scoped synchronization significantly       scoped fence ( threadfence block) guarantees that all writes
burdens programmers [24], [25]. With ScopeAdvice, program-        by the issuing thread are made visible to all the threads in its
mers can first focus on functional correctness while writing      threadblock. In contrast, a device-scoped fence ( threadfence,
new programs, possibly with over-synchronization, and then        default) must ensure that the writes before it are visible across
use ScopeAdvice to find optimization opportunities.               all the threadblocks. This requires flushing dirty cache lines
   Over-synchronizations will likely become more prevalent as     from the private, incoherent L1 cache to the coherent L2. The
GPU vendors continue to introduce newer levels in the pro-        device scope must ensure that the reads after the fence get
gramming hierarchy and, thus, newer synchronization scopes.       the data from the L2 cache, which requires invalidating the
For example, in Hopper GPUs, NVIDIA introduced a new              L1 cache contents. In comparison, a block-scoped fence must
level in the hierarchy called a cluster, encompassing up to       ensure visibility only within a threadblock. It does not require
eight threadblocks sharing a memory region (§2.2.1 of [18]).      flushing or invalidating L1 cache contents since all threads
The associated new synchronization scopes further increase        in a threadblock share the same L1 cache. The system scope
the possibility of introducing over-synchronizations.             ensures visibility across all GPUs (multi-GPU system) and
   In summary, we make the following contributions.               CPU threads. We focus on execution on a single GPU and,
• We are the first to report sub-optimal performance of GPU       thus, limit our discussion on block and device scopes.
programs due to over-synchronization. We find three variants         Atomic read-modify-write (RMW) instructions can also be
of over-synchronization.                                          qualified with scopes. Block-scoped atomics ensure visibility
                 (global) volatile *flags; (global) *sum, *status;
                                                                        1) Variant 1: Programmers assume that they must use
1     volatile Flags *mFlag = flags + blockIdx.x;
2     mFlag->sum = SUM;                                              device-scoped fences when communicating across threads
3     __threadfence();                                               from different threadblocks. While generally true, a block-
4     mFlag->status = 1;
5     ...                                                            scoped fence is enough if the instruction reading the shared
6     volatile Flags *sFlag = flags + (blockIdx.x - 1);              variable skips the L1 cache to directly access the L2
7     while (sFlag->status == 0);
8     __threadfence(); // _block() is sufficient                     cache/memory. This observation holds because the visibility
9     accumulate += sFlag->sum;                                      of updates by threads across threadblocks, i.e., values read, is
                    (a) In the presence of volatile variables.       guaranteed by such instructions. Thus, the fence is necessary
1     oldVal = atomicAdd(sum, SUM);
                                                                     to ensure only the ordering among memory operations in
2     __threadfence(); // _block() is sufficient                     the issuing thread. Since all fences, including the narrowest
3     atomicExch(status, 1);
4     ...
                                                                     scope, ensure ordering, a wider-scoped one is unnecessary. We
5     while (!atomicCAS(status, 1, 0));                              find that such over-synchronizations can manifest in two ways
6     __threadfence(); // _block() is sufficient
7     accumulate += atomicAdd(sum, 0);
                                                                     based on how the shared variable is read. We use producer-
                                                                     consumer constructs to describe these cases.
              (b) In the presence of device-scoped atomic RMWs.      In the presence of volatile variables: Consider a simplified
    Fig. 1: Over-synchronization due to accesses skipping L1$.       snippet (Figure 1a) from the ‘decoupledLookback’ kernel in
                                                                     cuML [17]. A thread updates the variable sum, followed by a
                                                                     device-scoped fence in lines 2-3. In line 4, the thread updates
across all threads within a threadblock. Since threads in a          status, indicating that the updated sum is available. The fence
threadblock share an L1 cache, block-scoped atomics can be           in line 3 is needed to ensure that status is updated after making
completed in the L1 cache. A device-scoped atomic ensures            the updated sum visible to all threads. A thread from another
visibility across all threadblocks. Thus, it must skip the L1        threadblock waits for the status to be updated and then reads
cache and read from/write to the coherent L2 cache shared by         the updated sum (lines 7-9).
all the threads of a kernel (§8.4.1 of [12]). Further, atomics in       The device-scoped fence in line 8 seems necessary as
CUDA follow relaxed semantics, i.e., loads/stores can bypass         the threads accessing sum are from different threadblocks.
atomics (§7.14 of [18]). Hence, lock/unlock routines in CUDA         However, mFlag and sFlag are declared volatile. Note that
are implemented as a combination of an atomic and a fence.           declaring a pointer to a struct type as volatile (here, mFlag
   CUDA also provides barrier ( syncthreads) instruction that        and sFlag) means that the pointer points to a volatile structure.
ensures all threads of issuing thread’s threadblock reach the        This implies that the entire structure is to be treated as volatile.
barrier before proceeding further. It encompasses the seman-         The loads to volatile variables (status and flag members in
tics of a threadfence block – writes by any thread of a              the Flags struct type) skip the L1 cache to read the latest
threadblock before the barrier is visible to all threads in that     value from the coherent L2 cache (§7.5 of [18]). This ensures
threadblock after the barrier.                                       the visibility of updates made by threads across threadblocks
Impact of volatile keyword: The volatile qualifier for variables     (§14.5.3.3 of [18]). Thus, threads always read the latest values
in CUDA impacts their visibility. Access to volatile variables       of sum. The fence in line 8 is needed only for ordering the
skips the L1 cache and returns data from the GPU’s L2 cache          reads of status (line 7) and sum (line 9). Thus, the narrowest
(§14.5.3.3 of [18]). Consequently, a block-scoped fence is           scoped fence (here, block-scope) is sufficient.
sufficient to order reads of two volatile variables even when        In the presence of device-scoped atomic RMWs: The mem-
threads from different threadblocks access them.                     ory operations (read/write) of a device-scoped atomic RMW
                                                                     behave similarly to those of a volatile variable – skip the L1
       III. OVER - SYNCHRONIZATION IN GPU PROGRAMS                   cache to access the latest data from the L2 cache. Thus, an
                                                                     accompanying fence is needed only for ordering.
   Programmers may use a wider scope in fences than needed              Consider the code snippet in Figure 1b. A thread updates
or add a redundant fence to be safe from introducing data            the variable sum using a device-scoped atomicAdd, followed
races [15], [16]. We call this over-synchronization, i.e., oc-       by a device-scoped fence in lines 1-2. In line 3, the thread
currences where using a narrower scoped fence would not              updates the variable status using a device-scoped atomicExch,
have introduced a data race (bug) and preserved the program’s        indicating that the updated data is visible to all threads. A
behavior. A program’s behavior is preserved by ensuring that         thread from another threadblock waits for the status to be
all store-to-load relations remain unaltered, i.e., the stores       updated and then reads the sum (lines 5-7) using device-scoped
whose output a load can return are the same in the original          atomicCAS and atomicAdd, respectively.
program and its version without over-synchronization.                   The device-scoped fences in lines 2 and 6 seem necessary
   We analyze open-source CUDA programs and libraries                as the threads accessing data are from different threadblocks.
to investigate how over-synchronization occurs in practice.          However, the snippet uses device-scoped atomic RMWs, en-
We find three distinct variants of over-synchronization. This        suring that all threads observe the latest value. The fences
analysis leads to understanding code constructs that cause it        in lines 2 and 6 are needed only for ordering the atomics in
and drives the tool’s design to detect over-synchronization.         lines 1, 3, and lines 5, 7, respectively. The atomicity guarantee
       (stack) *saveValue, *Aval; (shared) *addressPad               one must use different scoped operations based on the com-
1   ...
2   for (i = 0; i < 8; i++)                                          municating threads. A block-scoped fence in Figure 3 would
3     saveValue[i] = addressPad[tid*depth+i];                        have ensured the ordering and visibility of memory operations
4   __threadfence(); // redundant
5   __syncthreads();                                                 inside the acquire-release critical section amongst threads
6   for (i = 0; i < 8; i++)                                          within a threadblock (§7.5 of [18]). Thus, fences in lines 3
7     addressPad[Aval[i]] = saveValue[i];
8   ...                                                              and 6 are over-synchronized.
            Fig. 2: Redundant fence due to barrier.
                                                                          IV. VALIDATION AGAINST PTX MEMORY MODEL
1   // implementing lock acquire                                        Section III reports over-synchronization as per the CUDA
2   while(atomicCAS(lock, 0, 1) != 0);
3   __threadfence(); // _block() for intra-threadblock lock          programming guide. However, CUDA does not formally spec-
4                                                                    ify a memory model. Instead, NVIDIA has a formally specified
5   // implementing lock release
6   __threadfence(); // _block() for intra-threadblock lock          memory model for PTX [12], [22]. PTX is a virtual instruction
7   atomicExch(lock, 0);                                             set for NVIDIA GPUs. CUDA programs are first lowered to
    Fig. 3: Over-synchronized intra-threadblock locking.             the PTX, which is then lowered to SASS or binary that runs on
                                                                     the GPU. It is natural to wonder if our proposed optimizations
                                                                     of removing over-synchronization are valid as per the PTX
requires that once the read to an atomic variable completes,         memory model. Removal of an over-synchronization is valid
there cannot be any intervening access before the write part         if it: 1 does not introduce data races, and 2 preserves
of the atomic. Therefore, ensuring that the reads of atomic          the original program behavior. The program’s behavior is
RMWs are ordered guarantees the ordering of atomics. The             preserved if a load can return only the values generated by
block-scoped fences are sufficient to ensure this.                   the same set of stores as in the original program.
   2) Variant 2: In CUDA, barriers implicitly contain the            Relevant background on PTX memory model: CUDA can be
semantics of a block-scoped fence (§7.6 of [18]). Besides            easily mapped to the PTX instructions. Accesses to volatile
ensuring that threads in a threadblock reach the barrier, it guar-   variables in CUDA are suffixed with ‘.volatile’ in PTX. Atom-
antees that writes before the barrier are visible to reads from      ics are prefixed with ‘atom’ and specify scope as a substring,
threads in that threadblock after the barrier. Thus, a block-        i.e., ‘.cta,’ ‘.gpu,’ and ‘.sys’ for block, device, and system
scoped fence immediately neighboring a barrier is redundant.         scopes, respectively. A threadfence is lowered to a membar
   Figure 2 shows a code snippet from the sort application           instruction suffixed with its scope. The barrier ( syncthreads)
in the cudpp library [19]. A thread reads from addressPad            is lowered to bar.sync. We describe a few definitions from the
in the shared address space (scratchpad) and writes to the           PTX memory model that are relevant to our discussion:
local variable saveValue (registers or thread-local memory).         • Morally strong memory operations (§8.6 of [12]). Mem-
This is followed by a device-scoped fence and a barrier. Next,       ory operations from different threads are defined as morally
saveValue is written back to addressPad, depending on the            strong when: 1 they are performed on the same address,
content of Aval. Note the double indirection in the accesses to      and 2 they have a scope that includes the involved threads.
addressPad in lines 3 and 7. The barrier (line 5) ensures that       For example, a read and a write from threads of different
the read in line 3 can race with the write in line 7.                threadblocks are morally strong if they are performed on the
   It would seem that the programmers intended to guarantee          same address and the operations are device or system-scoped.
ordering using a device-scoped fence on line 4. However,             • Data races (§8.6.1 of [12]). The PTX model defines a data
a block-scoped fence would suffice since threads within              race as a set of conflicting operations, with at least one write,
a threadblock are communicating via the scratchpad. No-              which are not morally strong and are not ordered.
tably, the barrier (line 5) immediately following the over-          • Program order (§8.8.1 of [12]) is the sequential order of
synchronized device-scoped fence (line 4) makes the fence            all operations performed by a thread.
redundant. Therefore, removing the fence does not alter the          • Observation order (§8.8.2 of [12]) relates morally strong
program’s behavior or introduce race.                                read, write originating from different threads where the value
   3) Variant 3: Applications often use lock (acquire) and           read is the value written by the write.
unlock (release) routines for synchronization. In CUDA, these        • Synchronization order (§8.8.4 of [12]) specifies the scope
routines are implemented with a sequence of fences and atom-         qualifier for ordering the memory operations from different
ics [13], [26], [27]. While synchronizing among threads from         threads. For example, a device-scoped fence establishes the
different threadblocks necessitates device-scoped operations,        synchronization order of memory operations from threads of
block-scope is sufficient if the communicating threads belong        different threadblocks.
to a threadblock. Using the same routine for lock or unlock
makes them over-synchronized when communicating threads              A. Validity of Variant 1 over-synchronization
do not straddle threadblocks.                                          The example programs in Variant 1 (Figure 1) follow
   Figure 3 shows a typical code snippet implementing                the producer-consumer construct. The programmer expects
acquire-release for fine-grain locking. For better performance,      that the consumer reads the value written by the producer
                   (global) sum = 0, flag = 0;                          in each thread and the observation order across the threads
 1   W1 st.volatile [sum], SUM;           // sum = SUM;                 provide the total ordering of W1→F1→W2→R1→F2→R2.
 2   F1 membar.gl;                        // __threadfence();
 3   W2 st.volatile [flag], 1;            // flag = 1;                  This establishes the observation order W1→R2. Thus, the
 4   ...                                                                consumer always reads the latest value written at W1 of sum
 5   LB_1:
 6   R1 ld.volatile %r1, [flag];          // while (flag == 0);         at R2, retaining the original program behavior.
 7      setp.eq %p1, %r1, 0;              //
 8      @%p1 bra LB_1;                    //
                                                                           2) Eliminating over-synchronization in presence of
 9   F2 membar.cta;                       // __threadfence_block();     device-scoped atomic RMWs: Figure 4b is a simplified PTX
10   R2 ld.volatile %r2, [sum];           // aggregate += sum;
                                                                        representation of the program from Figure 1b after eliminating
          (a) PTX representation of program in Figure 1a.               Variant 1 over-synchronization. The inline comments provide
                                                                        reference to the corresponding CUDA syntax. The instructions
 1   A1 atom.gpu.add %r2, [sum], SUM;     // atomicAdd(...);
 2   F3 membar.cta;                       // __threadfence_block();     in lines 1-3, Figure 4b, map to lines 1-3, Figure 1b. The in-
 3   A2 atom.gpu.exch %r3, [flag], 1;     // atomicExch(...)            structions in lines 6-10, Figure 4b, map to lines 5-7, Figure 1b.
 4   ...
 5   LB_2:                                                              No data race: All memory operations in the program are
 6   A3 atom.gpu.cas %r1, [flag], 1, 0;    // while (!atomicCAS(...))   device-scoped atomic, and thus morally strong. Consider the
 7      setp.eq %p1, %r1, 0;               //
 8      @%p1 bra LB_2;                     //                           atomic RMWs A2 and A3 on the flag. These are performed
 9   F4 membar.cta;                        // __threadfence_block();    on the same address, and both are device-scoped. The com-
10   A4 atom.gpu.add %r6, [sum], 0;        // atomicAdd(...);
                                                                        municating threads fall within the scope as it covers all GPU
          (b) PTX representation of program in Figure 1b.               threads. As all memory operations are morally strong, they
Fig. 4: Simplified PTX representation of programs in Figure 1           cannot cause a data race (§8.6.1 of [12]).
after eliminating Variant 1 over-synchronization.                       Block-scoped fences ensures latest value: The atomic at A4
                                                                        must observe the effect of A1 if A3 observes the effect of
                                                                        A2. The loop (Figure 4b, lines 6-8) ensures that the consumer
                                                                        does not proceed till it reads the value of 1 for flag, i.e., the
after the program’s execution. Thus, the elimination of over-
                                                                        observation order A2→A3. There can only be two possibilities
synchronization are valid if they satisfy two requirements: 1
                                                                        when A4 may not observe the effect of A1: 1 if A4 gets re-
there are no data races in the optimized code, and 2 the
                                                                        ordered before A3, or 2 if A1 gets re-ordered after A2.
consumer receives the value written by the producer.
                                                                           However, fences F3 and F4 help avoid these re-orderings
   1) Eliminating over-synchronization in the presence of               since fences of any scope guarantee program order (§9.7.12.3
volatile variable: Figure 4a is a simplified PTX representation         of [12]). The fences ensure program order of A1→F3→A2
of the program from Figure 1a after removing the Variant 1              in the producer and of A3→F4→A4 in the consumer. The
over-synchronization. The inline comments provide reference             fence F3 ensures that the ‘read’ in A1 completes before A2
to the corresponding CUDA syntax. The instructions in lines             completes. As A1 is an atomic RMW, once the read completes,
1-3, Figure 4a, map to lines 2-4, Figure 1a. The instructions           its ‘write’ must also complete before any other intervening
in lines 6-10, Figure 4a, map to lines 7-9, Figure 1a.                  access to data. The program order in each thread and the
No data race: Note that all memory operations in the program            observation order across the threads provide the total ordering
are volatile. Volatile operations are equivalent to ‘relaxed.sys’       of A1→F3→A2→A3→F4→A4. Thus, the consumer always
memory operations (system scope) as per the PTX memory                  observes the value written at A1 for data at A4.
model (§9.7.8.8 of [12]). Thus, all pairs of memory operations             3) Eliminating redundant synchronization order: Thanks
accessing the same address are morally strong. Morally strong           to the use of device-scoped fences, the original programs
operations cannot cause a data race (§8.6.1 of [12]).                   in Figure 1 had synchronization order between the producer
A block-scoped fence in the consumer ensures latest value:              and consumer threads. However, in the presence of volatile
The load at R2 must read the value written at W1 if R1 reads            variables (Figure 4a), R1 and R2 themselves ensure visibility
the value written at W2. Note that the loop (Figure 4a, lines           across threadblocks. For device-scoped atomic RMWs (Fig-
6-8) ensures that the consumer does not proceed till it reads           ure 4b), the visibility is guaranteed by the atomic themselves
the value of 1 for flag, i.e., the observation order W2→R1.             (A1-A4). Thus, we make a crucial observation that the syn-
There can only be two possibilities when the load at R2 may             chronization order in the original program is unnecessary to
not read the latest value written at W1: 1 if the instruction at        preserve the program’s semantics since fences are not needed
R2 gets re-ordered (e.g., by the compiler) before that at R1,           for visibility. We leverage this to use block-scoped fences that
or 2 if the instruction at W1 gets re-ordered after that at W2.         sacrifice synchronization order in the optimized program.
   However, the fence instructions at F1 and F2 guarantee
sequential program order (§9.7.12.3 of [12]) preventing the             B. Validity of Variant 2 and Variant 3 over-synchronizations
above-mentioned possibilities since a fence of any scope                  Eliminating over-synchronizations due to Variants 2 and
guarantees program order. The fences ensure program order               3 are trivially valid. In PTX, the elimination of Variant 2
of W1→F1→W2 in the producer and of R1→F2→R2 in                          suggests that when a block-scoped fence immediately follows
the consumer. Further, F1 ensures that W1 is visible at the             a bar.sync or vice-versa, the block-scoped fence is redundant.
device scope before W2 in the same scope. The program order             The semantics of bar.sync is a superset of a block-scoped fence
(§9.7.12.1 of [12]). Thus, removing the fence cannot alter the     1    ...
program’s behavior.                                                2    shm = shared[idx]
  In the PTX representation, eliminating Variant 3 suggests        3    shm *=10
                                                                                                getNextSync          Window 1
using a block-scoped fence to synchronize threads of a thread-     4    global[idx] = shm
block. A block-scoped fence guarantees visibility of memory        5    ...
operations within a threadblock (§9.7.12.3 of [12]) and, thus,     6    __threadfence()
cannot affect the program’s behavior.                              7    ...
                                                                                                                     Window 2
                                                                   8    data = global[idx] getPrevSync
  V. S COPE A DVICE : F INDING OVER - SYNCHRONIZATION              9    ...

   Manually finding over-synchronization can be challenging,                       Fig. 5: Example of windows.
as evidenced by over-synchronizations in libraries written by
CUDA experts. Thus we set out to build ScopeAdvice, a tool
that automatically highlights cases of over-synchronization in     not capture barriers either. Static analysis of the source code
programs. Such tools can also ease programming by allowing         is enough to infer whether a block-scoped fence is redundant
programmers to focus on functional correctness. The tool can       in the presence of a neighboring barrier.
help find opportunities to optimize performance later.
   To infer if a fence is over-synchronized, we need to as-        B. Relating Memory and Fence Operations
certain its necessity for a program’s functional correctness.         The necessity of synchronization and its scope depends
We assume that all fences in a program that are not qualified      upon the memory accesses and the threads accessing a shared
with the narrowest scope (block here) are over-synchronized        location. Thus, we relate memory accesses to a fence to infer
and establish the necessity of their wider scopes based on         the necessity through windowing, similar to prior work [29].
memory accesses. First, we map the stream of memory and               We split the stream of instructions into windows with
synchronization accesses onto a trace for reasoning [28]. Next,    fences acting as sentinels. A window is a set of instructions
we lay the rules on the trace to detect over-synchronization.      between two fences for each thread. We treat the beginning
                                                                   and the end of a kernel to contain a fence implicitly. Thus,
A. Modeling Program Execution as a Trace
                                                                   all instructions are part of a window, even if the kernel has
   We define a trace as a stream of operations executed on a       no fences. Windows relate every memory operation that needs
GPU, i.e., a dynamic execution trace of a kernel. It captures      synchronization (rd, wr, rdSkipL1, and wrV) to their closest
necessary information to infer if a fence is over-synchronized.    fence. For rd and rdSkipL1, the corresponding fence is the
The trace contains memory instructions to global memory and        one that started the window in which the operation lies. This
relevant synchronization operations. Access to other types of      fence affects the value that is read. Similarly, for wr and wrV,
GPU memory, e.g., scratchpad, does not necessitate a fence         the corresponding fence is the one that terminated the window
with a wide scope and, thus, is ignored. The trace contains        in which the operation lies. This fence determines the group
the following elements:                                            of threads to which the write is guaranteed to be visible.
• rd(t, x) or wr(t, x); thread t performs a read or write          We define two routines for the purpose: 1 getPrevSync(t,
operation on a global memory address x.                            read); returns the preceding fence for a read (rd or rdSkipL1)
• rdSkipL1(t, x); thread t performs a read on a global memory      executed by thread t, 2 getNextSync(t, write); returns the next
address x that skips the L1 cache.                                 fence for a write (wr or wrV) executed by thread t.
• wrV(t, x); thread t performs a write on a global memory             We illustrate the creation of windows with the example in
address x that is declared volatile.                               Figure 5. The code is split into two windows; one consists of
• atm(t, x, scope); thread t performs a scoped atomic RMW          all instructions until line 6 and one after. The getNextSync()
operation on a global memory address x.                            for the write in line 4 and the getPrevSync() for the read in
• fence(t); thread t performs a device-scoped fence.               line 8 returns the fence in line 6.
   The trace captures the addresses of load/stores and the
thread ID performing it (e.g., rd(t, x)). Loads to global memory   C. Identifying Over-synchronized Operations
that skip the L1 cache (e.g., variables declared ‘volatile’)          We assume that every fence with a scope wider than block,
are captured as rdSkipL1(t, x). Stores to variables declared       i.e., device and system, to be over-synchronized unless its
‘volatile’ are captured as wrV. For atomic RMWs, the trace         necessity is established by Rules 1 and 2 below.
captures its scope, the address, and the thread ID. A block-       Rule 1: Given two operations, rdSkipL1(t1 , x) and wrV(t2 ,
scoped atomic is treated as if rd and wr are put together, while   x), such that t1 belongs to threadblock b1 , t2 belongs to
a device-scoped atomic has an effect of rdSkipL1 and wrV put       threadblock b2 , and b1 != b2 , then: 1 the fence identified
together. However, unlike wrV, a device-scoped atm does not        by getPrevSync(t1 , rdSkipL1) should be block-scoped, 2 the
need a fence to guarantee visibility across threadblocks. The      fence identified by getNextSync(t2 , wrV) should be device-
trace contains device-scoped fences executed by threads. As        scoped unless the wrV is due to a device-scoped atm. A
block-scoped fences have the narrowest scope, they cannot be       block-scoped fence ensures the ordering of all rd and rdSkipL1
over-synchronized. Thus, the trace skips them. The trace does      operations. This assurance, and the ones provided by rdSkipL1,
  GPU                                 CPU                                                         [64]   [1]  [1]     [5]    [2]    [23]
                                            analysis threads                                    Address Load Store WindowId Scope ThreadId
              Application
                                                ...                Detection
        Dynamic Instrumentation
                                                                                      Fig. 7: A unit of memory execution trace (with bit width).
                                                ...                      Over-
   Memory     Metadata  Memory                                        synchronized
                         Trace                         N-buffers       operations
            Fence Trace  Filter                                                                      [1]   [1]      [1]      [20]   [2]
                Memory Trace      Channel             CPU thread
                                                                                                    Valid Store MultiBlock BlockID Count


        Fig. 6: High-level components of ScopeAdvice.                                    Fig. 8: A unit of memory metadata (with bit width).


                                                                                              VI. I MPLEMENTATION OF S COPE A DVICE
ensures that the thread reads the latest value written by a thread                      Figure 6 provides an overview of ScopeAdvice’s imple-
from a different threadblock [18]. A device- or system-scoped                        mentation. A CUDA kernel is instrumented to collect the
fence would be over-synchronized. For wrV, a device-scoped                           execution trace as it executes on the GPU. ScopeAdvice ships
fence is necessary to ensure visibility across threadblocks.                         the collected trace and additional metadata to the CPU for
Rule 2: Given two operations rd(t1 , x) and wr(t2 , x), such                         analysis. The multi-threaded analyzer on the CPU enforces the
that t1 belongs to threadblock b1 , t2 belongs to thread-                            rules described in Section V on the execution trace, metadata,
block b2 , and b1 == b2 , then: 1 the fence identified by                            and source to identify over-synchronization.
getPrevSync(t1 , rd) should be block-scoped, 2 the fence
identified by getNextSync(t2 , wr) should be block-scoped. This                      A. Instrumentation and Execution Metadata
rule effectively says that a block-scoped fence is sufficient                           We use NVIDIA’s NVBit library [23] to instrument CUDA
if the threads writing to and reading from a shared location                         kernels. NVBit enables inspection and modification of CUDA
belong to the same threadblock. A wider scope (e.g., device                          assembly code (SASS). We use NVBit’s ‘channel’ (memory
or system) would be over-synchronized.                                               buffer) to communicate between the GPU and the CPU.
Rule 3: If a block, device or a system-scoped fence that                             Creating logical windows of memory instruction: First,
was found over-synchronized following Rule 1 or 2 is an                              ScopeAdvice instruments the kernel to divide the code into
immediate neighbor to a barrier in the source code, then the                         windows with fences as sentinels (Section V-B). A counter,
fence is redundant. This rule is not applied to the trace. Instead,                  called FenceId (initialized to 0), is incremented every time
it is applied during static source-code analysis of the source                       a fence is encountered in the code (static instruction). Each
after applying Rules 1 and 2 on the trace.                                           memory instruction (load, store, atomic) in the code is tagged
                                                                                     with the current counter value (FenceId), creating logical
   We demonstrate how these rules detect over-                                       windows of memory instructions. The counter value tagged
synchronizations in examples from Section III. Rule 1                                to each memory instruction acts as its WindowId.
captures the examples of Variant 1. The fence in line 8 of                           Instrumenting for execution trace: ScopeAdvice instruments
Figure 1a would be declared over-synchronized due to volatile                        loads, stores, atomic RMWs, and fences in a kernel. Figure 7
reads. Similarly, the fences in lines 2 and 6 of Figure 1b                           depicts a unit of trace for memory instructions (load, stores,
would be declared over-synchronized due to the presence                              atomic RMWs). Besides the address and instruction type,
of device-scoped atomic operations. To understand how the                            ScopeAdvice captures the instruction’s WindowId. Note that
rules work for Variant 2, note that 1 the execution trace                            each static instruction may execute many times (e.g., in a
contains accesses to the global memory only, 2 all fences are                        loop). Each such dynamic instance of an instruction will
assumed over-synchronized unless proven otherwise by Rules                           generate one unit of the trace. Recall that the trace should
1 or 2. In Figure 2, all accesses are to non-global memory                           contain only global memory accesses. We use isspacep.global
(scratchpad and thread-local store). Therefore, those accesses                       instruction to check if the address belongs to global memory.
will not appear in the trace. Consequently, the fence in line 4                      ScopeAdvice sets both the load and store bits in the trace for
will be declared over-synchronized since Rules 1 and 2 fail                          atomics and captures its scope. NVIDIA’s compiler compiles
to establish the need for its device scope. This fence will be                       loads and stores on ‘volatile’ data into distinguishable opcodes
treated as a block-scoped fence while applying Rule 3 during                         (e.g., LDG.SYS), making them easily identifiable. The Scope
the static analysis. That would then be declared redundant.                          field for loads and stores to ‘volatile’ data is set as ‘system.’
The over-synchronization in Figure 3 (Variant 3) is caught                              The execution trace also tracks fences. However, tracking
by applying Rule 2 since the accesses to the shared address                          every dynamic instance of fence is unnecessary in practice.
are from threads of the same threadblock.                                            Memory instructions requiring synchronization and the corre-
   Notice that while the trace contains atomic RMWs, the rules                       sponding fence often lie in the same code block (i.e., inside
are silent on them. While atomic RMWs are needed to track                            the same conditional). Thus, when a thread executes a memory
access to global memory, they are treated as a combination of                        instruction, it also executes the fence. Thus, tracking which
rd and wr or that of rdSkipL1 and wrV, as discussed earlier.                         thread executed which fence is adequate. Whenever a fence is
                                                                     1   def getPrevSync(threadId, windowId) -> fenceId:
executed, its static FenceId (assigned during instrumentation)       2      fenceId = windowId - 1 # decrement for previous
and the thread identifier (ThreadId) that executed the fence         3      while (fenceId > KERNEL_BEGINNING):
                                                                     4         if (fenceTrace(threadId, fenceId)):
are captured. ScopeAdvice keeps a two-dimensional bit vector         5            return fenceId
indexed by FenceId and ThreadId. On encountering a fence             6         fenceId -= 1
                                                                     7      return KERNEL_BEGINNING
during the kernel’s execution, the corresponding bit in the bit
vector is set (initially unset). Note that the dimensions of this         Fig. 9: Simplified implementation of getPrevSync.
bit vector are known before the kernel is launched. The number
of unique FenceIDs is known during the instrumentation of the        1   def detect(memTrace, memMetadata, fences) -> list[fenceId]:
                                                                     2      osd = fences # All fences assumed over-synchronized
code, and the number of threads is a kernel launch parameter.        3      for (address : globalAddressesAllocated):
Memory address metadata: Along with memory access trace              4         isSt = getBit(memMetadata[address], Store)
                                                                     5         isMB = getBit(memMetadata[address], MultiBlock)
and fences, we keep additional metadata for each unit of             6         if (isSt and isMB):
memory for ease of analysis. Figure 8 shows the layout of each       7            for (I: memTrace[address]):
                                                                     8               if (I.Load and not isVolatileLd(I)):
unit of metadata for every 4Bytes of global memory. The Store        9                  # Correctly scoped, removing
bit indicates if the location was modified. The MultiBlock bit      10                  osd.remove(getPrevSync(I.ThreadId, I.WindowId))
                                                                    11               if (I.Store and not isDevAtomic(I)):
is set if multiple threadblocks access the location. BlockId        12                  # Correctly scoped, removing
keeps the threadblock identifier of the thread that last accessed   13                  osd.remove(getNextSync(I.ThreadId, I.WindowId))
                                                                    14      return osd # over-synchronized fencesIds
the location. When a memory location is accessed for the first
time, the corresponding Valid bit and BlockId field are set.                 Fig. 10: Detection logic that runs on the host.
The Store bit is set on wr or atm. On further accesses, if the
BlockId of the accessing thread is different from the one in
the metadata, the MultiBlock bit is set. Section VI-C details       instances of memory instructions to the fences that deter-
how the Count is used in an optimization.                           mine the visibility of value consumed or produced by those
Managing memory for trace and metadata: ScopeAdvice                 instructions. The loads use getPrevSync, while stores use
ships trace units for memory accesses to the CPU as they            getNextSync. Figure 9 shows the pseudo-code of getPrevSync.
are generated while the kernel executes. However, the trace         It takes the threadId of the thread that executed the load and
units for fences and the memory metadata are shipped to the         the windowId of that load. It typically returns the windowId
CPU after the kernel’s execution ends. The size of the memory       argument decremented by 1. This value corresponds to the
needed to hold the trace for fence and memory metadata is           FenceId of the fence that affects the value read by that load.
known before the kernel launch. As discussed earlier, the size         While this is typically sufficient, the memory instruction and
of the two-dimensional bit vector of the fence trace is known       the identified fence may be present in different code blocks in
apriori. The size of the memory metadata is related to the          some cases. For example, the memory instruction is present
memory allocated for a kernel. CUDA programs typically pre-         in the else block, and the identified fence is in the if block of
allocate memory (e.g., using cudaMalloc) before the kernel          a conditional. The thread would not have executed the fence
launch. In contrast, the number of memory accesses is not           as they are on different paths. In such cases, ScopeAdvice
known apriori. Thus, trace units are not allocated on the GPU       uses the fence execution trace to check if the thread executed
but are shipped to the CPU for analysis as they are generated.      the fence. Line 4 of Figure 9 checks if the given fenceId
   Reserving GPU memory for the fence trace and memory              was executed by the thread (threadId). If not, it continues to
metadata would limit the memory available for the kernel.           decrement the counter until it finds a fence executed by the
ScopeAdvice thus allocates memory using Unified Virtual             thread or the beginning of the kernel. In short, it identifies the
Memory (UVM) [30], avoiding reservation. UVM driver auto-           latest fence executed by the GPU thread that determines the
matically brings the portions of allocated data from the CPU        value the load observes. Similarly, getNextSync identifies the
onto the GPU memory as necessary upon GPU page faults.              earliest fence after a store’s execution, which guarantees the
                                                                    visibility of the value produced by that store.
                                                                    Analysis to detect over-synchronization: The analysis on the
B. Trace Aggregation and Metadata Analysis on the CPU
                                                                    CPU starts after the kernel finishes execution, and all traces
Aggregating execution trace of memory instructions: As the          and metadata are available on the CPU. The detection logic
instrumented kernel executes, memory access traces fill the         starts by assuming all fences are over-synchronized. It then
buffer across the GPU and the CPU. ScopeAdvice employs              applies rules from Section V-C to establish the necessity of
CPU threads to move the buffer contents onto CPU memory.            each fence or else declare the fence as over-synchronized.
These threads also aggregate the trace units into a structure          Figure 10 shows the pseudo-code that identifies over-
that maps an address accessed by threads to the operation (rd,      synchronized fences. It receives the memory instruction trace,
rdSkipL1, wr, wrV, atm), scope (if applicable), WindowId, and       the memory metadata, and the list of fence identifiers. It then
the ThreadId to ease analysis. This continues till the kernel       iterates over all units of global memory addresses allocated
ends. The analysis of the trace starts after the kernel ends.       by the kernel (globalAddressesAllocated). For every address,
Implementing getPrevSync and getNextSync: The two es-               it consults the trace and the memory metadata. It checks if
sential routines — getPrevSync (getNextSync) relate dynamic         an address was written to and if it was accessed by multiple
threadblocks (line 6). If not, access to the given address does     the array is read-only, they cannot necessitate fences. However,
not necessitate a device-scoped fence (Rule 2). In line 7,          this will not be known until the CPU analysis. Thus, the traces
it iterates over the aggregated execution trace units for all       are wastefully shipped to the CPU for analysis.
instructions that have accessed the given address. Lines 8 and         ScopeAdvice addresses this problem by buffering a few
11 check for the first and second parts of Rule 1, respectively.    units of traces on the GPU, anticipating that these traces will
If any of these conditions are satisfied, then the necessity        not be needed for the analysis. During the analysis on the CPU,
of the corresponding fence is established, and the fence in         when an address necessitates fences (i.e., memory metadata
question is not over-synchronized (lines 10 and 13). It uses the    points that an address is read and written to by multiple
getPrevSync and getNextSync routines to find corresponding          threadblocks), ScopeAdvice fetches trace units corresponding
fences for a load and a store.                                      to that address for analysis. The trace units that are never
   Finally, the tool is left with FenceIds of over-synchronized     fetched by the CPU are never analyzed by the CPU.
fences (if present). It then performs a source analysis to apply       However, buffering the entire memory access trace on the
Rule 3 to find redundant fences due to neighboring barriers.        GPU requires an unbounded amount of memory. ScopeAdvice
ScopeAdvice considers only the block-scoped fences or fences        bounds the number of trace units per address (4Bytes) that
found over-synchronized in the previous step here.                  can reside on GPU. To keep a count, it uses the Count field
Actionable advice: ScopeAdvice reports over-synchronized            (Figure 8) and maintains the trace units indexed by addresses.
and redundant fences with their source file, line number, and       Count is incremented on access to an address. If Count is
the reason (i.e., Variants 1, 2, or 3). The programmer can          below a threshold (inGPUTraces), the generated trace unit is
then improve the performance of their code by removing the          held on GPU memory. Otherwise, it is shipped to the CPU.
reported over-synchronizations.                                     During CPU analysis, ScopeAdvice fetches the buffered traces
                                                                    for an address only if necessary (line 6, Figure 10).
C. Optimizations to Reduce Overheads of ScopeAdvice
   An obvious concern for any tool, even for a debugging tool       D. Use of Diverse Inputs and Fuzzer
like ScopeAdvice, is its runtime overheads. We apply three
                                                                       As in any runtime tool, ScopeAdvice’s advice is accurate
optimizations to limit its overheads.
                                                                    for the given execution trace, i.e., for an input and kernel’s grid
N-Buffering and Parallelism: ScopeAdvice employs multiple
                                                                    dimensions. While rare, a fence reported as over-synchronized
buffers and many analysis (CPU) threads to quickly empty
                                                                    for an execution trace may be correctly synchronized for a
the buffer between GPU and CPU onto the CPU’s memory to
                                                                    different execution trace. We recognize this as a false positive.
avoid stalling the kernel. The detection routine is also multi-
                                                                       ScopeAdvice runs a kernel with multiple inputs to increase
threaded. The for loop (line 3, Figure 10) iterates over all
                                                                    confidence in its advice, i.e., eliminating false positives, a
addresses accessed by the GPU threads.
                                                                    typical practice in software testing and dynamic analysis [31],
Execution sampling: While, in principle, the tool should track
                                                                    [32]. It utilizes the test inputs shipped with the libraries and ap-
every load/store to global memory, it is often unnecessary
                                                                    plications. We provide a wrapper script that runs ScopeAdvice
in practice. An instruction in the code (static instance) may
                                                                    against multiple inputs and reports an over-synchronization
have many dynamic instances (e.g., in a loop). It is natural
                                                                    only if it manifests on executions with every input.
that dynamic instances of a pair of static load/store issued
by a pair of producer and consumer threads have the same               Further, ScopeAdvice can leverage automatic input gener-
traits. If one set of dynamic instances of a pair of static loads   ation techniques, such as evolutionary programming [33] and
and stores are from threads within a threadblock, the other         fuzzing [34]–[36]. We extended and integrated a fuzzer for
instances are likely to follow suit. Thus, ScopeAdvice always       GPU programs, CLFuzz [35], with ScopeAdvice to automati-
instruments the first dynamic instance of a given (static)          cally generate inputs for CUDA kernels. CLFuzz is a coverage-
memory instruction by each thread. After that, it randomly          guided fuzzer, i.e., it generates inputs to exercise different
chooses an instance of the same instruction issued by the same      paths through programs to eliminate false positives.
thread with a probability Bound1
                                    (Bound is parameterized).          In Section VII, we show that the approach of using multiple
   ScopeAdvice needs additional metadata for this. It maintains     inputs gives accurate advice, i.e., the proposed optimization
a 1Byte counter per thread, per static instruction, to track        will not introduce bugs (e.g., data races). We stress-tested
the dynamic instances of an instruction executed by a thread.       ScopeAdvice against 25 kernels (e.g., Rodinia [37], Poly-
Each time a thread executes an instruction, its counter is          bench [38], and ScoR [16] benchmark suites) with and without
incremented. The dynamic instance is instrumented if the            over-synchronization and encountered no false positives – a
counter value equals a pre-assigned integer between 1 and           testament to its robustness. Furthermore, a cautious program-
Bound. Threads in each threadblock are assigned a random            mer can run the optimized program containing ScopeAdvice’s
number between 1 and Bound beforehand.                              suggestions against race detectors too (e.g., [15], [28], [39]).
Filtering of memory access trace: Shipping trace units and an-
                                                                    E. Discussion: Why not static analysis?
alyzing them on the CPU adds large overheads. However, most
of the trace units play little role in the analysis. For example,     It is natural to wonder if static analysis could detect over-
array elements are read once in the reduction kernel [16]. As       synchronization to avoid the drawbacks of dynamic analysis,
TABLE I: Over-synchronization reported by ScopeAdvice.                         TABLE II: Performance improvement by avoiding over-
(V: Volatile, DA: Device-scoped atomic)                                        synchronization and reduction in stall cycles.
 Application                            Actual   Reported   Type                                       Performance      Stalls due to fences
                                                                                       Application
 cuML (CU) [17]                           3          3      Variant 1 (V)                            Improvements (%)   Original     Modified
 String sort (ST) [19]                   10         10      Variant 2                      CU               37            24.8         14.5
 Compress (CP) [19]                       2          2      Variant 2                      ST               29             6.7           0
 Matrix multiplication (MM) [16]          3          3      Variant 1 (V), 3               CP                0              0            0
 Sgemm (GE) [41]                          -          1      Variant 2                     MM                54             4.4         0.13
 Unbalanced tree search (UT) [16]         2          2      Variant 1 (V)                  UT               17             2.3          0.8
 Unbalanced tree search (UT-A) [16]       2          2      Variant 1 (DA)                UT-A              11             2.2          0.7
 Stencil (SC) [42]                        2          2      Variant 3                      SC               50             2.8           0
 Reduction (RD) [16]                      0          0      -
 Hash table (HS) [43]                     0          0      -
 Parallel linear recurrence (PL) [21]     0          0      -
 Parallel merge (MG) [19]                 0          0      -                  We found that across all evaluated applications, at most two
                                                                               test inputs were enough to eliminate possible false positives.
                                                                               A. Effectiveness of ScopeAdvice
i.e., the need for multiple inputs and runtime overheads. How-
ever, we found many reasons why static analysis is inadequate:                    Table I shows the number of cases of over-synchronization
 1 index analysis techniques [40] for determining memory ac-                   in each application (manually confirmed) and that reported by
cesses fall short for kernels with complex access patterns (e.g.,              ScopeAdvice. It also shows the type of over-synchronization.
input-dependent indexing), 2 whether a load/store would                        We noticed that ScopeAdvice could find all the cases of over-
access global memory is often known only during execution                      synchronization without reporting any false positives.
and 3 a fence may or may not execute based on the input,                          We noticed that for ST, CP, SC, RD, HS, PL, and MG,
thus, known only at runtime. In these cases, static analysis                   reported cases of over-synchronization were the same across
becomes too conservative to yield helpful advice.                              all tested inputs. However, we saw false positives for CU,
                                                                               MM, UT, and UT-A for one of their inputs, particularly the
                            VII. E VALUATION                                   small input set. Smaller inputs require few threads for compu-
                                                                               tation and exhibit little interaction amongst groups of threads
   We evaluate ScopeAdvice on an NVIDIA RTX 3090 GPU,                          (threadblock). Thus, fences were deemed over-synchronized.
with applications compiled using CUDA 11.2. The system has                     However, ScopeAdvice, with its integrated fuzzer, reports
a 16-core CPU and 128GB of DRAM with NVIDIA driver                             over-synchronization only when it appears for all inputs. Thus,
v470. We use the buffer (channel) of 2MB between the GPU                       it ultimately reported no false positives.
and the CPU, 768 (=N) buffers, 12 CPU threads, Bound=15                           ScopeAdvice also reported 1 case of Variant 2 in GE [41].
(sampling), and inGPUTraces=2 (trace filtering). These values                  However, we could not validate it since the library is closed-
are parameterized with defaults chosen empirically.                            source. Many such cases likely exist in closed-source libraries
   We answer the following questions: 1 Can ScopeAdvice                        that could benefit from ScopeAdvice, too.
effectively identify over-synchronization? 2 Do applications
speed up by removing the identified over-synchronizations? 3                   B. Application Performance Improvement
What are the runtime overheads of ScopeAdvice?                                    We measure speedups achieved by modifying the kernels
   We use applications from popular libraries such as                          as per the advice provided by ScopeAdvice. We execute two
cuML [17], cudpp [19], cuBLAS [41], ScoR [16],                                 versions of each application: 1 the original version, and 2
KiloTM [43], and gpufilter [21]. Table I lists the different                   the modified version without reported over-synchronization.
variants of over-synchronization in the applications we studied.                  Table II reports the performance improvements, comparing
We modify MM to include and evaluate Variant 3 on an actual                    the modified version with the original. We omit the applica-
application. The unbalanced tree search (UT) application has                   tions that exhibit no over-synchronization. We report the mean
Variant 1 over-synchronization due to volatile variables. We                   performance improvement for each application. Following the
then modified UT to evaluate Variant 1 in the presence of                      tool’s advice, applications sped up by 0-55%.
device-scoped atomics (UT-A) since we did not find such cases                     All applications except CP observe speedups. To better
of over-synchronization in the programs we studied. Except                     understand why different applications saw vastly different
for UT-A, all applications having Variant 1 are due to the                     improvements, we use Nsight [44] profiling tool to analyze
presence of volatile variables. Note that the code for Stencil                 the GPU usage. We measure the average number of GPU stall
(SC) is not publicly available. We wrote the program using                     cycles due to fences for each application. The stall cycles indi-
the pseudo-code available in the article (Figure 11 of [42]). To               cate the number of cycles for which the execution was stalled
evaluate if ScopeAdvice reports false positives in the absence                 between issuing two consecutive warp (SIMD) instructions,
of over-synchronization, we evaluate hash table (HS) from                      on average. A higher number indicates a significant impact of
KiloTM, reduction (RD) from ScoR, parallel linear recurrence                   fence instructions on an application’s performance.
(PL) from gpufilter, and parallel merge (MG) from cudpp.                          The last two sub-columns of Table II report the average
We also evaluated cuBLAS’s sgemm (GE) to check if over-                        stall cycles due to fences in the original application and the
synchronization exists in hand-tuned, closed-source libraries.                 modified one, respectively. The stall cycles are significantly
Runtime overheads          NVBit        Naive        Para      Para+Sampling        ScopeAdvice   TABLE III: ScopeAdvice’s overheads (vs. application foot-
                10000                                                                             print) for metadata, fence, sampling and trace filter.
                    1000
     (times)

                                                                                                       Applications   Metadata      Fence    Sampling   Trace filter
                    100                                                                                    CU            1           15.9     144.06        2
                                                                                                           ST            1          0.005       0.36        2
                     10
                                                                                                           CP            1       0.0000002    0.00007       2
                      1                                                                                   MM             1          0.002       0.06        2
                           CU      ST   CP      MM    UT    UT-A   SC   RD     HS     PL   MG              UT            1          0.009       1.12        2
                                                                                                          UT-A           1          0.009       1.12        2
Fig. 11: Overheads of ScopeAdvice with each optimization                                                   SC            1           0.06     1.0002        2
                                                                                                           RD            1        0.00001      0.006        2
compared to baseline (no instrumentation).                                                                 HS            1        0.00007      0.002        2
                                                                                                           PL            1          0.002       0.61        2
                                                                                                          MG             1        0.000002     0.001        2

reduced in the modified versions for all applications except
for CP. Since CP barely stalled due to fences to begin with,
it is apparent why it did not witness any speedup.                                                    The bar labeled Para+Sampling represents the runtime with
                                                                                                  the optimizations from Para and execution sampling. Sam-
                                                                                                  pling reduces the amount of trace communicated to the CPU.
C. Runtime Overheads of ScopeAdvice
                                                                                                  Applications with high lock contention and memory operations
   ScopeAdvice is a (performance) debugging tool. Like any                                        in loops benefit the most from sampling. In particular, the
debugging tool, it adds runtime and memory overheads. The                                         number of times GPU-CPU communication occurred reduced
runtime overheads of ScopeAdvice stem from: 1 instrument-                                         by at least 90% for MM and UT compared to Para, lowering
ing the kernel, 2 executing the instrumented kernel on the                                        their overheads to 162× and 113×, respectively.
GPU, 3 communicating the trace from the GPU to the CPU                                                The bar labeled ScopeAdvice represents the runtime with
and 4 the time needed on the CPU to analyze the trace.                                            all the optimizations from Section VI-C. This further benefits
   Figure 11 shows the tool’s runtime overheads and the                                           from filtering memory access traces. The applications PL, ST,
importance of the optimizations in reducing the overheads                                         and RD benefited the most (improved by 6.8×, 2.2×, and
(lower is better). Each application has four bars, and each bar                                   ×1.5 over Para+Sampling). They had many sparsely accessed
adds an optimization to the ones present in the bars to its left.                                 addresses that did not need fences. ScopeAdvice did not ship
We normalized the height of each bar to the runtime of the                                        these trace units to CPU, reducing overheads by at least 35%.
application executing without our tool. Notice the logarithmic                                    Finally, the tool’s overheads stand at 146× for CU, 431× for
y-axis. Each bar also has an NVBit component (bottom half).                                       ST, 29× for CP, 158× for MM, 116× for UT, 111× for UT-
This component quantifies the contribution of the NVBit                                           A, 65× for SC, 499× for RD, 137× for HS, 522× for PL,
instrumentation library to the overheads. This component is                                       and 318× for MG. The overheads include the time spent on
the same across configurations for each application since                                         the CPU to analyze the traces. The detection logic on the CPU
the amount of instrumentation remains similar irrespective of                                     accounts for up to 25% of the overheads (highest for ST).
optimizations. NVBit is the key contributor to the runtime                                            While these overheads may look large, we put them in the
overheads (≈64% of end-to-end execution latency on average).                                      context of other GPU dynamic analysis tools. A recent race
This component is orthogonal to ScopeAdvice, and any future                                       detector, iGUARD [15], had overheads between 27× (CP) to
improvements in NVBit can reduce our tool’s overheads.                                            649× (PL). Another data race detector, BARRACUDA [28],
   The leftmost bar in each cluster, labeled Naive, represents                                    reported runtime overheads of up to ≈ 3700×. Note that the
the runtime without any optimization. It uses one buffer to                                       goals of those tools are orthogonal to ScopeAdvice.
communicate the traces from the GPU to the CPU and one                                            Memory overheads: ScopeAdvice maintains 4Bytes of meta-
CPU thread for analysis. Here, the CPU thread often fails                                         data for every 4Bytes of application memory on the GPU,
to empty the buffer quickly enough, stalling the GPU kernel.                                      adding a 1× memory overhead. The memory required (in
Programs that generate many memory accesses and, thus, trace                                      bits) to maintain the fence execution trace is the product
units in a short duration fill the buffer quickly, stalling the                                   of the number of GPU threads and the number of unique
kernel often. The application RD suffered the most due to                                         fences in the code. The memory overhead for the sampling
these stalls, witnessing an overhead of 12611×.                                                   optimization is the product of the number of GPU threads
   The bar labeled Para uses 12 analysis threads and 768                                          and the number of instrumented instructions. ScopeAdvice
buffers for GPU-CPU communication. UT benefits the most                                           also maintains 2 (#inGPUTraces) * 4Bytes of memory for
from parallelism — its overheads dropped from 10349× to                                           every 4Bytes of application memory on the GPU for the trace
570×. However, CU is an exception here. In CU, only a leader                                      filtering optimization, adding a 2× overhead.
thread among 256 threads of a threadblock writes the results                                          Table III shows the memory overheads for each applica-
of the threadblock’s computation to the memory. Since the rate                                    tion for memory metadata, fence execution trace, execution
of accessing memory is relatively low, CU does not stall due                                      sampling, and trace filtering. Overheads are calculated by
to full buffers. It sees a slight increase in runtime due to the                                  normalizing the memory needed over the application’s mem-
synchronization overheads among the CPU analysis threads.                                         ory footprint. Typically, the trace filtering optimization is the
major contributor in most applications (2×). However, for          Fellowship Scheme for Doctoral Research, co-sponsored by
CU, the fence execution trace and sampling metadata add            the Confederation of Indian Industry, the Government of India,
large overheads. In CU, many threads perform loads and             and Microsoft Research India. This work is partially supported
stores on a small memory region (0.49MB here). As many             by generous research grants from VMware Inc., AMD Inc.,
as 32678 threadblocks were launched with 256 threads each,         and Google faculty awards.
with nine memory instructions and eight fence instructions.
Though the absolute count of threads is large, all the threads                                A PPENDIX
cannot reside on the GPU simultaneously due to hardware            A. Abstract
constraints. The peak occupancy for CU is 492 threadblocks            We provide the source code and setup of ScopeAdvice, our
at any point in time. Therefore, the amount of memory that         tool to identify over-synchronization. We include pre-compiled
must be resident on the GPU for fence execution trace is 492       application binaries used in the evaluation. These binaries
* 256 * 8 bits (≈0.12MB). Similarly, the amount required           support NVIDIA Volta, Turing and Ampere-based GPUs. We
for sampling metadata is 492 * 256 * 9 Bytes (≈1.1MB).             include scripts to run and parse metrics used to generate the
Thus, the memory required is relatively small (2.48× of            results in Section VII, i.e., Tables I, II, III, and Figure 11.
the application footprint). Using UVM to allocate metadata
ensures that only the required amount is present on the GPU.       B. Artifact check-list (meta-information)
                                                                     • Compilation: CUDA 11.2, GCC 9.4.0, Ubuntu 20.04.
                   VIII. R ELATED W ORK                              • Binary: Included for x86-64 host and NVIDIA Volta, Turing
                                                                       and Ampere GPUs.
Performance of GPU programs: Nsight [44] highlights pro-
                                                                     • Run-time environment: Applications can be run on the bare
gram inefficiencies with profiling but provides minimal action-        machine. To run the applications, CUDA 11.2 is required on a
able advice to eliminate them. Recent studies overcome this            Linux installation of Ubuntu 20.04.
limitation by developing tools that provide hints to improve         • Hardware: An x86-64 machine with at least 16 cores (prefer-

program performance by eliminating value redundancies [45],            ably AMD Ryzen 9 5950X) and 64GB of DDR4 DRAM,
                                                                       NVIDIA Ampere-based GPU (perferably RTX 3090), and PCIe
[46], memory-use inefficiencies [47], GPU stall cycles [48]–
                                                                       4.0 interconnect.
[50], and redundant barriers [33]. These optimizations are           • Execution: The binaries are executed on a system with an
complementary to ScopeAdvice. Orr et al. [51] proposed                 attached NVIDIA GPU. Scripts are provided for the same.
remote scope promotion to dynamically ‘promote’ scopes               • Metrics: Performance improvement, memory overhead, and

depending on the access-performing thread using modified               runtime overhead.
                                                                     • Output: We provide tabular data for every result in Section VII.
hardware. ScopeAdvice is a software-only approach.
                                                                       Raw numbers for each experiment are present in the application
Correctness of GPU programs: Race detection for GPU                    folder corresponding to the experiment.
programs has been extensively studied in recent literature [15],     • How much disk space required (approximately)?: At least
[16], [28], [33], [39], [52]–[55]. These works aim to find             2GB.
                                                                     • How much time is needed to complete experiments (approx-
bugs, which are cases of under-synchronization. ScopeAdvice
                                                                       imately)?: 14 hours.
performs an orthogonal task, i.e., finding over-synchronization.     • Publicly available?: Yes
Synccheck [56] aims to find incorrect use of barriers in             • Archived (provide DOI)?:          Yes, https://doi.org/10.5281/
CUDA programs, whereas ScopeAdvice aims to find ineffi-                zenodo.13743937
cient choices of fence usage. Recent studies have aimed at
finding memory-safety violations in GPU programs [57]–[59].        C. Description
These bugs are orthogonal to the aim of ScopeAdvice.                  The artifact contains the source of ScopeAdvice along with
                                                                   the pre-compiled binaries of evaluated applications. It allows
                      IX. C ONCLUSION                              to reproduce the following results:
   We show how CUDA programs do not fully harness scoped           • Table I: Cases of over-synchronization reported by Sco-
synchronization which leads to over-synchronization. We de-        peAdvice (Key result I).
scribed three different variations of over-synchronization and     • Table II: Performance improvement observed by eliminating
measured their impact on performance. We built ScopeAdvice,        over-synchronization (Key result II).
a performance debugging tool that detects cases of over-           • Figure 11: Runtime overheads of using ScopeAdvice.
synchronization in CUDA programs. Following the tool’s             • Table III: Memory overheads of using ScopeAdvice.
advice, programs sped up by up to 55%.                                1) How to access: The artifact is made available at https:
                                                                   //github.com/csl-iisc/ScopeAdvice-MICRO24.git and https://
                    ACKNOWLEDGMENT                                 doi.org/10.5281/zenodo.13743937.
   We thank Ashish Panwar and Shweta Pandey for their                 2) Hardware dependencies: To evaluate ScopeAdvice, we
feedback. We thank Kingshuk Majumder for discussions on            recommend the following hardware configuration: 1 NVIDIA
compiler-based program analysis techniques. We gratefully          Ampere-based GPU (preferably RTX 3090), 2 x86-64 CPU
acknowledge the Google Travel Award and IISc’s Sarukkai            with at least 16 cores (preferably AMD Ryzen 9 5950X), 3
Jagannathan Travel Award for enabling our participation in         PCIe 4.0×16 interconnect between the CPU and the GPU (for
MICRO 2024. Ajay is supported by the Prime Minister’s              Unified Virtual Memory).
   3) Software dependencies: The artifact needs CUDA-11.2             be compared against tables and figures reported in the paper.
on a bare machine with Ubuntu 20.04. To install CUDA 11.2             The generated results can be viewed in the following paths:
on a bare machine, follow the instructions from NVIDIA.                $ vi table-1-and-3/table-1-result.csv
The artifact needs Docker installation to run our containerized        $ vi table-1-and-3/table-3-result.csv
setup. To install Docker on an Ubuntu machine, use:                    $ vi figure/result.csv
                                                                       $ vi table-2/result.csv
$ sudo apt install docker.io
                                                                         The results of runtime overheads (Figure 11) can vary across
  4) Data sets: Data generators are provided for few appli-
                                                                      runs (due to a comparatively small baseline). The results
cations with large dataset requirement (e.g., RD).
                                                                      will be within error margin (check get-error.py). The
D. Installation                                                       trends of different optimization levels will remain the same
  The artifact can be downloaded and accessed as:                     ir-respective of this variance.
$ git clone \                                                         G. Notes
https://github.com/csl-iisc/ScopeAdvice-MICRO24.git
$ cd ScopeAdvice-MICRO24                                                 If running on a non-docker setup, ensure that fetching
                                                                      GPU performance counters is enabled for all the users before
E. Experiment workflow                                                running the scripts used to reproduce results from Table II.
                                                                      NVIDIA Nsight requires sudo privilege to fetch GPU perfor-
   The outermost directory of the artifact consists of three
                                                                      mance counters. This can be disabled with modifications to
folders corresponding to the results in Section VII: table-1-
                                                                      linux configuration files [60].
and-3, table-2, and figure. table-1-and-3/ consists of all the
                                                                         The pre-compiled binaries support NVIDIA Volta, Turing
application binaries and corresponding processing scripts to
                                                                      and Ampere-based GPUs. On all the GPUs, observations in
generate the results for Tables I and III. table-2/ consists of all
                                                                      Table I remain the same. However, other results (namely
the application binaries and corresponding processing scripts
                                                                      Table II) will change based on the GPU.
to generate the results for Table II. figure/ consists of all
the application binaries and processing scripts to generate the                                     R EFERENCES
results for Figure 11. Each folder has a script (run.sh) that
                                                                       [1] Google, “Cloud gpus,” 2019. [Online]. Available: https://cloud.google.
generates the results for the corresponding experiment.                    com/gpu/
   The experiments can be run within a docker container                [2] Amazon, “P3 instances with v100,” 2020. [Online]. Available:
(advised) or a bare machine. Check README for further steps                https://aws.amazon.com/ec2/instance-types/p3/
                                                                       [3] NVIDIA, “Gpus everywhere,” 2019. [Online]. Available: https:
to setup docker environment for enabling GPU usage. To run                 //blogs.nvidia.com/blog/2017/05/08/microsoft-azure-gpu-instances/
experiments within the container, first build the container in         [4] SHI,        “Nvidia       a100       80gb      pcie      gpu,”     2021.
the outermost directory as follows:                                        [Online].        Available:       https://www.shi.com/product/41094090/
                                                                           NVIDIA-Tesla-A100-GPU-computing-processor
$ docker build . -t sa:v1                                              [5] SHI, “Nvidia dgx station a100,” 2021. [Online]. Available: https:
                                                                           //www.shi.com/product/41820041/NVIDIA-DGX-Station-A100
   Run the docker container in interactive mode using the              [6] J. Dean, “Google ai chief jeff dean interview: Machine learning trends
following command. This command opens a bash shell in the                  in 2020,” 2019. [Online]. Available: https://venturebeat.com/2019/12/13/
                                                                           google-ai-chief-jeff-dean-interview-machine-learning-trends-in-2020//
docker image.                                                          [7] W. Xiao, S. Ren, Y. Li, Y. Zhang, P. Hou, Z. Li, Y. Feng, W. Lin,
$ docker container run -it \                                               and Y. Jia, “AntMan: Dynamic scaling on GPU clusters for deep
--runtime=nvidia --gpus all sa:v1 bash                                     learning,” in 14th USENIX Symposium on Operating Systems Design
                                                                           and Implementation (OSDI 20), 2020.
                                                                       [8] M. Jeon, S. Venkataraman, A. Phanishayee, J. Qian, W. Xiao, and
   To run all the experiments we provide a single script                   F. Yang, “Analysis of Large-Scale Multi-Tenant GPU clusters for DNN
(run.sh) in the outermost directory of the artifact. This script           training workloads,” in 2019 USENIX Annual Technical Conference
executes the run scripts in the folders mentioned above. This              (USENIX ATC 19), 2019.
                                                                       [9] M. Zhao, N. Agarwal, A. Basant, B. Gedik, S. Pan, M. Ozdal, R. Komu-
script must be run as follows:                                             ravelli, J. Pan, T. Bao, H. Lu, S. Narayanan, J. Langman, K. Wilfong,
$ ./run.sh                                                                 H. Rastogi, C.-J. Wu, C. Kozyrakis, and P. Pol, “Understanding data
                                                                           storage and ingestion for large-scale deep recommendation model train-
                                                                           ing: Industrial product,” in Proceedings of the 49th Annual International
  Individual experiments can be run from the corresponding                 Symposium on Computer Architecture, 2022.
experiments folder as follows:                                        [10] Z. Cai, Q. Zhou, X. Yan, D. Zheng, X. Song, C. Zheng, J. Cheng,
                                                                           and G. Karypis, “Dsp: Efficient gnn training with multiple gpus,”
$ cd table-1-and-3;./run.sh;cd ..                                          in Proceedings of the 28th ACM SIGPLAN Annual Symposium on
$ cd figure;./run.sh;cd ..                                                 Principles and Practice of Parallel Programming, 2023.
$ cd table-2;./run.sh;cd ..                                           [11] Y. Gao, Y. He, X. Li, B. Zhao, H. Lin, Y. Liang, J. Zhong, H. Zhang,
                                                                           J. Wang, Y. Zeng, K. Gui, J. Tong, and M. Yang, “An empirical study
                                                                           on low gpu utilization of deep learning jobs,” in 2024 IEEE/ACM
F. Evaluation and expected results                                         46th International Conference on Software Engineering (ICSE). Los
  Observations for each experiment in Section VII is printed               Alamitos, CA, USA: IEEE Computer Society, apr 2024, pp. 880–880.
                                                                           [Online]. Available: https://doi.ieeecomputersociety.org/
on the terminal and a comma separated file is generated in the        [12] NVIDIA, “Parallel thread execution isa,” 2021. [Online]. Available:
corresponding experiments folder. The generated results can                https://docs.nvidia.com/cuda/parallel-thread-execution/
[13] J. Sanders and E. Kandrot, “Cuda by example: Errata page,” 2021. [On-        [36] W. Li, Z. Chen, X. He, G. Duan, J. Sun, and H. Chen, “Cvfuzz: Detecting
     line]. Available: https://developer.nvidia.com/cuda-example-errata-page           complexity vulnerabilities in opencl kernels via automated pathological
[14] C. Flanagan and S. N. Freund, “Fasttrack: Efficient and precise dynamic           input generation,” Future Generation Computer Systems, 2022.
     race detection,” in Proceedings of the 30th ACM SIGPLAN Conference           [37] S. Che, M. Boyer, J. Meng, D. Tarjan, J. W. Sheaffer, S.-H. Lee, and
     on Programming Language Design and Implementation, 2009.                          K. Skadron, “Rodinia: A benchmark suite for heterogeneous computing,”
[15] A. K. Kamath and A. Basu, “Iguard: In-gpu advanced race detection,” in            in 2009 IEEE International Symposium on Workload Characterization
     Proceedings of the ACM SIGOPS 28th Symposium on Operating Systems                 (IISWC), 2009.
     Principles, 2021.                                                            [38] S. Grauer-Gray, L. Xu, R. Searles, S. Ayalasomayajula, and J. Cavazos,
[16] A. K. Kamath, A. A. George, and A. Basu, “Scord: A scoped race                    “Auto-tuning a high-level language targeted to gpu codes,” in 2012
     detector for gpus,” in Proceedings of the ACM/IEEE 47th Annual                    Innovative Parallel Computing (InPar), 2012.
     International Symposium on Computer Architecture, 2020.                      [39] Y. Peng, V. Grover, and J. Devietti, “Curd: A dynamic cuda race
[17] S. Raschka, J. Patterson, and C. Nolet, “Machine learning in python:              detector,” in Proceedings of the 39th ACM SIGPLAN Conference on
     Main developments and technology trends in data science, machine                  Programming Language Design and Implementation, 2018.
     learning, and artificial intelligence,” arXiv preprint arXiv:2002.04803,     [40] R. Alur, J. Devietti, O. S. Navarro Leija, and N. Singhania, “Gpudrano:
     2020.                                                                             Detecting uncoalesced accesses in gpu programs,” in Computer Aided
[18] NVIDIA, “Cuda c++ programming guide,” 2022. [Online]. Available:                  Verification, 2017.
     https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html             [41] NVIDIA, “Cuda samples,” 2021. [Online]. Available: https://github.
[19] cudpp, “Cuda data parallel primitives library,” 2021. [Online]. Available:        com/NVIDIA/cuda-samples
     https://github.com/cudpp/cudpp                                               [42] S. Dey, M. Mukul, P. Sharma, and S. Biswas, “Predictive data
[20] NVIDIA, “Basic linear algebra on nvidia gpus,” 2022. [Online].                    race detection for gpus,” CoRR, 2021. [Online]. Available: https:
     Available: https://developer.nvidia.com/cublas                                    //arxiv.org/abs/2111.12478
[21] D. Nehab, A. Maximo, R. S. Lima, and H. Hoppe, “gpufilter -                  [43] W. W. L. Fung, I. Singh, A. Brownsword, and T. M. Aamodt, “Hardware
     gpu recursive filtering,” 2016. [Online]. Available: https://github.com/          transactional memory for gpu architectures,” in 2011 44th Annual
     andmax/gpufilter                                                                  IEEE/ACM International Symposium on Microarchitecture (MICRO),
[22] D. Lustig, S. Sahasrabuddhe, and O. Giroux, “A formal analysis of                 2011.
     the nvidia ptx memory consistency model,” in Proceedings of the              [44] NVIDIA, “Nsight compute - an interactive kernel profiler for cuda
     Twenty-Fourth International Conference on Architectural Support for               applications,” 2021. [Online]. Available: https://developer.nvidia.com/
     Programming Languages and Operating Systems, 2019.                                nsight-compute
[23] O. Villa, M. Stephenson, D. Nellans, and S. W. Keckler, “Nvbit: A            [45] K. Zhou, Y. Hao, J. Mellor-Crummey, X. Meng, and X. Liu, “Val-
     dynamic binary instrumentation framework for nvidia gpus,” in Pro-                ueexpert: Exploring value patterns in gpu-accelerated applications,” in
     ceedings of the 52nd Annual IEEE/ACM International Symposium on                   Proceedings of the 27th ACM International Conference on Architectural
     Microarchitecture, 2019.                                                          Support for Programming Languages and Operating Systems, 2022.
[24] M. D. Sinclair, J. Alsop, and S. V. Adve, “Efficient gpu synchronization     [46] K. Zhou, Y. Hao, J. Mellor-Crummey, X. Meng, and X. Liu, “Gvprof: A
     without scopes: Saying no to complex consistency models,” in Proceed-             value profiler for gpu-based clusters,” in Proceedings of the International
     ings of the 48th International Symposium on Microarchitecture, 2015.              Conference for High Performance Computing, Networking, Storage and
[25] J. Alsop, M. S. Orr, B. M. Beckmann, and D. A. Wood, “Lazy release                Analysis, 2020.
     consistency for gpus,” in 2016 49th Annual IEEE/ACM International            [47] M. Lin, K. Zhou, and P. Su, “Drgpum: Guiding memory optimization
     Symposium on Microarchitecture (MICRO), 2016.                                     for gpu-accelerated applications,” in Proceedings of the 28th ACM
[26] J. Alglave, M. Batty, A. F. Donaldson, G. Gopalakrishnan, J. Ketema,              International Conference on Architectural Support for Programming
     D. Poetzl, T. Sorensen, and J. Wickerson, “Gpu concurrency: Weak be-              Languages and Operating Systems, Volume 3, 2023.
     haviours and programming assumptions,” in Proceedings of the Twenti-         [48] K. Zhou, X. Meng, R. Sai, and J. Mellor-Crummey, “Gpa: A gpu
     eth International Conference on Architectural Support for Programming             performance advisor based on instruction sampling,” in 2021 IEEE/ACM
     Languages and Operating Systems, 2015.                                            International Symposium on Code Generation and Optimization (CGO),
[27] J. Sanders and E. Kandrot, “Cuda by example: An introduction                      2021.
     to general-purpose gpu programming,” 2021. [Online]. Available:              [49] D. Shen, S. L. Song, A. Li, and X. Liu, “Cudaadvisor: Llvm-based
     https://developer.nvidia.com/cuda-example                                         runtime profiling for modern gpus,” in Proceedings of the 2018 Inter-
[28] A. Eizenberg, Y. Peng, T. Pigli, W. Mansky, and J. Devietti, “Barracuda:          national Symposium on Code Generation and Optimization, 2018.
     Binary-level analysis of runtime races in cuda programs,” in Proceedings     [50] L. Braun and H. Fröning, “Cuda flux: A lightweight instruction profiler
     of the 38th ACM SIGPLAN Conference on Programming Language                        for cuda applications,” in 2019 IEEE/ACM Performance Modeling,
     Design and Implementation, 2017.                                                  Benchmarking and Simulation of High Performance Computer Systems
[29] S. Biswas, M. Zhang, M. D. Bond, and B. Lucia, “Valor: Efficient,                 (PMBS), 2019.
     software-only region conflict exceptions,” in Proceedings of the 2015        [51] M. S. Orr, S. Che, A. Yilmazer, B. M. Beckmann, M. D. Hill, and D. A.
     ACM SIGPLAN International Conference on Object-Oriented Program-                  Wood, “Synchronization using remote-scope promotion,” in Proceedings
     ming, Systems, Languages, and Applications, 2015.                                 of the Twentieth International Conference on Architectural Support for
[30] NVIDIA, “Unified memory for cuda beginners,” 2017. [Online].                      Programming Languages and Operating Systems, 2015.
     Available: https://devblogs.nvidia.com/unified-memory-cuda-beginners/        [52] NVIDIA,         “Racecheck       tool,”     2021.      [Online].     Avail-
[31] M. Zalewski, “American fuzzy lop - whitepaper,” 2016. [Online].                   able: https://docs.nvidia.com/compute-sanitizer/ComputeSanitizer/index.
     Available: https://lcamtuf.coredump.cx/afl/technical details.txt                  html#racecheck-tool
[32] A. Fioraldi, D. Maier, H. Eißfeldt, and M. Heuse, “AFL++: Combining          [53] M. Zheng, V. T. Ravi, F. Qin, and G. Agrawal, “Grace: A low-overhead
     incremental steps of fuzzing research,” in 14th USENIX Workshop on                mechanism for detecting data races in gpu programs,” in Proceedings
     Offensive Technologies (WOOT 20), 2020.                                           of the 16th ACM Symposium on Principles and Practice of Parallel
[33] M. Wu, Y. Ouyang, H. Zhou, L. Zhang, C. Liu, and Y. Zhang, “Simulee:              Programming, 2011.
     Detecting cuda synchronization bugs via memory-access modeling,”             [54] A. Betts, N. Chong, A. Donaldson, S. Qadeer, and P. Thomson, “Gpuver-
     in Proceedings of the ACM/IEEE 42nd International Conference on                   ify: A verifier for gpu kernels,” in Proceedings of the ACM International
     Software Engineering, 2020.                                                       Conference on Object Oriented Programming Systems Languages and
[34] H. Chen, S. Guo, Y. Xue, Y. Sui, C. Zhang, Y. Li, H. Wang, and Y. Liu,            Applications, 2012.
     “MUZZ: Thread-aware grey-box fuzzing for effective bug hunting in            [55] G. Li, P. Li, G. Sawaya, G. Gopalakrishnan, I. Ghosh, and S. P.
     multithreaded programs,” in 29th USENIX Security Symposium (USENIX                Rajan, “Gklee: Concolic verification and test generation for gpus,” in
     Security 20), 2020.                                                               Proceedings of the 17th ACM SIGPLAN Symposium on Principles and
[35] C. Peng and A. Rajan, “Automated test generation for opencl kernels               Practice of Parallel Programming, 2012.
     using fuzzing and constraint solving,” in Proceedings of the 13th Annual     [56] NVIDIA,         “Synccheck       tool,”     2021.      [Online].     Avail-
     Workshop on General Purpose Processing Using Graphics Processing                  able: https://docs.nvidia.com/compute-sanitizer/ComputeSanitizer/index.
     Unit, 2020.                                                                       html#synccheck-tool
[57] M. Tarek Ibn Ziad, S. Damani, A. Jaleel, S. W. Keckler, and M. Stephen-   [59] B. Di, J. Sun, D. Li, H. Chen, and Z. Quan, “Gmod: A dynamic gpu
     son, “Cucatch: A debugging tool for efficiently catching memory safety         memory overflow detector,” in Proceedings of the 27th International
     violations in cuda applications,” Proc. ACM Program. Lang., 2023.              Conference on Parallel Architectures and Compilation Techniques, 2018.
[58] C. Erb, M. Collins, and J. L. Greathouse, “Dynamic buffer overflow        [60] NVIDIA, “Nvidia permission issue with performance coun-
     detection for gpgpus,” in 2017 IEEE/ACM International Symposium on             ters,” 2018. [Online]. Available: https://developer.nvidia.com/ERR
     Code Generation and Optimization (CGO), 2017.                                  NVGPUCTRPERM
