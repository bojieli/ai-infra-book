  ICED: An Integrated CGRA Framework Enabling
            DVFS-Aware Acceleration
       Cheng Tan†§ , Miaomiao Jiang¶§ , Deepak Patil§ , Yanghui Ou∥ , Zhaoying Li‡ , Lei Ju¶ , Tulika Mitra‡ ,
                                Hyunchul Park† , Antonino Tumeo∗ , Jeff Zhang§

                    † Google, § Arizona State University, ¶ Shandong University, ∥ Cornell University,
                      ‡ National University of Singapore, ∗ Pacific Northwest National Laboratory
              † {chengtan, parkhc}@google.com, § {chengtan, mjiang58, dppatil3, jeffzhang}@asu.edu,
         ¶ {miaomiaojiang, julei}@sdu.edu.cn, ∥ yo96@cornell.edu, ‡ {zhaoying, tulika}@comp.nus.edu.sg,
                                                   ∗ antonino.tumeo@pnnl.gov




   Abstract—Coarse-grained reconfigurable arrays (CGRAs) are         achieving the theoretical peak performance provided by the
a promising solution to enable energy-efficient acceleration of      hardware resources. A widely used approach maps the data-
applications from different domains. By leveraging reconfigu-        flow graph (DFG) of a kernel (typically a performance critical
ration at the functional level, they can adapt to significantly
different computational patterns. However, the relationships of      loop nest of the application) on the time-extended Modulo
voltage and frequency with the utilization of CGRA resources         Routing Resource Graph (MRRG) [15], [16], [22] of the
and the dynamic management of them are not well explored,            spatial-temporal CGRA, trying to minimize the initiation inter-
leading to inefficient designs.                                      val (II), i.e., the delay in clock cycles needed to launch a new
   CGRAs have also been successful in accelerating data-             loop iteration. However, mismatches between the size of the
dependent streaming applications. However, in these applications,
the execution time of each kernel in the pipeline might dynam-       kernels and available computing and communication resources
ically vary depending on the characteristics of the input. This      could lead to under-utilization of the reconfigurable substrate.
also leads to under-utilization of resources for the dynamically     For example, in the simple case of a small kernel with only
changing kernels that do not limit the application throughput.       a few DFG nodes (e.g, tens) and a large CGRA with many
DVFS can also improve energy efficiency for these applications       homogeneous tiles (e.g., hundreds), only the tiles allocated to
by dynamically changing the voltage and frequency levels of tiles
that host non-performance-constraining kernels.                      the kernel are utilized, while the others remain idle [35]. In a
   This paper proposes ICED – an integrated DVFS-aware frame-        more complex case, loop-carried dependencies can complicate
work to map applications on CGRAs that support power islands.        the mapping and lead to imbalanced tile utilization. In fact,
ICED proposes a CGRA architecture supporting DVFS islands            even if all tiles are mapped to a DFG node, the resources
at varying granularity (from a single tile to a group of tiles)      can still be under-utilized due to a high II. This can happen
and the related DVFS-aware compilation and mapping toolchain.
ICED is the first work that introduces DVFS support for              if the DFG has a long recurrence cycle due to loop-carried
spatio-temporal CGRAs at power-island levels. The experimental       dependencies, thus requiring a large MRRG for the mapping,
evaluation shows that ICED improves average utilization by 2.3×      which can be viewed as replicating resources of the CGRA for
and energy-efficiency by 1.32× over a conventional CGRA. With        II times. In both cases, the end result is that energy is wasted
streaming applications, ICED can achieve up to 1.26× energy-         on tiles with low or no utilization. One possible solution to
efficiency compared with a state-of-the-art CGRA that introduces
partial dynamic reconfiguration to adapt to variations in kernels’   improve energy efficiency and utilization is to apply dynamic
throughput.                                                          voltage and frequency scaling (DVFS) to each tile, or subsets
                                                                     of tiles (islands).
                      I. I NTRODUCTION                                  Previous works [19], [25], [29] also show that data-
   Coarse-grained reconfigurable arrays (CGRAs) are acceler-         dependent streaming applications composed of multiple ker-
ators composed of tiles, containing one or more functional           nels can lead to under-utilization of resources. In fact, in these
units (FU), interconnected through an on-chip network. They          applications, the execution time of a kernel can vary with the
usually are loosely coupled to a general-purpose central pro-        input data set, leading to an imbalanced pipeline of kernels
cessing unit and enable the acceleration of a variety of com-        where the bottleneck can dynamically change. If the resource
putational patterns through their reconfigurability [21]. They       allocation for each kernel is fixed, the kernels that currently
have been shown to provide energy-efficient acceleration of          are not a bottleneck (meaning that they could execute faster
applications from different domains, including machine learn-        if not limited by other ones) will under-utilize their resources
ing (ML) [17], [38], high-performance computing (HPC) [9],           and have tiles with long idle times.
and embedded systems [15].                                              In this paper, we take the first steps towards solving all
   With all reconfigurable devices and, in particular, with          these challenges by introducing ICED — an Integrated CGRA
CGRAs, the compiler infrastructures play a critical role in          framework Enabling DVFS-aware acceleration. The main con-
