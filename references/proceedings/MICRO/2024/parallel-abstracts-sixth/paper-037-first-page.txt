                         2024 57th IEEE/ACM International Symposium on Microarchitecture (MICRO)



                            Timely, Efficient, and Accurate
                               Branch Precomputation
               Aniket Deshmukh                          Lingzhe(Chester) Cai                           Yale N. Patt
           University of Texas at Austin              University of Texas at Austin            University of Texas at Austin
                 United States                              United States                            United States
            a.deshmukh@utexas.edu                       chestercai@utexas.edu                     patt@ece.utexas.edu



   Abstract—Out-of-order cores rely on high-accuracy branch            predicted is hard to meet in many applications. Prior work
predictors to supply useful instructions to the processor backend.     therefore focuses on specific branches with fixed control flow
However, there remains a large fraction of mispredictions caused       properties to improve their timeliness. This reduces mispre-
by hard-to-predict (H2P) branches that modern predictors have
been unable to improve. Precomputation is an alternative to            diction coverage as many H2P branches are left out.
prediction that speculatively executes the dependence chain of            Precomputation is often inaccurate, i.e. precomputation
a branch earlier to override the branch predictor at Fetch             overrides correct branch predictions, causing additional mis-
time. However, prior work sacrifices H2P branch coverage and           predictions. Prior work thus only targets H2P branches with
precomputation accuracy to produce timely results.
   Our work relaxes this timeliness constraint by using precom-        simple or specific types of dependence chains to maintain
putation results to issue early misprediction flushes instead of       high accuracy. This further decreases misprediction coverage.
overriding the branch predictor. This allows us to construct a         Heavy-weight helper threads generated using compiler-based
highly accurate precomputation thread with good misprediction          techniques are more accurate but perform poorly as they
coverage, without sacrificing timeliness. The thread is efficient as   compromise on timeliness.
it utilizes on-core execution resources and re-uses existing hard-
ware for issuing early flushes. Using our Timely, Efficient, and          Finally, precomputation is inefficient as it requires a
Accurate thread for precomputation yields a 10.1% improvement          separate core or execution engine to provide timely results and
in performance over an aggressive baseline OoO core.                   to avoid wasting on-core resources when the precomputation
   Index Terms—Branch Prediction, Precomputation                       is late or incorrect.
                       I. I NTRODUCTION                                   We argue that these limitations can be overcome if pre-
                                                                       computation results that arrive ”late”, i.e. after the branch is
   Branch mispredictions still present a major barrier to single-
                                                                       fetched but before it is executed, can still be used to correct
thread performance in processors today. Despite years of
                                                                       the instruction stream. Relaxing this constraint allows us to
research, there remains a portion of branches that cannot be
                                                                       trace longer dependence chains across complex control flows
easily predicted by modern high-accuracy branch predictors
                                                                       that are initiated much earlier than prior work, thus optimiz-
such as TAGE-SC-L [23] and Perceptron [15], even with
                                                                       ing for misprediction coverage and accuracy without hurting
significant hardware investment. These are commonly referred
                                                                       timeliness. Our work presents a mechanism for constructing a
to as hard-to-predict (H2P) branches. They comprise data-
                                                                       more Timely, Efficient, and Accurate (”TEA”) thread that
dependent branches [12], [21] and branches with complex
                                                                       uses on-core hardware to trigger early mispredictions flushes
control flow patterns [29] that are hard to identify at runtime.
                                                                       via precomputation instead of overriding the branch predictor
   Prior work has looked into developing alternative solutions
                                                                       at fetch time. Our contributions can be summarized as follows:
for these branches. Precomputation is one such approach that
identifies instructions in the dependence chains leading up to           • We describe a method to generate a lightweight yet highly
H2P branches to create a standalone, speculatively executed                accurate dynamic precomputation threads (99.3%) with
thread. This thread begins execution early enough to override              high misprediction coverage (76%).
the branch predictor at Fetch time.                                      • Our precomputation thread contains synchronized times-
   Early precomputation approaches used static analysis and                tamps for H2P branches that can leverage existing flush
profiling to construct ”helper threads” [8], [22], [27],                   mechanisms. This greatly simplifies the hardware re-
[30] for frequently mispredicting branches. More recent ap-                quired for triggering early misprediction flushes and
proaches [13], [21], [26] generate a lightweight instruction               enables both direction and target precomputation.
stream or ”precomputation thread” by detecting H2P branches              • We show that using on-core resources with accurate pre-
and analyzing their dependencies at runtime. Unfortunately,                computation threads provides performance comparable
prior work faces several limitations that restrict the set of              to using a dedicated core or execution engine if the
branches they can effectively precompute.                                  processor backend prioritizes the precomputation thread.
   Precomputation is often late: The timeliness constraint of            • Finally, we show that focusing on accuracy and coverage
having the precomputation result arrive before the branch is               allows the TEA thread to outperform Branch Runahead,


2379-315/24/$31.00 ©2024 IEEE                                      480
DOI 10.1109/MICRO61859.2024.00043
