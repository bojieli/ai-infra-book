                                                  LoAS: Fully Temporal-Parallel Dataflow for
                                                    Dual-Sparse Spiking Neural Networks
                                                  Ruokai Yin                   Youngeun Kim                                 Di Wu                                      Priyadarshini Panda
                                                 Yale University               Yale University                 University of Central Florida                              Yale University
                                               New Haven, USA                 New Haven, USA                         Orlando, USA                                       New Haven, USA
                                              ruokai.yin@yale.edu          youngeun.kim@yale.edu                     di.wu@ucf.edu                                    priya.panda@yale.edu



                                           Abstract—Spiking Neural Networks (SNNs) have gained signif-                                                            Dataflow                      Hardware
arXiv:2407.14073v3 [cs.AR] 1 Sep 2024




                                        icant research attention in the last decade due to their potential                                                                …                           LoAS (Ours)
                                        to drive resource-constrained edge devices. Though existing SNN                   Workload
                                                                                                                                                                                               U           FTP-friendly
                                        accelerators offer high efficiency in processing sparse spikes                                                  Tick-Batch Temporal Sequential                      inner-join




                                                                                                                                                    X
                                        with dense weights, opportunities are less explored in SNNs                 Layer 1 Layer 2       Layer N                         …
                                        with sparse weights, i.e., dual-sparsity. In this work, we study                              …             X

                                        the acceleration of dual-sparse SNNs, focusing on their core                                                       Partially Temporal Parallel        X            FTP-friendly
                                                                                                                                                                                          !!,! !!,$ !!,#
                                        operation, sparse-matrix-sparse-matrix multiplication (spMspM).                                                                                                    compression
                                                                                                                     Temporal Sequential            ✓                     …
                                        We observe that naively running a dual-sparse SNN on existing                  dual-sparse SNN                                          ≈        1010
                                                                                                                                                                                             0000 0111
                                        spMspM accelerators designed for dual-sparse Artificial Neural
                                                                                                                                                        Fully Temporal Parallel (Ours)
                                        Networks (ANNs) exhibits sub-optimal efficiency. The main
                                        challenge is that processing timesteps, a natural property of
                                        SNNs, introduces an extra loop to ANN spMspM, leading to                Fig. 1. An illustrative example of FTP dataflow and LoAS. FTP dataflow is
                                        longer latency and more memory traffic. To address the problem,         shown along with the prior dataflow design for SNNs. Temporal sequential
                                        we propose a fully temporal-parallel (FTP) dataflow, which              tick-batch is from SpinalFlow [36], and partially temporal parallel is from
                                                                                                                PTB [29]. Each arrow loop indicates the processing of one timestep. The
                                        minimizes both data movement across timesteps and the end-              vertical line indicates that the processing is in parallel.
                                        to-end latency of dual-sparse SNNs. To maximize the efficiency
                                        of FTP dataflow, we propose an FTP-friendly spike compres-
                                        sion mechanism that efficiently compresses single-bit spikes and
                                        ensures contiguous memory access. We further propose an FTP-            Certain works have managed to achieve approximately 98%
                                        friendly inner-join circuit that can lower the cost of the expensive    weight sparsity and 90% spike sparsity [23], leveraging the
                                        prefix-sum circuits with almost no throughput penalty. All the          lottery ticket hypothesis [13]. These works have outlined
                                        above techniques for FTP dataflow are encapsulated in LoAS,
                                        a Low-latency inference Accelerator for dual-sparse SNNs. With
                                                                                                                the potential of dual-sparse SNNs in reaching unprecedented
                                        FTP dataflow, compression, and inner-join, running dual-sparse          energy efficiency and memory footprint with little to no
                                        SNN workloads on LoAS demonstrates significant speedup (up to           compromise in accuracy.
                                        8.51×) and energy reduction (up to 3.68×) compared to running
                                        it on prior dual-sparse accelerators.                                   Challenge. Although dual-sparse SNNs have made strides
                                                                                                                with algorithmic advancements, the hardware is not yet catch-
                                                               I. I NTRODUCTION                                 ing up to make full use of such dual-sparsity. In general, exist-
                                                                                                                ing SNN accelerators can be categorized into two main groups.
                                           Spiking Neural Networks (SNNs) have attracted consid-
                                                                                                                First, multi-core neuromorphic systems1 employ a plethora of
                                        erable interest as potential energy-efficient substitutes for
                                                                                                                cores, even chips, to exploit the inherent parallelism in spiking
                                        Artificial Neural Networks (ANNs) [5], [11], [43]. Inspired
                                                                                                                neuron dynamics [1], [7], [14], [48]. Though capable of
                                        by the biological neuron, SNNs leverage highly sparse unary-
                                                                                                                capturing the massive parallelism and sparse activities across
                                        coded ({0,1}) spikes to compute and communicate infor-
                                                                                                                neurons, multi-core neuromorphic systems require all neurons
                                        mation [54]. Thus, running SNNs on hardware significantly
                                                                                                                (including weights) to be mapped on-chip. This undoubtedly
                                        reduces computation and data movement, making it suitable
                                                                                                                wastes a huge amount of hardware resources on the neurons
                                        for edge computing. Therefore, SNNs have been widely used
                                                                                                                that are not involved in any computations due to the dual-
                                        in computer vision tasks, such as image classification [46],
                                                                                                                sparsity [40]. Second, dataflow-based SNN accelerators draw
                                        [56], optical flow estimation [28], semantic segmentation [21],
                                                                                                                inspiration from dataflow-based ANN accelerators and take
                                        and object detection [20].
                                                                                                                advantage of the rich data reuse among the array of processing
                                        Opportunity. As the need for edge devices with limited                  elements [29], [33], [36]. Nonetheless, these designs have
                                        memory capacity increases, recent research on SNNs high-                mainly focused on processing dense SNN workloads. Cur-
                                        lights the significance of dual-sparse (both spikes and weights         rently, there is a lack of dataflow architectures that uniquely
                                        are sparse), which can be achieved by neural pruning tech-
                                        niques [5], [23]. Pruning the weight connections of SNNs has              1 We are not comparing with those systems due to our focus on single-core
                                        been explored during both training [4], [49] and inference [38].        dataflow SNN accelerator designs.
                           TABLE I                                                       LIF-based SNN Unary data
  C OMPARISON OF L OAS WITH PRIOR SNN ACCELERATORS . S AND T
     DENOTE THE SPATIAL AND TEMPORAL DIMENSIONS . S PATIAL                                         threshold                     &   Acc
                                                                                                               output: 0010
          PARALLELISM MEANS PE- LEVEL PARALLELISM .
                                                                                                                                     LIF
    Accelerator        Spike     Weight      Parallel     Neuron                            𝑡! 𝑡" 𝑡# 𝑡$         𝑡! 𝑡" 𝑡# 𝑡$
                      Sparsity   Sparsity    support      support
                                                                                        ReLU-based ANN Binary data
    SpinalFlow [36]     ✔          ✘            S          LIF
    PTB [29]            ✔          ✘        S+partial-T    LIF                                   ReLU                            x   Acc
                                                                                                                      x if x>0
    Stellar [33]        ✔          ✘         S+fully-T     FS                                             output:
    LoAS (ours)         ✔          ✔         S+fully-T     LIF                                                                       ReLU
                                                                                                                      0 else
                                                                                             0

target dual-sparsity in SNNs. Table. I summarizes existing              Fig. 2. Difference between the LIF-based SNN neuron and ReLU-based ANN
                                                                        neuron. We compare the behavior of LIF-based and ReLU-based neurons and
dataflow SNN accelerators.                                              their hardware implementations.
Insight. Though spikes and weights have varying bitwidth,
in dual-sparse SNNs, their interactions follow the pattern in                 fails to efficiently process timesteps in SNNs with low
sparse-matrix-sparse-matrix multiplication (spMspM), which                    latency and memory traffic.
has been extensively studied in ANNs [9], [15], [18], [19],                2) To improve the efficiency of processing timesteps, we
[39], [41], [42], [51], [62], [64]. However, naively running                  propose a fully temporal-parallel (FTP) dataflow. FTP
dual-sparse SNNs on existing spMspM accelerators is ineffi-                   avoids extra memory traffic across timesteps and min-
cient. The reason is multifaceted. First, the timesteps in SNNs               imizes the latency penalty in processing timesteps se-
complicate the dataflow design for existing spMspM accel-                     quentially.
erators. spMspM operations in ANNs are triple-nested for-                  3) To make the most of FTP, we propose FTP-friendly
loops [41], [55]. Different spMspM dataflows are obtained by                  spike compression for efficient yet contiguous memory
permuting the order of loops. However, in SNNs, the timesteps                 access and an FTP-friendly inner-join mechanism for
introduce an extra level of for loop, leading to extra latency                low-cost computation with almost no latency penalty.
and memory traffic. What’s worse, it constrains dataflow de-               4) We build LoAS, a novel architecture that exemplifies
pendency and doubles the dataflow design space, delaying the                  the FTP dataflow. With both FTP-friendly compression
time-to-solution. Second, the asymmetric bitwidth of spikes                   and inner-join, LoAS is able to achieve high speedup
and weights in SNNs makes it inefficient to use conventional                  and energy efficiency against other sequential-running
compression formats in ANN spMspM accelerators. Existing                      spMspM baselines.
ANN spMspM accelerators store sparse matrices with popular                 The remainder of the text is organized as follows. Section II
compressed formats like compressed sparse row (CSR). These              reviews the background and justifies the motivation. Section III
formats usually have multiple bits to record the coordinates            and IV articulates our proposed FTP dataflow and LoAS
of the non-zero values, and so does the hardware designed.              architecture. Next, Section V and VI evaluate our design.
Consequentially, using multiple bits to compress single-bit             Finally, Section VII and VIII discuss and conclude this work.
spikes (valued at either 1 or 0) is extremely inefficient for
dual-sparse SNNs.                                                                    II. BACKGROUND AND M OTIVATION
Proposal. To solve these problems and unleash the potential             A. Preliminary of SNNs
of dual-sparse SNNs in the presence of spMspM, we propose
fully temporal-parallel (FTP) dataflow, illustrated in Figure 1.          1) Leaky-Integrate-and-Fire Neuron: The Leaky-Integrate-
FTP dataflow parallelizes all timesteps to avoid complicated            and-Fire (LIF) neuron is a classical neuron model [8] and
dataflow dependency for minimized latency and memory traf-              widely adopted by prior SNN works [23], [24], [63], [65],
fic. To maximize the efficiency of FTP dataflow on memory               thanks to its bio-plausibility and high accuracy. In this work,
and computation, we design FTP-friendly spike compression               we focus on accelerating the workloads of dual-sparse SNNs
and inner-joint mechanism. The proposed compression packs               that use LIF neurons.
spike along timesteps and can access the relevant memory                  During inference, each layer has an input spike tensor A ∈
space in a contiguous manner. The proposed inner-join nearly            UM ×K×T where U ∈ {0, 1} and a weight matrix defined as
halves the cost of cumbersome prefix-sum circuits with almost           B ∈ ZK×N . Here T is the number of total timesteps; M ,
no throughput penalty compared to prior inner-join designs. To          N , and K are the spatial dimensions of the input and weight
validate FTP dataflow, we design LoAS, a Low-latency Infer-             matrix. The behavior of an SNN layer can be described below:
ence Accelerator for Dual-Sparse Spiking Neural Networks.               Step 1: Sparse Matrix Multiplication Sparse matrix multi-
Our contributions are listed below:                                     plication across all timesteps is performed to obtain the full
  1) We observe that SNNs with rich dual-sparsity from both             output matrix O ∈ ZM ×N ×T , which will be sent to LIF
     input spikes and weight connections are sub-optimal                neurons.
                                                                                                     K
     on existing hardware. SNN hardware usually does not                                Om,n [ti ] =
                                                                                                     X
                                                                                                         Am,k [ti ]Bk,n ,          (1)
     support sparse weights, while ANN spMspM hardware                                                      k=0




                                                                    2
where the ti is the current timestep. With dual-sparsity, sparse                      units that they require. As shown in Figure 2, compared to
matrix multiplication becomes spMspM.                                                 the multiply-accumulate (MAC) of ANNs, SNN only requires
Step 2: LIF firing LIF neurons take the snapshot of O at                              simple bitwise-AND and accumulate (AC) operations during
timestep ti and generate a snapshot of the output spike tensor                        inference time.3 Without the expensive multipliers [16], the
C ∈ UM ×N ×T for current timestep ti :                                                computations for SNNs require extremely low power and area.
                                                                                     Feature 2: Sparse Spike Activity The second feature of
                          1       Xm,n [ti ] > vth
           Cm,n [ti ] =                                    (2)                        SNNs is their highly sparse spike-firing activity. In ANNs,
                          0       else,
                                                                                      upon completion, MAC results go through the ReLU unit,
where                                                                                 which filters out non-positive outputs. Different from ANNs,
                Xm,n [ti ] = Om,n [ti ] + Um,n [ti−1 ].                               AC results in SNNs go through the Leaky-Integrate-and-Fire
                                                                                      (LIF) unit, which only fires (generates an output of 1) when the
Here, U [ti−1 ] is the membrane potential that carries over the                       input is greater than a pre-set threshold. As a result, the output
temporal information from previous timestep ti−1 , and vth is                         sparsity in SNNs is usually much higher (∼ 90%) [60], [61],
the firing threshold, a pre-defined scalar value.                                     [63], [65] than that of ANNs (∼ 50%) [41], [45]. More sparse
Step 3: Membrane Potential Update After the output spikes                             outputs apparently lead to more computation and memory
are generated, we update the membrane potential that will                             saving under the context of spMspM acceleration.
carry residual information to the next timestep according to                          Challenge: Repeated Timesteps Despite the aforementioned
the equation below.2                                                                  hardware-friendly features, one main challenge of deploying
               Um,n [ti ] = τ Xm,n [ti ](1 − Cm,n [ti ]),                  (3)        SNNs on hardware is their intrinsic repeated timesteps. A
                                                                                      timestep is the minimum unit of time in SNNs, thus discrete.4
where τ ∈ (0, 1) is the leaky factor. From the above equations,                       In one timestep, each neuron needs to complete the AC
we observe that to generate the output spike matrix C for                             operations for all inputs, fire a spike if necessary, and update
timestep ti , we need to know the information from the previous                       its membrane potential (will be discussed shortly). The SNN
timestep U [ti−1 ]. This brings temporal dependency between                           needs to run across multiple timesteps to capture the temporal
output spike matrices across timesteps. The behavior of a LIF                         dynamics from the input data, as shown in Figure 2. Running
neuron can be found in Figure 2.                                                      multiple timesteps increases latency and fails to be energy
   2) Spike Encoding and SNN Training: One key step in                                efficient, diluting the advantage of low-power circuits unless
leveraging SNNs in conventional machine learning tasks is                             we have a specialized architecture design [36].
encoding the input source data (e.g., image pixels or text
embeddings) into spike trains across multiple timesteps. The                          C. spMspM Dataflows in SNNs
input spike trains are then sequentially sent to the SNN                                 There are various ways to map spMspM onto hardware,
for processing. Recent SNN works adopt direct encoding (a                             each with unique efficiency [31], [34]. Three different spM-
special case of rate encoding) to achieve high accuracy on                            spM dataflows have been proposed in existing dual-sparse
conventional computer vision tasks in very few timesteps                              ANN accelerators: Inner-product (IP) [15], [18], [19], [42],
(≤ 4) [?], [23], [25], [57], [65]. In direct encoding, the                            Outer-product (OP) [9], [39], [41], [64], and Gustavson’s
source data, instead of being directly converted into spike                           (Gust) [51], [62]. In Figure 3, we illustrate these three
trains, first goes through one ANN layer. The output from                             dataflows in SNNs for two input matrices A and B, and an
the ANN layer is then converted into spike trains. We will                            output matrix C. We also formulate their abstract loop nests
focus on accelerating direct-coded dual-sparse SNNs in this                           on the right-hand side. As we discussed in Section II-B, it is
work. The SNNs are trained using backpropagation-through-                             impossible not to consider the multiple timesteps for spMspM
time (BPTT) [53] with surrogate gradient [37] to achieve very                         operations in SNNs.
close performance to ANNs on many complex tasks [57],                                    Inside the black box in Figure 3, the dataflow is for one
[65].                                                                                 timestep, thus identical to ANN dataflow. Outside the black
B. Distinctive Features and Challenge of SNNs                                         box, multiple input matrices A (blurred) represent the input
                                                                                      spike matrices across different timesteps, which need to be
  Several distinctive features make SNNs favorable for low-                           processed. Meanwhile, multiple output spike matrices C that
power edge deployment, but they also come with challenges.                            have temporal dependency between each other are also gener-
Feature 1: Unary Activation One of the most distinctive                               ated. Specifically, to accommodate the timesteps in SNNs, we
features of SNNs is their unary spike activation. More specif-                        need to consider one more loop dimension (t dimension) in
ically, the SNNs leverage single-bit non-weighted activation                          the original triple-nested for-loop. The t dimension (annotated
to propagate information through layers. The primary benefit                          in the blue box) brings temporal dependency to each output
of the unary activation is the simplified low-power arithmetic
                                                                                         3 There exist other implementations using multiplexers instead [29], [36].
  2 We focus on the hard reset (membrane potential is reset to zero if there is       We focus on using bitwise-AND gates in this work.
an output spike of one) in this work. Though there exist other reset schemes,            4 Timestep is also called tick [36] or time-point [29] in other works. We
sticking with one of them will not lose generality in the hardware design.            follow the naming convention adopted by the latest SNN algorithm works.




                                                                                  3
                                    T                                                                    T
            K                                                   N                       N                                                       Inner-Product dataflow
        M                                       1           K                       M                                                 for m in [0,M-1]
                                                                                                                                        for n in [0,N-1] 3
                       …                2               ×                   3   =                                 …                       for t in [0,T-1] 2
                                                                1
                                                                                                                                            for k in [0,K-1] 1
            A[𝑡"#$ ]       A[𝑡$ ]           A[𝑡! ]                  B                       C[𝑡! ]       C[𝑡$ ]         C[𝑡"#$ ]              C[m,n,t] += A[m,k,t]*B[k,n]

            K                                                   N                    N                                                          Outer-Product dataflow
        M                                                   K           1           M                                                  for k in [0,K-1]
                       …            2
                                                        ×                       =                                                        for m in [0,M-1] 3
                                        3                                                                          …                       for t in [0,T-1] 2
                                                                                                                                             for n in [0,N-1] 1
            A[𝑡"#$ ]       A[𝑡$ ]           A[𝑡! ]                  B                       C[𝑡! ]       C[𝑡$ ]          C[𝑡"#$ ]              C[m,n,t] += A[m,k,t]*B[k,n]


            K                                                   N                    N                                                          Gustavson’s dataflow
        M                                           3       K           1           M                                                  for m in [0,M-1]
                       …            2                   ×                       =                                                        for k in [0,K-1] 3
                                                                                                                   …                       for t in [0,T-1] 2
                                                                    3                                                                        for n in [0,N-1] 1
            A[𝑡"#$ ]       A[𝑡$ ]           A[𝑡! ]                  B                       C[𝑡! ]       C[𝑡$ ]          C[𝑡"#$ ]              C[m,n,t] += A[m,k,t]*B[k,n]

Fig. 3. Comparison of different spMspM dataflow for SNNs. Here, for illustration purposes, we put C[ti ] as the spMspM result between A[ti ] and B to
align with spMspM in ANNs. In SNNs, we need to go through one more LIF step (Equation (2)) to get C[ti ]. The circled numbers illustrate the order of
computation for the specific spMspM dataflow. Please note that we fix the position of t dimension for illustration purposes. In practice, there will be a total
of 16 possible permutations of spMspM dataflow in SNNs, which we will discuss in Section III.

pixel in SNNs. For example, to process the SNN using IP                                                                                   Inner-join Design (ANN)
dataflow as shown in Figure 3, we first calculate the output                                                        (SNN)             System       *
                                                                                                                                                 ~45%
                                                                                                                                      Power                Original data A
cell at (0,0) position for timestep 0 (C[0,0,0]), then instead                                                    𝑡# 0 1 1 0 0                              0 0 3 0 9
of moving to the position (0,1), we move on to process the                                                        𝑡" 1 0 0 0 1                                 data-A
output cell at (0,0) for timestep 1 (C[0,0,1]). Since the output                                                  𝑡$ 0 0 0 0 0     Bitmask A                    3 9
                                                                                                                  𝑡! 0 1 0 0 0                         U                 3
cell C[0,0,1] is temporal dependent on the result of the output                                                                     0 0 1 0 1
                                                                                                                                                                           MAC
                                                                                                                                    0 1 1 0 0                  data-B
cell C[0,0,0], we cannot process C[0,0,1] before C[0,0,0].                                                        𝑡$       𝑡!      Bitmask B                    5 6      6
                                                                                                                                                           Original data B
                                                                                                                   𝑡#      𝑡"       U
D. ANN spMspM Hardware for dual-sparse SNNs                                                                                             : Inner-join        0 5 6 0 0

   We review existing ANN spMspM accelerators to under-                                                  Fig. 4. An example of the inner-join design. The difference between the
stand why naively running dual-sparse SNNs on these accel-                                               behavior of ANN and SNN is shown. *Data from SparTen [15].
erators is sub-optimal.                                                                                                            AlexNet-L1           VGG16-L8       ResNet19-L8

Inner-join Design: For the IP dataflow, prior accelerators
usually adopt the inner-join-based design [9], [15]. In such
designs, non-zero values in rows of matrix A and columns
of matrix B are compressed using bitmask representation (a
bit string that has 1’s for positions with non-zero values
and 0’s otherwise). An inner-join unit scans two bitmasks
                                                                                                         Fig. 5. Off-chip traffic of partial sum matrices on different SNN layers. We
on the fly to determine if there’s a matched position (both                                              envision SNNs with a timestep of 1 and 4 running on GoSPA [9], an OP
multiplicands are non-zero) and then sends the matched pairs                                             dataflow spMspM accelerator.
to the compute units. Running dual-sparse SNNs on an inner-
join-based design does not require the extra bit-masks for the                                           once, leading to efficient input data reuse. However, one partial
input spike matrix A (the unary spike train itself can be viewed                                         sum is generated at a time and merged later. While these
as a bit-mask). However, as shown in Figure 4, the timesteps                                             two dataflows have better data reuse on the input matrix, the
will impose multiple extra rounds of running the expensive                                               partial sum matrices (rows) potentially bring more off-chip
inner-join units (e.g., occupying roughly 46% of the system-                                             data traffic. To amortize the large memory traffic of partial
level power [15]), thus incurring high energy cost. Moreover,                                            sums, some designs implement large and costly mergers (e.g.,
since the spike trains are used as bit-masks, all the spikes,                                            38× more area than multipliers [64]) to merge as many as
no matter 1 or 0, are necessary to be fetched from off-chip                                              partial sum matrices (rows) before sending them back to the
DRAM. This brings no memory traffic saving on the sparse                                                 off-chip DRAM. Due to the extra t dimension, running dual-
spike matrix A.                                                                                          sparse SNNs on a merger-based design either requires a more
Merger-based Design: Unlike IP dataflow designs that ex-                                                 complex merger that is capable of digesting the extra partial
hibit full output matrix (C) reuse, OP and Gust dataflow                                                 sum traffic or incurs more off-chip memory traffic. As shown
designs focus on the reuse of input matrix A and B. In OP,                                               in Figure 5, for a timestep of four, on average, 4× more partial
each column of A and each row of B will only be transversed                                              sum traffic will be induced compared to a single timestep.



                                                                                                     4
                                                  𝑡!       𝑡#                      Algorithm 1 Fully Temporal-Parallel dataflow (FTP)
                          win-1 win-2
                                                   𝑡"       𝑡$
                                                                                   Input:
         neuron-i
                                                                 group-i           Input spike matrix A ∈ UM ×K×T (U ∈ {0, 1})
                           𝑡! 𝑡"   𝑡# 𝑡$ Time
                                                                                   Weight matrix B ∈ ZK×N
      neuron-i+1                                                 group-i+1         Output:
                           𝑡! 𝑡"   𝑡# 𝑡$ Time                                      Output spike matrix C ∈ UM ×N ×T




                                                       …
                                   …
                                                                 group-j            1: for m ∈ M do
         neuron-j                                                                   2:     for n ∈ N do
                           𝑡! 𝑡"   𝑡# 𝑡$ Time   Systolic-array of PEs
                                                                                    3:         for k ∈ K do
Fig. 6. Example of PTB’s partially temporal parallel design. Each column            4:           parallel-for t ∈ T do        ▷ Spatially unrolled
of the PE array processes a time-window that consists of multiple timesteps.
Different time-windows run in parallel, but the timesteps inside the window         5:               O[m, n, t] += A[m, k, t] × B[k, n]
are still processed sequentially.                                                   6:         end for
                                                                                                 parallel-for t ∈ T do        ▷ Spatially unrolled
E. Dataflow Architecture for SNNs                                                   7:              C[m, n, t] = LIF (O[m, n, t])
                                                                                    8:     end for
SpinalFlow: Temporal Sequential Design. SpinalFlow [36]
                                                                                    9: end for
is the first SNN-tailored accelerator for extracting the effi-
ciency from the single-bit activation and the extremely sparse
spike activity. The authors identified the challenge of sequen-
tially processing the entire SNN network through timesteps.                        Stellar: Fully Temporal Parallel but with non-LIF neurons.
To overcome the challenge, SpinalFlow proceeds all timesteps                       Stellar [33] is another systolic array SNN accelerator which
for one layer and then proceeds to the next layer, as shown in                     attempts to process timesteps in a fully parallel manner.
Figure 1. SpinalFlow dispatches LIF neurons across different                       Nonetheless, Stellar focuses on optimizing for the Few Spikes
processing elements (PEs) and parallelizes the computation.                        (FS) neuron [52], as shown in Table I. FS neurons behave dif-
Within each layer, the timesteps are processed sequentially,                       ferently from LIF neurons by detaching the spike accumulating
as shown in Figure 1. Spinalflow is optimized exclusively                          and firing stages. Therefore, FS neurons naturally do not have
for the temporal-coded SNNs that potentially lag in terms                          temporal dependency among the input data at the spike accu-
of accuracy performance compared to rate-coded SNNs [29].                          mulation stage. This makes fully parallel temporal processing
In this work, we focus on accelerating spMspM for general                          straightforward in Stellar. On the contrary, as discussed in
rate-coded SNNs that yield competitive accuracy as ANNs in                         Section II-A, temporal dependency naturally exists in the input
various tasks.                                                                     data for the LIF neuron, which makes its design space different
                                                                                   from the one in Stellar for fully temporal parallel processing.
PTB: Partially Temporal Parallel. While SpinalFlow’s de-                           Unlike the widely adopted LIF neurons, supporting FS neurons
sign is tailored to the temporal-coded SNNs, PTB [29] pro-                         also requires non-trivial algorithm-hardware codesign, which
poses a general architecture design for the rate-coded SNN.                        is out of the scope of this work.
By leveraging the high data-reuse pattern across different
PEs in the systolic array architecture [27], PTB breaks the                               III. F ULLY T EMPORAL PARALLEL DATAFLOW
processing of all timesteps into multiple time-windows (each
consists of several contiguous timesteps) and run these time-                         We propose a fully temporal-parallel dataflow (FTP) that
windows in parallel, as shown in Figure 1. PTB parallelly                          targets reducing the negative effects of repeatedly processing
maps multiple time-windows across different columns of the                         the timesteps on spMspM accelerators (Section II-D). The
systolic array. The computation of different LIF neurons is also                   proposed FTP is formulated in Algorithm 1.
parallelized across the rows of the systolic array. We illustrate                     An SNN-friendly spMspM dataflow should satisfy three
this hardware mapping strategy in Figure 6 with details.                           goals: (1) avoid as much data refetch as possible across the
Though PTB tries to parallelize the processing of timesteps,                       timesteps; (2) generate as few partial sums as possible on
the parallelization is on the granularity of the time-window.                      the temporal dimension (timesteps); (3) reduce the latency as
Inside each time-window (column of PEs), the timesteps are                         much as possible on the temporal dimension to reduce the
still processed sequentially. Consequently, we categorize PTB                      extra cost of sparsity handling units.
as a partially temporal parallel design. One unique aspect of                         Our first observation is that for all three spMspM dataflows
LoAS from PTB is that LoAS places the temporal dimension                           (Section II-C), unless placing the temporal dimension (t-dim)
in the inner-most loop, enabling all optimizations.                                at the innermost loop, it will bring at least T times more
   Prior SNN accelerators with LIF neurons process timesteps                       data refetch to the dimensions below, compared to the original
in a sequential or partially parallel manner. In this way, as                      dataflow. For example, in OP, if t-dim is placed between m
we discussed in (Section II-C & II-D), it is very challenging                      and n, T times more access to B’s rows is required. If t-
for those existing SNN designs to have good performance on                         dim is placed between k and m, T times more access to
spMspM SNN acceleration. Thus, we need a spMspM-friendly                           A’s columns and B’s rows is required. Depending on the on-
strategy to process timesteps.                                                     chip buffer capacity, repeated memory access might lead to



                                                                               5
more expensive access to the off-chip memory, which opposes                                                        Input Neuron Behavior               Unpacked Real Data
goal (1).                                                                                                            position                                  row 0 in A
                                                                                                                      𝑎&,'
   Our second observation is that both OP and Gust dataflow                                                                                            A[𝑡$ ] 1 0 0 0
                                                                                                                   𝑎$,$
                                                                                                                                                  1    A[𝑡# ] 0 0 0 1
are not suitable for dual-sparse SNNs since they oppose                                                           𝑎$,#
                                                                                                                                                       A[𝑡" ] 1 0 0 1
goal (2). In OP dataflow, we observe that no matter where we                                                      𝑎$,"
                                                                                                                                                       A[𝑡! ] 0 0 0 1
insert the t dimension into the original triple-nested loop, we                                                   𝑎$,!
                                                                                                                             𝑡$ 𝑡# 𝑡" 𝑡!           2         k=0     …    k=3
always produce T times more partial sum matrices compared
                                                                                                                                                           Compressed Fiber
to the original OP dataflow. The partial sums need to be stored                                                           Packed Real Data
in an on-chip cache till all partial sums along both spatial (k)                                                                   0000                3    1001 bm ptr

and temporal dimensions (t-dim) are accumulated. This will                                                    A[𝑡$ ,𝑡# ,𝑡" ,𝑡! ] 𝑎$,$ 𝑎$,# 𝑎$," 𝑎$,!
                                                                                                                                                                   𝑎$,$ 𝑎$,!
add extra memory overhead in OP. The same problem also                                                                                                      1010
                                                                                                                                1010 0000 0111                           0111
exists for Gust dataflow. The t-dim will either generate T
times more partial sum rows or have T times more access                                             Fig. 8. Example showing how input spikes are compressed in LoAS. bm
                                                                                                    stands for the bitmask, and ptr stands for the pointer.
to both k and n dimensions. The last observation is that
regardless of the position of t-dim, as long as we process it                                       spikes from P-LIFs and writes them back to the on-chip
sequentially, it always incurs T times more processing latency,                                     memory. An on-chip SRAM is equipped to capture data reuse.
which opposes goal (3).
   Our solution is straightforward but effective. We first choose                                   A. Spikes Compression
to position the t-dim at the innermost of the IP dataflow, as                                          We first discuss how sparse input spikes (matrix A) across
given in Algorithm 1. This design choice has several advan-                                         timesteps are compressed in LoAS. Efficiently compressing
tages. Firstly, putting the t-dim at the innermost loop ensures                                     matrix A in SNNs necessitates solving two challenges:
that no extra data movement will be incurred (goal (1)).                                            How to maximize the compression ratio of 1-bit spikes?
Secondly, since IP dataflow has efficient output reuse, no                                          Assume that the input spike matrix A has a size of 128×128
extra partial sums will be generated on the t-dim (goal                                             for each timestep. Then for either CSR or CSC, we need to use
(2)). Lastly, we fully parallelize the t-dim and eliminate the                                      two 7-bit coordinates to compress each 1-bit non-zero spike.5
latency brought by sequentially processing timesteps. This is                                       Furthermore, SNNs naturally run for multiple timesteps, which
equivalent to transforming the for-loop of t into a parallel-                                       means that for the same coordinate, different spike values may
for loop [55]. This parallel-for loop parallelizes the operation                                    occur at different timesteps (e.g., 0 for T=1&3, and 1 for
across different spatial instances, requiring minimum hardware                                      T=2&4). To faithfully capture all the non-zero spikes, we need
overheads due to only cheap accumulators being duplicated,                                          separate coordinate values for each timestep.
and timesteps of direct-coded SNNs are small (Section II-A).
We later show in the ablation studies that FTP scales well                                          How to maintain contiguous memory access of non-zero
with the increasing timesteps.                                                                      spikes across timesteps? The FTP dataflow we proposed in
                                                                                                    Section III requires spatial unrolling of the input spike matrix
             Compressor                  Streaming data broadcast bus                               A across all timesteps beneath the k dimension. Consequently,
                                                                                                    a dis-contiguous memory layout of A along the t dimension
P-LIF




                                                  Bitmask
             P-LIF


                          P-LIF




                          …                       B    A
                                             Data B
                                                                            𝑿𝒕𝟎          𝒗𝒕𝒉        will cause fragmented memory access at all levels of memory
                                                                            0 <<
                                                        Fetcher
                                                         AU                              𝑶𝒕𝟎        hierarchies, leading to higher data movement costs.
                                          checker




        TP           TP           TP                                      𝑿𝒕𝟏                          To better illustrate these two points, we provide an example
                          …                                                     +
        PE           PE           PE                                                                in Figure 8. Envisioning that the input spikes sent to the
                                                          +
                                                                      v    0 <<          𝑶𝒕𝟏
                                                                                                    system have the pre-synaptic neuron a0,0 (first element of row-
                                                              𝑿
               Scheduler
                                                                                    …




                                                                                                    0 in matrix A) firing a spike at t0 and t2 . As shown in step
                                                                                                     1 , to represent this pre-synaptic neuron behavior, a single-
                                                                          …




                                                    +         +           𝑿𝒕𝑻"𝟏+
         Global Cache                               𝑿𝒕𝟎
                                                          …
                                                              𝑿𝒕𝑻"𝟏
                                                                                        𝑶𝒕𝑻"𝟏
                                                                                                    bit 1 needs to be stored at row-0, column-0 of matrix A for
                                                                                                    both timestep 0 and 2 into the memory, shown in the box of
                     Memory                                                                         ’unpacked real data.’ Then, for each non-zero spike in row-0
                                                                                                    of matrix A for each timestep, if we need to use a coordinate
Fig. 7. Architecture of LoAS and the microarchitecture of the TPPE. Red
                                                                                                    value (e.g., 4-bit for CSR) to record its position. We then need
arrows are the enable signals that skip the computation on 0 spikes [59].                           2 × 4 = 8 bits to compress 2 bits (2 spikes). The compression
                                                                                                    efficiency in this case is only 25%. Furthermore, memory
                                       IV. L OAS                                                    access to spikes across different timesteps is discontinuous
   An overview of LoAS is shown in Figure 7. LoAS consists                                          (sequentially access different rows of A). We propose the
of multiple temporal parallel processing elements (TPPEs) and                                       following spikes compression format for LoAS to solve these
parallel Leaky-Integrate-Fire units (P-LIFs) that are tailored to                                     5 For 128 columns, we need log (128) = 7 bits for coordinates. We neglect
                                                                                                                                      2
run the FTP dataflow; a scheduler that distributes workloads                                        the offsets in the discussion, which will further increase the number of bits
across TPPEs; and a compressor that compresses the output                                           used for coordinates.




                                                                                                6
two challenges. In our method, as shown in step 2 , we pack                                                     Process
                                                                                                                          From Bitmask Buffer




                                                                                                            …
                                                                                                                 time
all the spikes (both 0 and 1) across all timesteps into one                                 Bitmask A
                                                                                                        …            t
                                                                                                                          Bitmask A       Bitmask B
continuous data block in the system for each pre-synaptic                                   From Bitmask Buffer B                                                   U
                                                                                                                                          Priority




                                                                           Match Position
neuron. In the example of Figure 8, we store a 4-bit value
1010 at the first position of row-0 of matrix A for a0,0 and                                    + +
                                                                                                     …+ +                       &
                                                                                                                                          Encoder

                                                                                                     …




                                                                                                                             …
                                                                                                                                      AND                   Prefix-Sum
0111 at the fourth position for a0,3 . Since neurons a0,1 and                                                                   &                Match
                                                                                                     Offset of fiber-A                result
a0,2 do not spike at any timestep, their packed value would be                                                                                  Position Offset of fiber-B
                                                                                                        To Data fiber-A     To Bitmask Buffer B          To Fetcher
0000 (shown in the box of ’packed real data’). We define these
                                                                                            Laggy prefix-sum circuit                      Fast prefix-sum circuit
neurons as silent neurons.6 With this strategy, only the non-                        Fig. 9. Illustration of the proposed FTP-friendly inner join unit.
silent neurons will be treated as non-zero values and stored in
the memory for matrix A, as shown in step 3 . In our example,          C. Inner-join Unit
we end up using 4 bits to compress 5 bits. The compression                The inner-join operation has been extensively studied by
efficiency in this case is 125%.                                       prior works [9], [15], [18] for spMspM acceleration in ANNs.
   To accommodate our FTP dataflow, we compress the input              The inner-join mechanism with prefix-sum circuit has been
spike matrix A in a row-wise manner and use the bitmask                efficiently implemented with the bitmask representation [15].
format [9], [15], [42] to represent the coordinates of the non-        In [15], a logical-AND operation is first applied to two
zero values. The bitmask format uses a 1-bit coordinate value          bitmasks to get the AND-result, which represents the location
for each position in the row. In our example, the bitmask is           where both data are nonzero. The AND-result is then sent to a
1001 since the first and the fourth elements in the row are            priority encoder to convert the matched positions into integer
non-zero. The second and third elements are silent neurons,            values. The matched positios are sent to two separate prefix-
so we do not store them in the memory (represented by a 0 in           sum circuits to get the number of 1s in front of the matched
bitmask). Following the bitmask, a pointer is stored to provide        position for each bitmask. This gets the offsets for each non-
the starting location of the non-zero values of the row. We call       zero data in the memory.
this compressed row: a fiber [34], [62].                                  During the above process, the use of two fast prefix-sum
   The key to our compression method is the ratio of silent            circuits is an expensive operation (taking more than 45%
neurons in the SNN. Fortunately, empirical studies have                power and area in [15]).7 To reduce the overhead brought by
shown that SNNs have a significant fraction of silent neurons          the prefix-sum circuits, we propose an FTP-friendly inner-join
(60% ∼ 70%, as shown in Table II). We further use a similar            unit that is detailed in Figure 9.
bitmask-based technique to compress weights in a column-                  We first observe that in ANNs, the MAC operation requires
wise manner. Each compressed weight column is also called              both inputs to be explicitly known at computation time.
a fiber.                                                               Therefore, we need two fast prefix-sum circuits to match the
                                                                       processing speed between two inputs. However, this is not the
B. Temporal Parallel Processing Elements                               case with SNNs. In SNNs, we only have two cases for the
   The fundamental building blocks of LoAS’s compute en-               input (1 or 0), meaning we either accumulate or discard the
gine are Temporal Parallel Processing Elements (TPPEs) and             weight. This provides the opportunity to have an imbalanced
Parallel Leaky-Integrate-Fire units (P-LIFs), which we de-             processing speed for two inputs at the prefix-sum stage.
scribe next. Figure 7 also details the design of TPPE. Each               In our design, instead of using two fast prefix-sum circuits as
TPPE produces the full sum for one output neuron across all            in ANNs, we have one fast and one laggy prefix-sum circuit,
timesteps (Line 5 in Algorithm. 1). Before the computation             as shown in Figure 9. Recall that our compression method
starts, the bitmask (bm-B) of a fiber from weight matrix B             only fetches the non-silent neurons (that fire at least once
(fiber-B) and its non-zero data are read from SRAM and                 across timesteps) from DRAM for A. Thus, as soon as we find
broadcasted into the small bitmask buffers (128 bits in our            a matched position in AND-result, we are confident that the
design) inside each TPPE. The bitmask (bm-A) of fiber from             corresponding non-zero value in fiber-B will be accumulated
input spike matrix A (fiber-A) is also fetched and sent to             at least once (at least one timestep). Therefore, we can begin
the TPPEs. Each TPPE will hold the bitmask for a distinct              accumulating the non-zero value in fiber-B without knowing
fiber along the row of A. After the data are loaded, an inner-         the exact spike information from fiber-A. In this way, we can
join operation [9], [15], [18] is performed between the two            ensure the throughput of consuming fiber-B is always high
bitmasks. Depending upon the inner-join result, the matched            regardless of the processing speed of fiber-A.
non-zero data of fiber-A will be fetched from the global cache            In our efficient inner-join unit, each time the fast prefix-
and sent to the pseudo-accumulator (soon be discussed) to              sum circuit generates an offset, the corresponding non-zero
perform the accumulation (AC) operation. After the TPPE                value of fiber-B will be directly sent to a pseudo-accumulator
completes the full computation of one output neuron, it will           for accumulation. This mechanism opportunistically presumes
send the result to the P-LIF unit to generate output spikes for           7 In [15], the design of the prefix-sum circuit is not described. We assume
all timesteps in one shot.                                             it to be a tree-like prefix-sum circuit with O(log(n)) complexity that can run
                                                                       in one clock cycle. n is the size of input and output for the prefix-sum circuit,
  6 We follow the same terminology used in [29].                       which is set to 128 in both [15] and our work.




                                                                   7
the matched non-zero value of fiber-A is all 1s (pre-synaptic                                                                         D. Other Units
neuron fires at all timesteps) to fully leverage the throughput                                                                          After the computation of the pseudo-accumulator com-
of the fast prefix-sum circuit. Since the non-zero value in                                                                           pletes, its accumulation results are duplicated and sent to
fiber-A is not always all 1s, we need a mechanism to ensure                                                                           each correction accumulator. The correction value inside each
that the accumulation results are correct. Instead of using                                                                           accumulator will be subtracted from the pseudo accumulation
the expensive fast prefix-sum circuit to access and check the                                                                         results for each timestep. Finally, we send the corrected results
matched non-zero value in fiber-A, we use a much simpler                                                                              to the P-LIF units to generate the output spikes. As shown
circuit to generate the offset of fiber-A. We defined the simpler                                                                     inside the purple box in Figure 7, we spatially unroll the LIF
prefix-sum circuit as the laggy prefix-sum circuit, illustrated on                                                                    operations so that the output spikes for all timesteps will be
the left of Figure 9. We use a group of adders to sequentially                                                                        generated at once.
add up the prefix-sum results and store them inside a small                                                                              LoAS uses a unified global buffer for holding compressed
buffer. These adders run in parallel, and hence, the latency of                                                                       fiber-A and fiber-B with their bitmask representations. We
generating all the offsets is equal to len(bm-A)/# of adders.                                                                         adopt a FiberCache design [62]. A unified shared cache
                                                                                                                                      exhibits better utilization compared to separate ones. Each line
                                Efficient Inner-join scheme in LoAS                                                                   in the global cache consists of two parts. The first part is
     TPPE          cycle1                      cycle2    …               cycle4                        cycle5                         the bitmask representation of a fiber, followed by a pointer.
                                  Fast Prefix-sum
 2         ready            4          ready                     gated                     gated                                      The second part is the non-zero values of that fiber. If the
                                                                                               Pseudo-Acc
                                                                                                                                      line manages to hold all the non-zero values, the pointer will
             Pseudo-Acc                  Pseudo-Acc                 Pseudo-Acc
                   +                           +                         +                             +                              be a NULL pointer. Otherwise, it will point to the location
      !!                          !"                    new fetch                    loading

                                                                                                                !"
                                                                                                                                      of the line where the rest of the data are held. Each PE
                    !!                         !" !!                     !" !!
       2                          4     2                    4      2                          4                                      will take responsibility for generating one output neuron.
             FIFO-B
     FIFO-mp                                                             discard
      not ready                    not ready                     ready                     ready
                                                                                                           !"            !"
                                                                                                                                      Therefore, we use a highly banked global cache to ensure
                                 Laggy Prefix-sum                                                                                     multiple PEs can access their data concurrently. Inside each
                                                                                                   +        +        +        +
  bm-B 10101 AND-result                                                                                                               bank, we fetch as many chunks as possible for one fiber in
  bm-A 01101 00101          Matched: [2,4] "                                     "" 1010           $# $! $$ $"
                                            ! 1111
                                                                                                   Correction-Accs                    matrix A and hold them as long as possible to maximally
          "# "! ""                        predict correct                    predict wrong
                                ! !
 fiber-A 1011 1111 1010 fiber-B ! " # !
                                                                                                                                      have the data reuse of A. This can be achieved by adopting
                                                                                                                                      a replacement policy for the global cache as in [31], [62].
Fig. 10. A walk-through example of the proposed FTP-friendly inner-join
unit. This example assumes the laggy prefix-sum circuit will be ready after 2                                                         Only one compressed row fiber of matrix B is fetched into
cycles. FIFO-mp is the FIFO to buffer the matched position. FIFO-B is the                                                             the global cache and broadcasted to all TPPEs. We follow
FIFO to buffer the matched non-zero value of B. Acc stands for accumulator.                                                           a compression unit as [15], where an inverted prefix-sum
                                                                                                                                      circuit is used to compress the output spikes and generate their
                                                                                                                                      bitmask representations. Similar to the observation in [15], this
    We provide a simple walk-through example in Figure10.                                                                             compression step does need to be performed fast. Therefore,
We first run the fast prefix-sum circuit; in every cycle, we                                                                          we equip an inverted laggy prefix-sum circuit to perform the
accumulate the matched non-zero value of fiber B and buffer                                                                           compression. The scheduler will be responsible for casting
it together with the matched position in small FIFOs. When the                                                                        the data to each TPPE through a simple swizzle-switch-based
laggy prefix-sum circuit finishes running, a ready signal is sent                                                                     crossbar [47].
out. We then check the non-zero value in fiber-A according
to the buffered position from FIFO-mp. If the matched value                                                                                       V. E XPERIMENTAL M ETHODOLOGY
is all 1s, we simply discard the current value in FIFO-B.
Otherwise, we need to send the buffered non-zero values of                                                                            Software Configuration: For the dual-sparse SNNs, we
fiber-B from the FIFO-B to the correction accumulators. As                                                                            train and compress the AlexNet [26], VGG16 [50], and
illustrated in Figure 10, at cycle 4, we check a2 and find its                                                                        ResNet19 [17]. We use the open-source toolchains for lottery-
value is 1111. Thus, we simply discard b2 . At cycle 5, we                                                                            ticket-hypothesis (LTH)-based SNN pruning [13], [22]. We set
check a4 and find its value is 1010. Thus, we send b4 to                                                                              the default timesteps T to 4 across all experiments. We use 15
the correction accumulator for t1 and t3 . This example shows                                                                         rounds of LTH searching, and all SNNs are trained towards
the motivation and benefits of using a combination of fast                                                                            convergence with similar accuracy as state-of-the-art dense
and laggy prefix sums. By having a fast prefix sum, we can                                                                            baselines [22]. We further select representative layers from
consume B at the earliest possible by first accumulating it into                                                                      each network to provide single-layer insights. The summary
the pseudo-accumulator. While waiting for the laggy prefix                                                                            of the workloads is in Table II. We further use a simple yet
sum to correct the accumulation results, we can proceed to                                                                            effective preprocessing technique: zeroing out all presynaptic
fetch the next fiber-B’s data into the buffer. This way, the                                                                          neurons that have a low firing activity to further improve
latency of fetching fiber B can be overlapped with the laggy                                                                          the number of silent neurons. We take the trained SNN and
prefix sum and correction to improve the overall throughput.                                                                          mask the neurons with only one output spike throughout all
At the same time, replacing one fast prefix sum with a laggy                                                                          timesteps. We find that with a very small number of fine-tuning
one saves the overall power and area of our TPPE.                                                                                     (<5 epochs), the accuracy can be fully recovered, as shown in



                                                                                                                                  8
                             TABLE II                                                                             TABLE III
SNN WORKLOADS . NL = # OF LAYERS . T = T IMESTEPS . AV S P{A, B} =                                   C ONFIGURATION OF THE L OAS S YSTEM .
AVERAGE SPARSITY OF THE MATRICES{A, B} IN (%). AV S PA- ORIGIN IS                             TPPEs             16 TPPEs, 8-bit weight
 THE ORIGINAL SPIKE SPARSITY ACROSS TIMESTEPS , AV S PA- PACKED IS                            Inner-join unit   16 Inner-join units
  THE DENSITY OF SILENT NEURONS , AND AV S PA- PACKED +FT IS THE                              Global cache      256 KB, 16 banks, 16-way associative
DENSITY AFTER FINE - TUNED PREPROCESSING . M/N/K DENOTES MATRIX                               Crossbars         16 × 16 and 16 × 16, swizzle-switch based
                              SHAPE .                                                         Main memory       128 GB/s over 16 64-bit HBM channels

  SNN              NL        T         AvSpA          AvSpA       AvSpB             (with 4 timesteps and 8-bit weights) is naively running (se-
                                       origin      packed(+FT)
                                                                                    quentially processing its timesteps) on these accelerators. To
  AlexNet(A)       7          4         81.2         71.3(76.7)     98.2            be conservative, we place the t dimension at the innermost
  VGG16(V)         14         4         82.3         74.1(79.6)     98.2
  ResNet19(R)      19         4         68.6         59.6(66.1)     96.8
                                                                                    loop of the original IP, OP, and Gust dataflow.9 We then
                                                                                    make essential simplifications for the two accelerators. For
  Layer                 T,M,N,K                                                     example, we remove the multipliers in these designs. To make
  A-L4              4,64,256,3456       75.8         63.2(69.7)     98.9            a fair comparison, we configure all designs to have 16 PEs
  V-L8              4,16,512,2304       88.1         76.5(86.8)     96.8            and the same global SRAM size. We call these three baselines
  R-L19             4,16,512,2304       57.9         51.4(55.7)     99.1
  T-HFF            4,784,3072,3072       -            - (86.8)      96.8            SparTen-SNN, GoSPA-SNN, and Gamma-SNN.
                                                                                      We implement the key components of LoAS and our hard-
Figure 11. Please note that this preprocessing technique aims                       ware baselines in RTL and synthesize them using Synopsys
to maintain the accuracy of the original workload instead of                        DC compiler at 800MHz with 32 nm technology. A 128
improving it. During hardware execution, the compressor will                        GB/s High-Bandwidth Memory (HBM) module is connected
discard the output neurons that have 0 or only 1 output spike.                      to LoAS as the off-chip memory. We use CACTI 7.0 [35]
From Table II, we see that preprocessing effectively creates                        to model the memory components. We built a simulator in
up to 1.1× more silent neurons8 .                                                   Python to model the cycle-level behavior of LoAS and the
                                                                                    baselines by tiling the loop and mapping it to hardware.
                                                                                                       VI. E XPERIMENTAL R ESULTS
                                                                                    A. Hardware Evaluation
                                                VGG16
                                                ResNet19
                                                                                    Overall Performances: Figure 12 compares the perfor-
                                                                                    mance between three dual-sparse SNN accelerator baselines
                         Origin Mask FT-e1 FT-e5 FT-e10                             (SparTen-SNN, GoSPA-SNN, and Gamma-SNN) and LoAS
Fig. 11. Accuracy trends of the fine-tuned preprocessing. Mask means                (with and without fine-tuned preprocessing) on three SNNs
masking out all presynaptic neurons that fire only once during the inference.
FT-ex means fine-tuning for x epochs.                                               (speedup w.r.t the cycle numbers of the SparTen-SNN).
                                                                                       The first observation is that LoAS significantly outperforms
Hardware Configuration: We evaluate LoAS with the
                                                                                    the other three accelerator baselines in all cases, obtaining
configuration in Table III. In our experiments, we configure
                                                                                    average speed-ups of 6.79× (vs. SparTen-SNN), 5.99× (vs.
the LoAS to support SNNs running with 4 timesteps. We
                                                                                    GoSPA-SNN), and 3.25× (vs. Gamma-SNN). This is due
use 16 TPPEs, each with 5 accumulators (1 12-bit pseudo-
                                                                                    to LoAS leverages FTP dataflow. The FTP dataflow com-
accumulator and 4 10-bit correction accumulators) and 1 inner-
                                                                                    pletely unleashes LoAS from the intra-PE latency penalty of
join unit. Inside each inner-join unit, there is 1 fast prefix-sum
                                                                                    sequentially running the timesteps. It also enables LoAS to
circuit and 1 laggy prefix-sum circuit. The fast prefix-sum
                                                                                    invoke less on-chip and off-chip data communications across
circuit can generate the offsets in a single cycle. The laggy
                                                                                    timesteps. The second observation is that LoAS’s performance
prefix-sum circuit contains 16 adders and a 128-bit buffer. It
                                                                                    gain is highly correlated with the sparsity of matrix A. This
generates the offset results in 8 cycles. The TPPE also has 2
                                                                                    relationship is expected since our workloads are extremely
depth-8 FIFOs (for correction purposes) and 2 128-bit buffers
                                                                                    sparse on matrix B; thus, the overall computation is matrix-A-
(for holding bitmasks). Finally, a 128-byte buffer is equipped
                                                                                    bounded. Consequentially, the performance of two baselines
inside the TPPE to hold the non-zero weights from f iber −B.
                                                                                    suffers more from sequentially running timesteps through
We allocate 256 KB (double-buffered) for the global cache.
                                                                                    matrix A with less sparsity. However, LoAS will not get this
For our workloads, this memory size is enough to capture
                                                                                    sequentially running penalty. As a result, LoAS achieves from
good on-chip data reuse and keep all TPPEs busy.
                                                                                    4.08× speedup (vs. SparTen-SNN) on VGG16 (highest matrix
Baseline: As discussed previously, there are currently very                         A sparsity) to 8.51× speedup (vs. SparTen-SNN) on ResNet19
limited spMspM accelerators available for dual-sparse SNNs.                         (lowest matrix A sparsity). Finally, we observe that with the
As a result, we construct our baselines in the following way,                       help of pre-processing (removing the neurons that only spike
We first pick three popular ANN spMspM accelerators that                            one time), LoAS further improves the performance by 20% on
use IP, OP, and Gust dataflow: SparTen [15], GoSPA [9],                             average. This is because the pre-processing technique helps to
and Gamma [62]. We then envision that a dual-sparse SNN
                                                                                      9 Adding the t dimension anywhere else will bring more data traffic, thus
  8 The source codes can be found at https://github.com/RuokaiYin/LoAS              worsening the performance.




                                                                                9
                                                                                         Weight         Input        Psum          Other
                                                                                                                                      Normalized
                                                                                                                                    SRAM miss rate
                                                                                                                                              4x
                                                                                                                                      16x




                                                                                Fig. 14. Normalized Off-chip traffic with breakup for SparTen-SNN, GoSPA-
                                                                                SNN, Gamma-SNN, and LoAS (with pre-processed) architectures across
                                                                                three SNN layer workloads. The normalized SRAM cache miss rate is also
                                                                                provided for the ResNet19 layer workload. All numbers are normalized to
                                                                                that of LoAS.


                                                                                input data reuse. This inefficient input data reuse pattern is
                                                                                exacerbated by the extra temporal dimension (t-dim) in SNN
Fig. 12. Performance and efficiency comparison between SparTen-SNN,
GoSPA-SNN, Gamma-SNN, and LoAS (with and without fine-tuned (FT)
                                                                                workloads. While FTP dataflow is a variant of inner-product,
pre-processed) architectures across three SNN workloads. All numbers are        it does not incur any extra executions on the t-dim since it
normalized to that of the SparTen-SNN baseline.                                 parallelizes the t-dim at the inner-most loop.
                                                                                   Not surprisingly, compared to GoSPA-SNN (OP), LoAS still
                                                                                achieves 2.87×(4.49×), 2.19×(2.78×), and 2.98×(3.03×)
                                                                                less on-chip SRAM (off-chip DRAM) access on Alexnet,
                                                                                VGG16, and ResNet19, respectively. This behavior is also
                                                                                expected even though OP dataflow design is known to have
                                                                                excellent input data reuse (on average, GoSPA-SNN has 1.45×
                                                                                less SRAM traffic than SparTen-SNN). The inefficiency for
                                       198.7               407
                                                                                GoSPA-SNN comes from the partial sum (psum) matrices.
                                                                                Because of the extra t-dim in SNNs, the size of psum
                                                                                matrices expands with the number of timesteps. GoSPA’s
                                                                                design allocates a small on-chip memory for the psum. The
                                                                                psum matrices that cannot fit on-chip must be written to off-
                                                                                chip DRAM and read back later for reduction. This incurs
                                                                                significant off-chip memory traffic.
                                                                                   Finally, compared to Gamma-SNN (Gust), LoAS is able to
Fig. 13. Off-chip traffic (KB) and on-chip memory traffic (MB) for              achieve 2.16×, 1.76×, and 1.91× less DRAM accesses. This
SparTen-SNN, GoSPA-SNN, Gamma-SNN, and LoAS (with and without pre-              result is aligned with Gust dataflow’s ability to reduce off-
processed) architectures across three SNN workloads.                            chip partial row accesses through on-chip SRAM and merg-
                                                                                ers. While reducing the DRAM accesses, Gamma’s SRAM
increase the density of silent neurons (Section IV-A), which                    accesses are exacerbated by the t-dim in SNNs. This ends up
LoAS is able to completely avoid the data communications and                    with on average 13.4× more SRAM traffic than LoAS.
computations. Figure 12 also compares the energy efficiency                        To better visualize the aforementioned analysis, we provide
of LoAS and three baselines on different SNN workloads. It                      a memory traffic breakup in Figure 14 for the three SNN
is observed that LoAS (with preprocessing) achieves (3.68×,                     layers in Table II. As shown in the figure, SparTen-SNN has
3.09×, 2.40×), (3.17×, 1.50×, 2.33×), and (3.54×, 1.34×,                        the largest input off-chip traffic, and GoSPA-SNN has the
2.47×) higher energy efficiency over (SparTen-SNN, GoSPA-                       largest psum off-chip traffic across all workloads. Among the
SNN, and Gamma-SNN) on Alexnet, VGG16, and ResNet19.                            three baselines, Gamma-SNN has the smallest off-chip traffic
Detailed Analysis: We next explain the performance gains of                     footprint due to Gust dataflow’s on-chip reuse of partial rows.
LoAS. Owing to the FTP dataflow, LoAS has much less on-                         GoSPA-SNN has the largest off-chip traffic for compressed
chip and off-chip memory traffic than the two baselines. As                     format due to its CSR format for each spike. We notice that
shown in Figure 13, compared to SparTen-SNN (IP), LoAS                          LoAS has slightly larger (2.1×) off-chip traffic for compressed
has 3.93×(3.70×), 3.57×(2.22×), and 4.07×(2.24×) less on-                       format compared to SparTen-SNN. This is because we need
chip SRAM (off-chip DRAM) access on Alexnet, VGG16,                             extra bitmasks to mark the position of non-silent neurons,
and ResNet19, respectively. This behavior is expected since                     while in SparTen-SNN, we can directly leverage the input
IP dataflow design like SparTen is known for having poor                        spike trains. Nevertheless, this overhead is negligible com-



                                                                           10
                           TABLE IV
    A REA AND P OWER BREAKDOWN OF L OAS ( LEFT ) AND ONE TPPE
                            ( RIGHT ).                                                                                            26.8%               origin     FT
                                                                                                  22.2%      36.3%
    Components        Area (mm2 )   Power (mW)     TPPE units       Area    Power         12.5%                      8.4% 15.5%
    16 TPPEs                 0.96      45.1        Accumulators     2e-3     0.16
    16 PLIFs                 0.02       1.2        Fast Prefix      0.04     1.46
    Global cache             0.80      124.5       Laggy Prefix     5e-3     0.32
    Others                   0.30       18.1       Others           0.01     0.88
    Total                    2.08      188.9       TPPE total       0.06     2.82          T=4    T=8 T=16           T=4   T=8 T=16

                                                                                     (a)                                                  (b)
                                                                                         Fig. 16. (a) The scalability of TPPE with increasing timesteps. The yellow
                                                                                         region denotes the portion that grows with the timesteps. (b) The scalability
                                               Fast Prefix-Sum
            Global Cache                            51.8%
                                                                           Accs          of the ratio of silent neurons (sparsity of matrix A) with increasing timesteps.
                                                                           5.6%          All values are normalized to the original silent neuron ratio at the timestep
               65.9%                 TPPEs
                                     23.9%                                               of 4.

                           Others
                           10.2%
                                     Laggy Prefix-Sum             Others
                                          11.4%                   31.2%

Fig. 15. On-chip power breakup of LoAS. Accs stands for the accumulators,
which include 1 pseudo-accumulator and 4 correction-accumulators.

pared to LoAS’s saving on off-chip traffic for other quantities.
Figure 14 also provides the normalized SRAM cache miss
rate for the layer workload in ResNet19. SparTen-SNN has                                 Fig. 17. Scalability of LoAS across different sparsity patterns of matrix B,
a 16× higher miss rate(1.47%) compared to LoAS. GoSPA-                                   number of timesteps, and layer size.
SNN has the lowest miss rate due to its Output-stationary
dataflow. However, the tradeoff is the higher off-chip traffic                           is that all TPPE components other than accumulators and the
of psums. Gamma-SNN has a higher SRAM miss rate than                                     input data buffer are agnostic to the number of timesteps. Even
GoSPA-SNN and LoAS. The reason is that the extra t-dim                                   at 16 timesteps, the TPPE only increases its area (power) by
enlarges the partial row traffic by t times. Some of the extra                           1.37× (1.25×) compared to 4 timesteps. We also showcase
traffic cannot be held in the on-chip SRAM, thus leading                                 how the ratio of silent neurons in VGG16 scales with the
to the cache eviction. Overall, the cache miss rate results                              number of timesteps. Figure 16(b) shows that with the help
align with the off-chip traffic trends. Since we set all the                             of the pre-processing technique, even at the timestep of 8, we
baselines to have the same global cache size, the reduction                              can still have a similar ratio of silent neurons as the timestep
in the memory traffic reflects LoAS’s improvement in both                                of 4. However, it is very likely to have fewer silent neurons
speedup and energy efficiency.                                                           when we have even larger timesteps (> 8). This is one of the
Area and Power:Table IV shows the area and power break-                                  challenge that LoAS needs to face when scaling up on the
down of LoAS with the configuration in Table III. Inside                                 number of timesteps.
each TPPE, one single fast prefix-sum circuit dominates both                             Scalability Study: Figure 17 further shows how the overall
the area (66.7%) and power (51.8%). Original SparTen [15]                                performance of LoAS scales with different quantities. We
even requires two fast prefix-sum circuits for both inputs and                           first test LoAS running on VGG16 with average sparsity of
weights.10 Thanks to the laggy prefix-sum circuits (8.3% of                              B (weight) at 98.2%(High), 68.4(Medium), and 25%(Low).
area and 11.4% of power) we proposed, LoAS only requires                                 The result shows that LoAS’s performance is highly sensitive
one fast prefix-sum circuit inside each TPPE. At the system                              to the sparsity level of B. When we scale the sparsity from
level, the global SRAM cache dominates both the power and                                98.2% to 25%, the performance scales down by roughly 88%.
area, which aligns with the previous works [31], [34], [62].                             We also find that LoAS’s performance scales pretty well
Figure 15 provides a visualization of the power breakup.                                 on timesteps. LoAS only loses roughly 14% of performance
                                                                                         when increasing the number of timesteps by 2×. Finally, we
B. Ablation Studies
                                                                                         test LoAS’s scalability on layer size. We compare one layer
Temporal Scalability Studies: In our experimental settings,                              from VGG16 and the hidden feed-forward (HFF) layer from
we configured the TPPE inside LoAS to run the SNNs with                                  SpikeTransformer [58]. The results show that LoAS scales
4 timesteps. Most state-of-the-art SNN algorithms [10], [12]                             pretty well, even on the layer with a larger parameter size.
usually use a timestep equal to or less than 8. So, we want to                           Dual-sparse SNN vs. Dual-sparse ANN: In this work,
understand how TPPE scales with the timesteps. Figure 16(a)                              we focus on providing insights for the community on how
shows that TPPE scales well with the timesteps. The reason                               the spMspM acceleration works on SNNs. However, it is
  10 This is not the case in SparTen-SNN. Since the input spikes are bitmasks
                                                                                         unavoidable to discuss the comparison between SNNs and
and data at the same time, thus SparTen-SNN only requires one fast prefix-               ANNs. In Figure 18, we show the comparison of normalized
sum circuit.                                                                             energy efficiency and memory traffic between SNNs (LoAS)



                                                                                    11
           comp
                                                                                                                                    46.9x




                            63.4%
                    60.2%
            61.7%




                                                                             Fig. 19. Normalized performance comparison between dual-sparse SNN
                                                                             accelerator (LoAS) vs. dense SNN accelerator baselines (PTB, Stellar).
Fig. 18. Normalized energy efficiency and memory traffic between SNNs
(LoAS., T=4) vs. ANN baselines (SparTen, Gamma).
                                                                             limited accuracy on challenging learning tasks [6], [29].
                                                                                                  VII. R ELATED W ORK
and ANNs (SparTen [15]) and Gamma [62] running VGG16
workload. We use the VGG16 workload in Table II for LoAS.                       Except for the prior SNN dense accelerator works we
ANN-version of VGG16 has 8-bit weights (98.2% sparsity)                      discussed in Section II-E, there also exists prior works that
and activations (43.9% sparsity). Overall, the SNN running on                try to leverage the sparsity in SNNs. In [3], a neuron filter
LoAS has roughly 2.5× and 1.2× energy efficiency compared                    unit is leveraged to only fetch the weight if there is a 1-spike.
to the ANNs running on SparTen and Gamma, respectively.                      However, dual-sparsity (both spike and weight sparsity) is not
We observe that around 60% of energy contributes to the data                 considered. In [2], the dual-sparsity of SNN is considered to
movement for both networks. We, therefore, also include the                  skip the unmatched computation. However, the weights and
DRAM and SRAM traffic comparison in Figure 18. It shows                      spikes are fetched in a dense format without any compression
that SNNs, on average, have ∼ 60% less memory traffic                        from the off-chip memory, thus failing to save data movement
compared to SparTen-ANN. The less memory traffic comes                       costs. In this work, LoAS leverages the dual-sparsity in SNNs
from less input bitwidth (4-bit vs. 8-bit) and higher input                  from both computation and data movement.
sparsity (79.6% vs. 43.9%), thanks to SNN’s features of unary                   As we discussed, PTB processes the timesteps in a partially
activation and sparse spike activity (II-B). Not surprisingly,               parallel manner. Even if one re-configures the PTB to run
Gamma-ANN has lower overall DRAM accesses compared to                        all timesteps in parallel (time-window=1), it still differs from
LoAS due to its Gust dataflow [62]. The tradeoff is 3.5× more                LoAS in the loop ordering. In PTB’s loop ordering, t-dim is
SRAM traffic, which explains why the LoAS has a slightly                     placed between m-dim and n-dim, while LoAS places the
higher overall energy efficiency.                                            t-dim in the inner-most loop. As discussed in Section III,
                                                                             LoAS’s loop ordering brings more efficiency in spMspM
Dual-sparse SNN vs. Dense SNN: To show the benefits of                       operation. Moreover, PTB targets accelerating workloads with
dual-sparsity in SNNs, we compare LoAS with the prior dense                  time-series data from DVS sensors [30], where the timestep is
SNN systolic-array accelerators, PTB [29] and Stellar [33],                  usually large (> 100). On our workloads, where the timesteps
running dense VGG16 with 4 timesteps. For a fair comparison,                 are small (< 8), PTB experiences low hardware utilization.
we set the array size for PTB to be 16 × 4, which generates                  In [32], processing timesteps in parallel is also studied.
16 full-sum outputs for 4 timesteps in parallel (same as                     However, they target the temporal-coded SNN workloads, and
LoAS). We further configure Stellar to the same array size.                  the loop ordering is not discussed. Finally, as discussed in
We leverage ScaleSim [44] to estimate both baselines’ memory                 Section II-E, Stellar [33] is another work that also tries to
traffic and cycle counts. We show the comparison in Figure 19.               process timesteps in parallel. However, it targets the non-LIF,
We first observe that LoAS has roughly 6× higher energy                      FS-coded SNNs and does not support the dual-sparsity.
efficiency compared to PTB, mainly resulting from the 3×
(12.5×) less DRAM (SRAM) traffic. Compared to Stellar,                                            VIII. C ONCLUSION
LoAS has roughly 2.5× higher energy efficiency, as well as the                  In this work, we observe that naively running dual-sparse
2.7× (6.6×) less DRAM (SRAM) traffic. We also observe that                   SNNs on existing spMspM accelerators exhibits sub-optimal
LoAS has 46.9× speedup against PTB. This is primarily due to                 efficiency due to the latency and memory traffic penalty
the data sparsity and the difference between PTB’s partially                 brought by processing timesteps. To improve the efficiency,
temporal parallel (Section II-E) and LoAS’s fully temporal                   we propose a fully temporal-parallel dataflow (FTP), which
parallel mechanism. We observe that Stellar outperforms PTB                  avoids the above problems. To maximize the benefits of
across all matrices. This is mainly due to Stellar’s optimized               FTP, we propose FTP-friendly spike compression and inner-
spatiotemporal row-stationary dataflow and its spike-skipping                join mechanism. We also build LoAS, a novel architecture
technique. However, compared to Stellar, we are still able to                that exemplifies the FTP dataflow. With the help of both
achieve roughly 7.1× speedup due to LoAS’s capability to                     FTP-friendly compression and inner-join, LoAS demonstrates
leverage the dual-sparsity. Please note that we do not compare               significant speedup (up to 8.51×) and energy reduction (up to
with the SpinalFlow [36] due to its temporal encoding achieves               3.68×) compared to prior dual-sparse accelerator baselines.



                                                                        12
                              R EFERENCES                                             [21] Y. Kim, J. Chough, and P. Panda, “Beyond classification: Directly train-
                                                                                           ing spiking neural networks for semantic segmentation,” Neuromorphic
 [1] F. Akopyan, J. Sawada, A. Cassidy, R. Alvarez-Icaza, J. Arthur,                       Computing and Engineering, vol. 2, no. 4, p. 044015, 2022.
     P. Merolla, N. Imam, Y. Nakamura, P. Datta, G.-J. Nam et al.,                    [22] Y. Kim et al., “Exploring lottery ticket hypothesis in spiking neural
     “Truenorth: Design and tool flow of a 65 mw 1 million neuron                          networks,” in Computer Vision–ECCV 2022: 17th European Conference,
     programmable neurosynaptic chip,” IEEE transactions on computer-                      Tel Aviv, Israel, October 23–27, 2022, Proceedings, Part XII. Springer,
     aided design of integrated circuits and systems, vol. 34, no. 10, pp.                 2022, pp. 102–120.
     1537–1557, 2015.                                                                 [23] Y. Kim, Y. Li, H. Park, Y. Venkatesha, R. Yin, and P. Panda, “Exploring
 [2] Q. Chen, C. Gao, and Y. Fu, “Cerebron: a reconfigurable architecture                  lottery ticket hypothesis in spiking neural networks,” in European
     for spatiotemporal sparse spiking neural networks,” IEEE Transactions                 Conference on Computer Vision. Springer, 2022, pp. 102–120.
     on Very Large Scale Integration (VLSI) Systems, vol. 30, no. 10, pp.             [24] Y. Kim and P. Panda, “Revisiting batch normalization for training
     1425–1437, 2022.                                                                      low-latency deep spiking neural networks from scratch,” Frontiers in
 [3] Q. Chen, G. He, X. Wang, J. Xu, S. Shen, H. Chen, Y. Fu, and L. Li,                   neuroscience, 2021.
     “A 67.5 µj/prediction accelerator for spiking neural networks in image           [25] Y. Kim, H. Park, A. Moitra, A. Bhattacharjee, Y. Venkatesha, and
     segmentation,” IEEE Transactions on Circuits and Systems II: Express                  P. Panda, “Rate coding or direct coding: Which one is better for accurate,
     Briefs, vol. 69, no. 2, pp. 574–578, 2021.                                            robust, and energy-efficient spiking neural networks?” in ICASSP 2022-
 [4] Y. Chen, Z. Yu, W. Fang, T. Huang, and Y. Tian, “Pruning of deep                      2022 IEEE International Conference on Acoustics, Speech and Signal
     spiking neural networks through gradient rewiring,” arXiv preprint                    Processing (ICASSP). IEEE, 2022, pp. 71–75.
     arXiv:2105.04916, 2021.                                                          [26] A. Krizhevsky et al., “Imagenet classification with deep convolutional
 [5] D. V. Christensen et al., “2022 roadmap on neuromorphic computing                     neural networks,” NeurIPS, 2012.
     and engineering,” Neuromorphic Computing and Engineering, vol. 2,                [27] H.-T. Kung, “Why systolic architectures?” Computer, vol. 15, no. 1, pp.
     no. 2, p. 022501, 2022.                                                               37–46, 1982.
 [6] I. M. Comsa, K. Potempa, L. Versari, T. Fischbacher, A. Gesmundo,
                                                                                      [28] C. Lee, A. K. Kosta, A. Z. Zhu, K. Chaney, K. Daniilidis, and K. Roy,
     and J. Alakuijala, “Temporal coding in spiking neural networks with
                                                                                           “Spike-flownet: event-based optical flow estimation with energy-efficient
     alpha synaptic function,” in ICASSP 2020-2020 IEEE International
                                                                                           hybrid neural networks,” in European Conference on Computer Vision.
     Conference on Acoustics, Speech and Signal Processing (ICASSP).
                                                                                           Springer, 2020, pp. 366–382.
     IEEE, 2020, pp. 8529–8533.
                                                                                      [29] J.-J. Lee, W. Zhang, and P. Li, “Parallel time batching: Systolic-
 [7] M. Davies, N. Srinivasa, T.-H. Lin, G. Chinya, Y. Cao, S. H. Choday,
                                                                                           array acceleration of sparse spiking neural computation,” in 2022 IEEE
     G. Dimou, P. Joshi, N. Imam, S. Jain et al., “Loihi: A neuromorphic
                                                                                           International Symposium on High-Performance Computer Architecture
     manycore processor with on-chip learning,” Ieee Micro, vol. 38, no. 1,
                                                                                           (HPCA). IEEE, 2022, pp. 317–330.
     pp. 82–99, 2018.
 [8] P. Dayan and L. F. Abbott, Theoretical neuroscience: computational and           [30] H. Li et al., “Cifar10-dvs: an event-stream dataset for object classifica-
     mathematical modeling of neural systems. MIT press, 2005.                             tion,” Frontiers in neuroscience, vol. 11, p. 309, 2017.
 [9] C. Deng, Y. Sui, S. Liao, X. Qian, and B. Yuan, “Gospa: an energy-               [31] Z. Li, J. Li, T. Chen, D. Niu, H. Zheng, Y. Xie, and M. Gao, “Spada:
     efficient high-performance globally optimized sparse convolutional neu-               Accelerating sparse matrix multiplication with adaptive dataflow,” in
     ral network accelerator,” in 2021 ACM/IEEE 48th Annual International                  Proceedings of the 28th ACM International Conference on Architectural
     Symposium on Computer Architecture (ISCA). IEEE, 2021, pp. 1110–                      Support for Programming Languages and Operating Systems, Volume 2,
     1123.                                                                                 2023, pp. 747–761.
[10] S. Deng, Y. Li, S. Zhang, and S. Gu, “Temporal efficient training                [32] F. Liu, W. Zhao, Z. Wang, Y. Chen, T. Yang, Z. He, X. Yang, and
     of spiking neural network via gradient re-weighting,” arXiv preprint                  L. Jiang, “Sato: spiking neural network acceleration via temporal-
     arXiv:2202.11946, 2022.                                                               oriented dataflow and architecture,” in Proceedings of the 59th
[11] W. Fang, Y. Chen, J. Ding, Z. Yu, T. Masquelier, D. Chen, L. Huang,                   ACM/IEEE Design Automation Conference, 2022, pp. 1105–1110.
     H. Zhou, G. Li, and Y. Tian, “Spikingjelly: An open-source machine               [33] R. Mao, L. Tang, X. Yuan, Y. Liu, and J. Zhou, “Stellar: Energy-
     learning infrastructure platform for spike-based intelligence,” Science               efficient and low-latency snn algorithm and hardware co-design with
     Advances, vol. 9, no. 40, p. eadi1480, 2023.                                          spatiotemporal computation,” in 2024 IEEE International Symposium
[12] W. Fang, Z. Yu, Y. Chen, T. Huang, T. Masquelier, and Y. Tian,                        on High-Performance Computer Architecture (HPCA). IEEE, 2024,
     “Deep residual learning in spiking neural networks,” Advances in Neural               pp. 172–185.
     Information Processing Systems, vol. 34, pp. 21 056–21 069, 2021.                [34] F. Muñoz-Martı́nez, R. Garg, M. Pellauer, J. L. Abellán, M. E. Aca-
[13] J. Frankle and M. Carbin, “The lottery ticket hypothesis: Finding sparse,             cio, and T. Krishna, “Flexagon: A multi-dataflow sparse-sparse matrix
     trainable neural networks,” arXiv preprint arXiv:1803.03635, 2018.                    multiplication accelerator for efficient dnn processing,” in Proceedings
[14] S. B. Furber, F. Galluppi, S. Temple, and L. A. Plana, “The spinnaker                 of the 28th ACM International Conference on Architectural Support for
     project,” Proceedings of the IEEE, vol. 102, no. 5, pp. 652–665, 2014.                Programming Languages and Operating Systems, Volume 3, 2023, pp.
[15] A. Gondimalla, N. Chesnut, M. Thottethodi, and T. Vijaykumar,                         252–265.
     “Sparten: A sparse tensor accelerator for convolutional neural networks,”        [35] N. Muralimanohar, R. Balasubramonian, and N. P. Jouppi, “Cacti 6.0:
     in Proceedings of the 52nd Annual IEEE/ACM International Symposium                    A tool to model large caches,” HP laboratories, 2009.
     on Microarchitecture, 2019, pp. 151–165.                                         [36] S. Narayanan, K. Taht, R. Balasubramonian, E. Giacomin, and P.-
[16] S. Han, X. Liu, H. Mao, J. Pu, A. Pedram, M. A. Horowitz, and                         E. Gaillardon, “Spinalflow: An architecture and dataflow tailored for
     W. J. Dally, “Eie: Efficient inference engine on compressed deep neural               spiking neural networks,” in 2020 ACM/IEEE 47th Annual International
     network,” ACM SIGARCH Computer Architecture News, vol. 44, no. 3,                     Symposium on Computer Architecture (ISCA). IEEE, 2020, pp. 349–
     pp. 243–254, 2016.                                                                    362.
[17] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image           [37] E. O. Neftci, H. Mostafa, and F. Zenke, “Surrogate gradient learning
     recognition,” in CVPR, 2016.                                                          in spiking neural networks: Bringing the power of gradient-based opti-
[18] K. Hegde, H. Asghari-Moghaddam, M. Pellauer, N. Crago, A. Jaleel,                     mization to spiking neural networks,” IEEE Signal Processing Magazine,
     E. Solomonik, J. Emer, and C. W. Fletcher, “Extensor: An accelerator for              vol. 36, no. 6, pp. 51–63, 2019.
     sparse tensor algebra,” in Proceedings of the 52nd Annual IEEE/ACM               [38] E. O. Neftci, B. U. Pedroni, S. Joshi, M. Al-Shedivat, and G. Cauwen-
     International Symposium on Microarchitecture, 2019, pp. 319–333.                      berghs, “Stochastic synapses enable efficient brain-inspired learning
[19] K. Hegde, J. Yu, R. Agrawal, M. Yan, M. Pellauer, and C. Fletcher,                    machines,” Frontiers in neuroscience, vol. 10, p. 241, 2016.
     “Ucnn: Exploiting computational reuse in deep neural networks via                [39] S. Pal, J. Beaumont, D.-H. Park, A. Amarnath, S. Feng, C. Chakrabarti,
     weight repetition,” in 2018 ACM/IEEE 45th Annual International Sym-                   H.-S. Kim, D. Blaauw, T. Mudge, and R. Dreslinski, “Outerspace:
     posium on Computer Architecture (ISCA). IEEE, 2018, pp. 674–687.                      An outer product based sparse matrix multiplication accelerator,” in
[20] S. Kim, S. Park, B. Na, and S. Yoon, “Spiking-yolo: spiking neural                    2018 IEEE International Symposium on High Performance Computer
     network for energy-efficient object detection,” in Proceedings of the                 Architecture (HPCA). IEEE, 2018, pp. 724–736.
     AAAI conference on artificial intelligence, vol. 34, no. 07, 2020, pp.           [40] A. Parashar, M. Rhu, A. Mukkara, A. Puglielli, R. Venkatesan,
     11 270–11 277.                                                                        B. Khailany, J. Emer, S. W. Keckler, and W. J. Dally, “Scnn: An




                                                                                 13
     accelerator for compressed-sparse convolutional neural networks,” ACM               [61] R. Yin, Y. Li, A. Moitra, and P. Panda, “Mint: Multiplier-less integer
     SIGARCH computer architecture news, vol. 45, no. 2, pp. 27–40, 2017.                     quantization for energy efficient spiking neural networks,” in 2024 29th
[41] A. Parashar, M. Rhu, A. Mukkara, A. Puglielli, R. Venkatesan,                            Asia and South Pacific Design Automation Conference (ASP-DAC).
     B. Khailany, J. Emer, S. W. Keckler, and W. J. Dally, “Scnn: An                          IEEE, 2024, pp. 830–835.
     accelerator for compressed-sparse convolutional neural networks,” ACM               [62] G. Zhang, N. Attaluri, J. S. Emer, and D. Sanchez, “Gamma: Leveraging
     SIGARCH computer architecture news, vol. 45, no. 2, pp. 27–40, 2017.                     gustavson’s algorithm to accelerate sparse matrix multiplication,” in
[42] E. Qin, A. Samajdar, H. Kwon, V. Nadella, S. Srinivasan, D. Das,                         Proceedings of the 26th ACM International Conference on Architectural
     B. Kaul, and T. Krishna, “Sigma: A sparse and irregular gemm ac-                         Support for Programming Languages and Operating Systems, 2021, pp.
     celerator with flexible interconnects for dnn training,” in 2020 IEEE                    687–701.
     International Symposium on High Performance Computer Architecture                   [63] W. Zhang and P. Li, “Temporal spike sequence learning via backprop-
     (HPCA). IEEE, 2020, pp. 58–70.                                                           agation for deep spiking neural networks,” NeurIPS, 2020.
[43] K. Roy, A. Jaiswal, and P. Panda, “Towards spike-based machine                      [64] Z. Zhang, H. Wang, S. Han, and W. J. Dally, “Sparch: Efficient
     intelligence with neuromorphic computing,” Nature, 2019.                                 architecture for sparse matrix multiplication,” in 2020 IEEE Interna-
[44] A. Samajdar, J. M. Joseph, Y. Zhu, P. Whatmough, M. Mattina, and                         tional Symposium on High Performance Computer Architecture (HPCA).
     T. Krishna, “A systematic methodology for characterizing scalability of                  IEEE, 2020, pp. 261–274.
     dnn accelerators using scale-sim,” in 2020 IEEE International Sympo-                [65] H. Zheng et al., “Going deeper with directly-trained larger spiking neural
     sium on Performance Analysis of Systems and Software (ISPASS). IEEE,                     networks,” in AAAI, 2021.
     2020, pp. 58–68.
[45] A. Sarma, S. Singh, H. Jiang, A. Pattnaik, A. K. Mishra, V. Narayanan,
     M. T. Kandemir, and C. R. Das, “Exploiting activation based gradient
     output sparsity to accelerate backpropagation in cnns,” arXiv preprint
     arXiv:2109.07710, 2021.
[46] A. Sengupta, Y. Ye, R. Wang, C. Liu, and K. Roy, “Going deeper in
     spiking neural networks: Vgg and residual architectures,” Frontiers in
     neuroscience, vol. 13, p. 95, 2019.
[47] K. Sewell, R. G. Dreslinski, T. Manville, S. Satpathy, N. Pinckney,
     G. Blake, M. Cieslak, R. Das, T. F. Wenisch, D. Sylvester et al.,
     “Swizzle-switch networks for many-core systems,” IEEE Journal on
     Emerging and Selected Topics in Circuits and Systems, vol. 2, no. 2,
     pp. 278–294, 2012.
[48] L. Shi, J. Pei, N. Deng, D. Wang, L. Deng, Y. Wang, Y. Zhang, F. Chen,
     M. Zhao, S. Song et al., “Development of a neuromorphic computing
     system,” in 2015 IEEE international electron devices meeting (IEDM).
     IEEE, 2015, pp. 4–3.
[49] Y. Shi, L. Nguyen, S. Oh, X. Liu, and D. Kuzum, “A soft-pruning
     method applied during training of spiking neural networks for in-
     memory computing applications,” Frontiers in neuroscience, vol. 13,
     p. 405, 2019.
[50] K. Simonyan and A. Zisserman, “Very deep convolutional networks for
     large-scale image recognition,” arXiv:1409.1556, 2014.
[51] N. Srivastava, H. Jin, J. Liu, D. Albonesi, and Z. Zhang, “Matraptor:
     A sparse-sparse matrix multiplication accelerator based on row-wise
     product,” in 2020 53rd Annual IEEE/ACM International Symposium on
     Microarchitecture (MICRO). IEEE, 2020, pp. 766–780.
[52] C. Stöckl and W. Maass, “Optimized spiking neurons can classify images
     with high accuracy through temporal coding with two spikes,” Nature
     Machine Intelligence, vol. 3, no. 3, pp. 230–238, 2021.
[53] P. J. Werbos, “Backpropagation through time: what it does and how to
     do it,” Proceedings of the IEEE, vol. 78, no. 10, pp. 1550–1560, 1990.
[54] D. Wu, J. Li, R. Yin, H. Hsiao, Y. Kim, and J. San Miguel,
     “Ugemm: Unary computing architecture for gemm applications,” in
     2020 ACM/IEEE 47th Annual International Symposium on Computer
     Architecture (ISCA). IEEE, 2020, pp. 377–390.
[55] Y. N. Wu, P.-A. Tsai, A. Parashar, V. Sze, and J. S. Emer, “Sparseloop:
     An analytical approach to sparse tensor accelerator modeling,” in
     2022 55th IEEE/ACM International Symposium on Microarchitecture
     (MICRO). IEEE, 2022, pp. 1377–1395.
[56] Y. Wu, L. Deng, G. Li, J. Zhu, and L. Shi, “Spatio-temporal backpropa-
     gation for training high-performance spiking neural networks,” Frontiers
     in neuroscience, vol. 12, p. 331, 2018.
[57] Y. Wu, L. Deng, G. Li, J. Zhu, Y. Xie, and L. Shi, “Direct training for
     spiking neural networks: Faster, larger, better,” in Proceedings of the
     AAAI conference on artificial intelligence, vol. 33, no. 01, 2019, pp.
     1311–1318.
[58] M. Yao, J. Hu, Z. Zhou, L. Yuan, Y. Tian, B. Xu, and G. Li, “Spike-
     driven transformer,” Advances in neural information processing systems,
     vol. 36, 2024.
[59] R. Yin et al., “Sata: Sparsity-aware training accelerator for spiking neural
     networks,” IEEE Transactions on Computer-Aided Design of Integrated
     Circuits and Systems, vol. 42, no. 6, pp. 1926–1938, 2022.
[60] R. Yin et al., “Workload-balanced pruning for sparse spiking neural
     networks,” IEEE Transactions on Emerging Topics in Computational
     Intelligence, 2024.




                                                                                    14
