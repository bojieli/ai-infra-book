 Extending GPU Ray-Tracing Units for Hierarchical
              Search Acceleration
                                       Aaron Barnes, Fangjia Shen, Timothy G. Rogers
                                  Elmore Family School of Electrical and Computer Engineering
                                                       Purdue University
                                                   West Lafayette, IN, USA
                                          {barnes88, shen449, timrogers}@purdue.edu


   Abstract—Specialized ray-tracing acceleration units have be-
come a common feature in GPU hardware, enabling real-time
ray-tracing of complex scenes for the first time. The ray-tracing
unit accelerates the traversal of a hierarchical tree data structure
called a bounding volume hierarchy to determine whether rays
have intersected triangle primitives. Hierarchical search algo-
rithms are a fundamental software pattern common in many
important domains, such as recommendation systems and point
cloud registration, but are difficult for GPUs to accelerate because                 HNSW              BVH        K-D Tree
they are characterized by extensive branching and recursion.           Fig. 1: Hierarchical Navigable Small World (HNSW)
The ray-tracing unit overcomes these limitations with specialized      Graphs, Bounding Volume Hierarchy, and K-D tree hier-
hardware to traverse hierarchical data structures efficiently, but
is mired by a highly specialized graphics API, which is not            archical data structures are commonly used to accelerate
readily adaptable to general-purpose computation. We present           search on GPUs and have similar branching characteristics
the Hierarchical Search Unit (HSU), a flexible datapath to             which make them difficult to execute on SIMT execution
accelerate a more general class of hierarchical search algorithms,     units. Current GPU ray tracing units only accelerate
of which ray-tracing is one. We synthesize a baseline ray-             BVH traversal, which has limited effectiveness at higher
intersection datapath and maximize functional unit reuse while
extending the ray-tracing unit to support additional computations      dimensions and with non ray-tracing workloads.
and a more general set of instructions. We demonstrate that
the unit can improve the performance of three hierarchical
search data structures in approximate nearest neighbors search         highly thread-divergent workloads (which is a common char-
algorithms and a B-tree key-value store index. For a minimal
extension to the existing unit, our HSU improves the state-of-         acteristic of RT workloads [8], [11]), and it provides fixed
the-art GPU approximate nearest neighbor implementation by an          function datapaths to perform complete ray-box and ray-
average of 24.8% using the GPU’s general computing interface.          triangle intersection tests with a single instruction [1]. These
   Index Terms—GPU, Ray-Tracing, BVH, Nearest Neighbors                RT accelerators implement CISC instructions that fetch data
                                                                       from memory and perform several calculations before return-
                       I. I NTRODUCTION                                ing the result. The ray-tracing problem requires searching a
   Ray-traced (RT) rendering generates photo-realistic images          large set of primitives and testing for intersections with a
by casting millions of rays into a scene and generating                given ray. In this work, we capitalize upon the observation
pixel colors based on the objects they intersect with [23],            that this search-and-compute software pattern is not limited to
[64]. While such techniques often generate computer graphics           ray tracing applications. The dedicated hardware to accelerate
of a higher quality than traditional rasterization techniques,         ray tracing can be easily extended to benefit more general
the large number of ray-triangle intersection computations             GPU search algorithms that may use different hierarchical data
required had prevented their adoption in applications with real-       structures.
time rendering requirements until the introduction of dedicated           Presently RT unit resources on Nvidia and AMD GPUs
RT hardware accelerators [44]. To reduce the number of                 are only accessible via graphics library APIs such as Optix,
intersection tests required for a given ray, scene primitives          Vulkan, or DirectX. This reduces RT unit accessibility for
(commonly triangles) are organized in data structures such as          scientific application developers who may be familiar with
Bounding Volume Hierarchies (BVH), which allow the search              CUDA but not graphics pipelines. However, several works
space to be culled by hierarchically eliminating candidate             have successfully adapted the use of RT units to acceler-
primitives. RT units accelerate the traversal of BVH trees, and        ate non-graphics workloads [6], [15], [21], [38], [40], [63],
the ray-triangle intersection tests computed upon reaching a           [71], [74]. While this requires significant programming effort,
leaf node.                                                             adapting the problem to execute on the GPU RT unit can
   There are two primary benefits the RT unit provides over            unlock substantial performance gains. For example, Zhu [74]
traditional GPU SIMT hardware: it improves throughput in               reformulates the nearest neighbors problem to use RT units
