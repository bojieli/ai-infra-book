 IvLeague: Side Channel-resistant Secure Architectures Using
        Isolated Domains of Dynamic Integrity Trees
                                         Md Hafizul Islam Chowdhuryy and Fan Yao
                                                     University of Central Florida
                                               hafizul.islam@ucf.edu, fan.yao@ucf.edu


    Abstract—Modern secure processors rely on hardware-assisted        security [17], [18], [19], [20], [21], [22], [23], [24]. As
encryption and tree-based integrity verification to protect off-chip   industry and academia increasingly advocate secure-by-design
data. However, despite extensive research on performance opti-         architectures [1], [25], [26], [27], it is imperative to investigate
mization, there is a significant lack of emphasis on side channel
vulnerabilities in secure architectures. Given the strong focus on     the impact of security mechanisms as they are integrated and
data security, it is critical to ensure that the integration of new    to enhance data security holistically in future systems.
design elements into secure architectures does not inadvertently          Although prior works have demonstrated microarchitectural
introduce additional vulnerabilities.                                  attacks in secure processors [12], [28], [29], [30], they gener-
    Existing integrity verification mechanisms use a global in-        ally exploit known vulnerabilities that are already manifested
tegrity tree shared across security domains, which can introduce
side channel leakage through integrity tree metadata sharing. In       in classical settings (e.g., timing channels on caches [9],
this work, we present IvLeague framework – a novel integrity ver-      [10]). As such, they do not necessarily expand the current
ification mechanism for side channel-resistant isolated integrity      microarchitecture attack surface. In fact, commercial-off-the-
trees among dynamic domains in secure processors. Specifically,        shelf SGX hardware explicitly excludes side channels from its
IvLeague splits the global tree into multiple fixed-size subtrees,     threat model [1], stating that microarchitecture security should
dynamically allocating these subtrees to domains during runtime.
IvLeague enables efficient runtime scaling of memory coverage          be handled separately. Unfortunately, security of microarchi-
for individual domains. Additionally, we IvLeague-Invert, an           tecture cannot be treated as a standalone problem in secure
optimization which shortens the integrity verification path by         architectures [31], [32]. Specifically, the recent research [32]
mapping data pages to high-level tree nodes. Finally, IvLeague-        unveils that the integrity verification (IV) mechanism using
Pro further improves the integrity verification of hotpages by         tree-based metadata in secure processors introduces new side
enabling efficient hotpage tracking and migrating hotpages closer
to the root. We extensively evaluate all three IvLeague schemes        channel leakage by design. With a global IV tree, mem-
using 16 real-world workloads with varying memory footprints.          ory accesses in one domain (e.g., an enclave) unavoidably
IvLeague scheme, along with its optimizations, demonstrates a          exercise integrity tree nodes at certain levels shared with
5%-19% speedup over the insecure baseline, while providing             other domains [32]. This implicit metadata sharing enables
effective side channel protection for the integrity tree. Moreover,    new shared-memory side channels even when regular data
IvLeague ensures high utilization of TreeLings (over 99.5%) and
supports workloads with highly skewed memory footprints.               sharing is prohibited among domains (i.e., to defeat existing
    Index Terms—Secure architectures, Metadata Side channels,          attacks such as Flush+Reload [13], [33], [34]). Such leakage
Trusted execution environment, Isolated integrity trees.               in secure processors exacerbates microarchitecture security,
                                                                       and more concerningly, cannot be effectively mitigated by
                       I. I NTRODUCTION                                directly adopting existing defenses such as resource access
   Trusted computing has drawn considerable attention due to           randomization and partitioning [35], [36], [37]. In contrast to
the growing concerns of trust in remote computing platforms            prior side channels that predominantly exploit the sharing of
(e.g., cloud services). State-of-the-art mechanisms employ             hardware resources, this vulnerability stems from a new source
secure architectures that take the processor as the root-of-trust      of sharing–metadata.
and employ hardware-based encryption and integrity verifi-                This paper aims to thwart side channels that exploit shared
cation to offer strong data protection. Best-practice solutions        security metadata in secure processors [32]. Since the underly-
such as Intel SGX [1] provide a trusted execution environment          ing vulnerability is the use of a global integrity tree, a plausible
(TEE) that protects program execution in enclaves against              solution is to enforce IV metadata isolation among security
adversaries that can compromise off-chip hardware [2] and              domains. While statically partitioning the integrity tree can be
privileged software [3], [4], [5], [6], [7]. While protecting          a straightforward approach [31], such a mechanism does not
off-chip data is essential, ensuring on-chip data security is of       allow runtime scaling of the number and size of secure do-
paramount importance. Recent advances in microarchitectural            mains. Ideally, an isolated integrity tree per domain should be
attacks (for example, timing channels) [8], [9], [10], [11], [12],     maintained to prevent metadata sharing across domains. This
[13], [14], [15], [16] highlight that program secrets can be           tree should also be adjusted at runtime in order to cover the dy-
severely exfiltrated by attackers by modulating microarchi-            namic range of memory footprints for the domain. However,
tectural states in various on-chip hardware resources, which           partitioning integrity trees and dynamically managing them at
leads to numerous proposals on protecting microarchitecture            runtime can introduce several main challenges: i) non-trivial
