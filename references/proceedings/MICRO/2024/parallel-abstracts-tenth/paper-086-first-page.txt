      Veiled Pathways: Investigating Covert and Side
              Channels within GPU Uncore
          Yuanqing Miao                      Yingtian Zhang                          Dinghao Wu                     Danfeng Zhang
       Penn State University              Penn State University                 Penn State University                Duke Univeristy
     University Park, PA, USA           University Park, PA, USA              University Park, PA, USA            Durham, NC, USA
        ypm5112@psu.edu                     yjz5396@psu.edu                       dinghao@psu.edu               danfeng.zhang@duke.edu

        Gang Tan                                     Rui Zhang                               Mahmut Taylan Kandemir
  Penn State University                        Penn State University                            Penn State University
University Park, PA, USA                     University Park, PA, USA                         University Park, PA, USA
      gtan@psu.edu                               rmz5227@psu.edu                                   mtk2@psu.edu



   Abstract—With the emergence of GPUs as first-class compute             neural networks and provide speedy and accurate inference
engines, more concentrated focus has been put into covert and             results for users.
side channel discovery in these architectures. However, most of              With the advent of GPUs, vulnerabilities on these powerful
the covert and side channels uncovered on GPUs to date are
rooted in “GPU cores”, which include computational cores, cache           and widely-used compute engines require more attention, so as
and core interconnects, but they do not consider “GPU uncore”,            to prevent costly errors and faults. Among the vulnerabilities,
which include non-computational engines, GPU DRAM, host-                  “side channels” and “covert channels” stand out, as these
GPU links and inter-GPU links.                                            unintended information channels cannot be simply mitigated
   In this paper, we delve into the less-explored domains of              by adding software patches. Such vulnerabilities root from
GPU uncore, unveiling four novel leakage sources for covert and
side channel exploitation: (1) GPU DRAM frequency scaling;                the intrinsic design patterns in architecture – sharing different
(2) NVENC utilization; (3) NVDEC utilization; (4) NVJPEG                  device components among different processes to maximize
utilization. What makes these covert and side channels interesting        device utilization and throughput. Side channels and covert
is that they all take effect under the GPU MPS mode –                     channels utilize the information such as time [23], [41],
which fractionalizes GPU cores and GPU memory on both                     [64], [89], temperature [61], electromagnetism [59], [90], and
desktop-scale and server-scale GPUs. Furthermore, our study
reevaluates PCI-e bandwidth allocation on GPUs. Notably, we               acoustics [22] that are unintentionally leaked. Side channels
have engineered covert and side channel capable of bypassing              are built by attacker to steal secrets on victim, and they
GPU MIG isolation – a mechanism implemented by NVIDIA to                  usually rely on the “gadgets” in victim code that has secret-
physically segregate hardware resources on server-scale GPUs.             dependent footprint on microarchitecture. Contrasting with
Our research showcases concrete examples of these covert and              side channels, a covert channel is built by a “sender-receiver”
side channels, highlighting their potency in breaching system
security, all achieved without necessitating root privileges. This        pair to transmit information secretly across the system. A
underscores the practical implications and urgency of addressing          lot of efforts from both academia and industry have been
these vulnerabilities in GPU architectures.                               put into discovering [23], [40], [41], [44], [50], [54], [57],
   Index Terms—Side channel leakage, GPU, MPS, MIG, video                 [68], [75], [82], [83], [86], [89] and/or mitigating [24],
encoder, video decoder                                                    [55], [56], [60], [63], [71], [87] side channels and covert
                                                                          channels on CPUs. Compared to CPUs, GPUs, particularly
                       I. I NTRODUCTION                                   those manufactured by NVIDIA, do not have as many
   As a Single-Instruction-Multiple-Threads (SIMT) processor              open-source documents, preventing themselves from being
optimized for throughput-sensitive tasks, GPUs have already               studied thoroughly for potential information leakage channels.
demonstrated performance superiority across numerous                      However, with GPUs gaining popularity and widespread usage
application domains. In fact, from small mobile devices to                in different application domains, the GPU-based side channels
large data centers, GPUs have proven to be both essential and             and covert channels certainly deserve more research.
irreplaceable. Originally designed for graphics processing, the              Motivated by the observations above, in this paper, we
parallel architecture of a GPU enables it to simultaneously               focus on GPU covert and side channels. Previous work has
process millions of pixels, resulting in vivid and life-like visual       studied the insufficient isolation that gives rise to covert
experiences. GPUs have also emerged as the backbone of                    channels on shared memories [47], caches [46], [64], [65] and
modern machine learning (ML) ecosystems, particularly with                interconnects [20], [38] in GPUs. However, there are further
the rapid expansion of ML models, algorithms, and datasets.               areas of concern in GPUs, especially in the “uncore” part,
In particular, deep learning algorithms rely heavily on the               which excludes the CUDA engine (with cache) but includes
massive parallel processing capabilities of GPUs to train large           GPU DRAM, video decoding/encoding engines, JPEG image



                                                                      1
