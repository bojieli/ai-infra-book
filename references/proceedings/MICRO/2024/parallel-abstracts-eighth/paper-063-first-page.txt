    Chaining Transactions for Effective Concurrency
    Management in Hardware Transactional Memory
       Vı́ctor Nicolás-Conesa, Rubén Titos-Gil, Ricardo Fernández-Pascual, Manuel E. Acacio, Alberto Ros

                           Computer Engineering Department, University of Murcia, Murcia, Spain
         Email: victor.nicolasc@um.es, rtitos@um.es, rfernandez@ditec.um.es, meacacio@um.es, aros@ditec.um.es



    Abstract—Hardware Transactional Memory (HTM) offers the           commit [18], [44]. Though transactions might fail for a variety
opportunity to ease parallel programming. However, driven             of reasons, like lack of buffering resources or the need to
by hardware limitations, commercial implementations eschew            handle hardware interrupts, the prevalent cause of aborts
the complexity involved in early sophisticated proposals from
academia, and, among other things, opt for simple conflict            are conflicts. In fact, a frequent critique of HTM today is
resolution policies that inevitably increase transaction aborts. To   that system performance quickly deteriorates when multiple
increase thread level parallelism, previous works propose conflict    transactions are trying to access the same data at the same
resolution schemes that, instead of aborting, add a second level      time and, at least one of them, is trying to update it. Behind
of speculation consisting in using not-yet-committed data from        this shortcoming lays the decision adopted by manufacturers
another transaction. This policy, which we refer to as requester-
speculates, has not yet been considered in the context of the kind    of employing the simplest possible approach for resolving
of best-effort HTM support provided by commercial processors.         conflicts, which consists in rolling back a transaction whenever
    This work proposes CHAining TransactionS (CHATS), a               it observes a remote memory access to a block in its read or
simple yet effective realization of the requester-speculates con-     write sets that would threaten atomicity [9], [32], [38], [44]. As
flict resolution policy in which cyclic dependencies between          a result of this choice, programs using existing HTM support
transactions are avoided and the commit ordering respects the
dependencies that transactions make once speculative values           are liable to experience livelocks [9], [31], [32], [44]. To
are communicated. The ultimate result is a best-effort HTM            escape from them, the use of HTM support must be combined
implementation that forces a partial order between transactions       with an alternative fallback path employing traditional locks to
in a way that ensures effective utilization of forwarded data         ensure forward progress [10], [44]. Because mutual exclusion
and that gets away from the complexity of previous proposals.         is required to guarantee the atomicity of transactions executed
Simulations using gem5 demonstrate the effectiveness of CHATS
in both commercial-like setups and academic state-of-the-art          non-speculatively, transactional workloads often take a severe
best-effort systems (22% and 16% reduction in execution time,         performance hit as soon as moderate contention arises and the
on average, respectively). These improvements are achieved by         fallback path is taken more frequently [7], [9], [10], [44].
requiring less than 280 bytes of extra storage.                          An alternative approach to manage conflicts, rather than
                                                                      aborting at least one of the transactions involved, is to allow
                       I. I NTRODUCTION                               the requester to speculate past the conflicting access as if
   Beginning roughly two decades ago with the advent of               no conflict had happened, so that a transaction can consume
multicores, Hardware Transactional Memory (HTM) sparked               not-yet-committed data from another transaction (or update
great interest among researchers, who picked up on the seminal        data that is still in its read-write set). This conflict resolu-
work by Herlihy and Moss [17] because of its potential                tion policy, referred to in this paper as requester-speculates,
to simplify synchronization in parallel programs. The 2000s           unlocks additional concurrency and, as shown in this work,
witnessed a myriad of publications in transactional memory            can achieve conflict serializability and ensure forward progress
(TM), where researchers attempted to broaden the kinds of             by means of simple mechanisms that respect the producer-
transactions supported in hardware with the goal of simpli-           consumer dependencies created by the forwardings. Several
fying their use as a synchronization primitive, for instance,         prior works have exploited this idea [2], [29], [34], [35],
by accommodating large, coarse-grain transactions or ensuring         [39], yet they represent a significant departure in terms of
progress despite high contention. Years later, the semiconduc-        hardware complexity from a typical best-effort HTM: they
tor industry began to commercialize HTM-equipped chips [1],           modify critical components such as the coherence protocol
[18], [20], [23], [42] that have proved significant advantages        (e.g., extra states), the communication fabric (e.g., dedicated
over traditional locks in specific niches [22], [43].                 signals/messages to enforce commit order) or virtual memory.
   In spite of such success stories, the limitations exhibited by        Part of the complexity brought by prior works implement-
most commercial HTM implementations, commonly referred                ing requester-speculates stems from the desire to allow the
to as best-effort HTM, have prevented a broader shift away            speculative forwarding of data that has itself been received
from locks. This is because HTM support found in real-                speculatively, which requires tracking multiple versions of the
world chips gives no assurances that a transaction will ever          same conflicting cache block [34], [35]. This means that cache
