StreamTensor: Make Tensors Stream in Dataflow Accelerators for LLMs                                 MICRO ’25, October 18–22, 2025, Seoul, Republic of Korea


                                                                  Automated        a linear programming (LP) algorithm to solve this problem,
   Application        Dataflow Kernels                             Manual          reducing resource utilization while avoiding deadlock.
                    ADL/HLS/HDL     DSL                                        (5) We evaluate StreamTensor on FPGA platforms with LLMs and
                                                                                   observe up to 0.76x and 0.64x lower latency compared to the
                      Dataflow Kernels
                                                Hardware        App-specific
                                                                                   state-of-the-art FPGA LLM accelerators and GPUs, and up to
                                                Compiler        Accelerator        1.99x higher energy efficiency compared to GPUs.
                     Layout Converters
      Dataflow
      Compiler
                      DMAs and FIFOs                                           2   StreamTensor Framework
                                                Software            DSA
                                                Compiler         Executable
                     Dataflow Schedule                                         StreamTensor is a compilation framework designed to transform
  StreamTensor
                                                                               PyTorch models into optimized dataflow implementations. It is
                                                                               built upon the MLIR [36] compilation framework. The overall ar-
Figure 3: Proposed paradigm of dataflow accelerator design.                    chitecture of StreamTensor is depicted in Figure 4. The compilation
                                                                               process begins with a PyTorch model from Torch-MLIR [55] and
                                                                               proceeds through several stages. Initially, tensor operations are
1.4     Our Proposal                                                           converted into a structured Intermediate Representation (IR) us-
Due to the pitfalls described in Section 1.3, the current paradigm             ing MLIR’s built-in Linear Algebra (Linalg) operations. This IR
shown in Figure 2 is difficult to scale up to large dataflow accelera-         is then optimized by MLIR’s Linalg passes like element-wise op-
tors. Therefore, we propose a shift in the design paradigm shown in            eration fusion. StreamTensor subsequently applies Design Space
Figure 3. We do not advocate for full automation, as ADL/HLS/HDL               Exploration (DSE) algorithms to determine optimal tiling strate-
or DSLs remain essential for designing individual dataflow kernels,            gies, considering factors such as tile sizes, unrolling factors, and
such as local buffers and vectorization. However, once individual              permutations based on computational patterns. The Linalg IR is
kernels are designed or generated, we argue that compilers should              then transformed into a dataflow IR, where computations are or-
automatically generate the dataflow schedule, assemble the ker-                ganized as hierarchical tasks. All dataflow components, including
nels into an application-level dataflow accelerator, and resolve the           DMAs, stream layout converters, and FIFOs, are generated during
pitfalls identified in Section 1.3 algorithmically. This is analogous          this stage. Critical optimizations are also performed here, such as
to the GPU software ecosystem, where DSLs like CUDA and Tri-                   stream-based kernel fusion to minimize external memory access
ton [54] are used to design or auto-tune individual GPU kernels,               and FIFO sizing to balance producer-consumer executions. In the
while kernel assembly and scheduling are handled automatically                 final stages, StreamTensor generates hardware-specific code and
by compilers, resulting in a programming paradigm that is both                 a host runtime. StreamTensor handles memory allocation, stream
efficient and scalable.                                                        connectivity, and directive materialization, which allows vendor
   In this spirit, we propose StreamTensor, a compiler that enables            compilers like HLS to generate the target dataflow architectures.
automatic tensor streaming in dataflow architectures. This paper de-           Concurrently, it produces host runtime code that manages data
scribes how each pitfall is addressed in a systematic and hierarchical         transfer, kernel execution, and synchronization between the host
manner. As a pioneering work, StreamTensor proposes algorithmic                CPU and the dataflow accelerator.
solutions for each challenge and demonstrates their effectiveness
through large benchmarks. While these solutions may not be op-                 3 Intermediate Representation
timal, they clearly expose well-defined optimization subproblems
                                                                               3.1 Typing System
and enable co-optimization opportunities across different design
spaces. Overall, this paper makes the following contributions:                 StreamTensor introduces a typing system to enable efficient verifica-
                                                                               tion and optimization of the IR. Through StreamTensor’s dedicated
(1) We propose StreamTensor, the first PyTorch-to-device dataflow              type and operation verifiers, the typing system helps ensure the
    compiler that automatically generates stream-based dataflow                IR’s validity after any transformation pass is applied.
    accelerators and their corresponding runtime systems.
(2) We propose an iterative tensor (itensor) type that systemat-               3.1.1 Motivation. Traditionally, tensor type encodes a data type
    ically encodes the stream information for the first time. This             and a list of integers representing its shape [16, 28, 36, 46]. Tensors
    typing system forms the foundation for stream-based kernel                 can be accessed in a memory-mapped manner, e.g., a slice can be ex-
    fusion and dataflow component generation, improving the scal-              tracted or inserted based on offsets and its shape. However, dataflow
    ability and productivity of dataflow accelerator design.                   kernels communicate via FIFOs, which enforce a strict access order
(3) We propose three design spaces, including tensor tiling space,             and follow a streamed access pattern rather than a memory-mapped
    kernel fusion space, and resource allocation space, that cover             one. Consequently, traditional tensor types may fail to ensure cor-
    the sophisticated design space of dataflow architecture in an              rectness in dataflow communication. Even when a producer and a
    algorithmic and hierarchical manner. We further propose an                 consumer share the same tensor type, the stream access order may
    exploration algorithm for each design space to reduce resource             remain ambiguous, causing unintended behaviors. For example,
    utilization and improve latency and throughput.                            in Graphene [28], the tensor type only encodes memory-mapped
(4) We propose a piecewise function-based token behavior model                 layout. As a result, a mismatch between a producer’s row-major
    that transforms the dataflow FIFO sizing problem of dataflow               stream generation and a consumer’s column-major expectation,
    accelerators into a scheduling problem. We further propose                 when both operate on the same tensor type, leads to incorrect data
