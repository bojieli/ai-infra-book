LLM.265: Video Codecs are Secretly Tensor Codecs                                                        MICRO ’25, October 18–22, 2025, Seoul, Republic of Korea


Table 1: Accuracy and average bits of LLaMA-3-70B [1] after
compression using different algorithms evaluated on PIQA,
                                                                                            0.4675            H.264




                                                                            Average Norm.
WinoGrande (W.G.) and HellaSwag (H.S.) datasets. "128G"
                                                                                                              H.265




                                                                              Accuracy
denotes quantizing each group of 128 values separately.                                     0.4650            AV1
       # Avg. Bits        Algorithm           PIQA    W.G.   H.S.                           0.4625
           16                 –                82.4   80.6   66.4                           0.4600
           3.25
                         GPTQ-128G             80.6   77.1   63.5                                          1.6              1.8                  2.0
                         AWQ-128G              81.4   78.6   63.5                                                    Average Bit Width
                            GPTQ               79.5   66.1   62.8
           3.00
                            AWQ                80.1   67.6   62.5
           2.88        LLM.265 (Ours)         81.5    77.5   63.7          Figure 6: Comparison of the tensor compression information
                                                                           efficiency of the three most popular video codecs: H.264,
                                                                           H.265, and AV1. At a reasonable storage budget, the difference
                                                                           in information efficiency between all three codecs is so small
                                                                           that can be attributed to noise.
using the LM Evaluation Harness [27]. These tasks include PIQA [9],
COPA [66], ARC-easy and ARC-challenge [13], WinoGrande [69],
HellaSwag [87], RTE [81], and OpenbookQA [53]. For baselines, we              GPU Gen.                 H.264          H.265             AV1             VP9
compared LLM.265 with two state-of-the-art quantization methods:             Ada Lovelace            4K Enc/Dec.    8K Enc/Dec.      8K Enc/Dec.       8K Dec
                                                                               Ampere                4K Enc/Dec.    8K Enc/Dec.           -            8K Dec
GPTQ [25] and AWQ [42]. As these baselines were calibrated using
                                                                                Volta                4K Enc/Dec.    8K Enc/Dec.           -            8K Dec
a few samples from WikiText-2 [52], we excluded the measurement
on the calibration dataset from our experiments.                                Table 2: GPU Support for Different Video Codecs [14]
   LLaMA-2-7B. In Figure 5, we compare LLM.265 and its fixed
bitrate variant against other baselines. Our method significantly
outperforms all baselines, maintaining full precision accuracy with
                                                                           4.1.2 Applying LLM.265 on a Variety of Models and Tasks Other than
approximately 3 bits. In contrast, GPTQ and AWQ achieve similar
                                                                           LLM. We apply LLM.265 to a diverse set of downstream tasks; the
accuracy with around 4.25 bits. Additionally, these baselines strug-
                                                                           results are presented in Figure 7. LLM.265 consistently surpasses
gle to keep accuracy under 3 bits, while LLM.265 generalize well to
                                                                           standard quantization baselines and AWQ [42] across all tasks and
2.5 bits. Another observation is that LLM.265 outperforms its fixed
                                                                           model families, underscoring its strong generalizability.
bitrate variant by a large margin in the extremely low bit-width
regime (i.e., < 3 bits). This validates that different components in
LLMs vary in their compression difficulty, and setting different bit
                                                                           4.2          KV Cache and Communication
widths can further push the limitations of compression.                                 Compression
   LLaMA-3-70B. To verify the scalability of our method, we                While our weight compression results show the ability of serving a
present the results of the compressed LLaMA-3-70B model on three           70B model on a single commodity-level GPU, it still suffers from
datasets in Table 1. Our method achieves similar accuracy to GPTQ-         two limitations described below.
128G and AWQ-128G with 0.37 fewer bits, and outperforms the                   Long-context scenarios: The large memory requirements of
3-bit baselines without groupwise quantization with a large mar-           KV cache poses challenges [21, 74, 89] for long-context LLMs. For
gin. Here, LLM.265 benefits from its fine-grained bit-width feature,       example, storing a 128k KV cache using FP16 requires 40 GB of
reducing the bit-width to as low as 2.88 bits. In contrast, prior          GPU memory for the LLaMA-3-70B model, which is larger than the
methods are limited to integer bit-widths with separate groups for         compressed model itself.
quantization, making them less flexible.                                      On-device Inference: It is infeasible to run inference for a 70B
                                                                           model on an edge device with only 8 GB memory. We address
4.1.1 Selection of Codecs. We also investigate how compressing us-         these challenges by applying LLM.265 to KV cache and commu-
ing different video codecs affect model accuracy. Each Nvidia GPU          nication compression, enabling distributed inference for LLMs in
generation supports a distinct set of codecs, summarized in Table 2.       long-context, on-device scenarios.
We exclude VP9 [54] from our evaluation because it supports only              Here, we detailed our final results that reduce the memory foot-
hardware decoding, whereas our method requires hardware sup-               print by 5.5× and communication volumes by 4.5× for the LLaMA-
port for both encoding and decoding. We evaluate model accuracy            3-70B model using LLM.265, which only lead to a minor accuracy
under the remaining codecs; Figure 6 shows the average normalized          drop (< 2%) in the zero-shot reasoning task.
accuracy across all tasks. For practical storage budgets (> 1.8 bits          Building on our compressed LLaMA-3-70B model in §4.1, we
per element) the curves for H.264, H.265, and AV1 overlap, with            further compress the KV cache to 2.9 bits and the activations be-
variations within the noise. Consequently, we adopt H.265 for GPU          tween different pipeline parallelism stages to 3.5 bits. For baseline
experiments, as it is the most widely available across GPU gener-          comparisons, we employ RTN quantization, SpinQuant [48] and
ations, supports the highest resolutions, and delivers the greatest        QuaRot [4] to directly reduce the KV cache and activations to 3
throughput.                                                                bits and 4 bits, respectively, using asymmetric min-max dynamic
                                                                       7




                                                                    451
