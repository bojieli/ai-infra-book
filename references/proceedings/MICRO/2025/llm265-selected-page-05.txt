LLM.265: Video Codecs are Secretly Tensor Codecs                                                           MICRO ’25, October 18–22, 2025, Seoul, Republic of Korea


(a) Raw Weights (b) Intra Prediction (c) Residuals           (d) Coefficients         improve the quality further. As long as the prediction is close to the
                      Edge Predicted
                                                                                      original block, the residuals will be small in size and much easier
                 -                     =                                              to encode compared to the raw values.
                                                                                          To our surprise, the intra-frame prediction works well for com-
                                                       DCT                            pressing tensors. We present an example in Figure 4. In (a), a block
 Channel-wise        Small prediction       Small               Sparse                of a weight tensor is depicted as an image. We then performed
  Distribution           states            Residuals          Coefficients
                                                                                      H.265 encoding on this image as a frame, extracting the predic-
                                                                                      tion from the intra-frame predictor of H.265 using the HEVC Test
Figure 4: An example of a block of LLaMA-2-7B [78] weights
                                                                                      Model (HM) [33]. The predicted image is shown in (b), and the
going through the H.265 pipeline. The intra-prediction step
                                                                                      residual, which is the difference between the original block and
generates a rough prediction of the entire block, making the
                                                                                      the prediction, is shown in (c). We made three observations for
residuals easy to code with the DCT transform.
                                                                                      applying intra-frame prediction for weight images. First, the orig-
                                                                                      inal weight, when viewed as images, contains edges and planar
                                                                                      blocks that are similar to real-world images due to the channel-
the frame and removes high-frequency information that is less                         wise distribution property, as shown in prior works [4, 42, 85]. The
perceptible to human eyes. One popular technique in transform                         channel-wise distribution property means each value’s distribution
coding is the DCT transform [38], which is utilized in both H.265                     aligns with the corresponding channel, causing values close to each
and H.264. The DCT transform is based on that for an orthogonal                       other to appear within the same channel, which visually looks like
basis matrix B and input X, the encoding process can be represented                   the edges of objects. Second, the intra-frame prediction mechanism
as Y = XB, and the encoded Y can be decoded by X = YB −1 .                            can detect the channel-wise distributions and efficiently encode
    In tensor compression, however, if we view tensors as images, the                 them using small-sized prediction states. Third, the residuals after
emphasis on low-frequency signals through DCT transform may                           the intra-frame prediction are much smaller in size compared to
visually preserve similarity, but this does not translate to improved                 the original weight distribution and require much fewer states to
compression quality in terms of the accuracy of model-specific                        properly encode. This, when used in conjunction with Transform
tasks (e.g., the quality of the generated sentences). Instead, trans-                 coding and quantization as shown in Figure 4 (d), results in sparse
form coding is effective in tensor compression for a different reason                 and small coefficients that are very easy and efficient to encode
— it mitigates the encoding difficulties caused by outliers in ten-                   using only a few bits.
sors [3, 4]. Outliers that are far away from the center distribution,                     Inter-Frame Motion Prediction Does not Work. While the
sometimes even degrees of magnitude different from centered val-                      Inter-Frame prediction, including the motion prediction, achieves
ues, put conventional quantization and compression techniques in                      great efficiency in compressing videos, based on our experiments,
a dilemma that could either encode the outliers but leave the center                  it does not work for compressing tensors. As shown in Figure 2 (b)
distribution’s encoding in low resolution or clip the outliers to bet-                (5) → (6), enabling the inter-frame prediction stage does not help
ter adapt to the range of the center distribution, but not both. Prior                reduce the number of bits per value but rather increases it. This
works [19, 85] showed that suppressing the outliers or decreasing                     observation suggests there is little inter-frame pixel correlation and
the center quantization granularity both decreases the accuracy of                    little inter-layer correlation of weights in LLMs.
LLMs.
    However, transform coding solves this dilemma. In Figure 3, we
show the effect of the DCT transform: (a) exemplifies a common ten-                   3.2    LLM.265 Implementation
sor distribution where the central distribution is near-normal, but                   We implement LLM.265 on top of the PyTorch [2] framework. Mod-
outliers exist at both tails. The DCT transform solves the challenge                  ern GPUs contain specialized hardware codecs designed to encode
of encoding outliers; its output, as seen in (b), no longer contains                  and decode videos. LLM.265 utilizes NVENC and NVDEC, which
outliers. A more concrete example is shown in the process from                        are the hardware video encoders and decoders present on Nvidia
(c) to (d), where we can see that the value of 128 is an outlier in                   GPUs [59]. As NVENC and NVDEC have a maximum limit on the
(c). The DCT transform addresses this by amortizing the difficulty                    height and width of a frame, we first partition each input tensor
of encoding the outlier value 128 to other values within the same                     into multiple chunks, each corresponding to a frame. Since video
block. This results in a matrix (d) containing no outliers and is much                codecs take 8-bit integers as input, the FP16 values in the tensor
easier to encode in binary space.                                                     need to be first rounded to 8 bits using quantization before feeding
    Intra-Frame Prediction: Intra-frame prediction is another                         to HEVC codec [4, 10], with only the Luma channel of the codec
crucial component in modern video codecs. It is based on the simple                   used. As mentioned in Figure 3.1, inter-frame compression is inef-
fact that objects in the frames can be predicted or approximated                      fective; therefore, LLM.265 enforces an intra-frame-only encoding
through a few classes of patterns. For example, smooth areas of the                   by setting codec parameters.
frame can often be approximated by predicting the pixel values from                      Although LLM.265 takes advantage of the hardware encoders
neighboring pixels using a planar or DC (direct current) prediction                   and decoders of Nvidia’s GPUs, LLM.265 is still bottlenecked by the
mode. Edge areas, which are common in real-world images, can be                       limited throughput of them. This is because high performance is
predicted using directional modes that capture the orientation of                     not the key design goal of existing hardware codecs. Since people
edges. Although it is usually impossible to predict the pixels in a                   typically watch videos at resolutions lower than 4K, and at a fram-
block with very high accuracy, residual encoding can be used to                       erate lower than 60 frames per second, the hardware codecs lack
                                                                                  5




                                                                                449
