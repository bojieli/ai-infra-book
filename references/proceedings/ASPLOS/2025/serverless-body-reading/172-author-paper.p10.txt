ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands

Shaoxun Zeng, Minhui Xie, Shiwei Gao, Youmin Chen, & Youyou Lu

7.1 Models
We use 10 popular LLM models from huggingface [46], including Falcon [6], Llama2 [49], Qwen1.5 [47], and Yi [56]. Table 1 shows their parameter sizes and the total number of the
CUDA graph nodes with 35 different batch sizes. Capturing
CUDA graphs of 35 different batch sizes is a default strategy
as in vLLM [23], and we keep it in all experiments.

0.85s
Init model
structure

0.39s 0.21s
0.50s
Load model Load
Init
weights tokenizer KV cache

0.90s
Capture
phase

2.85s

(a) vLLM
Load
tokenizer

0.85s
Init model
structure

0.21s

0.52s

0.47s
bubble
Load model
weights

Init
KV cache

0.90s
Capture
phase

2.48s
7.2 Overall Performance
We evaluate the loading phase latency using three different
(b) vLLM + async
strategies in Figure 7(a), and the overall cold start latency
Init
in Figure 7(b). We make the following observations.
Load
KV cache
Warm-up
tokenizer
First, Medusa can reduce the loading phase latency effec0.02s
0.23s
0.31s
tively (Figure 7(a)). The average loading phase latency reduction is 42.5% compared with vLLM, and 34.4% compared with
0.85s
0.42s
0.26s
vLLM + async. This demonstrates that the naive asynchroInit model
Load model
Restore 1.67s
nous model weight loading is limited in the aspect of hiding
structure
weights
CUDA graph
all the latency. Instead, Medusa further enables the mate(c) Medusa
rialization of the CUDA graph, which reduces the latency
of the capturing stage (including warm-up and capturing
Figure 8. Breakdown of different strategies.
forwarding). Medusa also employs the materialization of
KV cache initialization to eliminate the dynamic profiling of
phase (including the warm-up and capturing forward) takes
available GPU memory to allocate KV cache. By reordering
0.9 seconds.
the KV cache initialization stage before the model weights
With the naive asynchronous weight loading (Figure 8(b)),
loading stage, Medusa also makes the warm-up of the capthe model weights loading is asynchronous with the tokturing stage be executed in parallel with the model weights
enizer loading as well as the KV cache initialization, reducing
loading stage, further reducing the loading phase latency.
the latency of loading phase by 13.0% compared with vLLM.
Second, by reducing the latency of the loading phase,
Medusa can reduce the overall cold start latency effectively ( Fig- We make two observations here.
First, model weights loading and the KV cache initializaure 7(b)), with 34.9% on average. The loading phase constition exhibit mutual interference, increasing longer latency in
tutes the predominant portion of the overall cold start latency.
model weights loading stage, i.e., 0.08 seconds. We observe
As a result, by reducing the overhead of the loading phase,
similar interference as in other model’s. The reason is that
Medusa eventually reduces the overall latency.
the KV cache initialization requires a profiling forwarding
Third, Medusa is effective across different types of models
with the maximum sequence length and the maximum batch
with different parameter sizes. Among all models, Medusa
sizes, during which, we find 3 that some of the asynchronous
demonstrates the most significant latency reduction when
CUDA memory copies issued by the model weights loading
applied to Llama2 13B, with 42.9% compared with vLLM. The
stage are blocked.
least latency reduction is observed in Qwen1.5 0.5B, which
Second, the model weights loading stage could not cover
is 21.1% compared with vLLM.
all of the tokenizer loading and the KV cache initialization
stages, leaving a 0.26 seconds bubble. We also confirm that 6
7.3 Breakdowns of Medusa
out of 10 models have such bubbles. This demonstrates the
To better understand the benefit of our designs, we measure
naive asynchronous execution could not hide all the latency.
the latency of each stage in the loading phase and report
Medusa further reduces the loading phase latency as
them in Figure 8. The example model is Qwen1.5 4B, with
shown in Figure 8(c) in the following two ways. First, Medusa
the total model parameters size of 7.4GB.
enables the materialization in the KV cache initialization
Figure 8(a) shows the latency breakdown of vLLM, where
stage and CUDA graph capturing stage. As a result, the laeach phase is executed in a synchronous manner. The total
tency of KV cache initialization stage is greatly reduced since
latency is 2.85 seconds. The model structure initialization
the profiling forward is eliminated, i.e. from 0.50 seconds
stage takes 0.85 seconds, followed by the model weights
to 0.02 seconds. With the materialization of CUDA graphs,
loading stage of 0.39s. After the model weights loading stage,
Medusa reduces the capturing stage from 0.90 seconds to
it takes 0.21 seconds to load the tokenizer, and another 0.50
3We use Nsight Systems tool, which is provided by NVIDIA.
seconds to initialize the KV cache. At last, the capturing

662

