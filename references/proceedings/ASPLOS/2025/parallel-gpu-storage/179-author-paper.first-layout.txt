Simplifying and Accelerating NOR Flash I/O Stack for
          RAM-Restricted Microcontrollers
Abstract                                                                            File System
                                                                                                                    Fine-Grained      Coarse-Grained
                                                                                                                    Management        Management




                                                                        Host
NOR flash has been increasingly popular for RAM-restricted
microcontrollers due to its small package, high reliability, etc.                   Block Layer     File System
To satisfy RAM restrictions, existing NOR flash file systems                                                          File System
migrate their functionalities, i.e., block-level data organiza-                                   Soft Controller
                                                                                     Hardware                                           File System
tion and wear leveling (WL), from RAM to NOR flash. How-




                                                                        Storage
                                                                                     Controller                        WL Layer             WL
ever, such fine-grained block-level management introduces
frequent index updates and NOR flash scanning, leading to                          Stored Data     Stored Data        Stored Data      Stored Data

severe I/O amplification, which further deteriorates as they                       Simpler devices from server to microcontroller with less RAM
are decoupled in existing NOR flash file systems.                                 (a) SSD, UFS    (b) Raw-NAND      (c) Existing NOR (d) Our Simplified
   To address the problem, we propose NF2FS, a NOR flash-                             I/O Stack       I/O Stack         I/O Stack        I/O Stack

friendly file system. Our key insight is that applications run-
                                                                        Figure 1. File Systems in Flash-based I/O Stacks. Gener-
ning on NOR flash usually have (1) small file sizes, therefore
                                                                        ally, file systems use indexes to organize data, while hardware
block-based data organization can be converted to flat file lay-
                                                                        controllers perform WL. Without hardware controllers, NAND
out (for fast file/dir scanning); (2) deterministic I/O patterns,
                                                                        file systems perform data organization and WL in host RAM. In
thereby WL can be achieved through coarse file swapping.
                                                                        contrast, NOR file systems incorporate the two functionalities
As a result, NF2FS relaxes data organization and WL to a
                                                                        into storage due to RAM limits.
coarse-grained file level, which are then cooperated within
the file system. We implement NF2FS in FreeRTOS using a
range of techniques, including the all-logging layout, along            to balance device wear [3]. However, the two layers both
with efficient layout management approaches such as dual                deploy complex data structures (e.g., B-tree) in host RAM,
bitmap space allocator and soft-update-like crash consistency.          contradicting NOR flash’s RAM restriction.
Experiments suggest that NF2FS significantly outperforms                   To address the problems, existing NOR flash file systems
existing works and can prevent quick NOR flash wear-out.                migrate their functionalities from host RAM to storage (by
                                                                        maintaining all data structures in NOR flash while leveraging
1   Introduction                                                        a small portion of host RAMs for I/O), as shown in Figure 1(c).
With the emergence of the Internet of Things (IoT), the de-             To accommodate in-place updating data structures in the file
mand for NOR flash has increased dramatically [8, 9, 29].               system and WL layer1 , they either arrange updates in an out-
Compared to traditional storage devices (e.g., NAND flash),             of-place manner or store WL metadata across blocks (i.e., a
NOR flash is small (MB level), byte-addressable, and deployed           decentralized approach). For example, LittleFS [17] leverages
with RAM-restricted microcontroller units (MCU). Similar to             copy-on-write (CoW) to update indexes, while SPIFFS [16]
NAND flash, NOR flash faces the erase-before-write problem.             distributes program/erase (P/E) information to each block.
Consequently, these characteristics emphasize two primary               Nevertheless, these approaches introduce severe I/O amplifi-
goals for building a NOR flash file system: (1) to minimize             cation, reducing both performance and lifespan. Specifically,
RAM usage and (2) to prolong NOR flash lifespans.                       (1) updating indexes leads to cascade pointer updating; (2)
   Unfortunately, the widely used block-based file systems              decentralizing P/E information forces block selection to scan
that serve for SSD or UFS [22] are unsuitable for NOR flash.            all NOR flash blocks. Our experiment suggests that in write-
The reason is that they rely on the hardware controller (e.g.,          intensive real-world workloads, LittleFS [17] and SPIFFS [16]
FTL for WL and L2P mapping [21]) to provide a unified                   spend up to 28.43× and 4.51× time compared to the native
disk-like interface, as shown in Figure 1(a), while NOR flash           file system that directly operates raw NOR flash.
has neither controller nor integrated RAM due to its low                   We observe that the key problems of existing NOR flash
cost and density. Meanwhile, raw-NAND file systems, which               file systems lie in two aspects: (1) they manage NOR in a fine-
aim to manage NAND flash without a controller, still fail to            grained block level even on the RAM-restricted platforms,
suit NOR flash. As shown in Figure 1(b), they aim to emu-               causing I/O amplification; (2) they seperate WL functionality
late hardware controllers using software (i.e., soft controller).       from the file system design, with both fine-grained block-
Specifically, they build upon a multi-layer I/O stack with              level management, further deteriorating I/O amplification.
separated functionalities: the file system leverages block in-          1 WL layer and soft controller are interchangeable for NOR flash file systems

dexes to organize data, while the soft controller provides WL           as the soft controller in NOR flash file systems only has WL functionality.
                                                                    1
