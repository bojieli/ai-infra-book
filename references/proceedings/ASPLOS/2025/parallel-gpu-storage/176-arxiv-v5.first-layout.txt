                                                                  Optimizing Datalog for the GPU
                                                        Yihao Sun                      Ahmedur Rahman Shovon                              Thomas Gilray
                                                      ysun67@syr.edu                           ashov@uic.edu                         thomas.gilray@wsu.edu
                                                    Syracuse University                 University of Illinois, Chicago             Washington State University
                                                  Syracuse, New York, USA                  Chicago, Illinois, USA                   Pullman, Washington, USA

                                                                         Sidharth Kumar                          Kristopher Micinski
                                                                         sidharth@uic.edu                          kkmicins@syr.edu
                                                                    University of Illinois, Chicago               Syracuse University
                                                                       Chicago, Illinois, USA                   Syracuse, New York, USA
arXiv:2311.02206v5 [cs.DB] 19 Nov 2024




                                         Abstract                                                              The second rule requires recursive computation to a fixed-
                                         Modern Datalog engines (e.g., LogicBlox, Soufflé, ddlog) en-       point: the engine iteratively discovers an ever-larger inten-
                                         able their users to write declarative queries which compute        sional database (Reach), starting from the extensional data-
                                         recursive deductions over extensional facts, leaving high-         base (Edge). Unfortunately, best-in-class in-memory Data-
                                         performance operationalization (query planning, semi-naïve         logs hit scalability walls around 8–16 threads due to the
                                         evaluation, and parallelization) to the engine. Such engines       challenges of working with locking, linked data structures
                                         form the backbone of modern high-throughput applications           in a parallel, shared-memory setting. For example, as we
                                         in static analysis, network monitoring, and social-media           will soon see, when run at 32 threads on transitive closure,
                                         mining. In this paper, we present a methodology for im-            Soufflé [21] (a state-of-the-art CPU-based engine) spends
                                         plementing a modern in-memory Datalog engine on data               77.8% of its time in serialized tuple deduplication/insertion.
                                         center GPUs, allowing us to achieve significant (up to 45×)           Compared to CPUs, GPUs offer hundreds of thousands
                                         gains compared to Soufflé (a modern CPU-based engine) on           of threads, along with extremely high memory throughput
                                         context-sensitive points-to analysis of httpd. We present          via HBM [20]. However, achieving optimal performance ne-
                                         GPUlog, a Datalog engine backend that implements iterated          cessitates embracing the GPU’s programming model. Tra-
                                         relational algebra kernels over a novel range-indexed data         ditionally, CPU-based graphics processing code has been
                                         structure we call the hash-indexed sorted array (HISA). HISA       accelerated via SIMD instructions (such as AVX and SSE), ne-
                                         combines the algorithmic benefits of incremental range-            cessitating that all threads operate in lockstep and strongly
                                         indexed relations with the raw computation throughput of           penalizing thread divergence. While GPUs are inspired by
                                         operations over dense data structures. Our experiments show        this SIMD paradigm, modern GPGPU programming is SIMT
                                         that GPUlog is significantly faster than CPU-based Data-           in nature, allowing parallel execution at a per-thread gran-
                                         log engines while achieving a favorable memory footprint           ularity. Threads can diverge at the sub-warp level using
                                         compared to contemporary GPU-based joins.                          advanced thread scheduling techniques [36]. As we will see,
                                                                                                            the SIMT nature of modern GPUs is a natural fit for Data-
                                                                                                            log, which stresses both massive data parallelism (e.g., large
                                                                                                            joins), and task parallelism (e.g., multiple rules).
                                         1    Introduction                                                     In this paper, we introduce GPUlog: a GPU-based library
                                                                                                            for parallel relational algebra kernels, enabling the execution
                                         Declarative languages enable their users to write high-level       of Datalog programs on modern GPUs. GPUlog is backed by
                                         logical rules that specify acceptable solutions to a prob-         a novel SIMT data structure: the hash-indexed sorted array
                                         lem, leaving efficient implementation to a high-performance        (HISA). We designed HISA to balance the concerns of modern
                                         backend. In particular, modern CPU-based Datalog engines           Datalog on the GPU, enabling three critical tasks: (1) efficient
                                         power state-of-the-art systems in program analysis [5, 6,          range queries, necessary for joins, (2) lock-free deduplication,
                                         12, 13], social-media mining [18, 44, 45, 49], and business        and (3) parallel iteration. Additionally, GPUlog provides
                                         analytics [40]. Such engines enable a user to specify de-          abstractions for optimized evaluation of 𝑛-way loop-joins
                                         ductions (forming an intensional database, henceforth IDB)         we call temporarily-materialized joins, offering a space-for-
                                         over extensionally manifest relations (the extensional data-       time trade-off we found crucial to scale to high data loads.
                                         base, henceforth EDB). For example, given an input relation           We have used GPUlog to implement and comprehensively
                                         Edge(from,to), the program REACH computes Edge’s transi-           study the performance of graph analytics (reachability, same
                                         tive closure:                                                      generation) and program analysis (points-to analysis), evalu-
                                                                                                            ating GPUlog against cuDF, GPUJoin, and Soufflé [21, 39, 48].
                                             Reach(from, to) ← Edge(from, to).                              We observe improved (5×) join performance compared to
                                             Reach(from, to) ← Edge(from, mid), Reach(mid, to).
                                                                                                        1
