# PIM、编译与 Serverless：摘要补读

本批按父任务指定，处理 154、156、158、170、171、172。开始时六篇均在唯一 DOI 覆盖表的摘要缺口中，也未在之前独立包中完成。六篇完整一手摘要已读，归档六份 PDF 共 **91 页**；六张首页图像已看，用于摘要完整性和身份核对。**正文选读为 0 页**，全文提取仅用于归档和核验。本批不增大纲、不改编号、不运行论文代码。

| 序号 | 摘要得到的问题与候选判断 | 下一步正文阅读收益 |
| --- | --- | --- |
| 154 PAPI | 推理算子的瓶颈会随并行参数变化，固定把某类算子映射到 GPU 或 PIM 可能失效。候选，适合说明动态算术强度和异构映射；摘要加速比不能作为现代模型或量产 PIM 的通用收益。 | 中：核算子分类改变的触发条件、在线判断成本和评测平台；本批未读。 |
| 156 CINM | 存内／近存计算的程序模型过于贴近各自硬件，需要逐层降低抽象，同时保留设备通用与设备专用优化。候选，与编译章的抽象边界有关；摘要前述“数量级”优势不是本编译器的实测收益。 | 中：看 UPMEM 与 memristor CIM 如何用共同中间表示表达、哪些成本仍需设备专用模型；未读。 |
| 158 CMSwitch | 同一批 CIM 阵列用于计算还是存储，是一个随模型需求变化的资源分配问题；编译器把模式选择加入网络分段和映射空间。候选，适合说明资源取舍必须跨算子考虑。 | 中：核模式切换成本、分段边界及目标硬件是否为实物或模拟；未读。 |
| 170 CIDRE | 并发请求到来时，可以等忙碌暖容器空闲，也可以立即冷启动；内存保活与驱逐会改变以后请求的等待。条件候选，可迁移到 Agent 工具环境的分析问题，但原摘要是通用 FaaS，不能称为已验证的 Agent 系统。 | 条件性：只有章节需要容器保活策略时才读，核等待／冷启动阈值与并发分布；未读。 |
| 171 Dilu | GPU 按需供给不只有增减实例，还包括调整实例获得的资源份额；静态粗分配与变化负载会留下碎片。高优先候选，与模型服务资源调度直接相关。 | 高：核纵向／横向伸缩各自的时延、资源互补的测量方式、QoS 与碎片的精确定义；未读。 |
| 172 Medusa | LLM 冷启动除了加载权重，还包含 KV 初始化与 CUDA Graph 捕获；把可复用状态提前物化，再在线恢复，是另一种启动优化。高优先候选，可把 CUDA Graph 的稳态收益与初始化代价联系起来。 | 高：核可恢复状态、地址不确定性、版本／设备限制以及实验中的加载延迟与 TTFT 口径；未读。 |

## 固定来源与身份边界

154 的 [arXiv:2502.15470v2](https://arxiv.org/abs/2502.15470v2) 日期为 2025-02-27。原始 HTML 摘要完整读取，13 页预印本题名与九名作者均和正式 manifest 一致；PDF 首页没有正式 DOI。正式 DOI 为 `10.1145/3676641.3716009`，身份对应依靠仓库既有官方节目／出版者 manifest 的完整题名和作者，不声称 DOI 印在预印本中。ETH 正式稿入口返回 HTTP 429，Crossref 新请求也返回 HTTP 429（后者响应体 0 字节）；两份响应与下载状态都保留，未重试。正式记录页数为 17 页，**13 页预印本不能冒充正式 17 页版本**。

156 使用 [TU Dresden 作者机构 PDF](https://cfaed.tu-dresden.de/files/Images/people/chair-cc/publications/2504_Khan_CINM_ASPLOS.pdf) 的完整摘要，16 页。首页正式 DOI `10.1145/3622781.3674189` 与 manifest 对应；PDF 标注 ASPLOS ’24 卷 4，程序记录为 ASPLOS 2025 presentation，两者均保留。不要把另一个同名 Cinnamon 加密 AI 工作混入。本稿把作者 Karl Friedrich Alexander Friebel 写为 Karl F. A. Friebel，缩写明确记录。另归档的 arXiv:2301.07486v4（2024-05-24）只作版本历史与作者对应参考，正文摘要采用机构 PDF，未把两份略有措辞差异的摘要混合。

158 的 [arXiv:2502.17006v1](https://arxiv.org/abs/2502.17006v1) 日期为 2025-02-24，16 页。摘要横跨首页两栏，已看图核完整性；HTML 提供完整原始段落，因此不手工拼接 PDF 的栏间文字。正式 DOI `10.1145/3676641.3716248`、完整题名与七名作者由新归档的 Crossref 发布者记录核对，预印本首页未印 DOI。摘要的平均 1.31 倍针对论文所比 CIM 编译基线，尚未读到硬件与负载条件。

170 的 [作者实验室 PDF](https://tddg.github.io/assets/pdf/asplos25-cidre.pdf) 为 15 页，首页题名、五名作者与 DOI `10.1145/3676641.3716253` 对应。CIDRE 的摘要把冷启动比例和平均调用开销作为不同指标，原报告分别为最高降低 75.1% 和 39.3%；不改写成相同含义的吞吐提升。

171 的 [arXiv:2503.05130v1](https://arxiv.org/abs/2503.05130v1) 日期为 2025-03-07，15 页，PDF 首页正式 DOI `10.1145/3669940.3707251`、七名作者均已核对。摘要的 10%—46% 去碎片化、最高 1.8 倍推理／1.1 倍训练吞吐、11%—71% QoS 违规率下降是不同评测指标，不是同一资源节约率。本批不根据摘要推断它已经处理现代大 MoE、RL rollout 或生产集群的全部约束。

172 的 [作者 PDF](https://minhui-xie.github.io/papers/asplos25-medusa.pdf) 为 16 页，首页正式 DOI `10.1145/3669940.3707285` 与五名作者均核对。完整摘要读取到 TTFT 尾延迟降低 53.0% 的末句；右栏的冷启动示意图不混入摘要。它是 Serverless 冷启动状态物化系统，须与同名多头推测解码工作区分。摘要里的 KV 初始化信息也不能改写为用户请求 KV 内容的持久化缓存。

这里的候选优先级是阅读安排，不是已完成正文后的取舍结论。特别是 Dilu 与 Medusa 的框架接口、硬件条件、论文实现与当前框架之间的差别，仍需要下一阶段读正文和对应源码证据后才能写入书中。
