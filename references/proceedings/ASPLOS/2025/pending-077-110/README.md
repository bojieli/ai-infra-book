# 并行任务交接材料（待正式核验）

这里保存 ASPLOS 2025 第 77–110 项中两批交接材料的原始文件，避免后续工作依赖临时目录。两批分别报告 12 和 18 篇摘要、10 和 15 份代表性 PDF；这些数字尚未加入正式阅读统计。

已逐项检查两个原始交接 JSON 的 SHA-256，以及 bundle 中 206 个文件对象的字节数和 SHA-256。此检查仅证明交接文件完整，不证明摘要提取、论文身份或正文结论正确。下一步仍需从原始 HTML/PDF 重新提取摘要，核对 DOI、作者、版本差异，以及跨栏和页脚对提取结果的影响。

`delivery-original.json` 保留原交接元数据；`bundle.json` 仅将临时目录路径改为仓库相对路径。下载失败记录、替代版本和相关预印本继续保留，不重复计入论文或 HTTP 响应数量。原始构建脚本作为来源记录保存。

未完成的核验以 `research/2026-infra-survey/parallel-integration-backlog.json` 为准。这里不新增正文阅读完成记录。
