      Controlled Preemption: Amplifying Side-Channel
                  Attacks from Userspace
                                 Yongye Zhu                                                                 Boru Chen
                    University of California, Berkeley                                          University of California, Berkeley
                           Berkeley, CA, USA                                                           Berkeley, CA, USA
                       yongye.zhu@berkeley.edu                                                      boruchen@berkeley.edu

                               Zirui Neil Zhao                                                     Christopher W. Fletcher
              NVIDIA/The University of Texas at Austin                                          University of California, Berkeley
                        Austin, TX, USA                                                                Berkeley, CA, USA
                      neil.zhao@utexas.edu                                                         cwfletcher@berkeley.edu

Abstract                                                                             only user-level privilege or require fewer colocated attacker
Microarchitectural side channels are an ongoing threat in                            threads).
today’s systems. Yet, many side-channel methodologies suf-                           CCS Concepts: • Security and privacy → Side-channel
fer from low temporal resolution measurement, which can                              analysis and countermeasures.
either preclude or significantly complicate an attack.
   This paper introduces Controlled Preemption, an attack                            Keywords: Microarchitectural side-channel attacks; Attack
primitive enabling a single unprivileged (user-level) attacker                       primitives; Temporal resolution; Completely Fair Scheduler;
thread to repeatedly preempt a victim thread after colocat-                          Earliest Eligible Virtual Deadline First Scheduler
ing with that victim thread on the same logical core. Be-
                                                                                     ACM Reference Format:
tween preemptions, the victim thread executes zero to sev-
                                                                                     Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W.
eral instructions—sufficiently few to enable high-resolution                         Fletcher. 2025. Controlled Preemption: Amplifying Side-Channel At-
side channel measurements.                                                           tacks from Userspace. In Proceedings of the 30th ACM International
   The key idea in Controlled Preemption is to exploit sched-                        Conference on Architectural Support for Programming Languages
uler fairness heuristics. Namely, that modern thread sched-                          and Operating Systems, Volume 2 (ASPLOS ’25), March 30–April 3,
ulers give a thread 𝐴 the ability to preempt another thread 𝐵                        2025, Rotterdam, Netherlands. ACM, New York, NY, USA, 16 pages.
until a fairness tripwire (signaling that 𝐴 is starving 𝐵) fires.                    https://doi.org/10.1145/3676641.3715985
We show how this idea enables hundreds of short preemp-
tions before tripping the fairness tripwire is robust to noise                       1    Introduction
and applies to both the Linux CFS and EEVDF schedulers.
We also develop a technique that helps colocate the attacker                         Microarchitectural side-channel attacks exploit the hard-
and victim threads onto the same logical core, an attacker                           ware resources shared between mutually distrusting pro-
capability overlooked by prior work.                                                 grams [8, 19, 26–28, 40, 42, 72, 73]. In such attacks, an at-
   Our evaluation tests Controlled Preemption in the context                         tacker program monitors a victim program’s utilization of
of several different victim programs, victim privilege levels                        shared resources to infer sensitive victim information like
(inside and outside of Intel SGX) and choices of side channel.                       cryptographic keys [15, 19, 24, 26, 28, 45, 73, 74]. These at-
In each attack, we demonstrate results that are competitive                          tacks pose a serious threat to both commercial multi-tenant
with prior work but make fewer assumptions (e.g., require                            cloud environments [32, 53, 81] and local client environ-
                                                                                     ments [40, 42, 56].
                                                                                        A fundamental characteristic in any microarchitectural
Permission to make digital or hard copies of all or part of this work for            side-channel attack is its temporal resolution, i.e., the num-
personal or classroom use is granted without fee provided that copies                ber of victim instructions that execute/retire in between
are not made or distributed for profit or commercial advantage and that
copies bear this notice and the full citation on the first page. Copyrights          attacker measurements. Higher temporal resolution enables
for components of this work owned by others than the author(s) must                  new attacks or otherwise significantly reduces the require-
be honored. Abstracting with credit is permitted. To copy otherwise, or              ments of existing attacks. Consider for example the T-table
republish, to post on servers or to redistribute to lists, requires prior specific   AES first-round attack [28, 48]. Given perfect temporal res-
permission and/or a fee. Request permissions from permissions@acm.org.               olution, a cache-timing attack on the T-Tables can extract
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.
                                                                                     the key after the AES routine runs a single time. By con-
© 2025 Copyright held by the owner/author(s). Publication rights licensed
to ACM                                                                               trast, an attack with low temporal resolution (e.g., concur-
ACM ISBN 979-8-4007-1079-7/25/03                                                     rently/continuously measuring the channel or measuring
https://doi.org/10.1145/3676641.3715985                                              the channel once after the AES routine completes) requires
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.           Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


         Time                                                         To understand Controlled Preemption, we first review why
    𝑉                                                              prior work on preemption from userspace requires many
                                           ……
                                                                   threads. See Figure 1a. Assume a single-core system for sim-
                                     Recharging
                                                                   plicity. The idea in prior work is for the attacker to spawn
    𝐴1
                Charging                                           and then sleep a thread (say 𝐴1 ) so that its scheduling prior-
                                                                   ity increases to the point where, on wake up, it preempts the
                                      Recharging                   victim thread 𝑉 . Conceptually, a thread’s scheduling priority
    𝐴2
                 Charging                                          is set to that of the thread that gets preempted on wake up.
          .                      .                        .
          .                      .                        .        Thus, once an attacker thread preempts the victim, it needs
          .                      .                        .
                                                                   to sleep again to “recharge” its priority. If the attacker wishes
                     (a) Prior work [11, 12, 28, 54]               to preempt the victim multiple times, it requires multiple
         Time                                                      threads (𝐴1 , 𝐴2 , etc.). Once thread 𝐴1 preempts 𝑉 and begins
    𝑉                                                              to recharge, thread 𝐴2 is responsible for the next preemption,
                                         ……
                                                                   at which point it begins to recharge, and so on.
                                                                      The key observation enabling Controlled Preemption is
    𝐴1                                   ……                        general and applies to multiple thread schedulers: To improve
                Charging
                             No recharge needed                    system responsiveness, a scheduler will strive to allow a well-
                 (b) Controlled Preemption (this paper)            slept thread 𝐴 to immediately preempt a running thread 𝑉 ,
                                                                   even if 𝑉 has not completed its minimum scheduling quantum.
                                                                   Further, subject to fairness checks, a scheduler will allow thread
Figure 1. Techniques to enable high temporal resolution            𝐴 to repeat the above process. The latter sentence is key to our
channel-agnostic side-channel analysis from userspace.             single-thread attack. Through careful inspection of current
                                                                   scheduler designs, we find that scheduler fairness heuristics
                                                                   create what we call a preemption budget which enables an
100s [52] to 1000s [75] of victim runs (due to cache pollution
                                                                   attacker thread to repeatedly preempt a victim up to the
from later rounds and other sources of accumulated noise).
                                                                   point where the amount of attacker CPU time exceeds a
   This paper advances a line of research that enables ex-
                                                                   threshold (at which point the scheduler determines that the
tremely high temporal resolution side channel analysis, i.e.,
                                                                   attacker is starving the victim).
giving the attacker the ability to nearly single step the victim
                                                                      Based on the notion of a preemption budget, we instantiate
program and perform side channel measurements in between
                                                                   and thoroughly characterize a Controlled Preemption prim-
each step. This line of work is based on thread preemption.
                                                                   itive and show that it enables a colocated single attacker
In a nutshell, the attacker coerces the OS kernel to interleave
                                                                   thread to nearly single step1 a victim thread hundreds of
the attacker and victim threads’ executions onto the CPU,
                                                                   times without recharging, as shown in Figure 1b. We show
giving the attacker the opportunity to monitor shared states
                                                                   that the notion applies to both the Linux Completely Fair
in between short periods of victim execution.
                                                                   Scheduler (CFS) [47] and the more-recent Earliest Eligible
   Existing work on malicious preemption has limitations.
                                                                   Virtual Deadline First (EEVDF) scheduler [17, 58].
For example, works targeting the Enclave threat model [29,
                                                                      Note that in multicore systems, the attacker additionally
39, 45, 61, 62] (e.g., SGX Step [62]) enable best-case tem-
                                                                   needs a technique to colocate the attacker thread with the
poral resolution and minimal noise, but require supervisor
                                                                   victim thread on the same logical core. Prior work ignores
privilege. While there does exist a parallel line of work that
                                                                   this step, either limiting their attacks to a single-core sys-
enables similar capabilities from userspace [11, 12, 28, 54]
                                                                   tem [28] or assuming the attacker and victim threads are
(that started with Cache Games [28]), it requires a large num-
                                                                   pinned to the same core throughout the attack [11, 12, 54].
ber (e.g., from 10s to 100s) of attacker threads to assist in
                                                                   Both of these assumptions are unrealistic. In this work, we
preemption. Requiring a large number of threads is disad-
                                                                   develop a simple yet effective core-level colocation technique
vantageous for a number of reasons. More threads make the
                                                                   that exploits the scheduler’s load-balancing logic.
attack less stealthy and may even be disallowed depending
                                                                      Finally, we demonstrate three proof-of-concept attacks
on the system (e.g., through ulimit). Further, in practice,
                                                                   leveraging Controlled Preemption. These attacks cover a
attacks involving multiple threads are difficult to implement
                                                                   range of side channels, victim programs and victim program
(e.g., due to thread synchronization requirements) which
                                                                   privilege levels. Specifically, we show that the attacker can
itself results in coarser-grain temporal resolution [11, 28].
This paper. We present Controlled Preemption, a technique          1 As detailed in Section 4.3.1, the victim thread executes only a few instruc-
that enables a single unprivileged attacker thread to repeat-      tions between preemptions. In fact, we show that a majority of preemptions
edly preempt a victim thread once it has been colocated to         result in the victim executing one instruction when Controlled Preemption
the victim’s logical core.                                         is combined with performance degradation.
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace               ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.


use Controlled Preemption to mount an AES T-table first-                Earliest Eligible Virtual Deadline First (EEVDF) scheduler in
round attack using Flush+Reload [73]. This attack achieves              Section 4.5.
the same attack efficiency as prior work [11] (leaking the                 In the CFS, each thread is assigned a virtual runtime (vrun-
upper nibbles of each AES key byte in ∼ 5 victim runs) but              time). When a thread executes, its vruntime is incremented
requires only one colocated attacker thread instead of 40               by Δ𝜏 = 𝛼Δ𝑡, where Δ𝑡 is the thread’s real-world execution
threads. We then use Controlled Preemption to demonstrate               time and 𝛼 is the increment rate determined by the thread’s
an attack on an SGX victim to extract sensitive information             priority. 𝛼 = 1 under the default priority. A high priority
that can lead to full RSA key recovery using a Prime+Probe              thread has a small 𝛼 value, allowing the thread to have a
on the last-level cache [42]. To our knowledge, this is the             longer execution time for the same amount of Δ𝜏. In this
first demonstration of an SGX Step-like attack mounted from             paper, we denote vruntime by 𝜏 and real-world time by 𝑡.
userspace. Finally, we utilize Controlled Preemption to re-                The CFS achieves fair scheduling by ensuring that the dif-
cover the secret-dependent control flow of an SGX victim                ference between threads’ vruntimes, in the local runqueue,
using a BTB side channel [77], providing confidence that                does not exceed the kernel parameter sysctl_sched_latency
Controlled Preemption can be applied to multiple side chan-             (𝑆𝑏𝑛𝑑 for short). We call this policy the fair scheduling invari-
nels.                                                                   ant. The exact value of 𝑆𝑏𝑛𝑑 depends on the total number
   In summary, this paper makes the following contributions:            of cores in the system, and is set to 24 ms in our evaluated
                                                                        system (Table 1).
      • We develop Controlled Preemption, the first userspace              The CFS’s exact scheduling behavior depends on many fac-
        framework that nearly single steps a victim thread              tors. Using Linux 6.5 as an example, we now briefly overview
        using as few as one colocated attacker thread.                  the characteristics that are important for our work. Com-
      • We comprehensively characterize the Controlled Pre-             plementary to the runqueue, threads that are not ready to
        emption primitive in the context of two widely-                 execute, e.g., are waiting on blocking IO events or are asleep,
        deployed popular thread schedulers (the Linux CFS               are stored in a shared waitqueue.2 Then, at a high level, the
        and EEVDF schedulers). To our knowledge, we are                 CFS can change which thread is running on a given logical
        the first to perform any characterization of user-level         core in three circumstances:
        malicious preemption using the EEVDF scheduler.
      • We develop an attack technique that colocates the                   • Scenario 1. When, within the local runqueue, a thread’s
        attacker and victim threads onto the same logical core.                vruntime exceeds another thread’s vruntime by 𝑆𝑏𝑛𝑑 .
      • We demonstrate three proof-of-concept attacks that                  • Scenario 2. When a thread wakes up, i.e., is removed
        show how Controlled Preemption can improve exist-                      from the waitqueue and enters the local runqueue.
        ing attacks on multiple victim programs, inside and                 • Scenario 3. When a thread becomes blocked, i.e., is
        outside of SGX and across multiple side channel types.                 removed from the local runqueue and enters the wait-
                                                                               queue.
We have open sourced an implementation of Controlled                    We discuss each in detail below.
Preemption, as well as this paper’s evaluation, here: https:
//github.com/FPSG-UIUC/Controlled-Preemption.                           Scenario 1: Runqueue stationary. First consider the case
                                                                        when no threads are moving between the runqueue and the
Responsible disclosure. We disclosed our findings to the                waitqueue. Here, the CFS will select the thread with the
Linux kernel security team, who confirmed our findings and              smallest vruntime in the runqueue and schedule the thread
designated Controlled Preemption as a low-priority threat.              onto the hardware. We call this thread 𝐴. To avoid excessive
They recommended enabling NO_WAKEUP_PREEMPTION to                       context switching, 𝐴 is allowed to execute for a minimum
prevent the waking attacker thread from immediately pre-                time slice configured by sysctl_sched_min_granularity (or
empting the victim thread at the cost of system responsive-             𝑆𝑚𝑖𝑛 for brevity). 𝑆𝑚𝑖𝑛 is 3 ms in our evaluated system. Once
ness.                                                                   𝐴 runs for 𝑆𝑚𝑖𝑛 time, the CFS checks whether the fair sched-
                                                                        uling invariant is violated. If so, 𝐴 is descheduled, and the
                                                                        CFS chooses the thread with the smallest vruntime to sched-
2     Background
                                                                        ule. Otherwise, 𝐴 is scheduled again. As will be discussed in
2.1    Linux Completely Fair Scheduler                                  the next scenario, this minimum time slice is only enforced
In Linux, threads that are ready to execute but not scheduled           in Scenario 1. This detail will be critical for our work.
(or runnable threads) are stored in a per-logical core software
structure named the runqueue. The thread scheduler is re-               Scenario 2: A thread is waking up. Now consider when
sponsible for deciding when to preempt the current thread               a thread 𝐵 wakes up, 𝐵 is removed from the waitqueue and
and switch to another in the runqueue. In this paper, we focus          2 To simplify the presentation, we assume a single system-wide waitqueue.
on the widely used Completely Fair Scheduler (CFS) [33, 47]             In reality, the system uses different waitqueues depending on the event type
and discuss the transferability of our techniques to the latest         that is blocking a given thread.
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.                        Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


is added to the runqueue. Since 𝐵 was blocked by an IO re-                     TLBs [25, 59], coherence directories [72], on-chip intercon-
quest or sleep, 𝐵’s vruntime can be significantly behind the                   nects [18, 49, 65], arithmetic ports [8], and BTBs [20, 21, 39,
vruntime of other threads. Consequently, 𝐵 could monop-                        77, 79].
olize the CPU for a long time before its vruntime catches                         We focus on stateful/persistent side-channel attacks [19–
up with others. To prevent this undesirable behavior when                      21, 25, 26, 39, 42, 59, 72, 73, 77, 79] where the victim encodes
awakening 𝐵, the CFS assigns 𝐵 an adjusted vruntime of                         (transmits) a message into a channel/hardware structure
              𝜏𝑤𝑎𝑘𝑒𝑢𝑝 = max(𝜏𝑚𝑖𝑛 − 𝑆𝑠𝑙𝑎𝑐𝑘 , 𝜏𝑠𝑙𝑒𝑒𝑝 ),                    (1)   that can be decoded (received) later. For these attacks, the
                                                                               attacker needs to interleave its execution with the victim
where 𝜏𝑚𝑖𝑛 is the smallest vruntime among existing threads
                                                                               to monitor the channel. Consider an example where the
in the local runqueue, 𝑆𝑠𝑙𝑎𝑐𝑘 is a fixed value determined by
                                                                               attacker runs on core 𝐴 to perform an L1 cache Prime+Probe
𝑆𝑏𝑛𝑑 , and 𝜏𝑠𝑙𝑒𝑒𝑝 is the vruntime of thread 𝐵 at the moment
                                                                               attack. During the attack, the attacker (1) preconditions the
when 𝐵 was blocked. This heuristic prevents 𝐵 from mo-
                                                                               channel by priming specific L1 sets, (2) triggers the victim’s
nopolizing the CPU and ensures that 𝐵’s vruntime strictly
                                                                               execution on core 𝐴 and (3) recovers the victim’s memory
increases. In the evaluated Linux kernel, 𝑆𝑠𝑙𝑎𝑐𝑘 is 12 ms.
                                                                               access behavior by probing the L1 sets. Steps (1)–(3) can be
   Notably, after thread 𝐵’s vruntime is adjusted to 𝜏𝑤𝑎𝑘𝑒𝑢𝑝 ,
                                                                               repeated multiple times to extract more information from
𝐵 can preempt the current running thread 𝐴 even if 𝐴 has yet
                                                                               the victim execution.
to complete its minimum time slice. Our attack exploits this
                                                                                  A major complication is how much victim code runs dur-
preemption logic. A preemption occurs when the following
                                                                               ing Step 2. Ideally, the attacker would like to interleave its exe-
condition is true:
                                                                               cution at a fine grain, ideally performing Step 1 and Step 3 be-
                    𝜏𝑐𝑢𝑟𝑟 − 𝜏𝑤𝑎𝑘𝑒𝑢𝑝 > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 ,                         (2)   fore and after each of the victim’s sensitive memory accesses.
where 𝜏𝑐𝑢𝑟𝑟 is the thread 𝐴’s vruntime and 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 is                         This is usually impossible, due to coarse-grain thread sched-
a fixed threshold configured by the kernel parameter                           uling, which adds significant noise that worst-case blocks
sysctl_sched_wakeup_granularity. 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 is 4 ms in our sys-                  the attack and best-case requires additional techniques to
tem. Note that the CFS only decides between scheduling the                     overcome [15, 30, 48, 66].
running thread 𝐴 and the waking thread 𝐵. Even if there is a
third runnable thread 𝐶 whose vruntime is smaller than the                     3     Threat Model
vruntime of 𝐴 and 𝐵, 𝐶 will not be considered for scheduling.
                                                                               We assume that the attacker and victim programs are colo-
Scenario 3: A thread is blocked. Finally, consider when                        cated on the same physical machine, following many prior
the current running thread is about to be blocked due to an                    works [11, 28, 48]. We assume an unprivileged attacker that
IO request or a voluntary sleep. In this case, the CFS will                    can interact with the Linux kernel using standard system
de-schedule the current running thread and schedule the                        calls like fork and sleep and can invoke the victim (start its
thread with the smallest vruntime from the local runqueue.                     execution) [15, 23, 24, 36, 44, 45, 48, 69, 74, 75]. The victim
  Table 1 summarizes the CFS configurations relevant to                        thread can be an unprivileged user-level thread or a thread
this paper and their default values. Note that their default                   inside an Intel SGX enclave.
values depend on the number of cores in the system.                               We do not make additional assumptions about the execu-
                                                                               tion environment or the priority of the victim thread, beyond
                                                                               that the kernel uses the Linux CFS or EEVDF schedulers to
            Table 1. Relevant CFS configurations.                              schedule the victim thread. Finally, we do not assume the
             Default Value                                                     availability of Simultaneous Multi-Threading (SMT).
 Config.                          Description
             (Our System)
   𝑆𝑏𝑛𝑑      𝜆×6 ms (24 ms)1      Upper bound of vruntime difference
  𝑆𝑚𝑖𝑛      𝜆×0.75 ms (3 ms)      Length of the minimum time slice             4     Controlled Preemption
  𝑆𝑠𝑙𝑎𝑐𝑘     𝑆𝑏𝑛𝑑 /2 (12 ms)2     A waking thread’s max. vruntime lag          In this section, we introduce Controlled Preemption, a series
                                  The vruntime threshold that the waking
 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡    𝜆×1 ms (4 ms)                                                     of techniques that enable a single unprivileged colocated
                                  thread can preempt the current thread
 1 𝜆 is a system-specific scaling factor and equals min(log (#cores) +1, 4).
                                                                               attacker thread to interleave its execution with a victim’s at
                                                           2
   𝜆 = 4 in our 16-core system.
                                                                               a high frequency (e.g., one to tens of victim instructions per
 2 This assumes the GENTLE_FAIR_SLEEPERS scheduler feature is set,             interleaving), enabling high-resolution side-channel obser-
   which is the default configuration; otherwise, 𝑆𝑠𝑙𝑎𝑐𝑘 = 𝑆𝑏𝑛𝑑 .              vations.
                                                                                  Controlled Preemption is possible because thread sched-
                                                                               ulers enable blocked threads to reclaim the CPU precisely
2.2   Microarchitectural Side-Channel Attacks                                  at the moment when they are due to become unblocked—
Microarchitectural side-channel attacks exploit shared hard-                   as opposed to only after the currently running thread has
ware resources to exfiltrate sensitive information. Commonly                   exceeded some minimum scheduling quantum (e.g., 𝑆𝑚𝑖𝑛 ).
exploited resources include CPU caches [19, 26, 42, 52, 73],                   This is by design and enables thread schedulers to be more
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace             ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.

                                                                                                                                                Vruntime
responsive. For example, when a thread calls sleep(𝛿), it
ideally wishes to be asleep for precisely 𝛿 time. Likewise,
                                                                                                                                                     (a)
when data becomes available (e.g., network packets arrive),
                                                                                   Hibernating…
the thread responsible for processing that data should get
CPU time immediately.                                                                                               𝜏𝑣𝑖𝑐𝑡𝑖𝑚
   We show how the above characteristic can be exploited                                                                                             (b)
                                                                                                      𝑆𝑠𝑙𝑎𝑐𝑘
by an adversary to preempt a victim thread at a moment                                                  𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟
of the attacker’s choosing. More subtly, we show how the
above enables a single attacker thread to repeatedly preempt                                                                                         (c)
the victim at a high frequency, enabling fine-grain side chan-                                    𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟
nel measurements. Repeated preemption does not “come for
free”—a scheduler’s fairness heuristics (e.g., those discussed                                                       𝐼𝑣𝑖𝑐𝑡𝑖𝑚                         (d)
in Section 2.1) should prevent a malicious thread from deny-                                           Δ > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 ?
ing service through repeated preemption. We show how
schedulers’ fairness heuristics can be avoided for a long
                                                                                                                                                     (e)
enough period of time (enabling hundreds to thousands of
preemptions) to attack security-critical software.                                                                             Δ > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 ?


                                                                        Figure 2. Detailed illustration of Controlled Preemption
4.1   Controlled Preemption on the CFS                                  where Δ = 𝜏𝑣𝑖𝑐𝑡𝑖𝑚 − 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 .
We now show how Controlled Preemption can be imple-
mented using the CFS scheduler (Section 2.1). The key ob-
servation is that the 𝑆𝑠𝑙𝑎𝑐𝑘 > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 characteristic in the
CFS creates an (𝑆𝑠𝑙𝑎𝑐𝑘 − 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 )-time preemption budget              𝛼 = 1, therefore Δ𝜏𝑣𝑖𝑐𝑡𝑖𝑚 = 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 and Δ𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 = 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 .
within which the attacker can repeatedly preempt the vic-               We will discuss the implications of priority in Section 4.3.2.
tim. Echoing earlier discussion, setting 𝑆𝑠𝑙𝑎𝑐𝑘 > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 is              The attacker requires a method to wake up at a specific
deliberate and important for ensuring system responsive-                time and be considered for scheduling. We discuss two mech-
ness. By ‘preemption budget’, we mean that the attacker can             anisms for this task in Section 4.2 and assume the first of
preempt the victim an arbitrary number of times, until the              those methods (where the attacker uses sleep(𝛿)) here. The
vruntime difference between the attacker and victim threads             attacker will use sleep in two contexts (explained in the fol-
is smaller than 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 . Beyond this point, no more pre-              lowing paragraphs). In the first context, it will set 𝛿 > 2∗𝑆𝑏𝑛𝑑
emptions are possible until the victim’s vruntime increases             (any value > 2∗𝑆𝑏𝑛𝑑 will do); in this case, we say the attacker
by a sufficient amount. For the parameters we use in the                is hibernating. In the second, it will set 𝛿 = 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 ; which we
paper, 𝑆𝑠𝑙𝑎𝑐𝑘 − 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 = 8 ms which is sufficient time for            refer to as the attacker napping.
the attacker to complete its attack. In the event that this is             The attack begins with the attacker hibernating (Fig-
not sufficient, we discuss several methods to extract a longer          ure 2 (a)). When the attacker thread unblocks itself and enters
trace of victim activity in Section 4.3.2 and Section 5.2.2.            the runqueue, its vruntime will be assigned to the left-hand
   We now explain how the attacker thread 𝐴 can repeatedly                                                                        ′
                                                                        argument of the max function in Equation (1), i.e., 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟    =
preempt other threads in the same runqueue at a high fre-                                                         ′       ′
                                                                        𝜏𝑣𝑖𝑐𝑡𝑖𝑚 − 𝑆𝑠𝑙𝑎𝑐𝑘 , where the tick mark (e.g., 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 ) denotes
quency. For simplicity, we consider an example illustrated              a new/updated vruntime. This is shown in Figure 2 (b). Com-
in Figure 2 where a single logical core is exclusively shared           bining this with the relation 𝑆𝑠𝑙𝑎𝑐𝑘 > 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 from earlier,
between an attacker thread 𝐴 and a victim thread 𝑉 . We                 Equation (2) says that the attacker immediately preempts
will discuss the implications of the core being shared with             the victim.
more (noisy) threads in Section 4.3.3 and how the attacker                 The attacker can now monitor and preempt the vic-
can colocate with the victim on the same logical core in                tim as follows. Once the attacker begins executing, it per-
Section 4.4.                                                            forms a side-channel measurement (i.e., the steps to pre-
   Let 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 and 𝜏𝑣𝑖𝑐𝑡𝑖𝑚 denote the attacker’s and victim’s         condition/receive on the channel it is measuring; c.f. Sec-
vruntimes, respectively. Let 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 denote the time it takes         tion 2.2) and then naps. Hence, it’s vruntime increases by
for the attacker to perform a side-channel measurement. To              𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 as shown in Figure 2 (c). This triggers the CFS to
enable repeated preemptions, we require that 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 be               schedule and execute instructions from the victim thread.
small, i.e., 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 < 𝑆𝑠𝑙𝑎𝑐𝑘 − 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 . Let 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 denote the    Once the victim runs for 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 time, the attacker wakes up.
amount of time that the attacker wishes the victim to run for           W.l.o.g. let 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 > 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 . Then, the attacker’s vruntime
in between preemptions. For simplicity, we consider both                will be assigned to the right-hand argument to the max func-
the attacker and the victim have the default priority and                                               ′
                                                                        tion in Equation (1), i.e., 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟   = 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 . Based on our
     ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.               Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher

                                      1   #define SIG SIGRTMIN
                                                                                An additional OS parameter, called timer_slack, controls
1    // tweak timerslack          2       // register signal handler
2    // to the lowest value 1ns 3         // as attacker procedure           the time allowed to pass beyond 𝛿 before the OS has to
3    prctl(PR_SET_TIMERSLACK, 1) 4        sigaction(SIG, attacker)           wake up the attacker thread. By default, timer_slack is 50𝜇s,
4    // sleep to make             5       // create a timer that             which is too coarse for our needs. Fortunately, we can reduce
5    // vruntime lowest           6       // sends SIG when fires            timer_slack to a small value (1 ns) using the non-privileged
6    sleep(5s)                    7       timer_create(SIG)
                                                                             prctl syscall with PR_SET_TIMERSLACK as the argument.
7    while(true){                 8       // set periodic interval
8        // sleep for interval    9       timer_settime(interval)
                                                                             4.2.2 Method 2: Timers. The second method uses the
9        nanosleep(interval)     10       // sleep to make
10       // call attacker        11       // vruntime lowest                 POSIX timer API to create a periodic high-resolution
11       // procedure            12       sleep(5s)                          timer. The attacker calls timer_create to create a timer and
12       attacker()              13       // pause main routine              timer_settime to set it with interval 𝛿. A signal handler is
13   }                           14       while(1) pause()                   registered to handle timer expirations. After setting up the
       (a) Method 1: Nanosleep                 (b) Method 2: Timer           timer, the attacker blocks itself indefinitely by calling pause
                                                                             and waits for the timer signal. When the timer expires, a sig-
     Figure 3. Pseudo-code describing two methods to force the               nal is sent to the attacker. The kernel adds the attacker back
     attacker to wake up at a precise time.                                  to the runqueue, sets the attacker’s vruntime and performs
                                                                             the preemption check as usual. If the preemption condition is
                                                                             met, the attacker preempts the victim. The attacker’s signal
     earlier requirement that 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 < 𝑆𝑠𝑙𝑎𝑐𝑘 − 𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡 , we fur-        handler then executes the side-channel measurement rou-
     ther have that Equation (2) holds, meaning that the attacker            tine. After the signal handler completes, the attacker blocks
     will preempt the victim as shown in Figure 2 (d).                       again, awaiting the next timer signal.
        The above process (Figure 2 (b)-(d)) can repeat, enabling               We note, when using the POSIX timer API, setting an
     repeated preemptions. An important detail is that because               analog to timer_slack is not necessary: timer interrupts are
     𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 > 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 , 𝜏𝑣𝑖𝑐𝑡𝑖𝑚 − 𝜏𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 gradually shrinks and        handled immediately by the kernel while the execution of the
     Equation (2) will eventually return false as shown in Fig-              attacker’s userspace handler is still subject to the preemption
     ure 2 (e). At this point, lpreemption will     m fail. This gives the   check.
                                   𝑆𝑠𝑙𝑎𝑐𝑘 −𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡
     attacker approximately 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 −𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 preemptions to com-
                                                                             4.2.3 Zero Stepping. Setting 𝛿 for both of the above meth-
     plete its attack. It is possible to stretch the ‘preemption bud-        ods is non-trivial because time continues to pass when the
     get’ by setting a larger 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 , but doing so allows the victim      OS is in the process of scheduling/context switching the vic-
     to make more forward progress in between preemptions                    tim onto the hardware. That is, 𝛿 must be set in a “Goldilocks”
     which may aggravate the side-channel analysis.                          fashion similar to SGX Step [62]:
                                                                                   • If 𝛿 is set to be too small, the timer fires when the victim
     4.2   Controlled Wake Up
                                                                                     is in the process of being scheduled onto the CPU,
     Following the example from the previous section, where we                       which interrupts and prevents the victim from making
     assumed only an attacker and victim thread, the attacker                        any forward progress before the CPU is yielded back
     needs a way to wake up at a precise time and be considered                      to the attacker.
     for scheduling. We utilize high-resolution hardware timers                    • If 𝛿 is set to be too large, the victim executes more
     for this purpose. We explore two methods to program these                       instructions before being preempted, which leads to
     timers in userspace, enabling the attacker to interrupt the                     lower time resolution side-channel analysis.
     victim potentially after the victim has executed only a single
     instruction. Figure 3 provides pseudo-code for both methods.            The former case is called a zero step [16, 62]. We analyze the
                                                                             characteristics of this Goldilocks zone in the next section.
     4.2.1 Method 1: Nanosleep. The first method employs                        Zero steps are benign but waste preemption budget. An
     the nanosleep system call with a specified sleep duration 𝛿.            oracle can be constructed to filter out signals caused by
     When the attacker invokes nanosleep, the operating system               instructions of interest (Section 4.3.3), and hence data col-
     removes it from the runqueue (Scenario 3 from Section 2.1)              lection when zero steps occur will be omitted. At the same
     and programs a one-shot hardware timer to trigger an in-                time, each zero step still costs the attacker 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 − 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚
     terrupt after 𝛿 time passes. With the attacker blocked, the             time in the preemption budget.
     scheduler selects the victim thread to run. After the interval
     𝛿 elapses, the hardware timer fires, the victim’s execution is          4.3    Evaluation
     interrupted, and control switches to the kernel’s interrupt             We now evaluate the Controlled Preemption primitive along
     handler. Finally, the kernel wakes up the attacker thread,              multiple axes: its temporal resolution, the number of pre-
     adds it back to the runqueue, sets its new vruntime and                 emptions, robustness to noise, techniques for colocation and
     performs the preemption check (as described in Section 2.1).            extensibility to the EEVDF scheduler.
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace                              ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.


Experiment setup. For the rest of the paper, we run all of                                  1.0
                                                                                                                                                            1225ns




                                                                          Occurance Ratio
                                                                                            0.8                                                             1230ns
our experiments on a desktop machine with a 16-core Intel                                   0.6                                                             1245ns
Core i9-9900K processor and 64 GB RAM. This machine runs                                    0.4                                                             1300ns
Ubuntu 22.04.1 with Linux kernel version 6.5 for CFS and                                    0.2
                                                                                            0.0     0    1     2-10 11-50 51-100101-20 201-40 401-60 601-2k 2k-4k
6.12-rc1 for EEVDF.                                                                                                                   0      0      0
   Until Section 4.3.3, we perform experiments on a quiescent
                                                                                                             (a) Nanosleep (Section 4.2.1)
machine to minimize interference from other processes. As
                                                                                            1.0
                                                                                                                                                            1550ns




                                                                          Occurance Ratio
required by our attack, we ensure that the attacker and victim                              0.8                                                             1560ns
threads are colocated on the same logical core; we show                                     0.6                                                             1570ns
how to achieve this in Section 4.4. At experiment launch,                                   0.4                                                             1600ns
                                                                                            0.2
the attacker sleeps (hibernates) for 5 seconds to ensure it is                              0.0     0    1     2-10 11-50 51-100101-20 201-40 401-60 601-2k 2k-4k
assigned the vruntime 𝜏𝑣𝑖𝑐𝑡𝑖𝑚 − 𝑆𝑠𝑙𝑎𝑐𝑘 upon wake-up.                                                                                  0      0      0

4.3.1 Temporal resolution. We characterize how many                                                          (b) Nanosleep + Evict iTLB
instructions the victim executes between interrupts using                                  1.0
                                                                                                                                                           4050ns




                                                                         Occurance Ratio
                                                                                           0.8                                                             4250ns
an eBPF [6] program that records the PC of the first victim                                0.6                                                             4300ns
instruction whenever the victim is scheduled. To translate                                 0.4                                                             4350ns
the change in the victim PC to the number of instructions                                  0.2
                                                                                           0.0      0    1    2-10 11-50 51-100101-20 201-40 401-60 601-2k 2k-4k
retired, we use a victim program that runs a long sequence                                                                           0      0      0
of same-Byte length instructions in an infinite loop.                                             Number of Instructions Retired per Execution
   Figure 4a and Figure 4c show histograms of the preemp-
                                                                                                              (c) Timer (Section 4.2.2)
tion resolution, in terms of how many victim instructions
retire per preemption. Each histogram is over 80,000 pre-               Figure 4. Temporal resolution of Controlled Preemption
emptions. We show results for both wake up methods from                 using different wake up methods and performance degrada-
Section 4.2, varying the sleep and timer interval value 𝛿.              tion techniques. Data is shown as a histogram, in terms of
As we increase 𝛿, the victim executes more instructions per             the number of victim instructions retired per preemption.
preemption on average. Notably, for small 𝛿, a majority of              Different lines correspond to different values of 𝛿.
preemptions occur after the victim has only executed a small
number (< 10) instructions but a sizable percentage of pre-               To count preemptions, we record the vruntime and process
emptions result in zero steps (Section 4.2).                            ID (PID) each time the kernel transfers control to userspace.
                                                                        Starting from when the attacker begins launching interrupts,
Combining Controlled Preemption with performance                        we monitor until there are two consecutive kernel exits to
degradation. To improve the ratio of single steps to zero               the victim process without interleaving with the attacker.
steps, we combine Controlled Preemption with well known                 We vary 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 by adjusting the attacker’s execution length
performance degradation techniques [9, 38, 50]. Specifically,           using different numbers of serialized cache-miss memory
the attacker evicts the victim instruction page’s translation           accesses, and run each experiment 50 times.
from the TLB before napping. As the victim page’s transla-                Figure 5 shows the plot of the difference between 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟
tion can be cached either in the L1 instruction TLB and the             and 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 against the number of repeated interrupts
unified L2 TLB, we construct eviction sets for both TLBs                achieved by the attacker. We also include a curve indicat-
using techniques from Gras et al. [25]. Figure 4b shows                 ing the expected relation (given the ratio from before). The
that combined with performance degradation, one can set                 results demonstrate that the actual number of preemptions
a higher 𝛿 while reliably making non-zero but still small               achieved closely matches up to the expected number.
amounts of victim forward progress per preemption, as de-
sired. The cost of this technique is a small increase in 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟      Varying thread priority. Since a high priority thread in-
for evicting the TLB entries, which is small compared to the            crements its vruntime at a slower rate, we further examine
main attacker measurement procedure.                                    the effect of the victim’s scheduling priority. In this experi-
                                                                        ment, we vary the victim’s priority by changing its nice value
4.3.2 Number of preemptions. Next, we characterize                      while keeping the attacker’s priority at the default value of
the number of consecutive preemptions that the attacker can             zero.3 As shown in Figure 6, decreasing the victim’s nice
perform. Recalll from Sectionm4.3, the expected number of               value (increasing its priority) reduces the number of consec-
                 𝑆𝑠𝑙𝑎𝑐𝑘 −𝑆𝑝𝑟𝑒𝑒𝑚𝑝𝑡
preemptions is 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟  −𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 . Note that 𝑆𝑠𝑙𝑎𝑐𝑘 −𝑆 𝑝𝑟𝑒𝑒𝑚𝑝𝑡 =     utive preemptions. Remarkably, even with the smallest nice
8 ms is fixed based on the system. Thus, the attacker can               value (highest victim priority), Controlled Preemption can
increase the number of preemptions by decreasing its own                3We do not set the attacker’s nice value below zero as this requires the
measurement time 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 or by increasing the victim time             attacker to be privileged. We do not set the attacker’s nice value above
per preemption 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 (by changing the blocking interval 𝛿).          zero as the attacker has no incentive to lower their priority.
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.                                                    Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


                  Number of Consecutive Preemptions   4000                                                   4.3.3 Measuring the impact of noise. We also test the
                                                      3500                                     y = 8e6
                                                                                                    x        robustness of Controlled Preemption to noise. We character-
                                                                                                             ize two sources of noise. First, scheduling noise refers to noise
                                                      3000
                                                                                                             caused by there being additional threads in the runqueue
                                                      2500                                                   that are not owned by the victim or attacker. Second, channel
                                                      2000                                                   noise refers to noise on the side channel (e.g., cache pollution).
                                                      1500                                                   We conduct our experiment using Method 1 (Section 4.2.1),
                                                      1000                                                   but the analysis is transferable to Method 2.
                                                       500
                                                             0   5000 10000 15000 20000 25000                Scheduling noise. We study a system where the runqueue
                                                                       Iattacker Ivictim(ns)                 is shared between a victim thread 𝑉 , an attacker thread 𝐴,
Figure 5. The number of repeated preemptions (y-axis)                                                        and a third compute-bound noise thread 𝑁 that does not
achieved by Controlled Preemption when varying 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 −                                                   make system calls. See Figure 7 for the results of an experi-
𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 (x-axis). The plot assumes Method 1 (Section 4.2.1)                                                 ment that analyze how/when the three threads’ vruntimes
although the result transfers to Method 2. Blue dots are ob-                                                 increase (which indicates when different threads get sched-
servations (concrete settings of 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 and 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 ) on our                                             uled). Results generalize in a natural way for more noise
test machine. The curve indicates the expected number of                                                     threads.
consecutive preemptions.                                                                                        Suppose the attacker (𝐴) hibernates, i.e., has the smallest
                                                                                                             vruntime, at the start. We analyze two cases. First, suppose
                                                                                                             𝑉 ’s vruntime is initially less than 𝑁 ’s vruntime. In this case,
                                 850
  Consecutive Preemptions




                                                                                                             Controlled Preemption proceeds as usual between the 𝐴 and
                                 800
                                 750                                                                         𝑉 threads until either 𝑉 ’s and 𝐴’s vruntimes are such that
        Number of




                                 700                                                                         Equation (2) returns false or until 𝑉 ’s vruntime equals 𝑁 ’s
                                 650                                                                         vruntime. Second, suppose 𝑁 ’s vruntime is initially less than
                                 600                                                                         𝑉 ’s vruntime (not shown in Figure 7). In this case, 𝑉 will not
                                 550
                                                         20       15        10        5          0       5   be scheduled until, again, 𝑉 ’s and 𝑁 ’s vruntimes are equal.
                                                                       Victim's Nice Value                      Then, the remaining question is: can the attacker perform
Figure 6. The number of repeated preemptions (y-axis) is                                                     Controlled Preemption after the victim and noise threads’
a function of the victim’s nice value (x-axis). We set the                                                   vruntimes become equal? This occurs at the dashed vertical
attacker to the default nice value 0. 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 − 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 ranges                                            line in Figure 7. In this regime, we find that the attacker
between 10 µs and 15 µs.                                                                                     gets interleaved with either the victim or noise thread in an
                                                                                                             unpredictable fashion (see the zoom-in in the figure). That
still achieve hundreds of consecutive preemptions. This is                                                   is, scheduling follows the pattern ((𝑉 |𝑁 )𝐴)+.
because 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 is near zero (regardless of nice). As a result,                                                To continue Controlled Preemption in this regime, we
𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 − 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 is always dominated by 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 .                                                      adopt a well-known side-channel template attack (e.g., [27])
   Orthogonally, we observe that temporal resolution is also                                                 to construct a “victim ran last?” (or victim presence) oracle.
largely unaffected by the victim nice setting.                                                               Specifically, by pre-computing the victim’s instruction trace
                                                                                                             at cache-line granularity, the attacker can monitor specific
Increasing the number of consecutive preemptions be-                                                         cache lines of interest during the measurement phase. By
yond the preemption budget. Controlled Preemption can                                                        probing these cache lines, the attacker gathers information
borrow the idea of using multiple preemption threads from                                                    about the last executing thread, and only records data points
prior work [11, 28, 54] to increase the number of consecu-                                                   if the victim thread ran last. We implement this oracle for
tive preemptions beyond the preemption budget. Similar to                                                    our attack in Section 5.2.2.
prior work, the attacker can launch 𝑛 preemption threads                                                        We remark that the attacks we evaluate finish in several
𝐴1 , 𝐴2 , ..., 𝐴𝑛 . All these threads are well slept before the                                              milliseconds. Further, the attacker can choose when to run
attack. During the attack, the attacker first uses 𝐴1 to repeat-                                             the victim (per Section 3). Thus, it will likely be the case
edly preempt the victim thread. As 𝐴1 is about to run out                                                    that any noise threads will be preexisting in the runqueue
of preemption budget, the attacker wakes up 𝐴2 to continue                                                   when the attack commences (as opposed to: are added to the
the attack, and so on. Since 𝐴1 is sleeping while subsequent                                                 runqueue when the attack is underway). In that case, noise
attacker threads continue the attack, it will be eligible to                                                 thread vruntime will be higher than victim thread vruntime
interrupt the victim after 𝐴𝑛 completes its budget. With this                                                and Controlled Preemption will proceed between just 𝐴 and
round-robin strategy, the attacker achieves an effectively                                                   𝑉 without wasting preemption budget on noise threads (until
infinite preemption budget.                                                                                  the victim’s vruntime catches up with the noise threads).
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace                                 ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.

                  1e7+4.319e11                                                                1.0
                                                                                                                                                             1500ns




                                                                            Occurance Ratio
                                                                Victim                        0.8                                                            1520ns
            7.5                                                 Noise                         0.6                                                            1540ns
                                                                                              0.4                                                            1560ns
                                                                Attacker                      0.2
                                                                                              0.0      0    1   2-10 11-50 51-100101-20 201-40 400-60 601-2k 2k-4k
            7.0                                                                                                                        0      0      0
                                                                                                     Number of Instructions Retired per Execution
 Vruntime




                                              +4.319754e11
                                                    Victim                 Figure 8. Temporal resolution of Controlled Preemption on
            6.5                         60000
                                                    Noise                  EEVDF. The figure represents the same experiment as that
                                        55000
                                                                           shown in Figure 4b, but using EEVDF.
                                        50000
            6.0                                                            one core 𝐶 idle. Next, the attacker invokes the victim thread,
                                        45000
                                                  54045 54050 54055
                                                                           which will be scheduled onto the idle core 𝐶 to improve load
            5.5 53250 53500 53750 54000 54250 54500 54750 55000 55250      balance. Finally, the attacker can launch the attack thread
                                                                           (as described previously) and pin it to 𝐶, colocating with the
                                    Sample ID                              victim thread. Note that the victim is unlikely to migrate
                                                                           to another core during the attack. This is because the CFS
Figure 7. vruntime progression in a ‘noisy’ system with a                  load balancer observes that all other cores are occupied by
third noise thread. The zoom-in shows the vruntimes of the                 the attacker’s dummy threads and there are no idle cores to
victim and noise thread at the point where they converge                   migrate the victim thread to.
(Sample 54033).                                                               The above scheme requires 𝑁 threads total (𝑁 − 1 dummy
                                                                           threads and 1 preemption/measurement thread) and is capa-
Channel noise. We identify two types of channel noise that                 ble of monitoring either core-private or core-shared channels.
interfere with the attacker’s side-channel measurements. The               It is also simple to implement: It does not require synchro-
first type comes from the kernel’s code/data footprint during              nization across attacker threads, and more generally presents
context switches. We mitigate this noise by monitoring a                   the preemption/measurement thread with the illusion of liv-
sufficiently-large structure (e.g., the L2 Cache and LLC in-               ing in a single-core system.
stead of the L1 cache) to not be polluted by the kernel. The                  If the system is fully loaded, the above scheme will not
second type of noise is random (non-systematic) noise com-                 work because there is no idle logical core. This situation
ing from the other threads in other cores. To ameliorate this              is rare, and (in some cases) actively avoided. For example,
noise, we adopt two strategies: 1) we run the victim several               Google Cloud Run tries to keep containers’ CPU utilization
times and take a majority vote; 2) alternatively, if possible,             below 60% [7]. Further, since the attacker can choose when
we measure private core structures like the BTB and TLB                    to invoke the victim (Section 3), it can opt to run the attack
(which cannot be polluted by the activity of other cores). We              when the system is not fully loaded.
demonstrate such an attack on the BTB in Section 5.3.
                                                                           4.5                      EEVDF Scheduler
4.4          Achieving Core Colocation                                     In this section, we show that Controlled Preemption is trans-
Our attack requires that the attacker and victim threads                   ferrable to the latest EEVDF scheduler [17, 58]. Intuitively,
reside in the same runqueue throughout the attack. Since                   this is because the EEVDF scheduler also allows a well-slept
the runqueue is a per-logical core structure, the attacker                 thread to immediately preempt the current running thread
and victim threads need to colocate on the same logical                    for better system responsiveness.
core. One straightforward approach would be pinning the                       To demonstrate the transferrability of our techniques, we
victim thread to an attacker-desirable logical core. However,              repeat the temporal resolution experiment from Section 4.3.1
pinning threads owned by other users requires supervisor                   on EEVDF, using the same environment except changing the
privilege, which is not part of our threat model (Section 3).              kernel version to 6.12-rc1. Figure 8 reports the temporal reso-
   We propose a simple strategy to achieve colocation with-                lution of the nanosleep method assisted by the TLB-flushing
out pinning, that works if the system has at least one idle                performance degradation technique. From the figure, it is
logical core. We use this scheme in our evaluation (Section 5).            clear that the victim retires only a few instructions between
   The idea is to exploit the load-balancing feature of the                preemptions when using a small 𝛿, a behavior closely resem-
CFS [71]. The CFS periodically checks the load on each core’s              bling that of Figure 4b.
runqueue and migrates tasks from busier cores to idle cores.                  We now discuss the preemption budget under the EEVDF
   Our approach starts with the attacker launching 𝑁 − 1                   scheduler. For simplicity, we consider the case where a single
compute-intensive dummy threads, where 𝑁 is the total                      logical core is exclusively shared between an attacker thread
number of logical cores in the system. The attacker then pins              𝐴 and a victim thread 𝑉 . When selecting a thread to execute,
these 𝑁 − 1 dummy threads to 𝑁 − 1 logical cores, leaving                  the EEVDF first identifies eligible threads whose vruntime is
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.           Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


smaller than the average vruntime of all the threads from the      encryption procedure involves 10 rounds of computation.
local runqueue. Among all the eligible threads, the EEVDF          Each round 𝑟 mixes a 16-byte input x (𝑟 ) with a 16-byte round
schedules the thread has the nearest virtual deadline to run.      key K (𝑟 ) derived from the secret key k to generate the input
Since we assume only two threads in the runqueue, there is         for the next round x (𝑟 +1) or the final ciphertext. The first
only one eligible thread; the scheduling algorithm is reduced      round input x (0) is generated by x (0) = p ⊕ k. Finally, the
to selecting the thread with the smallest vruntime. Therefore,     output of the last round x (10) is the ciphertext.
we omit the details of the virtual deadline and refer interested      To enhance performance, the OpenSSL T-table AES imple-
readers to [17, 58].                                               mentation simplifies the mixure computation in each round
   Under such a scheduling policy, the attacker thread 𝐴           with table lookups using precomputed T-tables, denoted as
can preempt the victim thread 𝑉 as long as 𝐴’s vruntime            𝑇0, . . . ,𝑇3 , each containing 256 4-byte entries. Using T-tables,
is smaller than 𝑉 ’s. As a result, the preemption budget is        each round of computation can be represented as follows.
simply the vruntime difference between 𝑉 and 𝐴 when 𝐴                (𝑟 +1)
                                                                   𝑥 0..3   ← 𝑇0 [𝑥 0(𝑟 ) ] ⊕ 𝑇1 [𝑥 5(𝑟 ) ] ⊕ 𝑇2 [𝑥 10
                                                                                                                    (𝑟 )           (𝑟 )
                                                                                                                         ] ⊕ 𝑇3 [𝑥 15        (𝑟 )
                                                                                                                                        ] ⊕ 𝐾0..3
wakes up from hibernation. We repeat the experiment from
                                                                     (𝑟 +1)
Section 4.3.2 (that characterizes the number of preemptions)       𝑥 4..7   ← 𝑇0 [𝑥 4(𝑟 ) ] ⊕ 𝑇1 [𝑥 9(𝑟 ) ] ⊕ 𝑇2 [𝑥 14
                                                                                                                    (𝑟 )
                                                                                                                         ] ⊕ 𝑇3 [𝑥 3(𝑟 ) ] ⊕ 𝐾4..7
                                                                                                                                              (𝑟 )

with EEVDF. When 𝐼𝑎𝑡𝑡𝑎𝑐𝑘𝑒𝑟 − 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 ranges between 10,000          (𝑟 +1)
                                                                   𝑥 8..11  ← 𝑇0 [𝑥 8(𝑟 ) ] ⊕ 𝑇1 [𝑥 13
                                                                                                    (𝑟 )
                                                                                                         ] ⊕ 𝑇2 [𝑥 2(𝑟 ) ] ⊕ 𝑇3 [𝑥 7(𝑟 ) ] ⊕ 𝐾8..11
                                                                                                                                              (𝑟 )
and 15,000 ns (i.e., the same setting in Figure 6 with default
                                                                     (𝑟 +1)         (𝑟 )
nice value 0), we find that the attacker can repeatedly pre-       𝑥 12..15 ← 𝑇0 [𝑥 12   ] ⊕ 𝑇1 [𝑥 1(𝑟 ) ] ⊕ 𝑇2 [𝑥 6(𝑟 ) ] ⊕ 𝑇3 [𝑥 11
                                                                                                                                   (𝑟 )      (𝑟 )
                                                                                                                                        ] ⊕ 𝐾12..15
empt the victim for a median number of 219 times from 165             Given the equation above, it is clear that the T-table AES
repeated experiments. We leave an in-depth exploration of          implementation makes memory accesses depending on the
Controlled Preemption in the EEVDF as a future work.               value of x (𝑟 ) . Since a single cache line fits 16 T-table entries,
                                                                   the attacker can recover the upper 4 bits of each byte of x (𝑟 ) ,
5     Proof-of-Concept Exploits                                    known as the upper nibble. Now consider the first round
We now show how Controlled Preemption can facilitate               computation (𝑟 = 0), where x (0) = p ⊕ k. If the attacker
breaking security-critical software. First, Section 5.1 char-      can learn information about x (0) with an attacker-controlled
acterizes a standard microarchitectural attack benchmark:          plaintext input p, they can partially recover the secret AES
Flush+Reload cache-timing attacks on the AES T-table al-           key k. This is known as the first round attack.
gorithm. Second, Section 5.2 demonstrates how Controlled              The main challenge of the first round attack is to distin-
Preemption can also be used to conduct SGX-Step-like at-           guish T-table accesses made in the first round from other
tacks from userspace and perform Prime+Probe cache-timing          rounds. This is difficult because each round of encryption
attacks. Finally, Section 5.3 demonstrates that Controlled Pre-    takes only about 120 cycles to complete on our system. There-
emption can utilize other (non cache) hardware channels            fore, we employ Controlled Preemption to monitor the vic-
(specifically, the BTB).                                           tim’s execution at a fine temporal granularity.
   All attacks follow the setup described in Section 4.3. To          Figure 9 shows one measurement trace of one T-table
colocate the attacker and victim onto the same logical core,       𝑇0 over a single AES encryption execution, where the four
we spawn 𝑁 − 1 = 15 dummy threads following Section 4.4.           lookup indexes (𝑥 0(0) , 𝑥 4(0) , 𝑥 8(0) , 𝑥 12
                                                                                                                (0)
                                                                                                                    ) used in the first round
Then, a single attacker thread performs preemptions and            have upper nibbles (0, 4, 12, 8). Yellow blocks highlight the
side-channel measurements.                                         T-table access at one cache line (y-axis) captured by one at-
                                                                   tacker measurement/sample (x-axis). As seen in Figure 9, the
5.1   Attacking T-Table AES                                        first four cache lines accessed by the victim are (0, 4, 12, 8),
To start, we demonstrate Controlled Preemption by collect-         which match the aforementioned upper nibbles of the secret
ing high temporal-resolution cache traces from a T-table           indexes. Ideally, the attacker should see a single cache ac-
Advanced Encryption Standard (AES) victim, which has               cess (one yellow block) in each sample, with no overlap. In
been widely used to evaluate cache side-channel attack tech-       practice, the attacker sees smears. This is due to imperfect
niques [11, 28, 30, 48, 54, 55, 75]. We show that Controlled       temporal resolution (stepping several vs. one instruction per
Preemption only requires 5 traces to conduct the first round       preemption) and speculative execution (more instructions
attack, which is comparable to the state of the art [11], but      execute speculatively than are retired per preemption). We
only uses a single attacker thread for preemptions instead         follow prior work to solve this by collecting more traces.
of 40 threads.
5.1.1 Overview. AES is a widely used symmetric block               5.1.2 Evaluation. In our evaluation, the attacker pro-
cipher with various key sizes. AES-128 uses a 16-byte secret       gram uses Flush+Reload [73] to monitor the victim’s secret-
key k to encrypt a 16-byte plaintext p. We use the subscript       dependent T-table accesses. Specifically, the attacker flushes
to denote the 1-byte data slice, for example, 𝑝 0 is the first     the entire T-table before napping and times the reload la-
byte of p, while 𝑝 14..15 is the last two bytes. The AES-128       tency to each entry after waking up. A short reload latency
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace                  ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.

               0
               1                                                             uses a lookup table (LUT)-based approach to translate each
               2
Cacheline Index

               3                                                             base64 character into a 6-bit binary value. This process intro-
               4
               5
               6                                                             duces secret-dependent access patterns, making it vulnerable
               7
               8                                                             to cache side-channel attacks.
               9
              10
              11                                                                OpenSSL’s base64 decoding function EVP_DecodeUpdate
              12
              13                                                             groups 64 characters to parse at a time. First, it performs a
              14
              15
                   0    5   10   15    20    25   30      35     40          validity check by looking up each character through the LUT.
                                      Sample ID                              Second, it decodes valid characters to their binary represen-
                                                                             tation. Both of the above are loops that read the LUT in a
Figure 9. Heatmap illustrating Flush+Reload results for a
                                                                             base64 character-dependent fashion. After that, the function
single AES run. The y-axis denotes attacker measurements
                                                                             returns the output and proceeds to decode the next chunk.
for each of the 16 cache lines making up the T-table (yellow
                                                                                Sieck et al. [57] uses SGX Step to single step the victim
indicates a hit during Reload; purple indicates a miss). Each
                                                                             enclave that runs the RSA PEM file decode procedure. They
column (x-axis) denotes a sample where the attacker pre-
                                                                             extract the victim’s precise LUT access pattern via a last-
empts the victim. The first four accesses (those made in the
                                                                             level cache (LLC) Prime+Probe side channel. The LUT used
first round) are circled in red.
                                                                             for translation is 128 bytes in size and spans two consec-
                                                                             utive cache lines. Knowing which cache set one LUT ac-
to an entry implies that the victim accessed the entry while                 cess touches shrinks the search space of one character. To
the attacker is napping.                                                     complete the attack, they leverage prior RSA cryptanalysis
   Following the prior work [11], we collect multiple victim                 to fully recover the RSA secret key. To reduce cache mea-
traces with randomized plaintexts to determine the upper                     surement noise caused by speculative or out-of-order execu-
nibble of each secret key byte and disable hardware prefetch-                tion, they compile the SGX program with Load Value Injec-
ers to reduce channel noise.4                                                tion [60] mitigations by setting MITIGATION-CVE2020-0551
   We demonstrate our exploits with both the CFS and                         to LOAD [3], which places load fences after every load instruc-
EEVDF schedulers. In each experiment, the victim is invoked                  tions.
5 times, generating 5 side-channel traces that correspond to                 5.2.2 Challenges. Based on the prior base64 decoding
the same key. When repeating the experiment 100 times on                     attack, we replace SGX Step with Controlled Preemption.
CFS and EEVDF, where each repetition uses a different key,                   We monitor the LLC using Prime+Probe, targeting the LUT
the attacker can infer the upper nibble of each key byte with                when it is accessed during the validity check. This is non-
an accuracy of 98.9% and 98.1%, respectively. Our recovery                   trivial compared to using SGX Step for several reasons:
accuracy and the required number of victim executions are
comparable to the state-of-the-art attack [11], which also                   Victim ‘overlooping.’ We are interested in monitoring one
exploits the CFS to single step the victim AES encryption                    LUT access per loop iteration during the validity check. This
process. But unlike Controlled Preemption, the prior attack                  presents a challenge. Ideally, we would like to set 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 to
requires 40 colocated attacker threads.                                      exactly the length of one loop iteration. Setting 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 smaller
                                                                             would result in extraneous preemptions, which consumes
5.2                Attacking SGX Enclaves                                    preemption budget. Yet, as can be seen in Figure 4b, it is
Next, we demonstrate how Controlled Preemption enables                       difficult to guarantee a specific amount of forward progress
high temporal resolution side-channel attacks on Intel SGX                   per preemption, beyond single stepping.
enclaves. This is akin to SGX Step [61, 62], but does not                       To side-step this issue, we (once again) combine Controlled
require the attacker to have supervisor privilege.                           Preemption with performance degradation techniques. We
   To start, we note that Controlled Preemption’s temporal                   construct an LLC eviction set that is congruent to the cache
resolution when attacking SGX follows very similar trends                    line that contains the LUT read instruction. This way, we
as Figure 4b but without explicit iTLB flushing (as SGX al-                  can use a larger 𝐼 𝑣𝑖𝑐𝑡𝑖𝑚 and use the instruction cache miss to
ready performs TLB flushes on asynchronous enclave exit                      stall the victim while waiting for the next preemption.
events [16]).                                                                Intra-victim induced channel noise. The LUT is accessed
5.2.1 Overview. Cryptographic keys are often stored as                       during both the validity and decode loops. We must ensure
base64-encoded PEM files for ease of transmission and are                    that measurements correspond to the former not the latter.
decoded into internal representations before use. OpenSSL                    To address this, we dual-purpose the eviction set that evicts
                                                                             the LUT access load in the validity loop (see above) to also
4 Note that the need of disabling hardware prefetchers is a limitation of    test whether the victim is in the validity or decode loop.
Flush+Reload and is not fundamental to Controlled Preemption. One can
circumvent this limitation with Prime+Probe-based attacks (used in the       Insufficient preemption budget. Recall Section 4.3.2 which
next section). We use Flush+Reload to be apples-to-apples with prior work.   characterizes the number of repeated preemptions that can
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.          Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher




Figure 10. Cache probe latency trace for the base64 decoding function running inside an SGX enclave. Blue and orange traces
denote probe latencies for the two LUT eviction sets. Red is the latency to probe the instruction cache eviction set. The grey
(white) area is the victim performing the validity (decode) loop.

be achieved by Controlled Preemption. Since a 1024-bit RSA        verify) that a userspace preemption mechanism would also
private key PEM file consists of nearly 900 base64 characters,    suffice. We verify this claim, enabling NightVision attacks
the preemption budget only allows the attacker to recover         from userspace.
about 60% of the LUT access trace.
   To address this, the attacker invokes the victim twice using   5.3.1 Overview. NightVision found that the BTB can be
the same RSA key. During the first victim execution, the          updated by both control-transfer instructions (e.g., jmp) and
attacker starts preempting the victim as soon as the victim       non-control-transfer instructions (e.g., nop). As a result, if
starts and captures the first half of the trace. During the       a non-control-transfer instruction 𝐴 collides with the BTB
second victim execution, the attacker times their hibernation     entry of a control-transfer instruction 𝐶,5 executing 𝐴 will
and only starts preempting the victim when the victim is half     invalidate 𝐶’s BTB entry, potentially leading to a control-flow
way through their execution, recovering the second half of        misprediction when 𝐶 is next fetched.
the trace. Finally, the attacker concatenates these two traces       The above enables an attacker to infer the victim’s control
to form a complete trace.                                         flow. For example, the attacker can execute a direct jump to
                                                                  create a BTB entry that collides with the victim’s instruction
5.2.3 Evaluation. We use the same victim setup (i.e., en-         of interest. Then the attacker triggers the victim execution,
ables LVI mitigation) as the prior work [57]. The attacker con-   and later checks whether its branch mispredicts. If a mispre-
structs one LLC eviction set for the victim’s load instruction    diction occurs, the attacker learns that the victim’s instruc-
(in the validity check) and two for the LUT. In the measure-      tion of interest was executed. This attack works regardless
ment phase, the attacker first probes the instruction eviction    of the type of the victim instruction.
set and then, if successful, probes the LUT eviction sets.
   Figure 10 shows one segment of the Prime+Probe measure-        5.3.2 Evaluation. As in NightVision, we attack the RSA
ment trace. The grey area represents the victim’s execution       key generation procedure in mbedTLS version 3.0 [4], which
in the validity loop, while the white area represents the exe-    contains a secret-dependent branch in the Greatest Common
cution in the decode loop. As we can see, the Prime+Probe         Divisor (GCD) function mbedtls_mpi_gcd. Extracting the
latency for the load instruction from the validity loop (the      branch direction in each loop iteration is required to fully
red line in Figure 10) is high in the grey area, but low in the   recover the RSA secret key [51]. Unlike NightVision relying
white area, indicating that it accurately detects when the        on privileged performance counters to decode BTB states,
victim is executing the validity loop.                            we use BTB Train+Probe gadgets from prior work [79] to
   We test on 30 randomized 1024-bit RSA private key files        encode branch predictor state into cache state.6
which contain on average 872 base64 characters. On average,          Before the attack, the attacker identifies two instruc-
our technique can recover the first 61.5% of the LUT access       tions, each belonging to a separate direction of the secret-
trace with a 99.2% accuracy from a single victim execution.       dependent branch. The attacker then creates two pairs of
With two victim executions and the trace concatenation            BTB Train+Probe gadgets that collide with these two victim
method (above), the attacker can fully recover the LUT access     instructions respectively. We use the same method as done
trace with 98.9% accuracy.                                        in the previous PoC (Section 5.2.2) to interrupt the victim
                                                                  at least once per loop iteration. During each measurement
5.3   Monitoring the BTB Side Channel                             phase, the attacker uses two BTB probes to infer which di-
                                                                  rection the victim most recently took.
Finally, we demonstrate how Controlled Preemption can
                                                                     We run our attack on 30 pairs of prime numbers, each of
also be applied to monitor non-cache channels by exploit-
                                                                  which results in 20∼30 loop iterations in the GCD function.
ing the BTB. We reproduce an exploit first described in
NightVision [77], which combines the fine-grain preemption        5 In our machine setup, the BTB entry is indexed by the lower 32 bits of the
primitive with a BTB side channel to leak fine-grain victim       PC. Instructions with the same lower 32 bits will collide in the BTB.
control-flow information. For the former, NightVision uses        6 Directly measuring branch prediction outcomes using the timestamp

an SGX Step-like framework, and hypothesized (but did not         counter (rdtsc) is extremely noisy [39] and not well suited for our attack.
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace            ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.


We are able to extract all branch directions in a single victim         is to use a special trusted prefetch handler after ERESUME
run with an average accuracy of 97.3%.                                  to ensure that the victim makes significant progress be-
                                                                        fore being preempted again. Unfortunately, this mechanism
6    Mitigations                                                        would require significant kernel modifications to be used by
                                                                        userspace programs. Moreover, this defense does not prevent
We discuss two avenues to mitigate Controlled Preemption:
                                                                        the attacker from making relative coarse-grained (50-100 in-
(1) blocking the underlying side channel and (2) hardening
                                                                        structions/preemption) observations that are still sufficiently
the thread scheduler.
                                                                        fine-grained to conduct certain attacks (e.g., the T-table AES
Blocking the underlying side channel. Controlled Pre-                   attack from Section 5.1).
emption increases the temporal resolution of existing
side channels. Therefore, mitigating the encapsulated side-             7   Related Work
channel leakage can stop our attack. Per Section 2.2, Con-              Improving the temporal resolution of side-channel analysis
trolled Preemption can be used in conjunction with any                  is an active area of research. Prior work splits into three
stateful/persistent channel. Thus, we focus on defenses that            categories, (1) those that induce preemption; (2) those that
nominally apply to any stateful/persistent channel.                     speed up the side-channel receiver; (3) those that slow down
   In software, the most widely-deployed channel-agnostic               the victim (performance degradation). These three are not
defense is constant-time or data-oblivious programming [10,             mutually exclusive. For example, Controlled Preemption can
13, 14, 36, 46]. Constant-time programming works by rewrit-             increase its preemption budget through approach (2) and
ing the program so that its observable execution trace is               Controlled Preemption already utilizes approach (3), e.g. TLB
independent of secret data. The downside of this approach               flushing (Section 4.3.1).
is that it may not be complete (secure). For example, re-
cent work [15, 67, 68] has demonstrated how traditional                 Preemption-based approaches. There is a rich line of work
constant-time programming guidelines are insufficient on                on preempting victim threads in an Enclave (or otherwise
modern microarchitectures. Thus, modest system/hardware                 privileged) threat model [29, 39, 45, 61, 62, 78]. Here, the
support [2, 5, 15, 76] is likely also required.                         attacker has supervisor privilege and uses supervisor capa-
   In hardware, the peer to constant-time programming is                bilities to facilitate the attack.
spatial/temporal partitioning [22, 35, 41, 80]. Partitioning               Our goal is to enable similar temporal resolution side chan-
works by isolating microarchitectural resources, used by                nel analyses in an unprivileged setting. The closest work
the victim, from the attacker. Before a partition is reused             is a series of papers [11, 12, 28, 54], starting with Cache
by programs belonging to a different security domain, its               Games [28], that also exploit the CFS to preempt the victim
microarchitectural state is flushed [1, 22]. The downside to            in a fine-grain manner. Controlled Preemption differs from
this approach is that it requires hardware support.                     these works in that they require multiple (10s to 100s) of
   Finally, one can limit hardware timer resolution [31, 64],           threads to preempt the victim. This is because prior work
i.e., aggravate the side-channel receiver. This is effective            overlooks Equation (2) in the CFS. They implicitly require
because many attacks (regardless of channel) rely on high-              attacker threads to always take the left-hand argument to the
resolution timers to detect microarchitectural events like              max function in Equation (1). This results in the following
cache misses and branch mispredictions. The downside to                 attack workflow. When an attacker thread performs a pre-
this approach is that there exists attack primitives that either        emption, it is forced to “cool down” (sleep) for a significant
do not rely on hardware timers to gather microarchitectural             period of time (𝑆𝑏𝑛𝑑 ) before it can perform another preemp-
information [19, 75, 79] or otherwise can increase the reso-            tion. If the attacker wishes to preempt the victim 𝑋 times in
lution of a low-resolution timer [37, 43].                              fast succession, it requires 𝑋 attacker threads—after the first
                                                                        preempts and is “cooling down”, the second will preempt and
Hardening the thread scheduler. Prior work [63] shows                   so on. By contrast, our work performs a careful analysis of
that setting minimum scheduling intervals for vCPUs in                  the CFS and enables repeated preemption with only a single
Xen hypervisors effectively mitigates frequent preemption-              attacker thread.
like attacks against hypervisors. Inside the Linux kernel,
a similar strategy is to enable the NO_WAKEUP_PREEMPTION                Speeding up the receiver or slowing down the victim.
feature. When this feature is enabled, the victim thread can            Beyond preempting the victim, an attacker can optimize
complete its minimum time slice before being preempted by               its receiver logic for a specific side channel (e.g., [30, 34,
the attacker’s awakening thread. The downside is that this              52]). For example, Prime+Scope [52] reduces the accesses
feature degrades system responsiveness.                                 needed to perform a Probe from the cache associativity to
   Agnostic to enforcing a minimum time slice, Constable                a single cache line; Spec-o-Scope [30] combines ideas from
et al. [16] has proposed a software-hardware co-design ap-              Prime+Scope and Katzman et al. [34] to achieve a 5 cycle
proach to mitigate single-step attacks on Intel SGX. The idea           resolution over the cache-timing channel. Preemption-based
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.                      Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


techniques, such as Controlled Preemption, differ from these                  [10] Jose Bacelar Almeida, Manuel Barbosa, Gilles Barthe, François Dupres-
works in that they are channel agnostic: they can improve                          soir, and Michael Emmi. 2016. Verifying Constant-Time Implementa-
the temporal resolution of any stateful channel.                                   tions. In USENIX Security Symposium (USENIX Security).
                                                                              [11] C. Ashokkumar, Ravi Prakash Giri, and Bernard Menezes. 2016. Highly
   Alternatively, the attacker can slow down the victim to                         Efficient Algorithms for AES Key Retrieval in Cache Access Attacks.
achieve finer-grain measurements, e.g., by flushing the active                     In IEEE European Symposium on Security and Privacy (EuroS&P).
instruction region [9, 38, 50], bus locking through cross cache               [12] C Ashokkumar, Bholanath Roy, M Bhargav Sri Venkatesh, and
line atomic memory accesses [70], and port contention [8].                         Bernard L Menezes. 2018. " S-Box" Implementation of AES is NOT
Performance degradation, by itself, cannot achieve as fine-                        Side-channel Resistant. https://ia.cr/2018/1002
                                                                              [13] Gilles Barthe, Gustavo Betarte, Juan Campo, Carlos Luna, and David
grain resolution as preemption-based methods, but can be                           Pichardie. 2014. System-level Non-interference for Constant-time
combined with preemption for various purposes (as we do                            Cryptography. In ACM SIGSAC Conference on Computer and Commu-
in Section 4.3 and Section 5.2.2).                                                 nications Security (CCS).
                                                                              [14] Gilles Barthe, Benjamin Grégoire, and Vincent Laporte. 2018. Secure
                                                                                   Compilation of Side-Channel Countermeasures: The Case of Crypto-
8    Conclusion                                                                    graphic “Constant-Time”. In Computer Security Foundations Symposium
                                                                                   (CSF).
This paper presented Controlled Preemption, the first                         [15] Boru Chen, Yingchen Wang, Pradyumna Shome, Christopher W.
userspace framework that enables a single colocated attacker                       Fletcher, David Kohlbrenner, Riccardo Paccagnella, and Daniel Genkin.
thread to nearly single step a victim thread. We comprehen-                        2024. GoFetch: Breaking Constant-Time Cryptographic Implementa-
sively characterized the Controlled Preemption primitive in                        tions Using Data Memory-Dependent Prefetchers. In USENIX Security
the context of the widely-deployed CFS and EEVDF sched-                            Symposium (USENIX Security).
                                                                              [16] Scott Constable, Jo Van Bulck, Xiang Cheng, Yuan Xiao, Cedric Xing,
ulers. We demonstrated three proof-of-concept attacks that                         Ilya Alexandrovich, Taesoo Kim, Frank Piessens, Mona Vij, and Mark
show how Controlled Preemption can improve existing at-                            Silberstein. 2023. AEX-Notify: Thwarting Precise Single-Stepping At-
tacks on multiple victim programs, inside and outside of                           tacks through Interrupt Awareness for Intel SGX Enclaves. In USENIX
SGX, and across multiple side channel types.                                       Security Symposium (USENIX Security).
                                                                              [17] Jonathan Corbet. 2023. An EEVDF CPU Scheduler for Linux. https:
                                                                                   //lwn.net/Articles/925371/.
Acknowledgments                                                               [18] Miles Dai, Riccardo Paccagnella, Miguel Gomez-Garcia, John McCalpin,
                                                                                   and Mengjia Yan. 2022. Don’t Mesh Around: Side-Channel Attacks and
We thank the anonymous reviewers for their helpful feed-                           Mitigations on Mesh Interconnects. In USENIX Security Symposium
back. This work was partially supported by NSF grants CNS-                         (USENIX Security).
1954521, CNS-1942888, CNS-2154183, and CCF-8191902; as                        [19] Craig Disselkoen, David Kohlbrenner, Leo Porter, and Dean Tullsen.
well as by a gift from Intel.                                                      2017. Prime+Abort: A Timer-Free High-Precision L3 Cache Attack
                                                                                   Using Intel TSX. In USENIX Security Symposium (USENIX Security).
                                                                              [20] Dmitry Evtyushkin, Dmitry Ponomarev, and Nael Abu-Ghazaleh. 2016.
References                                                                         Jump over ASLR: Attacking Branch Predictors to Bypass ASLR. In
                                                                                   IEEE/ACM International Symposium on Microarchitecture (MICRO).
 [1] 2018. Indirect Branch Predictor Barrier. https://www.intel.com/          [21] Dmitry Evtyushkin, Ryan Riley, Nael CSE Abu-Ghazaleh, ECE, and
     content/www/us/en/developer/articles/technical/software-security-             Dmitry Ponomarev. 2018. BranchScope: A New Side-Channel Attack
     guidance/technical-documentation/indirect-branch-predictor-                   on Directional Branch Predictor. In International Conference on Archi-
     barrier.html.
                                                                                   tectural Support for Programming Languages and Operating Systems
 [2] 2021. Arm Armv8-A Architecture Registers. https://developer.arm.              (ASPLOS).
     com/documentation/ddi0595/2021-12.                                       [22] Qian Ge, Yuval Yarom, Tom Chothia, and Gernot Heiser. 2019. Time
 [3] 2021.         Intel® Processors Load Value Injection Advi-                    Protection: The Missing OS Abstraction. In Proceedings of the European
     sory.           https://www.intel.com/content/www/us/en/security-             Conference on Computer Systems (Eurosys).
     center/advisory/intel-sa-00334.html.                                     [23] Daniel Genkin, Lev Pachmanov, Itamar Pipman, and Eran Tromer.
 [4] 2022. Mbed-TLS: An Open Source, Portable, Easy to Use, Readable and           2015. Stealing Keys from PCs Using a Radio: Cheap Electromagnetic
     Flexible SSL Library. https://github.com/Mbed-TLS/mbedtls.                    Attacks on Windowed Exponentiation. In Cryptographic Hardware
 [5] 2023. Data Operand Independent Timing Instruction Set Archi-                  and Embedded Systems (CHES).
     tecture (ISA) Guidance. https://www.intel.com/content/www/us/            [24] Daniel Genkin, Adi Shamir, and Eran Tromer. 2013. RSA Key Ex-
     en/developer/articles/technical/software-security-guidance/best-              traction via Low-Bandwidth Acoustic Cryptanalysis. In International
     practices/data-operand-independent-timing-isa-guidance.html.                  Cryptology Conference (CRYPTO).
 [6] 2024. eBPF - Introduction, Tutorials & Community Resources. https:       [25] Ben Gras, Kaveh Razavi, Herbert Bos, and Cristiano Giuffrida. 2018.
     //ebpf.io/.                                                                   Translation Leak-Aside Buffer: Defeating Cache Side-Channel Pro-
 [7] 2025. About instance autoscaling in Cloud Run services. https://cloud.        tections with TLB Attacks. In USENIX Security Symposium (USENIX
     google.com/run/docs/about-instance-autoscaling.                               Security).
 [8] Alejandro Cabrera Aldaya, Billy Bob Brumley, Sohaib ul Hassan, Ce-       [26] Daniel Gruss, Clémentine Maurice, Klaus Wagner, and Stefan Mangard.
     sar Pereida García, and Nicola Tuveri. 2019. Port Contention for Fun          2016. Flush+Flush: A Fast and Stealthy Cache Attack. In Detection of
     and Profit. In IEEE Symposium on Security and Privacy (IEEE S&P).             Intrusions and Malware, and Vulnerability Assessment (DIMVA).
 [9] Thomas Allan, Billy Bob Brumley, Katrina Falkner, Joop van de Pol, and   [27] Daniel Gruss, Raphael Spreitzer, and Stefan Mangard. 2015. Cache
     Yuval Yarom. 2016. Amplifying Side Channels through Performance               Template Attacks: Automating Attacks on Inclusive Last-Level Caches.
     Degradation. In Annual Conference on Computer Security Applications           In USENIX Security Symposium (USENIX Security).
     (ACSAC).
Controlled Preemption: Amplifying Side-Channel Attacks from Userspace                     ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.


[28] David Gullasch, Endre Bangerter, and Stephan Krenn. 2011. Cache           [47] Ingo Molnar. 2007. sched-design-CFS.txt. https://www.kernel.org/doc/
     Games – Bringing Access-Based Cache Attacks on AES to Practice. In             Documentation/scheduler/sched-design-CFS.txt.
     IEEE Symposium on Security and Privacy (IEEE S&P).                        [48] Dag Arne Osvik, Adi Shamir, and Eran Tromer. 2006. Cache Attacks
[29] Marcus Hähnel, Weidong Cui, and Marcus Peinado. 2017. High-                    and Countermeasures: The Case of AES. In The Cryptographers’ Track
     Resolution Side Channels for Untrusted Operating Systems. In USENIX            at the RSA Conference (CT-RSA).
     Annual Technical Conference (USENIX ATC).                                 [49] Riccardo Paccagnella, Licheng Luo, and Christopher W. Fletcher. 2021.
[30] Gal Horowitz, Eyal Ronen, and Yuval Yarom. 2024. Spec-o-Scope:                 Lord of the Ring(s): Side Channel Attacks on the CPU On-Chip Ring
     Cache Probing at Cache Speed. In ACM SIGSAC Conference on Com-                 Interconnect Are Practical. In USENIX Security Symposium (USENIX
     puter and Communications Security (CCS).                                       Security).
[31] Wei-Ming Hu. 1992. Reducing Timing Channels with Fuzzy Time.              [50] Cesar Pereida García, Billy Bob Brumley, and Yuval Yarom. 2016. "Make
     Journal of computer security (1992).                                           Sure DSA Signing Exponentiations Really are Constant-Time". In ACM
[32] Mehmet Sinan İnci, Berk Gülmezoğlu, Gorka Irazoqui Apecechea,                  SIGSAC Conference on Computer and Communications Security (CCS).
     Thomas Eisenbarth, and Berk Sunar. 2016. Cache Attacks Enable Bulk        [51] Ivan Puddu, Moritz Schneider, Miro Haller, and Srdjan Capkun. 2021.
     Key Recovery on the Cloud. In Cryptographic Hardware and Embedded              Frontal Attack: Leaking Control-Flow in SGX via the CPU Frontend.
     Systems (CHES).                                                                In USENIX Security Symposium (USENIX Security).
[33] Nikita Ishkov. 2015. A Complete Guide to Linux Process Scheduling.        [52] Antoon Purnal, Furkan Turan, and Ingrid Verbauwhede. 2021.
     Master’s thesis. University of Tampere.                                        Prime+Scope: Overcoming the Observer Effect for High-Precision
[34] Daniel Katzman, William Kosasih, Chitchanok Chuengsatiansup, Eyal              Cache Contention Attacks. In ACM SIGSAC Conference on Computer
     Ronen, and Yuval Yarom. 2023. The Gates of Time: Improving Cache               and Communications Security (CCS).
     Attacks with Transient Execution. In USENIX Security Symposium            [53] Thomas Ristenpart, Eran Tromer, Hovav Shacham, and Stefan Savage.
     (USENIX Security).                                                             2009. Hey, You, Get Off of My Cloud: Exploring Information Leakage in
[35] Vladimir Kiriansky, Ilia Lebedev, Saman Amarasinghe, Srinivas De-              Third-Party Compute Clouds. In ACM SIGSAC Conference on Computer
     vadas, and Joel Emer. 2018. DAWG: A Defense Against Cache Timing               and Communications Security (CCS).
     Attacks in Speculative Execution Processors. In IEEE/ACM Interna-         [54] Bholanath Roy, Ravi Prakash Giri, Ashokkumar C., and Bernard
     tional Symposium on Microarchitecture (MICRO).                                 Menezes. 2015. Design and Implementation of an Espionage Net-
[36] Paul C. Kocher. 1996. Timing Attacks on Implementations of Diffie-             work for Cache-based Side Channel Attacks on AES. In International
     Hellman, RSA, DSS, and Other Systems. In International Cryptology              Joint Conference on e-Business and Telecommunications (ICETE).
     Conference (CRYPTO).                                                      [55] Till Schlüter, Amit Choudhari, Lorenz Hetterich, Leon Trampert,
[37] David Kohlbrenner and Hovav Shacham. 2016. Trusted Browsers for                Hamed Nemati, Ahmad Ibrahim, Michael Schwarz, Christian Rossow,
     Uncertain Times. In USENIX Security Symposium (USENIX Security).               and Nils Ole Tippenhauer. 2023. FetchBench: Systematic Identifica-
[38] Andrew Kwong, Walter Wang, Jason Kim, Jonathan Berger, Daniel                  tion and Characterization of Proprietary Prefetchers. In ACM SIGSAC
     Genkin, Eyal Ronen, Hovav Shacham, Riad Wahby, and Yuval Yarom.                Conference on Computer and Communications Security (CCS).
     2023. Checking Passwords on Leaky Computers: A Side Channel Anal-         [56] Michael Schwarz, Moritz Lipp, and Daniel Gruss. 2018. JavaScript
     ysis of Chrome’s Password Leak Detect Protocol. In USENIX Security             Zero: Real JavaScript and Zero Side-Channel Attacks. In Network and
     Symposium (USENIX Security).                                                   Distributed System Security Symposium (NDSS).
[39] Sangho Lee, Ming-Wei Shih, Prasun Gera, Taesoo Kim, Hyesoon Kim,          [57] Florian Sieck, Sebastian Berndt, Jan Wichelmann, and Thomas Eisen-
     and Marcus Peinado. 2017. Inferring Fine-grained Control Flow Inside           barth. 2021. Util::Lookup: Exploiting Key Decoding in Cryptographic
     SGX Enclaves with Branch Shadowing. In USENIX Security Symposium               Libraries. In ACM SIGSAC Conference on Computer and Communica-
     (USENIX Security).                                                             tions Security (CCS).
[40] Moritz Lipp, Daniel Gruss, Raphael Spreitzer, Clémentine Maurice,         [58] I. Stoica and H. Abdel-Wahab. 1995. Earliest Eligible Virtual Deadline
     and Stefan Mangard. 2016. ARMageddon: Cache Attacks on Mobile                  First: A Flexible and Accurate Mechanism for Proportional Share Re-
     Devices. In USENIX Security Symposium (USENIX Security).                       source Allocation. https://people.eecs.berkeley.edu/~istoica/papers/
[41] Fangfei Liu, Qian Ge, Yuval Yarom, Frank Mckeen, Carlos Rozas, Ger-            eevdf-tr-95.pdf
     not Heiser, and Ruby B. Lee. 2016. CATalyst: Defeating last-level cache   [59] Andrei Tatar, Daniël Trujillo, Cristiano Giuffrida, and Herbert Bos.
     side channel attacks in cloud computing. In International Symposium            2022. TLB;DR: Enhancing TLB-based Attacks with TLB Desynchro-
     on High-Performance Computer Architecture (HPCA).                              nized Reverse Engineering. In USENIX Security Symposium (USENIX
[42] Fangfei Liu, Yuval Yarom, Qian Ge, Gernot Heiser, and Ruby B. Lee.             Security).
     2015. Last-Level Cache Side-Channel Attacks are Practical. In IEEE        [60] Jo Van Bulck, Daniel Moghimi, Michael Schwarz, Moritz Lippi, Marina
     Symposium on Security and Privacy (IEEE S&P).                                  Minkin, Daniel Genkin, Yuval Yarom, Berk Sunar, Daniel Gruss, and
[43] Ross Mcilroy, Jaroslav Sevcik, Tobias Tebbi, Ben L. Titzer, and Toon           Frank Piessens. 2020. LVI: Hijacking Transient Execution through Mi-
     Verwaest. 2019. Spectre is here to stay: An analysis of side-channels          croarchitectural Load Value Injection. In IEEE Symposium on Security
     and speculative execution. https://arxiv.org/abs/1902.05178                    and Privacy (IEEE S&P).
[44] Robert Merget, Marcus Brinkmann, Nimrod Aviram, Juraj Somorovsky,         [61] Jo Van Bulck and Frank Piessens. 2023. SGX-Step: An Open-Source
     Johannes Mittmann, and Jörg Schwenk. 2021. Raccoon Attack: Finding             Framework for Precise Dissection and Practical Exploitation of Intel
     and Exploiting Most-Significant-Bit-Oracles in TLS-DH(E). In USENIX            SGX Enclaves. In Annual Conference on Computer Security Applications
     Security Symposium (USENIX Security).                                          (ACSAC).
[45] Ahmad Moghimi, Gorka Irazoqui, and Thomas Eisenbarth. 2017.               [62] Jo Van Bulck, Frank Piessens, and Raoul Strackx. 2017. SGX-Step: A
     CacheZoom: How SGX Amplifies the Power of Cache Attacks. In                    Practical Attack Framework for Precise Enclave Execution Control. In
     Cryptographic Hardware and Embedded Systems (CHES).                            Workshop on System Software for Trusted Execution (SysTEX).
[46] David Molnar, Matt Piotrowski, David Schultz, and David Wagner.           [63] Venkatanathan Varadarajan, Thomas Ristenpart, and Michael Swift.
     2005. The Program Counter Security Model: Automatic Detection and              2014. Scheduler-based Defenses against Cross-VM Side-channels. In
     Removal of Control-Flow Side Channel Attacks. In USENIX Security               USENIX Security Symposium (USENIX Security).
     Symposium (USENIX Security).                                              [64] Bhanu C. Vattikonda, Sambit Das, and Hovav Shacham. 2011. Eliminat-
                                                                                    ing fine grained timers in Xen. In Cloud Computing Security Workshop
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands.                      Yongye Zhu, Boru Chen, Zirui Neil Zhao, and Christopher W. Fletcher


     (CCSW).                                                                  [74] Yuval Yarom, Daniel Genkin, and Nadia Heninger. 2016. CacheBleed:
[65] Junpeng Wan, Yanxiang Bi, Zhe Zhou, and Zhou Li. 2022. MeshUp:                A Timing Attack on OpenSSL Constant Time RSA. Journal of Crypto-
     Stateless Cache Side-channel Attack on CPU Mesh. In IEEE Symposium            graphic Engineering (2016).
     on Security and Privacy (IEEE S&P).                                      [75] Jiyong Yu, Aishani Dutta, Trent Jaeger, David Kohlbrenner, and
[66] Alan Wang, Boru Chen, Yingchen Wang, Christopher W. Fletcher,                 Christopher W Fletcher. 2023. Synchronization Storage Channels
     Daniel Genkin, David Kohlbrenner, and Riccardo Paccagnella. 2025.             (S2C): Timer-less Cache Side-Channel Attacks on the Apple M1 via
     Peek-a-Walk: Leaking Secrets via Page Walk Side Channels. In IEEE             Hardware Synchronization Instructions. In USENIX Security Sympo-
     Symposium on Security and Privacy (IEEE S&P).                                 sium (USENIX Security).
[67] Yingchen Wang, Riccardo Paccagnella, Elizabeth Tang He, Hovav            [76] Jiyong Yu, Lucas Hsiung, Mohamad El Hajj, and Christopher W.
     Shacham, Christopher W. Fletcher, and David Kohlbrenner. 2022.                Fletcher. 2018. Data Oblivious ISA Extensions for Side Channel-
     Hertzbleed: Turning Power Side-Channel Attacks Into Remote Timing             Resistant and High Performance Computing. In Network and Dis-
     Attacks on x86. In USENIX Security Symposium (USENIX Security).               tributed System Security Symposium (NDSS).
[68] Yingchen Wang, Riccardo Paccagnella, Alan Wandke, Zhao Gang,             [77] Jiyong Yu, Trent Jaeger, and Christopher Wardlaw Fletcher. 2023. All
     Grant Garrett-Grossman, Christopher W. Fletcher, David Kohlbrenner,           Your PC Are Belong to Us: Exploiting Non-control-Transfer Instruction
     and Hovav Shacham. 2023. DVFS Frequently Leaks Secrets: Hertzbleed            BTB Updates for Dynamic PC Extraction. In International Symposium
     Attacks Beyond SIKE, Cryptography, and CPU-Only Data. In IEEE                 on Computer Architecture (ISCA).
     Symposium on Security and Privacy (IEEE S&P).                            [78] Yinqian Zhang, Ari Juels, Michael K. Reiter, and Thomas Ristenpart.
[69] Zixuan Wang, Mohammadkazem Taram, Daniel Moghimi, Steven                      2012. Cross-VM Side Channels and Their Use to Extract Private Keys.
     Swanson, Dean Tullsen, and Jishen Zhao. 2023. NVLeak: Off-Chip                In ACM SIGSAC Conference on Computer and Communications Security
     Side-Channel Attacks via Non-Volatile Memory Systems. In USENIX               (CCS).
     Security Symposium (USENIX Security).                                    [79] Zhiyuan Zhang, Mingtian Tao, Sioli O’Connell, Chitchanok Chuengsa-
[70] Zhenyu Wu, Zhang Xu, and Haining Wang. 2012. Whispers in the                  tiansup, Daniel Genkin, and Yuval Yarom. 2023. BunnyHop: Exploiting
     hyper-space: high-speed covert channel attacks in the cloud. In USENIX        the Instruction Prefetcher. In USENIX Security Symposium (USENIX
     Security Symposium (USENIX Security).                                         Security).
[71] Wei Xu. 2024. Deep into Linux and Beyond. https://wxdublin.gitbooks.     [80] Zirui Neil Zhao, Adam Morrison, Christopher W Fletcher, and Josep
     io/deep-into-linux-and-beyond/content/cfs_internals.html.                     Torrellas. 2023. Untangle: A Principled Framework to Design Low-
[72] Mengjia Yan, Read Sprabery, Bhargava Gopireddy, Christopher W.                leakage, High-performance Dynamic Partitioning Schemes. In Interna-
     Fletcher, Roy H. Campbell, and Josep Torrellas. 2019. Attack Directo-         tional Conference on Architectural Support for Programming Languages
     ries, Not Caches: Side Channel Attacks in a Non-Inclusive World. In           and Operating Systems (ASPLOS).
     IEEE Symposium on Security and Privacy (IEEE S&P).                       [81] Zirui Neil Zhao, Adam Morrison, Christopher W. Fletcher, and Josep
[73] Yuval Yarom and Katrina Falkner. 2014. FLUSH+RELOAD: A High                   Torrellas. 2024. Last-Level Cache Side-Channel Attacks Are Feasible in
     Resolution, Low Noise, L3 Cache Side-Channel Attack. In USENIX                the Modern Public Cloud. In International Conference on Architectural
     Security Symposium (USENIX Security).                                         Support for Programming Languages and Operating Systems (ASPLOS).
