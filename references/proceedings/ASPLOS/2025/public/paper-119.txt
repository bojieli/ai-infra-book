  ClosureX: Compiler Support for Correct Persistent
                     Fuzzing
                 Rishi Ranjan                                     Ian Paterson                                    Matthew Hicks
             rishiranjan@vt.edu                                   ianp94@vt.edu                                   mdhicks2@vt.edu
                Virginia Tech                                      Virginia Tech                                    Virginia Tech
          Blacksburg, Virginia, USA                          Blacksburg, Virginia, USA                        Blacksburg, Virginia, USA

Abstract                                                                          ’25), March 30–April 3, 2025, Rotterdam, Netherlands. ACM, Salt Lake
Fuzzing is a widely adopted and pragmatic methodology for                         City, USA, 13 pages. https://doi.org/10.1145/3669940.3707281
bug hunting as a means of software hardening. Research
reveals that increasing fuzzing throughput directly increases                     1    Introduction
bug discovery rate. The highest performance fuzzing strategy
                                                                                  Modern applications grow in complexity at an unyielding
is persistent fuzzing, which reuses a single process for all test
                                                                                  rate. As code bases continue growing, so too does the tech-
cases by looping back to the start upon completion, instead
                                                                                  nical overhead required to test and validate them [22]. With
of exiting. This eliminates all process creation, initialization,
                                                                                  this growing test debt, industry turns to fuzzing to vet soft-
and tear-down costs—which are on-par with execution cost.
                                                                                  ware as an automated supplement to traditional verifica-
Unfortunately, persistent fuzzing leads to semantically incon-
                                                                                  tion [10, 17, 26].
sistent program states because process state changes from
                                                                                     Fuzzing is the process of generating test cases based on
one test case remain for subsequent test cases. This semantic
                                                                                  a set of seed test cases and running the test case against
inconsistency results in missed crashes, false crashes, and
                                                                                  a program to find bugs. Even short of bug finding, a key
overall incorrectness that undermines fuzzer effectiveness.
                                                                                  aspect of fuzzing is the discovery of “interesting” test cases
   We observe that existing fuzzing execution mechanisms
                                                                                  that explore the nether regions of program functionality. The
exist on a continuum, based on the amount of state that
                                                                                  most popular definition of “interesting” is whether a test case
gets discarded and restored between test cases. We present
                                                                                  exercises some as-of-yet unseen portion of the program; this
ClosureX, a fuzzing execution mechanism that sits at a new
                                                                                  is called coverage-guided fuzzing. Incorporating coverage
spot on this state restoration continuum, where only test-
                                                                                  into testing enables orders-of-magnitude improvement in
case-execution-specific state is reset. This fine-grain state
                                                                                  testing effectiveness compared to randomized testing.
restoration provides near-persistent performance with the
                                                                                     Previous work shows that the surest way to increase code
correctness of heavyweight state restoration. We construct
                                                                                  coverage via fuzzing is to execute more test cases [1, 11, 18].
ClosureX as a set of LLVM passes that integrate with AFL++.
                                                                                  There are two ways to increase test case execution rate: de-
Our evaluation on ten popular open-source fuzzing targets
                                                                                  crease program execution time and decrease the time spent
show that ClosureX maintains semantic correctness, while
                                                                                  executing support code. Recent work has nearly eliminated
increasing test case execution rate by over 3.5x, on aver-
                                                                                  fuzzing-related overhead incurred during the execution of
age, compared to AFL++. ClosureX also finds bugs more
                                                                                  program code by baking-in coverage tracking [6] and defer-
consistently and 1.9x faster than AFL++, with ClosureX
                                                                                  ring coverage tracking to the uncommon case of an interest-
discovering 15 0-day bugs (4 CVEs).
                                                                                  ing test case [11, 18, 20, 29]. While these systems eliminate
CCS Concepts: • Security and privacy;                                             most of the wasted cycles spent executing monitoring code,
                                                                                  they do not address cycles spent preparing for and cleaning-
Keywords: Fuzzing, System Security, Security and Privacy,
                                                                                  up from the target’s execution, i.e., execution support.
Software Testing
                                                                                     Minimizing execution support overhead centers on identi-
ACM Reference Format:                                                             fying and eliminating all sources of test-case-invariant code
Rishi Ranjan, Ian Paterson, and Matthew Hicks. 2025. ClosureX:                    execution; this paper targets minimizing process manage-
Compiler Support for Correct Persistent Fuzzing. In Proceedings of                ment overhead. At one extreme, the fuzzer creates an entirely
the 30th ACM International Conference on Architectural Support for                new process for each test case. This is a very coarse grain that
Programming Languages and Operating Systems, Volume 1 (ASPLOS
                                                                                  must create, load, and destroy a process for every test case.
              This work is licensed under a Creative Commons Attribution-         This achieves correctness at the expense of performance.
              ShareAlike International 4.0 License.                               At the opposite extreme is persistent fuzzing that reuses
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands
                                                                                  a single process for all test cases by jumping back to the
© 2025 Copyright held by the owner/author(s).
                                                                                  program’s start instead of exiting. Ignoring any changes to
ACM ISBN 979-8-4007-0698-1/25/03                                                  software-level state across test cases sacrifices correctness
https://doi.org/10.1145/3669940.3707281                                           for increased performance. In the middle of the spectrum are



                                                                            151
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                    Rishi Ranjan, Ian Paterson, and Matthew Hicks


approaches that keep parts of the process around between                own program analysis and transformation passes at compile-
test cases and rely on more fine grain state clean-up. We               time [13]. LLVM works by transforming the source code into
observe that even the most fine-grain approach is relatively            an intermediate representation called LLVM IR. The LLVM
coarse grain.                                                           IR is a strongly-typed assembly-like intermediate represen-
   Our insight is that it is possible to reuse a single process         tation with an added catch of infinite virtual registers. The
across test cases as long as we preserve the abstraction of             language and platform agnostic optimizations are applied
each test case getting its own process. Preserving this ab-             directly on the LLVM IR. The way that virtual registers are
straction requires a form of rollback recovery [8] where, at            utilized is a feature of Static Single-Assignment (SSA), which
the completion of a test case’s execution, we rollback to the           is a methodology for structuring code during parsing to en-
starting state for the next test case. We design and implement          sure every instance of a register is assigned-to only once
a fuzzer ClosureX. ClosureX logs changes to program state               in IR. Developers implement tooling in the form of LLVM
during execution (e.g., which global variables were modified)           optimizations (opt) passes written in C++, which process
and restores them before executing the next test case. By               and modify the LLVM IR.
doing this fine-grain, execution-specific state tracking and               LLVM based code modification is now the state of the art
restoration, ClosureX eliminates process creation, loading,             methodology for implementing instrumentation and tool-
and tear-down overheads—while preserving correctness.                   ing for fuzzers [6, 9, 14, 24, 25, 30]. LibFuzzer and AFL++
   We evaluate ClosureX using a set of popular open-source              both implement LLVM-based instrumentation in the form of
programs and compare ClosureX against the most pop-                     pcguard instrumentation [6, 14, 35]. Pcguard is used to mea-
ular fuzzer, AFL++. Our evaluation shows that ClosureX                  sure the state of the program at certain points in execution,
increases test case execution rate by over 3.5x and discovers           and is used for calculating the code coverage during execu-
bugs 1.9x faster—while maintaining semantic correctness.                tion to feed this information for analysis and test generation.
Across our evaluation, ClosureX finds 15 unique bugs across             Other forms of instrumentation are in the form of error de-
4 programs, resulting in 4 CVEs, and 7 patches.                         tectors [24, 30]. ClosureX is a set of LLVM passes that
   ClosureX embodies several technical contributions:                   integrate with the fuzzer AFL++.
    • We show that there is a fine-grain, execution specific
       way to track and undo state changes to create a correct          2.2 Fuzzing Overview
       version of persistent fuzzing.                                   Fuzzing is the process of feeding under-constrained test cases
    • We implement a set of LLVM passes to reset the state              into an application and monitoring the resulting execution.
       of a program to its starting state after execution.              The execution results provide insight into the behavior of the
    • We show that AFL++’s persistent mode leads to se-                 application with the end goal of finding crashes and triaging
       mantically impossible states, but ClosureX maintains             the crash-inducing test case to uncover the associated bug.
       semantic correctness.                                            The process of fuzzing consists of four steps [35]:
    • We integrate ClosureX with the leading fuzzer AFL++                   1. Program instrumentation
       and use that implementation to find 15 0-day bugs.                   2. Test case generation
    • We open source ClosureX for use and extension by                      3. Program execution and monitoring
       the broader software testing community [23].                         4. Feedback analysis and triage
                                                                           In practice, applications are fuzzed in either binary-only or
2    Background                                                         source-code-available methods. These two different modes
                                                                        mean that we have two different ways to harness the tar-
Source-available fuzzing offers the promise of the most ef-             get program and measure its execution. Binary-only fuzzing
ficient and effective fuzzing due to the ability to directly            requires analysis of the binary program to disambiguate
analyze, modify, and integrate with application source code.
                                                                        structure and inject coverage tooling at the basic block level
The two most prevalent use cases of source-available fuzzing
                                                                        utilizing binary rewriting [19, 33]. This is done traditionally
are software developers looking to vet their own code and               in the form of callbacks [9] and system signals [18, 20] at the
the vetting of open-source code. Previous work leverages                entry to each basic block to determine code coverage. Binary-
this level of program access to provide low-overhead code               only fuzzing also requires either dynamic [36] or static [4]
coverage tracing [6] and to improve effectiveness via sani-             rewriting to harness the target and insert coverage tracing
tizers [6, 24, 30]. ClosureX supplements those advance-
                                                                        code. Alternatively, source-available fuzzing harnesses the
ments with increased performance by eliminating the
                                                                        target program and inserts coverage tracing logic directly
overhead of process management.                                         into the source code [6, 9, 13, 25, 28, 30]. This allows source-
                                                                        available fuzzing to achieve both higher performance and
2.1 Compiler Optimizations                                              higher effectiveness in bug finding compared to binary-only
LLVM is a language- and platform-agnostic compiler archi-               fuzzing, making it the most common use case within indus-
tecture, which allows users to implement and integrate their            try. Microsoft, Google, and the U.S. Government all require



                                                                  152
ClosureX: Compiler Support for Correct Persistent Fuzzing                                  ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands




   Figure 1. Traditional fuzzing loop: red portions represent the current performance bottlenecks for source-available fuzzing targeted by ClosureX.



                                                              OS Support
                Management Mechanism                                                      Correct       Target       Performance
                                                     Linux    Windows MacOS
                Fresh Process - system()               ✔          ✔      ✔                    ✔         App                 1.00
                Fresh Process - fork/exec              ✔          ✗      ✔                    ✔         App                 1.27
                Forkserver                             ✔          ✗      ✔                    ✔         App                 3.27
                Kernel-based                          ✔*          ✗      ✗                    ✔         App                 6.22
                Persistent                             ✔          ✔      ✔                    ✗          Lib               14.22
                ClosureX                               ✔          ✔      ✔                    ✔        App, Lib            13.20
Table 1. Comparison of available process management techniques. ClosureX is the only approach that works across OSes, maintains correctness, can use
both whole applications and functions as targets, and is performant. *Table 2.




fuzzing within their SDLC for validating the security of their                 by the fuzzer, and determining its root-cause. ClosureX is a
tools and applications [10, 17, 26].                                           coverage-guided, mutational, source-available fuzzer.
   Test case generation is the process of taking an initial seed
corpus and using it to generate similar test cases that differ
just enough to explore new parts of a program. Ideally, test
case generation will change portions of the seed test case that
govern conditional statements, exposing unseen program
                                                                               3    Motivation
behavior. The process of generating new test cases based
on the current coverage is coverage-guided fuzzing. Test                       Faster execution of the target program is one of the most
case generation falls into two styles: generational [3, 31] and                important criteria for effective fuzzing. In order to discover
mutational [2, 6, 9, 14, 26, 35]. Generational fuzzers build test              bugs, a fuzzer needs to execute a large number of test cases.
cases from scratch, often paired with an abstract model of                     Previous work shows that fuzzers spend over 90% of their
acceptable test case as a part of grammar-based fuzzing [27,                   time on test case execution [18]. Thus, improving the execu-
32], while mutational fuzzers apply random mutations to                        tion speed of the target directly improves the fuzzer’s effec-
saved test cases to build new ones.                                            tiveness. The great success on reducing the overhead of
   Triage consists of processing the feedback obtained from                    program instrumentation to speed up fuzzing [6, 11, 18–
executing the target with a test-case and determining whether                  20] has made the overhead due to process management
the test-case is “interesting” and should influence future mu-                 the main source of run-time overhead in fuzzing. In
tations. Crash triage is the process of isolating a crash found                this section, we analyze the various process management
                                                                               techniques used in fuzzers in terms of performance and cor-
                                                                               rectness, as Table 1 highlights.



                                                                         153
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                                   Rishi Ranjan, Ian Paterson, and Matthew Hicks


                       Kernel-based snapshot                 AFL++               fork leverages knowledge of the fuzzing use case to reduce
 Linux 4.8.10                    ✔                             ✔                 the amount of process state that needs to be protected and
                                                                                 tracked across test cases. This provides a modest perfor-
 Linux 5.15.0                    ✗                             ✔
                                                                                 mance improvement, but only works for open source op-
 Linux 6.2.0                     ✗                             ✔
                                                                                 erating systems and frequent updates, as the compatabil-
 Windows                         ✗                             ✔                 ity results in Table 2 show. Real world adoption of fuzzing
 MacOS                           ✗                             ✔                 advancements shows that any fuzzing execution approach,
Table 2. Comparison of kernel-based snapshot and AFL++ portability. Clo-         while being fast and effective, must also be portable and
sureX extends AFL++ in a kernel agnostic way.
                                                                                 maintainable across platforms. Apart from compatibility, we
                                                                                 also observe that kernel-based snapshotting is not fine-grain
                                                                                 enough, that there is no need for multiple target processes,
                                                                                 and, most of all, there is no need for specialized OS support.
3.1 Fresh Process
The most primitive execution mechanism, which is the go-to                       3.4 Persistent (i.e., Single Process)
technique of target execution in Windows-based fuzzers, is                       AFL++ provides a test case execution mode through LLVM
per-test-case process creation. In this technique, a new pro-                    instrumentation called persistent mode. Persistent fuzzing,
cess, its related data structures in kernel, and all its memory                  unlike traditional fork-based fuzzing, reuses a single pro-
pages are allocated and populated with the binary for every                      cess for all test cases.1 Using only a single process for the
test case. Creating a new process for each test case provides                    entire fuzzing campaign improves test-case execution rate at
a consistent starting state for all test cases, but incurs a huge                the cost of breaking inter-test-case isolation, i.e., the effects
overhead due to time spent creating, initializing, and tearing                   on program and process state from one test case carry over
down the process. The Linux analog to fresh process cre-                         to all subsequent test cases. While persistent fuzzing elimi-
ation on Windows is the fork and exec system calls, which                        nates process management overhead [5, 6, 12], breaking this
copy the parent process (i.e., the fuzzer process) and load the                  isolation leads to program states inconsistent with program
target program’s binary in the resulting child process. We                       semantics. Semantically inconsistent states occur when a
observe that this approach erases and reloads far more of                        test cases starts in a program state not possible given the
the target program than is necessary, wasting fuzzer time.                       program’s code due to the latent effects of a previous test
                                                                                 case’s execution. The impact of semantically inconsistent
3.2 Forkserver-based
                                                                                 execution comes in the underlying idea of stale state (shown
To deal with the high overhead of target initialization, AFL-                    in Figure 2). Semantically inconsistent executions result in:
based Linux fuzzers employ a forkserver, a significant im-
provement over simple fork/exec-based execution. A fork-                              • Missed crashes: leave programs vulnerable as testers
server initially forks and loads the target binary into memory,                         require crashes to identify bugs.
pausing at the beginning of its main function. The forkserver                         • False crashes: waste tester time by requiring them
then waits for a new test case from the fuzzer. When it is                              perform in-depth analysis of test cases and program
received, the forkserver forks itself (i.e., creates a copy of                          behavior that does not lead to a bug.
the target’s process at that point in execution) and continues                        • Non-reproducability: reduce trust in fuzzing as a
executing of the child into the target’s original main, using                           useful tool for testing as regression testing tool as
the received test case. The forkserver is more fine-grain than                          program behavior is now influenced by test case order.
than creating a fresh process as it ensures that the process ini-                   To prevent semantically inconsistent executions and their
tialization steps are done—only once—and provides a trans-                       knock-on effects, developers must leverage a deep under-
parent page-level rollback of test-case-induced state changes                    standing of the code to manually track and revert all such
via copy-on-write. Even though a forkserver does away with                       persistent state changes, which is challenging even at the
process loading costs, it still suffers from page-level duplica-                 library level [14, 25]. Unfortunately, such expertise and ac-
tion/management costs, process tear-down costs, as well as                       curacy does not scale to applications.
the kernel-level cost of process duplication. We observe that
this technique is still relatively coarse-grain as it works at                   4     ClosureX Design
the page level and also relies on creating many processes.                       We observe that there is a gap between the no-state-restoration
                                                                                 of persistent mode and the general-purpose-state-restoration
3.3 Kernel-based                                                                 of process-level approaches. We posit that a best of both
An operating-system-specific improvement on the forkserver
is kernel-based snapshotting [34]. Kernel-based snapshot-                        1 In practice, developers limit the number of test cases to use a single process
ting makes fuzzing process management a first class abstrac-                     for before creating a fresh process as a hack to deal with cross-test-case state
tion with a fuzzing specific fork system call. The fuzzing                       contamination. This number can be as low as one to maintain correctness.




                                                                           154
ClosureX: Compiler Support for Correct Persistent Fuzzing                                        ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands


                                                                                      is that exit-based termination is common in fuzzing since a
                                                                                      majority of the test cases are malformed due to randomized
                                                                                      mutation and the under-constrained nature of fuzzing.
                                                                                         ClosureX handles exit calls by the target in a method
                                                                                      similar to a try catch block in modern programming lan-
                                                                                      guages. Before calling the target’s main, ClosureX sets a
                                                                                      restore point using the C language feature setjmp. setjmp
                                                                                      effectively saves the current state of the stack and registers in
                                                                                      case the ensuing execution needs to be unwound. Then, Clo-
                                                                                      sureX replaces the target’s exit calls with longjmp calls,
                                                                                      which restore the stack to the values saved by the setjmp.
                                                                                      ClosureX then rollbacks all other state changes made by
                                                                                      the target. Note that exit calls performed by libraries are
                                                                                      considered critical which need handling by the fuzzer, so
                                                                                      ClosureX leaves those intact.
                                                                                         The last part of injecting a harness is communicating with
                                                                                      the fuzzer. ClosureX inserts code that coordinates with the
                                                                                      fuzzer to wait for a new test case to be generated. Beyond
Figure 2. Example of stale state scenario, in this situation, after the first         creating a synchronized channel to pass test cases to the
iteration we get locked out of the true branch which could hold deeper                target, this informs the fuzzer that new coverage data is
points of code to explore with a certain input and without accounting for             available from the previous test case.
this we end up wasting fuzzing efforts.
                                                                                      4.2 State Restoration For Correct Execution
                                                                                      Whenever a program executes, it modifies program state
worlds approach is possible through combining static and dy-                          during its execution. Without restoring software state, se-
namic analysis to make self-contained, naturally restartable                          mantically impossible states occur that hide crashes and
programs. We advocate baking into a program the ability                               create false crashes. To ensure semantically correct starting
to track its own state changes, at a fine grain, and reset                            state for every test case, we identify the major sources of
them between test cases, enabling automatic, correct, whole-                          program state which we address and restore at the end of
application persistent fuzzing–without reliance on special                            every execution.2
OS support. Doing so achieves high performance while re-
taining semantic correctness.                                                         Program stack. The stack is the address space in the process
   To validate this best of both worlds solution, we design                           where the local variables, as well as subroutines’ information
ClosureX. ClosureX is a compile-time instrumentation tool                             are stored during program run time. This is a static memory
which transforms unmodified target programs into naturally-                           section of the process and a stack pointer is used to increase
restartable, fuzzing-oriented programs. ClosureX tackles                              or decrease the size of the stack during execution. When a
two high-level challenges:                                                            function is called, the caller stores the return address and
    • Injecting a persistent fuzzing loop into the program so                         the base pointer of its own stack frame on the stack. The
      that a single process can support executing multiple                            callee function then allocates space on the stack by reducing
      test cases.                                                                     the stack pointer appropriately. The saved return address
    • Adding fine-grain program state tracking and rollback                           and caller’s stack pointer value are used during function
      between test cases to ensure test case isolation.                               return to restore the stack back to its caller. Thus, the stack
                                                                                      holds important program state during run time. Incorrect
4.1 Injecting a Persistent Fuzzing Loop                                               restoration of the stack after every iteration leads to the
All executable C programs have a primary function named                               program running out of space or collisions with the heap.
main that the C runtime calls after initialization to start exe-                         To deal with this, there are two cases that ClosureX must
cution. After execution, the main function returns and the                            handle: normal return flows and exit flows. In normal flows,
operating system reaps the process’s resources. Instead of re-                        execution returns back to ClosureX’s persistent loop via a
turning to the C runtime, we wrap ClosureX’s main around                              series of return statements. This naturally clears the stack.
the target’s main such that the target returns to ClosureX.
   A second exit path is also possible in the case of abnormal                        2 ClosureX is an extensible platform for correct persistent fuzzing. The
program termination. Programs that detect malformed input                             types of state addressed in this section are what we saw with a diverse
often call exit to quickly pass control to the OS, which then                         set of real-world programs. Developers can use these state tracking and
reaps the target process. Making matters more challenging                             restoration patterns to handle other scenarios not accounted for here.




                                                                                155
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                            Rishi Ranjan, Ian Paterson, and Matthew Hicks


In an exit flow, ClosureX’s added longjmp uses the values                         ClosureX Pass                Functionality
saved by setjmp to restore the stack.                                             RenameMainPass Rename target’s main
                                                                                  HeapPass       Inject tracking of target’s heap memory
Program heap. The heap is a dynamic memory section
                                                                                  FilePass       Inject tracking of target’s file descriptors
which is used to fulfill the memory requests by the program
                                                                                                 Move target’s writable globals into
at run time. These requests are made using calls to malloc,                       GlobalPass
                                                                                                 a separate memory section
calloc, realloc, etc. The memory obtained by these calls                          ExitPass       Rename target’s exit calls
must later be returned to the C runtime using free. However,                                     Table 3. ClosureX LLVM Passes
most of the end user programs that we explore have memory
leaks—because they rely on the operating system to reap any
outstanding dynamic memory after execution completes.
Thus, these memory leaks are not harmful in such isolated                       We observe that during fuzzing, the file handles opened
executions due to the kernel taking care of it, but devastating              by the target fit into two sets. First is the handles which
for persistent execution as the memory leaks add up and                      are opened once during the initialization of the target i.e.,
consume all memory. This leads to false crashes.                             they’re part of the initialization process. Closing and reopen-
   We identify that memory allocations done during a pro-                    ing such handles after every iteration leads to inefficiency
cess’s lifetime originate from different sources. The C stan-                and a better approach is to reset the handle to its starting
dard library libc as well as other dynamically-loaded li-                    position. The second set of file handles are the ones opened
braries also allocate dynamic memory. If such memory is                      by the target during the fuzzing relevant execution. For cor-
freed after every iteration, it can lead to undefined behaviour              rect execution, ClosureX tracks such file handles during the
in the target. Thus, ClosureX hooks all dynamic memory                       target’s execution and closes them between test cases.
related functions in the target and frees and allocated, but
unfreed memory by the target between test cases.                             5       ClosureX Implementation
Global memory. Global memory is used by programs to                          We implement ClosureX in five compile-time passes for
store data that is accessible to all functions or to store function-         LLVM, as shown in Table 3. We choose LLVM due to its
scoped variables that need to persist across function calls.                 modularity and ease of development, but our approach can
This is a static memory region as it is present in the exe-                  be extended to any compiler toolchain like GCC. Our instru-
cutable itself. Global memory, thus is an important source of                mentation is compatible with any coverage-tracking instru-
program state—and one most harmed by persistent fuzzing.                     mentation, and to benchmark the fuzzing performance, we
After the execution, the kernel cleans up all the process-                   compile the target with AFL’s Sanitizer Coverage Guard
related memory pages which also includes global memory                       on top of ClosureX’s instrumentation.
pages. During fresh process execution, the target always
starts with a non-contaminated global memory state as it                     5.1 ClosureX Harness
is loaded directly from the executable. However persistent                   To implement correct persistent fuzzing, we inject a harness
fuzzing uses the same process for a large number of target                   to loop over the target code for each test case. Our harness
executions and contaminated global variables from previous                   initializes the target and stores relevant program state be-
execution lead to both missed and false crashes.                             fore target execution. After every iteration of the target, our
   ClosureX takes a fine-grain approach to global tracking                   harness restores the state of the target program. Listing 1
and restoration to increase performance. ClosureX narrows                    depicts the harnessed target.
down the restoration to only the relevant sections of the
global memory that are possible to be changed during execu-                   1 target_main(argc,   argv) {
tion. ClosureX creates a snapshot of potentially modifiable                   2       // Input parsing related code
global memory region of the target and restores it after ev-                  3       // Body of target program
ery target iteration using byte-level, rather than page-level                 4}
writes, improving performance.                                                5 //    Buffer for saved context
                                                                              6 jmp_buf    longjump_buf;
File descriptors. In Linux, every open file is associated with
                                                                              7
a file descriptor, which is handled by the kernel on behalf of
                                                                              8 main(argc,    argv) {
the process. The kernel sets an upper limit on the number of
                                                                              9     global copy =
file descriptors a process can use. When a process exits, the
                                                                                        copy_writable_global_sections();
kernel closes all the open file descriptors, thus cleaning up
                                                                             10     while(1) {
all zombie file handles for the process. Due to the limit on
                                                                             11       // Wait fuzzer to collect coverage and
active file descriptors, in persistent fuzzing, even a single file                        new test case
descriptor left open after an iteration leads to the process                 12       wait_for_fuzzer();
running out of file descriptors, creating false crashes.                     13       if (setjmp(longjmp_buf)) {



                                                                       156
ClosureX: Compiler Support for Correct Persistent Fuzzing                                    ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands


                                                                                   5.4 Dealing with Abnormal Program Exits
                                                                                   Apart from returning normally after execution of the target’s
                                                                                   main, a target can use the exit system call to exit the process
                                                                                   immediately—without unwinding the stack. This is a typical
                                                                                   behaviour in the case of failed file parsing.
                                                                                      To deal with this, ClosureX uses setjmp and longjmp
                                                                                   from the C language. We set a jump target at the begin-
                                                                                   ning of our harness’s main function using the setjmp func-
                                                                                   tion, which also stores the values present in general pur-
      Figure 3. The transformation performed by ClosureX’s Global pass             pose registers at the jump target. During compile-time, we
                                                                                   replace all the calls to exit in the target code with our func-
                                                                                   tion wrapper exitHook. This is done by ExitPass using
                                                                                   the replaceAllUsesWith method. The exitHook function
                                                                                   wrapper calls longjmp to the setjmp target with the exit
14             ;// Target called exit()                                            status. The longjmp function transfers the control flow back
15         } else {                                                                to our harness’s execution loop and also restores the saved
16         // This is the main function of target                                  values into the general purpose registers, restoring the stack
17             target_main(argc, argv);                                            along with them. ClosureX hooks exit calls only in the
18         }                                                                       instrumented source code of the program, and thus this has
19         // Stack already restored; restore                                      no effect on any exception handling code or exit calls in any
               everything else                                                     externally loaded libraries like standard library libc.
20         restore_global_sections();
21         reset_heap_memory();
                                                                                   5.5 Resetting Global Memory
22         close_open_file_handles();
23     }                                                                           As shown in Figure 3, to reset the global memory of the
24 }                                                                               target, our compile-time pass GlobalPass first identifies
                                                                                   the potentially modifiable global variables in the target, and
     Listing 1. Target injected with the ClosureX harness at compile time.         then moves all such variables into a new section in the target,
                                                                                   which we name as closure_global_section. In order to
                                                                                   identify potentially modifiable variables in an LLVM module,
                                                                                   we iterate over all the global variables in a module and call
5.2 Renaming main                                                                  isConstant method on it which returns true if it is a con-
We rename the target’s main function to target_main. Clo-                          stant and false otherwise. If the variable is not a constant, we
sureX’s stub harness then calls target_main from its own                           move it to the new section using the setSection API call in
main function in a loop. We perform this function renaming                         LLVM. Our approach greatly reduces the size of the global
using our LLVM Module pass RenameMainPass. This pass                               section by excluding entities such as immutable strings from
operates on the module and tries to locate a function called                       closure_global_section.
main in the module. RenameMainPass renames the function                               During the fuzzing campaign, the address of the new
using the setName method call on the Function object. In                           global section is passed as the environment variable
cases where the target takes fuzzed input from a file, the                         CLOSURE_GLOBAL_SECTION_ADDR, and the size is passed as
arguments passed to the target’s main function are the same                        the environment variable CLOSURE_GLOBAL_SECTION_SIZE.
as command-line arguments, i.e., argv and argc. When the                           The address and size of closure_global_section can be
target takes the fuzzed input directly from the command line,                      obtained using an ELF parsing tool like readelf. Observe
target_main includes logic to replace the appropriate argv                         in Figure 4, before the execution of target’s main function,
with the test case supplied by the fuzzer.                                         the new section is copied into a buffer by the ClosureX’s
                                                                                   harness. Between test cases, this buffer is used to restore the
5.3 Resetting the Program Stack                                                    writable global section, thus rolling back any modifications.
ClosureX’s stub harness implicitly resets the stack of the tar-
get. Since ClosureX’s harness’s main function replaces the                         5.6 Resetting Dynamic Memory
target’s main function, the calls to the target’s main function                    To manage the dynamic memory of the target, we deal with
are semantically correct. In the case of normal execution end                      two sources of leaks. Not only do we free all the memory
(return from target_main), the series of return statements                         leaks by the target, we also ensure that no allocated memory
which eventually flow back to the harness unwind the stack                         already freed by the target is freed by our mechanism. To
automatically.                                                                     achieve this, ClosureX tracks all the memory allocations



                                                                             157
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                   Rishi Ranjan, Ian Paterson, and Matthew Hicks


                                                                        6   Evaluation
                                                                        ClosureX provides developers the performance of persistent
                                                                        fuzzing with the correctness of fresh process fuzzing. Thus,
                                                                        the evaluation systematically explores those two criteria.
                                                                           1. Correctness: Is test case execution the same no mat-
                                                                              ter if it was executed in a fresh process versus being
                                                                              executed in the same process after many other test
                                                                              cases?
                                                                           2. Performance: What is the improvement that Clo-
                                                                              sureX’s instrumentation achieves over the state-of-
                                                                              the-art correct process management approach in terms
            Figure 4. ClosureX global resetting procedure                     of test case throughput and bug finding?




                                                                        6.1 Evaluation Setup
and deallocations by the target by replacing all instances of           We compare ClosureX with the process management mech-
the following functions in the HeapPass:                                anism of the most popular Linux fuzzer AFL++. ClosureX
    • malloc → myMalloc                                                 instrumented targets are fuzzed with AFL++ and no modifica-
    • realloc → myRealloc                                               tions are needed in AFL++ to support ClosureX’s instrumen-
    • calloc → myCalloc                                                 tation. Both ClosureX and AFL++ use the same coverage
    • free → myFree                                                     tracing and seed mutation mechanisms. Doing this isolates
HeapPass operates on an LLVM module. For every mod-                     the impact of ClosureX’s persistent execution mechanism.
ule, we declare a new function for myMalloc, myCalloc,                  The ideal outcome is ClosureX finds the same crashes
myRealloc and myFree in the module using the                            and coverage as AFL++ (since the underlying fuzzer is
getOrInsertFunction method in the module. These decla-                  the same and ClosureX maintains correctness), but
rations are resolved during the linking-phase of the compi-             much faster than AFL++. Finding new crashes faster is
lation, as they’re defined in ClosureX’s harness. We then               important as it reduces the resources needed for a given
perform the replacements using the replaceAllUsesWith                   exploration rate or decreases the vulnerability time-window
method on the Function object for every malloc-family func-             of a program.
tion. Figure 5 shows an example. When the target requests                  To ensure consistency and reproducibility, we perform
memory using malloc-family functions, our wrapper stores                all experiments on Microsoft Azure cloud instances of type
the pointer returned into a hash map structure. Whenever                Standard_DS1_v2, with 3.5 GB memory and a single CPU
a pointer is freed by the target, it is removed from the hash           core running Ubuntu 20.04 LTS. In order to account for ran-
map structure by our custom free function myFree.                       domness and add statistical rigour to our results, we perform
   After the target’s main function is executed and the control         5, 24-hour trials of each benchmark/fuzzer configuration.
flow returns to ClosureX’s harness, we iterate over all the                The evaluation consists of ten diverse, open-source bench-
pointers still stored into our hash map and free them. This             mark programs, common to fuzzing platform evaluations [16,
prevents the target from leaking dynamic memory.                        20, 26]. The benchmarks selected for the evaluation satisfy
                                                                        the following criteria:
                                                                             • Extensive fuzzing: each of the benchmark has been
5.7 Closing Open File Handles                                                  part of multiple peer-reviewed academic works or well
ClosureX deals with all file descriptors in the target with                    fuzzed open-source fuzzing benchmarks. This enables
function replacement techniques similar to resetting dy-                       cross comparison as well as making any bugs we un-
namic memory of the target. We replace file opening routine                    cover significant as they have not been discovered
fopen with fopen_hook which tracks and stores every open                       previously under extensive fuzzing.
file handle into a hash map. We replace calls to fclose with                 • Diversity: benchmarks vary in complexity and task.
fclose_hook which deletes the file handle from our hash                      • Rich variety of input formats: Table 4 shows that our
map when it is closed. This is done by our FilePass Func-                      benchmarks have a diverse set of input formats.
tion Pass. After every test case, ClosureX iterates over the
hash map and closes all the open file handles. This pattern de-
scribed is useful for managing other resource handles which
can be acquired on Linux like sockets and shared memory.



                                                                  158
ClosureX: Compiler Support for Correct Persistent Fuzzing                               ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands




                                            Figure 5. ClosureX heap resetting procedure during runtime


     Benchmark         Input Format         Executable Size
                                                                             Benchmark          ClosureX AFL++ Speedup                     𝜌 value
     bsdtar                   tar               4.7 M
                                                                             bsdtar                379M   93M    4.09                        0.0079
     libpcap                 pcap               2.4 M
                                                                             libpcap              565M   201M    2.81                        0.0079
     gpmf-parser        mp4 (GoPro)             720 K
                                                                             gpmf-parser          456M   193M    2.36                        0.0079
     libbpf               bpf object            1.9 M                        libbpf               884M   212M     4.6                        0.0079
     freetype                  ttf              4.6 M                        freetype             493M   168M    2.94                        0.0079
     giftext                  gif               232 K                        giftext              1118M  233M    4.79                        0.0079
     zlib                zlib archive           260 K                        zlib                 1117M  293M    4.00                        0.0079
     libdwarf                 ELF                2.8 K                       libdwarf             913M   265M    3.44                        0.0079
     c-blosc2               bframe               12 M                        c-blosc2             905M   213M    4.24                        0.0079
     md4c                markdown               652 K                        md4c                 538M   215M    2.50                        0.0079
                   Table 4. Evaluation benchmarks.                           Average                             3.53                        0.0079
                                                                            Table 5. Average number of test cases executed in 24 hours along with
                                                                            the statistical significance 𝜌 value for the Mann-Whitney U-test. 𝜌 < 0.05
                                                                            indicates statistical significance [15].
6.2 Test Case Execution Rate
Previous work shows that the test case throughput is the
most significant part of efficient and successful fuzzing cam-               Benchmark ClosureX AFL++ % Improvement 𝜌 value
paign [18]. ClosureX enables the target to isolate its pro-                  bsdtar                   18.13% 13.80%             31.4 0.031
cess initialization, loading, and tear-down procedures from                  libpcap                  15.64% 15.22%             2.76 0.547
the main fuzzing loop, thus improving test case through-                     gpmf-parser              14.43% 13.82%             4.41 0.222
                                                                             libbpf                    6.45%      6.34%          1.8 0.015
put. In order to measure this performance gain, we compare
                                                                             freetype                 16.30% 16.11%             1.17 0.150
ClosureX with AFL++’s forkserver process management                          giftext                  27.57% 27.21%             1.32 0.003
mechanism, which is the fastest correct process management                   zlib                     36.22% 35.91%             0.87 0.111
mechanism in fuzzing.                                                        libdwarf                  5.20%      5.04%         3.34 0.020
   We run 5, 24-hour trials for our benchmarks and report                    c-blosc2                  2.12%      1.76%        20.41 0.093
the average number of test cases executed by both fuzzers,                   md4c                     82.17% 82.08%             0.11 0.010
as well as the improvement ClosureX achieves over AFL++.                     Average                                             7.8 0.079
                                                                            Table 6. Edge Coverage percentage of ClosureX and AFL++ for different
Table 5 summarizes the throughput results. On an average,
                                                                            benchmarks, along with the 𝜌 value for Mann-Whitney U-test. 𝜌 < 0.05
ClosureX executes more than 3.5x as many test cases than                    indicates statistically significant results.
AFL++.

6.3 Code Coverage Improvement
Given that ClosureX is equivalent to fresh process execu-                     Table 6 summarizes the average code coverage for Clo-
tion, just much faster, ClosureX and AFL++ will cover the                   sureX and AFL++ across benchmarks. On an average, Clo-
same portions of code (ignoring randomness), but ClosureX                   sureX covers 7.8% more edges. Mann-Whitney U-test indi-
should do it much faster. Code coverage is an important met-                cates statistically significant improvement across five of the
ric of coverage-guided fuzzing. It guides the fuzzer to test                benchmarks.
deeper and more complex logic in the target. Our coverage
mechanism is same as AFL++, which has its own hitcount-                     6.4 Time-to-bug Improvement
based edge coverage collection implementation loosely based                 Even though faster execution and quicker code coverage
on LLVM’s Sanitizer Coverage Guards.                                        demonstrate the efficiency of a fuzzer, its practical value



                                                                      159
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                              Rishi Ranjan, Ian Paterson, and Matthew Hicks

 Benchmark ClosureX AFL++                           Bug Type                          2. Control-flow equivalence: program execution path
   c-blosc2          7148 (4)   11896 (2)        Null Ptr Deref.                         must be equivalent for both ClosureX execution and
   c-blosc2         25358 (4)   12471 (2)        Null Ptr Deref.
   c-blosc2          7442 (4)   18299 (2)        Null Ptr Deref.
                                                                                         fresh process execution.
   c-blosc2         22957 (1)   16097 (1)        Null Ptr Deref.                     To determine whether ClosureX correctly restores state,
 gpmf-parser         798 (5)     1386 (5)       Division by Zero                  we use the entire test case queue accumulated during the
 gpmf-parser        43178 (3)    2365 (1)    Unaddressable Access                 fuzzing campaign of a target. This ensures that we cover all
 gpmf-parser         2368 (5)   20340 (5)    Unaddressable Access                 seen program behaviors during fuzzing. For each input in
 gpmf-parser        33028 (5)   22721 (5)       Division by Zero
 gpmf-parser         6144 (5)    6238 (4)         Invalid Write
                                                                                  the queue, we compare two state snapshots from the target.
 gpmf-parser        14025 (2)    5493 (2)          Invalid Read                   The first snapshot is taken after executing the target in a
    libbpf            61 (5)      91 (5)         Null Ptr Deref.                  fresh process. We take the second snapshot after executing,
    libbpf           515 (5)      552 (5)        Null Ptr Deref.                  in ClosureX’s persistent mode, the test case after 1000 it-
    libbpf           2278 (5)    9964 (4)        Null Ptr Deref.                  erations of test cases randomly selected from the queue. If
     md4c            8489 (5)   22254 (4) Memcpy with negative size
     md4c           44714 (3)   54342 (2) Array out of bounds access              ClosureX correctly restores state, then the two snapshots
Table 7. Time (sec) for AFL++ and ClosureX to find bugs (number of trials         are identical.
which found that bug) for each fuzzer.
                                                                                  Dataflow equivalence
                                                                                     There are three sources of program state that ClosureX
                                                                                  must manage in our benchmark corpus. The program stack
comes from finding bugs—and finding them faster. In order                         is controlled by well-formed function calls in ClosureX’s
to showcase the practical value of ClosureX, we evaluate                          harness, as well as the arguments passed to the target’s main
AFL++ and ClosureX, on their speed and effectiveness in                           function is correct at every execution. This ensures that the
finding bugs. As a part of the performance and coverage                           stack is fresh for every iteration.
evaluation, we find 15 0-day bugs in the latest source-code                          ClosureX frees all the stale heap objects after every itera-
across 4 different benchmarks. Table 7 summarizes the bugs                        tion of the target. To verify that the dynamic memory of the
discovered. When ClosureX and AFL++ find the same bugs,                           target is correctly reset by ClosureX, we use valgrind [21],
ClosureX does so 1.9x faster. ClosureX also finds bugs                            a debugging and profiling tool, to check for dynamic mem-
more consistently than AFL++, with 25% more trials finding                        ory related inconsistencies like double-free or use-after-free.
bugs.                                                                             We find no such occurrences for any queue input in any of
                                                                                  our benchmarks. We also compare the memory usage of the
0-days and bug disclosure All the bugs reported above
                                                                                  target during its execution to a fresh process’s execution. Ex-
in Table 7 are 0-day bugs. We reported all the bugs to their
                                                                                  cluding ClosureX’s own memory, the memory usage of the
respective developers. As of writing, bugs in c-blosc2 and
                                                                                  target is identical to a fresh process execution, showcasing
gpmf-parser have been fixed. The developers of libbpf
                                                                                  that the dynamically allocated memory was freed correctly.
acknowledged the bugs. We have not heard back from the
                                                                                     To verify global state equivalence, we compare the global
md4c developers. We have been assigned 4 CVEs (CVE-2023-
                                                                                  state in the first snapshot (taken after execution of a queue
37185, CVE-2023-37186, CVE-2023-37187, CVE-2023-37188).
                                                                                  input in a fresh process) to the second snapshot. If the two
   One of the bugs found during our fuzzing campaign is a
                                                                                  global states match, we establish dataflow equivalence be-
null pointer dereferencing bug in libbpf. libbpf is a Linux
                                                                                  tween ClosureX’s persistent mode execution and a correct
kernel library used to load and run bpf object files into the
                                                                                  fresh process execution. To handle non-determinism in pro-
Linux kernel in a sandboxed environment. The library tries
                                                                                  gram execution, which happens naturally even outside of
to parse the relocation section of the crashing ELF object, and
                                                                                  fuzzing, we execute a target in a fresh process multiple times,
reads the data of relocation section, which returns a NULL
                                                                                  and compare the global state for each execution. This cap-
pointer. When this NULL pointer is accessed in the library, it
                                                                                  tures ground-truth non-determinism: we consider the bytes
causes a crash.
                                                                                  that vary between these fresh executions non-deterministic.
6.5 ClosureX Correctness                                                          The reason for the existence of such non-deterministic bytes
                                                                                  could be storing address of some data on the heap, or data
To validate that ClosureX correctly restores program state
                                                                                  generated or processed through the PRNGs.
between test cases, we show that no matter the number
                                                                                     All the targets have the same snapshot as a fresh process
or order of test cases preceding a test-case-of-interest, it is
                                                                                  execution for all the fuzzing queue entries. This establishes
equivalent, semantically, to running that test case in isola-
                                                                                  that ClosureX is dataflow equivalent to a fresh process
tion, with a fresh process. We divide this into two kinds of
                                                                                  execution.
equivalence with fresh process execution:
   1. Dataflow equivalence: program state updates must be                         Control-flow equivalence
      equivalent for both ClosureX execution and fresh                              We take a similar approach to dataflow equivalence to
      process execution.                                                          verify the execution path equivalence to a fresh process



                                                                            160
ClosureX: Compiler Support for Correct Persistent Fuzzing                          ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands


execution. For every test case in the queue, we first get the            ClosureX depends only on LLVM APIs and does not require
ground-truth path sensitive edge-coverage by running the                 any operating system level primitives to function. LLVM
target with the queue input in a fresh process. Then we                  officially supports the Windows operating system and the
iterate over ClosureX persistent mode and execute 1000                   ClosureX compile-time passes are directly applicable to
other randomly-selected queue inputs. That same process                  Windows. To extend ClosureX’s support to Windows (or
is then used to execute the test case of interest and its path           to any new OS), OS-specific functions like HeapAlloc and
sensitive edge-coverage is recorded. We then compare this                VirtualAlloc need to be hooked in a similar way to malloc
coverage data with the ground truth coverage for the queue               and free. Our techniques for tracking and restoring C lan-
input. If these two coverages are equal, ClosureX and fresh              guage APIs remain unchanged across OSes. Our technique
process execution are control-flow equivalent for this test              of resetting global state of a target by moving it to a new sec-
case. We repeat this process, independently, for all the test            tion is also directly portable to Windows and other OSes. We
cases in the queue. We find that no queue entry leads to                 expect similar or better results on the Windows operating
an inconsistency between ClosureX execution and a fresh                  system due to its lack of a copy-on-write based fork.
process execution.
   As in dataflow equivalency, we exclude test cases that                7.3 Extending ClosureX
induce a non-deterministic execution path on the target,
i.e., their execution path differs across multiple fresh process         Our benchmark targets only have four sources of latent pro-
executions. To determine the ground-truth non-deterministic              gram state: local memory, global memory, dynamic memory,
queue test cases, we run each test case 10 times within a fresh          and file descriptors. We acknowledge that there are other
process and collect the path-sensitive edge-coverage. The                sources for other programs. For example, in multi-threaded
test cases which generate a discrepancy among these fresh                processes, if the main thread spawns multiple threads, which
executions are then flagged as non-deterministic. We only                continue executing after the main thread exits, the operating
notice such non-deterministic queue inputs in the freetype               system kills all such spawned threads. However, in persis-
benchmark. We suspect that a PRNG affected control-flow                  tent mode execution, the main thread never exits but loops
sequence is the root cause.                                              back and such child threads continue executing until they’re
   We find no inconsistency in control-flow equivalence com-             killed manually. To deal with such multi-threaded programs,
pared to fresh process execution. Given no deviation in data-            we use a similar approach to dynamic memory resetting: we
or control-flow behavior, we claim that ClosureX ensures                 track all the threads that are created during an execution and
semantic consistency: test cases behave as if they were exe-             kill any threads that remain after the main thread completes
cuted in a fresh process.                                                execution (this would be when they are killed by the operat-
                                                                         ing system). This is just one example of how we extend the
                                                                         presented patterns to handle new sources of latent state.
7    Discussion                                                             We open-source ClosureX and its artifacts on GitHub [23].
7.1 Deferred Initialization                                              We are currently creating a ClosureX port for U.S. govern-
Our research shows that most of the programs have an ini-                ment use cases and expect to see ports for different OSes,
tialization phase which is independent of user input, and                fuzzers, architectures, and even programming languages due
ideally needs to be executed only once during fuzzing. To                to the low engineering effort of extending ClosureX.
provide a greater speed-up for applications that exhibit this
behavior, a more aggressive form of code motion is needed to             7.4 Handling More Complex Program State
isolate and pull initialization steps outside of the target pro-
                                                                         ClosureX is able to handle the most essential source of
gram into the stub harness at a deferred initialization point
                                                                         latent state in a program, our open source framework also
inside of the persistent fuzzing loop. Implementing this is
                                                                         allows to cover a lot more program states. However, there
non-trivial and requires additional research into flow analy-
                                                                         are some program states that require more target-tailored
sis and methodologies for hoisting large blocks of complex
                                                                         approach, some of which are:
code outside of its current context while maintaining seman-
tic accuracy. When successful, this enhancement allows for                   • Source-unavailable libraries: ClosureX does not han-
the elimination of test-case-independent initialization steps,                 dle third-party code which comes pre-compiled and
improving performance.                                                         is later linked in with the target object file. Since we
                                                                               follow a compile-time approach to insert state track-
7.2 Supporting Other Operating Systems                                         ing and resetting code in the program, we must have
While we have implemented ClosureX on the Linux op-                            access to all source code under test.
erating system, our approach is operating system agnostic,                   • Storing/retrieving state from a file: The majority of
and requires little engineering effort to support other op-                    fuzzing literature focuses on stateless programs, i.e.,
erating systems. Consider the Windows operating system:                        programs that start from a constant state and take



                                                                   161
ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands                                        Rishi Ranjan, Ian Paterson, and Matthew Hicks


      inputs in some way (e.g., command line or a file). Ex-            8.3 Less Instrumentation
      ploring effective fuzzing of stateful programs is an              Previous work shows that reducing the number of instru-
      emerging area which is not in the scope of ClosureX.              mented points in the program via dynamic rewriting of bi-
    • Custom memory allocators: An example of complex                   naries increases the throughput of the targeted applications
      program state that ClosureX currently does not han-               to effectively no overhead in the common case [11, 18–20].
      dle is custom memory allocators, which will require a             ClosureX is orthogonal to any coverage collecting mecha-
      more target-tailored approach to solve.                           nism; combining ClosureX with a coverage-guided tracing
                                                                        approach will further magnify the performance benefit of
                                                                        ClosureX given that reducing target execution time creates
8    Related Work                                                       a relative increase in process management overhead—which
                                                                        ClosureX tackles.
ClosureX is orthogonal to many other fuzzing improve-
ments, and can be combined together to achieve an even                  8.4 Faster Execution for Multi-core Fuzzing
higher performance. Thus, the focus of this section is on
competing process management schemes and on orthogonal                  Multi-core fuzzing benefits from ClosureX. Being able to
fuzzing performance improvements.                                       parallelize fuzzing efforts is greatly dependent on manag-
                                                                        ing OS resources [10, 34, 35]. By instrumenting the program
                                                                        with ClosureX, we have multiple self managing instances
                                                                        running in parallel maintaining optimal resource utilization,
8.1 Linux-kernel-based Process Management                               thus removing kernel and OS-related contentions completely.
Kernel-based snapshotting is an execution technique of restor-          Without ClosureX, resource starvation impacts the perfor-
ing the state of the process to a prior taken "snapshot". Work          mance of all fuzzing efforts leading to reduced throughput.
done in “Designing new operating primitives” implements                 We expect to unlock further improvements with ClosureX
a new system call snapshot, which stores the important as-              instrumentation. Similar scalability improvements like dual-
pects of program state like process pages, brk values and               filesystem for better in-target test case operations, as well
restores them after every execution [34]. This technique is             as in-memory test case logging for faster test case sharing
also implemented as a Linux Kernel Module in AFL++ [7].                 between fuzzer instances are compatible with ClosureX and
Implementing this technique for closed-source kernels like              will improve the scalability of multi-core fuzzing [34].
Windows or MacOS is infeasible. Even though Linux is an
open-source operating system with a rich API for tooling,               9    Conclusion
such a snapshot process still requires a significant engineer-          Advancements in reducing time spent tracing code cover-
ing effort to maintain it for the ever-changing Linux ker-              age have improved fuzzing’s performance such that process
nel, hence why we can only find one kernel version that                 management is as a significant source of run-time overhead
works with kernel-based snapshotting. The same issue is                 in fuzzing. We show that through compiler-level program
also present in AFL-LKM [7].                                            instrumentation, it is possible to create naturally restartable
                                                                        programs that allow an entire fuzzing campaign’s worth of
                                                                        test cases to execute within a single process. Doing so sim-
8.2 Binary-only Windows Process Management                              plifies fuzzing, increases performance and bug finding, and
Recent work has introduced binary-level snapshotting for                makes high-performance fuzzing independent of operating
Windows programs, where a binary rewriter adds code to                  system features. The evaluation results indicate more oppor-
checkpoint and restore a binary’s initial state [29]. WinFuzz           tunities for fuzzer improvements through more advanced,
is designed for tackling the lack of a fork system call on Win-         fuzzing-specific compiler analysis.
dows without the need to modify the Windows kernel. Even
though ClosureX improves performance over state-of-the-                 10     Acknowledgment
art forkserver approaches a comparison to WinFuzz would                 We would like to thank the reviewers for their valuable com-
be interesting as ClosureX also support Windows programs                ments and our shepherd Dr. Antony Hosking for his guid-
with minimal engineering. Unfortunately, WinFuzz is not                 ance and collaboration. This material is based upon work
yet open-source, although qualitatively, ClosureX should                supported by the National Science Foundation under Grant
outperform WinFuzz due to WinFuzz being a binary-only                   No. 2046589.
solution requiring runtime code injection overhead and im-
precision with its state checkpoints, while ClosureX inserts            References
all the snapshotting code at compile-time—-which is further              [1] Marcel Böhme, Valentin JM Manès, and Sang Kil Cha. Boosting fuzzer
optimised during compilation—and can perform fine-grain                      efficiency: An information theoretic perspective. In Proceedings of the
snapshotting.                                                                28th ACM Joint Meeting on European Software Engineering Conference




                                                                  162
ClosureX: Compiler Support for Correct Persistent Fuzzing                                         ASPLOS ’25, March 30–April 3, 2025, Rotterdam, Netherlands


     and Symposium on the Foundations of Software Engineering, pages                       Proceedings of the 2021 ACM SIGSAC Conference on Computer and
     678–689, 2020.                                                                        Communications Security, CCS ’21, page 351–365, New York, NY, USA,
 [2] Sang Kil Cha, Maverick Woo, and David Brumley. Program-adaptive                       2021. Association for Computing Machinery.
     mutational fuzzing. In 2015 IEEE Symposium on Security and Privacy,              [21] Nicholas Nethercote and Julian Seward. Valgrind: A framework for
     pages 725–741, 2015.                                                                  heavyweight dynamic binary instrumentation. In Proceedings of the
 [3] Chen Chen, Han Xu, and Baojiang Cui. Psofuzzer: A target-oriented                     28th ACM SIGPLAN Conference on Programming Language Design and
     software vulnerability detection technology based on particle swarm                   Implementation, PLDI ’07, page 89–100, New York, NY, USA, 2007.
     optimization. Applied Sciences, 11(3), 2021.                                          Association for Computing Machinery.
 [4] Sushant Dinesh, Nathan Burow, Dongyan Xu, and Mathias Payer.                     [22] Norman Peitek, Sven Apel, Chris Parnin, André Brechmann, and Janet
     Retrowrite: Statically instrumenting cots binaries for fuzzing and sani-              Siegmund. Program Comprehension and Code Complexity Metrics: A
     tization. In 2020 IEEE Symposium on Security and Privacy (SP), pages                  Replication Package of an FMRI Study, page 168–169. IEEE Press, 2021.
     1497–1511, 2020.                                                                 [23] Matthew Hicks Rishi Ranjan, Ian Paterson. ClosureX: Compiler
 [5] Andrea Fioraldi. Frida api fuzzer, 2020.                                              Support for Correct Persistent Fuzzing. https://github.com/FoRTE-
 [6] Andrea Fioraldi, Dominik Maier, Heiko Eißfeldt, and Marc Heuse.                       Research/closure.
     AFL++ : Combining incremental steps of fuzzing research. In 14th                 [24] Konstantin Serebryany, Derek Bruening, Alexander Potapenko, and
     USENIX Workshop on Offensive Technologies (WOOT 20). USENIX As-                       Dmitry Vyukov. Addresssanitizer: A fast address sanity checker. In Pro-
     sociation, August 2020.                                                               ceedings of the 2012 USENIX Conference on Annual Technical Conference,
 [7] Andrea Fioraldi, Marc van Hauser, Joey Jiao, and Heiko Eisfeldt. Afl++                USENIX ATC’12, page 28, USA, 2012. USENIX Association.
     snapshot lkm. https://github.com/AFLplusplus/AFL-Snapshot-LKM,                   [25] Kosta Serebryany. Continuous fuzzing with libfuzzer and addresssani-
     2020.                                                                                 tizer. In 2016 IEEE Cybersecurity Development (SecDev), pages 157–157,
 [8] Erol Gelenbe. A model of roll-back recovery with multiple check-                      2016.
     points. In Proceedings of the 2nd International Conference on Software           [26] Kostya Serebryany. OSS-Fuzz - google’s continuous fuzzing service
     Engineering, ICSE ’76, page 251–255, Washington, DC, USA, 1976. IEEE                  for open source software. Vancouver, BC, August 2017. USENIX Asso-
     Computer Society Press.                                                               ciation.
 [9] Google. Afl, 2017.                                                               [27] Prashast Srivastava and Mathias Payer. Gramatron: Effective grammar-
[10] Google. cluster-fuzz, 2022.                                                           aware fuzzing. In Proceedings of the 30th ACM SIGSOFT International
[11] Jinho Jung, Stephen Tong, Hong Hu, Jungwon Lim, Yonghwi Jin, and                      Symposium on Software Testing and Analysis, ISSTA 2021, page 244–256,
     Taesoo Kim. Winnie : Fuzzing windows applications with harness                        New York, NY, USA, 2021. Association for Computing Machinery.
     synthesis and fast cloning. In NDSS, 2021.                                       [28] Richard M. Stallman and GCC DeveloperCommunity. Using The Gnu
[12] George Klees, Andrew Ruef, Benji Cooper, Shiyi Wei, and Michael                       Compiler Collection: A Gnu Manual For Gcc Version 4.3.3. CreateSpace,
     Hicks. Evaluating fuzz testing. 2018.                                                 Scotts Valley, CA, 2009.
[13] C. Lattner and V. Adve. Llvm: a compilation framework for lifelong               [29] Leo Stone, Rishi Ranjan, Stefan Nagy, and Matthew Hicks. No linux, no
     program analysis amp; transformation. In International Symposium on                   problem: Fast and correct windows binary fuzzing via target-embedded
     Code Generation and Optimization, 2004. CGO 2004., pages 75–86, 2004.                 snapshotting. In 32nd USENIX Security Symposium (USENIX Security
[14] LLVM. Libfuzzer – a library for coverage-guided fuzz testing., 2022.                  23), pages 4913–4929, Anaheim, CA, August 2023. USENIX Association.
[15] Patrick E. McKnight and Julius Najab. Mann-Whitney U Test, pages                 [30] The Clang Team. Sanitizercoverage, 2019.
     1–1. John Wiley & Sons, Ltd, 2010.                                               [31] Peach Tech. “peach fuzzing platform”, 2021.
[16] Jonathan Metzman, László Szekeres, Laurent Simon, Read Sprabery,                 [32] Junjie Wang, Bihuan Chen, Lei Wei, and Yang Liu. Skyfire: Data-driven
     and Abhishek Arya. Fuzzbench: An open fuzzer benchmarking plat-                       seed generation for fuzzing. In 2017 IEEE Symposium on Security and
     form and service. In Proceedings of the 29th ACM Joint Meeting on                     Privacy (SP), pages 579–594, 2017.
     European Software Engineering Conference and Symposium on the Foun-              [33] Matthias Wenzl, Georg Merzdovnik, Johanna Ullrich, and Edgar
     dations of Software Engineering, ESEC/FSE 2021, page 1393–1403, New                   Weippl. From hack to elaborate technique—a survey on binary rewrit-
     York, NY, USA, 2021. Association for Computing Machinery.                             ing. ACM Comput. Surv., 52(3), jun 2019.
[17] Microsoft. Onefuzz, 2022.                                                        [34] Wen Xu, Sanidhya Kashyap, Changwoo Min, and Taesoo Kim. De-
[18] Stefan Nagy and Matthew Hicks. Full-speed fuzzing: Reducing fuzzing                   signing new operating primitives to improve fuzzing performance.
     overhead through coverage-guided tracing. 2019 IEEE Symposium on                      In Proceedings of the 2017 ACM SIGSAC Conference on Computer and
     Security and Privacy (SP), pages 787–802, 2019.                                       Communications Security, CCS ’17, page 2313–2328, New York, NY,
[19] Stefan Nagy, Anh Nguyen-Tuong, Jason D. Hiser, Jack W. Davidson,                      USA, 2017. Association for Computing Machinery.
     and Matthew Hicks. Breaking through binaries: Compiler-quality                   [35] M. Zalewski. Technical whitepaper on afl-fuzz. 2017.
     instrumentation for better binary-only fuzzing. In 30th USENIX Se-               [36] Zhuo Zhang, Wei You, Guanhong Tao, Yousra Aafer, Xuwei Liu, and Xi-
     curity Symposium (USENIX Security 21), pages 1683–1700. USENIX                        angyu Zhang. Stochfuzz: Sound and cost-effective fuzzing of stripped
     Association, August 2021.                                                             binaries by incremental and stochastic rewriting. In 2021 IEEE Sympo-
[20] Stefan Nagy, Anh Nguyen-Tuong, Jason D. Hiser, Jack W. Davidson,                      sium on Security and Privacy (SP), pages 659–676, 2021.
     and Matthew Hicks. Same coverage, less bloat: Accelerating binary-
     only fuzzing with coverage-preserving coverage-guided tracing. In




                                                                                163
