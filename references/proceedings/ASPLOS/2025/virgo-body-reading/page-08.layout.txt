ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands Hansung Kim, Ruohan Richard Yan, Joshua You, Tieliang Vamber Yang, Yakun Sophia Shao


      GMEM→SMEM                  GMEM→SMEM                    GMEM→SMEM
                                                                                           4.5     Mapping to FlashAttention-3
DMA
                                                                                           To better illustrate the flexibility of Virgo’s programming
        GEMM 2
Matrix Unit GEMM
                   GEMM 1             GEMM 2    GEMM 1             GEMM 2    GEMM 1        model, we demonstrate how to map FlashAttention-3 [37], a
                                                                                           GPU kernel that fuses the self-attention layer in the Trans-
rowmax elem-wise sum scale
SIMT Cores
                                max elem-wise sum scale      max elem-wise sum scale       former architecture, to Virgo. Detailed utilization and energy
                                                                                           measurements for this kernel are provided in Section 6.2.
                        fence all +                  fence all +            loop-carried
                                                                            dependency
                          barrier                      barrier                                FlashAttention-3 defines three compute pipelines that can
                                                                                           be overlapped to improve hardware utilization: (1) GEMM-1
         Figure 5. FlashAttention-3 mapping to Virgo.                                      of Q and KT tiles, (2) GEMM-2 of P and V tiles, and (3) soft-
                                                                                           max. In the Virgo microarchitecture, we can map the two
                                                                                           GEMMs sequentially to the matrix unit, and the softmax to
4.4     Mapping to the GEMM Kernel                                                         the SIMT core, allowing both to execute concurrently. Given
We now describe how the aforementioned programming                                         the producer-consumer relationship between the GEMM and
model can be used to write an optimized GEMM kernel.                                       softmax pipelines, we use fence calls and cluster-wide bar-
                                                                                           riers to synchronize and enforce ordering across compute
4.4.1 Thread block tiling. We extend the well-established                                  units. This enables a software-pipelined mapping onto the
work partitioning scheme of GEMM kernels on GPUs,which                                     hardware, along with double-buffering of intermediate tiles
employs tiling of the input matrices at multiple levels to                                 in shared memory. Figure 5 illustrates this mapping scheme.
maximize data reuse at the different memory hierarchy [28].                                Listing 1 provides simplified pseudocode of a Virgo kernel
Virgo’s matrix unit accelerates the first level of thread block                            implementing this scheme. In the loop iterating over tiles,
tiling, which leverages parallelism across clusters and caches                             the SIMT threads initiate matrix unit operations for the two
tiles at the shared memory. In comparison, Tensor Cores                                    GEMMs, and DMA operations to preload tile data into shared
accelerate the second level of warp tiling.                                                memory for the next iteration. Since all operations are asyn-
    In our configuration, the matrix unit exposes 128×64×128                               chronous, threads can immediately proceed with the softmax
as the tile size of a single operation, which determines the                               computation within the SIMT core. Notably, because both
thread block size. Each thread block, spatially partitioned                                the GEMM and softmax pipelines access the same activa-
across the (M,N) output space, completes the full GEMM                                     tion tile (smem_O), an additional fence is required. This fence
by accumulating across the K dimension temporally in a                                     only blocks until the GEMM-2 operation completes, without
loop, shown in Figure 4. As the loop iterates, the Gemmini                                 affecting subsequent operations.
matrix unit accumulates partial sum data onto its private
accumulator memory, which gets moved out and stored to
                                                                                           for (int iter = 0; iter < tiles; iter++) {
the global memory at the end of the loop. Then, the kernel                                   // block until all previous-iter async ops complete
moves on to the next (M,N) output tile that is allocated to that                             virgo_fence(0);
thread block. The kernel can launch multiple thread blocks,                                  // synchronize cores cluster-wide at every loop iteration
                                                                                             threadblock_barrier();
where the (M,N) output space is divided equally across the                                   // double-buffer SMEM tile addresses
thread blocks.                                                                               smem_Pp = (iter%2) ? smem_P0 : smem_P1; // produce
                                                                                             smem_Pc = (iter%2) ? smem_P1 : smem_P0; // consume
4.4.2 Software pipelining and double buffering. Im-                                          ...
                                                                                             // initiate asynchronous matrix ops
portantly, the Virgo-optimized GEMM kernel employs soft-                                     // GEMM-2: O = O + P*V
ware pipelining which is enabled by the asynchronous pro-                                    virgo_compute(smem_Pc, smem_Vc, smem_O, /*accum=*/1);
gramming interface of the matrix unit. As shown in Figure 4,                                 // GEMM-1: S = Q*K
                                                                                             virgo_compute(smem_Qc, smem_Kc, smem_Sp, /*accum=*/0);
while the matrix unit is computing a tile along the K dimen-                                 // asynchronously copy K and V tile for the next iter
sion consisting a consumer pipeline, either the DMA unit or                                  virgo_dma_load(gmem_K, gmem_V, smem_Kp, smem_Vp, ...);
a set of SIMT core warps collaboratively fetch the next input                                ...
                                                                                             // overlap with SIMT softmax compute
tile along the K dimension from the global memory to the                                     online_softmax(smem_Sc, smem_Pp, per_row_scale, ...);
shared memory, consisting the producer pipeline. Another                                     // block until the 2 most recent operations (GEMM-2)
set of SIMT core warps can collaborate to form an additional                                 virgo_fence(2);
                                                                                             row_rescale(smem_O, per_row_scale);
consumer pipe that does post-processing activation com-                                    }
pute on a previous result tile. Because both the producer                                  ...
and consumer pipes run in parallel, the tile data are double-                              // copy final activation tile to global memory
                                                                                           virgo_fence(0);
buffered in the shared memory. This mechanism allows both                                  virgo_dma_store(gmem_O, smem_O);
the SIMT core warps and the matrix unit to participate in use-
ful work at all times, maximizing utilization of all hardware                                    Listing 1. Pseudocode of FlashAttention-3 on Virgo.
components in the cluster.
