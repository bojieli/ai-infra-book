Virgo: Cluster-level Matrix Unit Integration in GPUs for Scalability and Energy Efficiency   ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands


                GPU                   V100       A100         H100               kernel (Table 1). Efforts like INTERPRET [30] and Duplo [29]
                                                                                 are developed to reduce duplicated data in the register file,
            Architecture              Volta     Ampere       Hopper              alleviating capacity constraints.
       Tensor FP16 TFLOPS              1x        2.5x         7.9x                  Furthermore, the tight coupling to the SIMT core’s register
       CUDA FP32 TFLOPS                1x        1.2x         4.3x               file imposes not only capacity constraints, but also bandwidth
      # of Tensor Cores (TC)           1x        0.7x         0.8x               constraints. According to [34], the register read bandwidth
           MACs per TC                 64        256          512                available in the Volta architecture is maximally utilized dur-
   Register usage (max. 255)           224        221          168               ing Tensor Core operations. Scaling up the operation size
       Warp occupancy                 12.5%      10.0%        14.1%              requires increasing register file bandwidth to deliver larger
Table 1. Scaling trends of the compute capabilities of                           operand data to the Tensor Core, introducing significant
NVIDIA datacenter GPUs across generations, and occupancy                         design challenges.
statistics of CUTLASS GEMM kernels on them. The multiply-                           Due to register file constraints, core-coupled matrix unit
accumulate (MAC) units per Tensor Core is analytically esti-                     designs typically support fine-grained operations. For exam-
mated from FLOPS using the clock frequency. For the kernel                       ple, the NVIDIA Tensor Cores handle tile sizes of 16 × 8 ×
statistics, we profile s884gemm_f16*, s16816gemm_f16* and                        16 for FP16 input operands in the MMA instruction [17];
sm90*_s64x128x16gemm_f16* for V100/A100/H100, and                                similarly, AMD CDNA2 Matrix Cores supports sizes of 16 ×
compute the average across five kernel parameter combi-                          16 × 16 and 32 × 32 × 8 in the MFMA instruction [35]. These
nations with the highest FLOPS for each architecture.                            small tile sizes incur additional data transfers from the back-
                                                                                 ing memory due to limited reuse. Additionally, they require
                                                                                 processing a large number of instructions and computing
                                                                                 extra tile memory addresses to complete the GEMM opera-
units such as the floating-point unit and the load/store unit.                   tion, leading to significant energy and power consumptions
Essentially, today’s Tensor Core is a specialized execution                      in hardware components beyond the matrix unit, including
unit for matrix multiplication that is tightly coupled into                      the instruction scheduling logic, ALU, and the register file.
the execution pipeline of the SIMT core. Prior efforts have
focused on improving the Tensor Core design in sparsity sup-
port [20, 25, 44] and generalized operations [40, 48], while
maintaining the core-coupled integration.                                        2.4    Recent Advances and Remaining Challenges
   Notably, Tensor Core has been rapidly increasing in its                       Recent industry GPU designs have made advancements in ad-
compute capabilities. Table 1 outlines the evolution of NVIDIA                   dressing the efficiency and scalability challenges of the core-
datacenter GPUs across generations [13, 14, 16]. The FP16                        coupled approach described above. Namely, NVIDIA Am-
throughput of Tensor Cores has improved at a rate surpass-                       pere Tensor Core introduces Asynchronous Data Copy [15],
ing that of FP32 throughput in CUDA cores. This increase                         which adds hardware acceleration for transferring data data
in throughput is not due to an increase in the number of                         directly from the global memory to the shared memory,
Tensor Cores, but rather to each Tensor Core instance grow-                      bypassing the core’s register file. By offloading data copy
ing larger in size. This trend underscores the need to scale                     operations from the core, resource usage within the core
up the individual capabilities of the matrix units for future                    pipeline—such as register files and instruction slots—is re-
generations of GPUs.                                                             duced, enabling more efficient allocation of such resources
                                                                                 to matrix operations. Additionally, this offloading enables
2.3    Limitations of the Core-Coupled Approach                                  asynchronous overlapping of data movement and compute,
However, the tightly core-coupled nature of today’s Tensor                       improving efficiency compared relying solely on warp con-
Core design poses significant challenges to further scaling of                   currency. In our profiling of CUTLASS, we observe 36% less
the matrix unit. First, modern GEMM kernels generate high                        warp stalls in the async-copy-enabled GEMM kernels, which
register pressure as they require extensive use of register file                 led to 22% increase in issue slot utilization.
space to store multiple input and accumulator data [28]. This                       Building upon Ampere, NVIDIA’s Hopper Tensor Core,
issue is compounded in kernels that utilize dedicated matrix                     introduced in 2022, further alleviates register pressure with
units, where the entire input and accumulator matrix tiles                       the introduction of the wgmma operation. This new mode of
must be stored within the register file, significantly increas-                  operation enables the Tensor Cores to read matrix operands
ing the register usage. As a result, this leads to decreased                     directly from shared memory, reducing dependency on the
kernel occupancy or frequent register spills to stack memory                     register file [16, 32]. This new operand delivery method mit-
in Tensor Core-accelerated GEMM kernels, as reported in                          igates the need to store the matrix data in the register file,
previous studies [7, 41, 43, 45, 47]. Our characterization of                    alleviating the scalability limitation. This is evidenced by the
CUTLASS GEMM kernels [28] confirms this as well, where                           significant decrease in register usage that enabled further
high register usage significantly limits warp occupancy of the                   scale-up of MACs in Hopper, shown in Table 1.
