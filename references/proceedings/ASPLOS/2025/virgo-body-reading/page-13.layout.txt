Virgo: Cluster-level Matrix Unit Integration in GPUs for Scalability and Energy Efficiency          ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands


                                                                      Dot-product Unit                             Tile      Matrix Unit    SMEM Footprint
     Tightly-coupled
    (Volta, Ampere)                                                   Operands Buffer                           Fragment      Design        MiB   Norm.
                                                                      Result Buffer
                                                                      Control              Tightly-coupled         8×8        Per-core        6       2.67
 Operand-decoupled                                                                        Operand-decoupled       16×16       Per-core        4       1.78
          (Hopper)
                                                                                           Disaggregated          16×16      Per-cluster    2.25      1.00

     Disaggregated                                                    Systolic Array     Table 4. On-chip shared memory read footprint compared
            (Virgo)                                                   Accum Mem          across the GPU designs. Virgo’s disaggregated matrix unit
                                                                      SMEM Interface
                       0          500       1000     1500    2000     Control            improves data reuse by (1) enlarging tile size over the tightly-
                           Matrix Unit Active Energy Breakdown (uJ)                      coupled design, and (2) unifying into a single cluster-level
                                                                                         unit over the operand-decoupled design.
Figure 11. Active energy breakdown of the matrix unit for
the 1024×1024 GEMM kernel.

                                                                                         energy efficiency primarily come from the aspects of opera-
                                                                                         tion granularity and operand/accumulator offloading, rather
operand delivery also significantly impacts core power. In a                             than from choosing a specific design for the matrix unit.
Volta-style tightly-coupled Tensor Core, operand matrices
are accessed by fetching register operands from the regis-                               6.1.3 Data Reuse in the Shared Memory. Table 4 lists
ter file as encoded in the instruction. Staging the operands                             the shared memory read footprint for the 256×256×256 GEMM
through the register file incurs additional energy compared                              kernel. “Tile fragment” refers to the amount of matrix data
to accessing them directly from the shared memory, as both                               transferred from shared memory to compute units in a single
the Hopper-style and Virgo matrix units do. This design                                  access. Its size depends on the backing memory internal to
choice explains the significant reduction in the issue stage                             the matrix unit that stages data between shared memory
power, which includes register file access power, from the                               and PEs: the operand buffer for both tightly-coupled and
Ampere-style to Hopper and Virgo figures.                                                operand-decoupled Tensor Cores, and the systolic array reg-
   Decoupled accumulation. Although the operands are                                     isters for Virgo. Unlike the tightly-coupled design, where
offloaded, the Hopper-style operand-decoupled Tensor Core                                tile size is severely constrained by the register file space,
continues to access accumulator data from the core regis-                                operand-decoupled and disaggregated designs increase tile
ter file. This explains the nontrivial core issue stage power                            fragment size by offloading operand storage, improving data
consumption in the Hopper-style baseline (Figure 10). In                                 reuse as seen in the lower footprint.
contrast, Virgo takes an additional step by offloading accu-                                More importantly, Virgo further improves data reuse be-
mulator data from the register file to a dedicated SRAM-based                            yond the operand-decoupled design by unifying the matrix
accumulator memory. The accumulator memory is single-                                    units into a single instance at the cluster level. This unified
banked, unlike the multi-banked register file that needs to                              design enables data sharing in scenarios where non-unified
support divergent SIMT accesses, reducing energy consump-                                Tensor Cores would otherwise duplicate shared memory ac-
tion per access. In addition, the Virgo matrix unit’s systolic                           cesses. Specifically, in GEMM, different Tensor Cores may
array architecture allows accumulation to partially occur                                compute output tiles along the same row or column. How-
within the mesh, reducing the number of accesses to the                                  ever, due to their physical separation across cores, they must
memory and further saving power.                                                         independently access the input row or column data multiple
   Energy consumption in matrix units. Figure 11 shows                                   times. In contrast, a unified design allows complete reuse
the active energy consumption of the matrix unit in isola-                               of input matrix data, which explains the further memory
tion. We focus on energy rather than power for the matrix                                footprint reduction for Virgo compared to the Hopper-style
unit, because energy directly correlates with the total FLOPs                            operand-decoupled design.
which is kept the same across the designs. The figure does                                  This reduction in memory footprint has both performance
indicate that energy consumption in the matrix unit, espe-                               and energy implications. We had to scale up the shared mem-
cially within the processing elements (“Dot-product Unit”                                ory bandwidth by 2x for Volta and Ampere-style designs
and “Systolic Array”), is similar across the designs. The PE                             with a more aggressive banking strategy, because otherwise
energy is slightly lower for Virgo, due to its use of fused                              the matrix unit utilization would be bottlenecked by mem-
multiply-add units in the systolic array, which are more                                 ory bandwidth. In one configuration, this change increased
energy-efficient than the separate multipliers and adders                                utilization from 46.9% to 55.0%. With better data reuse, Virgo
used in Tensor Core’s tree-reduction PEs [34]. However, this                             achieved optimal utilization without demanding higher mem-
difference in the matrix unit energy is minuscule compared                               ory bandwidth. Energy-wise, the shared memory in the Virgo
to the overall system-level energy savings at the SoC (Fig-                              design consumed 41.0% less active energy compared to the
ure 8). This suggests that Virgo’s advantages in power and                               operand-decoupled design.
