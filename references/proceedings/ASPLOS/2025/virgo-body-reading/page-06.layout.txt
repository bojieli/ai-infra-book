ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands Hansung Kim, Ruohan Richard Yan, Joshua You, Tieliang Vamber Yang, Yakun Sophia Shao


accounts for 5.5% of the area and 6.8% of the power consump-              3.2.4 Efficient Data Movement with DMA. We further
tion in the SoC. Also, compared to a baseline design that                 facilitate efficient data delivery to the matrix units beyond
does not support Gemmini connection, the shared memory                    what memory coalescing provides by incorporating a dedi-
area only increases by 9.6%.                                              cated DMA engine. Hardware-accelerated data movement
                                                                          is becoming increasingly common in modern GPU designs.
3.2.2 Accumulator Memory. Virgo’s disaggregated ma-
                                                                          The NVIDIA Ampere architecture introduces Asynchronous
trix unit features a dedicated SRAM memory for storing
                                                                          Data Copy to accelerate direct memory transfers between
accumulator matrix data, also shown in Figure 2. Equipping
                                                                          global and shared memory [15], while the Hopper architec-
the matrix unit with a dedicated accumulator memory is an
                                                                          ture features a dedicated TMA engine that supports flexible
important design choice we make for two reasons.
                                                                          address generation modes [16]. In our models of Ampere-
   First, this design enables providing the matrix units with
                                                                          style and Hopper-style Tensor Core systems, as well as in
a large accumulator data storage without impacting paral-
                                                                          Virgo, we include a MMIO-programmable DMA engine in
lelism of the kernel. GPU’s register file space is privatized
                                                                          the cluster dedicated to issuing direct memory accesses be-
across different warps in the core, establishing the SIMT ab-
                                                                          tween global memory and shared memory. Additionally in
straction. Because core-coupled matrix units such as Tensor
                                                                          Virgo, the same DMA is capable of drawing from the matrix
Cores accumulate data and return the result also through
                                                                          unit’s accumulator memory as well, depicted in Figure 2.
the SIMT registers, scaling up the matrix units increases per-
warp register usage, and results in higher register pressure
and decreased occupancy of the kernel (see also Table 1).                 3.3    Cluster-wide Synchronization
While lower occupancy does not always lead to lower matrix                As will be discussed in Section 4, the SIMT cores in a cluster
FLOPS, it may affect the performance of fused DNN ker-                    work together collaboratively to either move matrix data to
nels where the activation compute has to run with reduced                 the matrix unit, or do post-processing computation on the
thread-level parallelism. In contrast, providing matrix units             resulting matrix from the unit. This requires an efficient syn-
with access to a separate accumulator memory without SIMT                 chronization mechanism across the cores to be implemented
privatization decouples the issue of register pressure from               at the entire cluster level.
the size of matrix compute, and allows the hardware designer                 To this end, we design a lightweight synchronizer mod-
to scale up matrix units without worrying its impact to the               ule in the cluster that interfaces with the warp scheduler
overall kernel performance.                                               of each SIMT core. When a designated set of warps reach a
   Second, regular access patterns to the accumulator data                barrier instruction in the kernel, the warp scheduler issues
enables a simpler and more energy-efficient memory de-                    a barrier release request to the synchronizer. The synchro-
sign. Unlike the register file, which must support divergent              nizer collects requests from other cores in the cluster, and
scatter-gather SIMT accesses, matrix units perform wide                   replies once receiving requests from all cores, ensuring every
and contiguous accesses to the accumulator data. This en-                 core participates in the barrier. We reuse Vortex’s vx_bar
ables a straightforward SRAM implementation—specifically,                 instruction to allow the programmer to specify which warps
the Virgo unit employs a single-banked SRAM, reducing                     participate in a particular barrier, and be able to use multiple
hardware complexity and lowering energy consumption per                   barriers in the kernel.
access. We present quantitative results in Section 6.1.2.
3.2.3 Memory coalescer. Memory coalescing [18] plays a                    4     Virgo Programming Model
crucial role in GPU memory systems by converting scalar                   As a result of architectural disaggregation from the core,
SIMT memory accesses into wide, vectorized accesses, and                  the matrix unit in Virgo operates as a separate thread of
thereby achieving high effective memory throughput. At the                execution from the SIMT cores. In this section, we estab-
onset of the project, Vortex had lacked hardware support                  lish a programming model that enables the programmer to
for memory coalescing, significantly limiting the memory                  efficiently coordinate the matrix unit and the SIMT cores,
bandwidth of SIMT load and store instructions. To this end,               based on two principles: (1) an asynchronous programming
we implemented a custom memory coalescing unit as shown                   interface and (2) collaborative execution of warps.
in Figure 2. The coalescer monitors the core’s SIMT memory
accesses at the core and L1 cache interface, and opportunisti-
cally merges them into wider memory requests of the same                  4.1    Asynchronous Matrix Unit Interface
size as the L1 cache line. The coalescer significantly increased          Virgo exposes an asynchronous programming interface to
data delivery rate from the global memory (i.e. DRAM, L2                  allow the programmer to flexibly overlap SIMT core com-
and L1) to the cores, and improved utilization of the matrix              pute concurrently to the matrix operations. As discussed in
units. Note that the coalescer is used for the Tightly-Coupled,           Section 3.1, the Gemmini-based matrix unit in Virgo exposes
Volta-style configuration (Section 2.5), which utilizes SIMT              a memory-mapped IO interface to the core. The accesses
instructions for data delivery instead of a DMA.                          to the MMIO interface are non-blocking, such that a single
