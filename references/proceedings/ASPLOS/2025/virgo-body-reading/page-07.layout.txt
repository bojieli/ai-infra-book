Virgo: Cluster-level Matrix Unit Integration in GPUs for Scalability and Energy Efficiency                                ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands

 SIMT+Tensor Cores
                                                    repeat K times                                            The collaborative execution of warps requires the cluster-
 DMA      L2→SMEM(0)          L2→SMEM(k+1)                                                     Store Result   wide synchronization mechanism described in Section 3.3,
 SIMT Cores,                  SMEM→RF      GEMM
                                                                             warp 0
                                                                                       Elem-wise              because the warps in the thread block may be running in
                                                                             warp 1
 with Tensor Cores
                     warp 2
                     warp 3
                                                                                                              different cores in the cluster.
                        fence +                                                  fence +
                         barrier                                                  barrier


  Virgo: SIMT+Cluster Matrix Unit                                                                             4.3    API Design
                                repeat K times
                                                                                                              Virgo’s application programming interface consists of two
  Matrix Unit                 GEMM + Accumulate (k)
                                                                                                              levels: A low-level interface that directly exposes the initia-
           L2→SMEM(0)         L2→SMEM(k+1)                   Store GEMM Result
  DMA
                                                                                                              tion and synchronization mechanism of the hardware, and
                                                    Store Activated Result                                    a higher-level library that helps the programmer to express
  SIMT Cores                  Activate                                                                        GEMM computations. The low-level API is composed of the
                              previous (m,n) Tile          all warps in thread block        Activate

                                                                                                              following operations:
Figure 4. Execution in the SIMT core, matrix unit, and DMA
                                                                                                                    • virgo_dma_{load,store} initiates an asynchronous
during a GEMM operation on Virgo and Tensor Core-based
                                                                                                                      DMA operation to copy the tile across the global mem-
designs.
                                                                                                                      ory, the shared memory and the accumulator memory
                                                                                                                      inside the matrix unit. It takes as argument the base
SIMT warp can kick off a matrix unit operation and then con-                                                          addresses of the tiles, matrix dimensions, and their
tinue to execute the next instructions in the kernel without                                                          memory layout, which configures the DMA for cor-
stalls. This enables a software-pipelining scheme where the                                                           rect addresses generation.
warp can overlap independent operations such as data move-                                                          • virgo_compute kicks off the asynchronous matrix
ment for the next tile or post-processed activation while the                                                         multiply-and-accumulate operation in the matrix unit,
long-latency matrix operations occur. We will detail how                                                              accessing the tile data from the shared memory.
the programmer can exploit the instruction-level parallelism                                                        • virgo_fence blocks the warp until all preceding asyn-
through this interface in Sections 4.4 and 4.5.                                                                       chronous operations have completed and their results
   An asynchronous matrix unit design is common in mod-                                                               are visible to the programmer.
ern GPU architectures. Notably, the NVIDIA Hopper Tensor
Core [16] and the AMD CDNA2 Matrix Core [2] support                                                              As described in the previous section, these API calls are
same-warp co-execution of matrix and SIMT vector com-                                                         used in conjunction with the cluster-wide synchronization
putations. Without such a mechanism, the SIMT core relies                                                     barrier to ensure all participating cores remain synchronized
solely on multithreading across multiple warps to hide the                                                    in relation to the matrix operation.
latency of matrix operations, which requires high warp occu-                                                     On top of the low-level API, we provide a library that con-
pancy to ensure the scheduler always has unblocked warps                                                      structs a full GEMM kernel using tiled matrix multiplication.
for issue. In contrast, the asynchronous interface enables                                                    The library consists of C++ templates that, given GEMM
programmers to exploit instruction-level parallelism within                                                   dimension constants, instantiates to a concrete kernel spe-
a single warp, providing additional opportunities for latency                                                 cialized to each GEMM size by determining (M,N,K) loop
hiding. Virgo particularly benefits from this interface, as its                                               iterations at compile time. Using the library, the program-
enhanced scalability supports larger tile sizes, which intro-                                                 mer can easily invoke a device-level GEMM kernel without
duce longer compute latencies to be overlapped.                                                               worrying about the details of mapping to the matrix unit, or
                                                                                                              write a fused kernel that includes a GEMM component.
4.2      Collaborative Execution of Warps                                                                        This API design is similar to existing GPU GEMM libraries
GEMM operation on GPUs require close coordination be-                                                         such as Composable Kernel [3] or CUTLASS [28], which
tween the core operation and the dedicated matrix unit                                                        similarly construct a hierarchy of API layers on top of the
operation, where the core handles data movement opera-                                                        hardware-dependent, tile-level operators. One difference
tions between the off-chip and on-chip memory, or post-                                                       is that, in Virgo, the indivisible atom of computation is a
processing compute on the result matrix such as non-linear                                                    threadblock- or workgroup-level matrix multiplication as a
activations in a deep learning workload. Therefore, the SIMT                                                  result of the enlarged tile size, while the other libraries map
cores need to achieve adequate throughput that matches                                                        to finer-grained operations at the warp or warpgroup-level
compute throughput of the matrix unit. To this end, in Virgo,                                                 atoms. Virgo’s larger compute unit enables more optimal
multiple warps collaborate to participate in a single opera-                                                  hardware scheduling decisions while reducing instruction
tion of the matrix unit. This is depicted in Figure 4, where all                                              processing overhead within the core, as discussed in as dis-
warps in the thread block works on activating the previous                                                    cussed in Section 6.1.2. However, this approach sacrifices
(M,N) output tile computed by the matrix unit operation.                                                      flexibility for workloads that use smaller matrix dimensions.
