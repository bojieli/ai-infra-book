Virgo: Cluster-level Matrix Unit Integration in GPUs for Scalability and Energy Efficiency       ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands

                                                                                     To SMEM     To RF                              From RF        From SMEM
4.5.1 Synchronization Overhead. To maximize overlap
                                                                                                               Access     Execute                    responses
and achieve high utilization in the mapping scheme de-                                                                              C data
                                                                                      requests                                               Mem
                                                                                                                                             Queue     A       B   tag
scribed, the synchronization between the core and the matrix
unit must have minimal overhead. To assess this, we mea-                             Address         State                     A Operand             Dot Product           B Operand
                                                                                     Gen             Machine
sure the cycle times the core spends in the busy register                                                                      Buffer                ×
                                                                                                                                                          +
                                                                                                                                                               ×           Buffer

polling loop within the virgo_fence operation. On average,                                                              Metadata                                     Step FSM
this interval is 260 cycles, which accounts for 2.4% of the                                                             Queue                              +

total runtime. This indicates that the kernel exploits high                        Addr A Addr B Reg C Warp ID                           Warp ID       Data        Reg C
instruction-level parallelism, as all remaining cycles overlap                               Dispatch                                                Writeback

matrix operations with SIMT computations.
   The synchronization overhead also depends on the pipelin-                     Figure 6. Microarchitecture of the decoupled Tensor Core,
ing scheme used for the workload and its throughput-matching                     which models NVIDIA Hopper’s integration scheme of de-
behavior. In our experiment, the softmax execution com-                          coupling operand storage to the shared memory [10]. The
pletes slightly earlier than the GEMM. In workloads where                        decoupled access/execute architecture effectively overlaps
pipeline throughputs are ideally matched, or where the ma-                       the access latency to the shared memory with compute.
trix computation completes first, the synchronization over-
head can be lower than what we measure.
                                                                                    Since our Vortex core configuration has a narrower SIMT
5     Methodology                                                                width than Volta (8 vs. 32), we correspondingly scale the
In this section, we outline how we implement both our pro-                       design to achieve full efficiency of the hardware. Specifically,
posed microarchitecture and the baseline GPU designs in                          we instantiate a single octet instance instead of four in the
RTL, and then set up experiments for evaluation.                                 original design [34]. The tile size of a single wmma instruc-
                                                                                 tion is determined by the register file space. As discussed
5.1    Baseline GPU Designs                                                      in Section 3.2.2, Tensor Core operates at a per-warp gran-
                                                                                 ularity, and therefore can access the register space SIMT-
We first describe how we implement the baseline GPU de-                          privatized to each warp, which is 1 KiB in our configura-
signs that feature core-coupled integration methods of ma-                       tion. This space allows storing two 8×16 FP16 operands and
trix unit. We continue to follow the classification we estab-                    one 8×8 FP32 accumulator tile, resulting in the tile size of
lished in Section 2.5, and focus on how to faithfully model                      (m,n,k)=(8,8,16). The available read bandwidth from the
each of the design points. This allows us to better evaluate                     register file limits the number of FP16 MACs per unit to 32.
our design in a way that achieves separation of concerns                         Our resulting Tensor Core implementation maintains the
without conflating multiple design considerations at play.                       same timing behavior of 2 cycles per HMMA step instruction
                                                                                 as in Volta, along with an identical Tensor-to-SIMT FLOPS
5.1.1 Volta-style Tightly-coupled Design. The Volta-
                                                                                 ratio of 4:1.
style design features matrix units tightly coupled to the core’s
register file. For its implementation, we closely refer to the                   5.1.2 Ampere-style Tightly-coupled Design with DMA.
microarchitecture proposed in [34], whose timing behav-                          The Ampere-style baseline features tightly-coupled matrix
ior is correlated with the NVIDIA GPU. The microarchitec-                        units and a dedicated DMA incorporated into the GPU. To
ture features SIMD-parallel dot-product units, composed of                       model this baseline, we use an identical matrix unit mod-
floating-point units in a tree-reduction configuration, that                     ule as the Volta-style baseline, but additionally instantiate a
execute multiply-and-accumulate (MAC) operations on the                          DMA unit described in Section 3.2.4.
input tile fragments. As a result of tight coupling, the tile                       Note that publicly available information does not explicitly
fragments are directly supplied from the register file via core                  confirm whether the Ampere architecture includes a dedi-
pipeline, and the result fragments are written back to the                       cated DMA engine similar to Hopper [15]. However, to more
register file as well.                                                           clearly isolate the impact of different design components
   We extend the Vortex RISC-V ISA to control the matrix                         in our evaluation, we introduce DMA in the Ampere-style
unit, modeling Volta’s HMMA SASS instructions. Specifi-                          design and incrementally incorporate operand decoupling
cally, a single tile operation is split into finer-grained set and               in the Hopper-style design.
step instructions, sequenced to perform the inner and outer
products of the input tile fragments. It is worth noting that                    5.1.3 Hopper-style Operand-decoupled Design. The
the Ampere and Hopper Tensor Cores manage the sequenc-                           Hopper-style baseline enhances the matrix unit design by
ing of sets and steps at the microarchitecture level [34, 39],                   enabling direct access to operand matrix data from shared
reducing the overall instruction count. We model this mech-                      memory, while accumulator data continues to be both read
anism in the Hopper-style baseline as well.                                      from and written to the register file [17]. To implement this
