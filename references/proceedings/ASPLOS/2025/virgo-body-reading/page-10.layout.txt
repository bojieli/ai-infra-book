ASPLOS ’25, March 30-April 3, 2025, Rotterdam, Netherlands Hansung Kim, Ruohan Richard Yan, Joshua You, Tieliang Vamber Yang, Yakun Sophia Shao


design, we extend the Volta-style matrix unit implementa-                                           GPU SoC Configuration
tion into a decoupled access-and-execute architecture [38],
                                                                                    Clusters                                1
illustrated in Figure 6. The access frontend includes a state                   Cores per cluster         8 in Volta-style / 4 in Hopper-style
machine and an address generation module that issues a se-                        SIMT Width                  8 warps/core, 8 lanes/warp
quence of read requests to the matrix fragments stored in the                     SIMD Units            2 ALUs, 1 FPU, 32 LSQ entries per lane
shared memory. The execute backend comprises decoupling                           Register File                8KB INT, 8KB FP per core
FIFO queues and operand buffers, which stage the responses                      Shared Memory              128 KB, 4 banks, 8-16 sub-banks
from shared memory and perform MAC operations when                                   Cache              16KB L1I, 16KB L1D per core, 512KB L2
operand data becomes available. Since the addresses of the                         Volta and Ampere-style Tightly-coupled Matrix Unit
matrix fragments are statically determined, the access fron-
                                                                               # of Units                      1 per core, 8 per cluster
tend can run ahead of the execute backend, effectively hiding
                                                                            MACs (Tensor:SIMT)               32 FP16 (4:1) / 16 FP32 (2:1)
shared memory access latency.
   We expose an asynchronous instruction interface for the                            Hopper-style Operand-decoupled Matrix Unit
Hopper-style through ISA extension that models the wgmma                       # of Units                      1 per core, 4 per cluster
PTX instruction [17]. A warp kicks off the matrix unit via an               MACs (Tensor:SIMT)               64 FP16 (8:1) / 32 FP32 (4:1)
initiate instruction without stalling, and later executes a wait
                                                                                             Virgo Disaggregated Matrix Unit
instruction to synchronize with the unit and retrieve a valid
result. Similar to Virgo, this asynchronous design enables                       # of Units                         1 per cluster
instruction-level parallelism within a single warp.                            Systolic Array                 16 × 16 FP16 / 8 × 8 FP32
                                                                            Accumulator Memory                          32KB
   While offloading operand data alleviates capacity con-
straints, storing accumulator data in the register file contin-           Table 2. Hardware configuration of the GPU designs used
ues to limit tile size. Similar to Volta, the Hopper unit has             for evaluation.
access to 1 KiB of register space per warp, accommodating
up to a single 16×16 FP32 accumulator tile. There is no sig-
nificant constraint to the shared-memory-stored operand
tile size; however, a large k tile dimension is not necessarily
advantageous as there is no data reuse over k. Consequently,              fencing, optimized warp scheduling, synchronization, among
the maximum tile size we support in the Hopper unit is                    others. Finally, we added Tensor Cores to the pipeline.
(m,n,k)=(16,16,32). The number of MACs in the unit is                        Gemmini-based Matrix Unit. We make extensive mod-
constrained by the shared memory bandwidth, which is 64                   ifications to Gemmini [21] in order to make the systolic ar-
FP16 operations per cycle in our configuration. This achieves             ray directly interface with the cluster-level shared memory.
the Tensor-to-SIMT FLOPS ratio of 8:1.                                    Gemmini coordinates the movement of the source matrices
   Note that the Hopper-style design also incorporates a                  from its private wide scratchpad banks through its systolic
dedicated DMA, similar to Ampere-style and Virgo.                         array, accumulating at either the individual processing ele-
                                                                          ments or at its private accumulator memory, depending on
                                                                          the selected dataflow. For Virgo, we retained the accumula-
5.2   System Implementation                                               tor memory to ensure full systolic array throughput, which
We leverage and extensively modify several key pieces of                  required single-cycle accumulation. We modified the scratch-
open-source hardware infrastructure to enable our full sys-               pad interface to use the TileLink protocol and go through
tem at the RTL level. At the top, we use the Chipyard SoC                 the SMEM interconnect, instead of directly interfacing with
generator framework [4] to integrate all hardware compo-                  SRAMs. The SIMT cores can therefore deposit data into the
nents into a single SoC design: Vortex SIMT cores, matrix                 SMEM directly for Gemmini to process.
units, shared memory, cache, interconnects, etc. Parameters
such as the number of cores and clusters, the size and data               5.3     Experiments
type of the matrix unit, as well as memory bus widths can                 In this subsection, we will detail the environment in which
all be adjusted, allowing Virgo to be a flexible and compre-              the workloads are run, the specifics of the kernels, as well
hensive generator covering a large design space.                          as the tools we use to run them.
   Vortex SIMT Cores. We take the SIMT core module from                      Hardware Setup. We summarize in Table 2 the hard-
Vortex [42] (vx_core) to construct our own cluster hierarchy              ware configuration of the GPU designs we evaluate, with the
and synchronization mechanisms, rather than incorporating                 GPU SoC configuration being shared by all. Both the Tensor
the full top-level design. We also adapt Vortex’s cache bank              Core and Virgo matrix unit uses fully-pipelined 1 op/cycle
for our L1 cache, positioned after the coalescer.                         hardfloat [23] units to implement floating point MACs. All
   We made changes to the core pipeline to facilitate higher              configurations possess the same number of MACs per cluster
instruction and memory throughput. These include store                    for a fair comparison.
