       (a) Multi-Modal Transformer (MMT)                     (b) DLRM                                  (c) CANDLE-Uno


Figure 6. End-to-end performance evaluation. GraphPipe outperforms both PipeDream [25] and Piper [40] in three different
models: Multi-modal Transformer-based model [31], DLRM [27], and CANDLE-Uno [1] at all but one GPU count configurations
tested. Missing data points indicate that no training strategy can be found within reasonable timeframes.

error bars for our plots, as we observe marginal standard             multi-branch models. As we use more devices, the number
deviations (less than 3%) for all results.                            of sequential pipeline stages tends to increase to achieve
    DNNs. We explore three multi-branch DNNs: Multi-Modal             a higher throughput, particularly when the model size is
Transformer-based model (MMT) [31, 44], DLRM [27], and                too large to apply data parallelism at the cost of weight
CANDLE-Uno [1]. Multi-Modal Transformer (MMT) is a                    memory footprint and weight synchronization. With a larger
backbone of most state-of-the-art multi-modal models [13,             number of stages, sequential pipeline schemes by generated
28, 31, 33, 46]. DLRM is a popular deep learning recommen-            by PipeDream or Piper suffer from extended warm-up and
dation model for personalization and ads recommendation.              cool-down phases. Directly, these extended pipeline bub-
CANDLE-Uno is a specialized model in the medical domain               bles negatively affect training throughput. Indirectly, these
(i.e., precision medicine). We describe the detailed model con-       bubbles increase activation memory footprints, which in
figurations in Appendix A.2. Despite different applications,          turn impede effective model partitioning. We visualize this
all these models feature parallel branches, each processing a         analysis in detail via a case study (see §7.5).
different type of data.
                                                                      7.2   Search Time
7.1   End-to-End Evaluation                                           Table 1 presents the search times by the three optimizers
We compare the training throughput of GraphPipe with                  (GraphPipe, PipeDream, and Piper) for the three models
existing pipeline-parallel systems such as PipeDream [25]             (Multi-Modal Transformer, DLRM, and CANDLE-Uno). The
and Piper [40]. We choose these two baselines since their             Multi-Modal Transformer-based model has two branches and
combined search space encompasses all possible model par-             the DLRM and CANDLE-Uno models have eight branches.
titions covered by other SPP approaches [8, 24, 48]. To be               GraphPipe is at least 9× faster than the baselines irre-
specific, PipeDream (with the operator granularity) basically         spective of the models or GPU configurations. In addition,
covers the pipeline partitioning and scheduling strategies            GraphPipe’s efficient partitioner produces a strategy within
of all baseline SPP approaches [8, 24, 48] but Piper. They            a minute for all configurations. The SPP baselines are much
all (1) linearize DNNs by transforming their computation              slower by comparison, and this search time discrepancy can
graphs into sequences of operators, (2) exhaust pipeline stage        be attributed in large part to the fact that the baselines rarely
partition choices, and (3) employ 1F1B scheduling.                    leverage DNN topology in expediting search. Note that Piper
   Figure 6 showcase the results. We measure the training             does not produce strategies for the DLRM and CANDLE-Uno
throughput (i.e., number of samples processed per second)             models for the aforementioned reasons.
as we increase the number of GPUs and mini-batch sizes.                  To see the large search space of each SPP baseline, it is
Note that Piper cannot generate training strategies for DLRM          helpful to approximate their time complexities. Let us con-
and CANDLE-Uno since its time and space complexity in-                sider a simple multi-branch model with each branch having
creases exponentially with respect to the number of parallel          𝑘 > 𝑛 operators, where 𝑛 is the number of branches. Recall
branches. GraphPipe outperforms PipeDream and Piper at                that Piper considers model partitions in which cross-branch
all but one GPU configuration. Moreover, the performance              stages exist. This level of granularity of model partitions
gap widens as the number of GPUs increases.                           significantly increases the number of model partitions to
   Our analysis reveals that we can attribute the widening            examine. Piper’s optimizer runs in 𝑂 (|D | 2 ) time (Appendix
performance gap to the pipeline depths greatly reduced                D in [40]), where D is the set of downsets (Definition 4.1 in
by GraphPipe compared to PipeDream and Piper for the                  [40]). According to the definition, model partitions in which
                                                                  9
