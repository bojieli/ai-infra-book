                        12 in-flight samples
      S1   F{1,2,3,4}       F{5,6,7,8}     F{9,10,11,12}                                         B{1,2,3,4}                        B{5,6,7,8}                     B{9,10,11,12}
      S2                    F{1,2,3,4}         F{5,6,7,8}                      B{1,2,3,4}       F{9,10,11,12}     B{5,6,7,8}                      B{9,10,11,12}
      S3                                       F{1,2,3,4}      B{1,2,3,4}      F{5,6,7,8}        B{5,6,7,8}     F{9,10,11,12}    B{9,10,11,12}

                                                            (a) Universal micro-batch size / schedule
                  10 in-flight samples
                (Less memory footprint)
      S1 F1 F2 F3 F4 F5 F6 F7 F8 F9 F10                                     B1 F11 B2 F12 B3 B4                 B5 B6 B7 B8                      B9 B10 B11 B12
                                                                                                                                                                     Shorter
      S2       F{1,2} F{3,4} F{5,6} F{7,8}                           B{1,2} F{9,10} B{3,4} F{11,12} B{5,6}      B{7,8}                   B{9,10} B{11,12}           Training
      S3                       F{1,2,3,4}              B{1,2,3,4}      F{5,6,7,8}      B{5,6,7,8}       F{9,10,11,12}    B{9,10,11,12}                              Iteration

                                                            (b) Per-stage micro-batch size / schedule


Figure 5. A comparison between universal and per-stage micro-batch size / schedule. F{𝑖, 𝑗 }, B{𝑖, 𝑗 } indicate forward and
backward passes for a micro-batch including samples 𝑖 and 𝑗. It showcases how per-stage micro-batch size and scheduling can
save memory footprint and training iteration time.

Algorithm 2 Static micro-batch scheduler.                                                        needs two micro-batches to be processed from 𝑆 1 to process
Input: Model partition G, initial current and next stage schedule                                a single micro-batch. Appendix A.1 explains the detail of
            configurations 𝑐 𝑓 , 𝑐𝑏 , number of devices 𝑑                                        ComputeInFlight(·) computes the minimal number of in-
                 Output: Optimized schedule Π𝑜𝑝𝑡                                                 flight micro-batches.
 1: function ScheduleStage(G, 𝑐 𝑓 , 𝑐𝑏 , 𝑑)                                                         Second, ScheduleTask(·) produces an optimized sched-
 2:    // Optimize schedule by minimizing number of                                              ule of forward and backward passes with optimized sched-
 3:    // in-flight micro-batches                                                                ule configuration (𝑐𝑜𝑝𝑡 ). It adopts greedy scheduling that
 4:    // while respecting data dependencies                                                     schedules backward pass as early as possible. It reduces both
 5:    𝑖 𝑓 = ComputeInFlight(𝑘 𝑓 , 𝑏 𝑓 , 𝑘𝑏 , 𝑏𝑏 , 𝑖𝑏 )                                          memory consumption and training iteration time since it
 6:    𝑐𝑜𝑝𝑡 = (𝑖 𝑓 , 𝑘 𝑓 , 𝑏 𝑓 )
                                                                                                 quickly resolves the corresponding in-flight forward pass.
 7:    if 𝑐𝑜𝑝𝑡 violates device memory constraint then
 8:         𝑐𝑜𝑝𝑡 = ∅ // Invalidate schedule 𝑐𝑜𝑝𝑡
                                                                                                    GraphPipe uses the following default schedule config-
                                                                                                 urations: 1) synchronous 1F1B schedule [25] adjusted to
 9:    Π𝑜𝑝𝑡 ← ScheduleTask(𝑐𝑜𝑝𝑡 )
                                                                                                 support graph-like dependencies and 2) the same micro-
10:    return Π𝑜𝑝𝑡
                                                                                                 batch size across stages. The synchronous 1F1B avoids gra-
                                                                                                 dient staleness with the same pipeline latency and lower
                                                                                                 activation memory footprint in comparison to alternatives
As in Figure 3, the input is fed by the stage partitioner, and                                   (e.g., GPipe [11]). Furthermore, except for some corner cases,
the output is returned back to the stage partitioner to form                                     we observe that performance improvements from per-stage
a stage graph with an optimized micro-batch schedule.                                            micro-batch sizes and 𝑘F𝑘B schedules are incremental to
   GraphPipe’s pipeline stage partitioner (Algorithm 1) first                                    justify the increased search times for models and device clus-
calls Algorithm 2 to discover an optimized micro-batch sched-                                    ters we explored. Still, with GraphPipe, users can choose to
ule for the last stage. It then traces back all directed edges                                   search over per-stage micro-batch sizes and 𝑘F𝑘B schedules
(𝑆𝑖 , 𝑆 𝑗 ) ∈ E𝑆 of the stage graph G𝑆 in the reverse direction                                  for more heterogeneous models and larger device clusters.
and determines a schedule for each stage 𝑆𝑖 until a schedule
for the first stage is determined. The reason for backward
traversal is that computing the activation memory usage,
and thus the total usage, for a stage 𝑆𝑖 requires complete                                       7      Evaluation
schedule information of its subsequent stages 𝑆 𝑗 .                                              We develop GraphPipe on top of FlexFlow [14], a distributed
   ComputeInFlight(·) and ScheduleTask(·) are two key                                            multi-GPU runtime for DNN training. We adjusted FlexFlow’s
functions in Algorithm 2. First, ComputeInFlight(·) is a                                         runtime for parallel execution of graphical pipeline stages
key subroutine to optimize schedule by effectively mini-                                         while introducing our own partitioner in §5 and scheduler in
mizing the number of in-flight micro-batches for a given                                         §6 to FlexFlow. We evaluate GraphPipe on the Summit su-
stage without increasing per-iteration training time. To take                                    percomputer [4]. For each compute node of Summit, we use
graphical stage dependency (i.e., multiple stages following                                      2 IBM POWER9 CPUs and 4 NVIDIA V100 GPUs with 512GB
one) into account, it factors in all following stages to decide                                  of main memory. GPUs within a node are interconnected
the minimum number of in-flight micro-batches. It also ac-                                       via NVLink while nodes are connected via Mellanox EDR
counts for scheduling constraints posed by micro-batch size                                      100Gb InfiniBand. We use the default schedule configurations
gap between subsequent stages. For instance, in Figure 5, 𝑆 2                                    of GraphPipe mentioned in Section 6. Note that we omit
                                                                                            8
