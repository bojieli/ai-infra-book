                                                                                                                  Xinsheng Tang et al.




                         (a) MHA on A10                                                     (b) MLA on H800




                     (c) MoE routing on A10                                      (d) FP8 PerToken Quant + GEMM on H800

                       Figure 5. The normalized performance of fusing four selected modules on GPUs.




(a) Normalized performance comparison of kernels fused at different   (b) Normalized performance of incremental and non-incremental
levels. Intra-block fusion achieves the best performance.             computation across different parallelism levels.

Figure 6. Performance comparison results. (a) fusion at different levels. (b) incremental and non-incremental computation.

5.4   Latency of Incremental vs. Non-Incremental                       shown in Figure 6b. Based on the experimental findings, we
      Mode                                                             draw the following conclusions:
This section evaluates the performance of incremental com-
putation versus non-incremental computation under varying
degrees of parallelism. We use the attention computation               • On-chip cache capacity limits the applicability of
pattern from the BERT-base model as our representative                   non-incremental computation: Non-incremental com-
workload. With a fixed problem size, we systematically con-              putation requires caching the complete previous reduction
trol the level of GPU parallelism by adjusting the sequence              results in on-chip shared memory or registers, making
length of kv processed per CTA, thereby modulating the                   its feasibility constrained by the SM’s on-chip storage
number of CTAs.                                                          resources. In our experiments, the non-incremental ap-
   In our experiments, we normalize all latency measure-                 proach only succeeds when the input sequence length is
ments with respect to the maximum observed execution                     ≤ 112, corresponding to Waves per SM ≥ 3.5. In contrast,
latency across all configurations, which serves as the per-              incremental computation avoids the need to retain full
formance baseline. The normalized performance results are                results on-chip, thereby eliminating strict constraints on
                                                                         segment length and enabling more flexible parallelism
                                                                         configurations.
