SuperOffload: Unleashing the Power of Large-Scale LLM Training on Superchips


   Based on the observation, we propose to replace the con-                                                iteration i                                               iteration i+1
ventional offloading synchronization-then-execution (STE)
                                                                                  GPU          F   F   F   F     B       B           B           B       U    F      F   F   F   R   F
scheme with a speculation-then-validation (STV) mechanism.                     Computation     1   2   3   4     4       3           2           1       1    1      2   3   4   B   1
                                                                                                                                                                                         …
Fig. 8 illustrates the idea. Instead of waiting for all gradients                GPU CPU                             G           G           G           G
to arrive, the CPU initiates the optimizer step speculatively                  Communication                         4           3           2           1

using the gradients available at that moment. Meanwhile,                         CPU GPU                                         P           P           P
                                                                                                                                                                                 P
                                                                               Communication                                     4           3           2
gradient clipping and normalization are deferred to occur
                                                                                  CPU      Post                              U           U           U       Post        Roll
during CPU idle periods, concurrent with the GPU executing                     Computation Valid                             4           3           2       Valid       Back
the forward pass for the next iteration. Meanwhile, to guar-                   F: Forward    B: Backward       U: Optimizer Step                         G: Gradient         P: Parameter
antee the synchronous nature of the optimization process,
we implement the in-place rollback for the optimization step.
                                                                            Figure 8. SuperOffload speculation-then-validation sched-
   Therefore, STV is an exact optimization that preserves
                                                                            ule. It enables optimizer steps (U) to overlap with backward
correctness by speculatively executing the optimizer step on
                                                                            propagation (B), eliminating synchronization bottlenecks
CPU in parallel with the next GPU forward pass. A back-
                                                                            and improving resource utilization.
ground process validates gradients in parallel by checking
NaNs/Infs and gradient clipping violations. Specifically, the
validation process is implemented using Python multipro-
cessing, and its results are passed to the GPU through a mul-               4.5     Superchip-Aware Casting (SAC)
tiprocessing queue. After the forward pass, the GPU checks                  In DL training frameworks like PyTorch and DeepSpeed, the
whether rollback is needed. There are two rollback scenarios:               mixed precision training is implemented through a graph
(1) if NaNs and Infs are detected, the iteration is skipped; (2)            rewriting process [47]. The default precision of all ops is
if gradients exceed clipping thresholds (e.g., after finishing              float32 (FP32). Mixed precision training casts certain model
computing the global gradient norm across all parameter gra-                states (e.g., weights, gradients) from FP32 to float16 (FP16),
dients), SuperOffload reverts the previous optimizer update                 or vice versa. For example, the gradients in the backward
and re-executes it using the clipped gradients.                             pass are produced in FP16, and the optimizer computes
   The speculation-then-validation scheduling offers two key                the updates using FP32 gradients. Therefore, the functions
benefits. First, it allows the optimizer steps on the CPU to                movebucket (𝑔𝑟𝑎𝑑) and movebucket (𝑝𝑎𝑟𝑎𝑚) not only transfer
overlap with backward propagation on the GPU, rather than                   tensors between the GPU and CPU but also involve convert-
placing all optimizer steps sequentially on the critical path,              ing tensor data types. These additional casting operations
which would significantly hurt throughput. Second, it elimi-                become complex when considering offloading strategies. Ex-
nates the time cost of normalization calculations and value                 isting offloading-based solutions often adopt a minimum
checking by relocating these operations to previously idle                  edge cut algorithm to computation graph [55], based on the
CPU cycles (when the GPU is executing the forward pass),                    implicit assumption that minimizing the data communica-
thereby improving overall resource utilization. Compared                    tion volume between the CPU and GPU leads to performance
with the ZeRO-Offload schedule in Fig. 3, SuperOffload re-                  improvements. However, these methods ignore the casting
duces the critical path of a single iteration from                          cost. Given that the C2C link between Hopper and Grace is
                                                                            an order of magnitude higher, is this assumption still valid?
         𝑇iter = fwdgpu + bwdgpu + movebucket (𝑔𝑟𝑎𝑑)                           We conduct experiments to examine the time cost of the
                                                                            two methods. The results are shown in Fig. 9. We observe
                 + stepcpu + movebucket (𝑝𝑎𝑟𝑎𝑚)                   (6)
                                                                            that 𝐶𝑎𝑠𝑡𝑐𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝16 takes around 2× execution time
                                                                            than 𝐶𝑎𝑠𝑡𝑔𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝32 on Superchips, depending on the
to                                                                          size of the input tensor involved in mixed precision training.
                                                                            In most cases, 𝐶𝑎𝑠𝑡𝑔𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝32 surpasses the perfor-
          𝑇iter = fwdgpu + bwdgpu + stepcpu (𝑏𝑢𝑐𝑘𝑒𝑡)              (7)       mance of 𝐶𝑎𝑠𝑡𝑐𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝16 by a large margin (e.g., tensor
                                                                            size 256MB to 2048MB). Based on our analysis, we opt for
   Interestingly, in theory, the STV schedule indicates that                𝐶𝑎𝑠𝑡𝑔𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝32 , as it incurs an overall lower cost com-
the iteration time of SuperOffload can be shorter than that                 pared to 𝐶𝑎𝑠𝑡𝑐𝑝𝑢 ↔ 𝑀𝑜𝑣𝑒 𝑓 𝑝16 even though the latter design
of non-offload training because the optimizer steps are per-                incurs a lower theoretical communication volume.
formed on the entire model using GPU in non-offload train-                     In addition to the casting cost, we also find that the casting
ing, while SuperOffload only updates a subset of the param-                 operation has an intricate interaction with the pinned mem-
eters on GPU. However, the conventional wisdom is that                      ory. A transfer-then-cast operation triggers an unpinned
offloading often comes at a performance cost. In our evalua-                temporary memory buffer allocation on the Grace CPU to
tion, we show that SuperOffload can indeed be faster than                   hold the FP16 copy of gradients swapped-out from the Hop-
GPU-only solutions.                                                         per GPU before casting FP16 gradients to FP32 on the Grace
                                                                        7
