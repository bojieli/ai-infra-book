token conventionally, but with higher computational inten-                              Reduce Tackle Rollout
                                                                        Systems         rollout rollout -train Core techniques
sity, speculative decoding amortizes the memory cost across                              time bubbles pipeline
multiple tokens. This approach is theoretically proven to pre-          HybridFlow [12]    ✗       ✗       ✗   Hybrid programming model
serve output distribution integrity. When acceptance rates              Kimi K2 [13]       ✗    Partially  ✗   Partial rollout
are favorable, it significantly accelerates LLM decoding [38].          DeepScaleR [17]    ✗       ✗      ✓    Pipelined rollout/train
                                                                        StreamRL [14]      ✗    Partially ✓    Skewness-aware scheduling
   RhymeRL is the first LLM RL system leveraging specu-                 AReaL [15, 40]     ✗       ✓      ✓    Fully async rollout
lative decoding to accelerate the time-consuming rollout                AsyncFlow [41]     ✗    Partially ✓    Streaming pipeline
process.                                                                DistFlow [42]      ✗       ✗      ✓    Distributed multi-controller
                                                                        RhymeRL            ✓       ✓      ✓    HistoSpec + HistoPipe
                                                                        Table 1. Overview of existing systems optimizing LLM RL.
3     Characterising RL Training in the Wild
Despite the widespread adoption of RL for LLM training, cur-            Pipelining rollout and training. Early RL systems like
rent RL systems still face significant performance challenges.          veRL [12] and OpenRLHF [45] adopt a colocated architecture,
This section presents the key implications derived from our             where GPUs repeatedly switch between rollout and training
practical experience in training state-of-the-art LLMs and              workloads, as Fig.4a shows. This design incurs significant
analysis of real-world traces.                                          overhead from context switching between workers and sub-
                                                                        stantial bubbles due to strict step-wise rollout-train depen-
3.1   Rollout as the Major Bottleneck in RL Training                    dency. Consequently, DeepScalaR [17] introduces a decoupled
Implication-1: Rollout dominates the RL training time-                  architecture, relaxing the dependency by using stale weights
line. During RL training, we observe that the rollout phase             that are one step behind for rollout (i.e., the off-policyness
dominates the RL time. Specifically, as Fig.2 shows, for LLMs           is 11 ), which is widely proven to maintain training accu-
trained with a maximum response length of 16K tokens, roll-             racy [14, 17, 47]. This enables coarse-grained rollout-train
out accounted for 91% of the entire RL processing time for              pipeline, and is adopted by systems including veRL [12, 47].
the math model and 84% for the code model. When the maxi-               AsyncFlow [41] further refines the rollout-train pipeline by
mum response length was increased to 32K tokens or longer,              granularizing dependencies from batch-level to mini-batch
the rollout time overhead exceeded 95%.                                 level, enhancing pipeline efficiency (as Fig. 4b shows, the
   This significant overhead stems from three key factors: (1)          reward and train processes proceed after a mini-batch’s roll-
Complex reasoning demands. During RL, LLMs are required to              out finishes). Nevertheless, imbalance persists among rollout
generate complex reasoning chains in responses. This results            workers, leaving significant bubbles within rollout stages.
in long responses, and crucially, the response length tends to          Tackling long-tail rollout. StreamRL [14] proposes skewness-
increase progressively as training advances. As Fig.3a shows,           aware scheduling, which allocates additional data-parallel
the mean response length grows from 1K to 10K in 320 steps.             GPUs to the prompts likely to generate long responses. By re-
(2) Memory-bound decoding. During rollout, generating each              ducing batch sizes for these prompts, it alleviates the long-tail
token requires an LLM forward pass, which is constrained by             effect in rollouts. However, due to the autoregressive nature
memory bandwidth [39]. (3) Sequential dependency. Due to                of LLM rollouts, this approach only marginally reduces the
the dependency between rollout and the subsequent training              long-tail and the rollout bubbles (∼10% [14]). Kimi-K2 [13]
step, the training phase cannot commence until the longest              introduces partial rollout, which truncates excessively long
sequence within the batch completes its rollout.                        responses and retains generated segments for continuation in
Implication-2: GPU underutilization from rollout im-                    subsequent steps. This method faces an efficiency-accuracy
balance. Preserving the rollout-train dependency is critical            dilemma: Excessive truncation causes significant portions of
for training accuracy, but it introduces significant compute            responses to be generated by stale model weights, ultimately
bubbles and leads to GPU idleness during rollout. We observe            producing outdated reward signals; conservative truncation
a significant long-tail effect within a batch, as shown in Fig.3b       diminishes its effectiveness in reducing rollout bubbles.
— rollout response lengths vary widely across sequences. Due            Fully asynchronous rollout. AReaL [15] adopts a radical
to the imbalanced distribution of rollout lengths, some roll-           approach (Fig.4c): (1) Fully async rollout. Rollout workers
out workers (i.e., GPUs cooperating with tensor parallelism)            continuously generate responses while train workers se-
finish early and become idle, yet must remain inactive un-              lect usable samples based on the maximum off-policyness
til all workers complete their tasks. As Fig. 3c shows, in a            threshold. (2) Aggressive dependency relaxation. Training
step, GPU monitoring reveals that the earliest-finishing GPU            can utilize rewards derived from rollouts generated by (po-
remains idle for ∼76% of the total rollout duration.                    tentially) stale model weights many steps prior. When new
                                                                        model weights are produced, ongoing rollouts are truncated
3.2   State-of-the-Art Efforts and the Limitations                      to propagate updated weights. The truncated rollouts are
We summarize existing efforts on optimizing the efficiency              1 The off-policyness refers to the temporal discrepancy (in steps) between

of LLM RL systems [12–15, 17, 40–46] in Table.1.                        the model used to generate rollouts and the current model being optimized.
                                                                    3
