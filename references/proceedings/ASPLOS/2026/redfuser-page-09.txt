RedFuser: An Automatic Operator Fusion Framework for Cascaded Reductions on AI Accelerators


• MoE routing module. The routing function consists of                       RedFuser achieves significant speedups across both work-
   a GEMM for calculating scores for each expert, followed                loads. In MoE routing, it delivers 1.7× and 6.6× speedup
   by a softmax + topk to select experts. We use different pa-            over PyTorch Dynamo and TVM, respectively. For Quant
   rameter configurations in different models (Switch Trans-              + GEMM, the improvements are even more pronounced,
   former [10], ERNIE [2], DeepSeek-V2-Lite [21] and                      reaching 3.4× over PyTorch Dynamo and 12.1× over TVM.
   Qwen3 [44]) for the experiment.                                           These experiments confirm that RedFuser exhibits strong
• FP8 PerToken Quant + GEMM module. This module                           performance across various representative workloads, rival-
   first computes dynamic scaling factors via reduction oper-             ing hand-optimized implementations. We evaluate RedFuser
   ations, then applies quantization and performs FP8 GEMM                on multiple hardware platforms to assess its portability. De-
   calculation. We select configurations from existing FP8                tailed results are provided in Appendix A.7.
   quantized MoE models (ERNIE [2], DeepSeek-R1 [12] and
   Qwen3 [44]) for testing.
   The detailed configurations of all modules are shown in                5.3    Latency of Different-Level Fusion
Appendix A.5.
                                                                          We evaluate four fusion strategies for the safe softmax oper-
   We also conduct experiments on non-ML workloads, such
                                                                          ator – intra-thread, intra-warp, intra-block, and inter-block –
as variance computation. Details on the experimental setup
                                                                          over input sizes from 1K to 8K to assess the impact of fusion
and results are provided in Appendix A.6.
                                                                          at different levels on the performance of the fused opera-
Baselines. We compare RedFuser against the following
                                                                          tor. All latency measurements are normalized with respect
strong baselines.
                                                                          to the unfused kernels. Figure 6a reports the normalized
• PyTorch (v2.7) Eager. The native PyTorch implementa-                    performance of different fusion strategies.
   tion serves as the primary baseline.                                       The results show that all fusion strategies reduce latency
• PyTorch Dynamo. We use torch.compile with the de-                       compared to the unfused baseline. And the latency ordering
   fault backend (Inductor) to enable graph capture, operator             is: intra-block < inter-block ≈ intra-warp < intra-thread. The
   fusion and code generation. The Inductor backend will                  performance differences are analyzed as follows:
   generate Triton [36] code on GPUs.                                         As shown in Figure 7, fusing cascaded reductions elim-
• TVM (v0.21). We compile all workloads using TVM’s                       inates redundant memory accesses, yielding performance
   Relax [17] frontend and perform the default optimiza-                  gains across all levels. However, as Equation (11) shows, fu-
   tion pipeline. In particular, we do not enable TVM’s CUT-
                                                                          sion at level 𝑘 requires correcting intermediate outputs 𝑑ˆ𝑘
   LASS [35] or FlashInfer [45] backends in order to compare
                                                                          of length 𝐿𝑘 , introducing computation overhead linear in 𝐿𝑘 .
   TVM’s own operator fusion capabilities.
                                                                          Fortunately, in the fused reduction tree at level 𝑘 (𝑘 < 𝐾),
• Hand-Optimized Libraries. We evaluate FlashAtten-
                                                                          subtrees ② and ③ are data-independent, enabling instruction-
   tion2 [4] and FlashMLA [15], which provide highly opti-
                                                                          level parallelism and overlap between memory access and
   mized CUDA kernels for MHA and MLA patterns.
                                                                          computation. As 𝑘 increases, the deeper subtree ③ provides
5.2   Subgraph Performance                                                longer computation paths, enhancing latency hiding.
5.2.1 MHA and MLA. Figure 5a and 5b show the nor-
malized performance of MHA and MLA workloads, with all
                                                                          • Intra-thread Fusion: Requires 𝐿1 corrections – the high-
measurements reported relative to PyTorch Eager.
                                                                            est overhead – and the sub-reduction tree ③ spans only
   For MHA, the fused kernel generated by RedFuser achieves
                                                                            level 1, offering insufficient computation to hide memory
performance on par with hand-optimized implementations,
                                                                            latency. Thus, it delivers the smallest performance gain.
averaging 1.09× that of FlashAttention2, while outperform-
                                                                          • Intra-warp Fusion: Spans level-1 and level-2, increasing
ing it in several configurations (H1-H5). On the LLaMA-65B
                                                                            computational intensity and better hiding global memory
configuration, RedFuser delivers 2.8× and 2.6× speedup over
                                                                            latency. With lower correction cost than intra-thread, it
PyTorch Dynamo and TVM. For MLA, our generated kernel
                                                                            achieves better performance.
achieves 102% of the performance of FlashMLA, while signif-
                                                                          • Intra-block Fusion: Introduces 𝐿3 additional corrections
icantly outperforming PyTorch Dynamo and TVM by 2.4×
                                                                            but enables the best overlap between accessing 𝑑 𝐾 and
and 8.7×. The performance gains stem from our fine-grained
                                                                            computing subtree ③. Experimental results show that the
fusion of the full attention reduction chain.
                                                                            latency-hiding benefit outweighs the overhead, resulting
5.2.2 MoE routing and Quant GEMM. Due to lack of                            in the lowest overall latency.
widely adopted hand-optimized implementations, we com-                    • Inter-block Fusion: No correction is needed since 𝑑ˆ𝐾 =
pare RedFuser only against general-purpose compilers. Fig-                  𝑑 𝐾 . However, strict data dependencies prevent memory-
ure 5c and 5d present the performance of MoE routing and                    computation overlap, limiting performance despite zero
Quant+GEMM, normalized to PyTorch Eager.                                    correction cost.
