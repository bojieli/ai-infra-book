                                                                             Generic Tagging for RISC-V Binaries

                                                     David Demicco, Matthew Cole, Gokturk Yuksek, Ravi Theja Gollapudi, Aravind Prakash,
                                                                             Kanad Ghose and Zerksis Umrigar
                                                                                    Department of Computer Science
                                                                               State University of New York at Binghamton
                                                                                      Binghamton, New York, USA
                                                                {ddemicc1,mcole8,gokturk,rgollap1,aprakash,ghose,umrigar}@binghamton.edu
arXiv:2212.05614v1 [cs.CR] 11 Dec 2022




                                         Abstract—With the widespread popularity of RISC-V – an                 While software-only security measures are highly desir-
                                         open-source ISA – custom hardware security solutions tar-          able and can be deployed with little effort, they typically
                                         geting specific defense needs are gaining popularity. These        suffer higher performance penalties compared to hardware
                                         solutions often require specialized compilers that can insert      solutions. Furthermore, the security mechanisms themselves
                                         metadata (called tags) into the generated binaries, and/or         may be a part of the attack surface, which brings about
                                         extend the RISC-V ISA with new instructions. Developing such       transparency issues. On the other hand, hardware-only se-
                                         compilers can be a tedious and time-consuming process. In          curity mechanisms must bridge the semantic gap problem
                                         this paper, we present COGENT, a generic instruction tag           and recover high-level security-relevant program semantics
                                         generator for RISC-V architecture. COGENT is capable of            in order to enforce rich and expressive policy abstractions.
                                         associating a tag of configurable and varying widths (1 to 20      Further, the tolerance for errors is low and “fixing” design
                                         bits) to each instruction. It is also capable of emitting labels   errors is very expensive (e.g. Intel MPX [10], [11] had
                                         that are central to the implementation of control-flow integrity   problems that resulted in eventual deprecation).
                                         (CFI) solutions. COGENT encodes all tags and labels as nop             With the gaining popularity of RISC-V, an open-source
                                         instructions thereby providing full backward compatibility.        ISA, hardware - software cohesive defenses have gained
                                                                                                            center stage and are desirable. Typically, defenders take
                                             We evaluate COGENT on a subset of programs from the
                                                                                                            advantage of (a) fine-grained control over the RISC-V
                                         SPEC CPU2017 benchmark suite and report the binary size
                                                                                                            hardware including (but not limited to) changes to ISA in
                                         increase to be 29.3% and 18.27% for the lowest and highest tag
                                                                                                            combination with (b) curated compilers capable of enrich-
                                         coverage levels respectively. Additionally, we executed tagged
                                                                                                            ing binaries with security metadata/stubs, deploying highly
                                         programs on Commercial Off The Shelf (COTS) RISC-V
                                                                                                            effective and fast performing defenses. For example, re-
                                         unmodified hardware and found the execution time overhead          cent defenses such as Cheri [12], Zero [13], RetTag [14],
                                         (w.r.t. backward compatibility) to be 13.4% and 5.72% for the      Framer [15], etc., all rely on a dedicated compiler that can
                                         lowest and highest coverage levels respectively. Finally, using    generate instrumented binaries for their modified hardware.
                                         a case study, we present possible use case scenarios where         The modified binaries contain metadata called “tags” that
                                         COGENT can be applied.                                             provides the hardware with specific information regarding
                                                                                                            the code or data unit in the program. Typically, there is
                                         1. Introduction                                                    a one-to-one mapping between a tag and corresponding
                                                                                                            code/data unit. These defenses strike a balance between
                                                                                                            security and performance requirements and are often tailor-
                                             Over the years, the advent of Internet of Things (IoT)         made to deployment needs. They rely on performant com-
                                         along with an increasing number of mobile, desktop and             pilers and/or hardware design and engineering, yet compiler
                                         server systems has led to a steady increase in cybersecurity       development remains an expensive process often requiring
                                         attacks. Most of the attacks–even those targeted at the            several hundred developer-hours for development and test-
                                         hardware (e.g. Spectre [1], and Meltdown [2]) are initiated        ing. More importantly, availability of a suitable compiler
                                         through software.                                                  would attract more hardware defenses that are currently
                                             The defense community has focused efforts at different         impeded by the lack of a compiler. Solutions that shorten
                                         layers of deployments. For instance, the software community        the development effort and allow quick incorporation of
                                         has explored software-only defenses such as StackGuard             metadata into binaries are missing and highly sought after.
                                         [3], and CFI via program instrumentation (e.g., [4]–[6]).              In this paper, we address the above need and present CO-
                                         Meanwhile, the hardware community has pursued defenses             GENT: COmpiler for GENeric Tagging, a generic LLVM-
                                         that leverage hardware features in order to detect and flag        based compiler that incorporates instruction tags (or code
                                         exploits (e.g., Intel CET [7], WˆX) or prevent information         metadata) into RISC-V binaries. In essence, COGENT re-
                                         leakage (e.g., Intel SGX [8], ARM TrustZone [9]).                  moves the burden of compiler development from RISC-
V hardware defenses that rely on embedding instruction          evaluate COGENT in section 5 and provide case studies to
metadata into binaries. Motivated by the needs of Control-      highlight benefits of using COGENT. Finally, we present
Flow Integrity (CFI) defenses that embed CFI labels into        related work in section 6 and conclude in section 7.
code, COGENT interleaves tag metadata into the instruction
stream. COGENT has several key features that make it            2. Background
highly favorable for hardware-software cohesive defenses.
First, it is highly flexible. It can accommodate various tag
                                                                2.1. Security via Tagging
widths (1 to 20 bits per instruction). This flexibility is
essential in order to achieve rich expressiveness in tags.
                                                                    Tagging is a technique that very broadly associates some
Second, it is functionally correct. That is, it ensures that
                                                                amount of metadata (the tag), with a memory location and
the inserted tags do not interfere with program logic. Func-
                                                                registers. Specially designed architectures then consume that
tional correctness is non-trivial due to several challenges
                                                                tag and enforce a security policy. Tags can be associated
(see Section 3.2). This is important since a tag system
                                                                with a) memory objects (data tagging) allowing policies to
is not usable without such guarantees. Third, COGENT
                                                                track and protect data in memory, and/or on b) code (in-
preserves backward compatibility. That is, tagged binaries
                                                                struction tagging), allowing policies to change or extend the
will seamlessly run on generic RISC-V processors that are
                                                                behavior of the tagged instructions. Of particular importance
tag-unaware. We accomplish backward compatibility by en-
                                                                to this paper is instruction tagging.
coding tag information into NOP instructions that are simply
ignored by unsupported hardware. Finally, our changes are       Instruction Tagging: allows a tag-aware architecture to ex-
segregated into LLVM IR-level and target-specific changes.      ecute additional code or checks on any instruction associated
Other architectures (e.g., ARM) can be supported by reusing     with a particular tag. Instruction tags are useful in imple-
the IR-specific changes and rewriting ARM LLVM backend.         menting Control-Flow Integrity (CFI) policies. Identical tags
    We make the following contributions:                        are used at control flow sources (call/jump sites) and control
                                                                flow targets in order to signify legal control flow.
   •   We implement a flexible and defense-scheme agnos-            Tags can be placed on load/stores to protect sensitive
       tic implementation codenamed COGENT, a LLVM-             data, forbidding access to a memory region if the tag is
       based compiler for RISC-V target. COGENT pro-            incorrect. These kinds of tags often require corresponding
       vides configurable instruction-level tagging support     metadata placed with the data they are meant to access. The
       to several different tag widths and coverage. We         tag policies and enforcement are implementation specific
       also make changes to the LLD linker to support           and form the essence of a tag-based defense. The specific
       COGENT.                                                  instruction tags are a function of the intended security
   •   We demonstrate the efficacy of COGENT via a CFI          policies, and therefore vary greatly between designs [16].
       implementation.                                          Differences in tag requirements can even result in different
   •   We evaluate the example CFI implementation               designs which use the same hardware for similar purposes
       for correctness and backwards compatibility. We          [17]. This implementation-specific design requirement is
       demonstrate moderate to low performance overhead         inherent to the nature of tagging. Tags are simply a way
       and binary bloat.                                        of providing more information to the program at run time,
   •   Through case studies, we demonstrate potential real-     but the information needed is dependent on the design of
       world applicability of COGENT.                           the system itself.
Summary of Results: We evaluated the correctness of                 Broadly speaking, there is a trade-off made in the design
COGENT by compiling and running a subset of the SPEC            of tag systems between how much information it provides
CPU2017 benchmark suite on Commercial Off The Shelf             to the hardware, and how much overhead the tags introduce
(COTS) hardware. Our subset has small function sizes,           into the system. This overhead can include power consump-
heavy memory overhead, and high loop counts, all of which       tion, hardware complexity, performance or any other costs
stress instruction caches. For our example CFI implemen-        paid by the design. Depending on how much information
tation, we found that COGENT increased the size of the          one’s defense needs, tags can be arbitrarily large, or as
.text section of these programs by an average of 29.32%         small as a single bit. Designers often use techniques such
for the lowest tag coverage, and 18.27% for the highest. We     as encoding the metadata information into the unused upper
found that backward-compatibility performance (i.e., over-      bits of a pointer, but this is often insufficient on its own [14],
head imposed by tagged binaries running on tag-unaware          requiring additional tagging or ISA(instruction set architec-
hardware) was impacted by an average of 13.40% for the          ture) changes. The choices of where to store metadata, how
low coverage and 5.72% for the highest. We also present         much metadata to store, and how to access it are defining
several possible use cases for the system, including specific   characteristics of tagging systems.
recent CVEs that could be addressed.
    The rest of the paper is organized as follows: section 2    2.2. LLVM Compiler Infrastructure
provides the technical background necessary to understand
the remainder of the paper. In section 3 and section 4, we          The LLVM compiler infrastructure [18] is a fully fea-
present the overview and technical details of COGENT. We        tured compiler designed with modularity in mind. It is
broken down into components, which operate on different         not set out to create a specific defense with this paper, or
parts of the compilation process. Important to COGENT           provide analysis that can be used to create one. Instead we
is the idea of the language-specific front end, the target      focused on three major tasks.
architecture-specific back end, and the language- and target-      •   Flexibility: COGENT must be able to generate bina-
agnostic intermediate representation (IR). The LLVM design             ries for RISC-V architecture that require varying tag
requires the language-specific front end to convert source             widths, with minimal changes. Additionally, we aim
code into LLVM IR, the optimizer schedules an analysis and             to modularize our implementation in order to min-
transformation pass pipeline on that IR, and its output is IR          imize the developer effort in modifying COGENT
for consumption by the back end. The back end might also               to support a new architecture. COGENT must also
perform additional transformations on the code via its own             be able conducive to supporting different types of
set of passes and built-in functionality. The final product            defense schemes including those that require large
of the back end is then output to an object file, which is             amounts of metadata.
fed into a linker to create the final binary. Modifying the        •   Functional Correctness: Insertion of tags and meta-
LLVM compiler can thus take two main forms. The simplest               data must not interfere with program logic. That is,
scenario is to create and modify transformation and analysis           all necessary tags must be appropriately inserted,
passes within the existing structure of the pipeline. These            and inserted tags must not alter the control flow or
transformation passes can be scheduled to run before or after          corrupt data.
any specific pass or combination of passes, allowing one           •   Backwards Compatibility: The tool must allow cre-
to place new transformations and analysis at any point in              ated code to be backwards compatible. That is,
the pipeline. The more complex scenario is to modify the               tagged binaries must be able to seamlessly execute
internal components of LLVM, changing or extending the                 on tag-unaware hardware. While backwards compat-
functionality of pre-existing code in the LLVM infrastruc-             ibility is a desirable feature for many systems, we
ture.                                                                  recognize that it may not be necessary in every case.
                                                                       If turning it off could provide benefits to a scheme
2.2.1. Relocations and Symbols. As part of the creation
                                                                       COGENT should allow that as well.
of an object file, the back end will emit target specific
information called a relocation into object files. These
                                                                3.2. Challenges
relocations are designed to inform the linker of how to
handle information that cannot be finalized before the link-    3.2.1. Metadata Lookup costs. A tag or metadata lookup
ing phase of compilation. Examples of this for RISC-V           action in the hardware could be expensive and inefficient.
include encoding addresses, which are often emitted as a        Therefore it is best to reduce the number of abstractions
pair of relocations (named HI and LOW), covering two            required to retrieve any tag for a given instruction. Likewise
instructions (i.e. auipc, jalr). These relocations tell the     having separate (but mapped) memory adds complexity to
linker to place the final address of the target symbol across   the process of bringing the tags into the hardware.
two instructions when it is required. Symbols themselves
can be kept as offsets from an unknown starting address,        3.2.2. Tag Semantic Gap. During the compilation pro-
allowing the linker to know the final address during the        cess, programs pass through different stages of compilation.
linking process, even though the absolute position cannot       While the tags are eventually assigned on the raw instruc-
be determined by the compiler back end.                         tion stream, the actual assignment of tags may occur at a
                                                                higher level of abstraction (i.e., source code or intermediate
3. Overview                                                     representation).
                                                                    Depending on the stage of compilation where tags are
3.1. Goals                                                      assigned, percolating them down to the instructions is im-
                                                                portant and requires bridging of the semantic gap between
    Tagging schemes are complex and varied, and different       the higher and lower levels of program representation. Fur-
schemes require different configurations. There are some        ther, during compilation the compiler will often expand
that require more space (i.e., metadata) to be accessible for   IR instructions/source statements into multiple assembly
any given instruction, while some might require only a sin-     instructions. This manifests as a Many-to-One relationship
gle bit for some defenses [13]. Additionally tagging schemes    where many tags map to a single IR instruction/source
must be able to provide the metadata to the hardware. A         statement. A very common example in RISC-V is the low-
generic instruction tagging scheme must be able to meet         ering of the LLVM-IR pseudo instruction PSEUDOCALL
this requirement under as many different configurations as      from MachineInstr level into the MCinst level. During
possible. To that end we set out to create COGENT.              this process the single PSEUDOCALL is expanded into
    The goal of COGENT is to enable embedding con-              an auipc,jalr pair. Likewise there are One-to-Many
figurable metadata tags into instruction stream, and not        relationships where multiple instructions at a higher-level
implementation details for different defense schemes such       of abstraction are lowered into a single assembly instruction
as forward or backward edge CFI, heap or stack, stack           (e.g. bitcast instructions). These one-to-many and many-to-
protection, or any other particular defense scheme. We did      one relationships can occur during any point of the lowering
process, and where possible, COGENT must provide ways                    tagging of code inserted after IR level optimizations
to handle them correctly. Unfortunately there are many cases             or back end insertions.
without a generic solution i.e. in the lowering from IR level       •    Backend and Defense Specific Changes. These
instructions to machine Instructions.                                    changes are both back end and defense specific.
                                                                         Representing choices that must be made in how the
3.2.3. Compiler-inserted Code. During compilation the                    target back-end and the scheme must interact. For
compiler may introduce instructions that did not exist at the            the RISC-V prototype we developed (see subsec-
previous (i.e., higher) level of representation. This is typi-           tion 4.2), One example would be the insertion of a
cally done to meet back end specifications or optimizations.             metadata label between an auipc jalr pair to include
Examples include in-lining and function prologue/epilogue                a CFI label. A second would be the specific behavior
insertion. COGENT must provide a way to tag compiler-                    of tags in the many to-one and one-to-many lowering
inserted code, and must ensure that it does not disrupt the              relationships. These changes cannot be handled by
tag schemes.                                                             back end passes alone and must be hard coded into
                                                                         the back end itself on a case-by-case basis.
3.2.4. Pre-calculated Offsets, Symbols, and Relocations.            •    Defense Scheme Agnostic Back End Changes. These
Features such as C++’s try/catch block implementation often              are changes to the back end that must be done to
pre-calculate the distance between two symbols instead of                allow for the insertion of the tag instructions as late
leaving it as a relocation wherever possible. COGENT must                as possible into the code stream. This includes turn-
be sensitive to these pre-calculated values. More generally,             ing off the pre-calculated offsets, ensuring correct
wherever possible the compiler calculates the offset to a                tag placement, and adjusting symbol and relocation
relocation or a symbol from the beginning of a compilation               offsets before object file emissions. These do not
unit. COGENT must preserve the relative locations in the                 interact with the logic of tag selection, only with
code section though any changes it makes.                                the emission of the tags themselves into an object
                                                                         file.
3.2.5. ISA Changes. Prior efforts have altered the ISA to           •    Configuration File. This file allows a user to specify
enrich code with metadata. Any ISA changes would by their                simply what emission options they would like for
very nature break backwards compatibility. COGENT must                   the final tag instructions. Details of the provided
be able to encode tags without any modifications to the ISA.             options are presented in subsubsection 4.1.1, but
                                                                         the configuration file allows developers to switch
3.3. Our Approach                                                        between tag widths for their own instruction tagging
                                                                         schemes.
   When examining both the challenges and the goals, we              To address the flexibility requirement of providing ad-
converged on the following design choices for COGENT.            ditional metadata where needed, we introduce metadata
These solutions address both the challenges and goals.           labels as a tool for a developer to use. These labels can
                                                                 be repurposed for any use, and are encoded as nops by
3.3.1. Flexibility. To address flexibility and the many back-    the compiler in order to maintain backwards compatibility.
end specific challenges we encountered, we designed CO-          In the RISC-V back end we chose lui instruction which
GENT to be as modular as possible, with front end and            provides 20 bits of metadata per inserted label.
back end components in order to allow for adaptability.
Unfortunately many (but not all) of the back end changes
are target specific in the best case, and design specific in     3.3.2. Functional Correctness. . Addressing correct tag
the worst. To solve this we clearly separate the COGENT          placement becomes challenging when inserting new tag
process into five major pieces.                                  instructions at specified alignments. Any changes made to
                                                                 the code after a tag has been inserted will displace the
   •   IR Level Tagging API. This component allows the           tags and must be adjusted in order to maintain consistent
       creation of IR level passes that can take advantage       tag placement. To this end, all functions are forced to be
       of the front and back end agnostic nature of the          aligned. Additionally, we assume that relaxation/compressed
       IR to perform analysis and select tags for IR level       instruction generation is turned off (see subsubsection 4.3.2
       instructions. These IR level tagging passes can be        for a discussion on compressed instructions). Additionally
       shared between any source and any target, and so          optimizations such as pre-calculating offsets based on the
       are the most portable method of tag selection. This       MCInst’s as described in challenges must be turned off. This
       component consists of an analysis pass that pro-          adds additional one time overhead to the linking process,
       vides propagation of assigned tags into any back-end      but no extra run time cost beyond the insertion of the tags
       passes where possible.                                    themselves. Optimizations introduced by IR passes can be
   •   Backend Level Tagging API. This component allows          left in place however, by insuring tag insertion is completed
       the creation of LLVM MachineInstr and MCinst              at the last point possible in the pipeline. Finally tag instruc-
       level passes that can tag code after it has been          tions can be placed in locations that disrupt relocation pairs,
       lowered into a target specific back end. This allows      meaning the linker must be made aware of this possibility.
3.3.3. Backward Compatibility. To allow for backwards                 For tagging in the IR level, we provide a pass that IR
compatibility, at least one of the possible tag instruction       level passes can push a mapping of IRInstruction : tag into,
configurations must be encoded as a nop instruction. For          and that backend passes can register with to access that tag
the RISC-V prototype, the instruction lui x0, <imm>               mapping to assign those tags to machineInstr. The lowering
was selected, as it is functionally a nop and gives the           of these IR tags is complicated by the needs of the target
maximum number of bytes to encode tag values. Other target        specific backend, as described in the next section.
architectures would have to select different nop-equivalent
instructions with differing amounts of bits to ensure back-       4.1.3. Lowering Behavior. For IR instruction tagging, we
wards compatibility.                                              provide a default behavior in a modified RISCV backend.
                                                                  However depending on the specific deployment needs, de-
4. Details                                                        velopers must investigate the lowering process and make
                                                                  changes where appropriate to their planned tagging behavior.
    With the solutions outlined in section 3 we created a         By default one-to-many transformations apply the tag on the
prototype version of COGENT. This prototype comes in              IR instruction on every machine instruction created by that
two parts, The underlying changes to LLVM itself, and an          instruction during the lowering process. For many-to-one,
example implementation that provides metadata labels on           the default behavior is the first tag. Any instructions not
computed jumps and computed callsites.                            directly created from another instruction (an optimization or
                                                                  specification specific to the back end) will be assigned the
4.1. LLVM Changes                                                 default tag value from the tag configuration file. This process
                                                                  may be insufficient for specific defense needs, but there is no
   We make all of the changes described in this section to        one-size-fits-all approach to the lowering problem and ne-
LLVM version 10.0.0.                                              cessitates developer effort. For machine instruction inserted
                                                                  tags, we modify the lowering process from machineInstr to
4.1.1. Tag Instruction Selection. The first and the most          MCInst allowing the tag field to be copied over. In cases
important step in COGENT implementation is to provide a           where this is a one to one lowering, we simply copy the
way for the developer to select how they wish to represent        field from the machineInstr to the MCInst. For one to many
the tags in the instruction stream. This representation is        relationships, such as PSEUDOCALL, we assign multiple
referred to as the tag instruction. The selection of this tag     tags to the machineInstr which are assigned to each of the
instruction determines                                            MCInst’s during this expansion process. Specific tag values
                                                                  for a given expansion will depend on the tagging scheme
   •    How many bits a user will have available to store         itself. Similar to IR level lowering there is no one size fits
        tags                                                      all solution to expansion. Fortunately at the machineInstr
   •    How many instructions any single tag can cover            level there are only a few of these cases to handle in the
   •    Backwards compatibility                                   RISCV back-end.
Table 1 shows the different choices we provide via a config-
uration file. We detail their bit layout, how many bits they      4.1.4. Emitting the Tags. The actual tag emission into the
provide, and the number of bits available for each instruction    instruction stream occurs in the finalize function during
based on the coverage desired. If backwards compatibility         code emission in the LLVM backend. At this point the byte
is not required, users can select a custom opcode for the         code for instructions has been placed into containers called
tag instruction. The tag enumeration itself is defined in a       fragments, and their layout relative to each other is known.
separate header file that must be included to the relevant        However they have not been emitted into the final binary.
passes.                                                           We modify LLVM such that as instructions are encoded
                                                                  into their fragment, their ordered list of tags are also added
4.1.2. Tagging API. Once the tag instruction has been             into a new “tag” array. With this tag array in place, and
selected, the next COGENT process is to assign tags to            the knowledge of the fragments offset (provided by llvm)
LLVM’s IR level instructions or machineInstr. In the back-        we create a new data array for the fragment, that has the
end, this process is done via a simple API we expose              tag instructions inserted into the instructions. We do this for
to the user, consisting of standard .set(<tag>) and               every fragment that contains instructions. In this manner we
.get(<tag>) extensions on top of LLVM’s base ma-                  can be sure that any MCinst that has received a tag, will
chineInst classes. These are employed by LLVM backend             have that tag emitted into the final binary Any non-zero
passes that developers insert into the compiler pipeline. In      fragment offset must be recalculated to account for tags that
general tag instrumentation passes should be inserted as          will have been inserted into previous fragments. However
late as possible in the pipeline to prevent optimizations         because this is a simple one tag instruction per-number of
from interfering with tag lowering. We also provide label         encountered instructions, it is always possible to calculate
emission, via an emission of a single lui nop at any              this correctly with just the original offset. (I.E. If the offset
point in the compilation process. This lui must be tagged         is 12 bytes, and the tag coverage is 3, there would be a
appropriately to indicate to the hardware to process this label   single tag instruction inserted before that offset, and the new
as a particular type of metadata.                                 offset would then be 16) Like the fragment offsets, during
                 Instruction            Bits available        C1 bits  C3 bits  C7 bits   C15 bits C31 bits               Backwards compatible?
                 addi                         12                12        4        1        NA       NA                           Yes
                 lui                          20                20        6        2         1       NA                           Yes
                 Custom opcode                25                15        8        3         1       NA                           No
                                                            TABLE 1. TAG INSTRUCTIONS USED BY COGENT.

C1, C3, C7, indicate the number of instructions ”Covered” by one tag instruction. Coverage 1 Coverage 3, Coverage 7 etc. Higher coverage’s mean less
 tags in the instruction stream. Bits available indicates the total amount of metadata space for that instruction, and the Bits columns shows how much
                                                            metadata there can be per instruction.



           Source Code                                                       Tagged objdump for main()

             typedef int (*fPtr)(int);
                                                                               0000000000000050 main:
             int sqaure(int a){                                                50: 37 10 00 00   lui    zero, 1                         #TAG
                 return (a * a);                                               54: 37 50 00 00   lui    zero, 5                         #CFL label

             }                                                                 58: 13 01 01 fd   addi     sp, sp, -48
                                                                               5c: 23 34 11 02   sd     ra, 40(sp)
                                                                               60: 37 00 00 00   lui    zero, 0                         #TAG
             int main(){                                                       64: 23 30 81 02   sd     s0, 32(sp)
                                                                               68: 13 04 01 03   addi     s0, sp, 48
                 fPtr func1 = foo;
                 int a = func1(3);                                             000000000000006c .LBB1_1:                                #symbol
                 return a;                                                     6c: 17 05 00 00   auipc     a0, 0
                                                                               000000000000006c: R_RISCV_PCREL_HI20            square   #Reloc
             }
                                                                               70: 37 00 00 00   lui    zero, 0         #TAG
                                                                               74: 13 05 05 00   mv      a0, a0
           LLVM IR for Main()                                                  0000000000000074: R_RISCV_PCREL_LO12_I            .LBB1_1 #Reloc

             define dso_local signext i32 @main() #0 !dbg !19 {
                                                                               …
             %1 = alloca i32, align 4
             %2 = alloca i32 (i32)*, align 8
                                                                               90: 37 00 04 00   lui    zero, 64                        #TAG
             %3 = alloca i32, align 4
                                                                               94: 83 35 84 fd   ld     a1, -40(s0)
             store i32 0, i32* %1, align 4
                                                                               98: 37 d0 02 00   lui    zero, 45                        #CFL label
             store i32 (i32)* @sqaure, i32 (i32)** %2, align 8, !dbg !25
             %4 = load i32 (i32)*, i32 (i32)** %2, align 8, !dbg !28           9c: e7 80 05 00   jalr    a1
             %5 = call signext i32 %4(i32 signext 3), !dbg !28
             store i32 %5, i32* %3, align 4, !dbg !27                          …
             %6 = load i32, i32* %3, align 4, !dbg !29
             ret i32 %6, !dbg !30




Figure 1. Demonstration of COGENT using the simple CFI described in subsection 4.2, contains compiler inserted code (0x58-0x68), many to one
relationships (0x94-0x9C), relocation and symbol adjustments (0x6c-0x74), a tag inserted between a relocation (0x70). Additionally it includes data that
must be persisted from the IR (the function labels) to the backend inserted labels at (0x50) and (0x98).


tag emission symbols and relocation must be adjusted to                             calculation of if a jump should be PC-relative or indirect
account for the tag insertions. As they contain offsets that                        is done before the final emission based on the maximum
are byte distances from the beginning of a fragments, these                         distance allowed in a single PC-relative jump instruction.
values must be adjusted by the same one per n calculation                           This calculation must be adjusted to assume an additional
as the fragment offsets. For both symbols and fragments                             one tag instruction per number of instructions insertion. An
we create a new array, with the updated values, and then                            example of the final result of these changes, and the final
overwrite the unmodified arrays using a ConstCast. There                            emission of tags is presented in Figure 1
are some corner cases in symbol resolution that must be
addressed. Specifically the compiler will attempt to calculate                      4.1.5. Linker Changes. To support both the insertion of
the distance between two symbols before the finalize layout                         tags and possibly metadata labels, the linker must be made
step if it is able to determine the distance early (i.e., if they                   aware that there can be instructions inserted between a HI
are in the same fragment), this is a compiler optimization to                       and LO relocation pair. Normally HI-LO relocation’s must
increase symbol resolution speed in the linker. To deal with                        appear as a pair, directly next to each other. We modify the
this, we disable this optimization and allow the linker to                          linker to allow for up to three instructions inserted between
resolve the difference after tag insertion. Additionally, the                       the pair, and adjust the final value calculations to account for
                                                                                    the added distance. Three additional instructions are selected
as a default to allow HI, tag, METADATA LABEL, META-            instruction (compressed or not), even if they are all tagged.
DATA LABEL, LO configurations, however this number              This can be done by using tags small enough to cover all
can be adjusted if more metadata is desired.                    possible instructions (i.e., 3 bit tags for an lui with an
                                                                uncompressed coverage of 3) or a two-tiered tag system,
4.2. Example CFI Implementation                                 where uncompressed instructions receive a larger share of
                                                                the tag space. The compiler must additionally be modified to
    We provide an example CFI implementation that demon-        insert alignment NOPs in the case where a non-compressed
strates the common design challenges and changes of a           instruction would cross a tag instruction, pushing it under
simple COGENT implementation. CFI is a very well stud-          the coverage of the next tag.
ied problem [6] [19] [20] and is a common use case for
tagging implementations [16] [13] [14]. This example CFI        4.3.3. Dynamic Linking. Currently COGENT only sup-
implementation places metadata labels at the beginning of       ports statically linked binaries. This is to ensure that the
exported or address taken functions, and before the function    tags for any externally linked libraries are always located
call at computed call sites. It consists of the following       in the correct place for interpretation. We plan to support
components.                                                     dynamic linking in future versions of COGENT.
   •   A tag configuration with 2 tags, using a lui-nop to      4.3.4. Handwritten Assembly Support. Handwritten As-
       provide backwards compatibility. The tags are sim-       sembly sections are loaded into the compiler backend for
       ply N (A normal instruction) or CFL (control flow        parsing before being emitted to the final object file. This
       label) this allows any of the coverage’s supported by    allows COGENT to insert the tag instructions in hand
       lui Table 1 to be selected.                              written assembly segments. However its ability to correctly
   •   An IR level pass that assigns CFI labels to func-        determine tags on these sections is limited. The correct
       tions and callsites based on their function signature.   tagging of these sections would be left to the implementation
       Function-signature-based CFI policy is consistent        of a specific tagging scheme, in much the same way as
       with the CFI approach in LLVM. A callsite and a          backend and scheme specific tagging would be and may
       destination must have matching CFL labels.               only be possible with manual tagging.
   •   A Machine instruction level pass that inserts and tags
       the CFI labels with CFL at the beginning of every        4.3.5. Additional Configurations. Other tag layouts be-
       non-internal function.                                   yond the single one we chose are desirable in a generic
   •   A back-end and scheme specific change to insert          tagging system. Possibilities include
       the CFL label between the AUIPC-JALR pair that
       RISCV generates for call sites.                             •   Placing all the tags for a function’s instructions at
                                                                       the beginning of that function. This would consist of
An example of this CFI implementation on a from source                 a series of tag instructions sufficient to cover every
to object file is provided in Figure 1. This example also in-          instruction in the function. On non tag aware systems
cludes many of the common challenges faced by COGENT.                  this would simply be the equivalent of a series of
    CFI is such a well studied problem, and our example                NOPs that run before a function starts.
solution is based on previous work [18]. As such we do not         •   Placing the tags for a basic block at the beginning
claim this implementation as a novel contribution.                     of that basic block. Again, this would consist of a
                                                                       series of tag instructions large enough to cover every
4.3. Further Expansions                                                instruction in that basic block.
                                                                   •   Placing the tag a separate .text.tag section in the
4.3.1. Tag Expansion. Although we do not claim it as a                 ELF executable file. It would be up to the hardware
contribution, there is no fundamental reason that placing two          to determine how to use this section, but it can be
tag instructions next to each other would not work. Such a             desirable, and non tag aware hardware can simply
configuration could allow for higher coverage at the cost              ignore the section completely.
of higher space overhead. This may be useful when space
is a nonissue, or for inserting complex metadata for testing        While COGENT currently does not support these ad-
purposes.                                                       ditional configurations, we have plans to extend support to
                                                                them in the near future.
4.3.2. Compressed Instruction Support. Likewise there is
no fundamental barrier to support compressed instructions.      5. Evaluation
The instruction opcode in RISCV indicates if an instruction
is compressed or non-compressed. With that knowledge and            COGENT is a generic system with many different pos-
the current PC, it is possible to infer what bits in the tag    sible use cases. As such we evaluate it Four primary ways.
instruction contain the corresponding tag. For compressed       Overhead on COTS hardware (i.e. the overhead of back-
instruction support, care must be taken when designing          wards compatibility.), and binary size overhead, are pre-
the tags and the tag policy, as the tag bits must be small      sented to demonstrate the cost of cogent in its most common
enough such that a single tag instruction can cover every       configurations and uses. The last two, are a comparison
                                          Lui Nop, coverage 3                 Lui Nop, coverage 3 no labels
                                              Lui Nop, coverage 7                 Lui Nop, coverage 15

                   25.00%

                   20.00%

                   15.00%

                   10.00%

                     5.00%

                     0.00%




                                                                                               _r




                                                                                                                       e
                                                                                      _r
                                      r


                                                 _r




                                                                          r
                                                              r




                                                                                                              r
                                   c_




                                                                         k_




                                                                                                                     ag
                                                              _




                                                                                                         g_
                                                                                             xz
                                                                                   64
                                              cf


                                                           pp
                                 gc




                                                                    bm




                                                                                                                   er
                                             m




                                                                                                         en
                                                                                 x2
                                                          t
                                                       ne




                                                                                                                  Av
                                                                                                       sj
                                                                    an




                                                                                                    ep
                                                      om



                                                                  xl




                                                                                                  de
           Figure 2. Backward compatibility results for Lui NOP, coverage 3, 7, and 15, with and without labels in percentage overhead


of related works, and case studies demonstrating how the                        the position and number of labels can add to the number of
system can easily be adapted to different use-cases.                            tags inserted into the program.
    The off the shell performance tests were done on a
SiFive U540 SoC, which contains four 64-bit RISC-V cores                        5.0.1. Backwards Compatible Results. Overall the aver-
and 8GB of memory, running Gentoo Linux with the kernel                         age backward-compatibility performance overhead of insert-
version 5.2.9.                                                                  ing a tagged lui nop every three instructions and labels
    For the backwards compatibility evaluation, we use the                      was 13.40%. Every 7 instructions and labels was 6.98%,
lui nop described in subsubsection 4.1.1. This is chosen                        and every 15 instructions and labels was 5.72%. This is not
to allow the maximum amount of space per tag and still                          a linear reduction due to two major factors. The inclusion of
allow the binaries to run on unmodified hardware. Coverage                      the labels, whose overhead is the same over every coverage,
3 provides 6 tag bits per instructions covered, 7 provides                      and varied properties of benchmarks themselves. Addition-
2, and 15 provides one. While the labels were inserted as                       ally, for small loops and functions in binaries the number
described in the example CFI configuration (see subsec-                         of nop’s encountered in the code stream can be greater than
tion 4.2), evaluating them requires special hardware capable                    simply 1 per 3 normal instructions.
of interpreting the tags. This is explored in a separate
                                                                                             Listing 1. Sample RISC-V assembly loop
hardware paper [21].                                                             loop_start:
    We chose a subset of the SPEC CPU2017 rate bench-                               <TAG>
marks for our tests. This subset has a wide range of char-                          addi
acteristics, including heavy memory operations in mcf r,                            addi
and rapid arithmetic operations in deepsjeng r [22]. Testing                        addi
against these characteristics reveals what kind of perfor-                          <TAG>
                                                                                    j     loop_start
mance impacts can be expected on unmodified hardware.
SPEC CPU2017 provides three different types of workloads.                           Consider the simplified toy example in Listing 1. In
Test, training, and reference. The test workload is a simple                    this set of instructions, we encounter 2 tags every 4 normal
test of the executable, the training is designed to give                        instructions every time we iterate over the loop. Examples
feedback for optimizations, and the reference workload is                       can be found for every coverage size, using larger loops or
the fully timed data set. For these tests we used the full                      functions.
reference workload.
    The size overhead includes the overhead of label inser-                     5.0.2. Binary Properties. The properties of the benchmarks
tion on the coverage 3 configuration. This is done comparing                    have a major impact on the results of our evaluation. For
the size of a binary compiled with and without labels for                       programs that are memory operation intensive, even in back-
each benchmark program. A simple count of the number of                         wards compatibility mode, the overhead of additional nops
inserted labels would be insufficient when including tags, as                   added to the instruction stream is quickly overwhelmed by
the cost of memory operations. mcf r has this property, and       We present a case study, offering a detailed look at systems
has an overhead of 3.79% for coverage 3, and 1.09% for            COGENT could be used to achieve. We selected CVE-
coverage 15 in backwards compatibility mode. Conversely,          2021-3330 to explore with detailed solutions presenting and
small looping in-register operations can cause the nops to        describing Tag Aware Arithmetic subsection 5.2. Table 4
have an oversized impact on program performance. Deep-            shows a selection of recent CVEs to which our findings
sjeng fits this profile, and the resulting overhead is 21.79%     could be applied, including the one we selected for our
for C3, 12.45% for C7, and 8.39% for C15. Programs that do        case study. Additionally, we present an alternate usage of
not exhibit such extremes in behavior tend to have overhead       COGENT for fuzzing and testing, where we use COGENT
results close to the average results. Full results can be found   to keep track of coverage and branching behavior of a
in Figure 2.                                                      program at runtime.

5.0.3. Size Overhead. Overall we expect the text section          5.2. Case study: CVE-2021-3330
to increase in proportion to the number of tag instructions
we insert. However complicating this number is the function       5.2.1. CVE Description. CVE-2021-3330 is a vulnerability
alignment requirements. In the text section of the binary, the    that affects the Zephyr RTOS, and comes from a series
space between functions is filled in with zeros that extend       of errors that includes an improperly constructed cyclical
from the end of one function to the beginning of the next.        list, where a singularly linked list is expected. Iterating
This effect is more pronounced in C++ programs with many          over this now cyclical list causes an unsigned value to be
small functions such as omnetpp r and xlancbmk r. Labels          repeatedly subtracted from another unsigned value, leading
have a minimal impact on binary size, averaging 0.78%.            to an integer underflow. This value is then fed as the length
Overall the text section size increase is on average 29.32%       argument into memmove(), leading to a massive memory
for coverage three with labels, 20.49% for coverage seven,        move that can overwrite kernel structures. This CVE con-
and 18.27% for coverage 15. The full text section size results    sists of several points of failure. Improper list construction,
are available in Table 2.                                         integer underflow, and the overwrite itself. In this case study,
                                                                  we focus on a potential solution for the integer underflow,
5.0.4. Feature Comparison Against State of the Art.
                                                                  which would prevent the memory overwrite by triggering
COGENT was created to enable flexibility in its deployment.
                                                                  an exception when the underflow occurs.
To demonstrate this against the current state of the art, we
compared COGENT against recent tag-based architectures            5.2.2. Type Aware Arithmetic. To do this we will introduce
using several criteria.                                           type aware arithmetic, requiring a tag with a width of a
     •  How easy is it to employ the tool in a different          single bit, giving two possible states for every instruction
        manner then one of the creators designs? A high           as shown in Table 5. These two tags are N and UN ARTH.
        rating means the tool was designed for that purpose,      The N tag is given to any instruction in the stream that
        while a low indicates it was created for one specific     will not be checked for integer overflows. This can be
        task.                                                     because the instruction does not perform any arithmetic
     •  Does this tool have a Variable tag size?                  on an unsigned integer, or because the programmer or the
     •  Can this tool provide an arbitrary amount of meta-        compiler has chosen to disable the check on that instruction.
        data to a user above and beyond the size of the tag,      While the UN ARTH would be automatically placed on any
        if that user desires it.                                  instruction that performs arithmetic operations on unsigned
     •  Does this tool change the underlying ISA, removing        types. To insert this tag, an llvm compiler pass would be
        backwards compatibility.                                  created that examines the left hand side of any arithmetic
     •  Does this tool use the unused pointer bits?               expression, determining if the type of the result is unsigned.
     •  What kind of additional memory protection does this       If it is, the compiler will add the tag to any and all arith-
        tool employ to protect its tags?                          metic instructions that result from the IR level instruction,
The results of this investigation are available in Table 3. In    which the COGENT system would then propagate down into
general we find that unless a tool was designed for reuse         the final binary. There are two cases where the compiler
(i.e. PUMP), it would be difficult to re-target the hardware      would not place the UN ARTH tag on an instruction that
for other tasks. State of the art solutions use ISA changes       creates an unsigned value. The first is when the compiler
to provide the compiler with the wrappers and tag aware           can determine that values being subtracted are hard coded
instructions they need to manipulate their metadata. These        immediate values, in this case we would have to assume the
ISA changes break backwards compatibility. The common             programmer is attempting to exploit the defined behavior of
use of unused pointer bits, or custom pointers, render solu-      unsigned (IE unsigned MAX = 0 - 1). The second would be
tions incompatible with each other.                               a provided annotation that allows the programmer to opt out
                                                                  of the checks if they had a reason to. This is required as in
5.1. Case Studies and Use Cases                                   the C standard integer over/underflow are defined behavior.
                                                                  Forcing the user to opt out of the behavior is done in
   COGENT is a generic tool designed to allow for the             an attempt to minimize developer errors when using this
easy implementation of other tagging and labeling schemes.        defense.
                                               Baseline
                                                      luic3         luic3 no label luic7        luic15
                            gcc r O0           14,872,236
                                                      21,237,048       21,082,660  18,689,424   17,984,840
                            mcf r O0               99,900143,022          141,850     125,626      121,722
                            omnetpp r O0        4,242,910
                                                       6,126,344        6,007,300   5,565,696    5,536,696
                            xlanbmk r O0        8,057,876
                                                      11,430,065       11,190,441  10,452,873   10,447,205
                            x264 r O0             858,148
                                                       1,189,080        1,181,316   1,042,292      998,772
                            xz r O0               317,973440,498          436,670     391,798      379,986
                            deepsjeng r O0        217,512307,464          305,860     269,684      259,092
TABLE 2. T EXT SECTION SIZE IN BYTES FOR BASELINE AND 4 DIFFERENT CONFIGURATIONS . L UIC 3 IS COVERAGE 3 USING THE LUI NOP. LUIC 7 IS
                              COVERAGE 7 USING THE LUI NOP. LUIC 15 IS COVERAGE 15 USING THE LUI NOP.



                             Flexibility     Variable         Arbitrary    ISA Changes    Uses            Memory Protection
                                             Tag Size         Metadata                    Pointer
                                                                                          Bits
          PUMP [16]          High            No*              Yes**        Yes            No              Tags only readable by
                                                                                                          handlers
          CHERI [12]         Low             No               No           Yes            Fat pointers    Capability registers
          R ET TAG [14]      Low             No               No           Yes            Yes             hardware protected secure
                                                                                                          key
          ZERØ [13]          Low             No               No           Yes            Yes             Must protect tagged mem-
                                                                                                          ory
          T IMBER -V [23]    Low             No               No           Yes            No              Must protect tagged mem-
                                                                                                          ory
         COGENT             High         Yes          Yes                  No            No               WˆX protection
TABLE 3. S TATE OF THE ART COMPARISON . *PUMP’ S TAGS ARE S I Z E O F ( I N T *) PER MEMORY WORD , SOME APPLICATIONS WILL NOT USE THE
               FULL SPACE . **PUMP ALLOWS ARBITRARY METADATA BY ALLOWING A POINTER TO BE ENCODED INTO THE TAG



                  CVE                  Description                                               COGENTsolution
                  CVE-2021-3330        Several points of failure lead to a large buffer overflow Type Aware Arithmetic
                  CVE-2022-24310       An integer underflow leads to a large buffer overflow     Type Aware Arithmetic
                  CVE-2022-23613       A missing lower bound check leads to an integer underflow Type Aware Arithmetic
                                            TABLE 4. S AMPLE CVE’ S AND TAG - BASED SOLUTIONS



    Tag           Enum      Meaning
                            Instruction should not be checked for
                                                                              These lines of code operate only on unsigned integers,
    N             0                                                       and do not have the opt-out annotation. After compilation,
                            Integer over/underflow
                            This instruction should be checked for        this code produces the following relevant lines of assembly,
    UN ARTH       1
                            integer overflow/underflow                    loading the required values into registers, and then subtract-
          TABLE 5. OVERFLOW CHECK TAGGING SCHEME                          ing them.
                                                                                      Listing 3. CVE-2021-3330 underflow ASM
                                                                          lhu    a2, -34(s0)             #Load hdr_len into a2
5.2.3. Required Hardware Support. The required hard-
                                                                          ...
ware support for this tag scheme is very simple, if an
                                                                          lhu   a0, 8(a0)                #load frag->len into
instruction is tagged with UN ARTH, an exception should
                                                                              a9
be thrown if the results from that operation over or under-
                                                                          sub a2, a0, a2                 #subtract frag->len -
flow. Hardware would require this additional tag support
                                                                              hdr_len
as without metadata, it would be unable to differentiate
                                                                          ...
between an operations on signed and unsigned integers.
                                                                          call memmove                   #calls memmove
This can either be a hard stop to the program, or be call
                                                                          lh    a1, -34(s0)              #loads hdr_len into a1
an exception handler if the policy calls for it. In this case,
                                                                          ld    a2, -32(s0)              #loads frag into at2
for CVE-2021-3330, we would halt the program execution,
                                                                          lh    a3, 8(a2)                #loads frag->len into
as the corruption that results from this bug can overwrite
                                                                              A3
kernel data structures.
                                                                          sub a1, a3, a1                 #subtracts frag->len -
                                                                               hdr_len
5.2.4. Code and Tag Layout. For CVE 2021-3330, the
original integer overflow occurs in the following two lines                   There are two places in this code that will need to be
of code.                                                                  tagged, sub a2, a0, a2 and sub a1, a3, a1. This information
                                                                          can be determined by the compiler, and so they will receive
          Listing 2. CVE-2021-3330 underflow C++ source                   the UN ARTH tags. Resulting int he following final code
memmove(frag->data, frag->data +                                          layout. Note the tag instructions themselves are not included
    hdr_len, frag->len - hdr_len);                                        in this listing, as there exact position would depend on
frag->len -= hdr_len;                                                     compiler optimizations levels, and overall layout of the code,
however they would be inserted into the instruction stream           •   CI (Counting Instruction) - Indicates that this in-
at the appropriate alignments.                                           struction should be counted. When the instruction
                                                                         stream reaches this tag, it will check the current
            Listing 4. CVE-2021-3330 underflow ASM                       program counter against a mapping of PC : Int to
lhu a2, -34(s0)            [N]                                           see if this instruction has been encountered before.
...                                                                      If it has, it will increment the associated Int by one.
lhu a0, 8(a0)              [N]                                           If not, it will create a new entry in the map, with a
sub a2, a0, a2             [UN_ARTH]                                     value of one. This counting instruction is explicitly
...                                                                      slower than the counting label, due to the need for
call memmove               [N]                                           the mapping lookup, but could be used for specific
lh   a1, -34(s0)           [N]                                           purposes.
ld   a2, -32(s0)           [N]                                       •   BCF (Branching Control Flow) - Indicates a branch-
lh   a3, 8(a2)             [N]                                           ing point in the instruction stream that needs to
sub a1, a3, a1             [UN_ARTH]                                     be tracked. This tells the hardware to check if the
                                                                         current PC is in the BCF mappings. If it is not, it
    With all this in place, when this tagged code is run and             creates a new entry in a {PC : {Next PC : Count}}
the bug is triggered, operation would proceed normally until             mapping. After it has either found, or created, the
the subtraction that causes the arithmetic underflow. Once               entry for the current PC, the Hardware then checks if
that happens, the hardware would trigger an exception based              the next program counter is in the {Next PC: Count}
on the UN ARTH tag, causing an extra check as part of                    mapping, adding it if it does not find it, or increment
the instruction processing phase. A final strength of this               the counter if it does. In this way the BCF tag builds
solution is that because of the simple nature of the two                 a map of each branching instruction, the targets it has
tags employed, it would be a trivial exercise for COGENT                 reached, and how many times it has reached those
(although not necessarily the hardware) to layer this tag                targets.
with another single bit tag that would not be placed on              •   N (Normal) - This tells the hardware that no special
arithmetic instructions. An example could be the single bit              hardware actions are needed to enforce the overall
return address protection provided by [16] and [13]. These               security policies within this instruction.
two protections could be layered without the need for extra
tag sizes.                                                        5.3.3. Compiler Support. For coverage tracking, the com-
                                                                  piler would be configured to place counting labels and their
5.3. Capturing Code Coverage via Tagging                          associated tags at the beginning of every basic block at
                                                                  the machine instruction level. Additionally, a pass would
5.3.1. Description. Inline tagging could be used during           be added to enable two levels of branching control flow
testing and fuzzing to allow for quick tracking of program        tracking depending on the desires of the tester. BCF labels
coverage, counts of reached instructions, and paths taken         could be placed on every control flow instruction or, just on
through the CFG. In this use case, run time security is not       computed control flow instructions.
considered as part of the design. Instead COGENT inserts
metadata informing testing hardware what information it           5.3.4. Hardware Support. This coverage system requires
needs to record, and where it needs to record it. Hardware        two major changes to the supporting hardware beyond the
changes are limited to what is required to facilitate these       ability to read tags. First is the ability to write the incre-
tasks.                                                            mented counters back into the counting labels. Second is
                                                                  the ability to record the PC of instructions tagged with CI
5.3.2. Tag and Label Design. This use case requires the           or BCF into mappings. To do this we would reserve a known
coverage 7, 2 bit LUI labeling scheme. Giving 4 possible          memory for the CI and BCF mappings, and ensure that the
tags, each indicating a different behavior to the testing hard-   size of each entry is known to the hardware. This means
ware. Additionally we introduce counting labels, with the         for BCF mappings there would be a configurable maximum
initialized value of 0 as the primary fast coverage tracking      number of targets in order to keep lookup times reasonable.
method. An in depth description of each tag and the labels
are as follows, with a summary provided in Table 6.               5.3.5. Example Code. The RISC-V assembly code below
                                                                  is an example of how the coverage tagging would look like
   •    CL (Counting label) - Indicates that this instruction     on a simple basic block, followed by a branching jump.
        is a Counting label. When this tag, and the associated
        label are encountered in the instruction stream. First                    Listing 5. Coverage Example ASM
        the value contained in them is incremented by one,        0x0      TAG
        Then written back into the label. This requires that      0x4      lui      x0, 0                    [CL]
        the system be allowed to write values back into the       0x8      lw       a1, -24(s0)              [N]
        code section. These will be placed at the beginning       0xC      addi     a1, a1, 1                [N]
        of every basic block.                                     0x10     sw       a1, -24(s0)              [N]
                      Tag    Enum     Meaning
                      CL     0        Increments the label value of this instruction when encountered
                      CI     1        Tells the hardware to record a counter for this PC in memory.
                      BCF    2        Tells the hardware to record what the next PC is for the current PC in memory.
                      N      3        Normal behavior
                                                 TABLE 6. C OVERAGE TAGS SUMMARY



0x14 lw           a1, -24(s0)     [N]                                      R ET TAG [14] uses compiler modifications to insert new
0x18 bne          a1, a0, .LBB0_2 [BCF]                               instructions in the function prologue and epilogue to gener-
.LBB0_1:                                                              ate a new instruction (thus modifying the ISA and preventing
0x1C lui          x0, 0                    [CL]                       backward compatibility) called pac. This instruction gener-
0x20 TAG                                                              ates a unique PAC using the a unique function ID, the virtual
0x24 lw           a0, -24(s0)              [N]                        address of the functions RA the SP, and a 128-bit RAA key
0x28 addi         a0, a0, -1               [N]                        and stores it in the 16 unused bits of a function pointer, and
0x2C sw           a0, -24(s0)              [N]                        it is used to protect the return address from corruption.
.LBB0_2:
                                                                          ZERØ [27], provides a set of new instructions via a
0x30 lui          x0, 0                    [CL]
                                                                      modification of the ISA and a method of encoding metadata
0x34 addi         a0, zero, 5              [N]
                                                                      that keeps the overhead to eight bytes per 4kb page. This
0x38 sw           a0, -24(s0)              [N]
                                                                      work discards backwards compatibility by including new
                                                                      ISA instructions for memory and metadata management.
    Assuming the above was entered three times, jump-                 Additionally it stores ten bits of information in the unused
ing once and falling through twice, the counting label                bits of pointers to enforce its own version of CFI using
at 0x4 would be lui x0, 3 and at 0x1C it would be                     unique-per-function signature labels. It modifies the com-
lui x0, 2. LBB0 2 is always reached at 0x34, making                   piler to insert its new instructions at the appropriate places.
its value lui x0, 3. The branching table would con-
tain 0x18 : 0x1C: 2, 0x30: 1, showing the exact                           T IMBER -V [28] allows the creation of up to four pro-
behavior of the branch over three runs.                               tected enclaves on a RISC-V system. It likewise uses a new
                                                                      set of instructions in order to tell the hardware when it
                                                                      can check and manipulate tags. Like ZERØ this removes
6. Related Work                                                       its ability to provide backwards compatible code. While
                                                                      Timber-V does not require compiler support, they specify
     PUMP [16] provides a flexible system to associate meta-          that compiler support would allow for better integration.
data with memory locations, including on instructions. Its
large fixed-width tag sizeof(*) provides unlimited metadata               Several works including F RAMER [15], R ET TAG [14]
size per memory location via allowing the tag to simply               and ZERØ [13] use the upper unused bits of a pointer as
be a pointer to additional memory. However schemes that               place to store there metadata. Doing this gives these works
actually use this amount of space are not common, and                 strong backwards compatibility and low overhead. However
even then, the common case for any given memory location              by their nature it is difficult to layer any works that use
is a small amount of metadata. To help alleviate the large            the unused pointer bits. Additional space is limited, leading
overhead of having so much unused space in there tags, they           framer to use a novel encoding scheme to provide access
propose and implement tag compression schemes to speed                to the pointers metadata, relying as much as possible on
up retrieval. Compared to PUMP, COGENT’s instruction                  relative offsets from known points (i.e. frames). Such works
tagging allows simpler protection schemes to use smaller              could be combined with COGENT as we do not manipulate
tag sizes when needed, while at the same time allowing                the pointers themselves, leaving the unused parts of the
that same arbitrarily sized metadata though the use of any            pointer available for any other scheme, or to be used for
number of inline labels. Importantly though, PUMP has an              a CFI label.
actual hardware design backing it, while COGENT does not                   FlexFit [29] is a generalized solution presented as a
have specialized hardware, so the only comparison can be              method to allow users to configure a set of instruction filters
between the location and storage size of metadata, not on             for use at run time. They implement this on a RISC-V
performance, hardware complexity, or overall overhead.                FPGA board to demonstrate the practicality, feasibility, and
     CHERI [12] is a well known capability based archi-               usability of there generic filtering scheme. They store the
tectural enhancement for RISC instruction sets. Work on               protection information in four unused bits of the RISC-V
it is current and on-going [24] [25] [26]. CHERI provides             PTE, giving up to 16 protected domains at a page granular-
its security though architectural capabilities, and tagged            ity. Using the unused bits in the PTE, has been explored by
memory. Additionally it is a full stack solution, and includes        other works as well [30] [31], which is why FlexFit limits its
compiler, OS, and hardware support. Cheri itself requires             protection domains to 16, using only 4 of the available bits.
that compiler to support on all levels. Language specific             While FlexFit does not use a compiler, the authors suggest
front end, IR level passes, and backend emissions.                    a specialized compiler and loader for future work.
7. Conclusion                                                                   [11] S. Ramakesavan and J. Rodriguez, “Intel® Memory Protection Exten-
                                                                                     sions Enabling Guide,” 2016, https://software.intel.com/en-us/articles/
                                                                                     intel-memory-protection-extensions-enabling-guide.
    This paper presents COGENT, a modified LLVM com-
                                                                                [12] R. N. Watson, J. Woodruff, P. G. Neumann, S. W. Moore, J. Anderson,
piler capable of generating RISCV binaries with config-                              D. Chisnall, N. Dave, B. Davis, K. Gudka, B. Laurie, S. J. Murdoch,
urable width instruction tags. These tags can be used to de-                         R. Norton, M. Roe, S. Son, and M. Vadera, “Cheri: A hybrid
sign security solutions that convey rich semantic information                        capability-system architecture for scalable software compartmental-
to a tag-aware architecture. COGENT tackles several chal-                            ization,” in 2015 IEEE Symposium on Security and Privacy, 2015,
                                                                                     pp. 20–37.
lenges including one-to-many and many-to-one instruction-
level tag associations. We evaluate COGENT on a subset                          [13] M. T. Ibn Ziad, M. A. Arroyo, E. Manzhosov, and S. Sethumadha-
                                                                                     van, “ZerØ: Zero-overhead resilient operation under pointer integrity
of SPEC 2017 benchmark programs for performance and                                  attacks,” in 2021 ACM/IEEE 48th Annual International Symposium
binary bloat. Further, we present case studies that highlight                        on Computer Architecture (ISCA), 2021, pp. 999–1012.
potential use cases of COGENT.                                                  [14] Y. Wang, J. Wu, T. Yue, Z. Ning, and F. Zhang, “Rettag: Hardware-
                                                                                     assisted return address integrity on risc-v,” in Proceedings of the 15th
                                                                                     European Workshop on Systems Security, ser. EuroSec ’22. New
References                                                                           York, NY, USA: Association for Computing Machinery, 2022, p.
                                                                                     50–56. [Online]. Available: https://doi.org/10.1145/3517208.3523758
[1]   P. Kocher, J. Horn, A. Fogh, , D. Genkin, D. Gruss, W. Haas,              [15] M. J. Nam, P. Akritidis, and D. J. Greaves, “Framer: A tagged-
      M. Hamburg, M. Lipp, S. Mangard, T. Prescher, M. Schwarz, and                  pointer capability system with memory safety applications,” ACM
      Y. Yarom, “Spectre attacks: Exploiting speculative execution,” in 40th         International Conference Proceeding Series, pp. 612–626, 2019.
      IEEE Symposium on Security and Privacy (S&P’19), 2019.                    [16] U. Dhawan, C. Hritcu, R. Rubin, N. Vasilakis, S. Chiricescu,
[2]   M. Lipp, M. Schwarz, D. Gruss, T. Prescher, W. Haas, A. Fogh,                  J. M. Smith, T. F. Knight, B. C. Pierce, and A. DeHon,
      J. Horn, S. Mangard, P. Kocher, D. Genkin, Y. Yarom, and M. Ham-               “Architectural support for software-defined metadata processing,”
      burg, “Meltdown: Reading kernel memory from user space,” in 27th               in Proceedings of the Twentieth International Conference on
      USENIX Security Symposium (USENIX Security 18), 2018.                          Architectural Support for Programming Languages and Operating
                                                                                     Systems, ser. ASPLOS ’15. New York, NY, USA: Association
[3]   C. Cowan, C. Pu, D. Maier, J. Walpole, P. Bakke, S. Beattie, A. Grier,         for Computing Machinery, 2015, p. 487–502. [Online]. Available:
      P. Wagle, Q. Zhang, and H. Hinton, “StackGuard: Automatic                      https://doi.org/10.1145/2694344.2694383
      adaptive detection and prevention of Buffer-Overflow attacks,” in
                                                                                [17] N. Roessler and A. DeHon, “Protecting the stack with metadata
      7th USENIX Security Symposium (USENIX Security 98). San
                                                                                     policies and tagged hardware,” in 2018 IEEE Symposium on Security
      Antonio, TX: USENIX Association, Jan. 1998. [Online]. Available:
                                                                                     and Privacy (SP), 2018.
      https://www.usenix.org/conference/7th-usenix-security-symposium/
      stackguard-automatic-adaptive-detection-and-prevention                    [18] C. Lattner and V. Adve, “Llvm: A compilation framework for lifelong
                                                                                     program analysis & transformation,” in Proceedings of the Interna-
[4]   M. Zhang and R. Sekar, “Control flow integrity for COTS                        tional Symposium on Code Generation and Optimization: Feedback-
      binaries,” in 22nd USENIX Security Symposium (USENIX Security                  Directed and Runtime Optimization, ser. CGO ’04. USA: IEEE
      13). Washington, D.C.: USENIX Association, Aug. 2013, pp.                      Computer Society, 2004, p. 75.
      337–352. [Online]. Available: https://www.usenix.org/conference/
      usenixsecurity13/technical-sessions/presentation/Zhang                    [19] D. Dewey and J. T. Giffin, “Static detection of C++ vtable escape vul-
                                                                                     nerabilities in binary code.” in Proceedings of 19th Annual Network
[5]   B. Niu and G. Tan, “Per-input control-flow integrity,” in Proceedings          and Distributed System Security Symposium (NDSS’12), 2012.
      of the 22nd ACM SIGSAC Conference on Computer and
                                                                                [20] Y. Li, M. Wang, C. Zhang, X. Chen, S. Yang, and Y. Liu,
      Communications Security, ser. CCS ’15. New York, NY, USA:
                                                                                     Finding Cracks in Shields: On the Security of Control Flow
      Association for Computing Machinery, 2015, p. 914–926. [Online].
                                                                                     Integrity Mechanisms. New York, NY, USA: Association for
      Available: https://doi.org/10.1145/2810103.2813644
                                                                                     Computing Machinery, 2020, p. 1821–1835. [Online]. Available:
[6]   M. Abadi, M. Budiu, Ú. Erlingsson, and J. Ligatti, “Control-flow              https://doi.org/10.1145/3372297.3417867
      integrity principles, implementations, and applications,” ACM Trans.      [21] R. Gollapudi, G. Yuksek, D. Demicco, M. Cole, G. Kothari, R. Kulka-
      Inf. Syst. Secur., vol. 13, no. 1, pp. 1–40, 2009. [Online]. Available:        rni, X. Zhang, K. Ghose, A. Prakash, and Z. Umrigar, “Control flow
      https://doi.org/10.1145/1609956.1609960                                        and pointer integrity enforcement in a secure tagged architecture,” to
[7]   V. Shanbhogue, D. Gupta, and R. Sahita, “Security analysis                     appear in 2023 IEEE Symposium on Security and Privacy.
      of processor instruction set architecture for enforcing control-          [22] R. Hebbar and A. Milenkovic, “Spec cpu2017: Performance, event,
      flow integrity,” in Proceedings of the 8th International                       and energy characterization on the core i7-8700k,” in ICPE ’19:
      Workshop on Hardware and Architectural Support for Security                    Proceedings of the 2019 ACM/SPEC International Conference on
      and Privacy, ser. HASP ’19.             New York, NY, USA:                     Performance Engineering, 04 2019, pp. 111–118.
      Association for Computing Machinery, 2019. [Online]. Available:           [23] S. Weiser, M. Werner, F. Brasser, M. Malenko, S. Mangard,
      https://doi.org/10.1145/3337167.3337175                                        and A.-R. Sadeghi, “TIMBER-V: Tag-Isolated Memory
[8]   I.    Corporate,    Intel    architecture   instruction   set    ex-           Bringing Fine-grained Enclaves to RISC-V,” in Network
      tensions      programming        reference,      2014.     [Online].           and Distributed System Symposium, ser. NDSS ’19, 2019.
      Available: https://www.intel.com/content/dam/develop/external/us/en/           [Online]. Available: https://www.ndss-symposium.org/ndss-paper/
      documents/329298-002-629101.pdf                                                timber-v-tag-isolated-memory-bringing-fine-grained-enclaves-to-risc-v/

[9]   A. Limited, TrustZone technology for Armv8-M Architecture Version         [24] N. Wesley Filardo, B. F. Gutstein, J. Woodruff, S. Ainsworth, L. Paul-
      2.1, ver 2.1, Fulbourn Road, Cambridge, England CB1 9NJ.A,                     Trifu, B. Davis, H. Xia, E. Tomasz Napierala, A. Richardson, J. Bald-
      2018. [Online]. Available: https://developer.arm.com/documentation/            win, D. Chisnall, J. Clarke, K. Gudka, A. Joannou, A. Theodore
      100690/0201/?lang=en                                                           Markettos, A. Mazzinghi, R. M. Norton, M. Roe, P. Sewell, S. Son,
                                                                                     T. M. Jones, S. W. Moore, P. G. Neumann, and R. N. Watson,
[10] Intel Corporation, “Introduction to Intel Memory Protection                     “Cornucopia: Temporal safety for CHERI heaps,” Proceedings - IEEE
     Extensions,”      2013,     https://software.intel.com/en-us/articles/          Symposium on Security and Privacy, vol. 2020-May, pp. 608–625,
     introduction-to-intel-memory-protection-extensions.                             2020.
[25] B. Davis, R. N. Watson, A. Richardson, P. G. Neumann, S. W.
     Moore, J. Baldwin, D. Chisnall, J. Clarke, N. W. Filardo, K. Gudka,
     A. Joannou, B. Laurie, A. T. Markettos, J. E. Maste, A. Mazzinghi,
     E. T. Napierala, R. M. Norton, M. Roe, P. Sewell, S. Son, and
     J. Woodruff, “CheriABI: Enforcing Valid Pointer Provenance and
     Minimizing Pointer Privilege in the POSIX C Run-time Environ-
     ment,” International Conference on Architectural Support for Pro-
     gramming Languages and Operating Systems - ASPLOS, pp. 379–
     393, 2019.
[26] H. Xia, J. Woodruff, S. Ainsworth, N. W. Filardo, M. Roe,
     A. Richardson, P. Rugg, P. G. Neumann, S. W. Moore, R. N. Watson,
     and T. M. Jones, “CHERIvoke: Characterising pointer revocation us-
     ing CHERI capabilities for temporal memory safety,” Proceedings of
     the Annual International Symposium on Microarchitecture, MICRO,
     pp. 545–557, 2019.
[27] M. T. Ibn Ziad, M. A. Arroyo, E. Manzhosov, and S. Sethumadhavan,
     “ZeRØ: Zero-Overhead Resilient Operation Under Pointer Integrity
     Attacks,” pp. 999–1012, 2021.
[28] S. Weiser, M. Werner, F. Brasser, M. Malenko, S. Mangard, and A.-R.
     Sadeghi, “TIMBER-V: Tag-Isolated Memory Bringing Fine-grained
     Enclaves to RISC-V,” no. February, 2019.
[29] L. Delshadtehrani, S. Canakci, W. Blair, M. Egele, and A. Joshi,
     “FlexFilt: Towards Flexible Instruction Filtering for Security,” Annual
     Computer Security Applications Conference (ACSAC’21), pp. 646–
     659, 2021.
[30] L. Delshadtehrani, S. Canakci, M. Egele, and A. Joshi, “Sealpk:
     Sealable protection keys for risc-v,” in 2021 Design, Automation Test
     in Europe Conference Exhibition (DATE), 2021, pp. 1278–1281.
[31] D. Schrammel, S. Weiser, S. Steinegger, M. Schwarzl, M. Schwarz,
     S. Mangard, and D. Gruss, “Donky: Domain keys – efficient
     In-Process isolation for RISC-V and x86,” in 29th USENIX Security
     Symposium (USENIX Security 20). USENIX Association, Aug.
     2020, pp. 1677–1694. [Online]. Available: https://www.usenix.org/
     conference/usenixsecurity20/presentation/schrammel
