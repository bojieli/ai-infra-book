                          Parameterized Hardware Design with
                              Latency-Abstract Interfaces
                   Rachit Nigam                       Ethan Gabizon∗                                Edmund Lam∗
                     MIT CSAIL                        Cornell University                           Cornell University
                   Cambridge, USA                        Ithaca, USA                                  Ithaca, USA

                   Carolyn Zech                      Jonathan Balkind                             Adrian Sampson
                     MIT CSAIL                      University of California                       Cornell University
                   Cambridge, USA                    Santa Barbara, USA                               Ithaca, USA

Abstract                                                          1    Introduction
Hardware designs must use latency-insensitive (LI) inter-         Interfaces create the foundation for modular design: they ab-
faces when timing is input-dependent. When timing is input-       stract away implementation concerns while providing suffi-
independent, designs should use latency-sensitive (LS) in-        cient details to enable compositional and efficient integra-
terfaces for maximum performance. However, designs com-           tion. In hardware design, interfaces consist of both struc-
monly use LI interfaces to integrate with externally gener-       tural details, such as the bitwidths of input and output ports,
ated LS modules–from, e.g., IP generators, high-level synthe-     and temporal behaviors, such as how often a module can ac-
sis, or domain specific languages. In every fully integrated      cept new inputs.
design, such uses of LI represent pure overhead. The chal-           Hardware interfaces’ temporal behavior broadly falls into
lenge is that generators can dramatically change timing in-       two categories: latency sensitive (LS) or latency insensitive
terfaces of the modules to meet performance objectives, and       (LI).1 With LI interfaces, these concerns take the form of
LI interfaces act as a useful design abstraction and enable       explicit control signals, such as valid and ready ports that
timing adaptation.                                                indicate when a module has produced an output or is ready
   We define latency-abstract (LA) interfaces, a new design       to accept an input. LS interfaces, on the other hand, spec-
abstraction, which provide the timing adaptability of LI in-      ify input-independent timing behaviors (“a four-cycle mul-
terfaces at design-time and the efficient integration of LS in-   tiplier that accepts new inputs every other cycle”) and are
terfaces. LA interfaces use output parameters, a novel compile-   efficient because communicating modules can simply use
time mechanism for child modules to return values parent          the already-available clock signal for synchronization. LI in-
modules, to abstract and encapsulate timing behaviors at de-      terfaces come with fundamental overhead of extra synchro-
sign time. During design elaboration, LA interfaces are com-      nization logic, but they are necessary for handling hardware
piled into efficient LS interfaces based on parameter values.     units where latencies are fundamentally input-dependent.
   While an attractive option, LA interfaces inherit the com-        Hardware designers also use LI interfaces to decouple mod-
plexities of parameterized hardware design: the user must         ules from each other, even when those modules have input-
reason how parameters influence timing behaviors of mod-          independent latency. One particularly important use case is
ules and ensure that designs adapt to interface changes. To       when integrating generated hardware modules using core
address this challenge and demonstrate the utility of LA in-      generators [1, 6, 9], high-level synthesis [30, 36], or acceler-
terfaces, we design Lilac, a parameterized HDL that uses          ator design languages [8, 11, 12, 22, 27]. Even when a gener-
a type system track the influence of parameters on timing         ated hardware module could technically use an LS interface,
behaviors and formally guarantee that every parameteriza-         it would be awkward to use because the timing behavior
tion of an LA design results in a circuit without structural      depends on the generator’s inputs. For example, changing
hazards. We demonstrate Lilac’s efficacy by using it to im-       the arithmetic precision of a floating-point core generator
plement parameterized designs and integrate designs gener-        might change the resulting module’s latency and pipeline
ated from external tools. We show that LA designs use 26–         depth; the designer would then need to manually inspect
33% fewer chip resources and achieve 6.8% better maximum          the tool’s output and carefully cascade the change through-
frequencies than comparable LI implementations.                   out their design. In this scenario, LI interfaces are a conve-
                                                                  nient but expensive design abstraction: an LS interface would
                                                                  suffice, but it would require frequent and invasive changes
                                                                  whenever the generated module changes.

                                                                  1
                                                                   Though these terms focus on latency, they are used for interfaces that
∗
    Equal contribution.                                           capture both latency and initiation interval information.
                                                                                                                 Rachit Nigam et al.


   There is an alternative: using compile-time parameteriza-
tion constructs to adapt to modules’ changing timing behav-                                               Mult
                                                                            Mult
iors and to automatically emit the correct LS interface. We
define a latency-abstract (LA) design as the approach that
uses parameterized LS designs to abstract over timing de-               Add                                 Add
tails. Because interfaces in LA designs are a compile-time
abstraction, they provide the best of both worlds: the effi-
ciency of LS interfaces and the flexibility of LI interfaces.
   However, mainstream HDLs often make LA design infea-
sible for two reasons:                                                (a) LS FPU, fixed timing.     (b) LI FPU, flexible timing.

                                                                    Figure 1. Circuit diagrams for different arithmetic unit
    1. Parameters in mainstream HDLs flow in the wrong di-          (FPU) implementations that can adapt to changing timing
       rection (Figure 3). They make it easy to pass values         interfaces of multipliers and adders.
       from parent modules to the child modules they in-
       stantiate: for example, code that instantiates a multi-    1 module FPU(clk, a, b, op, out);
       plier might specify its numerical precision as a param-    2    wire [31:0] add_o, mul_o, add_d[1:0]; wire [3:0] op_d;
       eter. But when using hardware generators, it is the        3    Add(.clk, .a, .b, .out(add_o)) add;
       generator that determines timing behavior, not the         4    Mul(.clk, .a, .b, .out(mul_o)) mul;
       module that uses the generator. There is no straight-      5    always @(posedge clk)
                                                                  6       op_d <= { op, op_d[3:1] };
       forward way for the generated multiplier to pass pa-       7       add_d[1] <= add_o; add_d[0] <= add_d[1];
       rameters capturing timing behaviors “upward” to its        8    assign out = op_d[0] ? add_d[0] : mul_o;
       parent. Developers therefore resort to ad hoc and error- 9 endmodule
       prone workarounds such as using hierarchical param-
       eter assignments [19] or external configuration files [31]. Figure 2. FPU with four-cycle multiply and two-cycle adder.
    2. LA interfaces require subtle reasoning about the influ-      The implementation delays the adder’s output and the op
       ence of parameters on timing behaviors. Parameter-           signal to balance the pipeline.
       ized designs are already difficult: they express fami-
       lies of circuits, requiring testing across many different
       parameterizations to gain confidence in their correct-
                                                                    (§5). Finally, we demonstrate that Lilac can be used to imple-
       ness. The addition of complex, parameter-dependent
                                                                    ment efficient LA designs which have substantially lower re-
       timing behaviors make them even more unappealing
                                                                    source usage and higher clock frequencies compared to sim-
       to use.
                                                                    ilar LI designs (§7). These features let Lilac flexibly express
                                                                    LA designs that offer the compositionality and correctness
                                                                    of LI interfaces without their overheads.
Together, these limitations make LA interfaces challenging
to use and leave users of mainstream HDLs with an undesir-
able trade-off: pay the cost of an inefficient LI interface or
                                                                    2 Integrating Generated Hardware
risk writing brittle parameterization code.                         We motivate the need for latency-abstract interfaces by de-
   We address the first challenge by introducing output pa-         signing a floating-point unit (FPU) that integrates high-quality
rameters, a novel, compile-time construct that enables child        adder and multiplier implementations generated by FloPoCo [6],
modules to return parameter values to their parents. Out-           an  FPGA-focused floating-point core generator. This section
put parameters precisely capture the kind of inverted flow          examines    what integrating such generated modules looks
needed to express the interfaces of generated modules. We           like when   using LS, LI, and parameterized design with cur-
show that LA interfaces with output parameters can express          rent HDLs. We then highlight the potential benefits of LA
generated module interfaces correctly and concisely (§6).           design and the challenges it poses with traditional HDLs.
   We address the second challenge by designing Lilac, a
parameterized HDL that uses output parameters to enable             2.1 Latency-Sensitive Interfaces
compile-time reasoning and verification of LA circuits. Lilac          FloPoCo accepts as input which computation to imple-
programs can explicitly capture LA interface and integrate          ment (add, multiply) and performance goals (frequency, FPGA
generated modules (§3). Lilac uses a novel type system (§4)         family) to target; it outputs a pipelined, LS implementation
based on timeline types [28] to ensure that every possible pa-      and reports its latency. Alternating the performance goals
rameterization of a design will correctly interface with such       may change the LS interface to the module in unpredictable
modules and lowers them into efficient circuit descriptions         ways.
   Our initial FPU implementation (Figure 1a) uses LS in-             Configuration LUTs Registers Freq. (MHz)
terfaces. We run FloPoCo with specific performance char-               LI (A=1, M=1)       614      824          134.5
acteristics and use the latency information to integrate the           LS (A=1, M=1)       441      205          163.0
generated modules. Figure 2 overviews the implementation               LI (A=4, M=2)       662     1426          224.4
code for a four-cycle multiplier and two-cycle adder: the              LS (A=4, M=2)       459      482          280.8
implementation forwards the inputs directly into the mod-         Table 1. Resource usage of latency-sensitive (LS) and
ules and selects the output based on the op signal. Since the     latency-insensitive (LI) FPU implementations. 𝐴 and 𝑀 are
multiplier takes two extra cycles, the implementation delays      the latencies of the FloPoCo-generated adder and multiplier.
the output from the adder and the op signal to balance the
pipeline (lines 5–7).
   While straightforward, this implementation has a signif-
icant limitation: trying a new design point requires chang-                     Top
                                                                                       AU
ing the pipeline balancing logic. This is because FloPoCo can                                     FAdd     flopoco
change the timing behaviors of the implementations it gen-
erates, and our LS implementation fundamentally relies on
                                                                                            latency=3
the specific timing characteristics of the adder and multi-
plier. This also limits performance portability: changing the     (a) Top-down parameterization. Adder latency is plumbed through
FPGA target would require manual changes to the control-          the hierarchy to parameterize pipeline-balancing shift register.
logic code.                                                                    Top
                                                                                      AU
2.2 Latency-Insensitive Interfaces                                                                FAdd     flopoco

The fundamental problem with our LS implementation is
that it cannot adapt to changing timing behaviors. Latency-
                                                                  (b) Bottom-up parameterization. Generated modules produce out-
insensitive (LI) interfaces [5] abstract away timing details
                                                                  put parameters resulting in clean encapsulation.
using synchronization signals. A common LI interface style
is a ready–valid handshake that synchronizes one producer
                                                                     Figure 3. Flow of parameters when using generators.
and one consumer. The producer uses a 1-bit valid signal to
indicate that a separate data signal is meaningful; the con-
sumer uses a 1-bit ready signal to tell the producer that it is
ready to consume the data. When ready and valid are both          comparing them to LS implementations. The LI interface re-
asserted in the same clock cycle, a transaction occurs.           quires 3–4× more registers, uses 29–31% more LUTs, and
   We can decouple our FPU’s control logic from the spe-          reduces the maximum frequency by 21–25%. For the higher
cific timing behaviors of the compute modules by wrapping         frequency designs, the critical path for the LS implementa-
them with a ready–valid interface that provides a stable in-      tion remains within the compute units; for the LI design, the
terface. With this approach, whenever FloPoCo generates a         handshaking logic itself becomes the critical path.
new adder or multiplier, we can locally change the LI wrap-          For designs with fundamentally input-dependent timing
per to generate the correct ready and valid signals; the FPU      behaviors, such as a memory hierarchy, LI interfaces are
control logic can remain unchanged. Figure 1b visualizes          mandatory and their cost is unavoidable. However, for our
this design: the adder and multiplier have extra logic that       FPU design, where each specific adder or multiplier has a
tracks their ready and valid signals which are then used by       latency-sensitive interface, the cost is pure overhead. The
the finite state machine (FSM) within the FPU to control the      design-time benefit of ease of integration and correct rea-
flow of the data. The FPU also has to use a FIFO queue to         soning provided by an LI interface conflicts with the need
hold the op signal so it can correctly multiplex between the      for the highest performing implementation.
adder and multiplier outputs. The extra resources used for
the coordination logic can quickly dominate the total cost of     2.3 Parameterized Design
smaller circuits, making LI interfaces especially expensive       A third alternative is to implement a parameterized design.
for fine-grained integration.                                     Such designs use compile-time values called parameters, along
   The LI wrappers provide design modularity, but there is        with metaprogramming constructs such as loops and con-
a cost. We need additional ready and valid signals, FSMs to       ditionals, to generate circuits. An HDL frontend evaluates
orchestrate them, and a FIFO for bookkeeping. These addi-         these constructs during its elaboration phase. In our case,
tional components result in both area (chip resources) and        this means that if we implement an FPU using parameters
performance overheads and yield a more complex design             for the latencies of the adder and the multiplier, we could
that is harder to verify. Table 1 quantifies the hardware cost    generate efficient LS interfaces without hard-coding any tim-
of LI interfaces by synthesizing designs using Vivado and         ing details.
                                                                                                             Rachit Nigam et al.


      Interface                  Design   Compile   Execute     1 gen "flopoco" comp FPAdd[#W]<G:1>( // Fully-pipelined
                                                                2   val_i: interface[G]              // Provide new input
      Latency Sensitive (LS)       3        3         3         3   l: [G, G+1] #W, r: [G, G+1] #W   // Required in cycle 1
      Latency Abstract (LA)        7        3         3         4 ) -> (o: [G+#L, G+#L+1] #W         // Provided on cycle L
      Latency Insensitive (LI)     7        7         3         5 ) with { some #L where #L > 0; }   // Output parameter
 Table 2. When an interface’s timing behavior is known.
                                                                 Figure 4. Latency-abstract interface for FloPoCo adders.
                                                                 The latency is determined at elaboration-time.



                                                                 2.4 Latency-Abstract Interfaces
   To cope with possible timing mismatches between the           The solution lies in recognizing that generator-produced de-
adder and multiplier, our parameterized design must gen-         signs occupy a unique point in the design space: their tim-
erate a configurable number of pipeline stages to balance        ing behaviors are parameterized at design-time but concrete
the latencies. For example, if the adder takes two cycles but    at compile-time (Table 2). We name such interfaces latency-
the multiplier takes four, the output from the adder needs       abstract (LA) interfaces as they abstract away timing details
to be delayed by two cycles to ensure that the FPU’s latency     of modules, but do so using parameters. They capture how
is always four cycles. The FPU can use a parameterized shift     a module’s timing behaviors are influenced by various pa-
register module, Shift that delays a signal by 𝑁 cycles:         rameters. When using a latency-abstract (LA) module, an
localparam Max = Add_L > Mul_L ? Add_L : Mul_L;
                                                                 implementation must itself be parameterized and adapt to
localparam Add_B = Max - Add_L;                                  changes in the timing behavior of the LA module. This is
localparam Mul_B = Max - Mul_L;                                  similar to how a module interfacing with an LI module must
Shift#(.N(Add_B)) s_add(.in(sum), .out(sum_d));                  itself present an LI interface. The complex interdependence
Shift#(.N(Mul_B)) s_mul(.in(mul), .out(mul_d));
                                                                 of parameters and timing behaviors also explains the chal-
Shift#(.N(Max)) s_op(.in(op), .out(op_d));
                                                                 lenges in verifying the correctness of integration of such
This delay logic suffices for any pair of latencies for our      modules.
FloPoCo-generated adder and multiplier, Add_L and Mul_L.            We address these challenges by designing Lilac, the first
But implementing it in current HDLs like Verilog can quickly     HDL to capture LA interfaces and provide compile-time rea-
become awkward.                                                  soning for them. Lilac uses a novel type system based on
   The first challenge is that our FPU must somehow know         timeline types [28] which can express and statically reason
these latency parameters, Add_L and Mul_L, to generate this      about parameters’ influence on a module’s timing behavior
delay logic. HDL parameters flow top down, from parent           and formally guarantee that there are no structural hazards
modules to child modules, so the only obvious solution is        or resource reuse violations in any parameterization of the
to add these parameters to the FPU module’s signature and        design.
“plumb” them down to the delay logic (Figure 3a). But this
violates encapsulation. Users of the FPU module should not       3     The Lilac Language
need to know about the adder’s and multiplier’s latencies;
                                                                 Lilac’s design must solve two related problems: specifying
FloPoCo determines the latency of the modules. Logically,
                                                                 latency-abstract (LA) interfaces of externally generated mod-
we want these parameters to flow bottom up, from the adder
                                                                 ules, and writing correct, parameterized programs using such
and multiplier up to the FPU (Figure 3b). Current HDLs do
                                                                 interfaces. Lilac extends timeline types [28]—a type-system
not allow this, so users adopt various ad-hoc approaches
                                                                 to reason about LS designs—to capture the effects of param-
such as using hierarchical references [19], passing them us-
                                                                 eters on timing behaviors and introduces output parameters
ing external configuration files [23, 31], or using complex
                                                                 to capture the timing behaviors of generated modules. To-
parameter negotiation frameworks [7].
                                                                 gether, these features enable it to capture interfaces from a
   The second challenge is that this parameterized approach
                                                                 variety of generators (§6) and provide compile-time reason-
implicitly encodes assumptions about the timing behavior
                                                                 ing about LA interfaces (§4).
of modules and the relationships between parameters. The
                                                                    We continue with our FPU example and show how Lilac
designer of the FPU module would like to locally reason
                                                                 captures design-time unknown timing behaviors using latency-
about the relationship between the parameters Add_L and
                                                                 abstract interfaces (§3.1), propagates output parameters to
Mul_L. However, an external user can easily make the mis-
                                                                 enable modular design (§3.2), and correctly adapts to chang-
take of flipping the two parameters when they are provided
                                                                 ing timing behaviors (§3.3).
as inputs causing the FPU module to generate incorrect bal-
ancing logic. The flexibility of metaprogramming also makes
                                                                 3.1   Specifying Latency-Abstract Interfaces
it error-prone and potentially more difficult to verify.
   External generators like FloPoCo choose the timing be-        1 comp FPU[#W]<G:1>(
haviors of modules based on input parameters, such as bitwidth, 2 op: [G, G+1] 1, l: [G, G+1] #W, r: [G, G+2] #W
                                                                 3 ) -> (o: [G, G+1] #W) {
as well as optimization directives. Figure 4 shows the Lilac     4    // Instantiate the modules
interface for a FloPoCo-generated adder. Like a traditional      5    Add := new FPAdd[#W]; Mul := new FPMul[#W];
parameterized module, it has an input parameter #W which         6    // Schedule execution when G occurs
controls the bitwidth of the ports (lines 1–4). The Lilac inter- 7    add := Add<G>(l, r); mul := Mul<G>(l, r);
                                                                 8    mx := new Mux[#W]<G>(op_s, add_s.out, mul_s.out);
face also captures the module’s timing behavior with time-
                                                                 9    out = mx.out; // produce output
line types. The module defines an event 𝐺, provided by the in- 10 }
put valid signal val_i, to represent when the module starts
executing. The delay of the event (G:1) captures the number                      (a) Initial, erroneous implementation.
of cycles needed between consecutive inputs; since the de-       1 comp FPU[#W]<G:1>(..) -> (o: [G+#L, G+#L+1] #W) with {
lay is one, the module can accept inputs every cycle. Avail-     2    some #L; } { .. // Instantiate and use modules.
                                                                 3    let #Max = Max[Add::#L, Mul::#L]::Out;
ability intervals impose constraints on inputs; the input l
                                                                 4    sa := new Shift[#W, #Add_B]<'G+Add::#L>(add.out);
and r are required in the first clock cycle represented by the   5    sm := new Shift[#W, #Mul_B]<'G+Mul::#L>(mul.out);
interval [𝐺, 𝐺 + 1).                                             6    so := new Shift[#1, #Max]<'G>(op.out);
   The cycle in which the output is produced, i.e., the latency  7    mx := new Mux[#W]<G+#Max>(so.out, sa.out, sm.out);
of the module, is determined by FloPoCo. Lilac introduces        8    #L := #Max;     // Latency of this module
                                                                 9 }
the output parameter #L to represent the latency, with a
where clause to constrain it to be at least one. Unlike the in-
                                                                            (b) Parameterized code to balance the pipeline.
put parameter #W, the output parameter is produced by the
FPAdd and can be accessed by the parent module. This pa-
                                                                   Figure 5. Latency-abstract FPU implementation in Lilac.
rameter is abstract during design-time, i.e., a parent module      The erroneous implementation is rejected by Lilac’s type
using the adder cannot assume anything about its value, ex-        system with a compile-time error. The corrected implemen-
cept that it is at least one. During compilation (§5), Lilac’s     tation balances the pipeline and itself provides a latency-
compiler executes FloPoCo to generate an adder implemen-           abstract interface.
tation and substitute a concrete value for #L.


3.2 Programming with Latency-Abstract Interfaces
   Section 2.3 described the challenges of correctly integrat-   is invoked on the first cycle, attempts to read its value in
ing modules whose parameters influence timing behaviors.         the first cycle. To fix the error, the user must invoke the
Using LA interfaces for the adder and the multiplier, we im-     multiplexer at the correct time. However, there is no con-
plement an FPU (Figure 5a) and introduce Lilac program-          crete value of time at which the computation will occur; it
ming constructs. However, this implementation is erroneous:      depends upon the latency of the adder that FloPoCo gener-
it does not balance the pipeline when the latency of the         ates during elaboration. To correctly schedule the computa-
adder and the multiplier are different and will only work        tion, the user must use the output parameter representing
when their latencies match. We demonstrate how Lilac’s           the adder’s latency:
type-system, which builds upon timeline types [28], reasons       mux := new Mux[#W]<G+Add::#L>(op, add.out, mul.out);
about output parameters and ensures that the design bug is
                                                                 The invocation is now scheduled using the output parameter
caught at compile-time.
                                                                 Add::#L: the compiler will select the exact cycle in which
   The implementation instantiates the adder and multiplier
                                                                 to execute the multiplexer after elaboration. Unfortunately,
modules (line 5) with the input parameter #W, which works
                                                                 this fix is insufficient:
like parameters in traditional HDLs. On line 7, it invokes
the modules with the inputs when the event G occurs. In-          mux := new Mux[#W]<G+Add::#L>(op, add.out, mul.out);
                                                                        Signal available in [G+Mul::#L, G+Mul::#L+1]
vocations (from Filament [28]) associate a particular use of            but required in [G+Add::#L, G+Add::#L+1]
an instance with when an event occurs and enable Lilac’s
type-system to reason about the clock cycle in which a com-      While the adder’s output is available in cycle Add::#L, the
putation occurs. When compiling this design to RTL, Lilac        multiplier’s output is not; it is available in cycle Mul::#L
generates the following error:                                   and Lilac cannot show that these are the same. A correct im-
                                                                 plementation has to balance the pipeline using the latency
 mux := new Mux[#W]<G>(op, add.out, mul.out);
       Signal available in [G+Add::#L, G+Add::#L+1]              of both the adder and the multiplier. Unlike in a traditional
       but required in [G, G+1]                                  HDL, this is a compile-time error rather than a silent error.
The error states that the output port of the adder will have a
valid value on cycle Add::#L while the multiplexer, which        3.3   Parameterization in Lilac
                                                                                                                    Rachit Nigam et al.


1 comp Shift[#N]<G:1>(
                                                     [G,G+1]           parameter Out which is constrained to the expression #A >
2    input: [G, G+1] 32) -> (                                           #B ? #A : #B; it is only used as a function over parame-
3    out: [G+#N, G+#N+1] 32) where #N > 0 {
                                                     [G+1,G+2]         ters. The combination of input and output parameters allows
4    bundle<#i> w[N+1]: [G+#i, G+#i w+1] 32;                           Lilac programs to use components as pure functions over
5    w[0] = input; out = w[#N];
6    for #k in 0..#N {
                                                                       parameters and abstract away routine computations. Next,
                                                     [G+#i,G+#i+1]
7      R := new Reg[32]<G+#k>(w[#k]);                                  we compute the amount of balancing needed (#Add_B and #
8      w[#k+1] = r.out;                                                Mul_B, elided), and use that to delay each compute module’s
9 }}                                                 [G+#N,G+#N+1]     outputs as well as the op signal. Finally, because the latency
                                                                       of the FPU depends on the latency of the compute module,
        (a) Shift register implementation.          (b) Timing.        we abstract its latency using #L and provide a binding in the
                                                                       body.
 Figure 6. A shift register implementation in Lilac and a
 block diagram visualizing the timing of each bundle wire.
                                                                       4   Type System
                                                                       Lilac’s type system analyzes each component in the pro-
   To fix the issue, we need a parameterized shift register to         gram to ensure that it matches its provided signature and
 delay the signal. Figure 6 implements a shift register in Lilac       uses its submodules correctly (based on their signatures). To
 and introduces its metaprogramming capabilities.                      enforce that each component can be safely pipelined, Lilac
                                                                       uses an SMT solver to enforce that its parameterized expres-
    Parametric signatures. Lines 1–3 in Figure 6a define               sions obey the requirements of timeline types [28].
 the signature of the Shift module. In this case, the designer,
 not an external tool, controls the latency by defining an in-         4.1 Syntax
 put parameter #N and setting the output to appear in the 𝑁th          Figure 7 lists the syntax of the Lilac HDL, which builds upon
 cycle. This means that, for example, when using the output            Filament’s basic structural constructs and timeline type sys-
 from a Shift[4], we schedule the downstream computa-                  tem [28]. Filament does not support parameters. It has three
 tion with G+4 instead of using an output parameter.                   main constructs: instantiations (to instantiate submodules),
    Bundles and loops. A Lilac program might use parame-               invocations (to schedule computations), and connections (to
 ters to instantiate and schedule modules. For example, Shift          connect ports). Lilac adds parameterization and metapro-
  instantiates 𝑁 Reg modules and forwards the output from              gramming constructs.
 register 𝑖 to register 𝑖 + 1. The availability interval for the          Parameter expressions. Lilac adds parameter expressions
 𝑖th register depends upon how many registers come before              (P) and allows them to occur in every location where con-
 it (Figure 6b). Lilac introduces bundles, a multidimensional          stants are allowed in Filament (availability intervals, event
 array where the availability of a value at a particular index         delays, and scheduling expressions, etc.). Parameters are de-
 depends on the index itself. For example, the bundle on line 4        fined at three locations: (1) signatures (𝑠𝑖𝑔) declare input
 has 𝑁+1 elements and states that the 𝑖th element’s availabil-         parameters, (2) some bindings (𝑏𝑖𝑛𝑑) define output param-
 ity interval is [𝐺 + 𝑖, 𝐺 + 𝑖 + 1). The input signal is assigned to   eters, and (3) let bindings (𝑐𝑚𝑑) name parameter expres-
 w[0] and the output signal is read from w[N] (line 5) which
                                                                       sions. The built-in arithmetic operators (𝑏𝑖𝑛𝑜𝑝) are directly
 have availabilities [𝐺, 𝐺 + 1), and [𝐺 + 𝑁, 𝐺 + 𝑁 + 1) respec-        encoded into SMT queries, allowing the solver to reason
 tively. Finally, we use a compile-time for loop (lines 6–9) to        about computations.
 instantiate registers and invoke them on the 𝑘th cycle, using            More complex computations can be specified in two ways.
 the value from the 𝑘th index in the bundle as the input, and          Parameter access expressions allow instantiation and use of
 assigning the output of the register to the index 𝑘 + 1.              components to get their output parameters; for example, Max
    Like loops, bundles are a compile-time construct: after            [#A, #B]::#Out instantiates the Max component and gets
 elaboration (§5), our shift register will look like the follow-       the output parameter representing the max of 𝐴 and 𝐵. Lilac
 ing code where the bundle has been completely eliminated              also declares common operations such as 𝑙𝑜𝑔2 and 𝑒𝑥𝑝2 as un-
 and the implementation simply forwards values through a               interpreted functions within its encoding (§4.2) and provides
 chain of registers:                                                   common equalities such as 𝑒𝑥𝑝2 (𝑙𝑜𝑔2 (𝑁)) = 𝑁; the solver
  R1 := new Reg; r0 := R0<G>(input);                                   relies on these equality to prove fact and requires the user
  R1 := new Reg; r1 := R1<G+1>(r0.out); ...
                                                                       to provide addition facts using assume statements if more
                                                                       complex reasoning is required.
    Putting it together. The shift register lets us balance our
 FPU’s pipeline (Figure 5b). First, we compute the maximum                Component signatures. Lilac signatures (sig) specify the
 of the two latencies using parameter access. The Lilac com-           events, parameters, and ports associated with a module. A
 ponent Max has an empty body and defines a single output              signature declares output parameters using with bindings
  𝑥    ∈ 𝑣𝑎𝑟𝑖𝑎𝑏𝑙𝑒𝑠   𝑝 ∈ 𝑝𝑎𝑟𝑎𝑚𝑒𝑡𝑒𝑟𝑠    𝐺 ∈ 𝑒𝑣𝑒𝑛𝑡𝑠               ⟦𝑃⟧ ∶ 𝒬𝑣 × 2𝐶       ⟦𝑐𝑚𝑑, 2𝐶 ⟧ ∶ 𝒟 × 2𝐶 × 𝒬 pfunc(𝑥𝑐 , 𝑥𝑜 ) ∶ 𝒬𝑣
  𝑏𝑜𝑝 ∈ {+, −, ×, ÷, %}    𝑢𝑛𝑜𝑝 ∈ {log2 , exp2 }                clauses(𝑥1 , 𝑥2 ) ∶ 𝐶 pargs(𝑥, P*) ∶ 𝐶 defs(𝑥) ∶ 𝒟 × 𝒟
  P    ∶∶= ℕ ∣ 𝑝 ∣ 𝑏𝑜𝑝(𝑃1 , 𝑃2 ) ∣ 𝑢𝑛𝑜𝑝(𝑃) ∣ 𝑥1 [P*]::𝑥2                                     𝑣1 , 𝐶1 = ⟦𝑃1 ⟧ 𝑣2 , 𝐶2 = ⟦𝑃2 ⟧
  C    ∶∶= 𝑃1 = 𝑃2 ∣ 𝑃1 ≤ 𝑃2 ∣ ¬𝐶 ∣ 𝐶 ∧ 𝐶 ∣ 𝐶 ∨ 𝐶               ⟦𝑛⟧ = 𝑛, ∅    ⟦𝑝⟧ = 𝑝, ∅ ⟦(𝑏𝑜𝑝(𝑃1 , 𝑃2 )⟧ = bop 𝑣1 𝑣2 , 𝐶1 ∪ 𝐶2
  ival ∶∶= [𝐺1 + 𝑃1 , 𝐺2 + 𝑃2 ]    port ∶∶= 𝑥 ∶ 𝑖𝑣𝑎𝑙 𝑝
                                                                𝐶1 = clauses(𝑥1 , 𝑥𝑜 ) 𝐶2 = pargs(𝑥1 , P*) 𝑓 = pfunc(𝑥1 , 𝑥𝑜 )
  bind ∶∶= some 𝑝 where c*                                                                                                     Acc
                                                                                   ⟦𝑥1 [P*]::𝑥𝑜 ⟧ = 𝑓, 𝐶1 ∪ 𝐶2
  sig ∶∶= 𝑥[P*]⟨𝐺 ∶ 𝑃⟩(port*) with {bind*} where C*
  mod ∶∶= comp sig {cmd*} ∣ extern sig ∣ gen tool sig                𝑑𝑖 , 𝑑𝑜 = defs(𝑥2 )      𝐶𝑜 = ⋃𝑥𝑜 ∈𝑑𝑜 clauses(𝑥2 , 𝑥𝑜 )
  acc ∶∶= 𝑥 ∣ 𝑥1 .𝑥2 ∣ acc[𝑃1 ..𝑃2 ]                                 𝐶𝑝 = pargs(𝑥2 , P*)
                                                                                                                                   Inst
  cmd ∶∶= 𝑐𝑚𝑑1 ; 𝑐𝑚𝑑2 ∣ acc1 = acc2 ∣ let 𝑝 = 𝑃 ∣ 𝑝 ∶= 𝑃        ⟦𝑥1 ∶= new 𝑥2 [P*], 𝑝𝑐⟧ = 𝑑𝑜 , 𝑝𝑐 ∪ 𝐶𝑜 , (assert 𝑝𝑐 ⇒ 𝐶𝑝 )
       ∣ 𝑥1 ∶= new 𝑥2 [P*] ∣ 𝑥1 ∶= 𝑥2 ⟨𝐺 + 𝑃⟩(acc*)             𝑑𝑡 , 𝑝𝑐𝑡 , 𝑞𝑡 = ⟦𝑐𝑚𝑑1 , 𝑝𝑐 ∧ 𝑐⟧ 𝑑𝑓 , 𝑝𝑐𝑓 , 𝑞𝑓 = ⟦𝑐𝑚𝑑2 , 𝑝𝑐 ∧ ¬𝑐⟧
       ∣ bundle⟨p*⟩ 𝑥[P*] ∶ ival* ∣ assume 𝐶 ∣ assert 𝐶                                                                            Cond
                                                                   ⟦if(𝑐) 𝑐𝑚𝑑1 else 𝑐𝑚𝑑2 , 𝑝𝑐⟧ = ∅, 𝑝𝑐, (assert 𝑞𝑡 ∧ 𝑞𝑓 )
       ∣ if 𝐶 {cmd1 } else {cmd2 } ∣ for 𝑝 in 𝑃1 ..𝑃2 {cmd }
                                                                      (b) Selected encoding rules for generating SMT constraints.
  (a) Language constructs. Filament constructs highlighted.

                           Figure 7. Lilac language and rule for encoding constructs into SMT queries

and can specify constraints on them using where clauses.              them.2 Lilac enforces them by generating constraints that
Finally, signatures can point to either Lilac modules (comp),         ensure:
modules implemented in Verilog (extern), or modules gen-                  1. Valid reads: Components only read from ports during
erated by a tool (gen). Lilac’s compiler uses the last kind                  their availability intervals, i.e., when their values are
of declaration to automatically invoke the generator while                   semantically valid.
elaborating the program (§5).                                             2. Non-conflicting writes: There is only one logical driver
                                                                             of a port per clock cycle.
   Metaprogramming constructs. Lilac supports bounded                     3. Appropriate delays: Components respect the initiation
for-loops, conditional expression, and recursive module in-                  intervals of their subcomponents: if d is the subcom-
stantiation with the standard semantics. Bundles provide                     ponent’s delay, then the component waits at least d
a way to track availability intervals for generated signals                  cycles before invoking the subcomponent again.
and assert and assume allow for compile-time reasoning                   For each property, Lilac’s type system generates a set of
of facts.                                                             constraints over symbolic parameters and ensures that, no
                                                                      matter what parameterization of a design is selected during
4.2 Constraint Generation                                             elaboration, it will not have such silent logical bugs.
Within a single component, there are two sources structural              Encoding function. The encode function (Figure 7b) an-
hazards: reading values when they are not semantically mean-          alyzes parameterized programs and generates constraints
ingful, and mapping multiple logical computations to the              for the SMT solver. The encode function transforms each
same physical resource in the same clock cycle. These are             language construct in Lilac into a set of SMT constraints.
logical bugs: within the design, they appear as mistakes within                             ⟦𝑐𝑚𝑑, 2𝐶 ⟧ ∶ 𝒟 × 2𝐶 × 𝒬
the control logic of a circuit which might attempt to use
a value on a wire before the correct amount of time has               It takes the current command (cmd) and a path condition
elasped, or assume that a module has accepted a input be-             (2𝐶 ), which represents the set of facts true in the current
fore it really does. However, such mistakes do not cause an           branch of the parametric program, and returns a set of SMT
obvious run time failure; the design will keep executing with         variables to define (𝒟), a new path condition, and the SMT
incorrect values causing silent data corruption.                      query (𝒬). The rules helper functions to access the parame-
   Filament [28] defines two properties to eliminate struc-           ters defined by components (defs) and constraints on them
tural hazards: latency safety requires that values on wires           (clauses), as well as constraints created by substituting con-
are only read when they are semantically meaningful, and              crete expression for input parameters (pargs).
resource safety requires that, when using a partially-pipelined       2
                                                                        Filament calls these properties well formedness and pipeline safety
module, inputs are sent with appropriate delays between               respectively.
                                                                                                              Rachit Nigam et al.


   Parameter expressions. Parameter expressions (⟦𝑃⟧) are             Design                         Lines   Time (ms)
encoded to generate SMT values (𝒬𝑣 )—which correspond to
computations over SMT variables—and a set of constraints.             RISC 3-stage Base              480          160
Encoding numbers and parameter variables returns their SMT            Gaussian Blur Pyramid (§7)     595          205
representation; binary operations return the equivalent SMT           FFT (Lilac only)               1207         403
function, and unary functions return the application of un-           FFT (using FloPoCo)            1221         442
interpreted functions to parameter expressions.                       Lilac’s standard library       1310         900
                                                                      BLAS Level 1 Kernels           1346        1295

   Encoding output parameters. For each output parame-
                                                                           Figure 8. Type checker’s performance.
ter defined by a component, Lilac declares a new uninter-
preted function that depends on all the input parameters
of the component. For example, given the following compo-           Top-down elaboration. In HDLs with only input param-
nent:                                                            eters, the compiler can process modules top-down, completely
comp Max[#A, #B]<G:1>(..) with { some #O }                       processing a parent module before elaborating its submod-
                                                                 ules. This is possible because all the parameters needed to
Lilac defines the following uninterpreted function:              elaborate a module are already available and fully concrete.
(declare-fun Max_O ((A Int) (B Int)) Int)                        Such a scheme does not work for Lilac programs. To under-
Whenever the specific output parameter is used in the pro-       stand why, consider the following fragment from Figure 5b:
gram, Lilac will replace it with (Max_O A B); the Acc rule       A := new FAdd[#W]; M := new FMul[#W]; // FloPoCo outputs
shows an example of this. This encoding allows Lilac pro-        let #Max = Max[A::#L, M::#L]::#Out;
                                                                 SA := new Shift[#W, #Max-A::#L];
grams to reason these specific examples:
FAdd[16,8]::#L == FAdd[16,8]::#L // True                         In order to instantiate the Shift module, the elaborator needs
Max[#A,#B]::#O == Max[#X,#Y]::#O // if #A==#X and #B==#Y         concrete values for the output parameters A::#L and M::#
  The instantiation rule (Inst) requires that the input ex-      L which will not be available until FloPoCo generates the
pressions obey the constraints on input parameters, declares     adder and multiplier modules. Furthermore, output parame-
output parameters defined in the signature as new variables,     ters can be used to control loops and conditionals and recur-
and allows the path condition to assume the where-clause         sive module instantiation allows Lilac to define components
assertions on them.                                              with no defined order of elaboration.
                                                                    Elaboration algorithm. The algorithm scans the mod-
   Compile-time language constructs. Lilac symbolically          ule for instantiations that only use parameter expressions
reasons about compile-time constructs such as conditionals.      that can be fully evaluated; if no such instantiation exists,
The Cond shows how the encoding function adds the if con-        then there is a cycle in the instantiation graph and the algo-
dition to the path condition when checking branches. Rules       rithm terminates with an error. The algorithm drills down
for port connections and for loops (not shown) ensure that       the module hierarchy until it finds a completely elaboratable
when a signal is read, it is available according to its avail-   module which only contains references to unparameterized
ability interval, and that instances are not reused more often   Lilac modules, external modules, and generator instantiated
than the component’s delay allows for.                           gen modules. Each generator provides a configuration file
   After constructing a constraint, the type system asserts      that defines the modules it produces and the mechanism
its negation and asks and SMT solver [26] to find a satisfy-     to extract bindings for output parameters for each module
ing assignment. If the solver finds such an assignment, this     (reading the command-line output, looking for a file, etc.).
means that the user design violates the constraint; we can       Using this information, the elaborator executes all loops and
use this assignment to construct a counterexample demon-         conditionals and instantiates gen modules by invoking the
strating to the user that a set of concrete parameters values    relevant tool. Once the generator produces a module, the
will create a bug in the design.                                 elaborator collects the bindings for the module’s output pa-
                                                                 rameters and continues elaborating the parent module. This
                                                                 continues until all the modules are completely elaborated
5   Elaboration                                                  and stitched back together. After this, the elaborator runs a
The compiler elaborates well-typed Lilac programs by exe-        second pass to inline all reads and writes from bundles to
cuting generators to get concrete bindings for output param-     get a fully structural Filament program.
eters, evaluating compile-time constructs, and inlining bun-
dles. The compiler then produces a valid Filament program        5.1 Compiler Performance
which can be compiled down to a Verilog implementation.          Figure 8 summarizes the performance of Lilac’s compiler.
The compiler is written in 16k lines of Rust code.               Compilation is dominated by the type checking step which
generates and discharges SMT queries. Most designs take             Generator         Features
less than a second to type-check.
                                                                    PipelineC [21]    in-dep
                                                                    FloPoCo [6]       in-dep, out-dep
6   Latency-Abstract Interfaces in the Wild                         XLS [10]          in-dep, ii-gt-1
We empirically justify our claim that LA interfaces can be          Spiral FFT [25]   in-dep, out-dep, ii-gt-1
used to capture interfaces for modules generated by existing        Aetherling [8]    in-dep, out-dep, ii-gt-1, multi
tools.                                                           Table 3. Generators integrated with Lilac and features
                                                                 needed to capture their interfaces. in-dep: input parame-
6.1 Vivado IP Core Generators                                    ters affect timing behaviors, out-dep: output parameters
Vivado’s IP core generators provide encrypted, opaque im-        affect timing behaviors, ii-gt-1: parameter-dependent
plementations optimized for specific AMD FPGAs [18]. To          pipelining, and multi: requires multi-cycle intervals.
study Lilac’s efficacy at capturing LA interfaces, we study
three IP core generators provided by Vivado.

  Multiplier. Like Shift (Figure 6), the multiplier core gen-
erator takes an explicit input parameter to specify the out-     and sets the appropriate output parameter values. In addi-
put latency. Lilac’s LA interfaces capture this well:            tion to demonstrating that LA interfaces can provide a uni-
comp Mult<G:1>[#W, #L](a: [G, G+1] #W,                           form interface for the different divider implementations, this
  b: [G, G+1] #W) -> (o: [G+#L, G+#L+1] #W)                      wrapper also encapsulates the documentation’s guidance.

   Divider. The divider generator [16] provides several pa-        FFT generator. Vivado’s FFT generator [15], similar to
rameters to control the generated modules’ timing behavior       High-radix, defines a table that uses the FPGA target and
(Figure 9). It allows the user to select between microarchi-     input parameter values to determine the module’s latency
tectures based on precision and performance requirements:        and provides an option to generate an AXI wrapper. The
   LutMult (Figure 9a) is recommended for bitwidths less         LA interface to wrap this generator would look similar to
than 12, is fully pipelined, and has an eight-cycle latency.     Figure 9c.
Radix-2 (Figure 9b), recommended for bitwidths less than
16, has an input parameter (#II) which controls the pipelin-     6.2 LA Interfaces for Generators
ing for the module and can be set to values greater than         Table 3 summarizes existing generators and the Lilac fea-
one to reduce resource usage. Based on other input param-        tures needed to capture their interfaces.
eters, such as whether a fractional vs. integer remainder is
required, it selects a particular formula to compute the mod-        • Input parameter dependent timing (in-dep). Sim-
ule’s latency (captured with output parameter #L). Since the           ilar to Vivado’s multiplier core generator, PipelineC
signature specifies the exact formula used, the parent mod-            allows user to specify the exact latency they want us-
ule can reason about the implementation’s concrete latency.            ing an input parameter.
Finally, High-radix (Figure 9c) should be used for bitwidths         • Output parameter dependent timing (out-dep).
greater than 16. The user guide provides a table that gives            FloPoCo uses both input parameters and high-level
the implementation’s exact latency based on the bitwidths              optimization goals to decide the timing behaviors of
of the divisor and the dividend; there is no closed form for-          a module. Similar to the High-Radix divider, such
mula to compute it. The LA interface simply abstracts the              modules need Lilac’s output parameters to capture
latency using an output parameter.                                     their design-time abstract timing behaviors.
   The complexity of the divider implementation makes it             • Parameter dependent pipelining (ii-gt-1). XLS
challenging to integrate into existing designs. Older versions         can generate modules that are partially pipelined, i.e.,
[14] only provided LS interfaces for integration but more re-          have an initiation interval of greater than one. The
cent versions [16] have added the ability to wrap the gen-             pipelining can depend on input parameters (similar to
erated modules in an LI AXI interface. This is a routine ex-           Radix-2) or be completely abstract, requiring output
ample of the convenience use of LI interfaces: the dividers’           parameters and Lilac’s capability to capture parameter-
timing behavior is extremely complicated to reason about               dependent event delays.
and therefore uses an LI interface instead.                          • Multi-cycle interval (multi). Aetherling generates
   With Lilac, programmers avoid this unnecessary overhead             modules where the user must hold the input signal
by using an LA interface. Figure 9d demonstrates this ap-              stable for more than one cycle. Capturing such inter-
proach: it provides a wrapper module that instantiates dif-            faces requires Lilac’s ability to describe multi-cycle
ferent divider implementations based on the input bitwidth             availability intervals.
                                                                                                                       Rachit Nigam et al.


comp LutMult<G:1>[#W](     comp Rad2<G:II>[#W,#II,#Fr](            comp HighRad<G:#L>[#W](     if #W<12 { D := new LutMult[..];
  n: [G, G+1] #W,            n: [G, G+1] #W, d: [G, G+1] #W          n: [G, G+1] #W,             #L := 8; #II := 1 }
  d: [G, G+1] #W           ) -> (q: [G+#L, G+#L+1] #W) with {        d: [G, G+1] #W            else if #W<16 { D := new Rad2[..];
) -> (                       some #L } where #II < 9, #II%2,       ) -> (                        #L := D::L; #II := D::II; }
  q: [G+8, G+9] #W           #Fr & #II > 1 ? L == #W+5               q: [G+L, G+L+1] #W        else { D := new HighRad[..];
)                          : #Fr & #II == 1 ? L == #W+4 ..         ) with { some #L; }           #L := D::L; #II := D::L; }}


(a) LutMult architecture, (b) Radix-2 architecture, input parame- (c) High-radix architec- (d) Selecting dividers based on input
latency-sensitive.        ter dependent timing.                   ture, latency-abstract.  bitwidth.

Figure 9. Interfaces for dividers produced by Vivado’s IP Core. The wrapper code provides a uniform selects the implemen-
tation based on input bitwidth and provides a stable interface to integrate any divider.

   Lilac allows integration of external Verilog modules by            comp AethConv[#W]<G:#II>( // 4x4 conv, gauss filter
                                                                        valid_i: interface['G],
defining external components which provide a Lilac signa-
                                                                        in[#N]: ['G, 'G+#H] #W // Num. inputs & hold time
ture as well as a path to the Verilog file that contains the im-      ) -> (out[#N]: ['G+#L, 'G+#L+1]) with {
plementation. During compilation, the Lilac compiler links-             some #H where #H > 0;   // Num. cycles to hold inputs
in the implementation of the module with the rest of the                some #N where 16 % #N == 0, #N > 0; // Chunk size
design.                                                                 some #L, #II where #L > 0, #II >= #H }

                                                                           (a) Latency-abstract interface for convolution in Lilac.
7   Gaussian Blur Pyramid
Gaussian blur pyramid is multi-scale image processing al-             module AethConv #(parameter W=32, N=16, L=6, II=1, H=1)(
gorithm commonly used in tasks such as object detection.               input logic val_i, input logic rdy_i,    // input intf
                                                                       input logic [N-1:0][W-1:0] in,
We use GBP to compare LA and LI interfaces. We imple-                  output logic val_o, output logic rdy_o, // output intf
ment two versions that use convolution modules generated               output logic [N-1:0][W-1:0] out);
by Aetherling [8]: one uses Lilac and LA interfaces; the sec-
ond uses Verilog and LI interfaces. We qualitatively evalu-             (b) Latency-insensitive interface for convolution in Verilog.
ate both interfaces for their ability to effectively encapsulate
the generated modules and report on the resource and per-             Figure 10. Interfaces for the Aetherling-generated 4 × 4 con-
formance overhead incurred by LI implementation.                      volution module. The parameter 𝑁 is used by the implemen-
                                                                      tation to describe how many of the 16 required inputs it will
7.1 Implementation                                                    accept in a cycle and partially streams some outputs.
The algorithm proceeds by repeatedly applying Gaussian
blur by convolving the image with a filter, downsampling              choice. One the other hand, the LI interface defines an in-
the resulting image, and repeating this to construct differ-          put parameter that must be correctly threaded through the
ent levels. Once all levels are created, the algorithm upsam-         hierarchy; if the user gets it wrong, the implementation may
ples the images, blurs them to remove aliasing artifacts, and         silently ignore the inputs.
blends them between level 𝑁−1 and 𝑁 by taking a weighted
average between the two, continuing until it has recovered               Timing behaviors. The LA interface defines three out-
the original image dimensions. Our GBP implementations               put parameters to capture the module’s timing behavior. The
unroll this dataflow graph and instantiate modules to per-           latency (#L) and initiation interval (#II) have correspond-
form the blurring, blending, and up- and downsampling.               ing signals in the LI interface, namely an output valid signal
                                                                     (val_o) and a ready-in (rdy_i) signal. Aetherling’s partially-
   Convolution interface. We use Aetherling [8], a DSL for           pipelined modules use multipliers that perform computa-
generating stream processing programs, to generate 4 × 4             tions over multiple cycles and require the module to hold
convolution implementations and vary the number of mul-              the input signal stable for multiple cycles. The third param-
tipliers used to express area–performance trade-offs. We de-         eter, #H, captures this. The LI interface lacks an explicit sig-
sign a Lilac-based LA interface (Figure 10a) and a Verilog-          nal to capture this information: it assumes that data transfer
based LI interface (Figure 10b) and analyze how they cap-            occurs when ready and valid are asserted together. Instead,
ture the timing behavior of the generated modules.                   we plumb the #H parameter through the hierarchy and use
                                                                     it to latch the input value for the required number of cycles.
  Number of inputs. First, upon varying the number of
multipliers, Aetherling changes the size of the input port to            Parameter pollution. The LI design suffers from param-
accept 𝑁 elements at a time, where 𝑁 is a factor of 16. Lilac         eter pollution: the top-level GBP module, which uses three
defines an output parameter to express this tool-dependent            separate blur modules, must pass four parameters to each,
1 comp Ser[#W, #N, #B, #C, #H]<G: #C*(#N-1)+#H>(
                                                                   Design (N)           LUTs        Registers    Freq. (MHz)
2   en: interface[G], in[N*B]: [G, G+#H] W
3 ) -> (o[#N][#B]: [G+#C*#i, G+#C*#i+#H] W) {                      Lilac / RV (1)    1824 / 2093   2532 / 3254     258 / 236
4   for #i in 0..#N { for #j in 0..#B {
                                                                   Lilac / RV (2)    1762 / 2062   2464 / 3165     284 / 219
5     let #CurIdx = #B*#i+#j;
6       d := new Reg[#W]<G, G+C*#i+#H>(in{#CurIdx});
                                                                   Lilac / RV (4)    1627 / 1983   2373 / 3129     270 / 306
7       o{i}{j} = d.out; }}}                                       Lilac / RV (8)    1227 / 2146   1733 / 3058     223 / 231
                                                                   Lilac / RV (16)   1311 / 2099   1688 / 3244     211 / 183
  Figure 11. Parameterized, pipelined serializer in Lilac. The
  interface specifies how to take 𝑁 × 𝐵 elements of an array      Figure 13. Resource usage and maximum frequency of GBP
  and chunk them into a stream of 𝑁 element bundles and           implementations for different convolutions configurations.
  can control gap between consecutive bundles (𝐶) and the
  number of cycles a bundle is available (𝐻).
                                                                    Pyramid implementation. For the LA implementation,
 1 case (st) // send state machine                                the various uses of the Blur module require pipeline balanc-
 2   IDLE: if (val_i) nxt_st = PROC;                              ing. Lilac’s type system ensures that we correctly delay and
 3   PROC: cv_val_i = 1; if (cv_rdy_i) nxt_idx = idx+1;           use the signals when passing them to subsequent stages:
 4   BLOCKED: if (rdy_o) nxt_st = IDLE;
                                                                  Blur0 := new BlurAbs[8]; Blur0 := new Blur[4];
 5 assign conv_in = in[N*idx+:N];
                                                                  level0 := Blur0<'G>(input);
 6 case (st) // recv state machine
                                                                  down := new Down<G+Blur0::#L>(level0.out);
 7   IDLE: if (val_i) nxt_st = PROC;
                                                                  level1 := Blur1<'G+#Blur0::#L>(down.out); ...
 8   PROC: cv_rdy_o = 1; if (cv_val_o) nxt_idx = idx+1;
                                                                  #L := Blur0::#L + Blur1::#L + BlurUp::#L + Blend::#L;
 9   BLOCKED: if (rdy_o) nxt_st = IDLE;
                                                                  // II is dictated by slowest blur.
10 assign out[N*idx+:N] = conv_out;
                                                                  #II := Max[Blur0::#II, Blur1::#II, BlurUp::#II]::#Out;

                                                                  The LI implementation uses a serial state machine to execute
  Figure 12. State machines for pipelined execution of
                                                                  each blur module through a ready–valid interface and itself
  Aetherling-generated convolution in the LI implementation.
                                                                  provides a ready–valid interface.

                                                                  7.2 Cost of Latency Insensitivity
  resulting in 12 extra parameters flowing through the design.
  For larger GBPs, this quickly becomes infeasible.               Figure 13 summarizes our results: in general, LI implemen-
                                                                  tations of the GBP take more resources to achieve the same
     LA blur implementation. Different levels of the GBP al-      maximum frequencies. We implement five design points for
  gorithm apply blurs of different sizes. For both implementa-    both the Lilac and the latency-insensitive interface by syn-
  tions, we provide a parameterized blur module that takes        thesizing Aetherling modules with different values for pa-
  an image of size 𝐼 × 𝐼 and repeatedly calls the Aetherling      rameter 𝑁, which affects the latency and the throughput of
  generated convolution module to process it. For the LA im-      the design. Each design is synthesized using Vivado v2023.2
  plementation, this is accomplished using a serializer module    with a target clock period of 3ns and input-output delays of
  (Figure 11) which takes 𝐼2 elements from the image and pro-     0.1ns. The reported frequency is computed by subtracting
  vides 𝑁 inputs to the convolution module (where 𝑁 is the        the worst negative slack from the target and we take geo-
  output parameter provided by the Aetherling module). The        metric means when reporting summary statistics.
  serializer interface (lines 1–3) provides control over when
  bundles are produced, if there are gaps between them, and          Performance analysis. On average, the LI designs achieve
  how long they are valid for. However, the module imple-         6.8% worse frequencies, and use 26.2% more LUTs and 33.0%
  mentation (lines 4–7) simply instantiates a register for each   more registers compared to the Lilac implementation. As ex-
  element and forwards its output; Lilac’s type system ensures    pected, the resource usage of the LI designs remains roughly
  that the complex reasoning about parameter-dependent bun-       constant across the various design points. However, for the
  dle availability is correctly handled.                          LA implementations, the resource usage goes down as we
                                                                  increase the value of 𝑁: Lilac-1 uses 22.2% fewer registers
     LI blur implementation. The LI blur implementation uses      than RV-1 while Lilac-16 uses 48% fewer registers. This is
  two state machines (Figure 12) to enable pipelined execution    because the serialization logic is a primary cost of the LA im-
  of the convolution module. The send state machine (lines        plementation and, as the convolution module provides more
  1–5) repeatedly extracts and sends an 𝑁-sized chunk of data     parallelism, it needs less serialization logic.
  to the Aetherling module; the cv_rdy_i indicates when the          The trend illustrates an important trade-off: LI interfaces
  convolution is ready to accept the inputs. The recv state       requires a large upfront resource cost to implement the hand-
  machine (lines 6–10) waits for the output (cv_val_o) and        shaking FSM; however, this cost remains constant regard-
  stores it in the right location in the output image.            less of the internal logic of the module. LA interface, on the
                                                                                                                        Rachit Nigam et al.


other hand, impose a parameter-dependent cost because of                  availability intervals and adds support for LI interfaces us-
their reliance on compile-time code-generation. This points               ing synchronization signals. However, Anvil does not sup-
of a design paradigm where designers connect smaller com-                 port explicit resource reuse, does not address the interac-
ponents using LA interfaces and, once they have a big enough              tion between compile-time parameters and interface behav-
“island” of LA modules, wrap them using an LI interface.3                 ior, and does not aim to express LA interfaces. Lilac is the
                                                                          first language to statically provide both latency safety and
   Design analysis. For the LI designs, the state machine                 resource safety for parameterized designs directly support
logic within the blur modules is the critical path; for the               LA interfaces.
Lilac implementation, it is the serializer implementation due
to high fanout. The resource overheads in the LI modules                    Pre-elaboration parameter checking. Bluespec [29] rea-
comes from the extra state machines and are amortized as                  sons about parameter values at compile-time using provisos,
the size of the module increases implying that designers                  which are similar to Lilac’s where clauses. However, Blue-
should prefer using LA interfaces to build large components               spec does not capture modules’ timing behaviors and thus
which can be wrapped using LI interfaces.                                 cannot reason about the impact parameters have on them.

8    Related Work                                                            Verification systems. Formal verification tools for hard-
   Parameters in HDLs. Traditional HDLs, like Verilog and                 ware design [4, 24, 34] can reason about general temporal
VHDL, support parameterizing designs using input param-                   properties but generally require whole program proofs and
eters and metaprogramming constructs. They also provide                   cannot directly handle parameterized designs. Lilac, in con-
mechanisms such as defparam to assign parameter values                    trast, uses a compositional type system to guarantee the ab-
anywhere in a design’s hierarchy; however, such constructs                sence structural hazards and resource-reuse violations and
break encapsulation and are discouraged from use due to                   reasons symbolically about parameterized designs.
poor tool support [17, 19]. Output parameters capture in-
verted parameter flow, provide strong encapsulation, and                     Hardware generators. Core generators [6, 18, 25], high-
work with Lilac’s type system to ensure correct usage. Em-                level synthesis [30, 36], and accelerator design languages [8,
bedded HDLs [2, 3, 20, 23] do not use parameters in the tradi-            10, 11, 22, 27] operate with higher-level, computational ab-
tional sense: they rely on the host software language to con-             stractions and do not expose timing concerns to the user. In
struct circuits and combine them. Because of this, eHDLs                  contrast, Lilac is an HDL that directly expresses and reasons
can easily implement output parameters. However, unlike                   about timing behaviors of low-level circuit descriptions.
Lilac, they do not capture the influence of parameters on tim-
ing behaviors and therefore make LA interfaces challenging                9    Future Work
to use correctly.
                                                                          Latency-abstract (LA) interfaces are a design abstraction that
   Safe HDLs. Traditional HDLs guarantee datatype safety:                 provide the flexibility of latency-insensitive (LI) interfaces
they ensure that the bits on every wire correspond to se-                 as well as the efficiency of latency-sensitive interfaces. They
mantically meaningful data. Safe HDLs provide orthogonal                  do not replace LI interfaces: a module with input-dependent
guarantees about how circuit elements are used. Latency-                  timing behaviors, such as a variable-latency divider, must
counting HDLs [2, 13, 32, 33] track the latencies of signals              use an LI interface. Similarly, there are cases where even
and ensure that pipelines are correctly balanced (latency safety)         though LA interfaces could work, an LI interface might be
but cannot reason about partially-pipelined modules. Fila-                a better choice because of the design’s performance objec-
ment [28] allows designs to be partially pipelined and rea-               tives. These trade-offs imply the need for new HDLs that
sons about how resources such as wires, registers, and mod-               unify LI and LA abstractions and allow designers to seam-
ules are reused over time, either explicitly or through pipelin-          lessly move between the two which we see as a fruitful area
ing, and statically guarantees the absence of resource reuse              for future work.
bugs (resource safety). By enforcing both latency safety and
resource safety, Filament is the first language to eliminate all          10    Conclusion
structural hazards at compile time. Anvil [35] is a channel-
                                                                          A combination of generators, domain-specific languages, and
based HDL that adapts Filament’s abstractions of events and
                                                                          HDLs allows modern hardware designs to be malleable and
                                                                          adapt to changes in performance goals and implementation
                                                                          strategies. Latency-abstract interfaces, coupled with safe ab-
3
 This mirrors the concept of globally-asynchronous, locally-synchronous   stractions of Lilac, allow designers to build such malleable
design where large islands of synchronously clocked modules are con-      designs without having to pick between performance, com-
nected using asychronous clock-domain crossings.                          position, and correctness.
Acknowledgments                                                              [17] AMD Inc. 2021. Vivado ERROR: [Synth 8-27] Complex defparam not
                                                                                  supported. Retrieved August 20, 2025 from https://adaptivesupport.
We thank Christopher Batten for insightful discussions and                        amd.com/s/article/64023?language=en_US
early feedback as well as anonymous reviewers for helping                    [18] AMD Inc. 2022. Vivado design suite user guide: designing with
clarify the framing of LA interfaces as a design abstraction.                     IP. Retrieved August 20, 2025 from https://www.xilinx.com/support/
This material is based upon work supported by the NSF un-                         documents/sw_manuals/xilinx2022_2/ug896-vivado-ip.pdf
                                                                             [19] Doulos Inc. 1996. The Verilog Golden Reference manual. (1996).
der Awards No. F569084 and 2237379. The first author was
                                                                             [20] Jane Street. 2022. HardCaml: Register Transfer Level Hardware De-
partly supported by the Jane Street graduate research fel-                        sign in OCaml. Retrieved October 15, 2022 from https://github.com/
lowship.                                                                          janestreet/hardcaml
                                                                             [21] Julian Kemmerer. 2022. PipelineC. Retrieved October 15, 2022 from
References                                                                        https://github.com/JulianKemmerer/PipelineC
                                                                             [22] David Koeplinger, Matthew Feldman, Raghu Prabhakar, Yaqi Zhang,
 [1] AMD Inc. 2022. Xilinx LogiCORE IP Multiplier v11.2. Retrieved Octo-
                                                                                  Stefan Hadjis, Ruben Fiszel, Tian Zhao, Luigi Nardi, Ardavan Pedram,
     ber 27, 2022 from https://docs.xilinx.com/v/u/en-US/mult_gen_ds255
                                                                                  Christos Kozyrakis, and Kunle Olukotun. 2018. Spatial: A language
 [2] C. Baaij, M. Kooijman, J. Kuper, A. Boeijink, and M. Gerards. 2010.
                                                                                  and compiler for application accelerators. https://doi.org/10.1145/
     C𝜆aSH: Structural Descriptions of Synchronous Hardware Using
                                                                                  3192366.3192379
     Haskell. In Euromicro Conference on Digital System Design: Architec-
                                                                             [23] Derek Lockhart, Gary Zibrat, and Christopher Batten. 2014. PyMTL:
     tures, Methods and Tools. https://doi.org/10.1109/DSD.2010.21
                                                                                  A Unified Framework for Vertically Integrated Computer Architec-
 [3] Jonathan Bachrach, Huy Vo, Brian Richards, Yunsup Lee, Andrew
                                                                                  ture Research. https://doi.org/10.1109/MICRO.2014.50
     Waterman, Rimas Avižienis, John Wawrzynek, and Krste Asanović.
                                                                             [24] Cristian Mattarei, Makai Mann, Clark Barrett, Ross G. Daly, Dillon
     2012. Chisel: constructing hardware in a Scala embedded language.
                                                                                  Huff, and Pat Hanrahan. 2018. CoSA: Integrated Verification for Agile
     https://doi.org/10.1145/2228360.2228584
                                                                                  Hardware Design. https://doi.org/10.23919/FMCAD.2018.8603014
 [4] Cadence Inc. 2022. Jasper Gold FPV App. Retrieved October 15,
                                                                             [25] Peter Milder, Franz Franchetti, James C. Hoe, and Markus Püschel.
     2022 from https://www.cadence.com/en_US/home/tools/system-
                                                                                  2012. Computer Generation of Hardware for Linear Digital Signal
     design-and-verification/formal-and-static-verification/jasper-gold-
                                                                                  Processing Transforms. ACM Transactions on Design Automation of
     verification-platform/formal-property-verification-app.html
                                                                                  Electronic Systems (2012). https://doi.org/10.1145/2159542.2159547
 [5] Luca P Carloni, Kenneth L McMillan, and Alberto L Sangiovanni-
                                                                             [26] Leonardo de Moura and Nikolaj Bjørner. 2008. Z3: An efficient SMT
     Vincentelli. 2001. Theory of latency-insensitive design. (2001).
                                                                                  solver. In International conference on Tools and Algorithms for the Con-
 [6] Florent De Dinechin and Bogdan Pasca. 2011. Designing custom arith-
                                                                                  struction and Analysis of Systems.
     metic data paths with FloPoCo. IEEE Design & Test of Computers
                                                                             [27] Rachit Nigam, Sachille Atapattu, Samuel Thomas, Zhijing Li,
     (2011).
                                                                                  Theodore Bauer, Yuwei Ye, Apurva Koti, Adrian Sampson, and Zhiru
 [7] Diplomacy Authors. 2025. Diplomacy: a parameter negotiation frame-
                                                                                  Zhang. 2020. Predictable Accelerator Design with Time-Sensitive
     work for Chisel. Retrieved August 20, 2025 from https://github.com/
                                                                                  Affine Types. https://doi.org/10.1145/3385412.3385974
     chipsalliance/diplomacy
                                                                             [28] Rachit Nigam, Pedro Henrique Azevedo de Amorim, and Adrian
 [8] David Durst, Matthew Feldman, Dillon Huff, David Akeley, Ross Daly,
                                                                                  Sampson. 2023. Modular Hardware Design with Timeline Types.
     Gilbert Louis Bernstein, Marco Patrignani, Kayvon Fatahalian, and
                                                                                  https://doi.org/10.1145/3591234
     Pat Hanrahan. 2020. Type-Directed Scheduling of Streaming Accel-
                                                                             [29] Rishiyur Nikhil. 2004. Bluespec System Verilog: Efficient, correct
     erators. https://doi.org/10.1145/3385412.3385983
                                                                                  RTL from high level specifications. In Conference on Formal Meth-
 [9] Franz Franchetti, Tze Meng Low, Doru Thom Popovici, Richard M
                                                                                  ods and Models for Co-Design (MEMOCODE). https://doi.org/10.1109/
     Veras, Daniele G Spampinato, Jeremy R Johnson, Markus Püschel,
                                                                                  MEMCOD.2004.1459818
     James C Hoe, and José MF Moura. 2018. SPIRAL: Extreme perfor-
                                                                             [30] Christian Pilato and Fabrizio Ferrandi. 2013. Bambu: A modular
     mance portability. Proc. IEEE (2018).
                                                                                  framework for the high level synthesis of memory-intensive appli-
[10] Google Inc. 2023. XLS: Accelerated Hardware Design. Retrieved No-
                                                                                  cations. https://doi.org/10.1109/FPL.2013.6645550
     vember 27, 2023 from https://google.github.io/xls/
                                                                             [31] Ofer Shacham, Megan Wachs, Andrew Danowitz, Sameh Galal, John
[11] James Hegarty, John Brunhaver, Zachary DeVito, Jonathan Ragan-
                                                                                  Brunhaver, Wajahat Qadeer, Sabarish Sankaranarayanan, Artem Vas-
     Kelley, Noy Cohen, Steven Bell, Artem Vasilyev, Mark Horowitz, and
                                                                                  siliev, Stephen Richardson, and Mark Horowitz. 2012. Avoiding game
     Pat Hanrahan. 2014. Darkroom: Compiling high-level image pro-
                                                                                  over: Bringing design to the next level.
     cessing code into hardware pipelines. ACMTransactionsonGraphics.
                                                                             [32] Frans Skarman and Oscar Gustafsson. 2023. Spade: An Expression-
     https://doi.org/10.1145/2601097.2601174
                                                                                  Based HDL With Pipelines.           https://doi.org/10.48550/arXiv.2304.
[12] James Hegarty, Ross Daly, Zachary DeVito, Jonathan Ragan-Kelley,
                                                                                  03079
     Mark Horowitz, and Pat Hanrahan. 2016. Rigel: Flexible multi-rate
                                                                             [33] Lennart Van Hirtum and Christian Plessl. 2024. Latency counting in
     image processing hardware. ACMTransactionsonGraphics. https://
                                                                                  the SUS language. In Workshop Languages, Tools, and Techniques for
     doi.org/10.1145/2897824.2925892
                                                                                  Accelerator Design.
[13] Steven F Hoover. 2017. Timing-abstract circuit design in transaction-
                                                                             [34] Srikanth Vijayaraghavan and Meyyappan Ramanathan. 2005. A prac-
     level Verilog. In IEEE International Conference on Computer Design
                                                                                  tical guide for SystemVerilog assertions. Springer Science & Business
     (ICCD).
                                                                                  Media.
[14] AMD Inc. 2011. LogiCORE IP Divider Generator v3.0. Retrieved Au-
                                                                             [35] Jason Zhijingcheng Yu, Aditya Ranjan Jha, Umang Mathur, Trevor E.
     gust 20, 2025 from https://docs.amd.com/v/u/en-US/div_gen_ds530
                                                                                  Carlson, and Prateek Saxena. 2025. Anvil: A General-Purpose Timing-
[15] AMD Inc. 2011. LogiCORE IP Fast Fourier Transform v7.1. Retrieved
                                                                                  Safe Hardware Description Language.           arXiv:2503.19447 [cs.AR]
     August 20, 2025 from https://docs.amd.com/v/u/en-US/xfft_ds260
                                                                                  https://arxiv.org/abs/2503.19447
[16] AMD Inc. 2021. LogiCORE IP Divider Generator v5.1. Retrieved Au-
                                                                             [36] Zhiru Zhang, Yiping Fan, Wei Jiang, Guoling Han, Changqi Yang, and
     gust 20, 2025 from https://docs.amd.com/v/u/en-US/pg151-div-gen
                                                                                  Jason Cong. 2008. AutoPilot: A platform-based ESL synthesis system.
                                                                                  In High-Level Synthesis. 99–112.
