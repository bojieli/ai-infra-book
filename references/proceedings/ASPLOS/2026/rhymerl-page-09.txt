N-1                                                                         ▶MIN_WKS/MAX_WKS: a ranking   ▶Find best worker allocation
                                                                            group' min/max worker number ▶The target time of group n:
                                                                            ▶Tool func: calculate workers t(n)=d*n+t0, 0 <= n < N




                                                                   t0
                          remained                0
                                               +t




                                                                n+
                                              n                             needed for a given d          func planAllocation(lens,
                           bubbles         x*




                                                                  *
                                                               nd
                                              a                             func calWks(d, lens, wks, t0)     wks, t_train)
                                           _m




                                                          _e
                                         d                                    wks_needed = 0               t0 = max(𝜏(lens[0], MAX_WKS),




                                                        t =d
    0                                 t=                                      for i in range(0, N):            t_train)
        (a) bubbles in naive design    (b) Tier-2 scheduling                    target_t = t0+i*d          d_min = 0.0
Figure 11. Bubbles caused by skewed rollout time distribu-                      ▶find min workers to meet d_max = (𝜏(lens[N-1],
                                                                                the group' s target time          MIN_WKS)-t0)/(N-1)
tion, and how the two-tier scheduling reduces bubbles.                          group_wks = -1             p = 1 # precision, adjustable
                                                                                for k in range(MIN_WKS,    ▶binary search
                                                                                        MAX_WKS+1):        while (d_max-d_min) > p:
scheduling mechanism to tackle this issue:                                        exec_t = 𝜏(lens[i], k)    d_mid = (d_max+d_min)/2
                                                                                  if exec_t <= target_t:    result = calWks(d_mid,
• Tier-1: from prompts to ranking groups. First, Histo-                             group_wks = k                     lens, wks, t0)
Pipe equally divides the ranked prompts into ranking groups.                        break                   if result.wks_needed > wks:
• Tier-2: mapping ranking groups to GPUs. If we allo-                           if group_wks == -1:          d_min = d_mid
                                                                                  return (Inf, [])          else: # feasible solution
cate equal GPUs per ranking group, their execution times                        wks_needed += group_wks      best_plan = result.plan
will exhibit exponential distributions due to long-tail rollouts.               plan[i] = group_wks          d_max = d_mid
Therefore, HistoPipe designs a distribution reshaping strat-                  return (wks_needed, plan)    return best_plan
egy: instead of evenly distributing GPUs among the groups, it               Figure 12. Tier-2 scheduling algorithm. It solves the problem
allocates fewer GPUs to the short-length and medium-length                  by minimizing the execution time gradient (𝑑) via binary search, as
groups while provisioning extra GPUs to the few groups with                 Fig.11b shows. We use t_train (time of the last finished step’s training
long responses. In this way, HistoPipe reshapes the groups’                 stage) to ensure the execution time of the shortest ranking group is
                                                                            longer than the training stage’s time, avoiding the next step’s rollout
rollout time distribution from exponential to linear, thereby
                                                                            waiting for train workers to generate weights. 𝜏 (𝑙, 𝑑𝑝) is determined by
further reducing rollout bubbles.                                           looking up the table obtained from pre-profiling. The cooperation of
    In RhymeRL, each rollout worker manages several GPUs                    HistoPipe with HistoSpec is also considered. 𝜏 (𝑙, 𝑑𝑝) also considers
cooperating via model parallelism with user-specified con-                  HistoSpec’s current speculation information (e.g., acceptance rate),
figurations. During Tier-2 scheduling, RhymeRL allocates                    which is omitted in the algorithm for ease of understanding.
GPUs at rollout-worker granularity. Rollout workers operate
via data parallelism, thus, increasing rollout workers reduces              across steps, speculation rates, and acceptance rates. To fur-
per-worker batch size and shortens the execution time.                      ther understand HistoPipe (§ 7.2.3), we study its improve-
Algorithm. HistoPipe determines the GPU allocation plan                     ments on training throughput across steps, and its migration
using the algorithm detailed in Fig.12. It uses the mean value              rates. Finally, we study the impact of RhymeRL on training
of the prompts’ historical response lengths within each rank-               accuracy and algorithm behavior (§ 7.3). We also compare
ing group (representative length) to estimate the group’s exe-              HistoSpec with other speculative decoding methods (§7.4).
cution time. Through pre-profiling of the inference engine                  Hardware settings. We deploy RhymeRL on a GPU cluster
(i.e., HistoSpec, also considering the impact of speculative                with 16 nodes and 128 GPUs. Each node is equipped with 8
decoding), we can obtain the relationship between a ranking                 high-performance GPUs, 2 Intel Xeon Sapphire Rapids CPUs
group’s execution time (t), its representative rollout length               with 96 cores, 1900GB host DRAM, and 9 * 400 Gb/s NVIDIA
(l), and DP worker number (dp): 𝑡=𝜏 (𝑙, 𝑑𝑝). Within the RL                  ConnectX7 InfiniBand NIC (8 GPU-dedicated NICs and one
cluster, there are 𝑤𝑘𝑠 rollout workers to serve 𝑁 ranking                   CPU-dedicated NIC).
groups, whose representative lengths are 𝑙𝑒𝑛𝑠. We have to                   Model and algorithm. We verify RhymeRL’s observations
assign varying numbers of rollout workers to each group,                    and performance improvements on Qwen-2.5 models [55],
and make their completion times close to linear distribution.               Qwen-3 models [4], DeepSeek-R1-Distill-Qwen models [1]
This constitutes an integer nonlinear programming problem                   and LLama-3 models [56]. Our evaluation utilizes Qwen3-
and we converge to the solution via binary search.                          8B-Base, Qwen3-14B-Base, and Qwen2.5-32B models, with
                                                                            detailed architectures listed in Table.3. We train these mod-
7        Evaluation                                                         els using GRPO algorithm [6], which is currently the main-
We evaluate RhymeRL with LLMs of sizes ranging from 8B                      stream LLM RL algorithm, powering advanced LLMs such
to 32B, on real-world math and code datasets.                               as DeepSeek-R1 [1]. We also verify RhymeRL’s efficiency on
Experiments. First, we compare RhymeRL’s end-to-end                         recent next-generation algorithms like DAPO [16] and pro-
training throughput with SOTA LLM RL systems on training                    vide the results. Based on our practice, the response group
LLMs of different sizes using different datasets (§7.1). Then,              size is set to 16, which yields peak algorithmic efficiency.
we break down the improvements of RhymeRL’s designs                         Metrics. As per convention [14, 43, 57], we report training
(§7.2.1). To gain a deeper understanding of HistoSpec (§7.2.2),             throughput, measured by the average number of samples
we study HistoSpec’s improvements on rollout throughput                     generated and processed per second. We sample 8K math-
                                                                        9
