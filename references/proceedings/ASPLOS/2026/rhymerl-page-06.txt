                      Math-1             Math-3            Code-2         10%                  Math-1                6%            Code-1                Dataset   Size
                      Math-2             Code-1




                                                                 Percentage
            100                                                                                                      4%
Acc. Rate (%)

                                                                                                                                                         Math-1    230K
             80
             60                                                               5%                                                                         Math-2     40K
             40                                                                                                      2%                                  Math-3     10K
             20                                                                                                                                          Code-1     40K
              0       2       3     4     5     6      7     8                0%   0.7          0.8       0.9     1.0 0%     0.6    0.7     0.8    0.9   Code-2     12K
                                        Epoch                                        Acceptance Rate                          Acceptance Rate
              (a) Acceptance rate across epochs      (b) Responses’ acceptance rate distribution (Math-1 and Code-1) (c) Datasets used
            Figure 6. Characterizing historical token similarity. We use five datasets to train 14B LLMs respectively using the GRPO algorithm.

        token/tokens                                                                     leaf             sands of tokens per prompt). Traditional draft generation
                   ix                   1.3 “value is” 1.3       “1”
               Pref “the”                                              1.9               0.6              methods struggle to operate efficiently under the constraints.
                          …         ……                              “2”                                   Async cache building. The RL workloads allow us to relax
                  “so”                              dra chosen                          1.3
                R …           261
                                    …                     Priority:     -0.6                              the constraints on draft generation. In RL sampling strategies,
                          …                           response’s reward                  -0.6             the same request is generally not re-sampled until multiple
                                                                                                          steps later. Therefore, we are able to shift the computational
    Figure 7. Suffix-tree based draft generation. R = Root. For ease
                                                                                                          overhead associated with retrieval to the indexing phase.
    of understanding, we convert token ids into words.
                                                                                                          RhymeRL employs history workers to index historical rollout
                                                                                                          sequences asynchronously. Upon generating new responses,
    be successfully “accept” using this routine (on average, 75%                                          the controller dispatches them to history workers for index
    for code). Furthermore, as training progresses (i.e., with in-                                        construction. When scheduling prompts to rollout workers
    creasing epochs), the similarity increases. Fig.6b shows the                                          (asynchronously), the controller notifies the corresponding
    distribution of the responses’ acceptance rates.                                                      history workers to transfer the relevant indexed cache.
                                                                                                          Reward-aware tree-based management. RhymeRL em-
    5.2           Speculative Rollout with History                                                        ploys suffix trees [51] to index cached responses, which en-
    Leveraging historical token similarity, we design HistoSpec,                                          ables 𝑂 (𝑚)-time matching for length-𝑚 prefixes. RhymeRL
    which utilizes speculative decoding to accelerate rollouts and                                        maintains a dedicated tree for each prompt, indexing all its
    uses historical responses as draft sources. In each decoding                                          historical responses generated in its last rollout. Since mod-
    iteration, HistoSpec uses the last few generated tokens as the                                        ern RL algorithms generate multiple responses per prompt
    prefix to search for matches within the prompt’s historical                                           to explore multiple solution paths, a prefix may branch to
    responses. Upon matching, it extracts a certain number of                                             divergent suffixes, complicating draft token selection. We ob-
    subsequent tokens following the prefix as drafts. The drafts                                          serve that, during training, RL algorithms optimize the model
    are then verified via a single LLM forward pass, with some                                            towards generating high-reward solutions with higher prob-
    (possibly all) tokens accepted (§2.2). This history-based spec-                                       ability. Therefore, we add a priority value to each tree node,
    ulative rollout improves computational resource utilization                                           weighted by the sum of the rewards of its branch. For every
    during rollouts, and reduces the time of the rollout stage.                                           suffix branch present, HistoSpec selects the one with the
    Technical challenges. However, this approach encounters                                               highest priority. Through this RL-algorithm-guided design,
    two challenges: (1) Over-long rollout sequences induce sig-                                           HistoSpec maximizes the speculation acceptance rate.
    nificant prefix matching overhead, and the branching in                                                  As Fig. 7 shows, in the suffix tree, each node represents
    historical responses complicates draft token selection; (2)                                           a subsequence formed by the path from root to that node,
    Unpredictable draft acceptance length creates a trade-off                                             where each leaf node corresponds to a complete suffix of
    between underutilizing compute capacity (when predicting                                              a response, and each edge represents one or more tokens
    too few tokens) and wasting resources on verifying rejected                                           extending the sequence of parent node to the sequence of the
    tokens (when over-predicting).                                                                        child. For each leaf node, its priority (marked on the nodes
                                                                                                          in Fig. 7) is the sum of the rewards for the responses that
    5.3           Tree-based Historical Rollout Management                                                end with this suffix (that the leaf node represents). A parent
    Draft generation overhead is a pivotal factor determining                                             node’s priority is the sum of its children’s priorities.
    speculative decoding’s effectiveness. However, existing corpus-                                       Resources. Both the tree’s construction time overhead and
    based speculation methods face a dilemma between retrieval                                            memory overhead are 𝑂 (𝑛) for 𝑛 tokens [52]. GPU clusters
    costs and index building costs. E.g., n-gram [36, 50] requires                                        have sufficient CPU resources (64–128+ cores and multi-
    time-consuming linear scans for prefix matching, and Suf-                                             TB host memory), which are mostly idle during previous
    fixDecoding [35] limits the lengths of draft source sequences                                         RL flows. RhymeRL strictly limits the resources used by
    to reduce index building costs. During LLM RL, each epoch                                             history workers using OS methods [53], preventing them
    generates multiple long responses (totaling hundreds of thou-                                         from interfering with other workers. For RL training with a
                                                                                                      6
