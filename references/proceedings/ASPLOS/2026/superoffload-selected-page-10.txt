                                                                                       Xinyu Lian, Masahiro Tanaka, Olatunji Ruwase, and Minjia Zhang


       Megatron   ZeRO-2      ZeRO-3     ZeRO-Offload       SuperOffload




                                                                                                                         OOM


                                                                                                 OOM                                           OOM

                               OOM     OOM      OOM


                     (a) 4-Superchip                    (b) 16-Superchip

                                                                                       (a) 13B, 4-Superchip   (b) 13B, 8-Superchip   (c) 30B, 8-Superchip
Figure 11. Training throughput comparison between Mega-
tron, ZeRO-2/3, ZeRO-Offload, and SuperOffload on 4 and
                                                                                Figure 12. Supported sequence lengths and corresponding
16 GH200 GPUs. We use batch sizes of 16 and 128 for the
                                                                                MFU using SuperOffload-Ulysses and Ulysses. OOM denotes
4-GPU and 16-GPU experiments, respectively.
                                                                                the point where increasing sequence length causes OOM.

consistently achieves less than 15 TFLOPS because it re-
lies on PyTorch’s native CPU-Adam implementation. As we
demonstrate later in § 5.6, native implementation is up to                              utilization of both GPU and CPU resources during
3.5× slower than our GraceAdam, creating a substantial ef-                              training.
ficiency bottleneck. Similarly, ZeRO-Infinity’s throughput                            • While Megatron and ZeRO-3 encounter memory limi-
remains below 50 TFLOPS. This limitation stems from ZeRO-                               tations at 15B parameters due to insufficient aggregate
Infinity’s bucket-based approach for transferring gradients                             GPU memory across 4 GPUs, SuperOffload effectively
and parameters between CPU and GPU, which fails to ac-                                  scales to models with up to 50B parameters. Moreover,
count for the characteristics of C2C bandwidth—specifically,                            SuperOffload demonstrates exceptional scalability by
that bandwidth can drop to as low as 50GB/s with small                                  efficiently training 200B models on 16 GPUs while
tensor sizes.                                                                           maintaining high computational throughput.
Multiple Superchips. We evaluate the training throughput
of SuperOffload against Megatron, ZeRO-2, ZeRO-3, and                           5.3    Unlock Massive Sequence Training
ZeRO-Offload across 4 and 16 GH200 Superchips. We exclude                       We combine SuperOffload together with Ulysses [22] to cre-
the PyTorch DDP from the comparison as it cannot scale out                      ate SuperOffload-Ulysses for long-sequence training and
to larger model sizes, and we exclude FSDP-offload and ZeRO-                    evaluate its performance against vanilla Ulysses using two
Infinity due to their significantly lower throughput compared                   commonly deployed model sizes: 13B and 30B parameters.
to other solutions on single Superchip. For Megatron, we                        Our experiments are conducted on both 4-Superchip and
use a MP degree that gives the best performance. Similar to                     8-Superchip. For all experiments, we utilize the maximum
the above single GPU experiment, when facing OOM errors,                        batch sizes that avoid out-of-memory errors, and imple-
we either use gradient accumulation with smaller micro-                         mented activation checkpointing when activation sizes are
batches or activation checkpointing with the largest possible                   substantial. As shown in Fig. 12, SuperOffload-Ulysses trains
micro-batch, reporting the higher throughput between these                      sequences up to 8× longer than Ulysses. SuperOffload-Ulysses
approaches. Fig. 11 shows the throughput per GPU results                        enables the training of 13B model with sequence lengths up
when training on 4 Superchips and 16 Superchips. We have                        to 1 million tokens on 8 Superchips while achieving 55% MFU.
the following observations:                                                     For sequence lengths that Ulysses can handle, SuperOffload-
     • For models ranging from 5B to 20B parameters, Su-                        Ulysses consistently achieves higher Model FLOPS Utiliza-
       perOffload consistently achieves higher throughput                       tion (MFU). This performance advantage stems from our
       compared to existing solutions. Specifically, it demon-                  adaptive Offloading policy (§ 4.2), which adaptively detects
       strates throughput improvements of up to 83%, 46%,                       extremely long sequences and dynamically offloads more
       and 37% over Megatron, ZeRO-2, ZeRO-3 respectively.                      weight parameters to CPU memory. This strategic offloading
       In comparison with ZeRO-Offload, SuperOffload achieves                   provides GPUs with additional memory space to accommo-
       2.5× throughput on average across a wider range of                       date larger activations without requiring activation check-
       model sizes. This is primarily attributed to highly over-                pointing, thereby maintaining computational efficiency even
       lapped CPU and GPU computation and sufficient GPU                        with extended sequence lengths.
       memory space for larger micro batch without using
       activation checkpointing, and those methods effec-                       5.4    Model Scale
       tively leverage the high-bandwidth C2C interconnect                      In this part, we first test the largest trainable models on a
       in the Superchip architecture, enabling more efficient                   single Superchip as well as 4-Superchip and 16-Superchip.
                                                                           10
