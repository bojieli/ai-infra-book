            Co-Exploration of RISC-V Processor
       Microarchitectures and FreeRTOS Extensions for
               Lower Context-Switch Latency
               Markus Scheck∗                                Tammo Mürmann∗                               Andreas Koch
       scheck@esa.tu-darmstadt.de                      muermann@esa.tu-darmstadt.de                koch@esa.tu-darmstadt.de
     Technical University of Darmstadt                 Technical University of Darmstadt        Technical University of Darmstadt
        Darmstadt, Hesse, Germany                         Darmstadt, Hesse, Germany                Darmstadt, Hesse, Germany

Abstract                                                                  ACM Reference Format:
Embedded real-time systems must respond to external events                Markus Scheck, Tammo Mürmann, and Andreas Koch. 2026. Co-
                                                                          Exploration of RISC-V Processor Microarchitectures and FreeRTOS
within tightly bounded timeframes to ensure safety, correct-
                                                                          Extensions for Lower Context-Switch Latency. In Proceedings of
ness, and reliability. While Real-Time Operating Systems
                                                                          the 30th ACM International Conference on Architectural Support for
(RTOSes) ease the development of complex applications by                  Programming Languages and Operating Systems, Volume 2 (ASPLOS
providing abstractions for multi-tasked execution, they in-               ’26), March 21–26, 2026, Pittsburgh, PA, USA. ACM, New York, NY,
troduce overheads in the form of task switching latency                   USA, 15 pages. https://doi.org/10.1145/3779212.3790141
and jitter, which impact timing predictability. Minimizing
these effects is essential for reducing response times and                1    Introduction
enabling robust worst-case timing analysis. We present RTO-
                                                                          Embedded real-time systems must respond to external stim-
SUnit, a configurable hardware acceleration unit designed to
                                                                          uli within strict timing bounds to ensure safety, correctness,
reduce context-switch latency and jitter in embedded real-
                                                                          and reliability. While such guarantees can be achieved with
time systems. By integrating the RTOSUnit into three RISC-V
                                                                          low-level "bare-metal" software implementations, Real-Time
cores of varying complexity, we demonstrate its portability.
                                                                          Operating Systems (RTOSes) simplify development by offer-
Through a range of configurations, from lightweight sched-
                                                                          ing a multi-tasked programming model. This abstraction
uling acceleration to full context-switch and -preloading
                                                                          allows developers to define multiple concurrent tasks that in-
support, RTOSUnit achieves up to 76 % reduction in mean
                                                                          teract via synchronization primitives, promoting modularity
context-switch latency and can be configured to completely
                                                                          and maintainability.
eliminate jitter on selected cores. Area overheads in 22 nm
                                                                             However, this simplification introduces overhead: the RT-
ASIC implementations range from negligible (within EDA
                                                                          OS manages task execution by scheduling, state tracking,
tool heuristics noise) to 44 %, with all configurations main-
                                                                          and context switching – services that, while useful, increase
taining viable operating frequencies and power envelopes
                                                                          code size and introduce latency. During a context switch, the
suitable for embedded systems. RTOSUnit offers a flexible
                                                                          RTOS saves the current task’s state to the stack, invokes the
and efficient, open-source1 foundation for hardware-assisted
                                                                          scheduler to select the next task, and restores the selected
real-time scheduling, paving the way for broader integration
                                                                          task’s state. The total time required for this process is known
into future embedded SoCs.
                                                                          as context-switch latency.
CCS Concepts: • Computer systems organization → Ar-                          Context switching directly impacts the system’s response
chitectures; Real-time operating systems; Real-time                       time to external events. While simple event handling can be
system architecture; Embedded hardware.                                   performed within the interrupt service routine (ISR), more
                                                                          complex logic must be deferred to tasks, requiring a full con-
Keywords: FreeRTOS; Context-Switch; Latency; Jitter; Real-                text switch before handling can commence. Consequently,
Time; RTOS; RISC-V                                                        reducing context-switch latency improves the minimal re-
∗ Both authors contributed equally to this research.                      sponse time to such events. While this work focuses on the
1 https://github.com/esa-tu-darmstadt/RTOSUnit_Integration                deferred handling case, note that our context storing opti-
                                                                          mizations also accelerate the non-deferred handling case.
                                                                             Additionally, many scheduling algorithms introduce vari-
                                                                          ability in context-switch durations, reducing predictability
This work is licensed under a Creative Commons Attribution-               and complicating worst-case execution time (WCET) anal-
NonCommercial-NoDerivatives 4.0 International License.
ASPLOS ’26, Pittsburgh, PA, USA.
                                                                          ysis. We define the difference between the maximum and
© 2026 Copyright held by the owner/author(s).                             minimum observed context-switch latency as jitter. Minimiz-
ACM ISBN 979-8-4007-2359-9/2026/03                                        ing both latency and jitter is essential for predictable timing,
https://doi.org/10.1145/3779212.3790141                                   especially in control loops or under high system load.
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                            Markus Scheck, Tammo Mürmann, & Andreas Koch


    Application                               Kernel                    the responsibility of the main processor. A similar hard-
                                                                        ware/software partitioning is used by Andrews et al. [1]
Running                                                                 and Vetromille et al. [25]. Both identify task scheduling as a
task 1                                                                  major source of jitter and thus a key candidate for hardware
             Interrupt:                           a
                                                      Save context      acceleration. Morton et al. [17] quantify the benefits of selec-
             (1) preemptively by expired timer        of task 1
                                                                        tive hardware offloading, showing scheduling acceleration
             - or -
             (2) externally by peripherals        b Scheduling          provides significant gains with minimal area cost. Chandra
             - or -                                                     et al. [5] also focus on scheduling and synchronization accel-
             (3) voluntarily by running task          Restore context
                                                  c
                                                      of task 2
                                                                        eration, opting to design their hardware through high-level
Running
                                                                        synthesis rather than manual HDL development.
                      Resume task 2
task 2                                                                     However, these approaches rely on memory-mapped I/O
                                                                        (MMIO) for communication with the host processor, intro-
Figure 1. Simplified context switching in a typical real-time           ducing higher latencies and bus contention. Our approach
system. During an interrupt, the processor saves the current            uses tightly coupled custom instructions, reducing overhead
task’s context, selects the next task, restores its context, and        and allowing efficient context save/restore acceleration via
resumes execution from where it was suspended.                          direct processor integration.
                                                                           Balas et al. [3] reduce context-switch latency by snapshot-
                                                                        ting half the register file upon interrupt entry,2 and storing
   To address these challenges, this work introduces a con-             the snapshot values in parallel using a dedicated memory
figurable hardware unit into the processor for accelerating             port.3 The other half of the register file is saved concurrently
scheduling and context switching in FreeRTOS. By offload-               in software via the LSU’s regular memory port. In contrast
ing key RTOS functions to hardware, our approach reduces                to our bank switching approach, their method accelerates
worst- and best-case context-switch latency as well as jitter           only part of the task’s context saving and does not provide
– resulting in faster, more predictable responses. These im-            hardware support for context restoration or scheduling. Ad-
provements support the development of embedded real-time                ditionally, their work is deeply integrated into the CV32E40P
systems that must adhere to strict deadlines and response-              processor, while we aim for (and demonstrate) better porta-
time bounds, and that require low execution-time variability            bility across cores via our modular approach.
for effective modeling and certification.                                  Rafla et al. [20] integrate custom instructions for context
   The key contributions of this work are as follows:                   save and restore into a MIPS processor. These instructions
    • We present a highly configurable hardware unit dedi-              interact with a banked context memory and require two cy-
      cated to scheduling and context-switching.                        cles to transfer the register state. Similarly, Grunewald et al.
    • We demonstrate the portability of our approach and                [11] propose assigning each task its own register file. Con-
      its applicability through integration into three differ-          text switching is then reduced to simply switching register
      ent RISC-V processors, ranging from microcontroller               banks. While effective, both approaches likely incur high
      (MCU) to powerful superscalar out-of-order cores.                 area costs, especially when supporting many concurrent
    • We greatly reduce context-switch latency and jitter               tasks, though neither work actually quantifies this overhead.
    • We analyze overheads in power, maximum core clock                 Both approaches inherently limit the number of tasks to
      frequency, and area using a 22nm ASIC process.                    which context save and restore acceleration can be applied.
                                                                        In contrast, our method uses main memory to store task
   The remainder of this paper is structured as follows: Sec-           state, making the acceleration independent of task count and
tion 2 reviews related work. Section 3 describes the tar-               thus more scalable. We can optionally accelerate scheduling
get RISC-V cores and the scheduling and context-switching               in hardware, as well, but our solution would then also be
mechanisms of FreeRTOS. Section 4 details the implementa-               limited to a statically configured ceiling in the number of
tion of RTOSUnit, while Section 5 discusses its integration             tasks.
into the RISC-V processors. Section 6 presents an evaluation               Some commercial ISAs offer similar mechanisms. ARM
of RTOSUnit in terms of performance, implementation over-               AArch32 introduces shadow registers for the Fast Interrupt
head, and power requirements. Finally, Section 7 summarizes             (FIQ) mode, reducing context-switch latency by removing the
the findings and discusses directions for future work.                  need to save/restore the banked registers [2]. Microchip’s

2    Related Work                                                       2 https://github.com/pulp-platform/cv32e40p/blob/

Many attempts at hardware-supported scheduling and con-                 6c5c2d6bdfb9c5d743aad240ce6e1ae7a0baf1b8/rtl/cv32e40p_register_
                                                                        file_ff.sv#L194-L308
text-switching have been made. STRON by Nakano et al. [18]              3 Note that the paper is inconsistent in this regard: While their Fig. 3 depicts a
offloads semaphores, task tracking, interrupts, and schedul-            bank-switching mechanism, the released implementation actually performs
ing to a coprocessor, while context management remains                  snapshotting.
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.


PIC32MZ expands this to seven shadow register sets for                     is a major factor in jitter, combining both in hardware is a nat-
fast handling of multiple interrupt sources [16]. While these              ural progression. Zagan et al. [26] replicate all state elements
techniques work well for short non-deferred interrupt han-                 across the processor pipeline and register file, allowing their
dling, they are unsuitable for general-purpose context switch-             scheduler coprocessor to select the active context without
ing, as the inactive register banks are inaccessible to soft-              flushing the pipeline. While this enables near-instantaneous
ware, inhibiting a replacement of their contents. Our ap-                  switching, it significantly increases the area and lowers the
proach, by contrast, accelerates both context management                   maximum operating frequency. Doidu et al. [9] present a sim-
and task scheduling, supporting deferred and non-deferred                  ilar design where all pipeline and register states are banked,
interrupt handling alike. Intel’s x86 provides a hardware                  and the scheduler runs on the negative clock edge. They
context-switching instruction [7], but it proved to be slower              report a 0.5-cycle context-switch latency but do not evaluate
than software solutions under flat memory models as used                   frequency or area overhead. Given the similarity to Zagan
by modern operating systems, and was removed in x86-64                     et al., it is likely that their design suffers from the same
[8, 12].                                                                   frequency degradation and area overheads. FASTCHART
   Other works [22, 29] follow a vastly different approach for             [14, 15, 24] pushes scheduling, synchronization, and context
reducing context-switch overhead. Rather than accelerating                 handling fully into hardware. However, their implementa-
context saving and restoring directly in hardware, the au-                 tion assumes the entire register file can be stored to memory
thors modify the compiler to identify so-called switch points,             in a single cycle, which the authors themselves acknowledge
i.e., program locations where only a small subset of registers             as unrealistic [15]. Our approach avoids this impracticality
is live. For each switch point, specialized software routines              by using register banking and latency-hiding techniques.
for context saving and restoring are generated, minimizing                 Furthermore, their approach uses one scheduling queue per
the size of state that must be preserved. However, when an                 priority level, while we implement a unified queue. In conse-
interrupt occurs, the hardware must defer handling until                   quence, our approach offers greater flexibility by only limit-
execution actually reaches the next switch point, introduc-                ing the total number of tasks, not the number of tasks per
ing additional jitter. As a consequence, fully preemptive task             priority level.
scheduling is effectively transformed into a coroutine-like                   In contrast to prior work, our approach combines low-
execution model with restricted preemption points. More-                   latency scheduling and context switching within a single,
over, this approach is evaluated solely in simulation and has              tightly integrated hardware unit. Additionally, while some
not been implemented in hardware, nor does it provide any                  work focuses exclusively on scheduling or context manage-
acceleration of the scheduling mechanism itself.                           ment, and others compromise frequency and area, our solu-
   Nurmi et al. [19] propose the HETI architecture as an al-               tion strikes a practical overall balance. We support real-time
ternative to RTOS-based systems. Their approach models                     constraints by improving both worst-case and best-case la-
tasks as interrupt-triggered routines that are executed directly           tency, reducing jitter, and maintaining compatibility across
within an ISR. To reduce interrupt latency, the processor is               multiple RISC-V cores. To the best of our knowledge, this
extended with a dedicated register file bank per interrupt,                combination of configurability, portability, and efficiency has
which is automatically selected on interrupt entry. While                  not been achieved previously.
their design enables very fast context switching by eliminat-
ing register save and restore operations, the hardware area
overhead scales rapidly with the number of supported inter-
rupts. Furthermore, the programming model is restrictive
compared to an RTOS, supporting only a single main thread
and one ISR per interrupt.                                                 3    HW/SW Platform Selection
   Cheshmikhani et al. [6] propose a hardware mechanism                    To enable the practical implementation and evaluation of
that caches interrupt handler addresses to avoid repeated han-             our approach, we must select both a processor core and an
dler lookups. Their work targets application-class processors              RTOS for extension.
running Linux, where interrupt dispatch involves dynamic                      We adopt the RISC-V Instruction Set Architecture due to
and potentially costly handler resolution. In contrast, the                its openness, extensibility, and extensive ecosystem of open-
small embedded systems that we target commonly rely on                     source cores spanning a wide range of complexity. Critically,
either a single shared interrupt handler or a small, statically            RISC-V supports custom instructions, which we leverage to
defined interrupt vector table with constant-time handler                  tightly integrate the proposed functionality. We demonstrate
selection, which leaves little opportunity for such caching                our approach on three representative RISC-V processors:
mechanisms to reduce interrupt latency in this domain.                     (1) CV32E40P, a microcontroller-class, 4-stage pipeline pro-
   Because scheduling and context switching both contribute                cessor [10], (2) CVA6, an application-class, 6-stage pipeline
significantly to total context-switch latency, and scheduling              processor [27], and (3) NaxRiscv, a powerful superscalar out-
                                                                           of-order processor supporting variable-latency instructions
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                         Markus Scheck, Tammo Mürmann, & Andreas Koch


        Re-queue task to end of queue
                                                                          Table 1. Overview of the proposed custom instructions
                                                       yield c or
             Ready Lists                               preemptive      Custom Instruction   Description                Required for
                                                       removal a       ADD_READY            Insert task into ready list HW scheduling
    create   Prio 2                      max              Running      ADD_DELAY            Insert task into delay list HW scheduling
             Prio 1                     priority           Task        RM_TASK              Remove task from HW lists HW scheduling
             Prio 0                                b                   SET_CONTEXT_ID       Set the next task           w/o HW scheduling
                                                                       GET_HW_SCHED         Get next task from HW       HW scheduling
                           Delay List             remove               SWITCH_RF            Switch back to the APP RF Context storing w/o loading
                                                    d f
        duration elapsed                       task sleeps f
                g
                           Event List
   resource available e                      resource unavailable d      All of these lists store pointers to Task Control Blocks
                                                                      (TCBs), which are expressed as C structs that hold essen-
Figure 2. FreeRTOS scheduling algorithm. Tasks are sched-             tial information for each task. A global variable, currentTCB,
uled in a round-robin manner within priorities, while the             points to the TCB of the currently running task and is up-
next task is always drawn from the highest priority of the            dated for the next task during scheduling.
ready tasks. In addition to the ready lists, FreeRTOS main-              While the scheduler selects the next task, context switch-
tains a delay list for tasks suspended for a specific number          ing is responsible for transitioning execution between tasks.
of time slices, and event lists for tasks waiting on events.          As illustrated in Figure 1, context switching comprises three
                                                                      phases: (a) saving the current task’s context, (b) scheduling
                                                                      to select the next task, and (c) restoring its context.
                                                                         The context of a task is saved by pushing its general-
                                                                      purpose registers, processor configuration (e.g., interrupt
                                                                      settings), and program counter onto its stack (cf. Figure 4
[23]. These processors span a broad spectrum of implemen-
                                                                      (a)). The address of the saved context (i.e., the stack pointer)
tation complexity and are chosen to demonstrate the porta-
                                                                      is stored in the task’s TCB, allowing the stored state to be
bility of our approach across diverse RISC-V cores. All three
                                                                      retrieved later (cf. Figure 4 (b)). The RISC-V architecture
processors are evaluated in their RV32IM_Zicsr configuration.
                                                                      includes 32 general-purpose registers, one of which is hard-
   Although RISC-V is particularly well suited to our work,
                                                                      wired to zero. The global pointer register provides a fixed
other architectures could be supported with modest engi-
                                                                      offset for accessing global variables and is typically initial-
neering effort, provided they offer well-defined interrupt
                                                                      ized during program startup. The thread pointer register
entry and exit boundaries, a clearly specified architectural
                                                                      points to thread-local storage, with usage defined by the OS.
state to be saved, and support for custom instructions.
                                                                      In FreeRTOS, neither register is modified after initialization.
   For our evaluation, we use the popular FreeRTOS, which
                                                                      Consequently, the global pointer and thread pointer regis-
is lightweight, open-source, and supports RISC-V. Other
                                                                      ters only contain static data in the RTOS execution scenario,
RTOSes – such as Zephyr [28] or ChibiOS [21] – feature
                                                                      leaving 29 registers to be preserved. Additionally, RISC-V
similar architectures and scheduling algorithms, and could
                                                                      uses Control and Status Registers (CSRs) to store the proces-
also benefit from RTOSUnit with moderate engineering ef-
                                                                      sor’s configuration and auxiliary information. The mstatus
fort. We expect such extensions to yield comparable results
                                                                      CSR (processor state) and the mepc CSR (program counter
and, consequently, solely focus on FreeRTOS in this work.
                                                                      at the time of interruption) are additionally saved to enable
   Figure 2 shows an overview of FreeRTOS’s scheduling
                                                                      task resumption and state restoration. In total, a context
algorithm. To enforce fair scheduling among tasks of equal
                                                                      comprises 31 32-bit words.
priority, FreeRTOS uses time slices: a timer interrupt preempts
                                                                         Saving the context not only enables task switching but
the running task after a fixed duration (a), after which the
                                                                      also frees the registers for use during scheduling. Restoring
highest-priority ready task is selected (b). Tasks may also
                                                                      the new task’s context involves retrieving the address of the
voluntarily yield (c) by triggering a software interrupt.
                                                                      saved state from its TCB and loading the saved values (cf.
   FreeRTOS provides synchronization primitives such as
                                                                      Figure 4 (d)). Context switching is initiated by an interrupt
mutexes and semaphores. When a task attempts to acquire
                                                                      originating from a hardware timer (for time slicing), external
a blocked resource, it is removed from the ready list and
                                                                      sources (for deferred interrupt handling), or software (vol-
placed into the event list (d). It returns to the ready list when
                                                                      untary yield). Execution resumes in the new task using the
the resource becomes available (e). Tasks may also suspend
                                                                      mret instruction.
themselves for a defined time, during which they are placed
in a delay list (f). This list is updated on every timer interrupt,
and tasks whose delay has expired are re-added to the ready           4     RTOSUnit Implementation
list (g). If a task waits on a resource with a timeout, the first     This section presents the RTOSUnit, a configurable hard-
event (either unblocking or timeout) causes reactivation.             ware accelerator for task scheduling and context switching.
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.


                                           RISC-V Processor                      Memory    tasks are also updated and moved to the ready list during
                                                          Memory Interface Arbitration g
                                                                                           scheduling if their delay has expired. Additionally, the timer
  RTOSUnit
                                                  Pipeline                                 is reset only on timer interupts, that is, after its expiry. The
                          Enters ISR
                                                                                           ISR concludes with an mret instruction, resuming execution
      HW Scheduler        Instructions                F       D e X         M      W
                                                                                           in the selected task.
                          (custom, mret)
                          Read RF
        Store FSM b                                                                        4.2   Context Storing (S)
                                            Regfile
                          Switch RF                              d
                                                                         CSRs              The (S) configuration accelerates context storing by enabling
                                                                         mstatus           immediate scheduler execution after an interrupt, eliminat-
                                               RF1   RF2                 mepc
                                              (APP) (ISR)                mcause            ing the need for a software routine to first save the current
                                                                             c
                                                                 d
                                                                                           task’s context. Instead, we employ a latency-hiding scheme
                          Write RF
                                                                     a                     and decoupled hardware-assisted background context stor-
      Restore FSM f
                          RF write trace                                                   ing.
                      Read / Write CSRs                                                       To this end, we add an alternate register file bank to the pro-
                                                                                           cessor (cf. Figure 3 (a)), duplicating the 29 general-purpose
Figure 3. Proposed architecture overview. Added compo-                                     registers required for context representation. The mstatus
nents are shown in green; required processor changes in                                    and mepc CSRs do not require replication, since they do not
yellow. Depending on the configuration, the RTOSUnit ac-                                   need to be freed for executing the scheduler. Upon an in-
celerates scheduling and/or context switching via custom                                   terrupt, the processor frees the register file, making fresh
instructions. Additionally, the register file (RF) must be du-                             registers immediately available, while a dedicated hardware
plicated. Blue and magenta arrows indicate active datapaths                                finite-state machine (cf. Figure 3 (b)) asynchronously stores
during ISR and application execution, respectively. A generic                              the previous context to memory. This allows the scheduler
five-stage pipeline is shown, while depth and stage functions                              to execute without delay (cf. Figure 4 (e)). A similar tech-
differ between specifically supported processors.                                          nique has been validated by Balas et al. [3], but we introduce
                                                                                           additional optimizations to reduce hardware overhead:
                                                                                              (1) Balas et al. [3] employ snapshotting to immediately free
The design emphasizes fine-grained configurability, allow-                                 half of the register file upon interrupt entry. While effective,
ing users to offload context storing, loading, and/or sched-                               this approach copies all 16 freed registers in parallel into
uling to hardware. We select those features since they have                                a second register file bank within a single cycle, incurring
the largest impact on context switch duration and, hence,                                  substantial wiring overhead and routing congestion. To avoid
are prime targets for acceleration. We use a combination of                                the need for such additional register file ports, we instead use
letters to describe which functionality has been moved to                                  a sparse MUX structure combined with bank switching: only
hardware. (vanilla) refers to the baseline case solely imple-                              RF1 is connected to both the core and the RTOSUnit, whereas
mented in software, while (SLT) (Store Context, Load Context,                              RF2 is connected exclusively to the core (cf. Figure 3(d)). This
Task Scheduling) has all of these operations accelerated by                                reduced overhead allows us to duplicate the entire register
the RTOSUnit.                                                                              file efficiently, thereby freeing all general-purpose registers
   Unlike memory-mapped approaches, our RTOSUnit uses                                      upon interrupt entry.
custom instructions for tight integration with the processor,                                 (2) We remove the need for a second memory port by
minimizing latency and communication overhead. Table 1                                     arbitrating memory access between the processor and RTO-
summarizes the new instructions; Figure 3 outlines the archi-                              SUnit, where the processor has priority (cf. Figure 3 (g)).
tecture and interface required to support the full RTOSUnit                                Hence, both components share a single memory port, where
feature set. The interface is designed for minimal intrusion                               the RTOSUnit utilizes the dead/idle cycles not required by
into the processor and hence is specific to the use case. The                              the processor.
following subsections describe the necessary processor mod-                                   (3) We reserve a fixed memory region inside the proces-
ifications and internal RTOSUnit components by feature.                                    sor’s DMEM to save the tasks’ contexts and assign each task
                                                                                           a 32-word-sized chunk. Additionally, we introduce a unique
4.1     Baseline (vanilla)                                                                 task ID that allows for quickly deriving the address of the
In the baseline case, context switching is handled entirely                                assigned context memory by indexing the 32-word chunks
in software. It is triggered by an interrupt, which may come                               with said ID. Thus, the memory addresses of saved contexts
from a timer (time-slice expiration), software (voluntary                                  remain constant, avoiding updating the saved contexts’ mem-
yield), or external events. Upon an interrupt, the proces-                                 ory address in the TCB in contrast to (vanilla) (cf. Figure 4
sor enters an ISR, where the current task’s context is saved                               (b)). The size of this fixed memory region defines an up-
to its stack (cf. Figure 4 (a)), a ready task is selected (c), and                         per bound for the task count, but can be chosen arbitrarily.
its context is restored (d). If triggered by a timer, delayed                              Since we use 32-word-sized chunks, we over-provision by
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                                                      Markus Scheck, Tammo Mürmann, & Andreas Koch


                          Vanilla:                                      (S) configuration:                             (SL) configuration:

                          [...] # Current task                          [...] # Current task                           [...] # Current task

                          ISR:                                          ISR:                          e                ISR:                          f
                            addi sp, sp, -30 * 4                          [...] # Check for exception                    [...] # Check for exception
                       a    sw    x1, 0 * 4(sp)                           [...] # Update delayed tasks                   [...] # Update delayed tasks
                            sw    x5, 1 * 4(sp)                           [...] # Scheduling    ctxId                    [...] # Scheduling    ctxId
                  save context


                            [...]                                         SET_CONTEXT_ID ctxId                           SET_CONTEXT_ID ctxId
                            sw   x31, 27 * 4(sp)                          [...] # Update currentTCB                      [...] # Update currentTCB
                            csrr x5, mstatus                              SWITCH_RF                                      mret
                            sw    x5, 28 * 4(sp)
                            csrr x5, mepc                                              lw    sp, currentTCB    ctxAddr [...] # Next task
                            sw    x5, 29 * 4(sp)                                       lw    x5, 30 * 4(sp)
                            sw    sp, currentTCB ctxAddr b                             csrw mepc, x5                   (SLT) configuration:




                                                                     restore context
                                                                                       lw    x5, 29 * 4(sp)
                               [...] # Check for exception                             csrw mstatus, x5                [...] # Current task
                               [...] # Update delayed tasks                            lw   x31, 28 * 4(sp)
                             c [...] # Scheduling                                      [...]                           ISR:                         g
                               [...] # Update currentTCB                               lw    x5, 2 * 4(sp)               [...] # Check for exception
                                                                                       lw    x1, 1 * 4(sp)               GET_HW_SCHED
                                    lw    sp, currentTCB   ctxAddr                     lw    sp, 0 * 4(sp)               [...] # Update currentTCB
                       d
                                    lw    x5, 29 * 4(sp)                               mret                              mret
                                    csrw mepc, x5
                  restore context




                                    lw    x5, 28 * 4(sp)                [...] # Next task                              [...] # Next task
                                    csrw mstatus, x5
                                    lw   x31, 27 * 4(sp)
                                    [...]
                                    lw    x5, 1 * 4(sp)                                                                    Alternate ISR register file is in use
                                    lw    x1, 0 * 4(sp)                         x2 = sp
                                    addi sp, sp, 30 * 4                         x3 = gp (static, not saved)                Decoupled HW context saving
                                    mret                                        x4 = tp (static, not saved)
                                                                                                                           Decoupled HW context restoring
                          [...] # Next task


Figure 4. ISR execution in software and hardware across different RTOSUnit configurations. As more features are offloaded,
the software ISR shortens and hardware parallelism increases. With full acceleration, the ISR only updates currentTCB.

one word (a context consists of 31 words), but this simplifies                                          Integration-wise, (S) requires modifications to the instruc-
the address generation to just shifting the task ID to the left                                      tion decoding of the processor, such that the added custom
and prepending the offset of the context memory region.                                              instructions are known and reported to the RTOSUnit. In
   In addition to the logic required for actually storing the                                        addition to custom instructions, interrupt entries must be
context, the RTOSUnit must be informed where to store                                                traced as well. Furthermore, the register file must be dupli-
the context. To this end, we introduce a custom instruction,                                         cated, (sparsely) multiplexed, and connected to the RTOSUnit.
SET_CONTEXT_ID, which is issued after the software scheduler                                         Lastly, the RTOSUnit must be connected to the CSRs of the
selects the next context. The instruction passes the selected                                        processor.
tasks ID to the RTOSUnit, which latches the ID and uses it
during the subsequent context switch to generate the store
address by indexing into the context memory region. Alter-
natively, the full context address could be passed to achieve                                        4.3      Context Loading (SL)
the same effect. However, because our later scheduling con-                                          Context loading is implemented similarly to context storing
figurations operate exclusively on IDs, we use IDs here for                                          by adding a restore FSM (cf. Figure 3 (f)). Once the custom
consistency.                                                                                         instruction SET_CONTEXT_ID announces the next task ID, the
   In theory, context storing completes in 31 cycles, plus any                                       RTOSUnit triggers its restore FSM to load the new context
stalls caused by prioritized processor memory accesses. In                                           concurrently (cf. Figure 4 (f)). This FSM performs the inverse
practice, this latency is hidden by the significantly longer                                         of the store FSM: It loads the context from memory and
time required to execute the scheduler concurrently.                                                 writes it into the application register file and CSRs.
   After scheduling, the context of the selected task must be                                           In this (SL) mode, the instruction SWITCH_RF for switching
restored. Due to our sparse MUX structure, the processor                                             back to the application register file is no longer required.
must actively switch back to the application register file us-                                       Instead, the register file bank switch is handled automatically
ing the SWITCH_RF custom instruction. This switch-back gets                                          during the mret instruction, which ends the ISR and resumes
delayed while storing is in progress. While the (S) configu-                                         execution at the saved address in mepc. The mret instruction
ration never actually required such stalling in our testing,                                         is stalled until the context restore completes, preventing a
we have implemented it to ensure correctness and support                                             premature return to task execution before restoration has
advanced configurations which are introduced later.                                                  finished.
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.


  Delay List                                                                         On timer interrupts (detected via mcause), all delay coun-
  Sorted by Delay Ticks, then Priority f
                                                                                  ters are decremented (e). The delay list is sorted by remaining
       Task ID               Task ID                  Task ID
                                                                                  delay time, with ties broken by priority (f). When a task’s
        Priority             Priority                 Priority    Priority        delay expires, it is moved to the ready list automatically (g).
                                                                              d
                                                                  Delay Ticks
       Valid Bit            Valid Bit                 Valid Bit                      In (T) mode, the custom instruction GET_HW_SCHED replaces
      Delay Ticks         Delay Ticks             Delay Ticks                     SET_CONTEXT_ID and returns the head of the ready list while
                                                                  Timer e         moving that entry to the tail of the ready list (h). To further re-
                         Decrement Ticks                          Elapsed         duce the ISR execution time, we modify the RISC-V hardware
                         If Ticks == 0, add to Ready List g
  Ready List                                                                      timer – used exclusively for preemptive scheduling inter-
  Sorted by Priority b
                                                                                  rupts – to auto-reset, eliminating the need for software-based
       Task ID               Task ID                  Task ID                     counter reads and compare register updates. Thus, given
                                                                  Task ID a
        Priority             Priority                 Priority
                                                                  Priority
                                                                                  the configuration (SLT), the ISR reduces to merely updating
       Valid Bit            Valid Bit                 Valid Bit                   currentTCB (cf. Figure 4 (g)). Since our hardware scheduling
                                                                                  uses task IDs rather than TCB pointers, we include a soft-
                                                                  Remove ID c
                     Invalidate entries matching ID                               ware lookup table to map each task ID to its corresponding
                                                                  Get head h
Re-queue task to end of queue to implement round-robin behavior                   TCB, enabling the required update of currentTCB.
                                                                                     Processor modifications for (T) are minimal: support for
Figure 5. Hardware scheduler architecture. Magenta indi-                          custom instructions, interrupt source tracing, and auto-reset-
cates events triggered by custom instructions; orange marks                       ting of the hardware timer. Note that the maximum number
external events. The hardware scheduler implements the                            of tasks is fixed at design time by the hardware queue size.
FreeRTOS ready and delay lists, maintaining correct order                         However, once this threshold is exceeded, the system can
via iterative sorting. Timer interrupts decrement delay coun-                     fall back to software scheduling at the cost of forfeiting the
ters, moving tasks to the ready list when reaching zero.                          benefits of hardware scheduling.
                                                                                     The presented features allow for the following configura-
                                                                                  tion permutations: (S), (SL), (T), (ST), (SLT). We additionally
  Integration-wise, (L) only works in conjunction with (S),                       support optional features for further mean-case latency re-
and additionally requires mret tracing and stalling signals to                    duction, though at the expense of larger jitter. These options
be added to the processor.                                                        are dirty bits (D), load omission (O), and preloading (P).

4.4     Task Scheduling (T)                                                       4.5   Dirty Bits (D)
Hardware-based task scheduling can be used independently                          Dirty bits accelerate context storing by avoiding unnecessary
or alongside context storing and loading. In (T) mode, FreeR-                     memory writes when tasks modify only a subset of regis-
TOSes ready and delay lists are moved to hardware, while                          ters. To exploit this, each register in the application register
the event list remains in software, as the RTOSUnit does not                      file is augmented with a dirty bit, which is set upon write.
manage synchronization primitives. Three custom instruc-                          During context storing, the RTOSUnit saves only the regis-
tions are added: (1) ADD_READY to add a task to the ready list,                   ters marked as dirty. After ISR completion, all dirty bits are
(2) ADD_DELAY to add the running task to the delay list, and (3)                  cleared. Integrating the (D) option requires a register write
RM_TASK to remove a task from either hardware-maintained                          tracing interface within the processor.
list. Figure 5 illustrates the hardware scheduler structure.
Adding a task to the ready list requires its ID and priority                      4.6   Load Omission (O)
(a). The list is iteratively sorted in hardware by priority, pre-                 Load omission skips context loading if the previous and next
serving the order among tasks with identical priorities (b).                      selected tasks are identical. In this case, the application reg-
Entries marked as invalid are sorted toward the tail of the                       ister file already contains the correct context, eliminating
list. Our prototype uses bubble sort as the sorting algorithm                     the need to reload it from memory.
because it has low area requirements and a sufficient time
elapses between inserting a task and querying the head of                         4.7   Preloading (P)
the list. For larger numbers of tasks in the system (and a                        When context storing, loading, and scheduling are all en-
correspondingly longer ready list), faster algorithms may be                      abled, the ISR reduces to updating currentTCB based on the
necessary to avoid stalls.                                                        next scheduled task ID. In this case, the context-switch la-
   Task removal is achieved by matching the task ID provided                      tency is bounded by the memory bandwidth.
by the RM_TASK instruction and clearing the valid bit of all                         Since hardware scheduling exposes the head of the ready
matching entries (c). Since only the currently running task                       list – the task with the highest priority – it can be accessed
can be delayed, ADD_DELAY requires only the task’s priority                       even outside the ISR. This enables an optional context preload-
and delay duration as parameters (d).                                             ing mechanism that speculatively loads the context of the
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                   Markus Scheck, Tammo Mürmann, & Andreas Koch


task at the head of the ready list, which is likely to run next.    pipeline flushes), allowing the switch to occur without addi-
A dedicated 31-word buffer is used to hold this preloaded           tional control logic. Additionally, SWITCH_RF must be delayed
context. Because the RTOSUnit accesses memory at a lower            while context storing is in progress to prevent premature
arbitration priority than the processor, preloading does not        RF switching and the resulting overwriting of data that has
interfere with ongoing computation, as long as no cache is          not yet been stored. We observed the need for such stalling
used, or, if one is, the RTOSUnit can bypass it.                    logic only in the (ST) and (SDT) configurations. In the remain-
   Preloading operates in lockstep with context storing: as         ing configurations that use SWITCH_RF, software scheduling
each register is saved to memory, it is immediately over-           introduces sufficient delay for context storing to complete.
written with the corresponding preloaded value. Due to this         Nevertheless, we implement this stalling mechanism in all
coupling, preloading is incompatible with the dirty-bit op-         configurations employing SWITCH_RF to ensure correctness.
tion.                                                                  The location of the memory arbitration in the pipeline
   Since tasks may enter the ready list immediately following       also affects integration complexity, particularly in the pres-
an interrupt, for instance, when a timer interrupt unblocks         ence of a cache. If arbitration occurs within the LSU, the
delayed tasks, the speculatively preloaded context may not          RTOSUnit can share the processor’s cache, potentially reduc-
match the actual next task. In such cases, the correct context      ing context-switch latency for cached contexts, especially
must be loaded from memory, resulting in the same latency           with high-latency memory. However, this may also increase
as if preloading had not been used in the first place.              worst-case latency and jitter due to additional cache miss
   If the preloaded context is correct, context loading can         penalties. In turn, arbitration at the bus-level leaves more
be skipped entirely, significantly reducing context-switch          request cycles available for the RTOSUnit, since some of
latency.                                                            the processor’s requests now get handled by the cache, and
                                                                    potentially reduces jitter by eliminating cache hit/miss vari-
                                                                    ability. However, bus-level arbitration increases the mean
5    RTOSUnit Integration                                           latency.
While the main functionality resides within the RTOSUnit,              The remainder of this section discusses the integration
integrating it with a processor requires several architectural      of the RTOSUnit into three representative RISC-V micro-
modifications. Specifically, the register file must be replicated   architectures of increasing complexity.
and multiplexed (cf. Figure 3 (d)), custom instructions must
be integrated (cf. Figure 3 (e)), interrupt and mret signaling      5.1   CV32E40P / Pipelined In-Order Processor
must be managed, CSR access must be handled, and memory             Among the evaluated cores, CV32E40P has the simplest ar-
arbitration must be implemented (cf. Figure 3 (g)).                 chitecture. It features a 4-stage pipeline that executes in-
    We identified three micro-architectural features and two        structions strictly in order, with no instruction reordering
integration decisions that significantly affect integration         or register renaming. As it lacks a cache, the LSU directly
complexity: instruction reordering, speculative execution,          interfaces with the memory bus, and arbitration requires
register renaming, configuration of the RTOSUnit, and mem-          only simple multiplexers on the outgoing memory signals.
ory arbitration at the bus or Load/Store Unit (LSU) level. All         Although CV32E40P performs speculative instruction fet-
proposed custom instructions (cf. Table 1) update the RTO-          ches, branches are resolved early, and mispredicted instruc-
SUnit’s internal state and therefore must execute in-order and      tions are never executed. Thus, no additional handling for
non-speculatively. More complex cores commonly reorder              speculation is required. In configurations using the SWITCH_RF
and speculatively issue instructions to improve instruction-        instruction, no additional hazard logic is needed due to the
level parallelism. Additionally, they use register renaming to      shallow pipeline and lack of speculation. The switch can
eliminate false dependencies (i.e., WAW and WAR hazards),           safely occur during operand reading.
which may complicate register file replication and RTOSUnit
interfacing.                                                        5.2   CVA6 / Out-of-Order WB
    Among all custom instructions, SWITCH_RF is unique in that      Like CV32E40P, CVA6 issues instructions in-order and em-
it alters the processor’s microarchitectural state by switching     ploys only limited speculation, enabling the RTOSUnit to
to the application register file. Since this change affects sub-    be integrated as a standard functional unit. However, unlike
sequent instruction execution, it requires hazard-handling          CV32E40P, CVA6 incorporates register renaming to enable
logic: all prior instructions must commit to the ISR RF, while      out-of-order write-back. This renaming is handled inside
all following instructions must utilize the APP RF. Depend-         the scoreboard module, leaving the register file to contain
ing on the pipeline depth, both prior and following instruc-        committed values only. Thus, the RTOSUnit can access the
tions may be in the pipeline simultaneously. If both storing        architectural state directly, making register file replication
and loading are enabled, register banks are switched au-            and multiplexing as straightforward as in CV32E40P.
tomatically upon interrupt entry and exit (using the mret              Configurations using the SWITCH_RF instruction require ex-
instruction). These events act as synchronization points (e.g.,     tra logic to avoid hazards, as instructions prior to SWITCH_RF
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.


must write to the ISR RF while those following SWITCH_RF al-               tapped into, and the translated address is forwarded to the
ready read from the APP RF, with both potentially coexisting               register file.
in the pipeline during a switch.                                              To provide a dedicated register file for the ISR, the physical
   Despite CVA6 using a write-through cache, we chose to                   register file and translation logic were duplicated (c). Notably,
arbitrate the memory at the bus level to reduce jitter.                    NaxRiscv allocates physical registers only upon first write.
                                                                           As a result, the alternate register file can be smaller than the
                                                                           full 32 registers, depending on actual usage.
5.3   NaxRiscv / Full Out-of-Order Execution                                  We found that as few as eleven registers were sufficient to
While integrations with CV32E40P and CVA6 were straight-                   execute our FreeRTOS benchmarks. However, such a reduc-
forward, adapting the RTOSUnit to the more complex Nax-                    tion in the register file size inherently imposes constraints on
Riscv core required additional considerations, due to its fully            the code that can be executed during an ISR. For maximum
out-of-order execution model, aggressive speculative execu-                generality, we thus chose a full-size (32 registers) alternate
tion, and register renaming. As we will show, our RTOSUnit                 register file for ISR use that still allows for arbitrary code
can also be integrated with such a complex base core.                      execution in the ISR.
   Unlike CVA6, which only reorders write-back, NaxRiscv                      Finally, the RTOSUnit’s memory interface must be arbi-
issues and executes instructions out-of-order and specula-                 trated with the processor’s memory requests. Due to the
tively. Since custom instructions irrevocably affect RTOSUnit              large and complex architecture of NaxRiscv, it is more likely
state, they must execute strictly in program order and non-                to be connected to high-latency memories, such as exter-
speculatively. To this end, we introduced a queue that buffers             nal DRAM. Therefore, we extended the core’s LSU to al-
custom instructions (cf. Figure 6) while their final specu-                low the RTOSUnit to share the processor’s write-back cache
lation status (correctly speculated or misspeculated) is not               (cf. Figure 8). The existing LSU includes speculative load
yet known. We preserve the program order by inserting the                  (a) and store (b) queues, which are flushed on mispredic-
custom instructions into this queue during the in-order de-                tion. Given the non-speculative nature of the RTOSUnit’s
code stage (a). Once issued, the resolved argument values                  requests, a dedicated queue, designated as ctxQueue (c), was
(RS1, RS2) are added alongside the buffered instructions (b).              added here. This approach avoids alterations in the core’s
If the instruction turns out to be on a misspeculated path,                flushing logic and significantly simplifies the prioritization
the queue is flushed (c). Otherwise, when the instruction                  of non-RTOSUnit memory requests (d).
reaches the commit stage (d), it is passed to the RTOSUnit (e)                The ctxQueue combines the functionalities of the load and
for execution, preserving program order and correctness. We                store queues. It supports out-of-order memory access and
found a short queue size of just two entries to be sufficient              uses a simple tag to indicate load or store (e). Each entry
to prevent pipeline stalling due to a full queue. Alternatively,           contains address and data fields (f). Since the RTOSUnit
one could stall the decode stage to ensure correct execu-                  expects the read responses in the same order as the requests,
tion and ordering of our custom instructions. However, this                we use the data field to latch the cache’s result until it can
would also block all subsequent normal instructions, whereas               be forwarded to the RTOSUnit. To balance performance and
the addition of the queue allows for out-of-order execution                area, we evaluated different queue sizes and identified eight
without stalling the pipeline.                                             entries as a Pareto-optimal solution. Further reducing the
   Register renaming, as explained in Figure 7, enables Nax-               queue size would negatively impact context-switch latency,
Riscv’s instruction reordering. In contrast to CVA6, renaming              while larger sizes offer no performance gain.
is not concealed within a scoreboard. Instead, NaxRiscv is                    Entires in the ctxQueue are allocated and freed in-order,
equipped with a larger physical register file capable of storing           and the RTOSUnit issues load and store addresses in identical
more than the 31 writable architectural registers (a). In this             order. Given this behavior, a load and store (or two stores)
fashion, the register file is employed to store both the current           to the same address could coexist in the queue only for a
architectural state and as-yet uncommitted values. In order                queue depth of at least 32 entries. With our chosen depth of
to differentiate between the current state and uncommitted                 eight, such aliasing cannot occur. Hence, store-to-load for-
results, as well as to translate an architectural register to the          warding and handling of aliasing stores within the ctxQueue
correct physical register, NaxRiscv contains additional logic              are unnecessary, ensuring safe out-of-order operation. These
(b) to keep track of the mapping between the architectural                 assumptions may be violated when dirty bits are used, in
and physical registers.                                                    which case context loading is stalled until the ctxQueue is
   As explained in Subsection 4.2, for a context-switch, 29                empty.
architectural registers must be saved and restored from mem-                  The design assumes exclusive access to the context mem-
ory. Given the RTOSUnit’s micro-architecture independence,                 ory region by either the core or the RTOSUnit at any given
it uses architectural register addresses (c), which must be                time. In consequence, no snooping or store-to-load forward-
translated into physical addresses before accessing the phys-              ing needs to be implemented between the ctxQueue and
ical register file. To this end, the existing translation logic is         NaxRiscv’s existing queues. When both context loading and
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                                                   Markus Scheck, Tammo Mürmann, & Andreas Koch


    Pipeline                                   out-of-order
                                                                                                            LSU                  RTOSUnit
                                                                                                                                        c
       F              D               I             X                W                  C
                                                                                                            storeQ a              ctxQueue              b loadQ
                 a                         b                                                                address                     address           address
                                          provide                                                          write data
                                                                                                                                 S     write data               ...
     insert custom                                            RTOSUnit
                                          instruction                                                                             e         f                 address
        instruction                       args                                                                 ...               ...        ...
                                                              args e               d
                                                                                                            address                     address
      c flush             Instruction queue                         isCommitted?                           write data
                                                                                                                                 L     resp data

                                                                                                                       write resp                 read resp
Figure 6. Integration of RTOSUnits’ custom instructions
into a full out-of-order core. The instruction queue is used
to collect out-of-order issued custom instructions (a)/(b) and                                             d                          Data                            d
passes them in-order to the RTOSUnit (e).                                                                            write req       Cache           read req


                                                  Physical Registers
                                  physAddr
             Arch to Phys b                                RF1                  RF2             Figure 8. Overview of the required changes to NaxRiscv’s
           Register Mapping                               (APP)      a         (ISR)        d
                                                                                                LSU to arbitrate memory requests from the RTOSUnit. Green
      c                           d                     0                0                      blocks show added logic. The ctxQueue (c) handles memory
     archAddr                                           ...   ...        ...      ...           requests from the RTOSUnit, enabling out-of-order execu-
               RTOSUnit            data                 63               31                     tion. Multiplexers (d) arbitrate between core and RTOSUnit,
                                                                                                prioritizing the core’s loadQ and storeQ.
Figure 7. Overview of NaxRiscv’s renaming architecture.
The 64-entry physical register file (a) holds committed and
in-flight values. The register-mapping (b) translates architec-                                 NaxRiscv, the dedicated port bypasses the write-back data
tural to physical registers (e.g., for the RTOSUnit (c)) and dis-                               cache. To prevent cache incoherence, the single cache line
ambiguates between committed and in-flight values. Green                                        containing the bypassed 16 words is explicitly invalidated.
shaded boxes indicate added logic for the ISR register file                                     The remaining context is saved in software and thus nor-
and RTOSUnit integration.                                                                       mally cached. We also tested flushing the cache and mark-
                                                                                                ing the context region as uncacheable; however, both ap-
                                                                                                proaches resulted in significantly worse latency compared
storing are performed in hardware, only the RTOSUnit exclu-                                     to the software-only (vanilla) solution. In contrast, all other
sively accesses the reserved context memory. If only context                                    configurations of NaxRiscv arbitrate within the LSU, as de-
storing is enabled, the core loads contexts in software but                                     scribed in Subsection 5.3, and do not require any cache inval-
never writes to the context memory region. If the sched-                                        idation, while allowing the entire context to be cached. All
uler immediately switches back to the suspended task, the                                       hardware list lengths are set to eight entries unless explicitly
SWITCH_RF instruction waits for all pending stores in the                                       mentioned otherwise.
ctxQueue to complete, ensuring that context saving finishes
before the register file is switched and the same context is                                    6.1   Context-Switch Latency
reloaded in software. After the SWITCH_RF instruction has                                       We evaluate context-switch latency in RTL simulation using
executed, a reschedule event is triggered, similar to a mispre-                                 20 iterations of all tests provided by the RISC-V port of RTOS-
diction, to re-execute the instructions following SWITCH_RF                                     Bench [4]. The ready and delay lists are configured with eight
on the switched register file.                                                                  entries each. The context load omission optimization (O), de-
                                                                                                scribed in Subsection 4.6, is enabled only for configurations
6    Evaluation                                                                                 that use dirty bits (D) or preloading (P), as these techniques
We assess RTOSUnit’s performance, silicon area, and power                                       already trade lower average latency for increased variability,
overheads as 22nm ASICs using the letter-based configu-                                         making them unsuitable for real-time applications that need
ration naming scheme introduced earlier: Store (S), Load                                        tighter time bounds. All cores are assumed to use tightly
(L), Task Scheduling (T), Load Omission (O), Dirty Bits (D),                                    coupled, single-cycle latency on-chip SRAM. Latency is mea-
and Preloading (P). As before, (vanilla) refers to an unmod-                                    sured from interrupt trigger to the execution of the mret
ified processor. Additionally, we provide a direct compar-                                      instruction, and jitter is reported as the difference between
ison with related work (CV32RT) [3]. To this end, we have                                       maximum and minimum latencies. We report latency in cy-
re-implemented their CV32E40P-only solution for CVA6 and                                        cles rather than time at 𝑓𝑚𝑎𝑥 , as embedded systems typically
NaxRiscv. In all three (CV32RT) variants, a dedicated mem-                                      operate at much lower frequencies to optimize for area and
ory port is used to save 16 of the 31 words of a context. For                                   power consumption. The results are aggregated in Figure 9.
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.




Figure 9. Context-switch latencies with different processors and configurations. Store (S), Load (L), Task Scheduling (T), Load
Omission (O), Dirty Bits (D), and Preloading (P). (CV32RT) corresponds to the approach by Balas et al. [3]. Δ is the jitter, as the
difference between the measured worst- and best-case switching latency. 𝜇 represents the average switching latency. Note that
some WCET values, marked with an 𝑥, greatly exceed the y-axis limit.

   Compared to software context switching (vanilla), the                   due to altered memory access patterns due to the dirty bit
approach of Balas et al. (CV32RT) achieves only modest aver-               optimization.
age latency reductions, ranging from 3 % to 12 %. In contrast,                The fastest average switching latency is achieved using
our simplest configuration (S) yields substantially larger im-             our preloading strategy (SPLOT). Selecting the head of the
provements, between 17 % and 27 %. This improvement is                     ready list proves to be a simple yet effective heuristic for
likely attributable to our ability to overlap the entire context           preloading the next context. Results fall into two clusters
saving with code execution, whereas the approach of Balas                  of similar size: one with higher latency due to mispredicted
et al. accelerates only half of the context saving in hardware.            preloads, and another with lower latency from correctly
In both cases, the jitter remains comparable.                              predicted contexts, where latency is reduced by up to 31
   Across all cores, offloading context storing and loading                cycles compared to (SLT). For mispredicted preloads, (SPLOT)
to hardware (ST) reduces mean latency while maintaining                    performs comparably to (SLT).
similar jitter. This offloading removes overhead from the
processor, while the inherently variable-latency scheduler
remains in software. Offloading scheduling alone to hard-
ware (T) significantly reduces jitter, especially on CV32E40P,
dropping from 188 to just 16 cycles, a reduction of more                   6.2   Worst-Case Timing Guarantees
than 90 %. Average latency is reduced by 23 %, 29 %, and 9 %               Due to our decoupled context-switch acceleration, the WCET
for CV32E40P, CVA6, and NaxRiscv, respectively. Moving                     of the ISR is the maximum latency required either by the
both context-switching and scheduling (SLT) to hardware                    RTOSUnit or the remaining software in the ISR. To calcu-
minimizes both latency and jitter. On CV32E40P, jitter is elim-            late the worst-case software latency, we analyze the longest
inated entirely, while jitter on CVA6 and NaxRiscv is reduced              instruction path, assuming maximum latency for every in-
by up to 88 %. The remaining jitter is likely due to micro-                struction and accounting for pipeline flushes and stalls due
architectural features like caches and speculative execution,              to dependencies. For software-scheduled setups, we assume
which cannot be directly influenced by our approach. In the                that eight tasks are delayed and must be moved to the ready
(SLT) configuration, context storing and restoring become                  lists by the ISR. Note that larger task counts would yield even
the primary bottlenecks, as the remaining ISR operations re-               higher worst-case latencies. For RTOSUnit FSM latency, we
quire fewer than 62 cycles, the minimum to fully hide context              analyze both the hardware and ISR code, considering stalls
handling.                                                                  from processor memory accesses.
   Enabling dirty bits and load omission (SDLOT) yields a                     Figure 9 shows the worst-case context-switch latencies
modest reduction in mean latency at the expected cost of                   for CV32E40P. Our approach significantly reduces WCET:
increased jitter. For CV32E40P, some cases show latency be-                for instance, the baseline shows a WCET of 1649 cycles,
low 50 cycles. Omitting hardware scheduling in (SDLO), dirty               while offloading context-switching (SL) or task scheduling (T)
bits and load omission show no improvement over (SL), sug-                 reduces it to 1442 cycles and 202 cycles, respectively. When
gesting that switching time is more influenced by software                 offloading both to hardware (SLT), the WCET is reduced to
scheduling than memory bandwidth. Jitter remains largely                   70 cycles, matching the measured context-switch latency.
unchanged, except for a minor deviation on NaxRiscv, likely                   Due to the complexity of CVA6 and NaxRiscv, WCET anal-
                                                                           ysis for these cores is considered out of scope. Their latencies
                                                                           are highly dependent on factors such as cache configurations
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                    Markus Scheck, Tammo Mürmann, & Andreas Koch


and memory access patterns, which makes WCET predic-                 (SPLOT). This higher overhead stems from NaxRiscv’s use of
tions potentially overly pessimistic. WCET analysis for out-         register renaming: snapshotting cannot just rely on static
of-order cores remains an ongoing research topic. Li et al.          addresses to access the register file and therefore requires
[13] propose a method for such an analysis, which requires           the costly addition of 16 extra read ports. As with CV32E40P,
very precise modeling of the core’s pipeline.                        enabling the (T) configuration does not introduce measurable
                                                                     area overhead. In CVA6 and NaxRiscv, further acceleration
6.3   ASIC                                                           of scheduling and context switching increases area by up
We assess the area and maximum frequency of our designs              to 8 % and 13 % respectively, rising to 14 % and 15 % when
using commercial ASIC tools (EDA), targeting a 22 nm tech-           preloading is also enabled.
nology node, and perform the implementation down to chip                The added pipeline hazard handling logic required for
layout for accurate power analysis. To ensure stable area            the SWITCH_RF custom instruction in (S), (SD), (ST), and (SDT)
measurements, the target frequency is fixed at or above the          on CVA6 leads to area increases beyond the corresponding
maximum frequency achievable by each unmodified base                 configurations with hardware context loading support ((SL),
core. Timing overheads for our RTOSUnit are reflected as             (SDLO)), (SLT), and (SDLOT)). NaxRiscv exhibits the opposite
negative worst-case setup slack, as reported by the EDA tools.       behavior, where omitting hardware context loading results in
These slowdowns are reported as 𝑓max drops. For NaxRiscv,            reduced area overhead, as those hazards are instead handled
we manually instantiate optimized process-specific SRAM              through pipeline rescheduling.
macros to support caches and branch prediction tables effi-             CVA6 experiences a slight frequency drop of around 8 %
ciently. However, their area is excluded from the reported           across configurations, while NaxRiscv maintains stable fre-
values to ensure a fair comparison, as the other cores are           quency throughout, with the exception of the (SPLOT) con-
evaluated without caches. Figure 10 shows the area of each           figuration, which exhibits a modest 4 % drop.
core alongside all supported RTOSUnit configurations, while             We also evaluate how area overhead scales with increas-
Figure 11 presents the corresponding maximum frequencies             ing lengths of the ready and delay lists. We synthesize the
achieved. Although these GHz frequencies are technically             CV32E40P core with hardware scheduling only (T), varying
achievable, frequencies in the hundreds-of-megahertz range           the number of slots in both lists while keeping them equal in
are more common in embedded systems, given their smaller             length. Figure 12 presents the absolute area across different
area and power / thermal envelopes.                                  list sizes. A list length of zero denotes the unmodified core.
   Among the evaluated designs, CV32E40P is the smallest             While minor fluctuations appear, especially at smaller list
core and thus most sensitive to area changes. The (S) config-        sizes, most likely due to "heuristics noise", the area increases
uration increases area by 21.9 %. This is only slightly larger       approximately linearly, reaching a 14 % overhead at 64 slots.
than the 21.2 % area increase observed for (CV32RT), while (S)          Lastly, we evaluate the power impact of our RTOSUnit. We
improves the average latency by 27 %, compared to (CV32RT)’s         perform highly accurate gate-level simulations on the actual
12 %. Introducing hardware scheduling (T) adds no observable         chip layouts for the mutex_workload test on all cores running
area overhead compared to (vanilla). However, combining              at 500 MHz. This approach is chosen because it includes the
scheduling with context-storing (ST) results in a 33 % area          precise signal transitions of an actual workload execution,
increase. Adding context-loading functionality (SLT) intro-          and not just estimated average toggle rates, and thus reflects
duces negligible additional overhead, while the integration          a realistic operating scenario. Using the resulting logic wave-
of dirty bits falls within the typical range of "heuristics noise"   forms and implemented ASIC designs, we derive the average
of the algorithms in the EDA tools. The (SPLOT) configura-           power consumption using industry-standard commercial
tion, which implements context preloading via dedicated              power analysis EDA tools. The results, shown in Figure 13,
registers, increases area by approximately 44 %. Across all          reflect average power draw over the full workload duration,
configurations, except for (CV32RT), CV32E40P experiences            rather than isolating the individual context-switching phases.
a frequency drop of roughly 15 %, yet remains well above             This approach is motivated by the fact that total power con-
typical operating frequencies for small embedded systems,            sumption across the processor’s operation, not just during
its primary target domain.                                           context switches, influences implementation decisions in
   CVA6 and NaxRiscv, being larger cores, exhibit similar            low-power designs. For NaxRiscv, we excluded the caches’
but less pronounced trends. For CVA6, (S) results in a 3-            SRAM power draw from reports to ensure a fair comparison.
5 % area increase. For NaxRiscv, the overhead reaches up to             As expected for the 22 nm technology node (and below!),
15 %, likely due to the duplication of register renaming logic       we observe a strong correlation between area and power
in addition to the alternate register file. (CV32RT) shows an        draw, primarily due to the impact of static power draw.
overhead of 2 % for CVA6. Overhead remains lower than for            For CV32E40P and CVA6, (CV32RT) exhibits the lowest addi-
CV32E40P, as routing congestion proves less critical during          tional power consumption. In the case of CVA6, the power
synthesis. In contrast, NaxRiscv exhibits an area increase of        consumption of the (S) configuration is nearly identical to
19 %, which exceeds that of the most advanced configuration          that of (CV32RT), while providing a substantial improvement
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.




Figure 10. Normalized ASIC area w.r.t. the baseline of the processors under different RTOSUnit configurations. Store (S), Load
(L), Task Scheduling (T), Load Omission (O), Dirty Bits (D), and Preloading (P). (CV32RT) corresponds to the approach by Balas et
al. [3]. The absolute area is reported above the bars.




Figure 11. ASIC 𝑓𝑚𝑎𝑥 of the base processors under different
configurations.




                                                                           Figure 13. Power estimation under different RTOSUnit con-
                                                                           figurations.


                                                                           power increase of up to 33 %. Due to NaxRiscv’s higher base-
Figure 12. ASIC area scaling with increasing scheduler list
                                                                           line power usage, the relative increase is more modest, up to
length.
                                                                           13 % (excluding (CV32RT)).

in context-switch latency. For NaxRiscv, the scheduling-                   6.4   Configuration Selection
only configuration (T) incurs the lowest additional power                  The choice of configuration depends on the relative impor-
consumption, with less than 2 mW. With the exception of                    tance of latency, jitter, and area overhead in the target appli-
NaxRiscv, the highest power draw is observed for the (SPLOT)               cation. No single configuration dominates across all metrics;
configuration. For NaxRiscv, however, the (CV32RT) config-                 instead, each represents a different point in the design space.
uration exhibits the highest power consumption, as it also                    The (SLT) configuration emerges as a strong all-round solu-
results in the largest area overhead. While CV32E40P’s rel-                tion. It provides substantial improvements in average latency
ative power increases by up to 72 %, the absolute increases                and jitter while maintaining predictable behavior across
remain small. CVA6 shows a similar trend, with a relative                  workloads. However, these benefits come at a noticeable
ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.                                     Markus Scheck, Tammo Mürmann, & Andreas Koch


area cost, which may limit their applicability in highly area      Similarly, context storing and loading could be extended to
(cost)-constrained systems. When average context-switch            support floating-point or vector registers.
latency is the primary optimization goal and area overhead            Finally, although similar in architecture, supporting addi-
is only of secondary concern, the (SPLOT) configuration is the     tional real-time operating systems such as Zephyr [28] or
preferred choice. By enabling context preloading, it achieves      ChibiOS [21] would further broaden the scope and adoption
the lowest average latency among all evaluated designs, al-        potential of the design in embedded environments.
beit with the highest area and power overheads.
    For systems where silicon area is the dominant constraint,     Acknowledgments
the scheduling-only configuration (T) represents an attractive     We thank our reviewers for their valuable feedback. We
option. It introduces virtually no area overhead while still       acknowledge the financial support by the German Federal
providing meaningful reductions in jitter and reasonable           Ministry of Research, Technology and Space in the project
improvements in latency. This makes (T) particularly well          “Scale4Edge” (grant: 16ME0139).
suited for cost-sensitive embedded designs.                           AI tools were used for language refinement.
    The (SL) configuration occupies an intermediate position
between (T) and (SLT). Compared to (T), it offers improved         References
average latency at the cost of a moderate area increase, while
                                                                    [1] David Andrews, Wesley Peck, Jason Agron, Keith Preston, Ed Komp,
jitter remains largely unchanged. Relative to (SLT), it reduces         Mike Finley, and Ron Sass. 2005. hthreads: A hardware/software
area overhead but also sacrifices some of the benefits in jitter        co-designed multithreaded RTOS kernel. In 2005 IEEE Conference on
reduction, making it suitable when latency improvements                 Emerging Technologies and Factory Automation, Vol. 2. IEEE, 8–pp.
are desired without fully committing to the cost of the more        [2] Arm Ltd. 2019. Registers in AArch32 state. https://developer.arm.com/
comprehensive solution.                                                 documentation/100076/0200/instruction-set-overview/overview-of-
                                                                        aarch32-state/registers-in-aarch32-state. Accessed: 2025-08-01.
    Overall, these configurations allow designers to trade off      [3] Robert Balas, Alessandro Ottaviano, and Luca Benini. 2024. CV32RT:
area, latency, and jitter in a controlled manner, enabling              Enabling Fast Interrupt and Context Switching for RISC-V Micro-
adaptation to a wide range of embedded real-time use cases.             controllers. IEEE Transactions on Very Large Scale Integration (VLSI)
                                                                        Systems 32, 6 (2024), 1032–1044. doi:10.1109/TVLSI.2024.3377130
                                                                    [4] Brannium. 2024.       RTOSBench.        https://github.com/Brannium/
                                                                        RTOSBench. Accessed: 2025-03-26.
                                                                    [5] Sathish Chandra, Francesco Regazzoni, and Marcello Lajolo. 2006.
7    Conclusion and Future Work                                         Hardware/software partitioning of operating systems: A behavioral
We introduced the RTOSUnit, a novel hardware accelera-                  synthesis approach. In Proceedings of the 16th ACM Great Lakes sym-
tion unit for tightly-coupled context-switching acceleration            posium on VLSI. 324–329.
in real-time systems. Our approach is portable, as demon-           [6] Elham Cheshmikhani and Hamed Farbeh. 2025. Interrupt Caching:
                                                                        A Hardware-Assisted Interrupt Handling to Enhance System Respon-
strated by integration into three RISC-V processors of vary-            siveness. IEEE Access (2025).
ing complexity. By its configurable architecture, the RTO-          [7] Intel Corporation. 1986. 80386 Programmer’s Reference Manual. Section
SUnit enables fine-grained trade-offs between area, mean                7.5: Task Switching.
context-switch latency, and jitter, making it easily adaptable      [8] Advanced Micro Devices. 2006. AMD64 architecture programmer’s
                                                                        manual volume 2: System programming. Section 12.3: Hardware Task-
for different application requirements.
                                                                        Management in Legacy Mode.
   Our most advanced hard real-time-focused configuration           [9] Eugen Dodiu and Vasile Gheorghita Gaitan. 2012. Custom designed
(SLT), which includes full context-switching and scheduling             CPU architecture based on a hardware scheduler and independent
acceleration, eliminates jitter entirely on some processors             pipeline registers—Concept and theory of operation. In 2012 IEEE
and achieves a mean latency reduction of up to 69 %, with               International Conference on Electro/Information Technology. IEEE, 1–5.
an area overhead of 31 %. Meanwhile, our most area-efficient       [10] Michael Gautschi, Pasquale Davide Schiavone, Andreas Traber, Igor
                                                                        Loi, Antonio Pullini, Davide Rossi, Eric Flamand, Frank Gurkaynak,
variant (T) has costs within EDA tool "heuristics noise", while         and Luca Benini. 2017. Near-Threshold RISC-V Core With DSP Ex-
reducing jitter by 91 % and lowering mean latency by 23 %.              tensions for Scalable IoT Endpoint Devices. doi:10.1109/TVLSI.2017.
Across all configurations, operational frequencies remain               2654506
within practical bounds for embedded deployments.                  [11] Winfried Grunewald and Theo Ungerer. 1996. Towards extremely fast
   While our RTOSUnit provides significant improvements                 context switching in a block-multithreaded processor. In Proceedings
                                                                        of EUROMICRO 96. 22nd Euromicro Conference. Beyond 2000: Hardware
for single-core FreeRTOS systems, its current design is limited         and Software Design Strategies. IEEE, 592–599.
to uniprocessor configurations. Future work could explore          [12] Intel Corporation. 2023. Intel® 64 and IA-32 Architectures Software
support for multi-core systems, extending RTOSUnit’s appli-             Developer’s Manual, Volume 3A: System Programming Guide, Part 1.
cability to multiprocessing scenarios.                                  Section 7.7: Task Management in 64-Bit Mode.
   Additionally, hardware acceleration of common synchro-          [13] Xianfeng Li, Abhik Roychoudhury, and Tulika Mitra. 2006. Modeling
                                                                        out-of-order processors for WCET analysis. Real-Time Systems 34, 3
nization primitives, such as the semaphores or mutexes ex-              (2006), 195–227.
amined in prior work, could further offload the processor          [14] L. Lindh. 1991. Fastchart-a fast time deterministic CPU and hardware
and reduce overhead in coordination-intensive workloads.                based real-time-kernel. In Proceedings. EUROMICRO ‘91 Workshop on
Co-Exploration of RISC-V Processors and FreeRTOS Extensions for Lower Context-Switch Latency ASPLOS ’26, March 21–26, 2026, Pittsburgh, PA, USA.


     Real-Time Systems. 36–40. doi:10.1109/EMWRT.1991.144077                    [22] Jeffrey S Snyder, David B Whalley, and Theodore P Baker. 1995. Fast
[15] Lennart Lindh and Frank Stanischewski. 1991. FASTCHART-Idea and                 context switches: Compiler and architectural support for preemptive
     Implementation. In Proceedings of the 1991 IEEE International Confer-           scheduling. Microprocessors and Microsystems 19, 1 (1995), 35–42.
     ence on Computer Design on VLSI in Computer & Processors (ICCD ’91).       [23] SpinalHDL. 2025. NaxRiscV. https://github.com/SpinalHDL/NaxRiscv.
     IEEE Computer Society, USA, 401–404.                                            Accessed: 2025-03-26.
[16] Microchip Technology Inc. 2015. PIC32MZ Embedded Connectivity              [24] Frank Stanischewski. 1993. FASTCHART-Performance, Benefits and
     with Floating Point Unit (EF) Family Data Sheet. Accessed: 2025-08-01.          Disadvantages of the Architecture. In Fifth Euromicro Workshop on
[17] Andrew Morton and Wayne M. Loucks. 2004. A hardware/software                    Real-Time Systems. IEEE Computer Society, 246–247.
     kernel for system on chip designs. In Proceedings of the 2004 ACM          [25] Melissa Vetromille, Luciano Ost, César AM Marcon, Carlos Reif, and
     Symposium on Applied Computing (Nicosia, Cyprus) (SAC ’04). As-                 Fabiano Hessel. 2006. RTOS scheduler implementation in hardware
     sociation for Computing Machinery, New York, NY, USA, 869–875.                  and software for real time applications. In Seventeenth IEEE Interna-
     doi:10.1145/967900.968077                                                       tional Workshop on Rapid System Prototyping (RSP’06). IEEE, 163–168.
[18] Takumi Nakano, Yoshiki Komatsudaira, Akichika Shiomi, and Masa-            [26] Ionel Zagan and Vasile Gheorghit, ă Găitan. 2022. Designing a Custom
     haru Imai. 1999. Performance evaluation of STRON: A hardware                    CPU Architecture Based on Hardware RTOS and Dynamic Preemptive
     implementation of a real-time OS. IEICE transactions on fundamen-               Scheduler. Mathematics 10, 15 (2022). doi:10.3390/math10152637
     tals of electronics, communications and computer sciences 82, 11 (1999),   [27] Florian Zaruba and Luca Benini. 2019. The Cost of Application-Class
     2375–2382.                                                                      Processing: Energy and Performance Analysis of a Linux-Ready 1.7-
[19] Antti Nurmi, Abdesattar Kalache, Henri Lunnikivi, Per Lindgren, and             GHz 64-Bit RISC-V Core in 22-nm FDSOI Technology. doi:10.1109/
     Timo D Hämäläinen. 2025. Efficient and predictable context switching            TVLSI.2019.2926114
     for mixed-criticality and real-time systems. IEEE Transactions on Very     [28] Zephyr Project. 2025. Zephyr Real-Time Operating System. https:
     Large Scale Integration (VLSI) Systems (2025).                                  //www.zephyrproject.org/. Accessed: 2025-05-10.
[20] Nader I Rafla and Deepak Gauba. 2011. Hardware implementation              [29] Xiangrong Zhou and Peter Petrov. 2006. Rapid and low-cost context-
     of context switching for hard real-time operating systems. In 2011              switch through embedded processor customization for real-time and
     IEEE 54th International Midwest Symposium on Circuits and Systems               control applications. In Proceedings of the 43rd annual Design Automa-
     (MWSCAS). IEEE, 1–4.                                                            tion Conference. 352–357.
[21] Giovanni Di Sirio. 2025. ChibiOS Real-Time Operating System. https:
     //www.chibios.org/. Accessed: 2025-05-10.
