         T HE O PEN H ANDS S OFTWARE AGENT SDK: A C OMPOSABLE AND
              E XTENSIBLE F OUNDATION FOR P RODUCTION AGENTS


         Xingyao Wang 1 Simon Rosenberg 1 Juan Michelini 1 Calvin Smith 1 Hoang Tran 1 Engel Nyst 1
              Rohit Malhotra 1 Xuhui Zhou 1 Valerie Chen 1 Robert Brennan 1 Graham Neubig 1


                                                        A BSTRACT
     Agents are now used widely in the process of software development, but building production-ready software
     engineering agents is a complex task. Deploying software agents effectively requires flexibility in implementation
     and experimentation, reliable and secure execution, and interfaces for users to interact with agents. In this paper,
     we present the OpenHands Software Agent SDK, a toolkit for implementing software development agents that
     satisfy these desiderata. This toolkit is a complete architectural redesign of the agent components of the popular
     OpenHands framework for software development agents.
     To achieve flexibility, we design a simple interface for implementing agents that requires only a few lines of code
     in the default case, but is easily extensible to more complex full-featured agents with features such as custom
     tools, memory management, and more. For security and reliability, it delivers seamless local-to-remote execution
     portability, integrated REST/WebSocket services. For interaction with human users, it can connect directly to
     a variety of interfaces, such as visual workspaces (VS Code, VNC, browser), command-line interfaces, and
     APIs. Compared with existing SDKs from OpenAI, Claude and Google, OpenHands uniquely integrates native
     sandboxed execution, lifecycle control, model-agnostic multi-LLM routing, and built-in security analysis. We
     validate the architecture empirically: production deployment data shows that V1 substantially reduces system-
     attributable failures over V0 with negligible event-sourcing overhead, and evaluations across multiple models and
     benchmarks demonstrate strong agent performance. Put together, these elements allow the OpenHands Software
     Agent SDK to provide a practical foundation for prototyping, unlocking new classes of custom applications, and
     reliably deploying agents at scale.

1    I NTRODUCTION                                                  sumptions, leaving practitioners without a well-validated
                                                                    reference design.
In software engineering, AI agents have evolved from as-
sistive tools (GitHub, 2021; Cursor Team, 2024) to au-              OpenHands (Wang et al., 2025) demonstrated that open-
tonomous systems capable of hours-long async execution              source software agents can achieve broad adoption and con-
on complex tasks (e.g., Devin (Cognition AI, 2024), Claude          tributions, reaching over 64k GitHub stars and hundreds
Code (Anthropic, 2025b), OpenHands (Wang et al., 2025)).            of contributors in just 18 months (Neubig, 2025). As the
Reliably deploying autonomous agents in production re-              project scaled, the original monolithic architecture—what
quires system foundations—including durable state manage-           we refer to as OpenHands V0—exposed growing architec-
ment, safe execution in a sandbox, and consistent behavior          tural tensions. The early design was driven by the need for
across environments ranging from local to containerized             fast prototyping and iteration, combining agent logic, evalu-
cloud deployments—that were unnecessary for earlier assis-          ation, and applications in a single codebase. Over time, this
tive tools that rely on largely local user-driven workflows.        approach led to rigid sandboxing assumptions, sprawling
Yet no consensus exists on how to architect these founda-           mutable configuration, and tight coupling between research
tions: existing SDKs from major providers differ widely             and production, eventually making a full architectural re-
in isolation models, state management, and deployment as-           design unavoidable.
  *
    Equal contribution 1 OpenHands. Correspondence to:              Guided by these lessons, OpenHands V1 introduces a new
Xingyao Wang <xingyao@openhands.dev>, Graham Neubig                 architecture grounded in four design principles that directly
<graham@openhands.dev>.                                             address the limitations of V0:
Proceedings of the 9 th MLSys Conference (Industry Track), Belle-   • Optional isolation. The agent runs locally by default but
vue, WA, USA, 2026. Copyright 2026 by the author(s).                  can switch to a sandboxed environment when additional
                       The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

            OpenHands GUI
                             Integrations
                                     OpenHands CLI

                   React Frontend                    Github App, Gitlab App, ...                         Command-line interface
                                                                                                                                                                          OpenHands Applications
              HTTP & WebSocket                                                              e te controller
                                                                                          Cr a



                    API Server                                                                                             Agent Core           OpenHands GUI
                  OpenHands CLI
                     Your Custom Client

                                                                  Agent Controller                                                              Command-line interface          Command-line interface                Automation & workflows




                                                                                           Agent Hub (Agent Implementations)
    Conversation Manager              WebSession                   Subscribe
                                                                                   Condenser            LLM Registry          Prompts
                          Session


                       AgentSession                                                       E vent Stream                                               openhands.sdk
                                        openhands.workspace

                                                                                                                                                          Core abstractions
          Extends                  Workspace Implementations


                                                                 Subscribe                                                                         Agent, LLM, Tool, MCP ...                             DockerWorkspace, RemoteAPIWorkspace
                                                                                                        Local MCP Implementation

      LocalFileStore                                         CLIRuntime
                                Local Tool Implementation
                                                          (CLI-specific local-                                                                              Extends                                            Spawns / Connects
    S3FileStore (SaaS)                                  execution w/o sandbox)

         Storage                                            DockerRuntime                                Action Execution Server

                                                                                                                                                                                                          openhands.agent_server

                                                                                                     (API Server for action execution)               openhands.tools

                                                                                                                                                                                                                  API Server for Agents

                                                         KubenetesRuntime 
                                                                             Tool Implementations
                                                                                                                                                                                                             FastAPI: REST + WebSocket
                                                          (via remote API)               Starts     Tools          SCode
                                                                                                                   V              MCP

                                                            Ru ntime Client
                                   ntime Server

                                                                                                              Ru
                                                   (Execute actions into observations)                    (Sandboxed Container)


                                                                                                                                                                OpenHands Software Agent SDK
(a) OpenHands V0: Monolithic architecture with tightly coupled
components and a mandatory sandboxing model. The design as-                                                                              (b) OpenHands V1: Modular SDK architecture with four decou-
sumed all executions occur in a sandbox, making later support for                                                                        pled packages. Applications consume software agent SDK; tool
local execution workflows (CLI) cumbersome — requiring special-                                                                          and workspace packages extend sdk’s abstraction with actual
case handling in the CLI runtime and duplicated local implementa-                                                                        implementations; the workspace package spawns and connects
tions of MCP and tools.                                                                                                                  to agent server.

Figure 1. Architecture Evolution from OpenHands V0 to V1. V0’s monolithic, sandbox-centric design tightly coupled components and
required duplicated local implementations. V1 refactors this into a modular SDK with clear boundaries, opt-in sandboxing, and reusable
agent, tool, and workspace packages. The color of each component in V0 roughly corresponds to its modular counterpart in V1.


  safety or resource control is required.                      built-in REST/WebSocket server for remote execution and a
• Stateless by default, one source of truth for state. All     suite of interactive workspace interfaces for human inspec-
  components—agents, tools, LLMs, etc—are immutable            tion and control.
  and validated at construction, while a single conversa-
  tion state object records all mutable context. This design 1    from openhands.sdk import LLM, Conversation
                                                             2    from openhands.tools.preset.default import
  ensures reliable recovery of agent sessions.                    ,→ get_default_agent
                                                                  llm =
• Strict separation of concerns. The agent core is decou- 3       ,→ LLM(model="openhands/claude-sonnet-4-5-20250929",
  pled from applications so that downstream systems (CLI,         ,→ api_key="...")
                                                             4    agent = get_default_agent(llm=llm)
  Web UI, GitHub App) use it as a shared library rather than 5    conversation = Conversation(agent=agent,
  duplicating logic.                                              ,→ workspace="/path/to/project")
                                                             6    conversation.send_message("Write 3 facts about this
• Two-layer composability. Developers can compose inde-           ,→ project into FACTS.txt.")
                                                                  conversation.run()
  pendent deployment packages (SDK, Tools, Workspace, 7
  Server) and extend the SDK safely by adding or replacing
  typed components such as tools or agents.
                                                                                                                                         Figure 2. Minimal example of OpenHands Software Agent SDK.
Building on these principles, we introduce OpenHands V1,
a complete software-agent ecosystem—including CLI and
                                                                                                                                         We systematically compare features of our SDK with those
GUI applications—built on a shared foundation: the Open-
                                                                                                                                         of the OpenAI Agents SDK, Claude Agent SDK, Google
Hands Software Agent SDK (Fig. 1b). We present the SDK
                                                                                                                                         ADK, and LangChain (Tab. 6), finding that our SDK
as a reference architecture for production agent systems, cod-
                                                                                                                                         uniquely combines native remote execution with sandbox-
ifying lessons from 18 months of open-source development
                                                                                                                                         ing, a production server, model-agnostic multi-LLM routing
and production deployment into an empirically validated
                                                                                                                                         across 100+ providers, a security analyzer for agent actions,
design. The SDK defines an event-sourced state model with
                                                                                                                                         and built-in QA instrumentation for production reliability.
deterministic replay, an immutable configuration for agents,
and a typed tool system with MCP integration. Its workspace                                                                              We support this architectural contribution with empirical
abstraction enables the same agent to run locally for proto-                                                                             evidence at two levels. At the systems level, a 15-day pro-
typing or remotely in secure, containerized environments                                                                                 duction comparison shows V1 reduces system-attributable
with minimal code changes. Unlike prior library-only SDKs                                                                                failures by 61% relative to V0, and event-sourcing over-
(Anthropic, 2025a; OpenAI, 2024), OpenHands includes a                                                                                   head is negligible. At the agent capability level, evaluations
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

across 14 language models and five benchmark categories—          web server for the GUI, frontend code, and diverse runtime
including SWE-Bench Verified, GAIA, SWE-Bench Multi-              providers (e.g., local Docker, Kubernetes in production) all
modal, SWT-Bench, and Commit0—demonstrate that the                coexist in a single codebase. Much of this architecture dated
redesigned architecture maintains strong agent performance.       back to the project’s first weeks, before the introduction
                                                                  of LLM structured tool use or the Model Context Protocol
The OpenHands Software Agent SDK1 and evaluation har-
                                                                  (MCP; MCP Team 2025). This section revisits the key ar-
ness2 are fully open-sourced under the MIT License.
                                                                  chitectural tensions observed in OpenHands V0 and distills
                                                                  them into four design principles guiding V1.
2     P RELIMINARIES
2.1   Agent Design                                                3.1   Universal Sandboxing vs. Local Flexibility

Modern AI agents can be viewed as systems that perceive,          V0 Challenges. V0 was built on the assumption that all
reason, and act within an environment to accomplish goals         tool calls should run inside sandboxed Docker containers
over time (Russell & Norvig, 2009). In recent work (Wang          for safety and reproducibility.
et al., 2023), agent design typically centers on these inter-     However, doing so introduced multiple layers of friction
connected components:                                             as each conversation spanned two independent processes
• Environment and Observations. The environment de-               (agent and sandbox) with potentially divergent states. Since
  fines the external context in which the agent operates,         the sandbox for tools might crash while the agent continued
  providing structured interfaces (e.g., APIs, files, user        (or vice versa), leading to corrupted sessions. In multi-tenant
  interfaces) from which it receives observations. These          deployments, resource-heavy actions from one user, such
  percepts—such as text, logs, or visual data—inform the          as mass webpage visits generating oversized screenshots,
  agent’s situational awareness and ground its decisions in       could exhaust container resources and crash other agents
  the evolving task state.                                        sharing the same application.
• State and Memory. An agent’s state encodes what it cur-         When the need arose to support local execution work-
  rently knows about the world and itself, often represented      flows—for example, running tools or MCP clients directly
  as a history of past interactions, retrieved information, and   on the host machine—we had to add exceptions and bypass
  internal reasoning.                                             layers that duplicated existing logic. As shown in Fig. 1a,
• Actions and Tools. Actions represent how the agent              this led to redundant local versions of MCP and tool im-
  influences its environment—issuing commands, executing          plementations, diverging from the original sandbox-based
  code, or invoking APIs—each realized through the use of         code path. These ad hoc extensions made the architecture
  tools that implement these capabilities.                        increasingly brittle and poorly aligned with MCP, which as-
                                                                  sumes agents can execute actions locally with direct access
2.2   Software Agents                                             to credentials, files, and IDEs.
In software engineering contexts, these agentic components          V1 Design Principle
manifest concretely in systems like OpenHands (Wang et al.,
2025), which emulate a developer’s end-to-end workflow.             Sandboxing should be opt-in, not universal. V1 uni-
The environment is a sandboxed workspace exposing a                 fies agent and tool execution in a single process by
filesystem, terminal, and web interface, allowing the agent         default, aligning with MCP’s assumptions. When iso-
to observe program output and test feedback. Its state and          lation is needed, the same stack can be containerized
memory are represented through an event log that records            transparently. Sandboxing becomes opt-in—preserving
commands, edits, and results, providing persistent context          flexibility without sacrificing safety.
across actions. The agent acts by editing files, running tests,
or invoking structured tools like web browser to interact
                                                                  3.2   Mutable Configuration vs. Deterministic State
with its environment.
                                                                  V0 Challenges. V0’s configuration system mixed different
3     C HALLENGES AND D ESIGN P RINCIPLES                         domains — deployment, agent behavior, LLM routing, sand-
                                                                  box settings, and UI options — within overlapping layers,
As OpenHands evolved, it has accumulated significant ar-          creating hidden dependencies and complex override logic.
chitectural complexity: core agent logic, multiple CLIs, a        The deeper issue was structural: configuration was split
  1                                                               across multiple parallel hierarchies (CLI/headless, Web UI,
    https://github.com/OpenHands/software-
agent-sdk                                                         GitHub App, and SaaS), each evolving its own precedence
  2
    https://github.com/OpenHands/benchmarks                       rules and assumptions. Different entry points were added
                                                                  incrementally, each patching configuration values in place,
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

so two runs with identical parameters could still diverge         The system lacked a structured notion of composability and
subtly. Later attempts to unify these systems only added          extensibility, forcing ad hoc hacks for even small changes.
more override layers and inconsistencies, while the web
configuration remained rigid due to its coupling with ORM             V1 Design Principle
models and database schemas. These issues led to severe
sprawl — 140+ fields, 15 classes, and 2.8K lines of config-           Everything should be composable and safe to ex-
uration code — a brittle system where small changes often             tend. V1 makes composability a first-class design goal
cascaded into unrelated failures.                                     at two levels. At the deployment level, its four mod-
                                                                      ular packages—SDK, Tools, Workspace, and Agent
  V1 Design Principle                                                 Server—combine flexibly to support local, hosted, or
                                                                      containerized execution. At the capability level, the
  Stateless by Default, One Source of Truth for State.
                                                                      SDK exposes a typed component model—tools, LLMs,
  V1 treats all agents and their components—tools,
                                                                      contexts, etc—so developers can extend or reconfigure
  LLMs, etc—as immutable and serializable Pydantic
                                                                      agents declaratively without touching the core.
  models validated at construction. The only mutable
  entity is the conversation state, which is a single, well-
  defined source of truth that tracks ongoing execution.
  This design isolates change to one place, enabling de-          4     A RCHITECTURE
  terministic replay, strong consistency, and stable long-
                                                                  OpenHands V1’s architecture emerged from both the op-
  term recovery.
                                                                  erational challenges we encountered in V0 analyzed in §3.
                                                                  OpenHands V1 is a broader ecosystem with applications
3.3   Monorepo vs. Modular SDK                                    like CLI and GUI. Its foundation is the OpenHands Soft-
                                                                  ware Agent SDK—a standalone developer framework com-
V0 Challenges. OpenHands V0 was implemented as a sin-             prising nine interlocking components: Event-Sourced State
gle monolithic repository that combined the agent core, eval-     Management (§4.2), LLM (§4.3), Tool System (§4.4), Agent
uation suite, and applications (frontend, backend, CLI) into      (§4.5), Context Window Management (§4.6), Local Conver-
one codebase. This monorepo simplified early development          sation (§4.7), Secret Registry (§4.8), Security and Confirma-
but gradually blurred boundaries between the agent and its        tion (§4.9), and deployment architecture (§4.10) with local
downstream applications. The same repository powered the          and remote workspace support (§4.10). This section focuses
CLI, web interface, and GitHub integrations, each intro-          on describing the SDK architecture rather than the full ap-
ducing divergent logic and configuration overrides. Over          plication and explains how these components collectively
time, the agent core absorbed application-specific branches,      support both local and production deployments.
benchmark dependencies, and environment-specific hacks,
making the system brittle and slow to evolve.                     4.1    Modular Four-Package Design
As OpenHands gained popularity in the academic commu-
                                                                  The OpenHands Software Agent SDK is organized into four
nity, many benchmarks were contributed, making the agent
                                                                  Python packages that compose together based on deploy-
more general but also introducing heavy dependencies and
                                                                  ment needs. Fig. 1b shows how these packages interact:
frequent version conflicts. These leaked into the main ap-
plication due to mono-repo design, making deployments             • openhands.sdk: Core abstractions (Agent, Conversa-
heavyweight and fragile.                                            tion, LLM, Tool, MCP, etc) and the event system.
                                                                  • openhands.tools: Concrete tool implementations
  V1 Design Principle                                               based on abstractions defined in openhands.sdk.
  Maintain strict separation of concerns. V1 isolates             • openhands.workspace: Execution environments
  the agent core into software engineering SDK as de-               (e.g., Docker, hosted API) that extend SDK base classes.
  scribed in this paper. Applications integrate via SDK           • openhands.agent server: A web server exposing
  APIs, allowing research to evolve independently from              REST/WebSocket APIs for remote execution.
  applications.                                                   The separation addresses key production concerns that
                                                                  slowed down development, QA, and release in OpenHands
                                                                  V0: (1) sdk stays lightweight for diverse integration scenar-
3.4   Monolith Logic vs. Extensible Architecture
                                                                  ios; (2) tools isolates slow-running tool tests from core
V0 Challenges. As V0 lacked clear boundaries between the          SDK changes, speeding up development; (3) workspace
agent core and its applications, adding new behaviors in V0       provides optional sandboxing implementations without
often required editing the core logic or branching for specific   bloating the core; and (4) agent server offers a generic
entry points, limiting experimentation and maintainability.       API server usable with or without containers. This mod-
                The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

ularity enables independent testing, selective dependency                          detecting incomplete conversations and continuing from the
management, and incremental release cycles—critical for                            last processed event.
production deployments where monolithic repositories cre-
                                                                                   Empirical measurements on agent traces confirm that event-
ate QA bottlenecks.
                                                                                   sourcing overhead is negligible relative to LLM round-trip
                                                                                   times, with sub-millisecond persist latency and crash recov-
4.2        Event-Sourced State Management                                          ery under 20 ms (see §5.2 for details).
At V1’s core lies an event-sourcing pattern treating all inter-
actions as immutable events appended to a log.                                     4.3   LLM Abstraction Layer
Event Hierarchy. The event system uses a multi-level hier-                         The LLM class provides a unified interface to language mod-
archy shown in Table 1. At the base, Event provides im-                            els. Through LiteLLM, it supports 100+ providers with two
mutable structure (ID, timestamp, source) with type-safe se-                       APIs: the standard Chat Completions API for broad com-
rialization via discriminated unions (Pydantic Team, 2025).                        patibility and the newer OpenAI Responses API for latest
LLMConvertibleEvent adds to llm message()                                          reasoning models.
for converting events into LLM format. The action-
                                                                                   Native Support for Reasoning / Extended Think-
observation loop uses ActionEvent for tool calls and
                                                                                   ing. The SDK captures and processes advanced na-
ObservationBaseEvent subclasses for results – all
                                                                                   tive reasoning fields from frontier models, such as
these are subclasses of LLMConvertibleEvent. Inter-
                                                                                   ThinkingBlock for Anthropic’s extended thinking, and
nal events (condensation, state updates, etc) inherit directly
                                                                                   ReasoningItemModel for OpenAI’s reasoning. The
from Event for bookkeeping without LLM exposure.
                                                                                   SDK supports the OpenAI Responses API transparently for
                                                                                   the agent, enabling client developers to use the agent with
Table 1. Event hierarchy organized by parent-child relationships.                  advanced reasoning models like GPT-5-Codex that are only
LLM-convertible events can be sent to the LLM, while internal                      available on the recently released Responses API.
events handle state management and control flow.
Parent Class             Child Classes & Purpose                                   Built-in Support for Non-function-calling Models. For
Event                    Base: Immutable structure, type-safe serialization
                         LLM-Convertible Events (visible to LLM)
                                                                                   models without native function calling support, the SDK
LLMConvertibleEvent
,→ Event
                          MessageEvent: User/assistant text messages
                          ActionEvent: Agent tool calls with thought & reasoning
                                                                                   implements a NonNativeToolCallingMixin, which
                          SystemPromptEvent: System prompt with tool schemas
                          CondensationSummaryEvent: Summary of forgotten events    converts tool schemas to text-based prompt instructions
                          ObservationBaseEvent: Base for tool responses
ObservationBaseEvent     ObservationEvent: Successful tool execution results
                                                                                   and parses tool calls from model outputs using structured
,→ LLMConvertibleEvent   UserRejectObservation: User rejected action
                         AgentErrorEvent: Agent/scaffold errors                    prompts and regex-based extraction. This enables models
Event
                          Internal Events (not visible to LLM)
                         ConversationStateUpdateEvent: State field changes
                                                                                   that do not support function calling to be used for agentic
                         CondensationRequest: Trigger history compression
                         Condensation: Compression result with forgotten events    tasks, dramatically expanding the set of usable models.
                         PauseEvent: User-requested pause

                                                                                   Multi-LLM Routing Support.                 SDK features
ConversationState: Single Source of Truth. By de-                                  RouterLLM, a subclass of LLM that enables the
sign, components like Agent, Tool, and LLM are im-                                 agent to use different models for different LLM requests.
mutable and serializable—all changing variables live in                            Custom implementations can extend RouterLLM and
ConversationState, making it the only stateful com-                                implement select llm() to choose a different model
ponent. This class maintains two types of state: (1)                               based on different LLM inputs. Please refer to Fig. 3 for a
mutable metadata fields (e.g., agent status, stats,                                pseudo-code example.
confirmation policy) stored directly in the Pydantic
model, and (2) an append-only EventLog recording all                               4.4   Tool System
agent interactions. A FIFO lock ensures thread-safe updates
                                                                                   The V1 tool system provides a type-safe and
through a two-path pattern: state-only updates for metadata
                                                                                   extensible     framework      grounded      in     an    Ac-
changes, and event-based updates that append to the log.
                                                                                   tion–Execution–Observation pattern. As illustrated
When persistence is configured, ConversationState                                  in Fig. 4, tool usage follows a strict contract: the LLM
selectively writes changes to disk. Metadata fields serialize                      proposes JSON tool calls, which are validated and parsed
to a single base state.json file on each modification,                             into Action; these are executed and the results are
while EventStore persists events as individual JSON                                returned as Observation. This abstraction unifies
files to the corresponding directory. This dual-path design                        how the SDK supports both custom tools and MCP tools,
enables efficient incremental persistence—only new events                          providing a single standard interface for defining, invoking,
write to disk, avoiding rewrites of large histories. Conversa-                     and managing tools.
tions resume by loading base state.json and replay-
                                                                                   Action–Execution–Observation Pattern. Each tool is de-
ing events from the directory, with agents automatically
                 The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

 1     class RouterLLM(LLM):                                                 1     class Schema(BaseModel):
 2         llms_for_routing: dict[str, LLM]          # Available             2         @classmethod
           ,→ models                                                         3         def to_mcp_schema(cls) -> dict[str, Any]
 3                                                                           4         @classmethod
 4          @abstractmethod                                                  5         def from_mcp_schema(cls: type[S], model_name:
 5          def select_llm(self, messages: list[Message]) ->                           ,→ str, schema: dict[str, Any]) -> type["S"]
            ,→ str:                                                          6
 6              """Return key of LLM to use from                             7     class Action(Schema):
                ,→ llms_for_routing."""                                      8         """Tool input with schema validation."""
 7                                                                           9         def visualize(self) -> str # For UI display
 8          def completion(                                                 10
 9              self, messages: list[Message], ... **kwargs,                11     class Observation(Schema):
10          ) -> LLMResponse:                                               12         """Tool output with LLM conversion."""
11              # Select appropriate LLM                                    13         def to_llm_content(self) -> str # For LLM
12              selected_model = self.select_llm(messages)                             ,→ context
13              self.active_llm =                                           14
                ,→ self.llms_for_routing[selected_model]                    15     class ToolDefinition[ActionT,
14              return self.active_llm.completion(...)                             ,→ ObservationT](BaseModel):
15                                                                          16         action_type: type[Action]
16     class MultimodalRouter(RouterLLM):                                   17         observation_type: type[Observation]
17         def select_llm(self, messages: list[Message]) ->                 18         executor: ToolExecutor
           ,→ str:                                                          19         def to_mcp_tool(self, ...) -> dict
18             has_images = any(m.contains_image for m in                   20         def to_openai_tool(self) -> dict #
               ,→ messages)                                                            ,→ ChatCompletions API format
19             return "primary" if has_images else                          21         def to_responses_tool(self) -> dict          # Responses
               ,→ "secondary"                                                          ,→ API format
20                                                                          22
21     # Usage: route text to cheaper model, images to                      23     class ToolExecutor[ActionT, ObservationT](ABC):
       ,→ multimodal model                                                  24         """Executor function type for a Tool."""
22     router = MultimodalRouter(llms_for_routing={                         25         def __call__(self, action: ActionT) ->
23         "primary": LLM(model="claude-sonnet-4-5"),                                  ,→ ObservationT
24         "secondary": LLM(model="devstral-small")})
25     agent = Agent(llm=router, tools=tools)

                                                                                 Figure 4. Tool system structure. Actions validate inputs, executors
     Figure 3. Multi-LLM routing example. Routers inherit from LLM               run logic, and observations format outputs for LLMs.
     to maintain a unified interface while delegating to selected models.

                                                                                 Tool Registry and Distributed Execution. SDK sup-
     fined by three well-separated components:                                   ports distributed agent architectures by decoupling tool
     • Action — Specifies the input schema for a tool call. LLM-                 specifications from implementations using a registry-based
       generated arguments are validated against a Pydantic                      resolution mechanism. Because Python executors are
       model before execution, ensuring type safety and pre-                     non-serializable, tools are represented as lightweight
       venting malformed requests.                                               Tool specification objects containing only a regis-
                                                                                 tered name and JSON-serializable parameters. Through
     • Execution — Implements the tool’s actual logic via the
                                                                                 register tool(name, ToolDefinition), each
       ToolExecutor, which receives a validated Action
                                                                                 identifier is bound to a resolver that reconstructs the full
       and performs the underlying execution.
                                                                                 definition—including its executor—at runtime based on con-
     • Observation — Captures the output of the execution,                       versation context. This allows tool specs to cross process or
       defining a structured return schema, and converting results               network boundaries as pure JSON, enables lazy instantiation
       (or errors) into a LLM-compatible format.                                 with environment-specific state (e.g., workspace paths), and
     MCP Integration.        The same abstraction addition-                      ensures a uniform interface for local and remote execution.
     ally enables seamless support for the MCP. MCP
     tools are treated as first-class SDK tools: their JSON                      4.5   Agent: Stateless Event Processor
     Schemas are automatically translated into Action
                                                                                 The agent abstraction separates configuration from execu-
     models, and their results are surfaced as structured
                                                                                 tion state. Agents are defined as stateless, immutable specifi-
     Observation.           MCPToolDefinition extends
                                                                                 cations, including LLM settings, tool specifications, security
     the standard ToolDefinition interface, while
                                                                                 policies, and agent content, that can be serialized and trans-
     MCPToolExecutor delegates execution to FastMCP’s
                                                                                 mitted across process boundaries.
     MCPClient, which manages server communication
     and transport details. As a result, external MCP tools                      Event-Driven Execution. Agents execute through an event-
     behave identically to native tools—validated on input,                      driven loop that processes conversation state step-by-step.
     type-safe at runtime, and serialized for LLM consump-                       Rather than directly returning results, agents emit structured
     tion—highlighting the flexibility and extensibility of the                  events (e.g., messages, actions, observations) through call-
     core tool abstraction.                                                      backs, i.e. on event(event: Event) -> None,
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

separating event generation from execution control. This        step, can reduce the overall cost of a conversation.
design enables: (1) security interleaving—actions can be        LLMSummarizingCondenser (the default condenser)
reviewed or blocked before execution based on risk analy-       has been shown to reduce API costs by up to 2× with no
sis (§4.9); (2) incremental execution—the agent advances        degradation in agent performance (Smith, 2025).
one step at a time, supporting pause/resume, recovery from
context overflows, and condensation for long conversations;     4.7   Local Conversation
and (3) event streaming—intermediate results (e.g., obser-
vations and reasoning traces) are emitted in real time for UI   LocalConversation provides the simplest and most di-
updates and monitoring.                                         rect execution mode of the SDK, designed for rapid iteration
                                                                and debuggability. It runs the full agent loop—LLM calls,
Customizing Agent Context with Skills and Prompts.              tool invocation, event callbacks, and state updates—entirely
AgentContext centralizes all inputs that shape LLM              in-process without network or container overhead. The
behavior, including prefixes/suffixes for system/user mes-      core API offers intuitive control: developers can ini-
sages and user-defined Skill objects. Skills can be de-         tialize a conversation with Conversation(agent,
fined programmatically or loaded from markdown files            workspace), send inputs using send message(),
(e.g., .openhands/skills/, or compatible formats                start execution via run(), pause and resume with
like .cursorrules, agents.md). Each skill may al-               pause() and run() respectively, inspect results through
ways be active (trigger=None) to persistently augment           event callbacks or direct access of .state attribute. Paus-
the system prompt, or conditionally activated via keyword       ing automatically persists state and emits a PauseEvent,
matching based on user input; skills may also include MCP       allowing agents to resume from the same point later.
tools. This design enables rich contextual and behavioral
customization without modifying agent logic.                    4.8   Secret Registry
Sub-Agent Delegation. The SDK supports hierarchical             SecretRegistry provides secure, late-bound, and re-
agent coordination through a delegation tool that demon-        motely manageable credentials for tool execution. Each
strates the extensibility of the tool abstraction. Sub-agents   conversation maintains its own instance, ensuring strict per-
operate as independent conversations that inherit the par-      session isolation. Tools access secrets only at execution
ent’s model configuration and workspace context, enabling       time, and all secret values appearing in outputs are masked
structured parallelism and isolation without any changes to     to prevent leakage. For example, the Bash Tool scans com-
the core SDK. The current implementation provides block-        mands for secret keys, exports the referenced ones as en-
ing parallel execution, implemented as a standard tool in       vironment variables, and replaces their occurrences in re-
the openhands.tools package, where the parent agent             sults with a constant mask (<secret-hidden>). Secrets
spawns and monitors sub-agents until all tasks complete.        may be static values or callables (e.g., token refreshers)
This pattern exemplifies how complex coordination behav-        and can originate from local stores or HTTP-based sources
iors—such as asynchronous delegation, dynamic scheduling,       with secret-aware header handling. New custom sources
or fault-tolerant recovery—can be implemented entirely as       can be easily added to integrate with external secret stores.
user-defined tools, reinforcing the SDK’s design principle      All secrets are redacted during serialization and can be en-
for extensibility that advanced agent orchestration requires    crypted with a configurable cipher. They can also be up-
no modification to the core framework.                          dated mid-conversation—locally or through the agent server
                                                                API—supporting live rotation without restarting agents.
4.6   Context Window Management
To ensure the ever-growing history fits inside the LLM’s        4.9   Security and Confirmation
context, the Condenser system drops events and replaces         AI agents can perform risky actions, making security espe-
them with summaries whenever the history grows too large.       cially important when they run on a user’s machine. To ad-
The results of any given condensation are stored in the         dress this, SDK treats security as a first-class concern within
event log as a CondensationEvent. Before sending                the agent’s control loop. Two abstractions form the core of
the event history to the LLM, the agent applies these con-      this design: the SecurityAnalyzer, which rates each
densation events by removing forgotten events and inserting     tool call as low, medium, high, or unknown risk, and the
summaries. This strategy lets the SDK preserve the en-          ConfirmationPolicy, which determines whether user
tirety of the event log, regardless of condensation, while      approval is required before execution based on the action’s
also keeping the condenser implementations stateless.           details and assessed risk.

Condensers enable long-running conversations and, as            When approval is required, the agent pauses in a special
a mechanism for constraining the tokens consumed per            WAITING FOR CONFIRMATION state until the user ex-
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

plicitly approves or rejects the action. Upon rejection, it 1         --- local.py
                                                             2        +++ remote.py
may retry with safer alternatives. The confirmation policy 3          @@
can be updated dynamically during a session, enabling adap- 4          from openhands.sdk import LLM, Conversation
                                                             5         from openhands.sdk.preset.default import
tive trust—such as relaxing restrictions for safe, read-only           ,→ get_default_agent
operations like grep.                                        6         llm = LLM(model="anthropic/claude-sonnet-4.1", ...)
                                                              7        agent = get_default_agent(llm=llm)
                                                                      -conversation = Conversation(agent=agent)
This architecture separates risk assessment from 89                   -conversation.send_message("Create hello.py")
enforcement, allowing developers define custom 10                     -conversation.run()
                                                                      +from openhands.workspace import DockerWorkspace
SecurityAnalyzer and ConfirmationPolicy 11             12             +with DockerWorkspace(...) as workspace:
without touching tool executors or core logic. The SDK 13             + conversation = Conversation(agent=agent,
                                                                      ,→ workspace=workspace)
includes a built-in pair: LLMSecurityAnalyzer, 14                     + conversation.send_message("Create hello.py")
which appends a security risk field to tool calls, and 15             + conversation.run()
ConfirmRisky policy, which blocks actions exceeding a
configurable risk threshold (default: high).
                                                                  Figure 5. Local-to-remote transition requires only importing and
4.10   Deployment Architecture: Local to Remote                   instantiating DockerWorkspace. All other code (agent config-
                                                                  uration, LLM setup, message handling) remains unchanged.
OpenHands Agent SDK’s key innovation is seamless transi-
tion from local prototyping to remote production with mini-                                HTTP/WS            spawns                  executes
mal code changes. This is enabled by the agent API server             RemoteConversation
                                                                           (Client)
                                                                                                     Agent                Local                  Agent +
                                                                                                     Server            Conversation               Tools
and the abstraction of Conversation and Workspace.                                                                       Container


Conversation Factory: Local & Remote Conversation.
                                                                  Figure 6. Agent server architecture. Client serializes agent con-
The Conversation class serves as a factory entry point
                                                                  figuration via HTTP; the server executes using SDK components
that abstracts over local and remote execution. When in-          inside the container and streams events via WebSocket.
stantiated with a string path or LocalWorkspace, it
returns a LocalConversation that executes the full
agent loop in-process, directly invoking tools and up-            stack—including the API server, VSCode Web, VNC desk-
dating state synchronously (§4.7). When provided a                top, and Chromium browser. Each agent instance runs in
RemoteWorkspace, the same call transparently con-                 an independent container with a dedicated file system, envi-
structs a RemoteConversation, which serializes the                ronment, and resource. This containerized design simplifies
agent configuration and delegates execution to an agent           deployment and enables SaaS-style multi-tenancy while
server over HTTP and WebSocket. Both implementations              preserving workspace isolation.
share an identical API allowing seamless migration from
local prototyping to containerized multi-user deployments         Workspace. The BaseWorkspace abstract class en-
without code changes. This factory pattern encapsulates           ables sandboxes for agents. Local Workspace executes
all environment-specific logic behind a unified interface,        in-process against the host filesystem and shell; it is ef-
achieving the SDK’s goal of “local-first, deploy-anywhere”        fectively a thin, no-op wrapper that forwards file/com-
development: the same agent code that runs interactively          mand/git operations directly, enabling fast prototyping with-
in a notebook can scale to distributed production simply by       out network hops. Remote Workspace preserves the
swapping the workspace type.                                      same interface but delegates all operations over HTTP
                                                                  to an Agent Server (see Fig. 7), with concrete sponsors
Agent Server.        The agent server module im-                  including a containerized server (DockerWorkspace)
plements an API server for remote agent execution                 or a API-managed runtime (APIRemoteWorkspace).
(Fig. 6).     It exposes REST endpoints for conver-               The factory Workspace(...) resolves to local when
sation control (e.g., POST /conversations, GET                    only working dir is provided and to remote when
/conversations/id) and WebSocket for event stream-                host/runtime parameters are present, ensuring the agent
ing. When a RemoteConversation starts, it serializes              code remains unchanged across environments.
agent configuration—including LLM settings, tools, and
context—into JSON and submits it to /conversations.
The server reconstructs the agent, launches a local execu-        5      R ELIABILITY AND E VALUATION
tion loop, and streams structured events back in real time,       We assess the OpenHands Agent SDK through four comple-
enabling responsive UIs without polling overhead.                 mentary lenses: (1) production reliability comparing V0 and
To support scalable and isolated execution, we provide            V1, (2) systems overhead of the event-sourcing mechanism,
official Docker images that bundle the full agent-server          (3) continuous testing, and (4) benchmark evaluation across
                                                                  diverse models and tasks.
                 The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

 1     class BaseWorkspace(ABC):                                           at 43.0 / 1k (inter-pod authentication failures),
 2         def __enter__(self) -> "BaseWorkspace"
 3         def __exit__(self, exc_type: Any, exc_val: Any,                 AgentRuntimeNotReadyError at 18.8 / 1k (run-
           ,→ exc_tb: Any) -> None                                         time pod readiness races), and connection/timeout errors
 4         def execute_command(self, command: str) ->
           ,→ CommandOutput:                                               at 3.1 / 1k (network instability). These failures stem
 5             """Run shell command in workspace                           from V0’s inter-pod HTTP communication between
               ,→ environment."""
 6         def file_upload(self, path: str, content: bytes)                the conversation manager and execution runtime. V1’s
           ,→ -> None:                                                     co-located execution model eliminates these observed
 7             """Write file to workspace filesystem."""
 8         def file_download(self, path: str) -> bytes:                    failure modes by removing this dependency entirely. The
 9             """Read file from workspace filesystem."""                  remaining V1 SDK errors (29.7 / 1k) were dominated
10
11     class LocalWorkspace(BaseWorkspace):                                by a condensation bug discovered during the rollout of
12         def execute_command(self, cmd: str) ->                          extended thinking support, as the LLM provider introduced
           ,→ CommandOutput:
13             return subprocess.run(cmd, shell=True,                      additional constraints on the event/message interface. This
               ,→ capture_output=True)                                     issue has since been fixed in the official release.
14
15     class RemoteWorkspace(BaseWorkspace):
16         def execute_command(self, cmd: str) ->
           ,→ CommandOutput:
                                                                           5.2      Event-Sourcing Systems Overhead
17             return self._api_client.post("/execute",
               ,→ json={"command": cmd})                                   To quantify the I/O cost of event sourcing, we re-
18
19     # Workspace factor class
                                                                           played real payloads from 433 SWE-Bench Verified
20     class Workspace:                                                    conversations (39,870 events) through the production
21         def __new__(cls, *, host=None,                                  LocalFileStore path (Tab. 3). All persist and recov-
           ,→ working_dir="workspace/project",
           ,→ api_key=None):                                               ery latencies are negligible relative to LLM round-trip times
22             if host is provided:                                        (typically 1–30 s), storage grows linearly, and crash recovery
23                 return RemoteWorkspace(working_dir, host,
                   ,→ api_key)                                             completes in under 20 ms even for the longest observed con-
24             else:
25                 return LocalWorkspace(working_dir)
                                                                           versation (358 events). These results demonstrate that event
                                                                           sourcing adds minimal overhead to the agent execution loop.

     Figure 7. Workspace interface. Implementations handle environ-        Table 3. Event-sourcing systems overhead measured on SWE-
     ment details.                                                         Bench Verified traces (433 conversations, 39,870 events).
                                                                           Metric                                            Median       P95    At max (358 events)
                                                                           Per-event persist latency                         0.20 ms   0.31 ms                    —
     5.1   Production Reliability: V0 vs. V1                               Action cycle persist (Action+Obs)                 0.40 ms   0.56 ms                    —
                                                                           Full state replay                                  4.1 ms    9.7 ms               18.9 ms
     To directly evaluate whether the architectural redesign im-           Crash recovery (replay + unmatched-action scan)    7.4 ms   14.9 ms               32.1 ms
                                                                           Storage per conversation                          380 KB    1.4 MB                3.4 MB
     proves operational reliability, we analyzed production logs
     from a 15-day parallel rollout in which V0 and V1 served
     users simultaneously. Exceptions were extracted from con-             5.3      Continuous Quality Assurance
     versation failure logs and categorized into three classes
     (Tab. 7):                                                             The SDK employs a three-tier testing strategy that balances
                                                                           coverage, cost, and depth:
     To ensure comparability, we report system-attributable er-
     rors—error classes directly attributable to agent infrastruc-         • Programmatic Tests — Run on every commit. These
     ture or SDK logic, excluding LLM provider errors that are               tests mock llm calls and verify core logic, data flow, and
     external to both architectures. Results are shown in Tab. 2.            API contracts within seconds. Mocking allows quicker
                                                                             feedback, ensuring that most regressions are caught before
     Table 2. System-attributable errors per 1k conversations during 15-     any external API calls are made.
     day production rollout.                                               • LLM-based Tests — Include both integration and exam-
                                         V0 (Legacy)      V1 (SDK)           ple tests (see below). Executed daily and on-demand for
        Infrastructure errors                 69.8 / 1k      0.0 / 1k
                                                                             pull requests. These tests use real models (Claude Son-
        SDK errors                                N/A       29.7 / 1k        net 4.5, GPT-5 Mini, DeepSeek Chat) to validate reason-
                                                                             ing, tool invocation, and environment stability. Each run
        System-attributable error rate        78.0 / 1k     30.0 / 1k
                                                                             costs $0.5–$3 and completes in less than 5 minutes.
                                                                           • Benchmark Evaluation — On-demand, high-cost evalu-
     Result: V1 reduces system-attributable failures by                      ations ($100–1000, hours per run) that measure compre-
     61% (from 78.0 to 30.0 errors per 1k conversa-                          hensive agent capabilities on academic datasets.
     tions).  The eliminated V0 infrastructure errors
     were dominated by:    HTTPStatusError (401)                           Integration tests cover multiple scenario-based workflows
                    The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

     (e.g., file manipulation, command execution, git operations,                     Model                          V0             V1 (SDK)
     and browsing), while example tests periodically run all                          Claude Sonnet 4.5            64.6%             72.8%
     SDK examples (custom tools, MCP integration, persistence,                        Claude Sonnet 4              68.0%             68.0%
     async execution, routing, etc) to ensure end-to-end reliabil-
     ity. The suite is continuously expanded as new agent behav-     Table 4. V0 vs. V1 on SWE-Bench Verified with matched mod-
     iors and failure patterns are discovered, improving coverage    els. Sonnet 4 confirms capability parity; the Sonnet 4.5 gain is
     and regression sensitivity over time. On-demand execution       attributed to extended thinking support enabled by V1’s architec-
     for these LLM-based tests further optimizes CI/CD cost: in-     ture.
     tegration tests target high-risk changes, example tests cover
     user-facing modules, and daily runs track regressions across    Comprehensive Multi-Model Evaluation. We evaluated
     overall codebase updates.                                       14 language models—7 closed-source (Anthropic, OpenAI,
                                                                     Google) and 7 open-weights (MiniMax, DeepSeek, Zhipu
 1     class BaseIntegrationTest(ABC):                               AI, Moonshot AI, Alibaba, NVIDIA)—across all five cate-
 2         def setup(self): """Prepare test environment"""
 3         def tools(self) -> list[Tool]: """Agent                   gories. Tab. 5 summarizes the best SDK result per category
           ,→ capabilities"""
 4
                                                                     alongside published state-of-the-art. The SDK achieves
 5              def verify_result(self) -> bool: """Check            SOTA on 3 of 5 benchmarks and remains competitive on
                ,→ success"""
 6              def run(self, instruction: str) -> bool:
                                                                     the other two (within 2.6 and 5.2 points), using a single
 7                  self.setup()                                     model per evaluation while several SOTA systems employ
 8                  conversation = Conversation(Agent(llm,
                    ,→ self.tools), workspace)
                                                                     multi-model orchestration.
 9                  conversation.send_message(instruction)
10                  conversation.run()                                                 Issue Resol.   Greenfield     Frontend          Testing      Info. Gather.
                                                                                      SWE-Bench V.    Commit0      SWE-Bench MM      SWT-Bench V.    GAIA (test)
11                  return self.verify_result()
                                                                                                                         4
                                                                     Published SOTA      79.2%          12.5%            -              84.0%          74.6%

     Figure 8. Integration test framework. Subclasses implement      Best SDK            76.6%         56.2%          44.1%             78.8%         80.0%
                                                                     Model              Opus 4.5       GPT-5.4     Gemini 3.1 Pro      Opus 4.6       Opus 4.6
     setup(), tools, and verify result() for specific sce-           2nd SDK            75.6%          56.2%          41.8%             78.5%         78.8%
                                                                     Model              GPT-5.4       Opus 4.6       Opus 4.6          Opus 4.5       GPT-5.4
     narios.
                                                                     Table 5. Multi-model benchmark evaluation across five SE task
     5.4        Benchmark Evaluation                                 categories (14 models evaluated). Bold indicates the SDK exceeds
                                                                     published SOTA. Full per-model results are available on the Open-
     We validate the SDK on academic benchmarks along two            Hands Index (https://index.openhands.dev).
     axes: first, that the V0-to-V1 architectural transition pre-
     serves (and, where the architecture enables new provider        SDK-Enabled Insights. The uniform evaluation harness
     features, improves) agentic capability; second, that the SDK    enables observations that would be difficult without a model-
     generalizes across models and task types.                       agnostic SDK. Models exhibit clear task-specific specializa-
     The evaluation spans five categories of software engineer-      tion: Claude models dominate issue resolution and testing,
     ing work: Issue Resolution (SWE-Bench Verified (Jimenez         while GPT-5.4 leads in long-horizon greenfield development
     et al., 2024)), Greenfield Development (Commit0 (Zhao           (62.5%, +12.5 pts ahead of second place).
     et al., 2025)), Frontend Development (SWE-Bench Mul-
     timodal (Yang et al., 2025)), Software Testing (SWT-            6       R ELATED W ORK
     Bench (Mündler et al., 2024)), and Information Gathering
     (GAIA (Mialon et al., 2023)). Results for all 14 evaluated      Recent agent SDKs share common goals of tool orches-
     models are maintained on the continuously updated Open-         tration, state management, and production deployment.
     Hands Index.3                                                   General-purpose frameworks such as LangChain and Lang-
                                                                     Graph focus on compositional pipelines and stateful graph
     V0 vs. V1: Capability Preservation. We compare V0 and           execution with durable checkpoints for long-running reason-
     V1 on SWE-Bench Verified using matched models to isolate        ing workflows (LangChain, 2025; Team, 2025). LangGraph
     the contribution of the SDK redesign (Tab. 4). With Claude      Platform further offers deployment and server capabilities,
     Sonnet 4, V0 and V1 achieve identical performance (68.0%),      making it one of the most feature-complete open-source
     confirming that the architectural redesign preserves baseline   alternatives. Provider SDKs from OpenAI and Anthropic
     agentic capability. With Sonnet 4.5, V1 gains +8.2 points;      emphasize production orchestration, guardrails, and hand-
     we attribute this to extended thinking support, which V1’s      offs within their own model ecosystems (OpenAI, 2025;
     event-sourced architecture integrates naturally but would       Anthropic, 2025a), while Google’s Agent Development Kit
     require significant engineering effort to retrofit into V0’s
                                                                         4
     multi-component design.                                              OpenHands Index uses the test dataset of SWE-bench Mul-
                                                                     timodal, for which no state-of-the-art results have yet been pub-
           3
               https://index.openhands.dev                           lished.
            The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

                                                      OpenAI            Claude         Google Agent      LangChain/       OpenHands
                                                     Agents SDK        Agent SDK         Dev. Kit        LangGraph        Agent SDK
Standalone SDK Features
MCP Support                                                ✓                ✓                ✓                 ✓               ✓
Custom Tools                                               ✓                ✓                ✓                 ✓               ✓
History Persistence & Restore                              ✓                ✓                ✓                 ✓               ✓
Sub-agent Delegation                                       ✓                ✓                ✓                 ✓               ✓
Model Agnostic (100+ LLMs)                                 ✓                ×                ✓                 ✓               ✓
Multi-LLM Routing                                          ×                ×                ×                 ∼               ✓
Conversation Cost & Token Tracking                         ✓                ✓                ×                 ✓               ✓
Pause/Resume Agent Execution                               ✓                ×                ×                 ✓               ✓
Native Support for Non-function-calling models             ×                ×                ×                 ×               ✓
Security Analyzer for Agent Action                         ×                ×                ✓                 ×               ✓
Action Confirmation Policies                               ∼                ×                ∼                 ∼               ✓
Context Files (e.g., repo.md, AGENTS.md)                   ×                ✓                ×                 ×               ✓
Agent Skills                                               ×                ✓                ×                 ×               ✓
Context Condensation                                       ×                ✓                ×                 ✓               ✓
Secrets Management with Auto-Masking                       ×                ×                ×                 ×               ✓
Agent Stuck Detection                                      ×                ×                ×                 ×               ✓
Production Server & Deployment
Builtin REST+WebSocket Server                              ×                ×                ×                 ✓               ✓
Session-Based Authentication                               ×                ×                ×                 ✓               ✓
Builtin Remote Agent Execution                             ×                ×                ×                 ✓               ✓
Agent Environment Sandboxing                               ×                ×                ∼                 ×               ✓
Continuous Quality Assurance
Programmatic tests                                         ✓                ✓                ✓                 ✓               ✓
Strong Type Checking                                       ✓                ✓                ✓                 ✓               ✓
Frequent LLM-based tests                                   ×                ✓                ✓                 ×               ✓
Built-in Academic Benchmark Evaluation                     ×                ×                ×                 ×               ✓

Table 6. Feature comparison across major agent SDKs (✓= full support, ∼= partial, ×= absent). Methodology: Features were assessed
from official documentation, public repositories, and release notes as of October 2025. Versions assessed: OpenAI Agents SDK v0.4.2,
Claude Agent SDK v0.1.6, Google ADK v1.17.0, LangChain v1.0.3 / LangGraph v1.0.2, OpenHands Agent SDK v1.0.0. “Partial”
indicates the capability requires significant external setup or community extensions rather than built-in support. OpenHands uniquely
combines: (i) native remote execution with environment sandboxing, (ii) LLM-powered action-level security analysis, (iii) model-
agnostic multi-LLM routing with first-class support for non-function-calling models, and (iv) built-in academic benchmark evaluation for
continuous quality assurance.


(ADK) targets model-agnostic agents integrated with Vertex            7    C ONCLUSION
AI for managed deployment (Google, 2025).
                                                                      The OpenHands Software Agent SDK provides a reference
In contrast, the OpenHands Software Agent SDK is a                    architecture for production agent systems, bridging the gap
fully open-source, vendor-agnostic platform purpose-built             between rapid local prototyping and production deploy-
for software engineering agents. It couples an event-                 ment through a stateless, event-sourced, and composable
sourced state model and immutable configuration with sand-            design spanning four packages (SDK, Tools, Workspace,
boxed execution, a built-in REST/WebSocket server, and                Server). From our experience scaling OpenHands from re-
workspace-level remote interfaces for human-agent collab-             search prototype to production system, we distill four key
oration. These design choices enable reproducible, de-                lessons: strict separation between core agent logic and appli-
terministic execution and seamless transition from local              cations is essential for maintainability; event-sourced state
prototyping to production-scale deployments. A feature-               enables reproducibility and fault recovery with negligible
level comparison with major agent SDKs—including                      overhead (sub-millisecond persist latency, crash recovery
LangChain/LangGraph—is provided in Tab. 6, with com-                  under 20 ms); immutable component design prevents config-
parison methodology detailed in the caption.                          uration drift; and a unified execution model supporting both
                                                                      local and sandboxed deployment streamlines the transition
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

from experimentation to production. These architectural          Mündler, N., Müller, M. N., He, J., and Vechev, M. SWT-
choices yield measurable reliability gains: a 15-day produc-      bench: Testing and validating real-world bug-fixes with
tion comparison demonstrates a 61% reduction in system-           code agents. In Advances in Neural Information Pro-
attributable failures relative to V0, while evaluations across    cessing Systems, 2024. URL https://openreview.
14 models and five benchmark categories confirm that the          net/forum?id=9Y8zUO11EQ.
redesign preserves and enables strong agentic performance,
achieving state-of-the-art results on three of five tasks. To-   Neubig, G. One year of openhands: A journey of open
gether, these results validate the SDK as a robust foundation      source ai development. All Hands AI Blog, March 2025.
for both research and industrial-scale deployment.                 URL       https://www.all-hands.dev/blog/
                                                                   one-year-of-openhands-a-journey-of-
                                                                   open-source-ai-development.
R EFERENCES
Anthropic. Claude agent sdk: Overview and python                 OpenAI. Openai agents sdk. https://github.com/
  sdk. https://anthropic.mintlify.app/en/                          openai/openai-agents-python, 2024.        Ac-
  api/agent-sdk/overview, 2025a.        Accessed:                  cessed: 2025-01-06.
  2025-10-29.
                                                                 OpenAI. Agents sdk and guide. https://platform.
Anthropic. Claude code: An agentic coding tool that lives in       openai.com/docs/guides/agents, 2025. Ac-
  your terminal, 2025b. URL https://github.com/                    cessed: 2025-10-29.
  anthropics/claude-code. GitHub repository, ac-
  cessed 2025-10-28.                                             Pydantic Team.      Unions — discriminated unions.
                                                                   https://docs.pydantic.dev/latest/
Cognition AI. Devin: Ai software engineer. https://                concepts/unions/#discriminated-unions,
  www.cognition.ai/devin, 2024. Accessed: 2025-                    2025. Accessed: 2025-10-29.
  01-06.
                                                                 Russell, S. and Norvig, P. Artificial Intelligence: A Modern
Cursor Team. Cursor: The ai-first code editor. https:              Approach. Prentice Hall Press, USA, 3rd edition, 2009.
  //www.cursor.com, 2024. Accessed: 2025-01-06.                    ISBN 0136042597.
GitHub. Github copilot: Your ai pair programmer. https:
  //github.com/features/copilot, 2021. Ac-                       Smith, C. Openhands context condensensation for more
  cessed: 2025-01-06.                                              efficient ai agents. All Hands AI Blog, April 2025. URL
                                                                   https://openhands.dev/blog/openhands-
Google. Agent development kit (adk). https://                      context-condensensation-for-more-
  google.github.io/adk-docs/, 2025. Accessed:                      efficient-ai-agents.
  2025-10-29.
                                                                 Team, L. Langgraph documentation (durable execu-
Jimenez, C. E., Yang, J., Wettig, A., Yao, S., Pei, K.,            tion, deployment, server/cloud). https://docs.
  Press, O., and Narasimhan, K. R. SWE-bench: Can lan-             langchain.com/oss/python/langgraph/,
  guage models resolve real-world github issues? In The            2025. Accessed: 2025-10-29.
  Twelfth International Conference on Learning Represen-
  tations, 2024. URL https://openreview.net/                     Wang, L., Ma, C., Feng, X., Zhang, Z., ran Yang, H., Zhang,
  forum?id=VTF8yNQM66.                                            J., Chen, Z.-Y., Tang, J., Chen, X., Lin, Y., Zhao, W. X.,
                                                                  Wei, Z., and rong Wen, J. A survey on large language
LangChain.      Langchain:    Runnables and the                   model based autonomous agents. ArXiv, abs/2308.11432,
  langchain expression language (lcel).  https:                   2023. URL https://api.semanticscholar.
  //api.python.langchain.com/en/latest/                           org/CorpusID:261064713.
  core/runnables.html, 2025. Accessed: 2025-10-
  29.                                                            Wang, X., Li, B., Song, Y., Xu, F. F., Tang, X., Zhuge, M.,
MCP Team. Model context protocol (mcp)? https:                    Pan, J., Song, Y., Li, B., Singh, J., Tran, H. H., Li, F.,
 //modelcontextprotocol.io, 2025. Accessed:                       Ma, R., Zheng, M., Qian, B., Shao, Y., Muennighoff, N.,
 2025-10-02.                                                      Zhang, Y., Hui, B., Lin, J., Brennan, R., Peng, H., Ji, H.,
                                                                  and Neubig, G. Openhands: An open platform for AI
Mialon, G., Fourrier, C., Swift, C., Wolf, T., LeCun, Y.,         software developers as generalist agents. In The Thir-
 and Scialom, T. Gaia: a benchmark for general ai as-             teenth International Conference on Learning Represen-
 sistants, 2023. URL https://arxiv.org/abs/                       tations, 2025. URL https://openreview.net/
 2311.12983.                                                      forum?id=OJd3ayDDoF.
           The OpenHands Software Agent SDK: A Composable and Extensible Foundation for Production Agents

Yang, J., Jimenez, C. E., Zhang, A. L., Lieret, K., Yang,        Limitations. The current implementation focuses on
  J., Wu, X., Press, O., Muennighoff, N., Synnaeve, G.,          single-agent conversations. While the event-sourced archi-
  Narasimhan, K. R., Yang, D., Wang, S., and Press,              tecture naturally supports interleaving events from multiple
  O. SWE-bench multimodal: Do AI systems gen-                    agents, coordination mechanisms for multi-agent collabora-
  eralize to visual software domains?       In The Thir-         tion require further design. The security framework, while
  teenth International Conference on Learning Represen-          substantially improved over V0, cannot guarantee complete
  tations, 2025. URL https://openreview.net/                     safety: LLM-based security analysis is subject to adversarial
  forum?id=DGSuigFLFq.                                           prompts and inconsistent classification.


Zhao, W., Jiang, N., Lee, C., Chiu, J. T., Cardie, C., Gallé,   Table 7. Error taxonomy for production reliability analysis.
  M., and Rush, A. M. Commit0: Library generation                  Category          Definition & Examples
  from scratch. In The Thirteenth International Confer-            Infrastructure    Runtime communication and orchestra-
  ence on Learning Representations, 2025. URL https:                                 tion failures (e.g., HTTPStatusError,
  //openreview.net/forum?id=MMwaQEVsAg.                                              AgentRuntimeNotReadyError,
                                                                                     ConnectionError)
                                                                   SDK               Internal SDK logic errors (e.g.,
                                                                                     CondensationError)
A    D ISCUSSION                                                   LLM Provider      External API failures, analyzed sep-
                                                                                     arately     (e.g.,    RateLimitError,
Architectural Alternatives Considered. During the V1                                 AuthenticationError)
redesign, we evaluated several alternative approaches. For
state management, we considered a traditional database-
backed model (used by many web frameworks) but rejected
it because it would couple the SDK to a specific storage
backend and make offline replay difficult; event sourcing
was chosen for its reproducibility and storage-agnostic de-
sign. For execution isolation, we considered mandatory
containerization (as in V0) and fully local-only execution;
the optional isolation model was selected as a compromise
that aligns with MCP’s local-first assumptions while preserv-
ing sandboxing for production. For the package structure,
we considered a monolithic SDK (simpler dependency man-
agement) versus a microservices architecture (maximum
flexibility); the four-package design balances independent
testability with practical dependency management.


Multi-Tenant Deployment. The current security and iso-
lation discussion focuses primarily on single-user scenarios.
In multi-tenant deployments—where multiple users sub-
mit requests to the same agentic application or multiple
applications share infrastructure components—additional
considerations apply. The containerized deployment model
(§4.10) provides process-level isolation between tenants,
with each agent instance running in an independent con-
tainer with a dedicated filesystem and resource limits. The
event-sourced state model naturally supports tenant isola-
tion since each conversation maintains its own event log.
However, shared resources such as LLM API keys, MCP
servers, and secret registries require careful access control.
The SDK’s SecretRegistry supports per-conversation
secrets, and the agent server’s session-based authentication
provides a foundation for multi-tenant access control, but
a comprehensive multi-tenant security audit remains future
work.
