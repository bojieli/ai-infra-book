 F LASH ATTENTION -4: A LGORITHM AND K ERNEL P IPELINING C O -D ESIGN
                FOR A SYMMETRIC H ARDWARE S CALING



                Ted Zadouri * 1 2 Markus Hoehnerbach * 3 Jay Shah * 4 Vijay Thakkar 3 5 Tri Dao 1 2


                                                       A BSTRACT
     Attention, as a core layer of the ubiquitous Transformer architecture, is the bottleneck for large language models
     and long-context applications. While F LASH ATTENTION -3 optimized attention for Hopper GPUs through
     asynchronous execution and warp specialization, it primarily targets the H100 architecture. The AI industry
     has rapidly transitioned to deploying Blackwell-based systems such as the B200 and GB200, which exhibit
     fundamentally different performance characteristics due to asymmetric hardware scaling: tensor core throughput
     doubles while other functional units (shared memory bandwidth, exponential units) scale more slowly or remain
     unchanged. We develop several techniques to address these shifting bottlenecks on Blackwell GPUs: (1) redesigned
     pipelines that exploit fully asynchronous MMA operations and larger tile sizes, (2) software-emulated exponential
     and conditional softmax rescaling that reduces non-matmul operations, and (3) leveraging tensor memory and
     the 2-CTA MMA mode to reduce shared memory traffic and atomic adds in the backward pass. We demonstrate
     that our method, F LASH ATTENTION -4, achieves up to 1.3× speedup over cuDNN 9.13 and 2.7× over Triton on
     B200 GPUs with BF16, reaching up to 1613 TFLOPs/s (71% utilization). Beyond algorithmic innovations, we
     implement F LASH ATTENTION -4 entirely in CuTe-DSL embedded in Python, achieving 20-30× faster compile
     times compared to traditional C++ template-based approaches while maintaining full expressivity.

1    I NTRODUCTION                                                slowly, creating increasingly unbalanced hardware pipelines
                                                                  that demand careful algorithmic co-design.
The Transformer architecture (Vaswani et al., 2017) contin-
                                                                  This has generated sustained interest in making attention
ues to serve as the primary backbone for nearly all AI appli-
                                                                  faster through algorithmic innovations that deeply inte-
cations, from large language models (Brown et al., 2020) to
                                                                  grate knowledge of GPU hardware characteristics. Dao
vision (Dosovitskiy et al., 2020) and multimodal systems.
                                                                  et al. (2022) introduced F LASH ATTENTION, which elim-
For Transformers, the attention mechanism constitutes the
                                                                  inates intermediate reads/writes to slow global memory
primary computational bottleneck, with self-attention scores
                                                                  through novel tiling and kernel fusion. Dao (2023) re-
computed between queries and keys exhibiting quadratic
                                                                  structured this as F LASH ATTENTION -2 to parallelize over
scaling in sequence length. Scaling attention to longer con-
                                                                  the sequence length dimension, improving GPU occu-
texts unlocks new capabilities such as reasoning over mul-
                                                                  pancy. Shah et al. (2024) further adapted the algorithm for
tiple documents (Guo et al., 2021; Shaham et al., 2022),
                                                                  Hopper GPUs as F LASH ATTENTION -3, exploiting asyn-
modeling entire codebases (Roziere et al., 2023), and pro-
                                                                  chronous execution through warp specialization and in-
cessing high-resolution videos (Chen et al., 2022; Ho et al.,
                                                                  corporating FP8 support. Recent work has also explored
2022). Meanwhile, accelerator hardware continues to evolve
                                                                  low-precision attention: SageAttention (Lin et al., 2024b)
rapidly (NVIDIA, 2024b), with each generation delivering
                                                                  achieves speedups through INT8 quantization, SageAtten-
substantially higher peak compute throughput. However,
                                                                  tion2 (Lin et al., 2024a) extends this with INT4/FP8 quanti-
this evolution is asymmetric: while matrix multiplication
                                                                  zation, and SageAttention3 (Lin et al., 2025) demonstrates
units scale aggressively, other functional units such as mem-
                                                                  FP4 quantization on Blackwell consumer GPUs. However,
ory bandwidth and specialized compute units scale more
                                                                  these approaches primarily target consumer GPUs, while
   *
     Equal contribution 1 Department of Computer Science,         most AI compute is deployed on datacenter GPUs. Mean-
Princeton University 2 Together AI 3 Meta 4 Colfax Re-            while, F LASH ATTENTION -3 primarily targets the NVIDIA
search 5 Georgia Tech. Correspondence to: Ted Zadouri             Hopper H100 architecture, while the AI industry has rapidly
<tz6037@princeton.edu>, Jay Shah <jayhshah@colfax-
                                                                  transitioned to deploying Blackwell-based datacenter sys-
intl.com>, Tri Dao <tri@tridao.me>.
                                                                  tems (NVIDIA, 2024b) such as the B200 and GB200, which
Proceedings of the 8 th MLSys Conference, Santa Clara, CA, USA,   represent a new generation of GPUs with fundamentally
2025. Copyright 2025 by the author(s).
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

different performance characteristics.                               and larger tile sizes.
A critical trend in accelerator evolution is the asymmetric    Beyond algorithmic innovations, we implement
scaling of hardware units. Although Blackwell B200 dou-        F LASH ATTENTION -4 entirely in CuTe-DSL embed-
bles the tensor core throughput compared to Hopper H100        ded in Python, achieving 20-30× faster compile times
(2.25 PFLOPS vs. 1 PFLOPS for FP16/BF16), other func-          compared to traditional C++ template-based approaches
tional units (shared memory bandwidth, exponential units,      while maintaining full expressivity. This framework sig-
and integer/floating point ALUs) scale more slowly or re-      nificantly improves developer productivity and lowers the
main unchanged. As a result, non-MMA resources emerge          barrier to entry, enabling researchers to rapidly prototype
as bottlenecks. Our roofline analysis (Sections 3.1 and 3.2)   and deploy new attention variants without deep expertise in
reveals that for typical attention workloads on Blackwell,     C++ template metaprogramming.
surprisingly, shared memory traffic and exponential opera-
                                                               To empirically validate our method, we benchmark
tions now dominate execution time, exceeding MMA com-
                                                               F LASH ATTENTION -4 on the B200 GPU and show that (1)
pute by 25-60%. Additionally, Blackwell introduces new ar-
                                                               BF16 achieves up to 1.3× speedup over cuDNN and 2.7×
chitectural features: 256 KB of tensor memory (TMEM) per
                                                               over the Triton implementation, (2) we achieve near-peak
SM for storing intermediate tensor core results, 128×128
                                                               utilization on the shifted bottleneck resources, reaching up
MMA tiles (double the area of Hopper’s 64×128), and fully
                                                               to ∼1600 TFLOPS (71% theoretical max), and (3) for large
asynchronous tensor core operations that write directly to
                                                               sequence lengths, F LASH ATTENTION -4 outperforms alter-
TMEM. Simply porting existing attention algorithms to this
                                                               native attention implementations.
new hardware either leaves significant performance on the
table or is impossible due to lack of forward compatibility    We open open source F LASH ATTENTION -4 with a
for Hopper MMA instructions.                                   permissive license and are working to integrate it
                                                               with popular libraries such as PyTorch and Mega-
To this end, we propose F LASH ATTENTION -4, which co-
                                                               tron LM to benefit the largest number of researchers
designs the algorithm and kernel implementation to ad-
                                                               and developers.   The code is available at https:
dress the shifting bottlenecks in modern GPU architectures.
                                                               //github.com/Dao-AILab/flash-attention/
Rather than treating hardware as a uniform compute re-
                                                               tree/main/flash_attn/cute
source, we explicitly identify and mitigate bottlenecks in
non-matmul units through algorithmic innovations:
                                                               2      BACKGROUND
1. Redesigned pipeline for maximum overlap: We de-
   velop new software pipelines for both forward and back-     2.1     Multi-Head Attention
   ward passes that exploit Blackwell’s fully asynchronous
   MMA operations and larger tile sizes to maximize over-      Let Q, K, V ∈ RN ×d be the query, key and value input
   lap between tensor cores, softmax computation, and          sequences associated to a single head, where N is the se-
   memory operations.                                          quence length and d is the head dimension. The attention
2. Exponential unit bottleneck mitigation: For the for-        output O ∈ RN ×d is computed as:
   ward pass, we implement software-emulated exponen-                             S = αQK⊤ ∈ RN ×N ,
   tial functions using polynomial approximation on FMA
   units, increasing exponential throughput. We also intro-                       P = softmax(S) ∈ RN ×N ,
   duce conditional softmax rescaling that skips unneces-                         O = PV ∈ RN ×d ,
   sary rescaling operations.                                                                                    √
3. Shared memory traffic reduction: For the backward           where softmax is applied row-wise and α = 1/ d is the
   pass, we leverage tensor memory to store more inter-        scaling factor. In practice, we subtract rowmax(S) from
   mediate results, reducing shared memory traffic. We         S for numerical stability. For multi-head attention (MHA),
   also leverage Blackwell’s 2-CTA MMA mode, so each           each head has its own set of projections, and this computa-
   CTA stages and loads half of operand B to further reduce    tion parallelizes across multiple heads and batches.
   shared memory traffic, which we exploit to restructure      Given output grad dO ∈ RN ×d , the backward computes:
   the dQ step to halve the number of atomic reductions.
   We also implement a deterministic execution mode with                      dV = P⊤ dO,     dP = dOV⊤ ,
   minimal performance overhead, enabling reproducible                         dS = dsoftmax(dP),
   training for reinforcement learning applications.
                                                                               dQ = αdSK,     dK = αdS⊤ Q,
4. Improved scheduling and resource allocation: We de-
   velop new CTA scheduling strategies and register alloca-    where dsoftmax(dP) denotes row-wise softmax gradient
   tion schemes tailored to Blackwell’s resource constraints   ds = (diag(p) − pp⊤ )dp for p = softmax(s).
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

2.2   GPU Hardware Characteristics and Execution                 Hardware support for asynchrony allows for warp-
      Model                                                      specialized kernels, where the warps of a CTA are divided
                                                                 into producer or consumer roles that only ever issue data
We describe the aspects of the GPU’s execution model rele-       movement or computation (Bauer et al., 2011).
vant for F LASH ATTENTION -4, with a focus on the NVIDIA
Blackwell architecture (B200 & GB200). We highlight key          2-CTA tensor core: Blackwell supports a 2-CTA tensor
differences from the prior Hopper architecture that motivate     core MMA mode in which a CTA pair within the same
the optimizations in F LASH ATTENTION -4.                        thread block cluster cooperatively executes a single MMA,
                                                                 allowing the operation to read and write tensor memory from
                                                                 both CTAs. One thread in the pair initiates the MMA, but
Memory hierarchy: The GPU’s memories are organized
                                                                 the peer CTA must be launched and remain active while the
as a hierarchy of data locales, with capacity inversely re-
                                                                 operation is in flight. Compared to single CTA MMAs that
lated to bandwidth. Global memory (GMEM), also known
                                                                 limit the dimension M to 128, the paired mode supports M
as HBM, is the off-chip DRAM that is accessible to all
                                                                 = 128 or 256 by partitioning the A tile and the accumulator
streaming multiprocessors (SMs). Data from GMEM are
                                                                 across the pair in dimension M and partitioning the B tile
transparently cached in an on-chip L2 cache. Next, each
                                                                 across the two CTAs in dimension N so that each CTA stages
SM contains a small, programmer-managed, highly banked
                                                                 only half of B in its own shared memory while the hardware
cache called shared memory (SMEM) on the chip. Lastly,
                                                                 consumes the combined B tile during the multiply. This
there is the register file within each SM.
                                                                 reduces redundant shared memory capacity and bandwidth,
Blackwell introduces a new memory level called tensor            but since these operations touch tensor memory across the
memory (TMEM), a 256 KB on-chip memory per SM specif-            CTA pair, kernels must launch CTAs in fixed pairs and use a
ically designed for storing intermediate results of tensor       consistent 2-CTA mode for tensor memory and tensor core
core operations. Unlike shared memory, TMEM is warp-             operations throughout the kernel
synchronous and tightly coupled with the tensor cores, en-
abling the matrix multiply-accumulate (MMA) units to write       Shifting bottlenecks: A key trend reflected in Blackwell
outputs directly to TMEM without consuming registers.            is that tensor core throughput scales faster than other func-
This alleviates the extreme register pressure that plagued       tional units. Blackwell doubles the FP16/BF16 tensor core
Hopper kernels and enables larger tile sizes. TMEM is allo-      throughput compared to Hopper (2.25 PFLOPS (NVIDIA,
cated in 32-column (16 KB) granules and requires explicit        2024b) vs 1 PFLOPS (NVIDIA, 2022) per GPU), but shared
programmer management for allocation, deallocation, and          memory bandwidth and exponential unit throughput remain
data movement.                                                   unchanged or scale more slowly. This imbalance shifts
                                                                 the performance bottleneck away from matrix multiplica-
Thread hierarchy: The GPU’s programming model is or-             tion toward shared memory traffic and non-matmul opera-
ganized around logical groupings of execution units called       tions like softmax. As our roofline analysis in Sections 3.1
threads. From the finest to the coarsest level, the thread       and 3.2 shows, this requires careful kernel design to maxi-
hierarchy is comprised of threads, warps (32 threads), warp-     mize overlap between MMA operations and these bottleneck
groups (4 contiguous warps), threadblocks (i.e. coopera-         resources.
tive thread arrays or CTAs), threadblock clusters, and grids.    The throughput of several hardware components on B200
Threads in the same CTAs are co-scheduled on the same            (and GB200) is listed below.
SM, and CTAs in the same cluster are co-scheduled on the
same GPC. SMEM is directly addressable by all threads            1. Tensor cores: The BF16 MMA has a throughput of 8192
within a CTA, whereas each thread has at most 256 registers         ops / clock / SM, doubled from 4096 ops / clock / SM of
(RMEM) private to itself.                                           Hopper. This can be derived from the theoretical maxi-
                                                                    mum FLOPS: 2.25 PFLOPS / 1850 Mhz clock speed /
                                                                    148 SMs = 8192 ops / clock / SM.
Tensor cores and increased asynchrony: Blackwell fea-
tures fifth-generation tensor cores that operate on tiles sig-   2. Exponential unit. The multifunction unit (MUFU) on
nificantly larger than in previous architectures. Each MMA          B200 and GB200 can perform 16 ops / clock / SM, the
tensor core instruction processes 128 × N tiles (typically          same as Hopper (NVIDIA, 2024a). We note that B300
N = 128 or 256), compared to 64×N on Hopper. Crucially,             and GB300 GPUs have doubled the exponential through-
Blackwell MMAs write their output directly to TMEM asyn-            put to 32 ops / clock / SM, though these GPUs are not
chronously, whereas Hopper MMAs write to registers. This            yet widely available at the time of writing.
full asynchrony enables better overlap between computation       3. SMEM: the read throughput is 128 bytes / clock / SM,
and other operations, as the MMA units no longer block on           the same as Hopper, as measured by microbenchmark-
register writeback.                                                 ing (Luo et al., 2025).
             F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

We see that the MMA throughput on Blackwell has doubled                      Resource           1283     256 × 1282
compared to Hopper, but other hardware units do not nec-                     MMA compute        1024       2048
essarily get faster at the same rate. This reflects a broader                Shared memory       768       1536
trend in accelerator design: increasing the throughput of the                Exponential unit   1024       2048
most important components (typically matrix multiply units)
to get higher performance under similar power / silicon area      Table 1: Roofline analysis (cycles) for the attention forward
constraint.                                                       pass. For both tile sizes, MMA compute and exponential
                                                                  unit are the primary bottlenecks.
3     A LGORITHM
                                                                  memory. The total shared memory reads are ⌈M/128⌉ ×
3.1     Attention forward pass                                    ⌈N/128⌉ × (128d + 128d) = ⌈M/128⌉⌈N/128⌉ × 256d
                                                                  elements. For PV (TS), computing the M × d output re-
We first do a roofline analysis to show the bottlenecks of        quires ⌈M/128⌉ × ⌈d/128⌉ MMA instructions, each read-
attention forward pass, which motivates our new pipeline          ing a N × 128 chunk of V from shared memory, totaling
design, as well as changes in the F LASH ATTENTION algo-          ⌈M/128⌉ × ⌈d/128⌉ × 128N elements. At 2 bytes per ele-
rithm to increase the throughput of the exponential unit and      ment (bf16) and 128 bytes per cycle bandwidth, the shared
avoid most of the softmax rescaling steps.                        memory  (Tsmemm) read time
                                                                      l ml                 l is ml m
                                                                      M        N           M        d
                                                                  = 2 128           256d+2 128         128N = 3M Nd
                                                                                                               8192 cycles
3.1.1    Feeds and Speeds                                                     128                  128
                                                                                                                      (2)
We provide intuition for our kernel design and optimizations      (assuming M , N , d are multiples of 128).
by first analyzing the roofline, based on the throughput of the   Exponential unit. The exponential unit computes element-
matmul units (tensor cores), shared memory (smem), and            wise operations required for the softmax computation. The
exponential unit. We note that this is a simplified analysis      forward pass requires exponential operations on M × N
that does not consider all resources in the GPU (e.g., floating   values (corresponding to the attention matrix S). With a
point math, register bandwidth, L2 bandwidth). Neverthe-          throughput of 16 operations per cycle, the exponential unit
less, it can identify bottlenecks.                                requires
Let the shape of the tile along the length dimension of                                       MN
                                                                                      Texp =        cycles.               (3)
the sequence of Q and K be M × N , and let the head                                            16
dimension be d. We analyze the compute and memory                 Table 1 summarizes the analysis for two typical tile con-
traffic requirements to identify the performance bottleneck.      figurations. For M = N = d = 128, the resources
                                                                  are well-balanced, with shared memory (768 cycles) be-
MMA compute. The forward pass performs two ma-
                                                                  ing slightly lower than both MMA compute and expo-
trix multiply-accumulate (MMA) operations per iteration:
                                                                  nential unit (both 1024 cycles). For the larger tile size
QK⊤ (computing M × N output from M × d and d × N
                                                                  M = 256, N = d = 128, the shared memory traffic in-
inputs) and PV (computing M × d output from M × N and
                                                                  creases to 1536 cycles due to reading MMA operands multi-
N × d inputs). Each MMA requires 2M N d floating-point
                                                                  ple times, while MMA compute and exponential unit double
operations. With a tensor core throughput of 8192 FLOPs
                                                                  to 2048 cycles. This analysis motivates our kernel design
per cycle, the total compute time is
                                                                  to (1) use large tile sizes and maximize overlap between
                           4M N d                                 MMA operations and softmax computations (2) increase
                  TMMA =            cycles.          (1)
                            8192                                  the throughput of exponential by using other hardware units
Shared memory traffic. Of the two MMAs, one is shared-            (3) reduce the time of unnecessary non-matmul operations.
shared (SS) where both operands are read from shared mem-
ory (QK⊤ ), while the other is tensor-shared (TS) where           3.1.2   New pipeline to overlap matmul and softmax
operand A is read from tensor memory and operand B from
                                                                  Since the Blackwell architecture doubled the tensor core
shared memory (PV). Since each MMA instruction op-
                                                                  flops again, taking care to overlap softmax and tensor core
erates on tiles of size 128 × 128, computing an M × N
                                                                  operations is even more crucial than on Hopper. We follow
output requires ⌈M/128⌉ × ⌈N/128⌉ MMA instructions.
                                                                  a ping-pong schedule similar to FA-3, where two tiles of
Crucially, when multiple MMA instructions are needed, the
                                                                  the output are computed per thread block. While one tile’s
shared memory operands are read multiple times.
                                                                  tensor core operations are executed, the other tile computes
For QK⊤ (SS), computing the M × N output requires                 softmax. While Hopper tensor cores hold the accumulator
⌈M/128⌉ × ⌈N/128⌉ MMA instructions, each reading a                in registers, with four threads per row in an interleaved
128×d chunk of Q and a d×128 chunk of K⊤ from shared              pattern, Blackwell tensor cores hold their accumulators in
             F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling




Figure 1: FlashAttention-4 forward pipeline. The superscript H denotes the matrices corresponding to the ”high” Q tile, and superscript L
denotes matrices corresponding to the ”low” Q tile. Each Q tile corresponds to 128 query tokens.


tensor memory. Additionally, a single accumulator tile on              tile of S and two tiles of P, or two tiles of S that overlap
Blackwell is 128 by 128 elements large, where Hopper’s                 with P. We choose the latter because it allows us to start
tile size was 64 by 128.                                               our software pipeline by immediately computing two S tiles.
                                                                       It also leaves some tensor memory to communicate rescale
The natural way to distribute work across these tiles is then
                                                                       statistics to the correction warpgroup.
to have two warpgroups of 128 threads each, with each
thread processing an entire row. This eliminates the need              One issue of the larger Blackwell tile sizes and the chosen
for inter-warp shuffles to reduce the row max, and for mul-            thread assignment is that, unless we re-load from tensor
tiple statistics registers per thread. Just like with FA-3, we         memory, we must hold an entire row of 128 elements in
explicitly synchronize the two softmax warpgroups to not               register. Given that we use two softmax warpgroups, one
overlap in their critical section, which is the part of expo-          correction warpgroup, and one warpgroup to drive tensor
nential computation. Each softmax warpgroup proceeds by                cores and TMA units, assigning sufficient registers to soft-
first loading the entire row into registers, then computing            max and preventing register spills is critical. For BF16 input
the maximum, then computing the softmax (i.e., subtract                data types, we need to hold 128 registers for the input, and
the max, rescale, exponentiate, convert to input precision),           potentially 64 registers for the output (plus miscellaneous
and finally computing the row sum.                                     and temporary registers). To reduce register pressure, we
                                                                       stage out storing P: The first three quarters are stored once
Another difference from FA-3 is that since we transfer P via
                                                                       (and trigger the corresponding MMA operations), and the
tensor memory rather than register file, we can decouple the
                                                                       last quarter is stored separately.
rescaling of the output to a separate “correction” warpgroup
and thus take it out of the critical path.
                                                                       3.1.3   Emulation of the exponential function
Several tensor memory partitionings are possible to achieve
this pipeline overlap. All must allocate two tiles worth               Exponential throughput bottleneck. On modern GPUs,
of output, leaving (at head dimension 128) half the tensor             the exponential function is computed by the multi-function
memory to store S and P. That memory can store two                     unit (MUFU), which has significantly lower throughput
copies of S or four copies of P (assuming the input of the             than the tensor cores used for matrix multiplication. On
FP16 or BF16 tensor core). This leaves us with roughly two             B200 and GB200 GPUs, MUFU provides 16 operations per
partitioning options for the remaining tensor memory: one              clock per SM, compared to 8192 operations per clock per
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

SM for matrix multiplication. Since softmax computation                                      FP32 vs FP64                BF16 vs FP64

requires many exponential evaluations, this disparity makes        Method              Max rel err   Mean rel err   Max rel err   Mean rel err

the exponential function a critical bottleneck in attention        Ideal (FP64→BF16)        —             —         3.89 × 10−3   1.41 × 10−3
                                                                   Hardware MUFU.EX2   1.41 × 10−7   3.04 × 10−8    3.89 × 10−3   1.41 × 10−3
kernels.                                                           Degree 3            8.77 × 10−5   5.43 × 10−5    3.90 × 10−3   1.41 × 10−3
                                                                   Degree 4            3.05 × 10−6   1.84 × 10−6    3.89 × 10−3   1.41 × 10−3
Software emulation via polynomial approximation. To                Degree 5            1.44 × 10−7   5.48 × 10−8    3.89 × 10−3   1.41 × 10−3
increase exponential throughput, we implement a software
emulation of 2x using floating-point FMA units, which can          Table 2: Accuracy of 2x polynomial emulation on [0, 1),
operate in parallel with MUFU. We use the classical range          measured against FP64 reference on 4M random inputs.
reduction technique (Cody-Waite) and then the polynomial           FP32 columns measure the raw polynomial output; BF16
approximation (Muller et al., 2018). The key insight is to         columns measure after rounding to BF16. The BF16 quanti-
decompose the exponential computation:                             zation error dominates for all degrees ≥ 3.
                     2x = 2⌊x⌋ 2x−⌊x⌋                   (4)
 where ⌊x⌋ is the integer part and x − ⌊x⌋ ∈ [0, 1) is the         all exponential evaluations would increase register pressure
fractional part.                                                   and could cause spills that negate the throughput benefit.
                                                                   Instead, we apply emulation to only a subset of the entries
The integer part 2⌊x⌋ can be computed efficiently using bit        in each softmax row (10–25%), with the remaining entries
manipulation of the IEEE 754 floating-point representation.        computed via hardware MUFU.EX2. The exact fraction is
Since the exponent field directly represents powers of two,        tuned empirically based on the ratio of MMA and exponen-
computing 2⌊x⌋ amounts to a shift and add operation on             tial throughput for a given tile configuration.
the exponent bits, which can be done using integer ALU
instructions.                                                      Numerical accuracy. Table 2 compares the accuracy of
                                                                   polynomial approximations of different degrees against the
For the fractional part, we approximate 2xfrac where xfrac ∈       hardware MUFU.EX2 instruction, measured on 4M random
[0, 1) using a polynomial:                                         inputs in [0, 1). We report two metrics: the FP32-level error
                                  n
                                  X                                (before any quantization) and the BF16-level error (after
                      xfrac
                     2        ≈         pi xifrac           (5)    rounding the FP32 output to BF16), both measured against
                                  i=0                              a FP64 reference.

with p0 = 1.0 and the remaining coefficients chosen to min-        At the FP32 level, the degree-3 polynomial has a maximum
imize the relative approximation error over [0, 1), calculated     relative error of 8.8 × 10−5 , roughly 600× higher than hard-
using the Sollya software package (Chevillard et al., 2010).       ware. However, after rounding to BF16, the errors become
The polynomial evaluation uses Horner’s method with FMA            nearly indistinguishable: the quantization error of BF16
instructions, achieving high throughput.                           (∼3.9 × 10−3 ) dominates the polynomial approximation
                                                                   error for all degrees ≥ 3. The degree-3 polynomial matches
The complete algorithm proceeds as follows:                        hardware to within 1 BF16 ULP on 99% of inputs, which
1. Clamp x to be at least −127 to avoid underflow                  is sufficient for attention computation where the softmax
                                                                   output is consumed with BF16 precision. Higher-degree
2. Compute ⌊x⌋ using round-down mode: add 223 + 222
                                                                   polynomials close the FP32 gap: degree 5 matches hardware
   to x (forcing the fractional bits into the mantissa), then
                                                                   to within 2× in maximum relative error, at the cost of two
   subtract it back with round-down mode
                                                                   additional FMA instructions per evaluation.
3. Compute fractional part: xfrac = x − ⌊x⌋
                                                                   3.1.4    Skipping online softmax rescaling
4. Evaluate polynomial to get 2xfrac
5. Combine integer and fractional parts: shift ⌊x⌋ into the        FlashAttention online softmax. FlashAttention computes
   exponent field and add the mantissa bits of 2xfrac              attention softmax(QK ⊤ )V in blocks to minimize memory
                                                                   traffic. For numerical stability, the algorithm maintains
By distributing exponential computations across both
                                                                   running statistics as it processes blocks. When computing
MUFU and FMA units, this approach effectively increases
                                                                   block j, let Sj = QKj⊤ be the attention scores for that
the exponential throughput, alleviating a key bottleneck in
                                                                   block. The online softmax algorithm tracks:
attention computation.
Partial emulation. Although polynomial emulation in-                        mj = max(mj−1 , rowmax(Sj ))
creases exponential throughput, it comes at a cost: addi-                    ℓj = emj−1 −mj ℓj−1 + rowsum(eSj −mj )
tional registers (to hold intermediate values and coefficients),
higher register bandwidth consumption, and longer latency          where mj is the running max and ℓj is the running sum
compared to the MUFU instruction. Using emulation for              of exponentials (normalizer). The intermediate output Oj
             F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

is updated as: Oj = emj−1 −mj Oj−1 + eSj −mj Vj . The             MMA involves an M × N matrix, an M × d matrix, and
rescaling factor emj−1 −mj ensures numerical stability by         an d × N matrix (varying which one is the output matrix),
renormalizing previous results when larger values are en-         requiring 2M N d floating-point operations. With a tensor
countered.                                                        core throughput of 8192 FLOPs per cycle, the total compute
                                                                  time is
Conditional rescaling. The step emj−1 −mj Oj−1 requires                                    10M N d
a vector multiplication. We make two simple observations:                         TMMA =            cycles.              (7)
                                                                                             8192
1. Rescaling is only necessary when mj > mj−1 , i.e.,             Shared memory traffic. Of the five MMAs, three of them
   when new larger values are found.                              – S⊤ = KQ⊤ , dP⊤ = VdO⊤ , and dQ = dSK – are
2. We can tolerate some ”slack” in the rescaling: only            shared-shared (SS) operations where both operands are read
   rescale when mj − mj−1 > τ , where τ is a thresh-              from shared memory, while two – dV = P⊤ dO and dK =
   old (typically set to log2 (256) = 8.0, corresponding to       dS⊤ Q – are tensor-shared (TS) operations where operand
   a rescaling factor of 256.0). As long as we keep track         A is read from tensor memory and operand B from shared
   of the statistics (the total scaling we have done), we can     memory. The SS MMAs read in total 2M d + 3N d + M N
   still get the true denominator at the end to get the right     elements from shared memory, while the TS MMAs read
   final output.                                                  in total 2M d elements from shared memory. At a shared
                                                                  memory bandwidth of 128 bytes per cycle and with each
In F LASH ATTENTION -4, we modify the algorithm as:               element being 2 bytes (bf16), this contributes
     (                                                                                   4M d + 3N d + M N
      emj−1 −mj Oj−1 + eSj −mj Vj             if mj − mj−1 > τ             Tsmem,MMA =                          cycles.     (8)
Oj =                                                                                              64
      Oj−1 + eSj −mj−1 Vj                     otherwise            Additionally, the algorithm writes the intermediate gradient
                                                           (6)    dS of size M × N to shared memory in bf16, requiring
When mj − mj−1 ≤ τ , we skip updating m and continue              2M N bytes or M N/64 cycles. The gradient dQ of size
using mj−1 . This maintains the correctness because at the        M × d is written in fp32 (4 bytes per element) to shared
end of the computation, all accumulated values are renor-         memory, then read back via TMA for the reduction, totaling
malized by the true maximum mfinal and the final normalizer       8M d bytes of shared memory traffic or M d/16 cycles.
ℓfinal :
                                  1                               The total shared memory access time (Tsmem ) is therefore
                    Output =           Ofinal
                                ℓfinal                                   4M d + 3N d + M N      MN       Md
 This modification significantly reduces the number of rescal-                               +        +       cycles.     (9)
                                                                                  64             64      16
ing operations while maintaining numerical accuracy, as the
final normalization step corrects any small deviations intro-     Exponential unit. The exponential unit computes element-
duced by skipping intermediate rescaling.                         wise operations (exponentials, logarithms, and related non-
                                                                  linear functions) required for the softmax and its gradient.
In practice, to avoid warp divergence, we rescale when any        The backward pass requires exponential operations on the
of the threads in the warp needs rescaling.                       M × N values (corresponding to the attention matrix S and
                                                                  related terms). With a throughput of 16 operations per cycle,
3.2     Attention backward pass                                   the exponential unit requires
                                                                                              MN
3.2.1    Feeds and Speeds                                                             Texp =        cycles.                (10)
                                                                                               16
                                                                   Table 3 summarizes the analysis for the typical tile configu-
Similar to the forward pass, we first provide intuition for our   ration M = N = d = 128. The shared memory traffic time
kernel design and optimizations by analyzing the roofline,        of 3328 cycles exceeds both the MMA compute time (2560
based on the throughput of the matmul units (tensor cores),       cycles) and the exponential unit time (1024 cycles), indicat-
shared memory (smem), and exponential unit.                       ing that shared memory bandwidth is the primary bottleneck,
Let the tile shape along the sequence length dimension of         though less severely than global memory traffic would sug-
Q and K be M × N , and let the head dimension be d. We            gest. This motivates our kernel design to maximize overlap
analyze the compute and memory traffic requirements to            between MMA operations and other computations to hide
identify the performance bottleneck. Unlike for the forward       shared memory latency.
pass, we make the assumption that M = N = d = 128 to
simplify the formulas for the smem cycle count, although          3.2.2   New pipeline to overlap matmul and softmax
we retain variable names for clarity.
MMA compute. The backward pass performs five matrix               The backward pass in flash attention performs five MMA
multiply-accumulate (MMA) operations per iteration. Each          operations, corresponding to recomputing S, and the two
               F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling




Figure 2: FlashAttention-4 backward computation graph (5 MMA operations + 2 elementwise operations), showing the 1-CTA MMA
mode software pipeline order across the prologue, main loop, and tail.


Resource                          Cycles (N = d = 128) Cycles (N = d = 128)   ing, MMA, compute, and reduction operations. In particular,
                                   1-CTA (M = 128)      2-CTA (M = 256)
MMA compute                              2560                  2560
                                                                              note that we do not have sufficient tensor memory to fit five
Shared memory (MMA operands)             2048                  1536           accumulator tiles. At most, four tiles of 128 by 128 ele-
Shared memory (dS write)                 256                    256
Shared memory (dS DSMEM)                  0                     384           ments can fit and dV and dK accumulate, and thus cannot
Shared memory (dQ write + read)          1024                  512            share their space. In our implementation, we have S and
Total shared memory                      3328                  2688
Exponential unit                         1024                  1024           P share one of the tmem blocks (at offset 0) and we have
                                                                              dP, dS and dQ share the other one. We demonstrate the
Table 3: Roofline analysis for the attention backward pass                    computational graph of the FA-4 bwd in Figure 2.
with M = N = d = 128. Shared memory traffic is
the bottleneck, exceeding MMA compute time by approx-                         3.2.3   2-CTA backward pass: Reducing shared memory
imately 30%. In the 2-CTA setting with M = 256 and                                    traffic and global atomic adds
N = d = 128 (with an exception to the dQ mma, with
M = N = 128 and d = 256), shared memory traffic ex-                           Even with improved pipelining and with two of the ten
ceeds MMA compute time by approximately 5%.                                   GEMM operands resident in tensor memory, shared memory
                                                                              bandwidth still dominates the backward pass. Across the
                                                                              five GEMMs, the remaining eight BF16 operands are loaded
gradient computations induced by QK (which yields dQ
                                                                              from shared memory to feed the tensor cores, and this shared
and dK) and PV (which yields dP and dV) respectively.
                                                                              memory traffic incurs about 30% more cycles than the tensor
In FA-3, accumulators are stored in registers and registers
                                                                              core compute. To further mitigate this bottleneck, we use
are a limited resource. This imposes significant ordering
                                                                              the 2-CTA mma mode introduced by Blackwell, in which
constraints that—in effect—serialize the compute graph,
                                                                              the output accumulator is partitioned in the M dimension.
i.e., compute S, dP, dV, dQ, dK, with only the TMA load
                                                                              With an MMA tile shape of M = 256 and N = K = 128,
running significantly out of turn. In addition to that, the al-
                                                                              the two CTA act as a single larger tile: each CTA loads and
gorithm is similar: It iterates along the KV sequence length
                                                                              stages half of operand B and keeps only its own accumulator
dimension and computes values transposed w.r.t. the for-
                                                                              slice.
ward pass, since that is the layout that the dV and dK
gradient calculations require to read one of their operands                   Shared memory traffic. With five GEMMS in the back-
from tensor memory. dQ accumulates through the atomics.                       ward pass, we use MMA tile shape of M = 256 and
                                                                              N = K = 128, which roughly halves the shared mem-
In FA-4, TMEM enables additional schedules compared
                                                                              ory traffic for operand B. In the FlashAttention backward
to FA-3 that provide significant overlap between MMA
                                                                              pass, each CTA holds a fixed KV tile (parallelize the outer
and non-MMA operations. Specifically, just as with the
                                                                              loop across N CTAs) and streams over M tiles in the in-
forward pass, we are trying to hide the latency of the softmax
                                                                              ner loop. The accumulation of dQ is a reduction in the
calculation. In FA-3, the softmax computation overlaps with
                                                                              KV sequence in the outer loop, but the 2-CTA MMA only
the MMA for dP. From the previous section, we know that
                                                                              splits the output tile, not the reduction axis, and the reduc-
on Blackwell we need at least two MMA operations to run
                                                                              tion dimension of the dQ MMA is N , which is naturally
concurrently.
                                                                              split across the CTA pair. As a result, each CTA still needs
We achieve this by using the dQ and dK MMA’s of the                           the full reduction for the rows it owns. To address this
previous iteration. This requires careful management of                       conflict on the reduction axis, we use distributed shared
shared memory and tensor memory resources between load-                       memory (DSMEM) to exchange half of the dS between the
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling




Figure 3: In the 2-CTA backward dQ step, the CTA pair uses DSMEM to exchange half of the dS tile so each CTA forms an ( M
                                                                                                                        2
                                                                                                                          × 2N )
operand and can run a CTA-pair UMMA with a doubled reduction.


two CTA’s since they are in the same cluster. This approach       form its reduction, and then release the lock by incrementing
repacks dS so that it is partitioned along the non-reduction      the semaphore counter.
axis, with each CTA owning its M    2 rows and holding the        This lock-based approach impacts performance for two main
full 2N reduction. As a result, the per CTA dQ MMA tile
                                                                  reasons: (1) issuing the memory fence to ensure device-
shape is ( M                                          M
           2 , 2N )(2N, d) and accumulates a tile ( 2 , d) in     wide visibility of the semaphore write (required for correct
tensor memory. In 2-CTA MMA mode, the MMAs for S,
                                                                  acquire–release semantics), and (2) introducing stalls as
dP, dV and dK run with the tile M = 256, while dQ uses
                                                                  each CTA waits for previous CTAs reducing on a common
M = 128 but with double reduction 2N = 256. We then
                                                                  dQ tile to complete. In load-imbalanced situations, a naive
reordered the software pipeline with respect to the 1-CTA
                                                                  choice of CTA order can severely degrade performance.
variant to hide the DSMEM latency. We compute dP for
                                                                  In general, we do CTA swizzling in the head and batch
the current tile before computing dQ for the tile of the pre-
                                                                  dimensions to reduce stalls (up to L2 cache capacity, cf.
vious iteration. The dQ tile is small enough to fit in TMEM
                                                                  Section 3.3). For causal masking, we additionally launch
alongside P, reusing the same TMEM region as S, so we no
                                                                  KV blocks in descending order, traverse query blocks in as-
longer reuse the same TMEM region for dP and dQ as we
                                                                  cending order starting from the diagonal, and order the dQ
do in the 1-CTA mode. With this new pipelining ordering,
                                                                  reductions by descending query block index. This “shortest-
we can compute the element-wise dS of the current tile in
                                                                  processing-time-first” (SPT) schedule ensures that no CTA
parallel with the dQ MMA of the tile from the previous
                                                                  is stalled on its first dQ write.
iterations. See Figure 3 for an illustration of how the dQ
step is decomposed.
                                                                  3.3   Scheduling
dQ atomic adds. A complementary benefit of this dQ
decomposition is that it halves the number of global atomic
reductions. Atomic updates introduce nondeterminism and           In many situations, such as with causal masking or vari-
are expensive as they occur in every iteration of the inner       able sequence length (varlen), the attention kernel is natu-
loop. Consequently, each CTA writes only half of the dQ           rally load-imbalanced – SMs are assigned worktiles whose
tile and performs half as many global atomic reductions as        mainloops differ in length, since some worktiles require
the 1-CTA counterpart.                                            more loads and MMAs than others. Furthermore, we can
                                                                  choose the order in which SMs process tiles, for instance
                                                                  by defining a preferred linearization of the grid coordinates.
3.2.4   Deterministic backward pass
                                                                  Abstracting away any specific features of attention, we can
                                                                  then apply general results on makespan minimization for
Our backward kernel introduces nondeterminism for the
                                                                  identical parallel processors to our context. In particular,
gradient computation due to inter-CTA reductions in global
                                                                  in FlashAttention-4, we use the classical idea of longest-
memory (affecting dQ in general, and dK/dV in the case
                                                                  processing-time-first (LPT) scheduling (Graham, 1969). We
of GQA). To ensure reproducibility and facilitate reliable
                                                                  emphasize that the way in which we apply this idea works
debugging during training, we also provide a determinis-
                                                                  across all GPU architectures and was also validated as an
tic execution mode. The standard solution, which we also
                                                                  improvement to FlashAttention-3 on Hopper GPUs.
adopt, is to serialize the global reductions using a semaphore
lock. Specifically, each CTA writing to a common dQ tile          LPT for causal masking. The standard attention grid is
must acquire the lock according to a predefined order, per-       given by (mblocks, heads, batches) and is computed in
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling




Figure 4: Forward pass TFLOPS on B200 (FP16/BF16) with head dimension 128. Left: non-causal attention. Right: causal attention.
FA4 achieves 1.1-1.3× speedup over cuDNN 9.13.0 and 2.1-2.7× over Triton across sequence lengths




Figure 5: Backward pass TFLOPS on B200 (FP16/BF16) with head dimension 128. Left: non-causal attention. Right: causal attention.


increasing order left-to-right. But scores are masked out         arbitrarily suboptimal with respect to load balancing – for
above the diagonal, so for fixed head and batch, SMs end up       example, we could have shorter square prefills followed by
inefficiently processing worktiles from shortest to longest.      long-context decodes. To ameliorate this, we can enforce
On the other hand, a naive LPT order is also suboptimal,          an LPT order by launching a preprocessing kernel to sort
since for different batches, mainloop KV loads won’t hit in       batches according to their maximum per-worktile execu-
L2 cache, and loading all KV heads first can thrash the L2        tion time, writing out the additional metadata of a virtual to
cache if they exceed its capacity. Instead, we always process     actual batch index mapping that will be subsequently read
batches as the outermost dimension, and swizzle over heads.       back into the attention kernel in order to traverse batches in
This means that we divide heads into sections that don’t          sorted order. This metadata can be cached and thus results
overflow L2 cache; the tile scheduler then traverses the grid     in no performance loss from sorting.
by heads per section, mblocks in reverse order, sections, and
finally batches. In particular, for MQA or GQA, we always
traverse all query heads per KV head before varying over          4    L ANGUAGE AND F RAMEWORK
mblocks. Empirically, we validate that this LPT order is
highly effective; for example, for BF16 and head dimension        We write F LASH ATTENTION -4 entirely in CuTe-
128 we obtain 4-8% FLOPS gain for MHA and 7-14% for               DSL (Nvidia, 2025), embedded in Python, without any
MQA 8 as measured on an H200 GPU.                                 component in CUDA C++. The CuTe-DSL compiler takes
                                                                  the source code in Python, lowers to PTX, then uses the
LPT for variable sequence length. For varlen, we also             PTX compiler (ptxas) to finally produce the assembly code
have to contend with load imbalance due to variation among        (SASS).
batches. For instance, in decode workloads, different
batches might attend to different amounts of context, and         Full expressivity with clean abstractions. The CuTe-DSL
in mixed or continuous batching, some batches might be            programming model is isomorphic to CUTLASS C++, en-
prefill while others are decode. The list of query and KV         suring that F LASH ATTENTION -4 retains the full expressiv-
sequence lengths per batch is typically stored as attention       ity of low-level GPU programming while benefiting from
metadata on device, and the standard varlen attention kernel      the productivity gains of meta-programming in Python in-
reads these integers at runtime while processing batches in       stead of C++ and fast JIT compilation times. CuTe-DSL
increasing order. However, the given batch order may be           provides direct access to PTX as an escape hatch, allow-
                                                                  ing developers to implement any functionality they need
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

without framework limitations. For example, we leverage          5         E MPIRICAL E VALUATION
custom PTX sequences for operations not yet fully exposed
in CuTe-DSL APIs (though these will be integrated in fu-         We evaluate F LASH ATTENTION -4 efficiency compared to
ture releases), demonstrating that our framework does not        various open-source and closed-source baselines.
constrain developers to a limited subset of GPU capabilities.
                                                                 Benchmarking attention. We measure the runtime of
Fast compilation through JIT. Compile time has been
                                                                 F LASH ATTENTION -4 across different sequence lengths and
a bottleneck in past FlashAttention implementations, due
                                                                 head dimensions, comparing it to standard implementa-
to complex C++ template metaprograms. By embedding
                                                                 tions in PyTorch, F LASH ATTENTION -21 , Triton (which
CuTe-DSL in Python with just-in-time (JIT) compilation,
                                                                 uses B200-specific instructions (Tillet et al., 2019)), Gluon
F LASH ATTENTION -4 achieves faster build times compared
                                                                 (a lower-level GPU programming language with finer con-
to traditional C++ template-based approaches.As shown
                                                                 trol than Triton (Team, 2024)), and cuDNN (a vendor’s
in Table 4, F LASH ATTENTION -4 reduces compile time by
                                                                 library optimized for B200 GPUs). We confirm that
20-30× compared to F LASH ATTENTION -3. This rapid it-
                                                                 F LASH ATTENTION -4 is up to 1.3× faster than cuDNN 9.13
eration cycle significantly improves developer productivity,
                                                                 and up to 2.7× faster than Triton. F LASH ATTENTION -4
enabling faster experimentation and debugging during ker-
                                                                 reaches up to 1613 TFLOPs/s, approximately 71% of the
nel development.
                                                                 theoretical maximum TFLOPs/s on B200 GPUs.

 Method                    Forward pass      Backward pass       Benchmark settings. We measure runtime on a B200
                                                                 GPU for different settings (with/without causal mask, head
 F LASH ATTENTION -3            55s               45s            dimensions 64, 128, and (192, 128)) for BF16 inputs. We
 F LASH ATTENTION -4            2.5s              1.4s           vary the sequence length as 1k, 2k, ..., 32k, and set batch
 Speedup                        22×               32×            size so that the total number of tokens is 32k. We set the hid-
                                                                 den dimension to 2048, and head dimension to be either 64
Table 4: Compile time for a single kernel: FA3 (C++ tem-         or 128 (i.e., 32 heads or 16 heads). For the (192, 128) con-
plates) and FA4 (CuTe-DSL). Typically FA2 and FA3 re-            figuration used in DeepSeek V3 (DeepSeek-AI, 2024), we
quire precompiling hundreds of kernels for different atten-      use 16 heads with 192 query dimensions and 128 key/value
tion variants.                                                   dimensions. To calculate the FLOPs of the forward pass,
                                                                 we use 4 · seqlen2 · head dimension · number of heads. With
                                                                 causal masking, we divide this number by 2 to account for
Flexibility and accessibility. The Python based framework        the fact that approximately only half of the entries are calcu-
has already demonstrated its flexibility in practice: develop-   lated. To get the FLOPs of the backward pass, we multiply
ers have successfully built FlexAttention and block-sparse       the forward pass FLOPs by 2.5 (since there are 2 matmuls
attention variants on top of F LASH ATTENTION -4 without         in the forward pass and 5 matmuls in the backward pass,
modifying the core framework. By lowering the barrier            due to recomputation).
to entry, our approach enables researchers and engineers
with just a few months of GPU programming experience             5.1        Forward pass
to contribute meaningful extensions without requiring deep
expertise in C++ template metaprogramming. This accessi-         We report forward pass results in Figs. 4 and 6, showing that
bility accelerates innovation and allows the attention mech-     F LASH ATTENTION -4 is 1.1-1.3× faster than cuDNN 9.13
anism research community to more rapidly explore new             and 2.1-2.7× faster than Triton. For medium and long se-
algorithmic variants.                                            quences (4k and above), F LASH ATTENTION -4 consistently
                                                                 outperforms all baselines across different head dimensions
Our vision is to provide a comprehensive framework for
                                                                 and causal masking settings. The gains are larger for the
building all kinds of attention variants with best-in-class
                                                                 causal case, which we attribute to the longest-processing-
performance. Rather than implementing each attention vari-
                                                                 time first (LPT) scheduler.
ant from scratch, F LASH ATTENTION -4 factors common
functionality into independent, composable primitives. Op-
                                                                 5.2        Backward pass
erations such as block-sparse patterns, masking strategies,
variable sequence length handling, and work scheduling are       We report backward pass results in Fig. 5.
all exposed as orthogonal primitives that can be freely com-     F LASH ATTENTION -4 achieves consistent speedups
bined. This modular design ensures that optimizations and        across long sequence lengths and causal masking,
new features benefit all attention implementations built on      demonstrating the effectiveness of our 2-CTA backward
the framework while still achieving the highest performance
                                                                       1
by compiling down to efficient GPU kernels.                                F LASH ATTENTION -3 does not run on B200
             F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

                                                                     can be extended to other accelerators as compute continues
                                                                     to outpace non-matmul units.

                                                                     R EFERENCES
                                                                     Bauer, M., Cook, H., and Khailany, B. CudaDMA: Opti-
                                                                       mizing GPU Memory Bandwidth via Warp Specializa-
                                                                       tion. In Proceedings of 2011 International Conference
                                                                       for High Performance Computing, Networking, Storage
                                                                       and Analysis, SC ’11, New York, NY, USA, 2011. Asso-
Figure 6: Forward pass TFLOPS comparison between cuDNN and             ciation for Computing Machinery. ISBN 9781450307710.
FA4 on B200 (FP16/BF16) with head dimension (192, 128) for
causal attention (typically used in DeepSeek V3 architecture). FA4
                                                                       doi: 10.1145/2063384.2063400. URL https://doi.
achieves 1.1-1.3× speedup over cuDNN across sequence lengths.          org/10.1145/2063384.2063400.

                                                                     Brown, T., Mann, B., Ryder, N., Subbiah, M., Kaplan, J. D.,
pass.
                                                                       Dhariwal, P., Neelakantan, A., Shyam, P., Sastry, G.,
We also show the performance of the deterministic backward             Askell, A., et al. Language models are few-shot learners.
pass in Fig. 7. Our careful swizzling and scheduling results           Advances in neural information processing systems, 33:
in a much faster deterministic backward pass, getting up to            1877–1901, 2020.
75% the speed of the nondeterministic backward pass of the
1-CTA backward pass.                                                 Chen, R. J., Chen, C., Li, Y., Chen, T. Y., Trister, A. D., Kr-
                                                                       ishnan, R. G., and Mahmood, F. Scaling vision transform-
                                                                       ers to gigapixel images via hierarchical self-supervised
                                                                       learning. In Proceedings of the IEEE/CVF Conference
                                                                       on Computer Vision and Pattern Recognition, pp. 16144–
                                                                       16155, 2022.

                                                                     Chevillard, S., Joldeş, M., and Lauter, C. Sollya: An en-
                                                                       vironment for the development of numerical codes. In
                                                                       Mathematical Software – ICMS 2010, volume 6327 of
                                                                       Lecture Notes in Computer Science, pp. 28–31. Springer
                                                                       Berlin Heidelberg, 2010.
Figure 7: Ablations for Deterministic Backward pass on B200
(FP16/BF16) with head dimension 128. Causal attention – SPT,         Dao, T. FlashAttention-2: Faster Attention with Better
LPT with reverse mblock order, LPT, and naive with no batch/head
                                                                       Parallelism and Work Partitioning, 2023. URL https:
swizzle.
                                                                      //arxiv.org/abs/2307.08691.

6    D ISCUSSION AND C ONCLUSION                                     Dao, T., Fu, D. Y., Ermon, S., Rudra, A., and Ré, C. FlashAt-
                                                                       tention: Fast and memory-efficient exact attention with
FlashAttention-4 addresses asymmetric hardware scaling,                IO-awareness. In Advances in Neural Information Pro-
where tensor cores are so fast that the dominant bottlenecks           cessing Systems, 2022.
shift to shared-memory traffic and exponential through-
put, motivating algorithmic and kernel co-design to mit-             DeepSeek-AI. Deepseek-v3 technical report. arXiv preprint
igate these limits. We redesign the pipeline around fully              arXiv:2412.19437, 2024.
asynchronous MMA to overlap softmax with larger-tiled
matmuls and introduce software-emulated exponential and              Dosovitskiy, A., Beyer, L., Kolesnikov, A., Weissenborn,
conditional softmax rescaling to reduce non-matmul op-                 D., Zhai, X., Unterthiner, T., Dehghani, M., Minderer, M.,
erations. We leverage tensor memory and 2-CTA MMA                      Heigold, G., Gelly, S., et al. An image is worth 16x16
mode to reduce shared memory traffic. In addition, 2-CTA              words: Transformers for image recognition at scale. In
enables restructuring the global atomic accumulation, halv-           International Conference on Learning Representations,
ing the number of global atomic adds. FlashAttention-4 is              2020.
implemented entirely in CuTe-DSL embedded in Python,
preserving low-level control while achieving 20-30× faster           Graham, R. L. Bounds on multiprocessing timing anomalies.
compile times than C++ template-based kernels. Although                SIAM Journal on Applied Mathematics, 17(2):416–429,
optimized for Blackwell GPUs, some of these algorithms                 1969. doi: 10.1137/0117039.
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

Guo, M., Ainslie, J., Uthus, D., Ontanon, S., Ni, J.,           Roziere, B., Gehring, J., Gloeckle, F., Sootla, S., Gat, I.,
  Sung, Y.-H., and Yang, Y. Longt5: Efficient text-to-            Tan, X. E., Adi, Y., Liu, J., Remez, T., Rapin, J., et al.
  text transformer for long sequences. arXiv preprint             Code llama: Open foundation models for code. arXiv
  arXiv:2112.07916, 2021.                                         preprint arXiv:2308.12950, 2023.

Ho, J., Salimans, T., Gritsenko, A., Chan, W., Norouzi, M.,     Shah, J., Bikshandi, G., Zhang, Y., Thakkar, V., Ramani, P.,
  and Fleet, D. J. Video diffusion models. Advances in            and Dao, T. Flashattention-3: Fast and accurate attention
 Neural Information Processing Systems, 35:8633–8646,             with asynchrony and low-precision, 2024. URL https:
  2022.                                                           //arxiv.org/abs/2407.08608.

Lin, J., Luo, M., Zeng, J., Zhai, J., and Hu, J. Sageatten-     Shaham, U., Segal, E., Ivgi, M., Efrat, A., Yoran, O., Haviv,
  tion2: Efficient attention with thorough outlier smooth-        A., Gupta, A., Xiong, W., Geva, M., Berant, J., et al.
  ing and per-thread int4 quantization. arXiv preprint            Scrolls: Standardized comparison over long language
  arXiv:2411.10958, 2024a.                                        sequences. arXiv preprint arXiv:2201.03533, 2022.

                                                                Team, T. Gluon: A lower-level gpu programming lan-
Lin, J., Zhang, J., Zheng, H., Zeng, J., Zhai, J., and Hu, J.
                                                                  guage. https://github.com/triton-lang/
  Sageattention: Accurate 8-bit attention for plug-and-play
                                                                  triton/blob/main/python/tutorials/
  inference acceleration. arXiv preprint arXiv:2410.02367,
                                                                  gluon/01-intro.py, 2024.
  2024b.
                                                                Tillet, P., Kung, H.-T., and Cox, D. Triton: an intermediate
Lin, J., Luo, M., Zhang, J., Xu, Z., Zheng, H., Zeng, J.,          language and compiler for tiled neural network computa-
  Zhai, J., and Hu, J. Sageattention3: Fast attention on           tions. In Proceedings of the 3rd ACM SIGPLAN Interna-
  blackwell gpus via fp4 quantization. arXiv preprint              tional Workshop on Machine Learning and Programming
  arXiv:2505.11594, 2025.                                         Languages, pp. 10–19, 2019.
Luo, W., Fan, R., Li, Z., Du, D., Liu, H., Wang, Q., and        Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones,
  Chu, X. Dissecting the nvidia hopper architecture through       L., Gomez, A. N., Kaiser, Ł., and Polosukhin, I. At-
  microbenchmarking and multiple level analysis. arXiv            tention is all you need. Advances in neural information
  preprint arXiv:2501.12084, 2025.                                processing systems, 30, 2017.

Muller, J.-M., Brunie, N., de Dinechin, F., Jeannerod, C.-
 P., Joldes, M., Lefèvre, V., Melquiond, G., Revol, N.,
 and Torres, S. Handbook of Floating-Point Arithmetic.
 Birkhäuser Boston, 2nd edition, 2018. ISBN 978-3-319-
 76525-9.

NVIDIA. Nvidia H100 tensor core GPU architecture, 2022.

NVIDIA. CUDA Programming Guide Version 12.4,
 2024a. URL https://docs.nvidia.com/cuda/
 cuda-c-programming-guide/index.html.

NVIDIA.         Nvidia   Blackwell architec-
 ture technical brief, 2024b.      https:
 //nvdam.widen.net/s/xqt56dflgh/
 nvidia-blackwell-architecture-technical-brief.

NVIDIA.    cudnn release notes.  https:
 //docs.nvidia.com/deeplearning/cudnn/
 backend/latest/release-notes.html,
 2025.

Nvidia. Nvidia cutlass documentation, 2025.            URL
  https://docs.nvidia.com/cutlass/
  media/docs/pythonDSL/cute_dsl_
  general/dsl_introduction.html.
               F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling

A     A DDITIONAL D ETAILS ON                                      seqlen    batch     Fwd time (ms)   Throughput (TFLOPS)
      E XPERIMENTS AND B ENCHMARKING                                2048         16            0.266                    1033.3
                                                                    4096          8            0.436                    1260.6
A.1    System and libraries                                         8192          4            0.785                    1401.0
                                                                   16384          2            1.470                    1496.4
                                                                   32768          1            2.847                    1544.6
We benchmark the speed on a B200 180GB SXM6 (1000W).
We warmup with 5 runs, then repeat the benchmarks 10
                                                                            Table 6: Default kernel, causal = True.
times, and take the average timing.
We generally used the latest versions of the libraries at the    q-stage 1 ablation.
time of writing (March 2025). Specifically, we use:
                                                                   seqlen    batch     Fwd time (ms)   Throughput (TFLOPS)

    • CUDA 13.1                                                     2048         16            0.553                     993.5
                                                                    4096          8            1.078                    1019.8
                                                                    8192          4            2.125                    1035.1
    • F LASH ATTENTION 2.8.3                                       16384          2            4.234                    1038.8
                                                                   32768          1            8.681                    1013.2
    • Triton 3.6
                                                                                Table 7: q-stage 1, causal = False.
    • PyTorch 2.10.0
                                                                   seqlen    batch     Fwd time (ms)   Throughput (TFLOPS)
    • CuTe-DSL 4.4.1                                                2048         16            0.406                     677.1
                                                                    4096          8            0.676                     812.8
                                                                    8192          4            1.211                     908.2
For cuDNN, in the main paper, we compare to cuDNN                  16384          2            2.288                     960.9
9.13 and the latest version cuDNN 9.19.1.2. Starting from          32768          1            4.446                     989.3
version 9.13 and 9.14 (NVIDIA, 2025), we have worked
with the cuDNN team to incorporate some techniques from                         Table 8: q-stage 1, causal = True.
F LASH ATTENTION -4 into cuDNN so that our work can
benefit as many practitioners as possible.                       e2e ablation.

A.2    Backward Deterministic non-causal                               seqlen     Fwd time (ms)    Throughput (TFLOPS)
                                                                        2048               0.412                     1333.8
For completeness, we also include performance numbers for               4096               0.785                     1400.2
the deterministic backward kernel without causal masking                8192               1.539                     1428.9
in Fig. 8, side by side that with causal masking.                      16384               3.039                     1447.4
                                                                       32768               6.465                     1360.7

B     A BLATIONS                                                     Table 9: No e2e (use MUFU.EX2), causal = False.
We report ablations that isolate the speedup contribution
                                                                       seqlen     Fwd time (ms)    Throughput (TFLOPS)
of each major technique and design choice. Unless oth-
erwise noted, headdim = 128, nheads = 16, and                           2048               0.269                     1022.6
nheads kv = 16. The results are presented at fixed                      4096               0.443                     1240.7
                                                                        8192               0.796                     1380.9
seqlen × batch = 2048 × 16, with seqlen ranging                        16384               1.504                     1461.7
from 2048 to 32768. For the tables, ”No e2e” refers to                 32768               2.929                     1501.4
disabling software emulation of the exponential.
Default kernel.                                                                 Table 10: No e2e, causal = True.

    seqlen    batch   Fwd time (ms)    Throughput (TFLOPS)       Correction rescale ablation.
     2048        16           0.399                     1379.2
     4096         8           0.750                     1466.0         seqlen     Fwd time (ms)    Throughput (TFLOPS)
     8192         4           1.459                     1507.5          2048               0.426                     1290.0
    16384         2           2.817                     1561.0          4096               0.822                     1336.9
    32768         1           5.625                     1563.7          8192               1.627                     1351.6
                                                                       16384               3.219                     1366.2
             Table 5: Default kernel, causal = False.                  32768               6.829                     1288.0
            F LASH ATTENTION -4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling




Figure 8: Ablations for Deterministic Backward pass on B200 with head dimension 128. Non-causal attention with batch/head swizzle
versus naive. Right: causal attention – SPT, LPT with reverse mblock order, LPT, and naive with no batch/head swizzle.


   Table 11: Always correction rescale, causal = False.

      seqlen    Fwd time (ms)    Throughput (TFLOPS)
        2048            0.285                    964.4
        4096            0.476                   1154.4
        8192            0.864                   1272.7
       16384            1.674                   1313.3
       32768            3.205                   1372.0

   Table 12: Always correction rescale, causal = True.
