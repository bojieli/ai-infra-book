                    M ASSIVE -S CALE O UT-O F -C ORE UMAP ON THE GPU


                     Jinsol Park * 1 Corey J. Nolet * 1 2 Akira Naruse 1 Edward Raff 3 2 Tim Oates 2


                                                           A BSTRACT
        The Uniform Manifold Approximation and Projection (UMAP) algorithm has become a widely popular technique
        to reduce the dimensionality of a set of vectors, both for visualization and as a pre-processing step for follow-on
        machine learning tasks. UMAP is often an integral part of iterative and exploratory workflows, but the heavy
        amount of compute and memory required makes scaling to tens or even hundreds of gigabytes of vectors intractable
        on the CPU, often taking several hours to days to complete. In this paper, we show how we improved UMAP while
        unlocking performance that permits interactive analysis, even at massive-scale, by introducing an out-of-core
        strategy with optional multi-GPU support. We observe 22.7x speedup using a single GPU on smaller data scales
        where CPU baseline runs to completion, and project up to 74x speedup using multiple GPUs on a single node at
        larger scales where CPU was not able to complete by extrapolating measured scaling behavior.

1       I NTRODUCTION                                              tationally prohibitive at scale. Unlike online vector search,
                                                                   where a small query set is compared against a larger train-
Manifold learning is an important technique for visualizing        ing dataset, an all-neighbors kNN uses each data point for
and analyzing high-dimensional data. Unlike traditional            both train and search. As a result, it has become common
linear methods like Principal Component Analysis (PCA)             practice to pre-compute it, reusing it for subsequent UMAP
(Hotelling, 1933), which only preserve global structure, the       runs. While this alleviates the computational burden, the
Uniform Manifold Approximation and Projection algorithm            number of nearest neighbors k decides the balance between
(UMAP) (McInnes et al., 2018a) provides parameters to              local and global structure that UMAP captures, and fixing it
trade-off the preservation of global versus local structure.       limits the method’s usefulness.
UMAP’s capability to preserve both local and global neigh-         Prior work (Nolet et al., 2021) has introduced a fully GPU-
borhoods while scaling efficiently has led to its wide use         accelerated implementation of UMAP, reducing runtime
across various domains such as genomics (Hao et al., 2021;         compared to the widely used CPU implementation (McInnes
Korsunsky et al., 2019; Becht et al., 2019), cancer research       et al., 2018b). However, two factors still contribute to the
(Martı́nez-Ruiz et al., 2023; De Zuani et al., 2024; Friebel       all-neighbors graph construction being the bottleneck in
et al., 2020), natural language processing and topic model-        UMAP on GPU. The first is compute, because building the
ing (Costa-Jussà et al., 2022; Perez et al., 2023; Grooten-       all-neighbors graph with naive methods, such as exhaustive
dorst, 2022), and time-series data analysis (Altin & Cakir,        brute-force, scales quadratically with the number of vectors
2024). Across these diverse applications, UMAP functions           in the dataset. Figure 1 shows that this graph construction
as an important tool for compressing high-dimensional data         dominates the total runtime, taking up to 99% of the overall
to low-dimensional embeddings for visualization as well as         execution time on the GPU for larger datasets, and over
a pre-processing step for follow-on machine learning tasks.        75% of the time on CPU. Our evaluation shows that this
Many workflows invoking UMAP are iterative and ex-                 proportion increases further as the dataset size continues
ploratory, often requiring the algorithm to run multiple times     to grow, since the quadratic time complexity of brute force
on the same dataset for analysis or tuning of parameters.          distance computation causes it to increasingly dominate
However, the first and most crucial step in the algorithm,         overall runtime. The second challenge is memory usage.
the construction of the all-neighbors kNN graph, is compu-         Since constructing the all-neighbors graph is the only step
    *
                                                                   that requires the full dataset to reside in GPU memory, this
   Equal contribution 1 NVIDIA 2 Department of Engineer-           stage often becomes a hard limitation that prevents running
ing and Computer Science, University of Maryland, Baltimore
County 3 CrowdStrike. Correspondence to: Corey J. Nolet <cno-
                                                                   UMAP end-to-end on large datasets.
let@nvidia.com>.                                                   In this paper, we introduce a scalable GPU-accelerated im-
Proceedings of the 9 th MLSys Conference, Bellevue, WA, USA,
                                                                   plementation of UMAP that builds upon the work of (No-
2026. Copyright 2026 by the author(s).                             let et al., 2021) to address both the compute and memory
                                            Massive-Scale Out-Of-Core UMAP on the GPU

                                                                       2     R ELATED W ORK
                                                                       UMAP’s increasing use for large-scale work across many
                                                                       domains motivates us to improve its computational effi-
                                                                       ciency and ease of use with datasets that exceed GPU mem-
                                                                       ory. One common use case is visualization. Some no-
                                                                       table examples include interpreting semantic differences in
                                                                       LLM prompts (Shen et al., 2024) and text corpus evalua-
                                                                       tions (Costa-Jussà et al., 2022). In genomics, UMAP is used
                                                                       to visualize and characterize cell populations (Hao et al.,
                                                                       2021; Korsunsky et al., 2019; Becht et al., 2019). Visual-
                                                                       izations can also be exploited for general exploratory data
                                                                       analysis (Ali et al., 2023) or even in reinforcement learn-
                                                                       ing (Haarnoja et al., 2024). Beyond visualization, UMAP
                                                                       is often used as a feature-extraction or embedding step in
                                                                       larger machine learning pipelines. UMAP has been inte-
                                                                       grated into topic modeling frameworks for dimensional-
                                                                       ity reduction (Grootendorst, 2022; Angelov, 2020). Sarthi
                                                                       et al. (2024) enhanced retrieval-augmented generation using
                                                                       UMAP embeddings. In time series analysis, UMAP is used
Figure 1. Proportion of time spent on building all-neighbors graph     for anomaly detection (Altin & Cakir, 2024).
within end-to-end UMAP on the GPU (Nolet et al., 2021) and
CPU (McInnes et al., 2018b). This proportion increases with the
                                                                       2.1   Manifold learning
dataset size ((rows, dims) in figure). Detailed dataset descriptions
are provided in Section 4.                                             Manifold learning, as employed by methods such as UMAP,
                                                                       is a class of nonlinear dimensionality reduction techniques
                                                                       based on the manifold hypothesis (Cayton et al., 2005). This
                                                                       is the assumption that high-dimensional data are only artifi-
                                                                       cially high-dimensional and lie on a low-dimensional mani-
challenges of end-to-end UMAP on large datasets. We                    fold. The ultimate goal of manifold learning is to uncover
contribute three components addressing the growing de-                 the low-dimensional structure while preserving meaningful
mand for scalable UMAP. First, we enable and evaluate                  relationships between data points. Manifold learning algo-
GPU-accelerated UMAP at previously unreachable scales                  rithms tend to consist of three general steps; construction of
for datasets that exceed a single GPU’s memory, establishing           a neighborhood graph, weighting the graph based on neigh-
a practical baseline for future research. Second, we intro-            borhood affinities, and finally learning a low-dimensional
duce a scalable out-of-core, multi-GPU all-neighbors graph             representation that models neighborhoods from the graph.
construction algorithm that can be applied to any down-                The first step is often the most computationally intensive, as
stream machine learning pipeline that relies on large-scale            it requires computing nearest neighbors for every vector in
all-neighbors graphs. Moreover, we contribute everything as            a training dataset.
a fully open-source implementation, making our method ac-
cessible to the community and impactful by enabling adop-              In addition to UMAP, several other manifold learning algo-
tion in real-world large-scale ML workflows. Our scalable              rithms can benefit from our scalable all-neighbors construc-
all-neighbors graph construction implementation is avail-              tion algorithm because they share this computation as their
able in the NVIDIA cuVS library. Our end-to-end scalable               initial step. These algorithms include Isomap (Tenenbaum
UMAP algorithm is available in the NVIDIA cuML library.                et al., 2000), Locally Linear Embeddings (LLE) (Roweis
                                                                       & Saul, 2000), Laplacian Eigenmaps (aka spectral embed-
                                                                       ding) (Belkin & Niyogi, 2003), and t-Distributed Stochastic
The remainder of this paper is organized as follows. Sec-              Neighbor Embeddings (T-SNE) (Maaten & Hinton, 2008).
tion 2 describes related works, discussing how our algorithm
draws upon popular techniques for accelerated vector search
                                                                       2.2   Vector Search
and ultimately benefits many other important data mining
algorithms. Section 3 outlines our approach for scaling                To construct the neighborhood graph for manifold learning
UMAP to handle massive datasets on the GPU. We share                   algorithms, top-k nearest neighbors are computed for the
our experiment setup and results in Section 4 and Section 5,           entire training dataset, resulting in an all-neighbors kNN
respectively. In Section 6 we summarize our contributions              graph. In contrast to latency sensitive workloads like online
and discuss opportunities for future optimizations.
                                        Massive-Scale Out-Of-Core UMAP on the GPU

search, where a kNN graph is constructed for a small number      all-to-all communications in the broadcasting of each vec-
of vectors at a time, the all-neighbors kNN graph is an          tor. Many vector databases have implemented this method
offline throughput bound task, requiring substantially more      (Wang et al., 2021b; Xian et al., 2024), since it is trivial to
compute and memory.                                              develop and fits well within existing log-structured merge
                                                                 tree architectures (O’Neil et al., 1996). This method allows
The exhaustive nature of brute-force makes it unsuitable
                                                                 for more chunks and greater parallelism through local re-
for computing the all-neighbors graph at scale, so many
                                                                 ductions within each chunk. However, architectures based
algorithms rely on approximate nearest-neighbor methods
                                                                 on log-structured merge trees typically exploit the ordering
(ANNs). Many ANN algorithms are machine learning mod-
                                                                 of structured data in order to scale efficiently, but sorting
els which build an index that helps to reduce the number
                                                                 vectors does not improve ANN search because vector simi-
of distance computations during search. On the CPU, these
                                                                 larity is geometric rather than order-based. Thus, the need
algorithms are often optimized for low latency online search,
                                                                 to have every chunk involved in the computation for every
with some modern and widely-used methods including hi-
                                                                 vector makes it challenging to scale in a performant manner.
erarchical navigable small-world graphs (HNSW) (Malkov
& Yashunin, 2018; Munyampirwa et al., 2024), DiskANN             Ideally, a scalable and performant solution would allow
(Jayaram Subramanya et al., 2019), Faiss’ FastScan (Douze        for more independent computation. An additional method,
et al., 2025), and SCaNN (Guo et al., 2020). Neighborhood        called global indexing, enables this by establishing spatial
graph construction in our work needs to be high throughput,      locality up front (Guo et al., 2020; Johnson et al., 2019a),
and GPU-accelerated variants of algorithms like IVF-PQ           using a clustering algorithm to partition the points into c
(Johnson et al., 2019b; Simhadri et al., 2022) and graph-        clusters, rather than distributing the points uniformly. This
based CAGRA (Ootomo et al., 2024) can significantly im-          can be viewed as a form of ordering the vectors to provide
prove query throughput for large batches of queries, while       better quality and scale. With this method, search points can
the nearest neighbors descent (nn-descent) algorithm (Dong       be dispatched to their c closest clusters, thus enabling only
et al., 2011) improves upon the performance of the afore-        a subset of clusters to be used for search. K-means is often
mentioned ANN methods by directly approximating the all-         used for clustering, and a hierarchical variant of k-means
neighbors graph. The subsequent GPU port of nn-descent           can provide better load balancing of points across clusters.
(Wang et al., 2021a) further improves this performance.          The resulting clusters also do not need to be disjoint, as
                                                                 points can be spilled (Liu et al., 2004; Sun et al., 2023;
Our approach is inspired by the clustered graph-build ap-
                                                                 Jayaram Subramanya et al., 2019) into multiple clusters
proach used by DiskANN (Jayaram Subramanya et al.,
                                                                 and de-duplicated at search time to improve search quality.
2019). This approach uses a technique common to vector-
                                                                 Global indexing is also referred to as an IVF. While it can
based inverted file index (IVF) algorithms called “spilling”
                                                                 often improve scale for online search, it still requires a
(Liu et al., 2004; Sun et al., 2023; Jayaram Subramanya
                                                                 significant amount of communication to multicast every
et al., 2019; Bergum, 2022), which partitions vectors into
                                                                 vector to its closest clusters and gather the resulting nearest
k-means clusters and assigns each vector to multiple parti-
                                                                 neighbor graphs into a single global all-neighbors graph.
tions, ensuring that true neighbors near partition boundaries
are not missed. We adopt this partitioned approach with          Therefore, from an algorithmic perspective, constructing a
spilling, but rather than separating index construction and      full all-neighbors graph is challenging. Even with multi-
query routing as in standard IVF search, we construct an         ple GPUs, such as in Faiss (Douze et al., 2025), efficiently
all-neighbors graph fully independently in each partition,       distributing the work across GPUs or partitions remains
and stitch the results together into a single global graph.      difficult (appendix A). Each vector still requires commu-
                                                                 nication or replication across partitions, and the standard
2.3   All-neighbors Graph Construction                           search-focused optimizations do not translate directly to the
                                                                 all-neighbors graph workload. Prior work such as LargeVis
Several approaches exist for finding nearest neighbors at        (Tang et al., 2016) has also constructed an approximate kNN
scale. One of the most naive approaches, called uniform          graph as part of a downstream machine learning task, but
sharding, breaks the training dataset into some number of        as a CPU-based method this does not scale to the dataset
chunks uniformly at random, and trains an ANN index on           sizes we target (appendix Section B). This motivates the
each chunk. At search time, every vector in the dataset is       need for a specialized algorithm that is explicitly optimized
broadcast to every chunk, where each chunk computes its          for large-scale all-neighbors graph construction.
local k-nearest neighbors. The nearest neighbors from each
chunk are then reduced down to a final set of k-nearest neigh-
                                                                 2.4   Alternate UMAP Implementations
bors by taking the smallest distances from all chunks. While
this method is simple to implement and can be executed           The widely used reference implementation of UMAP on the
across multiple processing units like GPUs, it requires costly   CPU (McInnes et al., 2018b) partially mitigates the compute
                                        Massive-Scale Out-Of-Core UMAP on the GPU

and memory challenges by using pynndescent (McInnes,             ing intermediate local all-neighbors graphs to be merged
2021), a Python version of the nn-descent algorithm. How-        into a single high-quality global all-neighbors graph while
ever, building the all-neighbors graph still dominates the       preserving as much of the actual neighborhood structure
end-to-end runtime, and fails to run on large datasets due to    as possible. As we show in Section 4, this graph allows
excessive memory consumption during this step.                   our GPU-accelerated UMAP implementation to faithfully
                                                                 reproduce the resulting visualization from the reference im-
A few GPU-accelerated implementations of UMAP exist
                                                                 plementation on the CPU.
in an attempt to exploit GPU parallelism. However, many
methods (Van Assel et al.; Eisenmann) operate as “ports” of      Our method consists of four main steps. Each step is illus-
the original CPU implementation (McInnes et al., 2018b),         trated in detail in Figure 2.
relying on general-purpose frameworks such as PyTorch
(Imambi et al., 2021) or Numba (Lam et al., 2015) to dis-          1. Compute c cluster centers to approximately evenly
patch GPU work, without considering GPU characteristics               partition the space using a load-balanced variant of
such as memory capacity and access patterns. Nolet et al.             k-means
(2021) fully accelerated UMAP on GPU showing improved              2. Assign each data vector to its nearest s clusters, where
performance. However, all approaches suffer from requiring            s is a spilling factor
the entire dataset to fit in GPU memory, which we resolve
                                                                   3. For each cluster in c, compute a local all-neighbors
in this work.
                                                                      graph

3     S CALING UMAP WITH GPU S                                     4. Merge local all-neighbors graphs for each c into a sin-
                                                                      gle global all-neighbors graph
As mentioned in Section 2.1, the UMAP algorithm is a
manifold learning algorithm that consists of three main steps,   The first two steps preprocess the dataset to preserve spatial
but the first step dominates the end-to-end runtime, and is      locality. Steps three and four are performed on each cluster
thus the focus of our scaling and acceleration efforts in this   c, and yield the final output.
paper. In this section, we propose a scalable and highly
parallel algorithm that is accelerated on the GPU end-to-        Compute cluster centers using hierarchical balanced k-
end, and efficiently computes the all-neighbors graph on         means To determine spatially meaningful partitions, we
datasets that far surpass the memory of the GPU.                 first run a clustering algorithm to compute cluster centroids.
                                                                 Since our approach targets datasets that exceed GPU mem-
The fixed degree k in our all-neighbors graph construc-          ory, we randomly sample Nc vectors from the dataset to
tion directly corresponds to the n neighbors parame-             run the clustering algorithm on the GPU, where N is the
ter in UMAP. This parameter controls the number of near-         number of vectors in the dataset. This subsample is assumed
est neighbors considered for each point when building the        to be representative of the overall data distribution while
neighborhood graph, thereby influencing the trade-off be-        remaining small enough to fit in GPU memory, allowing
tween preserving local structure versus introducing more         cluster centroids to be efficiently extracted on GPU without
global structure into the embedded space. Smaller values         processing the full dataset.
focus more on fine-grained local relationships, while larger
values capture broader patterns.                                 We employ a two-level hierarchical balanced k-means
                                                                 scheme to capture both global and local structure while
3.1   Scalable all-neighbors Graph Construction                  reducing overall computational cost. At the meso  √ level,
                                                                 balanced k-means divides the subsample into c coarse
Following from the global indexing solution outlined in          clusters, capturing the global spatial layout of the dataset.
Section 2.3, our approach establishes spatial locality and       Each meso cluster is then refined independently by another
uses an IVF, spilling points across clusters to induce over-                                                               √
                                                                 round of balanced k-means, producing approximately c
lap. However, rather than performing online search against       fine clusters within each region. The total
a subset of closest clusters, we gather only the vectors as-                                               √number
                                                                                                                √     of clus-
                                                                 ters thus becomes c, yielding a balanced c– c hierarchy
signed to each cluster at a time onto the GPU and build          that provides uniform spatial resolution and naturally limits
a local all-neighbors graph on each cluster independently.       the per-cluster workload. Because each fine-level clustering
The reason for this is two-fold– first, it avoids the commu-     operates on a much smaller subset of data, this hierarchi-
nication overhead of multi-casting vectors to their nearest      cal decomposition significantly reduces the overall runtime
clusters– second, it allows for larger amounts of spilling       compared to performing a single flat clustering over all c
without having to actually duplicate vectors on any GPU at       clusters.
any point in time.
                                                                 Our balanced k-means is designed to achieve approximate,
Spilling trades off quality for additional computation, allow-   rather than exact, uniformity of cluster sizes. During the
                                         Massive-Scale Out-Of-Core UMAP on the GPU




Figure 2. Four steps of our out-of-core all-neighbors construction. Green text indicates user-configurable parameters that affect GPU
memory usage or all-neighbors graph accuracy. Red boxes highlight steps that can be parallelized across multiple GPUs.


iterative process, after each assignment step, we monitor the        Compute local all-neighbors for each cluster Since we
size of all clusters. If a cluster falls below a predefined min-     expect the points to be approximately balanced across clus-
imum size threshold, it is removed, and the largest cluster          ters, we can use c and s to control the relative number of
is split into two new clusters to maintain the total cluster         points in each cluster, which ultimately determine how much
count. This heuristic adjustment prevents the emergence of           GPU memory is required. The vectors assigned to each clus-
degenerate tiny clusters while redistributing data points from       ter, in addition to the corresponding all-neighbors graph,
overpopulated regions. Unlike strict capacity-constrained            need to fit in a single GPU at a given time. Equation (1)
formulations, this approach provides a lightweight and GPU-          shows the approximate memory components used on the
friendly mechanism to maintain roughly balanced partitions           GPU for a local all-neighbors graph construction.
without sacrificing local spatial structure.
                                                                                  s
                                                                       Mdata = N ·  · D · sizeof (Tdata )
                                                                                  c
Assign each data vector to nearest s clusters Instead                             s                                        
                                                                      Mgraph = N · · k · sizeof (Tinds ) + sizeof (Tdists )
of hard assigning each vector to a single cluster to produce                      c
disjoint clusters, our approach spills vectors across clus-           Mtotal = Mdata + Mgraph
ters by assigning each vector in the training dataset to its                                                               (1)
s closest clusters, where s is called a spill factor. Spilling
increases the likelihood that we can find the true neighbor-        Equation 1. Memory components used on the GPU for a local all-
hoods, even when faced with points very close to the cluster        neighbors graph construction, D is the dimension of the dataset,
                                                                    N is the number of vectors in the training dataset, T is the data
boundaries. Spilling provides connections across clusters,
                                                                    type of the corresponding data arrays.
so they can ultimately be stitched into a single global coher-
ent all-neighbors graph.
                                                                     For each cluster, we use the inverted index constructed in the
Using the cluster assignment information, we construct a             previous step to obtain the data indices assigned to that clus-
compressed-sparse row (CSR) matrix to represent an in-               ter. The corresponding vectors are gathered as a contiguous
verted index to map each vector in the training dataset to           chunk in system memory and transferred to the GPU, where
its assigned clusters. Each row corresponds to a cluster id,         the local all-neighbors graph is computed. This method is
and the nonzero entries indicate data indices that belong to         agnostic to the nearest neighbors algorithm used to build
that cluster. This structure allows for efficient data gathering     the all-neighbors graph, allowing for any ANN algorithm
during the subsequent all-neighbors graph construction step.         to be used. Our implementation supports GPU-accelerated
                                        Massive-Scale Out-Of-Core UMAP on the GPU

versions of brute-force and nn-descent algorithms, but it can    denotes the number of vectors in the current cluster for a
easily be extended to use others.                                local graph, or in the full dataset for the global graph. We
                                                                 leverage the inverted index constructed in step 2 to map lo-
Merge local all-neighbors graphs into global all-                cal row indices to their corresponding indices in the original
neighbors graph A single global all-neighbors graph is           dataset. The CUDA kernel for this merge operation uses a
maintained in GPU memory throughout the computations             separate thread block to process each row in the CSR.
of the local all-neighbors graphs. After each local graph is     To minimize additional GPU memory allocation, and to im-
computed, it is merged into the global graph.                    prove performance, distances and indices from the same row
Two or more all-neighbors graphs are merged by performing        in A and B graphs are concatenated in CUDA shared mem-
a column-wise concatenation followed by a row-level k-           ory. The distances are treated as keys and the indices as val-
selection, effectively reducing the degree of the final graph    ues in a block-wise collaborative sort using NVIDIA CUB’s
back down to k. The k-selection requires a row-wise sort         (CCCL Development Team, 2023) BlockMergeSort to
by distance so that the neighborhoods are ordered by their       exploit all threads in each block to sort the concatenated
proximity from the target vector. Duplicates introduced by       result by distances.
the spill factor s are then removed and only the top-k entries   Following the sort, a de-duplication step is performed on
retained from the final list of neighbors in each row.           each neighbor list to account for duplicates introduced by
                                                                 the overlap of the spilling parameter s. Assuming duplicated
Algorithm 1 CUDA kernel to merge all-neighbors sub-              indices occupy adjacent spaces in the sorted neighborhood
graphs                                                           list, CUDA threads collaboratively compute a bit-level mask
input k, distsa , indsa , distsb , indsb , map                   within each thread block, marking the first occurrence of
output Updated distsa , indsa                                    each id in the concatenated 2k-length list with 1, and subse-
  keys[2k] {shared memory for distances}                         quent duplicates with 0. A prefix-sum is computed on the
  values[2k] {shared memory for indices}                         unique mask to determine the positions of unique items in
  Brow ← blockIdx.x                                              the final graph and the de-duplicated results are written back
  Arow ← map[Brow ]                                              to the merged graph.
  keys[: k] ← distsa [Arow ]
                                                                 3.2   Using multiple GPUs
  values[: k] ← indsa [Arow ]
  keys[k : 2k] ← distsb [Brow ]                                  We further improve the scale and performance of our algo-
  values[k : 2k] ← indsb [Brow ]                                 rithm by distributing computation across multiple GPUs.
  sorted keys, sorted values ←                                   Since constructing the all-neighbors graph for each cluster
                                                                 can be done independently, our algorithm can parallelize
              cub::BlockMergeSort(keys, values)
                                                                 well across multiple GPUs, removing the need for costly
  unique keys, unique values ←
                                                                 inter-GPU communication of data vectors. For parallelizing
                 GetUnique(sorted keys, sorted values)           across ngpu number of GPUs, we launch ngpu threads, each
  distsa [Arow ] ← unique keys[: k]                              responsible for submitting CUDA kernels on a single GPU.
  indsa [Arow ] ← unique values[: k]                             Figure 2 notes the stages that can be parallelized across
                                                                 multiple GPUs.
  function GetUnique (sorted keys, sorted values)
    mask[2k] {shared memory for mask}                            Assign each data vector to nearest s clusters The matrix
    M [i] ← sorted keys[i] ̸= sorted keys[i − 1]                 of cluster centroids computed in step 1 tends to be fairly
                             Pi
    Perform prefix sum Pi = j=0 Mj                               small in footprint, so it is broadcasted to all GPUs. During
    unique keys[Pi ] = sorted keys[i] where Mi = 1               the cluster assignment stage, each GPU is responsible for
                                                                                                                     N
    unique values[Pi ] = sorted values[i] where Mi = 1           processing approximately n vecs per gpu = ngpu          data
  end function                                                   vectors, where N is the number of vectors in the training
                                                                 dataset. n data per gpu vectors are further divided into
Algorithm 1 contains the pseudo code for merging two             smaller batches if necessary to fit within the GPU’s memory
all-neighbors graphs together. Each all-neighbors graph          capacity. The closest s k-means centroids are assigned to
is stored as a CSR matrix. Since the graph has a fixed de-       each vector in the batch and each thread writes the results
gree of k, the array mapping each row to its column offsets      from its batch to a global N × s matrix.
is omitted. The remaining two arrays we refer to as inds
and dists store the column indices and distances, respec-        Compute local all-neighbors for each cluster In the all-
tively. These arrays both have a size of N × k, where N          neighbors graph computation step, each GPU is responsible
                                        Massive-Scale Out-Of-Core UMAP on the GPU

for processing a similar number of clusters out of c. Iterating   4.1       Datasets
through its assigned range of clusters, each GPU indepen-
                                                                  Table 1 summarizes the datasets used for our experi-
dently uses the inverted index to efficiently gather vectors
                                                                  ments. The Amazon reviews text datasets (Ni et al.,
that belong to each of its clusters from system memory, and
                                                                  2019) consist of user reviews collected from Amazon,
builds a local all-neighbors graph on the GPU. Again, the
                                                                  organized into multiple categories. We selected four
use of balanced k-means clustering produces clusters of
                                                                  categories from this dataset (beauty, appliances, food, and
roughly uniform size, ensuring that each GPU receives a
                                                                  electronics reviews) to evaluate scalability and perfor-
comparable amount of work without significant stragglers
                                                                  mance across various shapes and scales. Some datasets
or additional scheduling overhead.
                                                                  required embedding raw text data, for which we used the
                                                                  sentence-transformers Python library (Reimers & Gurevych,
Merge local all-neighbors graphs into global all-                 2019). For the 384 dimensional embeddings, we used the
neighbors graph As each GPU finishes computing their              all-MiniLM-L6-v2 model and for the 768 dimensional
local all-neighbors graph for each partition, the result is       embeddings we used the all-mpnet-base-v2 model.
merged into the global all-neighbors graph. As an optional        The dataset we refer to as Wiki-all contains multilingual
intermediate step to increase parallelism and decrease the        text chunks from Kaggle 1 and Cohere 2 , embedded with the
need for costly synchronization, multiple intermediate all-       paraphrase-multilingual-mpnet-base-v2
neighbors graphs can be maintained so that several local          model. The MIRACL dataset (Zhang et al., 2023) is
graphs can be merged together independently of all others         composed of wikipedia passages across multiple languages
prior to merging into the final global all-neighbors graph.       embedded with the llama-3.2-nvembed model. The
This hierarchical merging pattern can also be done as a           larger datasets (Wiki and MIRACL) exceed the memory
tree reduction to reduce synchronization points further and       capacity of modern GPUs to help evaluate the scalability of
enable efficient scaling to larger numbers of GPUs, even          our out-of-core approach.
across physical node boundaries.

                                                                             Table 1. Summary of datasets used for evaluation.
4    E XPERIMENTS
                                                                   Dataset                                Rows        Dimensions
We compare the execution time and correctness of our imple-        Beauty Amazon Reviews                  639,806          384
mentation against the reference UMAP implementation on             Appliances Amazon Reviews             1,824,589         384
the CPU (McInnes et al., 2018b), which uses Numba (Lam             Food Amazon Reviews                   5,081,277         384
et al., 2015) just-in-time (JIT) compilation to parallelize        Electronics Amazon Reviews           20,984,669         768
                                                                   Wiki                                 87,555,327         768
Python loops across CPU threads to improve throughput on           MIRACL                              106,332,152        2048
multi-core systems. The reference implementation was con-
figured to take advantage of all the available cores on the ma-
chine and defaults to using ANN (pynndescent) (McInnes,
                                                                  4.2       End-to-End Comparison
2021) for efficient all-neighbors graph construction.
                                                                  We compare end-to-end performance and correctness of
Our work focuses on scalability with efficient memory man-
                                                                  our UMAP implementation on GPU against the reference
agement and end-to-end performance. The existing GPU
                                                                  UMAP implementation on CPU. Memory bottlenecks in the
alternatives to our approach (Eisenmann; Van Assel et al.)
                                                                  all-neighbors graph production prevented UMAP on CPU
fall short in both of the categories above, and therefore we
                                                                  from successfully running on on Wiki-all and MIRACL,
do not consider them comparable baselines in our large-
                                                                  even when 2TiB of system memory was available. For these
scale out-of-core experiments. We share experiments of
                                                                  massive datasets, we project the performance of UMAP on
other GPU alternatives in appendix C.
                                                                  CPU by measuring its performance on smaller subsets and
All experiments were conducted on an NVIDIA DGX ma-               extrapolating to the full datasets. Section 5.1 shows how
chine with 8 NVIDIA H100 GPUs (NVIDIA, 2022) and                  the performance was projected and compares against the
an Intel Xeon 8480CL 224-core CPU with 2T iB of RAM.              performance measurements on GPU.
The remainder of this section is as follows: Section 4.1 pro-
vides an overview of the datasets used in our experiments.        4.3       Precomputed all-neighbors on GPU
Section 4.2 and Section 4.3 outline the workflows used to
measure the performance and correctness of our implemen-          Since the all-neighbors graph on CPU is prohibitive at the
tation against the reference UMAP implementation on CPU.          larger scales, we precompute it on the GPU using the algo-
Section 4.4 discusses our correctness metric and Section 4.5            1
                                                                       https://www.kaggle.com/datasets/jjinho/wikipedia-
highlights the UMAP configurations that were used in the          20230701
                                                                     2
experiments.                                                           https://huggingface.co/datasets/Cohere/wikipedia-22-12
                                        Massive-Scale Out-Of-Core UMAP on the GPU

rithm outlined in Section 3 in order to demonstrate the per-     5     R ESULTS
formance benefits when comparing CPU and GPU versions.
This experiment also shows that our approach achieves cor-       5.1   End-to-end Speedup
rectness comparable to CPU on the larger datasets Wiki-all       We conduct end-to-end comparisons between CPU UMAP
and MIRACL, even while they failed to run end-to-end on          and our UMAP implementation, where each method con-
the CPU.                                                         structs its own all-neighbors graph as part of the full pipeline.
                                                                 Due to the high computational cost and memory require-
4.4   Metrics                                                    ments of all-neighbors graph construction in CPU UMAP,
                                                                 we were unable to complete end-to-end runs on the largest
We report time in seconds for the performance metric, but
                                                                 datasets Wiki-all and MIRACL despite the full dataset fit-
evaluate two aspects as a correctness metric– trustworthi-
                                                                 ting in our CPU memory. Running CPU UMAP on a 16M
ness and visual inspection.
                                                                 subsample of the MIRACL dataset required approximately
Trustworthiness Score The trustworthiness score (Venna           1.4TB of CPU memory. Following this, we were unable to
& Kaski, 2001) measures the degree to which the local neigh-     run the 32M subsample on our machine.
borhood structure in the original high-dimensional data is
                                                                 Therefore, as mentioned in Section 4.3, we predicted the
preserved in the low-dimensional space. The score ranges
                                                                 end-to-end CPU UMAP run times by taking smaller sub-
between 0 and 1, with values closer to 1 indicating better
                                                                 samples from larger datasets and ultimately extrapolating
preservation of local neighborhood structure. Computing
                                                                 points for the full dataset. To model the relationship between
the trustworthiness score can be prohibitive at larger scales
                                                                 dataset size and execution time, we fit a power-law function
because it requires computing the full pairwise distances
                                                                 of the following form for each large dataset: T (N ) = aN b ,
of the high-dimensional vectors. Empirically, we find that
                                                                 where T (N ) denotes the runtime for a dataset of N data
computing trustworthiness scores over multiple uniformly
                                                                 vectors, and a and b are fitting parameters. This function
sampled subsets and averaging the results provides a fast
                                                                 is used for extrapolation because the all-neighbors graph
converging approximation of the true global trustworthiness
                                                                 construction shows power law scaling and dominates the
score, which can be viewed as a monte-carlo approximation.
                                                                 end-to-end runtime.
Visual Inspections We have found the trustworthiness
                                                                 Extrapolated points are plotted with the measured points in
score can sometimes be deceiving when quantifying the de-
                                                                 Figure 3 (a). We note that the projected CPU UMAP run
gree to which a visualization maintains faithfulness across
                                                                 times grow super-linearly with sampled dataset size. This
implementations, so we also perform visual inspections of
                                                                 is not only due to the inherent algorithmic complexity, but
the resulting 2-dimensional embeddings. Since we are eval-
                                                                 also because larger datasets consume increasing amounts of
uating datasets at massive-scale, we uniformly subsample
                                                                 system memory, particularly during the all-neighbors graph
vectors from the low-dimensional embedding so that the
                                                                 construction step. On the CPU, this memory pressure even-
final visualization provides intuitive confirmation that our
                                                                 tually brings usage to the system’s full capacity, even on the
implementation faithfully compares to the reference imple-
                                                                 2TiB RAM machine used in our experiments, significantly
mentation on the CPU.
                                                                 impacting performance. This demonstrates that running
                                                                 such large problems on CPUs quickly becomes prohibitive
4.5   UMAP Configurations                                        at these scales.
We used the default values for both CPU and GPU imple-           Figure 3 (b) shows the resulting speedups and trustwor-
mentations in all experiments with the exception that the em-    thiness scores for varying dataset scales. Based on the
bedding initialization method was set to random for both.        predicted end-to-end CPU runtime in Figure 3 (a), our im-
We selected specific values for c and s to efficiently utilize   plementation achieves 74× speedup on the largest MIRACL
available GPU memory. The parameters (c = 16, s = 2)             dataset. It also achieves noticeable speedups on smaller
were used for Wiki-all and (c = 64, s = 2) were used for         datasets while maintaining similar trustworthiness scores.
MIRACL to balance memory usage and computational ef-             Beyond the performance gain, this result highlights a key
ficiency. More details about how these parameters affect         advantage of our approach. Our memory and performance
the memory usage and trustworthiness score are outlined in       aware implementation makes processing such large datasets
Section 5.4.                                                     tractable on GPU, whereas the CPU implementation strug-
                                                                 gles to complete within practical time or memory limits.
                                                                 To the best of our knowledge, there is no publicly available
                                                                 multi-node CPU implementation of UMAP today. By using
                                                                 the fastest (original) single-node CPU implementation and
                                                                 using perfect linear extrapolation, we are overestimating
                                          Massive-Scale Out-Of-Core UMAP on the GPU


                                                                     Table 2. Shows mean time in seconds ± variance to build the all-
                                                                     neighbors graph. We used multiple GPUs for an efficient build for
                                                                     larger datasets.
                                                                        Dataset                         #GPUs         Time(s)
                                                                        Beauty Amazon Reviews              1         2.62 ± 0.05
                                                                        Appliances Amazon Reviews          1         7.48 ± 0.01
                                                                        Food Amazon Reviews                1        21.16 ± 0.15
                                                                        Electronics Amazon Reviews         1        96.04 ± 0.30
                                                                        Wiki-all                           8       272.68 ± 44.54
                                                                        MIRACL                             8       341.74 ± 14.47




Figure 3. (a) shows measured and projected CPU UMAP runtimes
for the Wiki-all and MIRACL datasets for varying subsample
sizes. (b) shows end-to-end runtime and trustworthiness scores
for various datasets. The blue projections come from the full size   Figure 4. Runtime and trustworthiness of UMAP on CPU vs GPU
data performance projection in (a). We use 8 GPUs for Wiki and       using a GPU-precomputed all-neighbors graph. Our implementa-
MIRACL, and 1 GPU for all other datasets. Our implementation         tion delivers strong performance gains running remaining steps of
delivers strong performance gains while maintaining comparable       UMAP while maintaining comparable trustworthiness.
trustworthiness.

                                                                     To further validate embedding quality, we include qual-
CPU performance, which disadvantages our approach. The               itative comparisons of the final two-dimensional UMAP
isoefficiency considerations for network communication,              projections for both CPU and GPU implementations. As
latency, and overheads are 0 for CPU UMAP in this analysis,          shown in Figure 5, for the largest MIRACL dataset, the
whereas our empirical multi-GPU implementation accounts              embedding produced by our UMAP closely matches that of
for all of these overheads. Our 74x speedup, despite this            CPU UMAP, confirming that our GPU-accelerated UMAP
disadvantage, ensures our method is both a conservative              correctly preserves the structure of the dataset. Embed-
estimate and significant improvement in practice.                    ding visualization comparisons for smaller datasets are in
                                                                     appendix Section D.
5.2   Precomputed kNN on GPU
                                                                     5.3   Scaling with Multiple GPUs
In this section, we present the performance and trustworthi-
ness scores of running the remaining steps of CPU UMAP               To evaluate the multi-GPU scalability of our UMAP imple-
and our GPU-accelerated UMAP, using an all-neighbors                 mentation, we measure the strong scaling for end-to-end
graph that was pre-computed on the GPU. The time re-                 execution time of distributing the workload across multiple
quired to construct the all-neighbors graph is in Table 2,           GPUs on our two larger datasets Wiki-all and MIRACL.
where k = 15 to match the default n neighbors parame-                Figure 6 (a) and (b) show the observed scaling trend. The re-
ter in both CPU and GPU implementations.                             sults show that the execution time decreases with the number
                                                                     of GPUs, demonstrating that parallelizing the all-neighbors
Figure 4 compares the end-to-end execution time across
                                                                     construction provides significant performance benefits. Also
different datasets. Our GPU-accelerated implementation
                                                                     note the robustness of our approach, as the trustworthiness
achieves up to 121× speedup over the CPU baseline on the
                                                                     score remains consistent regardless of the number of GPUs.
largest MIRACL dataset, without noticeable degradation of
trustworthiness score against the CPU implementation.                We note in Figure 6 (c) that the all-neighbors construction
                                        Massive-Scale Out-Of-Core UMAP on the GPU




Figure 5. Resulting embeddings of CPU UMAP using a GPU-
precomputed all-neighbors graph, and our GPU UMAP one the
MIRACL dataset. We randomly sampled 30,000 points for visual-
ization.



speedup gain is not perfectly linear as the number of GPUs
increases. One of the primary sources of the observed sub-
linear scaling is CPU-side utilization. Our GPU nn-descent
implementation includes multi-threaded operations that are
done on CPU within each iteration. With more GPUs, the
number of CPU cores available per GPU to do these multi-
threaded operations is reduced, thus causing contention for
the CPU. We observe that the per-thread CPU time to do
these multi-threaded operations increases by 1.3x and 1.95x
for 4 and 8 GPUs, respectively, compared to 2 GPUs. For
the 8 GPU case, the multi-threaded CPU operations account
for 13% of the end-to-end kNN construction time. This
explains the observed speedup being more pronounced for
the larger dataset (MIRACL), where a greater proportion is
GPU compute-bound.
The serialization point for merging the local graphs from the
GPU into the global graph does not introduce a significant
                                                                  Figure 6. (a) and (b) show strong scaling results of our approach
bottleneck. Profiling on the Wiki dataset (88M x 768) with 8
                                                                  for increasing number of GPUs. (c) shows the speedup of the all-
GPUs (c = 16, s = 2) reveals that the average CPU launch          neighbors graph construction step for the Wiki-all and MIRACL
wait per merge kernel execution accounts for 0.02% of the         datasets, relative to using a single GPU. Execution time continues
end-to-end kNN construction (merge kernel is launched c           to shrink as more GPUs are added, showing our approach scales
times). This efficiency arises because our merging kernel         well with multiple GPUs.
is highly optimized to avoid unnecessary GPU memory
allocations, and to exploit CUDA shared memory. A single
merge kernel execution runtime contributes to 0.4% of the         The parameter c defines the number of clusters, thus impact-
total end-to-end kNN construction time. Additionally, GPUs        ing the GPU memory usage. A larger c produces smaller
process k-means clusters of varying sizes and complete at         clusters, thereby reducing the memory pressure during the
slightly different times, further hiding latencies and reducing   all-neighbors construction. The parameter s determines the
serialization overhead.                                           degree of spilling. A larger s increases computation by
                                                                  adding more vectors per cluster, but enhances the accuracy
5.4   Impact of kNN Parameters                                    of the resulting all-neighbors graph by improving cross-
                                                                  cluster connectivity. Each cluster contains approximately
We analyze how the number of clusters c and spill factor s
                                                                  N · sc data vectors where N is the number of data vectors in
in our all-neighbors construction algorithm affect the trust-
                                                                  the whole dataset.
worthiness and end-to-end performance of UMAP. We also
report the GPU memory usage during the all-neighbors con-         In practice, Equation (1) can be used to estimate values
struction stage to show how c and s affect memory usage.          of s and c that satisfy the target GPU memory constraints.
                                           Massive-Scale Out-Of-Core UMAP on the GPU

For a fixed ratio z, where z = sc , which preserves per-              Figure 7 (b) shows the runtime and trustworthiness scores
GPU memory usage, improving kNN graph recall can be                   of running UMAP on the same configurations. Although
achieved by increasing s while adjusting c in proportion              the differences in trustworthiness scores are not significant,
to keep z constant. A typical tuning strategy is to begin             we observe a trend. Trustworthiness drops slightly as c is
with s = 2 and increase s in small integer steps while still          increased from 16 to 64 with s = 2, because true neighbors
maintaining a constant z. In practice, good kNN recall                are more likely to fall into different partitions. While com-
and embedding quality are often achieved after only a few             paring configurations (2, 16), (4, 32), and (8, 64), we notice
increases (s = 2 → 3 → 4). For very large datasets                    larger s improves trustworthiness scores because it increases
where c is large (e.g. c > 100), increasing s in slightly             the chance of seeing a true neighbor by being assigned to
larger steps (s = 2 → 4 → 6) may be more appropriate.                 multiple clusters. The end-to-end runtime increases with s,
However, s should not be increased excessively, as higher             as higher values require processing more vectors. It’s impor-
values of s improve kNN recall at the expense of increased            tant to note that the UMAP algorithm itself provides several
computation.                                                          parameters to tune the quality of the embedding, while this
                                                                      experiment is only concerned with demonstrating the impact
Figure 7 shows the result of running the Wiki-all dataset
                                                                      of c and s on trustworthiness. The two parameters s and
with varying (s, c) configurations. Figure 7 (a) shows GPU
                                                                      c exhibit a clear trade-off between performance and GPU
memory usage excluding the global all-neighbors graph
                                                                      memory usage, while also affecting the trustworthiness of
for better observation of how s and c impact the memory
                                                                      the UMAP embedding. The impacts that these parameters
usage. The peak GPU memory usage for all-neighbors
                                                                      directly have on the kNN recall is in appendix E.
construction roughly halves as c increases from 16 to 64
with s = 2, because larger c reduces the size of each cluster.
Comparing configurations (2, 16), (4, 32), and (8, 64), we            6    C ONCLUSION
see similar memory usage, as they all result in comparable
                                                                      In this work, we introduced a novel algorithm for computing
number of vectors in GPU memory. A small overhead
                                                                      a UMAP embedding out-of-core on GPU, providing the abil-
remains for larger c because additional clusters and data
                                                                      ity to scale to massive datasets. Our algorithm introduces
vector assignments need to be maintained.
                                                                      two new parameters, c and s, for trading off space and time,
                                                                      allowing it to scale far beyond the memory capacity of a
                                                                      single GPU. Further, our contribution reduces the burden
                                                                      of communications as it scales, allowing it to be executed
                                                                      efficiently across multiple GPUs. We analyzed how our
                                                                      method improves all-neighbors graph construction, which
                                                                      is the primary runtime and memory bottleneck on both CPU
                                                                      and GPU today, and we outlined an algorithm based on IVF
                                                                      with spilling that maintains quality of the resulting UMAP
                                                                      embedding.
                                                                      We demonstrated groundbreaking speedup at scale and be-
                                                                      lieve our work will open new opportunities for iterative and
                                                                      exploratory large-scale analysis with UMAP, enabling both
                                                                      academic research and industrial applications to operate in-
                                                                      teractively on datasets that were previously intractable on
                                                                      the CPU. Our GPU-accelerated UMAP implementation is
                                                                      fully open-source within cuML with Python wrappers for
                                                                      ease of use. In addition to enabling massive-scale UMAP,
                                                                      our partitioned all-neighbors construction framework can
                                                                      serve as a foundation for many other GPU-accelerated data
                                                                      mining algorithms in the manifold learning and clustering
                                                                      space. This capability is already available as a standalone
                                                                      feature in cuVS.
Figure 7. (a) shows peak GPU memory (in megabytes) used for
                                                                      While this algorithm has unlocked extreme scale for many of
building the all-neighbors graph, excluding the GPU memory al-
                                                                      today’s demanding applications and workflows, its primary
located for the global all-neighbors graph. x axis is for different
configurations of (s, c). (b) shows runtime and trustworthiness       limitation is the requirement that the final all-neighbors
scores. Trustworthiness improves with the spilling factor s, and      graph fit into the memory space of a single GPU. This
memory usage improves with number of clusters c.                      constraint ultimately imposes an upper bound on the number
                                       Massive-Scale Out-Of-Core UMAP on the GPU

of data vectors it can handle. As applications continue to      Dong, W., Moses, C., and Li, K. Efficient k-nearest neigh-
push to billion-scale and beyond, we continue to improve          bor graph construction for generic similarity measures.
upon our approach with the goal of scaling efficiently to         In Proceedings of the 20th international conference on
utilize 1) the larger space of system memory, 2) multiple        World wide web, pp. 577–586, 2011.
physical nodes, and 3) the massive bandwidth of emerging
NVMe technologies being enabled by PCIe 5 and above.            Douze, M., Guzhva, A., Deng, C., Johnson, J., Szilvasy, G.,
                                                                  Mazaré, P.-E., Lomeli, M., Hosseini, L., and Jégou, H.
                                                                  The faiss library. IEEE Transactions on Big Data, 2025.
R EFERENCES
                                                                Eisenmann, P. GitHub - p3732/gpumap: GPU-Parallelized
Ali, S., Abuhmed, T., El-Sappagh, S., Muhammad, K.,
                                                                  UMAP — github.com. https://github.com/
  Alonso-Moral, J. M., Confalonieri, R., Guidotti, R.,
                                                                  p3732/gpumap. [Accessed 30-10-2025].
  Del Ser, J., Dı́az-Rodrı́guez, N., and Herrera, F. Ex-
  plainable artificial intelligence (xai): What we know and     Friebel, E., Kapolou, K., Unger, S., Núñez, N. G., Utz, S.,
  what is left to attain trustworthy artificial intelligence.     Rushing, E. J., Regli, L., Weller, M., Greter, M., Tugues,
  Information fusion, 99:101805, 2023.                            S., et al. Single-cell mapping of human brain cancer re-
                                                                  veals tumor-specific instruction of tissue-invading leuko-
Altin, M. and Cakir, A. Exploring the influence of dimen-         cytes. Cell, 181(7):1626–1642, 2020.
  sionality reduction on anomaly detection performance in
  multivariate time series. IEEE Access, 12:85783–85794,        Grootendorst, M. Bertopic: Neural topic modeling
  2024.                                                           with a class-based tf-idf procedure. arXiv preprint
                                                                  arXiv:2203.05794, 2022.
Angelov, D. Top2vec: Distributed representations of topics.
  arXiv preprint arXiv:2008.09470, 2020.                        Guo, R., Sun, P., Lindgren, E., Geng, Q., Simcha, D., Chern,
                                                                  F., and Kumar, S. Accelerating large-scale inference with
Becht, E., McInnes, L., Healy, J., Dutertre, C.-A., Kwok,         anisotropic vector quantization. In International Con-
  I. W., Ng, L. G., Ginhoux, F., and Newell, E. W. Dimen-         ference on Machine Learning, pp. 3887–3896. PMLR,
  sionality reduction for visualizing single-cell data using      2020.
  umap. Nature biotechnology, 37(1):38–44, 2019.
                                                                Haarnoja, T., Moran, B., Lever, G., Huang, S. H., Tirumala,
Belkin, M. and Niyogi, P. Laplacian eigenmaps for di-             D., Humplik, J., Wulfmeier, M., Tunyasuvunakool, S.,
  mensionality reduction and data representation. Neural          Siegel, N. Y., Hafner, R., et al. Learning agile soccer
  computation, 15(6):1373–1396, 2003.                             skills for a bipedal robot with deep reinforcement learn-
                                                                  ing. Science Robotics, 9(89):eadi8022, 2024.
Bergum,       J.   K.            Billion-scale vec-
  tor    search    using   hybrid     HNSW-IF    —  Hao, Y., Hao, S., Andersen-Nissen, E., Mauck, W. M.,
  blog.vespa.ai.        https://blog.vespa.ai/        Zheng, S., Butler, A., Lee, M. J., Wilk, A. J., Darby,
  vespa-hybrid-billion-scale-vector-search/, C., Zager, M., et al. Integrated analysis of multimodal
  2022. [Accessed 11-02-2026].                        single-cell data. Cell, 184(13):3573–3587, 2021.
Cayton, L. et al. Algorithms for manifold learning. Univ. of    Hotelling, H. Analysis of a complex of statistical variables
  California at San Diego Tech. Rep, 12(1-17):1, 2005.            into principal components. Journal of educational psy-
                                                                  chology, 24(6):417, 1933.
CCCL Development Team. CCCL: CUDA C++ Core
 Libraries, 2023. URL https://github.com/                       Imambi, S., Prakash, K. B., and Kanagachidambaresan, G.
 NVIDIA/cccl. [Accessed 30-10-2025].                              Pytorch. In Programming with TensorFlow: solution
                                                                  for edge computing applications, pp. 87–104. Springer,
Costa-Jussà, M. R., Cross, J., Çelebi, O., Elbayad, M.,         2021.
  Heafield, K., Heffernan, K., Kalbassi, E., Lam, J., Licht,
  D., Maillard, J., et al. No language left behind: Scal-       Jayaram Subramanya, S., Devvrit, F., Simhadri, H. V., Kr-
  ing human-centered machine translation. arXiv preprint          ishnawamy, R., and Kadekodi, R. Diskann: Fast accurate
  arXiv:2207.04672, 2022.                                         billion-point nearest neighbor search on a single node.
                                                                  Advances in neural information processing Systems, 32,
De Zuani, M., Xue, H., Park, J. S., Dentro, S. C., Sefer-         2019.
  bekova, Z., Tessier, J., Curras-Alonso, S., Hadjipanayis,
  A., Athanasiadis, E. I., Gerstung, M., et al. Single-cell     Johnson, J., Douze, M., and Jégou, H. Billion-scale similar-
  and spatial transcriptomics analysis of non-small cell lung     ity search with gpus. IEEE Transactions on Big Data, 7
  cancer. Nature communications, 15(1):4388, 2024.                (3):535–547, 2019a.
                                       Massive-Scale Out-Of-Core UMAP on the GPU

Johnson, J., Douze, M., and Jégou, H. Billion-scale similar-   Nolet, C. J., Lafargue, V., Raff, E., Nanditale, T., Oates, T.,
  ity search with GPUs. IEEE Transactions on Big Data, 7          Zedlewski, J., and Patterson, J. Bringing umap closer
  (3):535–547, 2019b.                                             to the speed of light with gpu acceleration. In Proceed-
                                                                  ings of the AAAI Conference on Artificial Intelligence,
Korsunsky, I., Millard, N., Fan, J., Slowikowski, K., Zhang,
                                                                  volume 35, pp. 418–426, 2021.
  F., Wei, K., Baglaenko, Y., Brenner, M., Loh, P.-r., and
  Raychaudhuri, S. Fast, sensitive and accurate integration     NVIDIA, i.      NVIDIA H100 GPU Whitepaper —
  of single-cell data with harmony. Nature methods, 16(12):      resources.nvidia.com.    https://resources.
 1289–1296, 2019.                                                nvidia.com/en-us-hopper-architecture/
Lam, S. K., Pitrou, A., and Seibert, S. Numba: A llvm-           nvidia-h100-tensor-c, 2022.        [Accessed
  based python jit compiler. In Proceedings of the Second        30-10-2025].
  Workshop on the LLVM Compiler Infrastructure in HPC,          Ootomo, H., Naruse, A., Nolet, C., Wang, R., Feher, T.,
  pp. 1–6, 2015.                                                  and Wang, Y. Cagra: Highly parallel graph construction
LargeVis.  Largevis. https://github.com/                          and approximate nearest neighbor search for gpus, 2024.
  lferry007/LargeVis, 2016. [Accessed 18-02-                      URL https://arxiv.org/abs/2308.15136.
  2026].
                                                                O’Neil, P., Cheng, E., Gawlick, D., and O’Neil, E. The
Liu, T., Moore, A., Yang, K., and Gray, A. An investiga-          log-structured merge-tree (lsm-tree). Acta informatica,
  tion of practical approximate nearest neighbor algorithms.      33(4):351–385, 1996.
  Advances in neural information processing systems, 17,
  2004.                                                         Perez, E., Ringer, S., Lukosiute, K., Nguyen, K., Chen, E.,
                                                                  Heiner, S., Pettit, C., Olsson, C., Kundu, S., Kadavath, S.,
Maaten, L. v. d. and Hinton, G. Visualizing data using            et al. Discovering language model behaviors with model-
 t-sne. Journal of machine learning research, 9(Nov):             written evaluations. In Findings of the association for
 2579–2605, 2008.                                                 computational linguistics: ACL 2023, pp. 13387–13434,
Malkov, Y. A. and Yashunin, D. A. Efficient and robust            2023.
 approximate nearest neighbor search using hierarchical         Reimers, N. and Gurevych, I. Sentence-bert: Sentence em-
 navigable small world graphs. IEEE transactions on               beddings using siamese bert-networks. In Proceedings
 pattern analysis and machine intelligence, 42(4):824–            of the 2019 Conference on Empirical Methods in Natu-
 836, 2018.                                                       ral Language Processing. Association for Computational
Martı́nez-Ruiz, C., Black, J. R., Puttick, C., Hill, M. S.,       Linguistics, 11 2019. URL https://arxiv.org/
 Demeulemeester, J., Larose Cadieux, E., Thol, K., Jones,         abs/1908.10084.
 T. P., Veeriah, S., Naceur-Lombardelli, C., et al. Genomic–
                                                                Roweis, S. T. and Saul, L. K. Nonlinear dimensionality re-
 transcriptomic evolution in lung cancer and metastasis.
                                                                  duction by locally linear embedding. science, 290(5500):
 Nature, 616(7957):543–552, 2023.
                                                                  2323–2326, 2000.
McInnes, L. Pynndescent, 2021.
                                                                Sarthi, P., Abdullah, S., Tuli, A., Khanna, S., Goldie, A., and
McInnes, L., Healy, J., and Melville, J. Umap: Uniform            Manning, C. D. Raptor: Recursive abstractive processing
 manifold approximation and projection for dimension              for tree-organized retrieval. In The Twelfth International
 reduction. arXiv preprint arXiv:1802.03426, 2018a.               Conference on Learning Representations, 2024.
McInnes, L., Healy, J., Saul, N., and Grossberger, L. Umap:     Shen, X., Chen, Z., Backes, M., Shen, Y., and Zhang, Y.
 Uniform manifold approximation and projection. The               ” do anything now”: Characterizing and evaluating in-
 Journal of Open Source Software, 3(29):861, 2018b.               the-wild jailbreak prompts on large language models. In
Munyampirwa, B., Lakshman, V., and Coleman, B. Down               Proceedings of the 2024 on ACM SIGSAC Conference on
 with the hierarchy: The’h’in hnsw stands for” hubs”.             Computer and Communications Security, pp. 1671–1685,
 arXiv preprint arXiv:2412.01940, 2024.                           2024.

Ni, J., Li, J., and McAuley, J. Justifying recommendations      Simhadri, H. V., Williams, G., Aumüller, M., Douze, M.,
  using distantly-labeled reviews and fine-grained aspects.       Babenko, A., Baranchuk, D., Chen, Q., Hosseini, L.,
  In Proceedings of the 2019 conference on empirical meth-        Krishnaswamny, R., Srinivasa, G., et al. Results of the
  ods in natural language processing and the 9th interna-         neurips’21 challenge on billion-scale approximate nearest
  tional joint conference on natural language processing          neighbor search. In NeurIPS 2021 Competitions and
  (EMNLP-IJCNLP), pp. 188–197, 2019.                              Demonstrations Track, pp. 177–189. PMLR, 2022.
                                       Massive-Scale Out-Of-Core UMAP on the GPU

Sun, P., Simcha, D., Dopson, D., Guo, R., and Kumar, S.
  Soar: improved indexing for approximate nearest neigh-
  bor search. Advances in Neural Information Processing
  Systems, 36:3189–3204, 2023.
Tang, J., Liu, J., Zhang, M., and Mei, Q. Visualizing large-
  scale and high-dimensional data. In Proceedings of the
  25th international conference on world wide web, pp.
  287–297, 2016.
Tenenbaum, J. B., Silva, V. d., and Langford, J. C. A
  global geometric framework for nonlinear dimensionality
  reduction. science, 290(5500):2319–2323, 2000.

Van Assel, H., Courty, N., Flamary, R., Garivier,
  A., Massias, M., Vayer, T., and Vincent-Cuaz, C.
  TorchDR. URL https://github.com/TorchDR/
  TorchDR. [Accessed 30-10-2025].
Venna, J. and Kaski, S. Neighborhood preservation in non-
  linear projection methods: An experimental study. In
  International conference on artificial neural networks, pp.
  485–491. Springer, 2001.
Wang, H., Zhao, W.-L., Zeng, X., and Yang, J. Fast k-nn
 graph construction by gpu based nn-descent. In Pro-
 ceedings of the 30th ACM International Conference on
 Information & Knowledge Management, pp. 1929–1938,
 2021a.
Wang, J., Yi, X., Guo, R., Jin, H., Xu, P., Li, S., Wang, X.,
 Guo, X., Li, C., Xu, X., et al. Milvus: A purpose-built
 vector data management system. In Proceedings of the
 2021 international conference on management of data,
 pp. 2614–2627, 2021b.
Xian, J., Teofili, T., Pradeep, R., and Lin, J. Vector search
  with openai embeddings: Lucene is all you need. In
  Proceedings of the 17th ACM International Conference
  on Web Search and Data Mining, pp. 1090–1093, 2024.
Zhang, X., Thakur, N., Ogundepo, O., Kamalloo, E.,
  Alfonso-Hermelo, D., Li, X., Liu, Q., Rezagholizadeh,
  M., and Lin, J. MIRACL: A Multilingual Retrieval
  Dataset Covering 18 Diverse Languages. Transactions
  of the Association for Computational Linguistics, 11:
  1114–1131, 09 2023. ISSN 2307-387X. doi: 10.1162/
  tacl a 00595. URL https://doi.org/10.1162/
  tacl_a_00595.
                                         Massive-Scale Out-Of-Core UMAP on the GPU

A    M ULTI -GPU ALL - NEIGHBORS WITH
     FAISS
To show that our all-neighbors algorithm is robust and scal-
able, we compare results against building an all-neighbors
graph with multi-GPU Faiss IVF-PQ. IVF-Flat is not appli-
cable at the scales we target in this paper, since it requires
the full dataset to reside in GPU memory.
Faiss supports two multi-GPU modes. The first is replica-
tion, where the full index is copied to each GPU and queries
are distributed across them. Second is uniform sharding
mentioned in Section 2.3, which requires all queries to be
broadcasted to all GPUs during search time. In this section
we use replication mode, which offers better multi-GPU
performance when the index fits on a single GPU.                   Figure 8. Recall vs E2E time in seconds for Faiss’ IVF-PQ and
                                                                   our all-neighbors algorithm. 4 NVIDIA H100 GPUs are used for
Since IVF-PQ uses lossy compression, a refinement step             all runs. The speed gap to reach the same recall increases as the
is necessary to improve recall by re-ranking candidates us-        dataset size increases.
ing exact distances. We use Faiss’s IndexRefineFlat,
which recomputes exact distances for given candidates.
                                                                   ranging from 3.5× at 1M vectors to 19.8× at 20M vectors.
To compare the two approaches, we evaluate on smaller
subsets of the Wiki-all dataset (1M, 5M, 10M, and 20M
vectors) for which ground truth can be computed via brute          B    A LL - NEIGHBORS IN L ARGE V IS
force. We run our all-neighbors algorithm across these             LargeVis (Tang et al., 2016) constructs an approximate kNN
subsamples with a fixed (c, s) = (4, 2) to be able to run it       graph before learning the low-dimensional embedding for
with 4 NVIDIA H100 GPUs. We tune Faiss’ IVF-PQ to                  visualization. It first generates candidate neighbors using
achieve recall levels comparable to our results and report         multiple random projection trees, where the points that fall
end-to-end runtime. Experiments for Faiss were done using          into the same leaf node are treated as potential neighbors.
the same 4 NVIDIA H100 GPUs.                                       The neighbor lists are then refined through a neighbors
                      √                                            exploration procedure that evaluates neighbors of neighbors.
We set n list = n rows, and n probes to 10% of
n list. Increasing n probes alone proved insufficient              We evaluated the official LargeVis implementation
to close the recall gap. To reach recall levels comparable to      (LargeVis) for kNN graph construction using the same DGX
our algorithm, we found it necessary to use 96 subquantizers,      system in our Section 4. Compared to our all-neighbors con-
which is the maximum PQ code length supported by Faiss             struction on a single GPU, LargeVis is 20× slower on the
for GPU IVF-PQ. Remaining tuning was performed via the             Beauty dataset (640K × 384) and 41× slower on the Appli-
refinement factor, where the search retrieves k ×                  ances dataset (1.8M × 384).
refinement factor number of nearest neighbors for
candidates for refinement. Detailed configurations for each
run is in Table 3.                                                 Table 4. kNN graph construction time comparison against
                                                                   LargeVis. All time in seconds. For computing speedup of our
                                                                   algorithm, we use the single-GPU all-neighbors kNN construction
Table 3. Configuration of Faiss’ IVF-PQ used to match our all-     time reported in Table 2.
neighbors recall for various subsample sizes. Number of subquan-           Data              LargeVis     Speedup (Ours)
tizers is fixed to 96.
   Size      n list n probes refinement factor                             Beauty         51.79 ± 2.37               19.8×
                                                                           Appliances    308.48 ± 5.12               41.2×
    1M        1000           100                         5.5
    5M        2236           223                         3.5
    10M       3162           316                         3.2
    20M       4472           447                           3       C     A LTERNATE UMAP I MPLEMENTATIONS
                                                                   Our primary contribution is our out-of-core, multi-GPU
Figure 8 shows the recall and end-to-end runtime for each          all-neighbors graph construction, which enables UMAP to
of the data subsamples. To achieve comparable recall, Faiss’       operate at dataset scales previously out of reach. The engi-
IVF-PQ requires significantly more computation than our al-        neering effort to adapt prior implementations is significant
gorithm, with the gap widening as the dataset size increases,      enough that attempting to modify them would require de-
                                            Massive-Scale Out-Of-Core UMAP on the GPU

vising a new publishable result, as they are non-viable as is.
This is exemplified by prior work (Nolet et al., 2021) show-
ing that GPUMAP (Eisenmann) fails to maintain a faithful
UMAP representation and falls short in performance even
for small datasets.

Table 5. TorchDR results. All time in seconds. For computing
speedup of our algorithm, we use the end-to-end time of our GPU
UMAP reported in Figure 3 (b).
    Data          E2E Time     Trustworthiness    Speedup (Ours)
    Beauty       17.43 ± 3.25        0.9491           4.13×
    Appliances   55.48 ± 0.76        0.9416           5.75×
    Food                  OOM fail on NVIDIA H100 (80GB)           Figure 10. Resulting embeddings of CPU UMAP (left) and our
                                                                   GPU UMAP (right) using a GPU-precomputed kNN graph on the
The newer TorchDR (Van Assel et al.) maintains similar             Appliances Amazon Reviews dataset.
trustworthiness but is 5.7× slower than our approach on
a single GPU for small datasets (Appliances), even when
using its fastest method, the Faiss (Douze et al., 2025) IVF
backend. Moreover, the medium-sized Food dataset, which
is about 8GB in size, causes an out-of-memory error for
TorchDR on an 80GB H100 GPU. For these reasons, we
focus on our method’s scaling and, where possible, compare
it to best-case extrapolations of prior art.

D    GPU- PRECOMPUTED K NN UMAP
     V ISUALIZATIONS
This section presents the visualization introduced in Fig-         Figure 11. Resulting embeddings of CPU UMAP (left) and our
ure 5 for smaller datasets (Figure 9, Figure 10, Figure 11,        GPU UMAP (right) using a GPU-precomputed kNN graph on the
                                                                   Food Amazon Reviews dataset.
Figure 12, Figure 13). Note that UMAP embeddings are
invariant to global translations, rotations, or reflections, so
visual differences in orientation or position do not indicate
differences in embedding quality.




                                                                   Figure 12. Resulting embeddings of CPU UMAP (left) and our
                                                                   GPU UMAP (right) using a GPU-precomputed kNN graph on the
                                                                   Electronics Amazon Reviews dataset.

Figure 9. Resulting embeddings of CPU UMAP (left) and our GPU
UMAP (right) using a GPU-precomputed kNN graph on the Beauty       figurations with similar s values show comparable runtimes
Amazon Reviews dataset.                                            due to similar computational requirements. For a fixed ratio
                                                                   z = sc , using large s (and therefore c) values achieve higher
                                                                   recall at the cost of increased runtime.
E    I MPACT OF s AND c ON K NN R ECALL
This section presents runtime and recall of the all-neighbors
kNN graph construction across various values of s and c in
Figure 14. We evaluate on smaller subsets of MIRACL for
which ground truth can be computed via brute force. Con-
                                            Massive-Scale Out-Of-Core UMAP on the GPU




Figure 13. Resulting embeddings of CPU UMAP (left) and our
GPU UMAP (right) using a GPU-precomputed kNN graph on the
Wiki-all dataset.




Figure 14. Result of runtime and recall for varying configurations
of (c, s). For a fixed ratio z = sc , using large s (and therefore c)
values achieve higher recall at the cost of increased runtime.
