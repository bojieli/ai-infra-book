                      H IP K ITTENS : FAST AND Furious AMD K ERNELS


        William Hu 1 Drew Wadsworth 1 Sean Siddens 2 Stanley Winata 2 Daniel Y. Fu 3 Ryan Swann 2
                          Muhammad Osama 2 Christopher Ré 1 Simran Arora 1


                                                          A BSTRACT
     AMD GPUs offer state-of-the-art compute and memory bandwidth; however, peak performance AMD kernels are
     written in raw assembly. To address the difficulty of mapping AI algorithms to hardware, recent work proposes
     C++ embedded and PyTorch-inspired domain-specific languages like T HUNDER K ITTENS (TK) to simplify high
     performance AI kernel development on NVIDIA hardware. We explore the extent to which such primitives — for
     explicit tile-based programming with optimized memory accesses and fine-grained asynchronous execution across
     workers — are NVIDIA-specific or general. We provide the first detailed study of the programming primitives that
     lead to performant AMD AI kernels, and we encapsulate these insights in the H IP K ITTENS (HK) programming
     framework. We find that tile-based abstractions used in prior DSLs generalize to AMD GPUs, however we need to
     rethink the algorithms that instantiate these abstractions for AMD. We validate the HK primitives across CDNA3
     and CDNA4 AMD platforms. In evaluations, HK kernels compete with AMD’s hand-optimized assembly kernels
     for GEMMs and attention, and consistently outperform compiler baselines. Moreover, assembly is difficult to
     scale to the breadth of AI workloads; reflecting this, in some settings HK outperforms all available baselines by
     1.2 → 2.4↑ (d = 64 attention, GQA backwards, memory-bound kernels). HK demonstrates a portable tile-based
     programming model that abstracts over hardware-specific implementation details. These findings help pave the way
     for a single, tile-based software layer for high-performance AI kernels across GPU vendors. H IP K ITTENS has been
     productionalized in AITER and is released at: https://github.com/HazyResearch/HipKittens.

1   I NTRODUCTION                                               attention kernels (Shah et al., 2024). Compilers like Tri-
                                                                ton (Tillet et al., 2019) are simpler to use, but sacrifice
While AI has largely used a single hardware vendor in the       performance and struggle to quickly support new hardware
past (Krizhevsky et al., 2012; AI@Meta, 2024; OpenAI            features (Spector et al., 2024; Triton, 2025). AI-designed
et al., 2025), AMD GPU hardware now offer state-of-the-art      kernels are showing early promise (Baronio et al., 2025;
peak compute and memory bandwidth (Table 2). However,           Lange et al., 2025), but current models also struggle to use
the lack of mature software support creates a hardware lot-     new hardware features (Ouyang et al., 2025) and are sus-
tery (“CUDA moat”) (Sara Hooker, 2021; SemiAnalysis,            ceptible to reward hacking (Baronio et al., 2025). Recently,
2024). Peak-performance AMD kernels are written in raw          lightweight C++ embedded DSLs like T HUNDER K ITTENS
assembly by a handful of experts (i.e., the AITER library       (TK) (and successors like CuTe DSL (NVIDIA, 2025b) and
(AMD, 2025a)), which is slow to scale to the breadth of AI      Gluon (Zhou et al., 2025)) consider simplifying develop-
workloads. For instance, AITER and PyTorch Llama GQA            ment by encoding kernel design in terms of a small set of
backwards achieve just 30% and 24% of SoTA performance          opinionated primitives that give the developer full control:
respectively on AMD MI355X GPUs (Section 4).
                                                                1. Tiles. The basic data type is a tile with optimized mem-
Developing NVIDIA kernels also required painstaking ef-            ory access patterns. TK exposes lightweight PyTorch-
fort a few years ago. For instance, using low level CUD-           inspired bulk compute operators (mma, exp, etc.) over
A/CUTLASS, it took two years between the H100 GPU’s                tiles, wrapping PTX. Tiles help developers explicitly
release and the release of peak performance open-source            manage data at each level of the GPU memory hierarchy.
   1
     Department of Computer Science, Stanford University        2. Overlapping. A few basic kernel patterns help develop-
2
 Advanced Micro Devices, Inc. 3 University of California, San      ers achieve high occupancy, or schedule workers (waves
Diego. Correspondence to: William Hu <willhu@stanford.edu>,        on AMD, warps on NVIDIA) onto the different hardware
Simran Arora <simarora@stanford.edu>.
                                                                   execution units. Modern NVIDIA kernels have consoli-
Proceedings of the 9 th MLSys Conference, Bellevue, WA, USA,       dated around wave specialization (producer-consumer)
2026. Copyright 2026 by the author(s).                             scheduling patterns (Shah et al., 2024; Spector et al.,
                                                                   2024; 2025; Zhang et al., 2025; Wang et al., 2025).
                                           H IP K ITTENS: Fast and Furious AMD Kernels




Figure 1. We study whether existing tile based programming primitives suffice for AMD kernels, or whether entirely new primitives are
needed. Our study led to H IP K ITTENS: a minimal and opinionated set of primitives for fast and furious AMD kernels. HK introduces a
general 8- WAVE PING - PONG SCHEDULE to overlap compute and memory, programmer controlled register allocation, and efficient shared
memory and chiplet-aware swizzling algorithms to enable a suite of high performance AMD AI kernels. In the profiler visualization, each
row corresponds to a wave and two consecutive waves occupy the same SIMD. While one wave executes memory instructions, the other
executes compute instructions.

3. Grid scheduling. By assigning work to thread blocks                   instruction shapes are all built from the same underlying
   in the appropriate order, a developer can maximize the                core matrix structure, which makes it easy to use a single
   reuse of non-programmable cache memory.                               tile swizzle strategy for all shapes as in TK and Linear
                                                                         Layouts (Zhou et al., 2025). AMD matrix instructions
Our work asks whether entirely new programming primitives                lack this compositional structure, leading to an explosion
are needed to simplify AMD kernel development, or whether                in the number of tile layouts. Further, the shared memory
existing primitives suffice. Ideally, we would have a simple             bank structure and order in which threads in a wave
framework that helps developers write a breadth of high                  run differ based on the memory instruction on AMD
performance kernels. Our exploration led to H IP K ITTENS                (Sec. 3.2). HK handles this complexity for the developer.
(HK), a minimal collection of C++ embedded programming
primitives for AMD:                                                   2. Schedules to overlap compute and memory. Ideally
                                                                         we would have simple, reusable patterns for scheduling
1. Optimized access patterns for programmable GPU                        the compute and memory within kernels that general-
   memory. Careful register memory management is crit-                   ize across AI workloads. The wave specialization pat-
   ical for peak performance kernels. HK retains the tile                tern dominates NVIDIA kernels and DSLs; producer
   data structures of prior DSLs to help developers manage               waves handle memory operations while consumers exe-
   memory (Spector et al., 2024). However, optimizing the                cute bulk compute operations over large tiles. However,
   tiles for AMD introduces new challenges.                              we find that this pattern underperforms on AMD CDNA3
                                                                         and CDNA4 GPUs due to architectural differences—
   Compilers such as Triton and HIPCC routinely impact                   AMD’s static register allocation means that producer
   the kernel developer’s ability to carefully schedule reg-             waves consume registers without contributing to compu-
   ister allocations and lifetimes (Sec. 3.2). For instance,             tation, which limits the output tile size that gets computed
   HIPCC prevents the HIP developer from using certain                   per thread block and thus the kernel’s arithmetic intensity.
   types of registers (AGPRs) as input operands to matrix                On the MI355X, wave specialization achieves just 80%
   instructions. 1 We thus introduce a way for the developer             of peak BF16 GEMM performance (Tab. 2).
   to bypass the compiler altogether, and explicitly pin the
                                                                         We identify two alternate scheduling patterns that consis-
   registers belonging to each tile.
                                                                         tently achieve peak AMD performance: 8- WAVE PING -
   As for memory access patterns, different NVIDIA matrix                PONG and 4- WAVE INTERLEAVE , which tradeoff pro-

    1
                                                                         grammability with performance (Tab. 3). The 8- WAVE
      AMD CDNA includes 512 registers per SIMD, which are                pattern employs two waves per SIMD that alternate be-
evenly partitioned across waves co-located on the SIMD. For ker-
nels with a single SIMD per wave, the hardware splits the registers      tween compute and memory roles, each executing large
into 256 vector general-purpose registers (VGPRs) and 256 accu-          bulk tile operations (Fig. 1), while the 4- WAVE pattern
mulator registers (AGPRs).                                               employs one wave per SIMD and finely interleaves in-
                                        H IP K ITTENS: Fast and Furious AMD Kernels

  S PEC                        NVIDIA B200 SXM5          AMD MI355X OAM
  BF16 MATRIX / TENSOR              2.2 PFLOP S               2.5 PFLOP S
  MXFP8 MATRIX / TENSOR             4.5 PFLOP S               5.0 PFLOP S
  MXFP6 MATRIX / TENSOR             4.5 PFLOP S              10.1 PFLOP S
  MXFP4 MATRIX / TENSOR             9.0 PFLOP S              10.1 PFLOP S
  M EMORY CAPACITY                    180 GB                     288 GB
  M EMORY BANDWIDTH                   8.0 TB/ S                  8.0 TB/ S

Figure 2. Hardware overview. (Left) Peak memory and compute speeds for the latest generation GPU platforms (AMD, 2025b; NVIDIA,
2025a). (Right) Diagram of the AMD GPU software and hardware hierarchy.

   structions over small tile sizes. Remarkably, the sim-          tile primitives proposed in the TK DSL transfer to AMD,
   ple 8- WAVE pattern is sufficient to match AMD’s hand-          providing evidence that one unified and performant pro-
   optimized assembly kernels across BF16 GEMM, FP8                gramming model is possible across AI accelerators. Scaling
   GEMM, and attention forward, and outperforms base-              kernel support across multiple silicon platforms is key to
   lines by 1.8↑ on GQA non-causal backward.                       unlocking the “compute capacity needed to achieve AI’s full
                                                                   potential” (OpenAI, 2025). We hope this work helps open
3. Optimized access patterns for non-programmable
                                                                   AI’s hardware landscape.
   GPU memory. Chiplet architectures are becoming the
   dominant path to GPU scaling—NVIDIA Blackwell uses              2     BACKGROUND
   2 chips, AMD MI355X uses 8—but existing frameworks
   ignore their hierarchical cache structures, leaving perfor-     In this section, we provide background on AMD GPU hard-
   mance untapped. Each AMD CDNA4 chiplet contains 32              ware in Section 2.1 and discuss related work in Section 2.2.
   processors with private L2 cache, while all chiplets share      Section B provides an extended discussion of related work.
   a last-level cache (LLC) between L2 and global memory.
   These hierarchical caches have orthogonal preferences           2.1   GPU fundamentals
   for how work is parallelized across thread blocks. Table 4      GPU kernels are small programs that load data, perform
   demonstrates an instance where a naive row-major order-         work on it, and write the results back out to memory. We
   ing for allocating work to thread blocks achieves only a        generally adopt AMD terminology in this work and pro-
   36% L2 hit rate for BF16 GEMMs. We show that opti-              vide a mapping between AMD and NVIDIA terminology in
   mizing solely for L2 reuse (e.g., to 79% hit rate) degrades     Appendix A.
   LLC performance and overall bandwidth. HK introduces
   an algorithm that models both cache levels when schedul-        1. Compute hierarchy. Kernels are executed by tens
   ing thread blocks. This improves performance by 19%                of thousands of threads across hundreds of processors,
   over the naive row-major baseline (Tab. 4).                        called “compute units” (CUs). CUs organize their hard-
                                                                      ware resources in 4 “single instruction, multiple data”
Evaluation. We validate H IP K ITTENS on AMD CDNA3                    (SIMD) units. Threads are arranged in a hierarchy:
MI325X and CDNA4 MI355X GPUs. On the most widely                      threads are the smallest units of execution, “waves”,
used and optimized workloads in AI, HK competes with                  groups of 64 threads, execute in lockstep on individ-
or outperforms all AMD baselines (BF16 FP8 GEMM,                      ual SIMDs, and “thread blocks”, groups of waves, are
GQA/MHA Attention forward and backward, RoPE, Lay-                    jointly scheduled on the CUs. AMD MI355X GPUs con-
erNorm). HK outperforms all available AMD baselines on                tain 256 CUs organized into 8 accelerator complex dies
average, including AMD’s hand-optimized kernels that have             (XCDs) of 32 CUs in a chiplet layout.
been written in raw assembly. However, assembly is not a
scalable method for kernel development and leaves many im-         2. Memory hierarchy. Memory is organized in a hierarchy:
portant AI workloads unsupported — in such settings (e.g.,            small amounts of quick-access and large amounts of
some attention shapes, GQA backwards, memory bound                    slow-access memory. A single SIMD contains 512 32-
kernels), HK outperforms the available AMD baselines by               bit vector registers (for a total of 512KB per CU). Each
1.2 → 10↑. Furthermore, HK consistently outperforms com-              CU has an L1 cache and shared memory that can be
piler approaches (e.g., by up to 3↑ the Triton BF16 GEMM              accessed by multiple waves in the same thread block.
and 2↑ the Mojo MHA forwards).                                        Each XCD shares a non-programmable 4MB L2 cache.
                                                                      All CUs share a large, slow global memory (HBM) and
We contribute (1) principles for writing high performance
                                                                      a last level cache (LLC) sits between L2 and HBM.
AMD kernels, (2) HK, a collection of opinionated C++
programming primitives for the AI community, and (3) a             3. Occupancy. Threads execute instructions on physical
suite of performant AMD kernels. We further show that the             execution units (ALU, FMA, matrix cores), which are
                                          H IP K ITTENS: Fast and Furious AMD Kernels

      specialized for different types of compute. Instructions    3     H IP K ITTENS
      performed by these units each have a fixed issue latency
      and limited amount of bandwidth. Different waves can        This section describes H IP K ITTENS (HK), a framework for
      occupy different units simultaneously to avoid saturating   programming AI kernels on AMD GPUs. HK builds on
      any single unit. Each unit imposes different constraints    the T HUNDER K ITTENS framework (Spector et al., 2024),
      on the memory layouts, or the mapping of logical data       which uses C++ embedded tile-based programming prim-
      elements to physical thread ownership (AMD, 2025a).         itives to simplify high-performance and flexible AI ker-
                                                                  nel development (discussed in Section 3.1). We describe
                                                                  HK’s principles for optimizing the access patterns of pro-
Software overview. Developers can write kernels at dif-           grammable GPU memory in Section 3.2, maximizing occu-
ferent levels of the software stack. Raw assembly provides        pancy in Section 3.3, and optimizing the access patterns of
maximal control over register usage and instruction selec-        non-programmable cache memory in Section 3.4.
tion and ordering. CUDA / HIP C++ gets compiled (via              3.1     Tile programming interface
NVCC, HIPCC) to assembly, and the compiler may intro-
duce its own instruction reordering and register lifetime         Like existing kernel frameworks, HK adopts tiles as the ba-
tracking. LLVM accepts compiler hints, which let the devel-       sic data structure and provides a suite of optimized operators
oper guide the compiler’s behavior. Some compilers expose         over tiles. The tile design and suite of operators is heavily
high level interfaces on top of C++ (e.g., Python (Paszke         inspired by PyTorch and NumPy (Paszke et al., 2019; Harris
et al., 2019), Triton (Tillet et al., 2019)).                     et al., 2020), given their familiarity to the AI community.
2.2       Related work                                            • Memory. The developer can initialize tiles in register
                                                                    or shared memory. A tile is parametrized by a dtype
Currently, peak-performance AMD kernels finely interleave           (FP32, BF16, FP16, FP8, FP6), rows, columns and a
compute and memory instruction issues in raw assembly               layout (row or column major). Tile rows and columns
(see the AITER and Composable Kernel libraries) (AMD,               are restricted to be a multiple of the matrix core shape.
2025a;b). In contrast, to simplify and accelerate the kernel        HK provides operators to load and store tiles across
development process, the AI community recently adopted              different levels of the GPU memory hierarchy.
bulk programming operators over optimized tile primitives
for kernel development, as proposed in T HUNDER K IT-             • Compute. HK provides a suite of bulk compute operators
TENS (Spector et al., 2024) 2 and successors (e.g., CuTe
                                                                    over tiles, inspired by the set of operators in PyTorch (e.g.,
DSL (NVIDIA, 2025b)3 , Gluon (Zhou et al., 2025)4 ). How-           mma, exp, add). The functions are lightweight and do
ever, these existing C++ based DSLs only run on NVIDIA              not add overhead as they directly wrap raw AMD CDNA
GPUs, wrapping PTX and CUDA. Compiler libraries such                assembly/HIP (NVIDIA PTX/CUDA for TK).
as Triton, TileLang, and Mojo are built on top of LLVM/M-         Given these familiar programming primitives, HK auto-
LIR (Lattner et al., 2020; Lattner & Adve, 2004; LLVM             matically optimizes the memory access patterns for tiles.
Compiler Infrastructure, 2025) and can compile for AMD            Memory management on AMD GPUs raises key challenges
GPUs. However, these works have neither provided reusable         at each level of the hierarchy, discussed next.
principles or primitives for AMD, nor released comprehen-
sive suites of high performance AMD kernels. For instance,        3.2     Optimizing programmable memory access
Mojo’s MHA kernel suffers from expensive bank conflicts
and achieves just 50% of the peak kernels’ performance on         We now discuss the specifics of H IP K ITTENS tiles.
the MI355X. 5 . H IP K ITTENS provides the first systematic       3.2.1    Developer-controlled register scheduling
set of AMD AI kernel primitives towards opening up the
hardware landscape.                                               Careful register management is critical for high performance.
      2
       https://github.com/HazyResearch/                           However, compilers either prevent (e.g., Triton) or impede
ThunderKittens (May 2024)                                         (e.g., in the HIPCC compiler) the developer’s ability to
     3
       https://docs.nvidia.com/cutlass/media/                     maximally control register allocations.
 docs/pythonDSL/cute_dsl.html (Sept 2025)
     4
       https://github.com/triton-lang/triton/                     For instance, in kernels with a single wave per SIMD, the
 tree/main/python/tutorials/gluon (June 2025)                     AMD hardware splits the SIMD’s 512 registers into 256
     5
       Measured        using          rocprofv3 --pmc             vector general-purpose registers (VGPRs) and 256 accu-
 SQ LDS BANK CONFLICT,SQ INSTS LDS                                mulator registers (AGPRs). However, while the hardware
--output-format csv --output-file profiles 3                      does support using AGPRs as input to matrix core instruc-
-d out -- mojo bench mha.mojo             at   https:
//github.com/modular/modular/tree/main/                           tions, HIPCC does not. For workloads that involve both
 max/kernels/benchmarks/gpu in the nightly Modular                matrix and vector operations (e.g., attention backwards),
 build on a MI355X GPU on 11/06/2025.                             kernels compiled via HIPCC would need to generate redun-
                                             H IP K ITTENS: Fast and Furious AMD Kernels




Figure 3. Swizzle pattern for a 16x32 tile of BF16s. Shared memory on AMD CDNA4 GPUs have different banking behavior depending
on the instruction. ds read b128 accesses shared memory through 64 banks, each 32-bits wide, and correspond the individual cells and
numbers in the figure. The shaded cells represent banks that are accessed by the first phase of a ds read b128 instruction for a 16x32
row layout register tile. On the left is an unswizzled layout suffering from 2-way bank conflicts. On the right is a swizzled layout with
no bank conflicts. The swizzle applied here swaps the first 8 columns with the last 8 starting from the 8th row. This swizzling strategy
simultaneously enables bank-conflict free accesses from column-major reads using ds read b64 tr b16. Details can be found in D.1.

dant v_accvgpr_read instructions that move data from                     result in bank conflicts if multiple threads in a wave at-
AGPRs to VGPRs prior to issuing matrix instructions.                     tempt to access the same bank simultaneously. Waves (and
                                                                         NVIDIA warps) execute shared memory accesses in phases;
Table 1. E XPLICIT REGISTER SCHEDULING enables increased
                                                                         i.e., a subset of threads in a wave accesses shared memory
developer control. A 4-wave MHA non-causal backwards kernel
implemented in HIP underperforms AMD’s raw assembly kernel
                                                                         concurrently (Forum, 2023).
(AITER) due to compiler limitations. We match AITER by by-               The complexity of AMD matrix layouts relative to NVIDIA
passing the compiler and pinning register tiles to explicit registers.   layouts impacts the access patterns at each level of the GPU
We use batch size 16, heads 16 and head dim 128.                         memory hierarchy. First, NVIDIA matrix instructions use
                                                                         a regular pattern (Fig. 4a); all shapes are composed from
  M ETHOD                              S EQ . L ENGTH     TFLOPS         an underlying 16 ↑ 16 core matrix building block that is
  HK                                        4096             855         stamped out multiple times depending on the overall matrix
  HK WITH P INNED R EGISTERS                4096             1024        instruction shape. Thus, prior frameworks like TK (Spector
  AMD A SSEMBLY (AITER)                     4096             1018        et al., 2024) and Linear layouts (Zhou et al., 2025), can use
  HK                                        8192             909         a unified swizzling strategy that generalizes across matrix
  HK WITH P INNED R EGISTERS                8192             1091        shapes. Meanwhile, each AMD matrix instruction uses
  AMD A SSEMBLY (AITER)                     8192             1169        an entirely different layout without a similar underlying
                                                                         structure.
E XPLICIT REGISTER SCHEDULING. The compiler con-
straints motivate a feature in HK that gives developers the
ability to fully control register scheduling. The developer
pins the registers belonging to each tile, rather than letting
HIPCC manage the registers. By bypassing the compiler,
the developer can use AGPRs as inputs to matrix instruc-                    (a) NVIDIA         (b) AMD 16→16→32            (c) AMD
tions, resulting in our SoTA-level backwards attention ker-                      core           MFMA A/B matrix          16→16→32
nel (Tab. 1). The interface for programming with pinned                        matrices                                   C/D matrix
register tiles exactly matches that of using standard compiler-          Figure 4. Matrix layouts on NVIDIA and AMD GPUs. The shaded
managed register tiles. We leave both options so developers              cells in each matrix represent elements owned by thread 0.
can choose the level of control they want.
                                                                         Second, NVIDIA instructions sequentially assign threads to
3.2.2    Tiles for heterogeneous matrix core shapes                      phases (e.g., threads 0-7 in phase one, 8-15 in phase two).
AI kernels use different matrix core instruction shapes                  Meanwhile on AMD, the phases are non-sequential and
(MxNxK), depending on the workload properties, in or-                    differ based on the memory instruction. 6
der to carefully manage register pressure. However, it is                   6
                                                                              For e.g., the threads in a wave execute the ds read b128
challenging to use multiple shapes on AMD GPUs.                          instruction in 4 phases and load data from 64 shared memory
                                                                         banks, each 4-bytes wide, while a ds read b96 executes over 8
Matrix layout complexity. Recall that GPU matrix in-                     phases and loads from 32 banks. The phases are undocumented
structions impose rules as to which thread owns each data                in the CDNA ISA so we create a solver to determine them, and
element in its registers. Further, shared memory accesses                document the phases in Tab. 7.
                                         H IP K ITTENS: Fast and Furious AMD Kernels

Optimized tile memory. We discuss how HK abstracts                   #P/#C           MFMA S HAPE          O UTPUT       TFLOPS
away this complexity from the AMD kernel developer:                  HK 4 / 8         16 → 16 → 32       128 → 256        893
1. Register. By default, register tiles in HK default to             HK 4 / 12        16 → 16 → 32       192 → 256        1278
   using the smallest MFMA instruction since this provides           HK 0 / 8         16 → 16 → 32       192 → 256        1281
   maximal scheduling control as highlighted in Section 3.3.         HK 0 / 8         16 → 16 → 32       256 → 256        1610
   However, for the edge case kernels that use alternate             TK             256 → 256 → 16       256 → 256        1538
   sizes, HK lets the developer parameterize the desired             CUTLASS        256 → 256 → 16       256 → 256        1570
   register tile by the MFMA instruction shape.
                                                                  Table 2. Producer consumer comparisons. We report results for
2. Shared. On AMD GPUs, it is not possible to use a single        a series of producer consumer BF16 GEMM kernels of shape
   swizzle pattern for all layouts (a simple counter-example      M = N = K = 8192. We denote the number of producers
                                                                  and consumers as P and C respectively. We denote the underlying
   is provided in Appendix D.1). While we could imple-
                                                                  matrix instruction size, output tile size computed per thread block,
   ment unique swizzle patterns for every matrix layout, this     and TFLOPS measured (500 iterations of warmup, 100 iterations
   adds code complexity. Instead, we identify the layouts         of measurement on inputs from N (0, 1)). AMD kernels run on an
   that commonly co-occur and support swizzle patterns            MI355X and NVIDIA kernels (TK, CUTLASS) on a B200.
   that are bank conflict free for these instances. Figure 3
   shows one such swizzle that is bank conflict free for both     3.3.1   Wave specialization underperforms on AMD
   the 16 ↑ 32 row layout and column layout load.
                                                                  NVIDIA kernels implement wave specialization using ded-
3. Global. AMD GPUs support direct asynchronous HBM               icated memory access hardware (tma), asynchronous ma-
   to shared memory loads. Like TMA, these loads bypass           trix multiplies which accept operands directly from shared
   the register file. The instruction takes as input per-thread   or tensor memory (wgmma, tcgnen05), deep pipelines en-
   addresses in HBM from which each thread will read data.        abled by large shared memory per processor (B200 has 40%
   While DSLs like TK directly swizzle the shared memory          larger SRAM than AMD MI355X per processor), regis-
   addresses, swizzling shared memory on AMD is instead           ter reallocation (where the register-efficiency of TMA lets
   accomplished by swizzling on the HBM addresses.                producers give their registers to consumers), and hardware
                                                                  synchronization primitives (mbarriers). AMD lacks these
3.3   Overlapping compute and memory utilization                  architectural features, changing the kernel design space.
We study the principles for scheduling instructions within        To evaluate how these differences impact performance, we
AMD AI kernels and identify two high-performance patterns         vary the synchronization mechanism, pipeline depth, and
that lead to peak utilization across diverse workloads.           producer-consumer ratios (Tab. 2). Our experiments reveal
Current approaches and their limitations. State-of-the-           two principles. We need to maximize the output tile size
art AI kernels and DSLs have consolidated around wave             computed per thread block to increase the arithmetic inten-
specialization—a pattern where specialized producer waves         sity (operations per byte moved), and we need to maximize
handle memory movement while consumer waves handle                the pipeline depth to hide the latency of memory loads.
computation. This approach dominates in NVIDIA imple-             Peak performance open-source TK and CUTLASS profiler-
mentations including FlashAttention-3 (Shah et al., 2024),        selected GEMMs use wave specialization and an output
COMET for MoE (Zhang et al., 2025), and GEMMs (Bik-               tile size of 256 ↑ 256 on the B200. 7 Our best AMD
shandi & Shah, 2023), and kernel DSLs like TK (Spector            GEMM achieves comparable performance when computing
et al., 2024) and TileLang (Wang et al., 2025). In this           a 256 ↑ 256 output tile per thread block only when using
paradigm, waves occupy specific hardware units for long           no wave specialization (i.e., zero producers) and degrades
periods of time, so they can issue bulk operations over large     as the number of producers increases (Tab. 2). This is be-
tile primitives. This tile-based programming makes the code       cause AMD hardware statically divides registers across all
size compact and readable.                                        waves (AMD, 2025), meaning producers consume registers
However, wave specialization struggles to generalize              without contributing to output computation. This limits the
to modern AMD devices due to fundamental architec-                usable output tile size when using wave specialization.
tural differences. Instead, state-of-the-art AMD kernels
(AITER (AMD, 2025a), CK (AMD, 2025b)) resort to raw as-           Tradeoffs. NVIDIA’s larger shared memory enables the
sembly to finely interleave instruction issues—an approach        use of deep pipelines while using large matrix instruction
orthogonal to tile-based programming. While it might seem         shapes (e.g., 256 ↑ 256 ↑ 16). However, AMD’s smaller
that AMD requires bespoke schedules for each AI work-             tensor core shapes (e.g., 16 ↑ 16↑ 32) provide an alternative
load, we identify simple general principles that achieve high        7
                                                                      The profiler sweeps and tunes the suite of CUTLASS GEMMs,
performance across diverse applications.                          selecting the best one for the shape and dtype.
                                            H IP K ITTENS: Fast and Furious AMD Kernels

Table 3. Scheduling patterns for AMD. We identify two primary             This pattern excels when compute and memory durations
paradigms—8- WAVE and 4- WAVE—that generalize across work-                are roughly balanced. A SIMD’s compute wave executes
loads. Both patterns can leverage HK’s tile primitives. We report         matrix fused multiply add (MFMA) instructions while
the hot loop code size and TFLOPs, showing how these patterns             its paired memory wave prefetches the next data, hiding
trade off programmability and performance.                                memory effectively.
     K ERNEL                 PATTERN       LOC     TFLOPS              2. 4- WAVE INTERLEAVE (imbalanced workloads). This
     FP8 GEMM                 8- WAVE      48         3222                pattern places exactly one wave on each of the proces-
     FP8 GEMM                 4- WAVE      183        3327                sor’s four SIMDs. Each wave issues both compute and
     MHA BACKWARDS            8- WAVE      331         894
                                                                          memory instructions in a carefully staggered sequence
     MHA BACKWARDS            4- WAVE      989        1091                to maximize occupancy of the hardware units.
                                                                          This fine-grained pattern better saturates both MFMA
path to establish deep pipelines by using finer-granularity               and LDS pipelines when workloads are imbalanced (ei-
load and compute stages.                                                  ther compute-heavy or memory-heavy). The wave per
                                                                          SIMD can adapt its instruction mix dynamically.
NVIDIA’s matrix multiply instructions, which accept
operands from shared or tensor memory, helps alleviate                 These schedules tradeoff programmability and performance.
register pressure, and it may be surprising that AMD can               HK lets developers use tile-based primitives to implement
match performance without this. However, AMD devices                   either of these patterns, albeit at different tile granulari-
have a 2↑ larger register file to compensate.                          ties. 8- WAVE PING - PONG allows for large tile primitives
                                                                       similar to the ones used in wave specialization. On the
We further validate that using shared memory atomics in-               other hand, 4- WAVE INTERLEAVE requires developers to
stead of mbarriers adds negligible overhead; we find the               program with small base tile primitives, extending the code
192 ↑ 256 producer consumer kernel, which uses atomics,                size due to finer grained instruction issues. This tradeoff is
and non-wave specialized kernel, which is atomic-free, per-            captured in Table 3. Surprisingly, we find that 8- WAVE is suf-
form similarly, emphasizing that the output tile shape is the          ficient to match or outperform AMD’s raw assembly kernels
dominant factor impacting performance (Tab. 2).                        across BF16 GEMM, FP8 GEMM, and attention forwards
3.3.2   Performant scheduling patterns for AMD AI kernels              workloads. On GQA non-causal attention backwards, our
                                                                       8- WAVE kernel outperforms the baselines (PyTorch SDPA,
AMD GPUs have four SIMD units per CU, and waves sched-                 CK, and AITER) by 1.8↑, and our 4- WAVE kernel delivers
uled on the same SIMD can overlap compute and memory                   an even larger 2.3↑ speedup.
instructions. We identify two scheduling patterns that con-
sistently achieve peak performance across AI workloads by              3.4   Optimizing the access patterns of
exploiting this parallelism differently:                                     non-programmable GPU memory
1. 8- WAVE PING - PONG (balanced workloads). This pat-                 Modern GPUs—AMD and NVIDIA—are moving towards
   tern employs eight waves per thread block—two resident              chiplet, rather than monolithic, architectures (e.g., Black-
   per SIMD. The waves are split into two groups of four,              well is comprised of two chips). This results in a disaggre-
   with each group containing one wave per SIMD. Within                gated cache hierarchy, where distinct clusters of processors
   each SIMD, the two waves alternate the type of work                 are attached to distinct slices of the GPU cache (see Fig-
   each does: one issues only compute instructions while               ure 2). Here, we explore principles for disaggregated cache
   the other issues only memory instructions, and then they            scheduling and introduce HK’s algorithm for cache reuse.
   swap roles, flipping back and forth between compute and
   memory as shown in Figure 1. A conditional barrier such             Cost model. AMD devices use two types of caches – L2
   as the one in Figure 5 controls the alternation.                    and LLC – where cache misses have a worst case miss
                                                                       penalty of 300ns for the L2 cache and 500ns for the LLC
if (kittens::warpid() / 4 == 1) {
                                                                       cache. AMD devices assign clusters of 32 (CDNA4) or 38
    __builtin_amdgcn_s_barrier();                                      (CDNA3) compute units to a cluster (accelerated complex
}                                                                      die, or XCD), and include 8 clusters per GPU. The hardware
                                                                       scheduler assigns thread blocks to XCDs in round-robin
Figure 5. Conditional Barrier. This control-flow primitive intro-      order. The grid schedule, or order of work assigned to thread
duces the alternating pattern of work between waves resident on        blocks, impacts the cache reuse and achieved bandwidth:
the same SIMD. In 8- WAVE PING - PONG, the conditional barrier
causes half of the waves - one on each of the four SIMDs - to stall
temporarily so that, at any given point in time, they are performing          Bandwidth = LLC Bandwidth ↑ LLC Hit %
different work than the waves that were left unimpeded.                                                                           (1)
                                                                                               +L2 Bandwidth ↑ L2 Hit %
                                         H IP K ITTENS: Fast and Furious AMD Kernels

Algorithm 1 XCD swizzle for cache reuse on GEMMs
Require: grid block indices (b.x, b.y, b.z); grid dimensions (g.x, g.y, g.z); number of XCDs nXCD; problem sizes M, N
    with tile sizes BLOCKM , BLOCKN ; window height W , chunk size C
Ensure: remapped block indices (b.x→ , b.y → , b.z)
 1: blocks ↓ g.x ↑ g.y                                                                  ω blocks per batch (a single b.z slice)
 2: xy ↓ b.x + g.x ↑ b.y                                                                  ω flatten (b.x, b.y) within the batch
 3: blocks per   !   cycle   ↓  nXCD ↑  C"
                         blocks
 4: limit ↓                                ↑ blocks per cycle                       ω largest full (nXCD↑C)-aligned prefix
                   blocks per cycle
 5: if xy > limit then
 6:     xy ↓ xy                                                                          ω tail region: leave order unchanged
 7: else
 8:     xcd ↓ xy #mod nXCD                                                ω which XCD this block belongs to (round-robin)
                       xy $
 9:     local ↓                                                                  ω local index after de-interleaving by XCD
                      nXCD
                         !         "
                           local
10:     chunk idx ↓
                               C
11:     pos ↓ local mod C
12:     xy ↓ chunk idx ↑ blocks per cycle + xcd ↑ C + pos
13: end if
14: num rows ↓ BLOCK   M
                          M
                                                                                                           ω tile rows along M
15: num cols ↓ BLOCKN  N
                                                                                                             ω tile cols along N
16: tid per group ↓ W ↑ num cols                                               ω one window (height W ) across all columns
                          xy
17: group id ↓ tid per       group                                                                    ω which window of rows
18: first row ↓ group id ↑ W
19: win h ↓ min(num rows → first row, W )                                                            ω tail-safe window height
20: ε ↓ xy mod tid per group                                                                 ω local index within the window
21: row ↓ first row + (ε mod win h)                                                 ω fast index: go down within the column
22: col ↓ win   ω
                  h                                                  ω slow  index:  move to next column after win h rows
23: return (row, col, b.z)                                                                 ω logical tile coordinates (+ batch)


In a GEMM kernel (D = AB + C), each thread block                     other words, multiple XCDs should work on nearby or
computes a distinct tile of the output matrix D. When                identical regions of the input matrices, so that shared
thread blocks are scheduled in naı̈ve row-major order, cache         data remains resident in the LLC.
reuse is suboptimal (↔55%) because blocks that share the
same L2 cache often load different, non-overlapping tiles of      By jointly optimizing these two principles, we can raise both
A and B. Thus, their memory accesses fail to exploit spatial      L2 and LLC hit rates, leading to higher effective bandwidth
locality, leading to redundant data movement. This behavior       (Figure 6c, Table 4, Row 3). For instance, Table 4 shows
is illustrated in Fig. 6a and Tab. 4 (Row 1). To mitigate this,   that an L2/LLC-aware schedule achieves up to 15% higher
we use two key principles to improve cache reuse:                 performance than the default grid order. The benefit is
                                                                  particularly pronounced when the output matrix width (in
1. L2 Reuse. Thread blocks mapped to the same XCD (and            tiles) is coprime with the number of XCDs—for example,
   thus sharing an L2 cache) should cover a rectangular           57 tiles across 8 XCDs on an AMD MI355X—since the
   region of the output matrix—an “L2 tile.” This layout          default schedule causes worst case reuse patterns (Tab. 4).
   ensures that consecutive blocks reuse both the same rows
   of A and the same columns of B. However, optimizing            H IP K ITTENS chiplet swizzling algorithm. To make
   purely for L2 locality can cause each XCD to fetch dis-        cache-aware scheduling accessible to developers, H IP K IT-
   joint portions of A and B, leading to redundant loads at       TENS provides a simple and tunable strategy for maximizing
   the next cache level.                                          cache reuse across a wide range of GEMM problem sizes.
2. LLC Reuse. To further improve reuse at the last-level          Algorithm 1 implements this strategy in two steps:
   cache (LLC), we must coordinate accesses across XCDs.
   Ideally, the combined access footprint of all XCDs—            1. XCD grouping. Flatten the 2D-grid into a linear se-
   the “LLC tile”—should overlap in both A and B. In                 quence and remap block ID’s such that chunks of C
                                           H IP K ITTENS: Fast and Furious AMD Kernels




    (a) Row-major     (b) XCD (W7, C216) (c) XCD (W5, C25)

 B LOCK O RDER         L2 % LLC % M EM . BW TFLOPS
    M ATRIX M ULTIPLY (M=N=K=9216, MT 192 X 256 X 64)
 ROW- MAJOR            55%      95%      15.1 TB/ S      1113
 XCD (W 7/C216)        79%      24%      14.9 TB/ S      991
 XCD (W 5/C25)         75%      93%      18.3 TB/ S      1145
 M ATRIX M ULTIPLY (M=N=K=14592, MT 192 X 256 X 64)
 ROW- MAJOR            36%      76%      10.7 TB/ S       900
 XCD (W 8/C542)        79%      7%       13.9 TB/ S      980
 XCD W 8/C64           78%      55%      16.6 TB/ S      1068

Table 4. Chiplet swizzling for cache reuse. Visualization of
three different grid schedules for the output matrix of a M =
N = K = 9216 BF16 GEMM. The color represents the XCD
assignment for the first set of thread blocks scheduled across the
GPU (256 CUs). Schedule 6a (Table Row 1) assigns blocks to
the grid according to block ID. Schedules 6b (Table Row 2) and
6c (Table Row 3) apply Algorithm 1 with different window and
chunk size parameters. Table 4 shows how these schedules trade       Figure 7. GEMM. We compare HK BF16 and FP8 GEMMs to the
off L2 and LLC reuse to gain performance. Figure 19a provides        strongest available baselines.
the corresponding visualization for the 14592 shape.
                                                                     AITER (AMD, 2025a), Composable Kernel (AMD,
     consecutive IDs are resident on the same XCD. This              2025b), ROCm Library Triton (AMD, 2025), and Hip-
     reduces cross-chiplet traffic.                                  BLASLT (AMD, 2025). We evaluate on both MI325
2. Hierarchical windowed traversal. Instead of process-              CDNA3 and MI355 CDNA4. We benchmark HK kernels
   ing the grid row by row, we process it in vertical win-           in Python scripts using Python bindings (except FP8 where
   dows of height W . This has the effect of “folding” the           AMD PyTorch support remains experimental). For each
   input block ID space into rectangular tiles, optimizing           kernel, we use 500 warmup runs and report the average
   L2 cache reuse.                                                   TFLOPs/s performance over 100 runs over randomly gener-
                                                                     ated input tensors from the standard normal distribution. All
The two parameters, W and C, control the trade-off be-               kernels are benchmarked in AMD’s recently released beta
tween L2 and LLC reuse. Since L2 bandwidth is roughly                Docker using ROCm 7.0 (rocm/7.0-preview:rocm7.
3↑ higher than LLC bandwidth, W should be chosen to                  0_preview_pytorch_training_mi35x_beta).
maximize L2 hit rate. On AMD MI355X, each XCD con-                   HK provides a comprehensive suite of peak-performance
tains 32 CUs, and empirical results show that L2 tiles of            AMD AI kernels, written using reusable tile-based abstrac-
shape 8 ↑ 4 or 4 ↑ 8 achieve the best hardware utilization.          tions. We also include code listings in Appendix E and
Tuning the chunk size C further improves LLC efficiency              additional results in Appendix C:
by coordinating access patterns across XCDs so that they
operate on similar rows of the input matrix.                         1. BF16 and FP8 GEMM. HK competes with the AMD
                                                                        baseline kernels that are written in assembly (AITER,
4     E XPERIMENTS                                                      HipBLASLT/PyTorch). HK outperforms the Triton com-
                                                                        piler by 1.3 → 3.0↑. Further, we obtain these results
In this section, we validate that HK enables peak perfor-               using a single 8-wave kernel schedule that generalizes
mance kernels, using simple and reusable tile-based primi-              across the evaluated problem shapes.
tives, across a breadth of AI operations.
                                                                     2. Attention forwards. We evaluate multi-head atten-
Baselines. We compare to the best performing base-                      tion (MHA) and group-query attention (GQA) kernels
line kernels across PyTorch (compiled and SDPA),                        in causal and non-causal settings, and for head dimen-
                                          H IP K ITTENS: Fast and Furious AMD Kernels




Figure 8. Attention forwards. We compare H IP K ITTENS GQA          Figure 9. Attention backwards. We compare H IP K ITTENS GQA
and MHA (Figure 17) to the strongest available baselines. We use    and MHA (Figure 16) to the strongest available baselines. We use
batch 16, query heads 64, key value heads 8, head dim 64 and 128.   batch 16, query heads 64, key value heads 8, and head dim 128.

   sions 64 and 128. HK outperforms all available AMD
   baselines on average, including the AITER kernels,               4. Memory bound results. We consider a fused dropout-
   which are written in hand-optimized raw assembly by                 residual-layernorm kernel (from prenorm Transformer
   AMD engineers. HK is 1.0 → 2.1↑ faster than AITER,                  architectures) and a rotary positional encoding kernel in
   1.3 → 4.5↑ faster than PyTorch (SDPA), 1.0 → 1.4↑ CK,               Figure 10. HK outperforms both AITER and PyTorch
   and 1.2 → 4.5↑ Triton kernels in Figure 8.                          compiled kernels by 1.1 → 2.2↑ across settings.
   HK’s attention forward kernel uses an 8- WAVE PING -             The inconsistent performance of AMD libraries and the
   PONG .     Within compute clusters, each wavefront               difficulty of scaling assembly-engineered kernels (e.g., re-
   interleaves online-softmax vector ops (max/subtract/-            inforced by head dim. 64 attention and GQA non-causal
   exp2/accumulate) with MFMA instructions. Despite                 backwards) reflects the value of having a simple set of ker-
   substantial scheduling and hardware differences between          nel programming abstractions to accelerate AMD kernel
   MI355X and NVIDIA B200, the kernel is competitive                development. Finally, to validate kernel stability, we use our
   with FlashAttention-3 under comparable settings (Shah            kernels to pretrain Llama 1B (AI@Meta, 2024) and BERT
   et al., 2024).                                                   110M (Devlin et al., 2019) on the Pile (Gao et al., 2020),
3. Attention backwards. Our GQA causal and non-causal               matching the perplexity of models trained using PyTorch
   backwards attention kernels outperform the baselines by          and AITER after 10B tokens of training.
   1.8 → 2.5↑ across settings (Fig. 9). Our MHA kernels
   compete with the strongest available baselines, which            5    D ISCUSSION AND CONCLUSION
   are written in assembly (Fig. 16).                               Ideally, AI systems can leverage the full diversity of modern
   Attention backwards is a notoriously register heavy work-        hardware. AMD CDNA4 GPUs offer state-of-the-art com-
   load. Our efficient HK kernel uses multiple MFMA                 pute and memory bandwidth, but the “CUDA moat” limits
   instruction shapes (16 ↑ 16 ↑ 32 and 32 ↑ 32 ↑ 16), dif-         adoption. While prior systems such as Triton aim for multi-
   ferent shared memory access patterns (e.g., both row and         silicon portability, our study shows that these compilers (and
   column layout loads to registers from the same shared            sometimes even C++ compilers) often fail to enable peak
   tile), and explicit register pinning.                            AMD performance.
                                        H IP K ITTENS: Fast and Furious AMD Kernels

                                                                 Sparsity), CCF1563078 (Volume to Velocity), and 1937301
                                                                 (RTML); US DEVCOM ARL under Nos. W911NF-23-2-
                                                                 0184 (Long-context) and W911NF-21-2-0251 (Interactive
                                                                 Human-AI Teaming); ONR under Nos. N000142312633
                                                                 (Deep Signal Processing); Stanford HAI under No. 247183;
                                                                 NXP, Xilinx, LETI-CEA, Intel, IBM, Microsoft, NEC,
                                                                 Toshiba, TSMC, ARM, Hitachi, BASF, Accenture, Ericsson,
                                                                 Qualcomm, Analog Devices, Google Cloud, Salesforce, To-
                                                                 tal, the HAI-GCP Cloud Credits for Research program, the
                                                                 Stanford Data Science Initiative (SDSI), and members of the
                                                                 Stanford DAWN project: Meta, Google, and VMWare. The
                                                                 U.S. Government is authorized to reproduce and distribute
                                                                 reprints for Governmental purposes notwithstanding any
                                                                 copyright notation thereon. Any opinions, findings, and con-
                                                                 clusions or recommendations expressed in this material are
                                                                 those of the authors and do not necessarily reflect the views,
                                                                 policies, or endorsements, either expressed or implied, of
                                                                 NIH, ONR, or the U.S. Government.

                                                                 7    C ONTRIBUTIONS
                                                                 WH, DW, and SA designed and implemented H IP K ITTENS,
                                                                 kernels, micro experiments, and baselines. SS designed the
                                                                 cache reuse strategy. WH and SA wrote the manuscript. SW,
                                                                 DF, RS, MO, CR advised and SA supervised the project.
Figure 10. Memory bound. We compare H IP K ITTENS fused
dropout-residual-layernorm and rotary kernels to the strongest
available baselines at batch 16, heads 16, and head dim 128.     R EFERENCES
                                                                 Helion, 2025.   URL https://github.com/
This work provides the first systematic analysis of the prin-      pytorch/helion.
ciples that enable high-performance AMD AI kernels and
introduces H IP K ITTENS, a minimal set of C++ embed-            AI@Meta.  Llama 3 model card. 2024. URL
ded programming primitives that capture those principles.          https://github.com/meta-llama/llama3/
Though the abstractions and front-end interface—tiles and          blob/main/MODEL_CARD.md.
PyTorch-inspired bulk operations over tiles—remains the          AMD. AITER, 2025a. URL https://github.com/
same across NVIDIA and AMD, the instantiation of those            ROCm/aiter.
abstractions—in terms of schedules, memory movement,
and cache optimizations—differ due to fundamental hard-          AMD. Composable Kernel, 2025b. URL https://
ware differences. We evaluate the ideas presented in HK           github.com/ROCm/composable_kernel.
by implementing a suite of representative AI workloads and
find that we can achieve peak performance across them.           AMD.   Rocm hardware features, 2025. URL
                                                                  https://rocm.docs.amd.com/projects/
By codifying the principles for AMD kernels into compos-          HIP/en/docs-develop/reference/
able, open abstractions, these findings move the community        hardware_features.html.
closer to the long-standing vision of a universal software
stack that performs well across diverse hardware platforms.      AMD. AMD Matrix Instruction Calculator, 2025a. URL
                                                                  https://github.com/ROCm/amd_matrix_
                                                                  instruction_calculator.
6    ACKNOWLEDGEMENTS
                                                                 AMD.   AMD Instinct™ MI355X GPUs, 2025b.
We thank Simon Guo, Michael Zhang, Stuart Sul, Arya               URL https://www.amd.com/en/products/
Tschand, Tom Papatheodore, the Hazy Research Lab                  accelerators/instinct/mi350/mi355x.
and Stanford AI Lab for providing feedback on this                html.
work. We gratefully acknowledge the support of NIH
under No. U54EB020405 (Mobilize), NSF under Nos.                 AMD. Rocm libraries, 2025. URL https://github.
CCF2247015 (Hardware-Aware), CCF1763315 (Beyond                   com/ROCm/rocm-libraries.
                                        H IP K ITTENS: Fast and Furious AMD Kernels

Baronio, C., Marsella, P., Pan, B., Guo, S., and Alberti, S.       verification, and optimization, 2025. URL https://
  Kevin: Multi-turn rl for generating cuda kernels, 2025.          arxiv.org/abs/2509.14279.
  URL https://arxiv.org/abs/2507.11948.
                                                                 Lattner, C. and Adve, V. Llvm: A compilation framework
Bikshandi, G. and Shah, J.     Developing cuda ker-                for lifelong program analysis & transformation, 2004.
  nels for accelerated matrix multiplication on nvidia
  hopper architecture using the cutlass library, 2023.           Lattner, C., Amini, M., Bondhugula, U., Cohen, A., Davis,
  URL         https://research.colfax-intl.                        A., Pienaar, J., Riddle, R., Shpeisman, T., Vasilache, N.,
  com/wp-content/uploads/2023/12/                                  and Zinenko, O. Mlir: A compiler infrastructure for
  colfax-gemm-kernels-hopper.pdf.                                  the end of moore’s law, 2020. URL https://arxiv.
                                                                   org/abs/2002.11054.
Chen, T., Moreau, T., Jiang, Z., Zheng, L., Yan, E., Shen, H.,
  Cowan, M., Wang, L., Hu, Y., Ceze, L., et al. {TVM}: An        LLVM Compiler Infrastructure. User Guide for AMDGPU
  automated {End-to-End} optimizing compiler for deep              Backend, 2025. URL https://llvm.org/docs/
  learning. In 13th USENIX Symposium on Operating Sys-             AMDGPUUsage.html.
  tems Design and Implementation (OSDI 18), pp. 578–594,         NVIDIA. Cuda templates for linear algebra subrou-
  2018.                                                           tines, 2017. URL https://github.com/NVIDIA/
Devlin, J., Chang, M.-W., Lee, K., and Toutanova, K. Bert:        cutlass.
  Pre-training of deep bidirectional transformers for lan-       NVIDIA.  NVIDIA CuTe, 2024. URL https:
  guage understanding. In Proceedings of NAACL-HLT                //github.com/NVIDIA/cutlass/blob/
  2019, 2019.                                                     main/media/docs/cute/00_quickstart.md.
Forum, N. D.        How to understand the bank con-        NVIDIA.     Nvidia blackwell architecture technical
  flict of shared memory, 2023.             URL https:       brief.     https://resources.nvidia.com/
  //forums.developer.nvidia.com/t/                           en-us-blackwell-architecture, 2025a.
  how-to-understand-the-bank-conflict-of-shared-mem/
  260900.                                                  NVIDIA. nvidia-cutlass-dsl, 2025b. URL https://
                                                             pypi.org/project/nvidia-cutlass-dsl/.
Gao, L., Biderman, S., Black, S., Golding, L., Hoppe, T.,
  Foster, C., Phang, J., He, H., Thite, A., Nabeshima, N., OpenAI. AMD and OpenAI announce strategic part-
  Presser, S., and Leahy, C. The Pile: An 800gb dataset      nership to deploy 6 gigawatts of AMD GPUs,
  of diverse text for language modeling. arXiv preprint      2025.     URL https://openai.com/index/
  arXiv:2101.00027, 2020.                                    openai-amd-strategic-partnership/.
Harris, C. R., Millman, K. J., van der Walt, S. J., Gommers,     OpenAI, :, Agarwal, S., Ahmad, L., Ai, J., Altman, S., Ap-
  R., Virtanen, P., Cournapeau, D., Wieser, E., Taylor, J.,        plebaum, A., Arbus, E., Arora, R. K., Bai, Y., Baker,
  Berg, S., Smith, N. J., Kern, R., Picus, M., Hoyer, S., van      B., Bao, H., Barak, B., Bennett, A., Bertao, T., Brett,
  Kerkwijk, M. H., Brett, M., Haldane, A., del Rı́o, J. F.,        N., Brevdo, E., Brockman, G., Bubeck, S., Chang, C.,
  Wiebe, M., Peterson, P., Gérard-Marchant, P., Sheppard,         Chen, K., Chen, M., Cheung, E., Clark, A., Cook, D.,
  K., Reddy, T., Weckesser, W., Abbasi, H., Gohlke, C.,            Dukhan, M., Dvorak, C., Fives, K., Fomenko, V., Garipov,
  and Oliphant, T. E. Array programming with NumPy.               T., Georgiev, K., Glaese, M., Gogineni, T., Goucher, A.,
  Nature, September 2020.                                          Gross, L., Guzman, K. G., Hallman, J., Hehir, J., Hei-
                                                                   decke, J., Helyar, A., Hu, H., Huet, R., Huh, J., Jain, S.,
Kim, Y. J., Henry, R., Fahim, R., and Awadalla, H. H. Who          Johnson, Z., Koch, C., Kofman, I., Kundel, D., Kwon,
  says elephants can’t run: Bringing large scale moe mod-          J., Kyrylov, V., Le, E. Y., Leclerc, G., Lennon, J. P.,
  els into cloud scale production. Proceedings of the Third        Lessans, S., Lezcano-Casado, M., Li, Y., Li, Z., Lin, J.,
  Workshop on Simple and Efficient Natural Language Pro-           Liss, J., Lily, Liu, Liu, J., Lu, K., Lu, C., Martinovic, Z.,
  cessing (SustaiNLP), 2022.                                       McCallum, L., McGrath, J., McKinney, S., McLaughlin,
Krizhevsky, A., Sutskever, I., and Hinton, G. E. Imagenet         A., Mei, S., Mostovoy, S., Mu, T., Myles, G., Neitz, A.,
  classification with deep convolutional neural networks.          Nichol, A., Pachocki, J., Paino, A., Palmie, D., Pantu-
  Advances in Neural Information Processing Systems 25             liano, A., Parascandolo, G., Park, J., Pathak, L., Paz, C.,
  (NIPS 2012), 2012.                                               Peran, L., Pimenov, D., Pokrass, M., Proehl, E., Qiu, H.,
                                                                   Raila, G., Raso, F., Ren, H., Richardson, K., Robinson,
Lange, R. T., Sun, Q., Prasad, A., Faldor, M., Tang, Y., and       D., Rotsted, B., Salman, H., Sanjeev, S., Schwarzer, M.,
  Ha, D. Towards robust agentic cuda kernel benchmarking,          Sculley, D., Sikchi, H., Simon, K., Singhal, K., Song, Y.,
                                        H IP K ITTENS: Fast and Furious AMD Kernels

  Stuckey, D., Sun, Z., Tillet, P., Toizer, S., Tsimpourlas,       systems, 2025. URL https://arxiv.org/abs/
  F., Vyas, N., Wallace, E., Wang, X., Wang, M., Watkins,          2504.17577.
  O., Weil, K., Wendling, A., Whinnery, K., Whitney, C.,
  Wong, H., Yang, L., Yang, Y., Yasunaga, M., Ying, K.,          Zhang, S., Zheng, N., Lin, H., Jiang, Z., Bao, W., Jiang,
  Zaremba, W., Zhan, W., Zhang, C., Zhang, B., Zhang, E.,          C., Hou, Q., Cui, W., Zheng, S., Chang, L.-W., Chen,
  and Zhao, S. gpt-oss-120b gpt-oss-20b model card, 2025.          Q., and Liu, X. Comet: Fine-grained Computation-
  URL https://arxiv.org/abs/2508.10925.                            communication Overlapping for Mixture-of-Experts. Pro-
                                                                   ceedings of the 8th MLSys Conference (MLSys), March
Ouyang, A., Guo, S., Arora, S., L. Zhang, A., Hu, W., Ré, C.,     2025.
  and Mirhoseini, A. Kernelbench: Can llms write efficient
  gpu kernels? In International Conference on Machine            Zhou, K., Lezcano, M., Goucher, A., Rakhmati, A., Niu, J.,
  Learning (ICML), 2025. doi: 10.48550/arXiv.2502.10517.           Lebar, J., Szczerbuk, P., Bell, P., Tillet, P., Raoux, T., and
  URL https://arxiv.org/abs/2502.10517.                            Moudallal, Z. Linear layouts: Robust code generation of
                                                                   efficient tensor computation using F2 . Proceedings of the
Paszke, A., Gross, S., Massa, F., Lerer, A., Bradbury, J.,         31st ACM International Conference on Architectural Sup-
  Chanan, G., Killeen, T., Lin, Z., Gimelshein, N., Antiga,        port for Programming Languages and Operating Systems,
  L., Desmaison, A., Köpf, A., Yang, E., DeVito, Z., Rai-         Volume (ASPLOS ’26), 2025.
  son, M., Tejani, A., Chilamkurthy, S., Steiner, B., Fang,
  L., Bai, J., and Chintala, S. Pytorch: An imperative
  style, high-performance deep learning library, 2019. URL
  https://arxiv.org/abs/1912.01703.
Sara Hooker. The Hardware Lottery. Communications of
  the ACM, 2021.
SemiAnalysis. MI300X vs H100 vs H200 Benchmark
  Part 1: Training – CUDA Moat Still Alive, 2024. URL
  https://semianalysis.com/2024/12/22/
  mi300x-vs-h100-vs-h200-benchmark-part-1-training/.
Shah, J., Bikshandi, G., Zhang, Y., Thakkar, V., Ramani, P.,
  and Dao, T. Flashattention-3: Fast and accurate attention
  with asynchrony and low-precision, 2024. URL https:
  //arxiv.org/abs/2407.08608.
Spector, B., Juravsky, J., Sul, S., Dugan, O., Lim, D., Fu,
  D., Arora, S., and Ré, C. Look ma, no bubbles! de-
  signing a low-latency megakernel for llama-1b, 2025.
  URL https://hazyresearch.stanford.edu/
  blog/2025-05-27-no-bubbles.
Spector, B. F., Arora, S., Singhal, A., Fu, D. Y., and Ré,
  C. Thunderkittens: Simple, fast, and adorable ai kernels.
  International Conference on Learning Representations
  (ICLR), 2024.
Tillet, P., Kung, H. T., and Cox, D. Triton: an intermediate
   language and compiler for tiled neural network computa-
   tions. In Proceedings of the 3rd ACM SIGPLAN Interna-
   tional Workshop on Machine Learning and Programming
  Languages, 2019.
Triton. Gluon?, 2025. URL https://github.com/
  triton-lang/triton/issues/7392.
Wang, L., Cheng, Y., Shi, Y., Tang, Z., Mo, Z., Xie, W.,
 Ma, L., Xia, Y., Xue, J., Yang, F., and Yang, Z. Tile-
 lang: A composable tiled programming model for ai
