       F LASHLIGHT : P Y T ORCH C OMPILER E XTENSIONS TO ACCELERATE
                             ATTENTION VARIANTS


Bozhi You 1 2 3 Irene Wang 4 2 Zelal Su Mustafaoglu 1 Abhinav Jangda 5 Angélica Moreira 5 Roshan Dathathri 5
                                        Divya Mahajan 4 Keshav Pingali 1


                                                        A BSTRACT
     Attention is a fundamental building block of large language models (LLMs), so there have been many efforts to
     implement it efficiently. For example, FlashAttention leverages tiling and kernel fusion to optimize attention.
     Recently, a number of variants of attention have been introduced to enhance model quality or efficiency. Supporting
     them efficiently remains difficult since they usually require specialized kernels or hand-tuned implementations.
     FlexAttention recently addressed part of this gap by using static programming templates to support FlashAttention-
     like kernels for a subset of attention variants.
     In this paper, we introduce F LASH L IGHT, a compiler-native framework within the PyTorch ecosystem that
     automatically generates fused, FlashAttention-style kernels for arbitrary attention-based programs, without
     relying on static templates or predefined kernel specializations. F LASH L IGHT leverages PyTorch’s compilation
     workflow to fuse and tile attention computations transparently, enabling efficient execution for diverse attention
     patterns. Not only does it support all variants expressible in the FlexAttention model but it also handles more
     general, data-dependent attention formulations that are beyond the capabilities of FlexAttention. Our results
     show that F LASH L IGHT produces kernels with competitive or superior performance to FlexAttention, while
     offering the flexibility of native PyTorch code, enabling developers to rapidly explore new attention models
     without sacrificing performance. F LASH L IGHT is open source and available as a fork of PyTorch at https:
     //github.com/bozhiyou/pytorch-flashlight.

1    I NTRODUCTION                                                high performance for new attention models often requires
                                                                  significant engineering effort, leaving many variants con-
Optimizing attention is crucial for accelerating training         strained by slower PyTorch implementations that lack fused,
and inference in machine learning pipelines. For instance,        memory-efficient execution, and hindering their adoption.
FlashAttention (Dao et al., 2022; Dao, 2023; Shah et al.,
2024) improves performance by tiling and fusing multiple          FlexAttention (Dong et al., 2024; He et al., 2024) and Flash-
operations into a single kernel, thereby reducing memory          Infer (Ye et al., 2025) attempt to circumvent this problem
reads/writes and launch overhead to enhance data locality         by providing a static, higher-order template that captures a
and GPU utilization. Variants of attention such as differ-        range of known attention variants. FlexAttention builds on
ential attention (Ye et al., 2024), row/column-wise gated         PyTorch’s compiler infrastructure, using the TorchInductor
self-attention in AlphaFold’s (Jumper et al., 2021) Evo-          to generate a fused Triton (Tillet et al., 2019) kernel from
former, AlphaFold’s Invariant Point Attention (IPA), and          template-specialized Triton kernels. In contrast, FlashInfer
Rectified Sparse Attention (RSA) (Sun et al., 2025) have          uses just-in-time compilation to generate a fused CUDA ker-
been introduced to improve model quality and reduce hallu-        nel from template-specialized CUDA kernels. Nevertheless,
cinations. Handcrafted kernels like FlashAttention cannot         both FlexAttention and FlashInfer have limited expressive-
be used for new attention variants. Therefore, achieving          ness since they require programmers to express their atten-
    1
                                                                  tion variant in terms of their template. In particular, variants
      Department of Computer Science, University of Texas at      such as differential attention, row/column-wise gated self-
Austin, Austin, USA 2 Work started while at Microsoft Research
3
  Now at Google 4 School of Computer Science, Georgia Institute
                                                                  attention, IPA, and RSA do not fit their template, preventing
of Technology, Atlanta, USA 5 Microsoft Research, Redmond,        them from achieving the performance of handcrafted imple-
USA. Correspondence to: Bozhi You <youbozhi@cs.utexas.edu>,       mentations.
Roshan Dathathri <roshan.dathathri@microsoft.com>.
                                                                  Compiler-driven optimization of attention variants is the
Proceedings of the 9 th MLSys Conference, Bellevue, WA, USA,      natural solution to these problems but the PyTorch compiler
2026. Copyright 2026 by the author(s).                            stack currently lacks key optimizations such as reduction
                        Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

fusion and complex operator fusion across memory bound-            Algorithm 1 Stable softmax.
aries, limiting the performance of the generated code.              1: m0 ← −∞
                                                                    2: for k = 1 to N do
In this paper, we propose F LASH L IGHT, a compiler-native
                                                                    3:   mk ← maximum(mk−1 , xk )
mechanism within the PyTorch ecosystem that automatically
                                                                    4: end for
generates FlashAttention-style tiled and fused kernels from
                                                                    5: d0 ← 0
standard PyTorch code without relying on static templates
                                                                    6: for j = 1 to N do
or specialized kernels.
                                                                    7:   dj ← dj−1 + exj −mN
F LASH L IGHT turns kernel optimization for attention vari-         8: end for                    PN
ants from a manual engineering effort into a compiler opti-         9: Assert: mN = max x and dN = j=1 exj −max x
mization problem, enabling AI scientists to develop new vari-
ants of attention without having to sacrifice performance or
scalability. Rather than requiring programmers to re-express       Algorithm 2 Online softmax
their models in a specialized API, F LASH L IGHT directly an-       1: m0 ← −∞
alyzes and transforms standard PyTorch attention code, auto-        2: d0 ← 0
matically discovering and fusing the constituent operations         3: for j ← 1 to N do
into a single, tiled Triton kernel. It achieves FlashAttention-     4:   mj ← maximum (mj−1 , xj )
level performance while preserving the full flexibility of          5:   dj ← dj−1 × emj−1 −mj + exj −mj
PyTorch. Users enable F LASH L IGHT by compiling their              6: end for
PyTorch code using torch.compile. F LASH L IGHT in-                 7:
                                                                                                  PN
tegrates seamlessly with PyTorch’s compiler infrastructure,         8: Assert: mN = max x and dN = j=1 exj −max x
extending the TorchInductor IR by (1) adopting a unified
reduction IR, which enables transforming matrix multiplica-
tions; (2) captures the algebraic semantics of reductions to             a unified, compiler-driven approach rather than relying
enable fusing complex reductions like softmax; and (3) in-               on static templates or custom kernels.
troduces logical grid dimensions, which enables fusing tiled           • F LASH L IGHT produces kernels with competitive or
dimensions. F LASH L IGHT introduces a set of global graph               superior performance to FlexAttention, while offering
rewrites: (1) structural fusion with dimension demotion, (2)             the flexibility of native PyTorch code, enabling devel-
semantic fusion with algebraic transformation, and (3) struc-            opers to rapidly explore new attention models without
tural fusion with tiling-aware dimension elimination. These              sacrificing performance.
rewrites can be applied in any order and in combination
with existing TorchInductor passes. Thus, F LASH L IGHT
maintains compatibility as new attention mechanisms and            2     BACKGROUND
backend optimizations evolve.                                      This section explains the math behind softmax and its vari-
We evaluate several attention variants, including those that       ants, and describes the PyTorch 2.0 compiler stack.
are not supported by FlexAttention’s template, on H100
and A100 GPUs. For variants that FlexAttention sup-                2.1   Softmax, Safe Softmax, Online Softmax
ports, F LASH L IGHT-generated code is competitive with
                                                                   The softmax function takes as input a vector x =
or faster than that of FlexAttention. For all variants, F LASH -
                                                                   (x1 , x2 , . . . , xN ) ∈ RN and returns a normalized vector
L IGHT-generated code is significantly faster than that of
                                                                   σ(x) ∈ RN over the N components. Formally, the i-th
torch.compile. For AlphaFold (Jumper et al., 2021), F LASH -
                                                                   component of the softmax function σ(x)i is defined as:
L IGHT improves the execution time of row/column-wise
gated self-attention by more than 5× and improves the in-                             exi
ference latency by 6% to 9%.                                                 σ(x)i = PN                for i = 1, . . . , N.      (1)
                                                                                                  xj
                                                                                          j=1 e
In summary, F LASH L IGHT makes the following contribu-
tions.                                                             Here, exi denotes exponentiation.
                                                                                              PN Eq. 1 ensures that each
   • F LASH L IGHT implements kernel-level fusion that             output σ(x)i > 0 and that i=1 σ(x)i = 1. Since expo-
     fuses compatible compute blocks (e.g., matmul + soft-         nentiation of large numbers causes numerical instability, a
     max, matmul + matmul) when parallelism permits,               stable implementation uses the maximum of all input ele-
     automatically exploiting opportunities for performance        ments, max x, as:
     gains.
   • F LASH L IGHT achieves generality by supporting both                           exi −max x
                                                                    softmax(x)i = PN                         for i = 1, . . . , N. (2)
     standard and emerging attention mechanisms through                                  xj −max x
                                                                                   j=1 e
                            Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

 1   def attention(q, k, v, attn_mask=None):                             1   from torch.nn.attention.flex_attention import (
 2       # Compute attention scores                                      2           flex_attention, create_block_mask)
 3       attn_scores = torch.matmul(q, k.transpose(-2, -1))              3   from functools import lru_cache
 4       attn_scores *= 1 / math.sqrt(q.size(-1))                        4
 5                                                                       5   @lru_cache
 6         if attn_mask is not None:                                     6   def create_block_mask_cached(mask, B,H,M,N, device):
 7             # Set INF when mask is True                               7       return create_block_mask(mask, B,H,M,N, device)
 8             attn_scores = attn_scores.masked_fill(                    8
 9                             attn_mask, INF)                           9   def generate_sliding_window(window_size):
10                                                                      10       def sliding_window_mask(b, h, q, kv):
11         # Apply softmax and compute output                           11           return (q >= kv) & ((q - kv) <= window_size)
12         attn_weights = torch.softmax(attn_scores, dim=-1)            12       # Set INF when mask is False
13         output = torch.matmul(attn_weights, v)                       13       return sliding_window_mask
14         return output                                                14
                                                                        15   # Create block mask for sliding window
                                                                        16   block_mask = create_block_mask_cached(
                    Listing 1. Attention in PyTorch.                    17       generate_sliding_window(window_size=256),
                                                                        18       B=1, H=1, # ignore actual B and H sizes
                                                                        19       M=q.size(2), N=k.size(2), device="cuda")
                                                                        20
                                                                        21   # Compile for performance
     When computing the denominator of Eq. 2, a naive imple-            22   flex_attn = torch.compile(flex_attention,
                                                                        23                           dynamic=False)
     mentation uses two serial loops to compute the max and             24
     sum. This implementation is inefficient for large N because        25   # Apply attention with sliding window's block mask
                                                                        26   output = flex_attn(q, k, v, block_mask=block_mask)
     it prevents efficient tiling of the computation.
     (Milakov & Gimelshein, 2018) proposed the online softmax                      Listing 2. Sliding Window Attention using FlexAttention.
     algorithm, shown in Alg. 2, which fuses the two reduc-
     tion loops into one by computing the maximum and the
     normalization denominator simultaneously. This improves                 FlexAttention (Dong et al., 2024) provides a template to
     performances by reducing the number of memory accesses                  write attention variants that modify the attention score:
     as the online softmax denominator computation accesses
     element of the vector x only once. The fused loop also                   FlexAttention(Q, K, V, score_mod) =
                                                                                                                 QK ⊤      (4)
                                                                                                                    
     enables tiling and kernel fusion optimizations, which are
     key to FlashAttention (Dao, 2023).                                                    softmax score_mod √           V
                                                                                                                  dk

     2.2    Attention, FlashAttention, FlexAttention                         FlexAttention only supports attention variants that fit this
     Given a sequence of length n represented by query, key, and             pattern. Users specify the score_mod as an element-wise
     value matrices Q, K, V ∈ Rn×dk , the scaled dot-product                 operation on the attention matrix that accepts an old score
     attention (Vaswani et al., 2017) is computed as follows:                and returns a new score. For performance reasons, they in-
                                                                             troduce a special case for score_mod called mask_mod.
                                                                             Users specify whether a given index of the attention ma-
                                                   QK ⊤
                                                         
            Attention(Q, K, V ) = softmax          √          V   (3)        trix should be masked (score set to infinity) or not (use old
                                                     dk                      score). As this function only depends on the shape of Q
                                                                             and K, they further specialize for this case by providing a
                                                                  ⊤
     Softmax is applied row-wise to the attention matrix QK
                                                          √
                                                            dk
                                                               ,             PyTorch function that inspects the mask_mod at runtime to
     producing normalized attention weights that sum to 1 over               create a custom block_mask representation that stores (in
     the key dimension for each query. Listing 1 shows the Py-               device memory) sparse matrices for empty, full, or partial
     Torch implementation of scaled dot-product attention (with              blocks. FlexAttention uses the PyTorch compiler stack to
     attn_mask=None). We refer to this as Vanilla Attention.                 generate a fused and optimized kernel from a specialized
     Computing the attention matrix or attn_scores requires                  template that executes attention with these inspected sparse
     O(n2 ) time and memory, which becomes a bottleneck for                  matrices. For example in Listing 2, the sliding window at-
     long sequences.                                                         tention (Beltagy et al., 2020) applies a sliding window mask
                                                                             (Line 10) to only consider the attention scores within a win-
     FlashAttention (Dong et al., 2024), tiles the computation               dow. For performance, users are expected to both create the
     and applies the online softmax within each tile. This design            block mask (Line 16) to represent this window and cache
     keeps intermediate results in the fast on-chip memory, thus             this mask (Line 5) to avoid its recomputation on later calls.
     reducing the required memory bandwidth and lowering the
     memory complexity to O(n), while maintaining the exact
                                                                             2.3     The PyTorch 2.0 Compiler Stack
     attention computation (in real numbers). FlashAttention is
     a hand-optimized kernel for vanilla attention and does not              PyTorch is widely adopted for machine learning due to
     support other attention variants.                                       its eager execution model, which treats model definitions
                       Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants




Figure 1. F LASH L IGHT extends TorchInductor within the torch.compile stack, adding structural and semantic fusion passes with
dimension demotion, algebraic transformation, and tiling-aware dimension elimination to generate optimized Triton kernels.


as imperative Python code. This design makes PyTorch 12           def get_sliding_mask(query, window):
                                                                      L = query.size(-2)
flexible and intuitive but complicates compiler optimiza- 3           idx = torch.arange(L, device=query.device)
tions that rely on static, graph-based representations of 4           q = idx.view(L, 1)
                                                             5        k = idx.view(1, L)
computation. Unlike frameworks like TensorFlow (Abadi 6               mask = (q < kv) or ((q - kv) > window) #(L, L)
et al., 2016) or Theano (James Bergstra et al., 2010), Py- 78         return mask.unsqueeze(0).unsqueeze(0)

Torch does not natively expose a full program graph suit- 9       def sliding_window_attn(q, k, v, window):
able for whole-graph analysis and transformation. The Py- 10          attn_mask = get_sliding_mask(q, window)
                                                            11        return attention(q, k, v, attn_mask=attn_mask)
Torch 2.0 compiler stack (Ansel et al., 2024), used via the 12
torch.compile() API, addresses this limitation by us- 13          # Compile with Flashlight enabled
                                                            14    sliding_window_attn = torch.compile(
ing two key components: TorchDynamo, a Python-level 15                            sliding_window_attn, dynamic=False,
graph extractor, and TorchInductor, a backend compiler 16                         enable_flashlight=True)
                                                            17
targeting both CPUs and GPUs.                               18    output = sliding_window_attn(q, k, v, window=256)

TorchDynamo extracts the program graph by detecting
                                                                       Listing 3. Sliding Window Attention using Flashlight.
PyTorch operations, which are transformed into an FX
graph (Reed et al., 2022), an intermediate representation
designed for further optimization and lowering. TorchDy-
namo integrates with AOTAutograd, which enables training         in torch.compile() to turn off dynamic shape tracing
by recording both forward and backward graphs.                   and generate shape-efficient kernels for faster performance.
TorchInductor functions as a general-purpose compiler            Performance PyTorch 2.0 achieves strong performance
backend designed to translate FX graphs into optimized,          on real-world workloads. Across 180+ models from
high-performance code. It supports multiple backends: a          TorchBench, HuggingFace, and TIMM, TorchDynamo and
built-in OpenAI Triton (Tillet et al., 2019) for GPU, C++        TorchInductor deliver a geometric mean speedup of 2.27×
with OpenMP for CPU execution, and custom backends               on inference and 1.41× on training with float32 on NVIDIA
defined by the user. TorchInductor introduces a Python-          A100 GPUs (Ansel et al., 2024). TorchDynamo’s graph cap-
based loop-level intermediate representation (IR) using a        ture overhead is under 5%, significantly outperforming Lazy
define-by-run model: tensor computations are expressed           Tensors and previous JIT mechanisms.
as Python functions over symbolic indices. This IR en-
ables (i) operator decomposition into a minimal core set of      The PyTorch 2.0 compiler stack introduces a hybrid
pointwise, reduction, and other primitives operations, (ii)      approach to dynamic language compilation, combining
fusion and scheduling informed by symbolic analysis of           bytecode-level tracing with backend-level optimization and
memory access patterns and aliasing, and (iii) efficient code    code generation. It makes aggressive compiler optimiza-
generation with optional use of CUDA Graphs to minimize          tion accessible to users without compromising Python’s
kernel launch overhead. TorchInductor supports vectoriza-        dynamism or usability.
tion, auto-tuning, and ahead-of-time kernel compilation.
Dynamic Shapes are supported in the PyTorch 2.0+ com-            3    F LASHLIGHT
piler stack. Using meta-functions that track and propa-          Figure 1 illustrates the torch.compile workflow and
gate shape information symbolically, TorchDynamo and             how F LASH L IGHT plugs into it. F LASH L IGHT extends
TorchInductor can reuse compiled code for inputs of dif-         TorchInductor with a principled approach to operator fu-
ferent sizes. The compiler also uses guards and simple           sion and scheduling, based on a set of composable compiler
symbolic checks to decide when it needs to recompile. For        transformations. This allow it to dynamically fuse com-
models with fixed input sizes, users set dynamic=False           plex subgraphs, such as the various forms of attention, into
                             Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

 1   def diff_attn(q, k, v, lambda_full):                              max algorithm to the online softmax algorithm. We use
 2       # Split Q and K into two heads
 3       q0, q1 = q.chunk(2, dim=1)                                    this in TorchInductor
                                                                                         √  to semantically fuse operations like
 4
 5
         k0, k1 = k.chunk(2, dim=1)                                    softmax QK ⊤ / d in Section 3.4. Section 3.5 describes
 6        # Compute attention for both heads                           structural fusion of tiled loops while eliminating small di-
 7        attn0 = attention(q0, k0, v)                                 mensions and Section 3.6 describes ways to accomplish
 8        attn1 = attention(q1, k1, v)
 9                                                                     this in TorchInductor using logical grid dimensions. This
10        # Differential: subtract weighted second head                would fuse consecutive
11        output = attn0 - lambda_full * attn1                                           √  matrix multiplication operations like
12        return output                                                softmax QK ⊤ / d V . We discuss some implementation
13
14   # Compile with Flashlight enabled
                                                                       details specific to torch.compile in Section 3.7. Fi-
15   diff_attn = torch.compile(                                        nally, we discuss the generality, flexibility, and trade-offs of
16                   diff_attn, dynamic=False,                         Flashlight and compare it with FlexAttention (Section 3.8).
17                   enable_flashlight=True)
18
19   output = diff_attn(q, k, v, lambda_full=0.2)
                                                                       3.1   Unified Reduction IR
            Listing 4. Differential Attention using Flashlight.        To provide a unified intermediate representation (IR) for
                                                                       tensor computations, tensor dimensions are classified into
                                                                       two categories: p-dimensions (parallel/pointwise) and r-
                                                                       dimensions (reduction). Computations over p-dimensions
     monolithic, high-performance kernels without requiring ex-        are data-independent, representing embarrassingly parallel
     plicit user annotations or modifications to idiomatic PyTorch     workloads that can be mapped directly to parallel execution
     code. Listing 3 shows sliding window attention (Beltagy           units, such as programs in the Triton language or CUDA
     et al., 2020) using F LASH L IGHT. It uses idiomatic PyTorch      thread blocks. For instance, in an element-wise operation
     code without requiring the user to build a block_mask             like torch.add(A, B), all dimensions of tensors A and
     or use a cache like in Listing 2 for FlexAttention. A user        B are p-dimensions. Conversely, r-dimensions introduce
     just needs to pass a flag to torch.compile to enable              data dependencies that require sequential accumulation or
     F LASH L IGHT. This approach stands in contrast to existing       specialized parallel primitives like reduction trees. A canon-
     systems that often depend on predefined static code tem-          ical example is torch.sum(T, dim=n), where dimen-
     plates (e.g., TorchInductor replaces vanilla attention with a     sion n is an r-dimension, as its elements are aggregated,
     hand-optimized kernel by searching for patterns in its IR) or     while all other dimensions remain p-dimensions.
     explicit user annotations (e.g., FlexAttention), limiting their
     applicability beyond common patterns like Equation 4. As          This conceptual framework is operationalized by TorchIn-
     a consequence, F LASH L IGHT enables compiling complex            ductor, where it lowers operations into a loop-level IR
     attention patterns that are not expressible in FlexAttention,     that explicitly categorizes them based on their dimensional
     such as differential attention (Ye et al., 2024) (Listing 4),     properties. However, TorchInductor has a special path for
     row/column-wise gated self-attention in Evoformer (Jumper         performance-sensitive tensor contractions like General Ma-
     et al., 2021), IPA (Jumper et al., 2021), and RSA (Sun et al.,    trix Multiplication (GEMM), where it bypasses the IR-to-
     2025).                                                            kernel generation path to either instantiate a pre-written,
                                                                       highly-optimized kernel template of the Triton language
     We demonstrate Flashlight’s approach using the vanilla at-        or generate calls to vendor-tuned libraries like ATen or
     tention in Eq 3. F LASH L IGHT automatically fuses the entire     cuBLAS. While this workflow ensures high performance for
     sequence of vanilla attention in Listing 1 into a single mono-    the standalone GEMM operation, this bifurcation creates a
     lithic Triton kernel. This kernel executes the computation in     fusion boundary that isolates the GEMM from surrounding
     a single pass over the input data, where each thread
                                                        √     block    computations. This prevents fusion of GEMM with preced-
     computes tiles of the dot-product S = QK ⊤ / d, applies           ing or subsequent operations beyond the support for simple
     the "online" softmax to each tile via a fused max-reduction       element-wise operations, reintroducing memory bandwidth
     and rescaled accumulation, and multiplies the resulting soft-     bottlenecks and kernel launch overheads.
     max output with the corresponding tiles of V .
                                                                       F LASH L IGHT dismantles this artificial fusion boundary by
     Section 3.1 describes the unified reduction IR in TorchIn-        modeling tensor contractions as a generalized reduction
     ductor that enables expressing the loops of matrix mul-           within our unified IR, which is compatible to the PyTorch
     tiplications like QK ⊤ . Section 3.2 describes structural         built-in reduction IR. We observe that a GEMM operation
     fusion of matrix multiplications with simple reductions           naturally conforms to the p- and r-dimension abstraction.
     by demoting dimensions to fuse the max() operation in
     sof tmax() with the preceding QK ⊤ . Section 3.3 de-                                  P 2D matrix multiplication, C = A·B,
                                                                       Consider the canonical
                                                                       defined as Cmn = k Amk Bkn . We model this as follows:
     scribes the semantics of algebraic transformation of reduc-
     tions that generalizes the transformation of the stable soft-
                          Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

   • The output dimensions, indexed by m and n, are p-                    cally. Fusion is also permitted between a pointwise opera-
     dimensions. The computation for each output element                  tion and a reduction if their p-dimensions align (e.g., fusing
     Cmn is independent of all others, and these dimensions               [(P0 , P1 ), ()] into [(P0 , P1 ), (R0 )]). This model, however,
     are preserved in the output tensor’s shape.                          is not sufficient for fusing operations where the dimensions
   • The inner contracted dimension, indexed by k, is an                  fundamentally realign, such as in producer-consumer pat-
     r-dimension. It is iterated over, and the products are               terns where the producer’s output dimension becomes a
     accumulated via a sum reduction. This dimension is                   reduction dimension for the consumer.
     shared by both inputs and consumed by the operation                  Flashlight introduces a more powerful fusion rule by
     and does not appear in the output.                                   leveraging a key insight that a parallel loop can be ex-
                                                                          ecuted sequentially. This allows us to "demote" a p-
By representing GEMM within the same semantic frame-                      dimension from a producer kernel into an r-dimension
work as other tensor operations, F LASH L IGHT enables it                 within the fused kernel. Formally, a producer ker-
to participate fully in our end-to-end fusion engine. This                nel K0 with sketch [(Pcommon , Pproducer ), (. . . )] can
unified approach unlocks advanced optimizations, such as                  be fused with a consumer kernel K1 with sketch
fusing chains of matrix multiplications or complex element-               [(Pcommon ), (Pproducer , . . . )]. The resulting fused kernel
wise prologues directly into a single, efficient kernel.                  will have the sketch: [(Pcommon ), (Pproducer , . . . )]. Here,
                                                                          the dimension Pproducer , which was a p-dimension in the
3.2   Structural Fusion with Dimension Demotion                           standalone producer kernel K0 , becomes an additional inner
                                                                          reduction loop in the fused kernel.
The p- and r-dimension abstraction provides a formal basis
for a canonical loop structure. For a given tensor operation,             The rationale for this transformation is the fundamental
the data-independent p-dimensions form the outer parallel                 trade-off between parallelism and memory latency. By de-
loops, while the data-dependent r-dimensions form the inner               moting a parallel dimension, we tradeoff some of the pro-
iterative loops. This structure naturally maps to the hierar-             ducer’s potential parallelism for complete elimination of
chical execution model of modern GPUs, where outer loops                  the high-latency materialization of the intermediate tensor
can be parallelized across thread blocks, and inner loops are             to global memory. As a consequence, the producer’s re-
executed sequentially across warps.                                       sults are generated and consumed immediately within the
We can therefore define a computation sketch for a kernel,                registers or local memory of a single, unified kernel. On
denoted as [(P0 , P1 , . . . ), (R0 , R1 , . . . )], which captures its   modern accelerators where memory bandwidth is often a
loop hierarchy.                                                           more critical bottleneck than raw compute parallelism, this
                                                                          trade-off is overwhelmingly favorable, leading to significant
for p0 in P0
  for p1 in P1                                                            performance improvements. An example for this scenario
    ...                                                                   is fusing only the max() operation inside sof tmax() with
    for r0 in R0
      for r1 in R1                                                        the preceding QK ⊤ in attention.
      ...

                                                                          3.3   Algebraic Transformation of Reductions
where the capital letters are the iteration ranges of dimen-
sions and the lowercase letters are used for indexing.For                 The Online softmax algorithm (Alg. 2) is key to imple-
example:                                                                  menting FlashAttention-like fused kernel. However, current
                                                                          compilers cannot generate the online implementation auto-
   • Element-wise Addition: C(P0 , P1 ) = A(P0 , P1 ) +                   matically. In Appendix A, we show that the conversion of
     B(P0 , P1 ) involves no reduction and has the sketch                 the stable softmax algorithm to the online softmax algorithm
     [(P0 , P1 ), ()]. Since the p-dimensions are data-                   can be generalized using the standard algebraic notion of a
     independent, the computation can be perfectly flattened              homomorphism. We describe the high level idea here.
     and parallelized across all elements.
   • GEMM: C(P0 , P1 ) = A(P0 , R0 )@B(R0 , P1 ) in-                      Definition 1 Let A be a set with a binary operation ⊕, and
     volves a sum-reduction over the inner dimension and                  let B be a set with a binary operation ⊗. A function f :
     has the sketch [(P0 , P1 ), (R0 )]. The computation for              A→B is said to be a homomorphism if for all a1 , a2 ∈ A,
     each output element (p0 , p1 ) requires an inner loop                f (a1 ⊕ a2 ) = f (a1 ) ⊗ f (a2 ).
     over r0 .
                                                                          In the context of softmax, A = B = R (the set of real
Under this model, existing compilers like TorchInductor pri-              numbers), and ⊕ and ⊗ are addition (+) and multiplication
marily fuse operations with identical sketches. For instance,             (×) of real numbers. The function f (x) = ex is a homomor-
two kernels with the sketch [(P0 , P1 ), ()] can be fused verti-          phism because f a+b = f a × f b . To generalize the online
                                  Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

softmax construction, we need the set A with operations ⊕                 F LASH L IGHT overcomes this barrier by identifying and
and ⊗ to satisfy the axioms of a ring.                                    transforming these dependent computation into a single-
In the stable softmax Algorithm 1, let us denote the se-                  pass, online algorithm when an underlying algebraic struc-
quence of m values produced by the first loop by m[1..N ]                 ture permits it. The key is to transform the dependency
and let m[0] = 0 by definition. The sequence of d values                  on the final result into an incremental update based on
produced by the second loop, which we denote by ds, is                    the running result. This is possible here due to the homo-
expressed abstractly by the following recurrence in which                 morphic property of the exponential function (Section A),
the elements of ds are members of a ring A and E:A→A is                   which maps addition/subtraction to multiplication/division:
a homomorphism.                                                           exp(x − y) = exp(x)/ exp(y). This property allows us to
                                                                          dynamically "rescale" the running sum whenever the run-
  ds[0] = 0
                                                                          ning maximum changes.
  ds[j] = ds[j−1] ⊕ (E(x[j]) ⊗ E(⊖m[N ])) |N ≥ j ≥ 1                      Within a single loop, two running accumulators are main-
                                                                          tained: the running max (mr ) and a running sum (sr ). The
The online softmax Algorithm 2 computes a different se-                   challenge is that whenever the running max is updated, the
quence, denoted by do, that can be expressed abstractly as                accumulated sum becomes invalid as it was normalized by
shown below.                                                              a now-outdated maximum. If the sum was calculated using
  do[0] = 0                                                      (5)
                                                                          an old max, mold , and the max is updated to mnew , the
  do[j] = (E(x[j]) ⊗ E(⊖m[j])) |N ≥ j ≥ 1                        (6)
                                                                          corrected sum can be found by multiplying by a correction
                                                                          factor:
The ds and do sequences will be different in general, but                                      X                       X exp(Ai )
we show that do can be expressed in closed-form by the                                Sold =       exp(Ai − mold ) =
                                                                                               i                       i
                                                                                                                         exp(mold )
following expression.
                                                                                      X                        X exp(Ai ) exp(mold )
                                                                             Snew =        exp(Ai − mnew ) =                           =
             j
            M                
                                                                                       i                       i
                                                                                                                 exp(mold ) exp(mnew )
  do[j] =           E(x[i])       ⊗ E(⊖m[j]) |N ≥ j ≥ 1          (7)
              i=1                                                                              Sold × exp(mold − mnew )


from which it follows that ds[N ] = do[N ]. The proof is in               This allows us to fuse the two passes into a single, efficient
the Appendix A.                                                           kernel. In each step of the loop, we update both the running
                                                                          max and the running sum simultaneously. If an element Ai
3.4   Semantic Fusion with Algebraic Transformation                       causes the running max to change, we apply the correction
                                                                          factor to the sum accumulated so far before adding the new
Beyond the structural compatibility addressed by computa-
                                                                          term. To accomplish this in TorchInductor, we decompose a
tion sketches, valid kernel fusion must also respect data de-
                                                                          reduction to introduce a loop-local variable that copies the
pendencies. A challenge for fusion arises from cross-kernel
                                                                          partially-aggregated, loop-carried value before applying the
data dependencies. For instance, one reduction kernel de-
                                                                          aggregation in this step.
pends on the fully aggregated result of another. This depen-
dency creates a synchronization barrier that prevents naive               By embedding such algebraic reasoning into TorchInduc-
loop fusion, even if the kernels share an identical sketch.               tor, Flashlight can semantically restructure the algorithm to
A canonical example of this challenge is the numerically                  fuse complex, state-dependent operations that are beyond
stable softmax operation, which involves two sequential                   the scope of fusion based on structural compatibility alone.
passes for numerical stability: first finding the maximum                 This unlocks end-to-end kernel fusion for a broader class√ of
                                                                          complex, multi-stage reductions like softmax QK ⊤ / d .
                                                                                                                                      
value of the input tensor, and second, computing the sum of
exponentials shifted by that maximum.
  1. Pass 1 (max): mi = max(mi−1 , Ai ).                                  3.5   Structural Fusion with Tiling-Aware Dimension
                              P                                                 Elimination
  2. Pass 2 (sub-exp-sum): S = i exp(Ai − mf inal ).
                                                                          The loop-based execution model can be further refined by
Although both operations have a compatible reduction                      iterating over contiguous tiles (or blocks) of data rather than
sketch, e.g., [(), (R)], they cannot be trivially fused because           individual elements. Practical GPU kernels adopt tiling as a
Pass 2 has a strict dependency on mf inal — the final scalar              critical optimization that improves data locality by staging
result of the first kernel. A direct merge by stacking the loop           data in fast on-chip memory like shared memory or registers.
bodies would be incorrect, as the computation of each term                The tiled execution also structures the computation to bet-
exp(Ai − mf inal ) requires a value that is only known after              ter exploit the GPU’s SIMT architecture with fine-grained
the first loop has fully completed.                                       parallelism within each tile. For instance, an associative
                            Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

reduction over a tile can be implemented efficiently using                     3.6   Flexible Tiling through Logical Grid Dimensions
parallel primitives like warp-level tree reductions or by vec-
                                                                               Practical implementations of tiling strategies in TorchInduc-
torizing the accumulation across multiple SIMT lanes.
                                                                               tor must address some framework constraints. The ideal
Crucially, tiling transforms the loop structure of a kernel. A                 approach allows for flexible, per-dimension tile sizes, en-
loop over a dimension of size D with a tile size of BD will                    abling a broad autotuning search space and the dimension
execute ⌈D/BD ⌉ times at the tile level. This allows us to                     elimination technique. However, systems like TorchInduc-
define a tiled sketch for a kernel: [(⌈ BPP00 ⌉, . . . ), (⌈ BRR0
                                                               0 ⌉, . . . )]
                                                                               tor often create a rigid coupling between the logical tiling
                                                                               dimensions and the physical GPU execution grid. This cou-
This transformation from an element-space sketch to a tile-
                                                                               pling arises from the asymmetric hardware limits of the
space sketch creates more opportunities for fusion. The
                                                                               grid; in CUDA, for example, the X dimension can be up to
key insight is that, if a dimension Pi is small enough to be
                                                                               231 −1, while the Y and Z dimensions are limited to 65, 535.
processed entirely within a single tile (i.e., BP i ≥ |Pi |), the
                                                                               To accommodate large tensors, TorchInductor often flattens
corresponding tile-level loop bound becomes ⌈|Pi |/BP i ⌉ =
                                                                               multiple p-dimensions into the expansive X grid dimension.
1. A loop with a single iteration can be conceptually elided
                                                                               This presents a dilemma:
from the sketch, effectively collapsing the dimension at the
tile level. This tiling-aware dimension elimination unlocks
fusion opportunities that are otherwise impossible.                               • Flattening: For a computation with a sketch [(P0 , P1 )],
                                                                                    both dimensions are mapped to the X grid. TorchInduc-
Consider twin matrix multiplication, E = (A · B) · D.                               tor forces them to share a single tile size, preventing
                                                                                    independent tuning of the tile sizes for P0 and P1 .
   • Kernel 0 (Producer): C[M, N ] = A[M, K]@B[K, N ]. The                        • Multi-Grid Mapping: Mapping P0 to the Y grid and
     element-space sketch is [(M, N ), (K)].                                        P1 to the X grid would allow separate tile sizes but
   • Kernel 1 (Consumer): E[M, P ] = C[M, N ]@D[N, P ].                             this approach fails if the cardinality of dimension P0
     The element-space sketch is [(M, P ), (N )].                                   exceeds 65, 535.


These kernels are incompatible under standard fusion rules.                    This inflexibility restricts the compiler’s ability to find the
Without tiling, these sketches are also incompatible for the                   optimal tiling configuration.
producer-consumer fusion described previously; the pro-                        F LASH L IGHT resolves this dilemma by decoupling the prob-
ducer’s p-dimension N does not match the consumer’s p-                         lem’s structure from the hardware’s using logical tiling.
dimension P . Fusing them would require materializing the                      Instead of directly affine mapping tiling dimensions to phys-
entire intermediate tensor C into global memory.                               ical grid dimensions, F LASH L IGHT defines a logical, multi-
F LASH L IGHT can fuse these kernels by leveraging tiling.                     dimensional grid of tiles based on the per-dimension tile
Let’s assume the dimension P is relatively small (e.g., 64,                    sizes. This logical grid is then "unrolled" into a linear se-
128). We can set the tile size for the P dimension in Kernel                   quence and mapped to a single physical grid dimension
1 to be its full size, BP = |P |. The tiled sketch for Kernel 1                (e.g., tl.program_id(0) in Triton). Inside the ker-
becomes:                                                                       nel, a simple inverse affine map recovers the logical multi-
                                                                               dimensional tile coordinates from the linear block ID.

      [(⌈
            M
               ⌉, ⌈
                    P
                       ⌉ = 1), (⌈
                                  N
                                     ⌉)] =⇒ [(⌈
                                                M
                                                   ⌉), (⌈
                                                          N
                                                             ⌉)]
                                                                               This approach allows tile sizes for each dimension to be set
            BM      BP            BN            BM        BN                   independently by the autotuner or controlled via hints. This
                                                                               ensures that powerful fusion strategies, like the tiling-aware
The P dimension has been collapsed out of the con-                             dimension elimination, can be applied robustly across a far
sumer’s tile-level p-dimensions. Now, the producer                             wider range of tensor shapes and sizes.
sketch [(Mtile , Ntile ), (Ktile )] and the consumer sketch
[(Mtile ), (Ntile )] can be combined as [(Mtile ), (Ntile , Ktile )]           3.7   Implementation Details
using our generalized fusion rule with dimension demotion.                     Precision handling. When GEMM is lowered as a gen-
The dimension N from the producer’s output is consumed                         eralized reduction in our unified IR, the computation type
directly as an r-dimension by the consumer on-the-fly within                   is unconditionally promoted to FP32 for FP16/BF16 in-
a single fused kernel, completely avoiding the materializa-                    puts. This ensures numerical stability during accumula-
tion of the intermediate tensor C. This tiling-aware transfor-                 tion (matching the behavior of hardware tensor cores) while
mation allows F LASH L IGHT to fuse complex, multi-stage                       keeping the input/output in the original lower precision. The
computations that have historically remained out of reach                      BF16 output type is preserved through the decomposition
for the PyTorch compiler.                                                      and meta-registration layers, with an ad-hoc type restore in
                         Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

the graph lowering to prevent the compiler from inadver-          out observable loss in model accuracy (Dao et al., 2022),
tently widening the output type.                                  they may not improve performance or may be inaccurate
                                                                  for some kernels on some hardware. Users should enable
L2 cache optimization. For kernels with two or more               F LASH L IGHT for PyTorch code based on this knowledge.
tiled parallel dimensions, the block iteration order is swiz-
                                                                  F LASH L IGHT enables users to explore a wide variety of at-
zled to improve spatial locality. Blocks are grouped into
                                                                  tention variants and other tensor computations without hav-
strips of width GROUP_M, and within each strip, the itera-
                                                                  ing to map their code to specific patterns or sacrifice perfor-
tion alternates between dimensions to maximize L2 cache
                                                                  mance or scalability. This flexibility also has performance
reuse for adjacent tiles. This is analogous to the swizzling
                                                                  trade-offs. Unlike FlexAttention or other attention-specific
technique used in Triton’s matmul tutorial 1 , generalized to
                                                                  DSLs, F LASH L IGHT does not specialize its transformations
arbitrary multi-dimensional tiling.
                                                                  for the mask in certain attentions. FlexAttention, for exam-
                                                                  ple, inspects the mask to build a block_mask of sparse
Indexing order tracking. When the compiler’s symbolic             matrices using which it can skip attention for the tiles or
engine (e.g., Sympy) rewrites an index expression like            blocks that are fully masked out. When the matrices are very
m · N + n into a simplified form, the variable order may be       sparse, the attention execution can be much faster because
lost. F LASH L IGHT records the ordering of index variables       it reduces redundant computation. However, the inspection
as they flow through symbolic simplifications and loop-body       code to create block_mask is expensive and storing the
substitutions. The record preserves this mapping so that the      block_mask consumes resource-constrained GPU mem-
codegen phase can emit correctly-shaped N-dimensional             ory. Users are expected to manage this trade-off by building
tensors, masks, and loads even after aggressive index rewrit-     a cache and managing its size. Moreover, even with a cache
ing.                                                              hit, executing attention with inspected sparse matrices might
                                                                  have runtime overheads. Thus, such inspection-execution
Block-reduction heuristic. F LASH L IGHT introduces               code is not always beneficial. We leave incorporation of
a new kernel launch heuristic, blockreduction,                    such techniques in torch.compile() to future work.
for fused block-reduction kernels. It uses template-
based autotuning over a configuration space of
(XBLOCK, RBLOCK, num_warps, num_stages)                  tu-      4     E VALUATION
ples. With aggressive autotuning enabled, the search space        4.1   Experimental Setup
expands to more configurations including smaller block
sizes for workloads with limited parallelism. When the            We run our experiments on two recent GPUs: an NVIDIA
scheduler provides block-size hints (from the blocking            H100 80GB and NVIDIA A100 80GB. We use Python
analysis), these override the default search space to focus       3.12, PyTorch 2.5.0, Triton 3.1.0, CUDA 12.9, FlashInfer
autotuning on the most promising region.                          0.2.5. Except for end-to-end inference latency experiments
                                                                  (Section 4.4), we report average runtimes of 20 runs after 10
Materialization threshold. The baseline compiler’s de-            warm up runs. To ensure that runs are within 1% standard
fines a materialization threshold, the maximum number of          deviation, we cap the SM frequency of H100 to 1290 MHz
fused operations before an intermediate tensor is forced to       and A100 to 1080 MHz, which is the steady state frequency
be materialized to global memory. F LASH L IGHT raises the        of the GPUs in our systems.
threshold to allow more complex fused subgraphs (such             Systems: We use these systems in our evaluation. FlexAt-
as ALiBi (Press et al., 2022)) to remain in a single kernel       tention (Dong et al., 2024) compiles attention code writ-
without premature materialization.                                ten in their template API in PyTorch to a fused Triton
                                                                  kernel. FlashInfer (Ye et al., 2025) is a code-generation
3.8   Generality, Flexibility, Tradeoffs                          based attention engine in CUDA; we use it as a representa-
The transformations in F LASH L IGHT are general and widely       tive of optimized and tuned CUDA/C++ attention kernels.
applicable, but they involve performance and accuracy trade-      torch.compile (Ansel et al., 2024) compiles idiomatic atten-
offs. Structural fusion (Section 3.2) reduces memory ac-          tion code in PyTorch to multiple Triton kernels. F LASH -
cesses at the cost of reduced parallelism. Semantic fusion        L IGHT is our system described in the paper that compiles
(Section 3.4) preserves algebraic semantics in real numbers,      the same PyTorch code to a fused Triton kernel.
but may not be precise in floating point numbers because          Benchmarks: We use these attention variants that are sup-
floating point arithmetic is not associative. While these         ported by FlexAttention: Vanilla (Vaswani et al., 2017),
transformations optimize a variety of attention variants with-    ALiBi (Press et al., 2022), Softcap (Dong et al., 2024),
   1                                                              Causal (Dong et al., 2024), Sliding Window (Beltagy
     https://triton-lang.org/main/getting-started/tutorials/03-
matrix-multiplication.html#l2-cache-optimizations                 et al., 2020), PrefixLM (Dong et al., 2024), and Document
                        Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants




Figure 2. F LASH L IGHT, FlexAttention, and FlashInfer on H100 for attention variants that are supported by FlexAttention template.




Figure 3. F LASH L IGHT, FlexAttention, and FlashInfer on A100 for attention variants that are supported by FlexAttention template.




Figure 4. Runtimes of F LASH L IGHT and torch.compile on H100/A100 for attention variants that are not supported by FlexAttention.
                       Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

Mask (Dong et al., 2024). Each of these variants are evalu-      be amortized over multiple calls by using a cache, but this
ated as Multi-Head Attention (MHA) and Grouped Query             depends on the workload. In Section 4.4, we evaluate an
Attention (GQA). For FlexAttention, Vanilla, ALiBi, and          end-to-end inference workload to quantify this.
Softcap use score_mod, while the rest compute and use a
                                                                 For almost all batch sizes and sequence lengths, FlashInfer
block_mask. Like FlashAttention (Dao, 2023), we vary
                                                                 is faster than both FlexAttention and F LASH L IGHT for all
the sequence length from 512 to 16k and set the batch size
                                                                 variants except ALiBi. F LASH L IGHT does not optimize for
so that their product (the number of tokens) is 16k. The
                                                                 block (mask) sparsity, while FlashInfer optimizes for block
head dimension is 64. For MHA, the number of heads is 16
                                                                 sparsity without needing to materialize sparse data structures
for Q, K, and V ; for GHA, the number of heads is 16 for Q
                                                                 as it passes specialized parameters (e.g., causal for causal
and 2 for K and V . The sliding window size and the prefix
                                                                 or window_left for sliding window) directly into its API
length is 256. The number of documents is 12.
                                                                 (e.g., plan()), which the CUDA kernel evaluates inline. In
We also use attention variants that are not supported by Flex-   contrast, while FlexAttention employs an LRU cache (keyed
Attention: differential attention (Ye et al., 2024) and row-     on tensor shapes and mask definitions) to construct and reuse
wise gated self-attention in AlphaFold’s Evoformer (Jumper       sparse block masks across forward passes, the attention ker-
et al., 2021). We will call these DiffAttn and Evoformer         nel must still fetch and evaluate this block mask from mem-
respectively in the rest of this section. DiffAttn is shown in   ory to determine sparsity. On the other hand, Flashlight and
Listing 4. Evoformer uses an additional (sequence length)        FlexAttention achieve higher performance than FlashInfer
dimension and adds two bias matrices before sof tmax, one        for ALiBi positional encodings. Due to FlexAttention’s com-
of which needs to be broadcasted along that dimension. For       pilation model, PyTorch’s torch.compile (via Triton)
DiffAttn, we use the same configuration for the MHA vari-        seamlessly fuses the score_mod function into the kernel.
ants above except that we also evaluate 16 heads and 128         For example, the ALiBi slope calculation in FlexAttention
head dimension. For Evoformer, we vary the batch size from       is evaluated at compile-time or folded into fast in-register
1 to 32 and use 256 for the two sequence length dimensions;      math. The FlashInfer implementation, however, either com-
we use 4 heads and evaluate 64 and 128 head dimensions.          putes the bias element-wise with high overhead, or passes
                                                                 the pre-computed ALiBi slopes as a separate buffer to the
Models: To evaluate end-to-end inference, we use two
                                                                 pre-compiled FlashAttention backend, incurring a global
models: AlphaFold2 (Jumper et al., 2021) and LLaMa-3.2-
                                                                 memory read penalty per block that FlexAttention avoids.
1B (Dubey et al., 2024). AlphaFold2 is a scientific model
used to predict the structure of proteins and it contains        We omit torch.compile in Figures 2 and 3 because they are
attention variants that are not supported by FlexAttention.      much slower than FlexAttention and FlashInfer in almost
We modify attention in LLaMa-3.2-1B, a large language            all cases (these results are included in Figures 6 and 7 in
model, to variants that are supported by FlexAttention.          Appendix B). The only exceptions are: (1) ALiBi, where
                                                                 FlashInfer is slower due to the overheads described above;
4.2   FlexAttention-Supported Attention Variants                 and (2) all block_mask variants for batch size 16 and
                                                                 32, where FlexAttention is slower due to the overheads in
Figures 2 and 3 show the runtimes for FlexAttention-             creating the block_mask. In both cases, F LASH L IGHT is
supported attention variants in FlexAttention, FlashInfer,       still faster than torch.compile.
and F LASH L IGHT on H100 and A100 respectively.
The runtime for FlexAttention is split into Block-Mask cre-      4.3   Complex Attention Variants
ation and Kernel execution times. The text on the bars show
                                                                 For attention variants that are not supported by FlexAt-
the speedup of F LASH L IGHT over FlexAttention. FlexAt-
                                                                 tention, Figure 4 show the runtimes of F LASH L IGHT and
tention is marginally faster than F LASH L IGHT for Vanilla
                                                                 torch.compile on H100 and A100. F LASH L IGHT is always
in some cases and for batch size 1 of ALiBi MHA on H100.
                                                                 faster than torch.compile. For DiffAttn, the speedup of
In all the other cases, F LASH L IGHT is similar or much
                                                                 F LASH L IGHT over torch.compile is higher on H100 than
faster. For score_mod variants, F LASH L IGHT may be up
                                                                 on A100. For Evoformer, the speedups are 5× or more on
to 1.48× faster because the fused Triton kernel is simpler
                                                                 both H100 and A100.
than that of FlexAttention because it does not have compute
or memory instructions needed for handling full, partial, or
empty blocks in FlexAttention’s templatized kernel. For          4.4   End-to-End Inference Latency
block_mask variants, FlexAttention’s Kernel execution            To evaluate end-to-end inference for models that contain
is always faster than F LASH L IGHT’s execution because it       complex attention variants that are not supported by Flex-
skips redundant computation by using a sparse block mask.        Attention, we use the AlphaFold2 (Jumper et al., 2021)
However, FlexAttention’s Block-Mask execution time to            model in the OpenFold (Ahdritz et al., 2022) repo. The
construct the sparse block mask is much slower. This can         model has 48 Evoformer layers. We evaluate its inference
                        Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

                                                                     5   R ELATED W ORK
                                                                     Efficient Attention Kernels. FlashAttention (Dao et al.,
                                                                     2022; Dao, 2023; Shah et al., 2024) introduced tiling and
                                                                     kernel fusion to reduce memory traffic while keeping exact
                                                                     results. FlexAttention (Dong et al., 2024) extended this idea
                                                                     with static templates for specialized attention patterns, and
                                                                     FlashInfer (Ye et al., 2025) applied similar techniques to
                                                                     speed up LLM inference. Unlike these systems, Flashlight
                                                                     is built directly into the PyTorch compiler, automatically
                                                                     generating optimized kernels during torch.compile.
                                                                     Flexible or Specialized Attention Models. Recent work
Figure 5. Mooncake conversation trace in vLLM for LLaMa-3.2-         on attention variants, including Longformer (Beltagy et al.,
1B inference with different attention variants using F LASH L IGHT   2020), ALiBi (Press et al., 2022), and DiffTransformer (Ye
and FlexAttention on H100.                                           et al., 2024), demonstrated how to improve efficiency and
                                                                     robustness through sparsity, positional biasing, or continu-
                                                                     ous updates. These motivate the need for a general compiler
latency with a sequence length of 256 (for both sequence             framework that could automatically optimize new attention
length dimensions) and vary the batch size (the number of            types without hand-written kernels.
sequences) from 1, 2, 4, ..., 32. Evoformer uses 8 heads and
head dimension 32; Invariant Point Attention uses 12 heads           Compiler Infrastructure for Deep Learning. Tri-
and head dimension 16. We evaluate PyTorch as-is without             ton (Tillet et al., 2019) provides the GPU DSL used by
using the compiler. We add a torch.compile step to                   TorchInductor. PolyBlocks (Bondhugula et al., 2026), a con-
the row-wise and column-wise gated self attentions in Evo-           current work, is a MLIR-based compiler that fuses attention
former and then evaluate it without and with F LASH L IGHT.          using affine access analysis. Other compiler frameworks,
There is negligible difference in inference latency between          such as TVM (Chen et al., 2018), which relies on autotun-
PyTorch and torch.compile. F LASH L IGHT improves the                ing, and Mirage (Wu et al., 2025), which uses program
inference latency by 6% to 9% on both H100 and A100                  synthesis to generate new kernels, have similar goals, but
GPUs.                                                                they are outside the PyTorch ecosystem. DSLs like Thun-
                                                                     derKittens (Spector et al., 2024) explore kernel fusion and
To evaluate end-to-end inference for models that contain             scheduling for simpler and faster GPU programming.
attention variants that are supported by FlexAttention, we
use LLaMa-3.2-1B (Dubey et al., 2024), which uses Vanilla
attention. We modify it to use Causal and Softcap attention          6   C ONCLUSION AND F UTURE W ORK
variants generating two new model variants. We use the               The success of attention in LLMs has inspired researchers
Mooncake (Qin et al., 2025) conversation trace to evaluate           to design many other attention models, but these are not
these models in the vLLM system (Kwon et al., 2023) on               supported well in existing frameworks. F LASH L IGHT is
the H100 GPU. We run only the first 200 requests in the              a compiler-native framework within the PyTorch ecosys-
trace (after a warmup run of the same requests). Figure 5            tem that automatically generates fused, FlashAttention-style
shows the time to first token latency (TTFT), inter-token            kernels for arbitrary attention programs, without relying
latency (ITL), and token throughput. For TTFT and ITL,               on static templates or predefined kernel specializations. It
lower is better; for token throughput, higher is better. Note        supports all attention variants expressible in the FlexAtten-
that PyTorch and default torch.compile run out-of-memory             tion model but also handles more general, data-dependent
for these model variants because the attention variants are          attention formulations beyond the capabilities of FlexAtten-
not fused (which leads to the materialization of the interme-        tion. Our results show that F LASH L IGHT produces kernels
diate attention tensor). F LASH L IGHT is more performant            with competitive or superior performance to expert-tuned
than FlexAttention for Softcap as its kernel execution time          implementations, while offering the flexibility of native Py-
is faster than that of FlexAttention, as shown in Figure 2.          Torch code, enabling developers to rapidly explore new
On the other hand, for block_mask variants like Causal,              attention models without sacrificing performance. While
FlexAttention’s kernel execution time is faster than that of         we evaluated F LASH L IGHT only for attention variants in AI
F LASH L IGHT and its sparse block mask creation time is             inference, the extensions and passes in F LASH L IGHT are
amortized over multiple calls to the kernel (for the same            general-purpose and can enable fusion in other use cases.
tensor shapes), so FlexAttention is more performant. Ex-             F LASH L IGHT could be extended to support training work-
tending F LASH L IGHT to better optimize for such structured         loads and to optimize for block sparsity in attention.
block sparsity is left for future work.
                     Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

ACKNOWLEDGEMENTS                                             R EFERENCES
The UT Austin team was supported in part by NSF grant        Abadi, M., Barham, P., Chen, J., Chen, Z., Davis, A., Dean,
2505085. This project is partially supported by the Natu-      J., Devin, M., Ghemawat, S., Irving, G., Isard, M., Kud-
ral Sciences and Engineering Research Council of Canada        lur, M., Levenberg, J., Monga, R., Moore, S., Murray,
(NSERC) [funding reference number 587440-2024]. This           D. G., Steiner, B., Tucker, P., Vasudevan, V., Warden, P.,
research was supported through cyber-infrastructure re-       Wicke, M., Yu, Y., and Zheng, X. Tensorflow: a system
search resources and services provided by the Partnership      for large-scale machine learning. In Proceedings of the
for an Advanced Computing Environment (PACE) at the           12th USENIX Conference on Operating Systems Design
Georgia Institute of Technology.                               and Implementation, OSDI’16, pp. 265–283, USA, 2016.
                                                               USENIX Association. ISBN 9781931971331.

                                                             Ahdritz, G., Bouatta, N., Floristean, C., Kadyan, S., Xia,
                                                               Q., Gerecke, W., O’Donnell, T. J., Berenberg, D., Fisk,
                                                               I., Zanichelli, N., Zhang, B., Nowaczynski, A., Wang,
                                                               B., Stepniewska-Dziubinska, M. M., Zhang, S., Ojewole,
                                                              A., Guney, M. E., Biderman, S., Watkins, A. M., Ra, S.,
                                                               Lorenzo, P. R., Nivon, L., Weitzner, B., Ban, Y.-E. A.,
                                                               Sorger, P. K., Mostaque, E., Zhang, Z., Bonneau, R., and
                                                              AlQuraishi, M. OpenFold: Retraining AlphaFold2 yields
                                                               new insights into its learning mechanisms and capacity
                                                               for generalization. bioRxiv, 2022. doi: 10.1101/2022.
                                                              11.20.517210. URL https://www.biorxiv.org/
                                                               content/10.1101/2022.11.20.517210.

                                                             Ansel, J., Yang, E., He, H., Gimelshein, N., Jain, A., Voz-
                                                               nesensky, M., Bao, B., Bell, P., Berard, D., Burovski, E.,
                                                               Chauhan, G., Chourdia, A., Constable, W., Desmaison,
                                                              A., DeVito, Z., Ellison, E., Feng, W., Gong, J., Gschwind,
                                                               M., Hirsh, B., Huang, S., Kalambarkar, K., Kirsch, L.,
                                                               Lazos, M., Lezcano, M., Liang, Y., Liang, J., Lu, Y.,
                                                               Luk, C. K., Maher, B., Pan, Y., Puhrsch, C., Reso, M.,
                                                               Saroufim, M., Siraichi, M. Y., Suk, H., Zhang, S., Suo, M.,
                                                              Tillet, P., Zhao, X., Wang, E., Zhou, K., Zou, R., Wang,
                                                              X., Mathews, A., Wen, W., Chanan, G., Wu, P., and
                                                               Chintala, S. Pytorch 2: Faster machine learning through
                                                               dynamic python bytecode transformation and graph com-
                                                               pilation. In Proceedings of the 29th ACM International
                                                              Conference on Architectural Support for Programming
                                                              Languages and Operating Systems, Volume 2, ASPLOS
                                                              ’24, pp. 929–947, New York, NY, USA, 2024. Associa-
                                                               tion for Computing Machinery. ISBN 9798400703850.
                                                               doi: 10.1145/3620665.3640366. URL https://doi.
                                                               org/10.1145/3620665.3640366.

                                                             Beltagy, I., Peters, M. E., and Cohan, A. Longformer:
                                                               The long-document transformer, 2020. URL https:
                                                               //arxiv.org/abs/2004.05150.

                                                             Bondhugula, U., Baviskar, A., Katel, N., Patel, V., JS, A.,
                                                               and Dutta, A. Polyblocks: A compiler infrastructure
                                                               for ai chips and programming frameworks, 2026. URL
                                                               https://arxiv.org/abs/2603.06731.

                                                             Chen, T., Moreau, T., Jiang, Z., Zheng, L., Yan, E., Shen, H.,
                                                               Cowan, M., Wang, L., Hu, Y., Ceze, L., et al. {TVM}: An
                      Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

  automated {End-to-End} optimizing compiler for deep           Press, O., Smith, N. A., and Lewis, M. Train short, test
  learning. In 13th USENIX Symposium on Operating Sys-            long: Attention with linear biases enables input length ex-
  tems Design and Implementation (OSDI 18), pp. 578–594,          trapolation, 2022. URL https://arxiv.org/abs/
  2018.                                                           2108.12409.

Dao, T. Flashattention-2: Faster attention with better par-     Qin, R., Li, Z., He, W., Zhang, M., Wu, Y., Zheng,
  allelism and work partitioning, 2023. URL https:                W., and Xu, X. Mooncake: Trading more storage
 //arxiv.org/abs/2307.08691.                                      for less computation — a KVCache-centric architec-
                                                                  ture for serving LLM chatbot. In 23rd USENIX Con-
Dao, T., Fu, D. Y., Ermon, S., Rudra, A., and Ré, C. Flashat-     ference on File and Storage Technologies (FAST 25), pp.
  tention: Fast and memory-efficient exact attention with io-     155–170, Santa Clara, CA, 2025. USENIX Association.
  awareness, 2022. URL https://arxiv.org/abs/                     URL https://www.usenix.org/conference/
  2205.14135.                                                     fast25/presentation/qin.

Dong, J., Feng, B., Guessous, D., Liang, Y., and He,            Reed, J., DeVito, Z., He, H., Ussery, A., and Ansel,
  H. Flex attention: A programming model for gener-               J. torch.fx: Practical program capture and transfor-
  ating optimized attention kernels, 2024. URL https:             mation for deep learning in python. In Marculescu,
 //arxiv.org/abs/2412.05496.                                      D., Chi, Y., and Wu, C. (eds.), Proceedings of Ma-
                                                                  chine Learning and Systems, volume 4, pp. 638–651,
Dubey, A., Koura, P. S., Singh, A., Feng, S., Xue, J.,            2022.     URL https://proceedings.mlsys.
  Shah, A., Huang, H., Chen, L., Kanigal, A., Chennabas-          org/paper_files/paper/2022/file/
  appa, S., et al. The Llama 3 herd of models. arXiv              7c98f9c7ab2df90911da23f9ce72ed6e-Paper.
  preprint arXiv:2407.21783, 2024. URL https://                   pdf.
  arxiv.org/abs/2407.21783.
                                                                Shah, J., Bikshandi, G., Zhang, Y., Thakkar, V., Ramani, P.,
He, H., Guessous, D., Liang, Y., and Dong, J. Flexattention:      and Dao, T. Flashattention-3: Fast and accurate attention
  The flexibility of pytorch with the performance of flashat-     with asynchrony and low-precision, 2024. URL https:
  tention, Aug 2024. URL https://pytorch.org/                     //arxiv.org/abs/2407.08608.
  blog/flexattention/.
                                                                Spector, B. F., Arora, S., Singhal, A., Fu, D. Y., and Ré,
                                                                  C. Thunderkittens: Simple, fast, and adorable ai ker-
James Bergstra, Olivier Breuleux, Frédéric Bastien, Pascal
                                                                  nels, 2024. URL https://arxiv.org/abs/2410.
  Lamblin, Razvan Pascanu, Guillaume Desjardins, Joseph
                                                                  20399.
  Turian, David Warde Farley, and Yoshua Bengio. Theano:
  A CPU and GPU Math Compiler in Python. In Stéfan              Sun, Y., Ye, T., Dong, L., Xia, Y., Chen, J., Gao, Y., Cao,
  van der Walt and Jarrod Millman (eds.), Proceedings of          S., Wang, J., and Wei, F. Rectified sparse attention, 2025.
  the 9th Python in Science Conference, pp. 18 – 24, 2010.        URL https://arxiv.org/abs/2506.04108.
  doi: 10.25080/Majora-92bf1922-003.
                                                                Tillet, P., Kung, H. T., and Cox, D. Triton: an inter-
Jumper, J., Evans, R., Pritzel, A., Green, T., Figurnov, M.,      mediate language and compiler for tiled neural net-
  Ronneberger, O., Tunyasuvunakool, K., Bates, R., Žídek,         work computations. In Proceedings of the 3rd ACM
  A., Potapenko, A., et al. Highly accurate protein structure     SIGPLAN International Workshop on Machine Learn-
  prediction with alphafold. Nature, 596(7873):583–589,           ing and Programming Languages, MAPL 2019, pp.
  2021. doi: 10.1038/s41586-021-03819-2.                          10–19, New York, NY, USA, 2019. Association for Com-
                                                                  puting Machinery. ISBN 9781450367196. doi: 10.
Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,           1145/3315508.3329973. URL https://doi.org/
 C. H., Gonzalez, J. E., Zhang, H., and Stoica, I. Efficient      10.1145/3315508.3329973.
 memory management for large language model serving
 with PagedAttention. In Proceedings of the 29th Sympo-         Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J.,
 sium on Operating Systems Principles (SOSP ’23), pp.             Jones, L., Gomez, A. N., Kaiser, Ł., and Polosukhin, I.
 611–626. Association for Computing Machinery, 2023.              Attention is all you need. In Guyon, I., Luxburg, U. V.,
 doi: 10.1145/3600006.3613162.                                    Bengio, S., Wallach, H., Fergus, R., Vishwanathan, S.,
                                                                  and Garnett, R. (eds.), Advances in Neural Information
Milakov, M. and Gimelshein, N. Online normalizer calcula-         Processing Systems, volume 30. Curran Associates, Inc.,
 tion for softmax, 2018. URL https://arxiv.org/                   2017. URL https://proceedings.neurips.
 abs/1805.02867.                                                  cc/paper_files/paper/2017/file/
                      Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

  3f5ee243547dee91fbd053c1c4a845aa-Paper.
  pdf.
Wu, M., Cheng, X., Liu, S., Shi, C., Ji, J., Ao, M. K.,
 Velliengiri, P., Miao, X., Padon, O., and Jia, Z. Mirage:
 A {Multi-Level} superoptimizer for tensor programs. In
 19th USENIX Symposium on Operating Systems Design
 and Implementation (OSDI 25), pp. 21–38, 2025.

Ye, T., Dong, L., Xia, Y., Sun, Y., Zhu, Y., Huang, G., and
  Wei, F. Differential transformer, 2024. URL https:
  //arxiv.org/abs/2410.05258.
Ye, Z., Chen, L., Lai, R., Lin, W., Zhang, Y., Wang, S.,
  Chen, T., Kasikci, B., Grover, V., Krishnamurthy, A.,
  and Ceze, L. Flashinfer: Efficient and customizable
  attention engine for llm inference serving, 2025. URL
  https://arxiv.org/abs/2501.01005.
                       Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants

A     A LGEBRAIC T RANSFORMATION OF
      R EDUCTIONS                                                ds[0] = 0
                                                                 ds[j] = ds[j−1] ⊕ (E(x[j] ⊕ (⊖m[N ]))) |N ≥ j ≥ 1
The online softmax algorithm (Milakov & Gimelshein,                   = ds[j−1] ⊕ (E(x[j]) ⊗ E(⊖m[N ])) |N ≥ j ≥ 1
2018) (Alg. 2) is key to implementing FlashAttention-like
fused kernel. Machine learning developers have to explic-      It is easy to show by induction that ds can be expressed    in
itly replace the standard stable softmax implementation with
                                                                                                                  L
                                                               closed-form by the following expression in which       stands
online softmax. However, modern compilers for machine          for the application of the associative operation ⊕ to a set of
learning frameworks, including the PyTorch compiler, do        elements of A.
not currently detect a pattern as such and therefore do not
generate an online implementation automatically.We show                    j
                                                                           M
that the conversion of the stable softmax algorithm to the       ds[j] =         (E(x[i] ⊕ (⊖m[N ]))) |N ≥ j ≥ 1
                                                                           i=1
online softmax algorithm can be generalized using the stan-                j
dard algebraic notion of a homomorphism. Informally, a                =
                                                                           M
                                                                                 (E(x[i]) ⊗ E(⊖m[N ])) |N ≥ j ≥ 1
homomorphism is a structure-preserving map between two                     i=1

algebraic structures of the same type such as two groups.               (E is a homomorphism)
                                                                         M j          
                                                                      =         E(x[i]) ⊗ E(⊖m[N ])
Definition 2 Let A be a set with a binary operation ⊕, and                   i=1

let B be a set with a binary operation ⊗. A function f :                   (from distributivity of ⊗ over ⊕)
A→B is said to be a homomorphism if for all a1 , a2 ∈ A,
f (a1 ⊕ a2 ) = f (a1 ) ⊗ f (a2 ).                              The online softmax algorithm (Alg. 2) computes a different
                                                               sequence, denoted by do, that can be expressed abstractly
In the context of softmax, A = B = R (the set of real          as shown below.
numbers), and ⊕ and ⊗ are addition (+) and multiplication
                                                                 do[0] = 0                                                (8)
(×) of real numbers. The function f (x) = ex is a homomor-                                           
phism because f a+b = f a × f b . To generalize the online       do[j] = do[j−1] ⊗ E(m[j−1] ⊕ (⊖m[j])) ⊕
                                                                                                                          (9)
softmax construction, we need the set A with operations ⊕                  (E(x[j] ⊕ (⊖m[j]))) |N ≥ j ≥ 1
and ⊗ to satisfy the axioms of a ring:                                                               
                                                                    = do[j−1] ⊗ E(m[j−1]) ⊗ E(⊖m[j]) ⊕
                                                                                                                         (10)
    • ⊕ is associative, there is an element 0 ∈ A such that                        (E(x[j] ⊕ (⊖m[j]))) |N ≥ j ≥ 1
      a ⊕ 0 = 0 ⊕ a = a, and every element a has an                              (from distributivity of ⊗ over ⊕)

      additive inverse, denoted by ⊖a, such that a ⊕ (⊖a) =
      (⊖a) ⊕ a = 0.                                            The ds and do sequences will be different in general, but
                                                               we show that do can be expressed in closed-form by the
    • ⊗ is associative, and there is an element 1 ∈ A such     following expression.
      that a ⊗ 1 = 1 ⊗ a = a.
                                                                            j
                                                                           M                
    • ⊗ distributes over ⊕; that is, (a ⊕ b) ⊗ c = (a ⊗ c) ⊕     do[j] =           E(x[i])       ⊗ E(⊖m[j]) |N ≥ j ≥ 1   (11)
                                                                             i=1
      (b ⊗ c).
                                                            from which it follows that ds[N ] = do[N ]. The proof of
It can be shown that these assumptions imply a ⊗ 0 =        correctness of (11) is by induction on j.
0 ⊗ a = 0. The standard definition of a ring requires ⊕ to
be commutative but we do not use this property in the de- • j=1: From (10),
velopment below. In the context of rings, a homomorphism
f must also satisfy f (0) = 1.
                                                                 do[1] = (do[0] ⊗ E(m[0]) ⊗ E(⊖m[1])) ⊕ (E(x[1]) ⊗ E(⊖m[1]))
                                                                                                                       (12)
In the stable softmax algorithm (Alg. 1), let us denote the
                                                                    = E(x[1]) ⊗ E(⊖m[1]) (since do[0] = 0)               (13)
sequence of m values produced by the first loop by m[1..N ]
and let m[0] = 0 by definition. The sequence of d values
                                                            This is the value obtained from (11) for j = 1.
produced by the second loop, which we denote by ds, is
expressed abstractly by the following recurrence in which • j>1: Assume inductively that
the elements of ds are members of a ring A and E:A→A is                             j−1        
a homomorphism.                                                          do[j−1] =
                                                                                     M
                                                                                         E(x[i]) ⊗ E(⊖m[j−1])
                                                                                                 i=1


                                                               From (10),
                              Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants


                                                
     do[j] =       do[j−1] ⊗ E(m[j−1]) ⊗ E(⊖m[j]) ⊕
                                        
             E(x[j]) ⊗ E(⊖m[j])

                j−1
                  M                 
          =               E(x[i])       ⊗ E(⊖m[j−1]) ⊗ E(m[j−1]) ⊗
                    i=1
                                          |         {z         }
                                             =1 because E is homomorphism
                                     
         E(⊖m[j]) ⊕ E(x[j]) ⊗ E(⊖m[j])

                j−1
                  M                              
          =               E(x[i])       ⊗ E(⊖m[j]) ⊕
                    i=1
                                        
             E(x[j]) ⊗ E(⊖m[j])

                j−1
                  M                            
          =               E(x[i])       ⊕ E(x[j] ⊗ E(⊖m[j])
                    i=1

          (from distributivity of ⊗ over ⊕)
             M j             
          =         E(x[i]) ⊗ E(⊖m[j]) (as required by (11))
                   i=1




B    T O R C H . C O M P I L E R ESULTS

Figures 6 and 7 shows the execution times for torch.compile
along with FlashInfer, FlexAttention, and F LASH L IGHT on
H100 and A100 GPU respectively. In almost all cases,
torch.compile is slower than the others. The only ex-
ceptions are: (1) ALiBi, where FlashInfer is slower due
to the overheads described in Sectin 4.2; and (2) all
block_mask variants for batch size 16 and 32, where
FlexAttention is slower due to the overheads in creating the
block_mask. In both cases, F LASH L IGHT is still faster
than torch.compile.
     Flashlight: A PyTorch Compiler Framework for Accelerating Attention Variants




Figure 6. Execution times on H100 for attention variants that are not supported by FlexAttention.




Figure 7. Execution times on A100 for attention variants that are not supported by FlexAttention.
