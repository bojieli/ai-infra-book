         S TREAM 2LLM: OVERLAP C ONTEXT S TREAMING AND P REFILL FOR
                       R EDUCED T IME - TO -F IRST-T OKEN


                  Rajveer Bachkaniwala 1 Chengqi Luo 1 Richard So 1 Divya Mahajan 1 Kexin Rong 1
                                                        A BSTRACT
        Context retrieval systems for LLM inference face a critical challenge: high retrieval latency creates a fundamental
        tension between waiting for complete context (poor time-to-first-token) and proceeding without it (reduced
        quality). Streaming context incrementally–overlapping retrieval with inference–can mitigate this latency, but
        doing so with concurrent requests introduces new challenges: requests contend for GPU compute and memory,
        and scheduling must adapt to dynamic context arrivals.
        We present S TREAM 2LLM, a streaming-aware LLM serving system for concurrent prefill-decode disaggregated
        deployments. S TREAM 2LLM introduces adaptive scheduling and preemption for two distinct retrieval patterns:
        append-mode (progressive context accumulation) and update-mode (iterative refinement with cache invalidation).
        It decouples scheduling decisions from resource acquisition, enabling flexible preemption strategies guided by
        hardware-specific cost models, and uses longest common prefix matching to minimize redundant computation
        when input changes dynamically. To evaluate S TREAM 2LLM, we collect two large-scale, real-world streaming
        workloads based on web crawling and approximate nearest neighbor search. Our evaluation demonstrates that
        streaming architecture delivers up to 11× TTFT improvements, with cost-aware scheduling providing critical
        benefits under memory pressure, all while maintaining throughput parity with non-streaming baselines.
        Code: https://github.com/rajveerb/stream2llm/




1       I NTRODUCTION                                                  vLLM No Context Streaming


Modern large language models (LLMs) increasingly rely                    Chunk₁        Chunk₂

on external context retrieval to provide accurate, up-to-date
                                                                                                               Prefill
responses (Gao et al., 2024; Lewis et al., 2020; Xu et al.,
2023). Context retrieval mechanisms, such as fetching rele-            Stream2LLM Context Streaming
vant pages from web search or vector search (ANNS) over
large vector databases, can take hundreds of milliseconds to             Chunk₁        Chunk₂
                                                                                                                    TTFT
seconds. This creates a fundamental tension between two                                                           Reduction!
undesirable outcomes: the system can either wait for all                             Prefill₁       Prefill₂

context to arrive, which increases time-to-first-token and                                      Time
degrades user interactivity, or begin generation early with        Figure 1. Context streaming overlaps retrieval with prefill, reduc-
partial context, which can reduce response quality.                ing TTFT by beginning inference as chunks arrive.
A natural approach is to stream context incrementally, feed-       ory improves throughput and profit margins but directly
ing chunks to the model as they arrive rather than waiting for     increases time-to-first-token (key metric for responsiveness).
retrieval to complete (Figure 1). In single-request settings,      Moreover, context arrives asynchronously and at varying
this can significantly reduce latency (Jiang et al., 2024; Yu      rates across requests, requiring the system to dynamically
et al., 2025). Production deployments, however, must bal-          adapt scheduling while managing competition for shared
ance per-user interactivity against serving cost: batching         GPU resources.
concurrent requests onto shared GPU compute and mem-
                                                                   Streaming with concurrent requests introduces three inter-
    1
    Georgia Tech, Atlanta, USA. Correspondence to: Rajveer         related challenges. First, GPU memory becomes contested:
Bachkaniwala <rr@gatech.edu OR @rajveerbach on X.com>.             each request maintains KV cache blocks for its growing
Proceedings of the 9 th MLSys Conference, Seattle, WA, USA,
                                                                   input, and memory exhaustion forces preemption—either
2026. Copyright 2026 by the author(s).                             swapping cache to CPU or discarding it for later recompu-
                                                                   tation. Second, the scheduler must decide which requests
                         Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

        Append Mode                         Update Mode              We conduct a comprehensive evaluation using real-world
                                                                     streaming traces collected from web crawling and ANNS-
 t1 :   c1                           t1 :    c1      c2       q      based retrieval systems, capturing both append-mode and
                                                                     update-mode retrieval patterns. Experiments on H100 and
 t2 :   c1         c2                t2 :    c1      c′2      q      H200 GPUs with Llama-3.1 models show that while stream-
                                                                     ing architecture provides substantial benefits across differ-
        LCP                                 LCP
                                                                     ent schedulers, intelligent scheduling becomes increasingly
 t3 :    c1        c2     c3 + q     t3 :    c′1     c′′2     q
                                                                     important as load increases. At low loads, all streaming
                                                   LCP = ∅           schedulers converge to similar performance, improving time-
             LCP
                                                                     to-first-token by up to 3.9-11.0× over non-streaming base-
  Cache grows incrementally.           Frequent recomputation.
                                                                     lines. However, as concurrency and memory contention
      No invalidations.               Larger invalidation regions.
                                                                     increase, scheduler choice becomes critical: proper request
                                                                     prioritization strategies enable efficient cache reuse and
                   Preserved       Invalidated          New
                                                                     maintain responsiveness, while naı̈ve scheduling causes
                                                                     catastrophic tail latency (up to 10× worse under extreme
Figure 2. S TREAM 2LLM supports two retrieval patterns–append-       memory pressure). These findings hold across GPU archi-
mode and update-mode–and uses longest common prefix (LCP)
                                                                     tectures and workload types, demonstrating that streaming is
matching to minimize cache invalidation by preserving shared
                                                                     necessary but insufficient; intelligent scheduling and cache
prefixes across input updates.
                                                                     management is essential for making streaming viable in
to prioritize as context chunks arrive at different times, bal-      production deployments.
ancing responsiveness, fairness, and cache locality. Third,          In summary, this paper makes the following contributions:
context retrieval itself exhibits two distinct patterns (Fig-
ure 2): append-mode, where chunks progressively extend                   • We present S TREAM 2LLM, a system that extends
the input, and update-mode, where the context set changes                  vLLM to support concurrent streaming inputs in LLM
iteratively as search algorithms refine results. Naı̈ve ap-                inference, handling both append-mode (progressive
proaches that treat all streaming requests uniformly suffer                context accumulation) and update-mode (iterative con-
from poor scheduling or excessive cache recomputation.                     text refinement) workloads.
                                                                         • We introduce a two-phase scheduling architecture that
We present S TREAM 2LLM, a system that extends the                         decouples request prioritization from GPU memory al-
vLLM inference engine (Kwon et al., 2023) to support                       location and preemption, enabling flexible policies that
streaming inputs with concurrent requests. S TREAM 2LLM                    improve KV cache reuse. This is combined with cost-
targets the prefill stage in a prefill-decode disaggregated                based adaptive preemption (recomputation vs. swap-
deployment (Patel et al., 2024; Qin et al., 2024; Zhong et al.,            ping) and longest common prefix-based cache invalida-
2024), following industry practice. This architecture enables              tion to reduce redundant computation for dynamically
optimization for time-to-first-token (TTFT) and throughput,                changing inputs.
where streaming decisions matter most, without impacting                 • We evaluate S TREAM 2LLM using real-world, stream-
decode-stage latency, i.e., time-per-output-token (TPOT),                  ing traces from web crawling and ANNS-based re-
which is handled by separate decode instances.                             trieval systems. Results show that streaming with intel-
Specifically, S TREAM 2LLM introduces a two-phase                          ligent scheduling and cache management delivers up
scheduling architecture that decouples priority-based re-                  to 11× improvements in TTFT.
quest ordering from resource acquisition. This separation
of concerns–deciding what to run versus how to allocate              2     BACKGROUND
resources–enables flexible preemption strategies without
hardcoded policies. When memory is exhausted, the system             2.1    LLM Inference and KV Cache Management
preempts low-priority requests using cost-based decisions            Large language model inference follows an autoregressive
guided by hardware-specific performance models, choosing             generation process consisting of two distinct phases. Given
between recomputing invalidated cache and swapping cache             an input with n tokens x = [x1 , x2 , . . . , xn ], the prefill
blocks to CPU. To minimize redundant computation when                phase processes all input tokens in parallel, computing
inputs change dynamically, S TREAM 2LLM uses longest                 attention scores and generating key-value pairs (Ki , Vi )
common prefix (LCP)-based cache invalidation, preserving             for each token position i ∈ [1, n]. These KV pairs are
cache for unchanged prefix tokens while invalidating only            stored in GPU memory as the KV cache. The decode
changed portions. This mechanism uses a single cache man-            phase then generates output tokens autoregressively: at
agement strategy for both append-mode and update-mode                each step t, the model attends to all previously computed
retrieval patterns.
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

KV pairs {(K1 , V1 ), . . . , (Kn+t−1 , Vn+t−1 )} to generate           fill phase for all ℓr tokens, incurring compute cost
the next token xn+t . The newly generated token’s KV pair               Crecomp (r) = ℓr · Cprefill where Cprefill is the per-token
(Kn+t , Vn+t ) is appended to the cache, avoiding recompu-              prefill latency.
tation of attention for tokens processed in earlier steps.            • Swapping: Transfer all blocks {B1 , . . . , B⌈ℓr /k⌉ }
                                                                        from GPU to CPU memory, incurring data transfer cost
For a transformer model with L layers, hidden dimension
d, total attention heads h, and hkv key-value heads (head di-           Cswap (r) = ⌈ℓrBW/k⌉·Mblock
                                                                                            PCIe
                                                                                                     where BWPCIe is the PCIe
mension dh = d/h), each token position stores 2Ld hhkv val-             bandwidth. Upon resumption, swap blocks back to
ues across all layers (keys and values). The total KV cache             GPU with symmetric cost. The blocks preserve com-
memory for a sequence of length ℓ is MKV = 2Lℓd hhkv · b,               puted KV values, avoiding recomputation.
where b is the number of bytes per parameter (typically 2           Selecting between the two strategies requires comparing
bytes for FP16). For Meta Llama-3.1-8B with L = 32,                 Crecomp (r) versus 2 · Cswap (r) (accounting for bidirectional
d = 4096, h = 32, hkv = 8, and ℓ = 32K, this amounts to             transfer); S TREAM 2LLM uses this cost model to guide its
MKV ≈ 4.0 GB per request in FP16 precision. When serv-              adaptive preemption decisions, as described in §4.3.
ing B concurrent requests through batching, total memory
consumption becomes B · MKV . As B increases to improve             2.3   Context Retrieval for LLMs
throughput, available memory per request decreases, forcing
systems to either limit batch size or preempt requests to free      Modern LLM deployments require external context C to
KV cache.                                                           augment the model’s parametric knowledge (Lewis et al.,
                                                                    2020). Given a query q, context retrieval systems produce
2.2   vLLM and PagedAttention                                       a set of relevant documents D = {d1 , d2 , . . . , dm } that are
                                                                    concatenated with q to form the final input. Two retrieval
Traditional KV cache implementations allocate a single              mechanisms are commonly used:
contiguous memory region of size MKV for each request,
leading to memory fragmentation as requests arrive and              Web crawler retrieval: Given a query q, the system fetches
complete at different times. vLLM (Kwon et al., 2023)               documents from the web or internal knowledge bases. La-
addresses this through PagedAttention, which divides the            tency Tweb includes network round-trip time, DNS resolu-
KV cache into fixed-size blocks. For a block size of k              tion, content fetching, and parsing, typically ranging from
tokens, the KV cache for a sequence is stored as a sequence         200ms to multiple seconds per document.
of blocks B = {B1 , B2 , . . . , B⌈ℓ/k⌉ }, where each block Bj      ANNS-based retrieval: Given a query embedding eq ∈
stores KV pairs for tokens [(j − 1)k + 1, min(jk, ℓ)]. These        Rde and a corpus C = {c1 , . . . , cN } with embed-
blocks need not be contiguous in physical memory, similar           dings {ec1 , . . . , ecN }, find the k-nearest neighbors D =
to virtual memory paging in operating systems.                      top-k({ci : sim(eq , eci ) | ci ∈ C}), where sim(·, ·) is a
The attention computation in PagedAttention operates block-         similarity metric (e.g., cosine similarity or L2 distance).
wise. For computing attention at position t, the system re-         While in-memory ANNS solutions such as HNSW (Malkov
trieves all blocks {B1 , . . . , B⌈t/k⌉ } containing KV pairs for   & Yashunin, 2018) achieve low retrieval latency, storing the
positions [1, t], regardless of their physical memory loca-         full index in memory is infeasible at scale (N ∼ 109 ). Disk-
tions. This design provides two key benefits: (1) reduced           based ANNS solutions such as DiskANN (Jayaram Sub-
fragmentation by allocating blocks on-demand from a free            ramanya et al., 2019) and SPANN (Chen et al., 2021) ad-
block pool, and (2) efficient prefix sharing when multiple          dress this by storing indexes on disk, where search performs
requests share common input prefixes, as blocks containing          O(log N ) graph traversals with disk I/O at each step. They
the shared prefix can be referenced by multiple requests            are widely deployed in production due to cost: SSDs are or-
without duplication (Gim et al., 2024).                             ders of magnitude cheaper per capacity than DRAM (Karsin
                                                                    et al., 2025; Li, 2025). End-to-end retrieval latency TANNS
vLLM supports continuous batching (Yu et al., 2022), main-          ranges from 100ms to several seconds depending on corpus
taining a dynamic batch Rt = {r1 , r2 , . . . , rBt } where         size, index complexity parameter (e.g., search list size in
requests can be added or removed at any scheduling step             DiskANN), disk bandwidth, and pipeline overhead (query
t. When total KV cache memory exceeds GPU capacity                  embedding, network, post-processing).
MGPU , the system must preempt requests. For a request r
with ℓr processed tokens occupying ⌈ℓr /k⌉ blocks, preemp-          2.4   Traditional vs Streaming Inference
tion follows one of two strategies:
                                                                    Traditional inference systems construct the complete in-
  • Recomputation: Discard all KV cache blocks for                  put x = [D, q] only after retrieval finishes at time Tretrieve ,
    r, freeing ⌈ℓr /k⌉ · Mblock memory (where Mblock =              then begin the prefill phase. This results in time-to-first-
    2Lkd hhkv · b). Upon resumption, recompute the pre-             token (TTFT) of TTTFT = Tretrieve + Tprefill (|D| + |q|), where
                   Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

Tretrieve often dominates. Streaming context approaches re-      this temporal priority. Schedulers must dynamically re-rank
duce TTFT by sending context chunks incrementally. In-           requests as chunk arrival patterns shift, balancing freshness,
stead of waiting for complete D, documents arrive over time:     fairness, and preemption cost.
d1 at time t1 , d2 at time t2 , etc. The LLM begins inference
                                                                 All requests draw from the same finite pool of GPU memory
with partial context Dt = {di : ti ≤ t} and continues as
                                                                 for KV cache blocks. As input sequences grow, total cache
new documents arrive.
                                                                 usage can exceed GPU capacity, forcing the system to free
We consider two complementary streaming modes that cap-          memory through preemption. The optimal strategy–whether
ture distinct classes of retrieval workloads. In append mode,    to recompute discarded cache or to swap it to CPU memory–
context grows monotonically (Dt ⊆ Dt′ for t < t′ ), corre-       depends on request progress, hardware bandwidth ratios,
sponding to pipelines that produce results incrementally         and expected resumption time.
without revisiting earlier results. For example, in web
crawling pipelines, documents arrive as pages are retrieved      Mode-Dependent Context Evolution. Streaming retrieval
and can be streamed immediately when no global post-             workloads differ in how input sequences evolve. In web-
processing (e.g., reranking) is required. More broadly, ap-      crawler-style retrievers (append mode), requests extend the
pend mode applies whenever documents are emitted in final        existing sequences, whereas in ANNS-style retrievers (up-
form upon arrival. In update mode, the context is refined        date mode), requests replace parts of the sequence as re-
over time, with Dt evolving as higher-quality candidates         trieval refines results. These modes require different schedul-
replace earlier ones, corresponding to pipelines that progres-   ing and cache management behaviors.
sively refine retrieval results. For example, AquaPipe (Yu       In update mode, the system must determine which KV cache
et al., 2025) enables early return of partial ANNS results be-   blocks remain valid after each update. If the scheduler
fore search completion; Vespa implements phased ranking          naively invalidates all blocks, it wastes memory by discard-
where documents are progressively filtered and re-ranked         ing valid cached results and forces expensive recomputation.
across stages (Vespa Team, 2024); and distributed search         Conversely, if the scheduler reuses blocks without verifi-
engines such as Elasticsearch similarly emit partial results     cation, it risks computing incorrect output based on stale
as individual shards complete. These pipelines naturally         cache, since subsequent attention computations would at-
produce a sequence of refined top-k sets, which can be           tend to outdated key-value pairs that no longer correspond
streamed to the inference system.                                to the current input sequence. Frequent replacements early
These modes have been explored separately in prior sys-          in the sequence can lead to high recomputation costs.
tems—PipeRAG (Jiang et al., 2024) for append and                 Append and update workloads also differ in optimal preemp-
AquaPipe (Yu et al., 2025) for update—and have been eval-        tion strategies. Append-mode requests favor swapping to
uated in single-request settings (B = 1). In this work, we       preserve reusable cache, while update-mode requests often
support both modes under a unified concurrent setting.           prefer recomputation to avoid retaining soon-invalid data.

3    C HALLENGES                                                 4     S YSTEM D ESIGN
Streaming concurrent inference workloads differ fundamen-
tally from static batch inference. Requests stream context       Deployment Assumption. S TREAM 2LLM targets the
incrementally over time, evolve asynchronously, and com-         prefill instance in a prefill-decode disaggregated architec-
pete for shared GPU resources. These unique properties           ture (Qin et al., 2024), where prefill and decode run on
create several system design challenges.                         separate GPU pools. This is standard practice in production
                                                                 LLM serving (Patel et al., 2024; Zhong et al., 2024). TTFT
Dynamic Workload Variability. Each request expands               and throughput are the metrics relevant to prefill instances;
dynamically as new chunks of context arrive and generates        token generation latency (TPOT) is handled by decode in-
outputs of unpredictable length until termination tokens         stances and is not evaluated.
appear. This variability makes both compute demand and           S TREAM 2LLM addresses the challenges with concurrent,
memory footprint time-dependent, and requires dynamic            streaming workloads with two key design principles:
policies.
                                                                     • Support for two distinct streaming modes: Requests
Chunk arrivals are asynchronous: some requests stall wait-             can either append new input chunks to the existing
ing for retrieval while others suddenly receive new context.           sequence or replace parts of the input sequence entirely.
Requests that just received a chunk should be prioritized to           Each mode imposes different requirements on cache
process it while the request’s KV cache blocks remain allo-            management, scheduling, and preemption.
cated in GPU memory, but traditional scheduling policies             • Decoupling scheduling from resource acquisition:
(e.g., FCFS or progress-based heuristics) cannot capture
                         Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

                                                                                  In a monolithic loop, these operations interleave with
       Phase 1 Priority Ordering & Feasibility
                                                                                  scheduling and allocation, making it unclear whether
                                                      Feasibility check:          to invalidate before or after attempting allocation.
  Unfinished       Scheduling Priority
                                                         1. Token Budget
  Requests
                   LCAS | FCFS | MCPS | …             2. GPU Block Budget    S TREAM 2LLM decouples scheduling and resource acqui-
                                                                             sition into two phases that explicitly separate concerns:
                                                      Scheduled Requests     Phase 1: Priority Ordering and Feasibility Analysis. The
        Phase 2 Resource Acquisition & Preemption                           scheduler invokes the selected scheduling algorithm (§ 4.4)
                                                                             to compute an ordered list of all unfinished requests ranked
             Adaptive Preemption            Failure        GPU Block         by priority. It then performs a feasibility analysis that deter-
               Eviction Strategy                           Allocator
                                            OOM                            mines which requests can theoretically be scheduled given
                                                                   Success
                                                                             the current token budget (i.e., the maximum number of
                      Swap             Recom
                                             pute                            tokens that can be processed in a scheduling step) and es-
      Allocated
                     CPU Block Pool                      GPU Block Pool      timated GPU block requirements. Specifically, for each
      Recompute
                                                                             request in priority order, the scheduler estimates the num-
      Swap                                  Swap
                                                                             ber of KV cache blocks required (based on computed and
      Free
                                                                             new tokens) and checks whether sufficient free GPU blocks
Figure 3. Overview of S TREAM 2LLM’s two-phase scheduler that                remain; if not, the request is marked infeasible. Crucially,
separates scheduling decisions from resource acquisition. Phase 1            this phase performs no resource allocation and modifies no
determines request priority and feasibility, while Phase 2 allocates         request state—it only computes feasibility. Requests that
KV cache blocks and applies preemption under memory pressure.                cannot fit within current resources are added to a not -
                                                                             scheduled reqs list while preserving their priority.
                                                                             Phase 2: Resource Acquisition with Adaptive Preemp-
        S TREAM 2LLM separates the decision of which re-                     tion. The scheduler attempts to allocate GPU blocks
        quest to prioritize from the mechanics of allocating                 for each request selected in Phase 1. If allocation fails,
        GPU memory and applying preemption. This decou-                      the scheduler selects a preemption candidate from the
        pling enables sophisticated policies that maximize KV                not scheduled reqs list in reverse priority order (i.e.,
        cache reuse while adapting to dynamic workloads.                     lowest-priority first). Because requests in not sched-
The subsequent subsections introduce the two-phase                           uled reqs may still hold KV cache blocks from previous
scheduling design (§ 4.1), cache invalidation mechanisms                     scheduling steps, preempting them releases those blocks,
(§ 4.2), cost-based preemption strategies (§ 4.3), and                       freeing GPU memory to proceed with allocation for higher-
scheduling policies (§ 4.4).                                                 priority requests. The scheduler then applies the decision
                                                                             framework from § 4.3 to choose between recomputation and
4.1      Two-Phase Scheduling and Request Lifecycle                          swapping as the eviction strategy.

A key insight in S TREAM 2LLM is the separation of                           This two-phase structure separates what to run from how to
scheduling decisions from resource acquisition. Rather than                  allocate resources and provides several benefits. The separa-
embedding both in a single control loop, S TREAM 2LLM                        tion ensures that priority decisions are made independently
organizes them into two distinct phases, allowing scheduling                 of resource constraints, and preemption naturally follows the
policies to reason about priorities independently of alloca-                 scheduling priority order without requiring policy-specific
tion and preemption mechanics. Figure 3 illustrates this                     rules. Cache invalidation occurs at precise boundaries be-
two-phase architecture.                                                      tween analysis and allocation. The resource-acquisition
                                                                             phase can also flexibly integrate cost-, or latency-aware
When scheduling, allocation, and preemption are tightly                      strategies without altering core scheduling logic.
coupled, several challenges arise:
                                                                             Request Lifecycle. The scheduler tracks requests through
  • Fixed Policy Coupling. A monolithic loop forces the
                                                                             three primary queues: waiting (arrived but not yet sched-
    scheduler to preempt immediately when resources are
                                                                             uled), running (currently in execution or scheduled in the
    exhausted, using a static rule that may contradict the
                                                                             current batch), and finished (completed). Transitions
    policy’s notion of priority. This prevents different
                                                                             between these queues are governed by the two-phase design
    scheduling algorithms from expressing distinct pre-
                                                                             (Figure 4). Requests move from waiting to running
    emption behaviors.
                                                                             when selected in Phase 1 and allocated in Phase 2. If a
  • Cache Invalidation Coupling. When an input sequence
                                                                             running request is preempted, it returns to waiting: re-
    is updated, two things must happen: the token count
                                                                             computation resets its progress, while swapping preserves
    increases and cached KV blocks may become invalid.
                           Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

                    Phase 1: selected
                    Phase 2: allocated
      Waiting                            Running        completion
        (not yet                         (executing /                finished
      scheduled)                           batched)
                   Phase 2: preempted

        Figure 4. S TREAM 2LLM request state transitions.

state by offloading KV blocks to CPU memory. Upon com-
pletion, requests transition from running to finished.

4.2    KV Cache Invalidation for Streaming Inputs                               Figure 5. Performance models for recomputation vs. total swap
                                                                                latency costs across token counts on H200 and A40.
S TREAM 2LLM supports two input sequence modification
modes suited to different context retrieval patterns:                           cache for token 0 (document chunk 1).
  • Append Mode: New input chunks are appended to the                           For analysis, S TREAM 2LLM tracks the total number of
    existing sequences, typical in crawler-style workloads.                     tokens invalidated across all input updates per request via
  • Update Mode: The entire input sequence is updated                           total tokens invalidated. This metric is reported
    dynamically, typical in ANNS-style workloads.                               when the request finishes, allowing researchers to measure
                                                                                the cost of cache invalidation in context retrieval workloads.
A unique challenge for streaming input workloads is that                        High invalidation counts indicate frequent or aggressive
the input token sequence itself changes dynamically. When                       input updates, suggesting that the system is spending signif-
a new input chunk arrives, the scheduler must decide which                      icant effort on recomputation.
cached KV blocks remain valid and which must be inval-
idated. Traditional LLM inference assumes a fixed input                         Cache Invalidation Semantics. When k tokens are invali-
at request arrival, computing KV cache blocks once for                          dated beyond the LCP, the scheduler frees the corresponding
the entire request lifetime. However, in streaming context                      KV cache blocks and returns them to the free block pool.
retrieval workloads, the input grows or changes as new doc-                     For CPU-swapped requests, the scheduler also frees the cor-
ument chunks arrive. For example, the input may evolve                          responding CPU blocks to reduce memory pressure. The
from [d1 , q] (document chunk 1 + query) to [d1 , d2 , q] as                    scheduler then sets num computed tokens to the LCP
document chunks are added, or from [d1 , d2 , q] to [d′1 , d2 , q]              length, forcing recomputation only for tokens that changed.
when chunks are replaced in ANNS-style systems. If the
scheduler naively invalidates all KV cache blocks upon each                     The interaction between cache invalidation and preemption
input update, it wastes memory by discarding previously                         requires careful sequencing to avoid freeing blocks that are
computed blocks that remain valid and forces expensive                          about to be swapped back in. When a preempted request
recomputation. Conversely, if the scheduler reuses blocks                       receives an input update, the scheduler invalidates blocks
without verification, it risks computing incorrect output                       from the LCP onward on CPU and free these blocks from
based on stale cache.                                                           CPU memory. When the request resumes, it first swaps
                                                                                its remaining blocks (those before the LCP) back to GPU,
Longest Common Prefix Invalidation. S TREAM 2LLM                                then recomputes tokens from the LCP onward. This design
addresses this by computing the longest common prefix                           ensures that input updates do not cause memory leaks or
(LCP) between the old and new input token sequences, as                         inconsistencies in the swapped state.
illustrated in Figure 2. When a streaming request receives
a new input chunk, the engine computes the LCP and in-                          4.3   Cost-based Preemption
validates only the KV cache blocks corresponding to to-
                                                                                When GPU memory is exhausted, S TREAM 2LLM pre-
kens beyond the LCP. Blocks for tokens within the LCP
                                                                                empts requests and chooses between recomputation and
are preserved, avoiding redundant recomputation. The LCP
                                                                                swapping strategies based on cost models. S TREAM 2LLM
approach is optimal when input updates involve appending
                                                                                profiles two hardware-specific latency cost functions of-
new chunks or replacing suffix tokens, which is typical in
                                                                                fline on each target platform, such as shown in Figure 5.
context retrieval systems. It becomes less beneficial when
                                                                                recomputation latency(T ) predicts the time to recompute T
updates are frequent or affect early tokens.
                                                                                tokens via prefill. We measure prefill latency across varying
For example, suppose a request previously computed KV                           token counts (1K-128K) on each GPU (A40, H100, H200)
cache for tokens [d1 , d2 , q, output1 , output2 ], and an input                and fit a piecewise-linear model to account for memory
update replaces the input with [d1 , d′2 , q, output1 , output2 ],              bandwidth saturation. swap latency(C) predicts the time to
where d′2 ̸= d2 . The LCP is then [d1 ] (length 1). The                         swap C KV cache blocks between GPU and CPU memory.
scheduler invalidates cache blocks for tokens 1 onward (cor-                    We measure transfer latency for typical block sizes 2 MB
responding to d2 , q, and output) while preserving the KV                       (for the given model Llama 3.1 8B, and default block size of
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token


                               Table 1. Scheduling algorithm performance on streaming workloads.
                                               Append Mode                             Update Mode
                  Policy              Rating       Key Behavior             Rating          Key Behavior
                  DEFAULT vLLM         Poor     Ignores arrival timing       Poor      Ignores recomputation cost
                  FCFS                 Good     No recency awareness        Moderate     No update prioritization
                  MCPS                 Good        Tracks progress           Poor        Unstable under updates
                  LCAS                 Good     Prioritizes recent input     Good       Prioritizes recent updates


16) and account for PCIe bandwidth constraints. On systems          work to be delayed behind requests requiring significant
with NVLink or faster interconnects (e.g., NVIDIA GB300             recomputation, extending their latency unnecessarily.
and GH200 has up to 450 GB/s host–device bandwidth),
swap costs are correspondingly lower.                               4.4.2    FCFS (First-Come-First-Served)
These cost functions are hardware-specific: a recomputation         This scheduler separates requests into two tiers: full requests
that takes 50ms on H100 may take 200ms on A40 due                   (input sequence complete) scheduled in arrival order, and
to differences in compute density and memory bandwidth.             partial requests (still receiving input) scheduled opportunis-
The scheduler evaluates both cost functions at preemption           tically. Eviction removes requests in reverse scheduling
time and selects the strategy with lower predicted latency.         order when memory is exhausted.
The cost functions can be updated dynamically to adapt to
                                                                    The two-tier structure improves upon FIFO by separating
changing system characteristics (e.g., contention for PCIe
                                                                    complete from incomplete requests and prioritizing com-
bandwidth), though our current implementation uses static
                                                                    pleted requests for output generation. However, FCFS
profiles derived from idle-system benchmarks.
                                                                    shares FIFO’s limitations: within each tier, scheduling uses
                                                                    only arrival order and ignores both chunk arrival recency
4.4     Scheduling and Eviction Policies
                                                                    and recomputation costs. Requests with older arrivals are
The scheduling algorithm determines request execution or-           prioritized over newer requests with fresh input or high-cost
der. Different policies optimize different tradeoffs: through-      updates, delaying progress on active requests.
put, latency, fairness. Each policy selects its lowest-priority
request for eviction when GPU memory fills, and uses the            4.4.3    MCPS (Most Chunks Processed Scheduling)
cost model from § 4.3 to decide between recomputation and
                                                                    Requests are prioritized by the number of tokens already
swapping strategies.
                                                                    computed (num computed tokens, highest first), with
S TREAM 2LLM implements multiple scheduling algo-                   ties broken by arrival time. Eviction removes the request
rithms with different trade-offs, selected via the SCHED-           with the fewest computed tokens.
ULER TYPE variable. Table 1 summarizes each algorithm’s
                                                                    MCPS’s progress-based metric works well in append mode,
performance under different streaming workloads.
                                                                    and naturally favors completing requests before starting new
                                                                    ones. Requests receiving chunks frequently maintain high
4.4.1    DEFAULT vLLM (variant of FIFO)
                                                                    priority and make steady progress. MCPS becomes prob-
The baseline scheduler uses arrival-time ordering for the           lematic in update mode. When a request’s input sequence
waiting queue, processing new requests in FIFO order. For           is updated with a short LCP, num computed tokens re-
running requests, it maintains their current execution order        sets to the LCP length (potentially near zero). A request
without explicit arrival-time re-sorting. When GPU memory           that had computed many tokens suddenly drops to lowest
is exhausted, it preempts the last request in the running           priority, even though significant work has been done. This
queue (LIFO eviction). Preempted requests are re-queued             wastes prior computation and delays progress on requests
at the front of the waiting queue, bypassing newly arrived          undergoing frequent updates with short LCPs.
requests to maintain priority.
                                                                    4.4.4    LCAS (Last Chunk Arrival Scheduling)
Default vLLM ignores both when input chunks arrive and
when sequences are updated. A request that arrived early            This scheduler combines two strategies: (1) separating com-
but has been idle receives higher priority than a request that      plete and partial requests into two tiers, and (2) ordering both
arrived later but has freshly received input or updates, delay-     tiers by most recent chunk arrival time (last chunk ar-
ing progress on active requests. In update mode, DEFAULT            rival time, most recent first). Eviction removes the
vLLM prioritizes based on arrival time rather than recom-           request with the oldest chunk arrival.
putation cost. This causes requests with less recomputation
                                                                    LCAS provides strong performance in append mode by pri-
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

oritizing requests with recent chunk arrivals. When a chunk       # e is engine object
                                                                  # append mode
is appended at time t, the request is boosted to highest prior-   e.new_stream(rid="q1", ids=[query],...)
ity. The scheduler processes the expanded input sequence          for chunk in chunks:
                                                                      e.append(rid="q1", ids=[chunk],...)
immediately while prior context is warm, maximizing effi-         e.append(rid="q1", ..., finished=True)
ciency. Requests transition from partial to complete tier as      # update mode
                                                                  e.new_stream(rid="q2", ids=docs1+[q],...)
input finishes, and complete requests are prioritized for out-    e.update(rid="q2", ids=docs2+[q],...)
put generation. The approach naturally handles temporally         e.update(rid="q2", ..., finished=True)
clustered chunk arrivals well.
                                                                      Listing 1. Sample API usage in append and update modes.
LCAS also performs well in update mode. When an input
sequence is updated, the request is boosted to high prior-
ity. This enables immediate recomputation of invalidated
tokens, making progress on high-cost updates quickly. The
                                                                  5.1   Public Interface
priority boost benefits requests with short LCPs.The main
weakness in both modes is potential starvation: requests          S TREAM 2LLM extends the vLLM engine interface with
with infrequent chunk arrivals may be delayed indefinitely        streaming-aware request objects and lifecycle management.
if other requests have more recent arrivals.                      The EngineCoreRequest object supports append and
                                                                  update modes via three key flags: is streaming -
5    I MPLEMENTATION AND U SAGE                                   prompt (incomplete input), is streaming prompt -
                                                                  finished (signals completion), and is prompt up-
We implement S TREAM 2LLM on top of the vLLM v1 en-               date (toggles update vs. append). Clients may append
gine, extending the core scheduler to support streaming           input chunks or replace the input sequence entirely, and
inputs and the two-phase scheduling architecture described        the engine automatically computes longest-common-prefix
in Section 4.1. The scheduler is modified to detect stream-       matches for cache invalidation.
ing request lifecycle events (initial request, input chunks,
                                                                  The driver provides convenient functions for both modes:
and completion) and invoke the KV cache manager to com-
                                                                  new stream (create initial request), append (add
pute the longest common prefix (LCP) between old and new
                                                                  chunks), and update (replace input). Listing 1 shows
inputs, invalidating only the cache blocks corresponding to
                                                                  sample usage in both modes.
changed tokens. We implement the preemption decision
framework as a cost comparison between recomputation              R EQUEST L IFECYCLE. Append mode submits an initial
and swapping strategies using performance models derived          request with is streaming prompt=True, appends
from hardware profiling. Multiple scheduling algorithms           additional chunks as they arrive, then signals completion
(Section 4.4) are implemented as priority sorting functions       with is streaming prompt finished=True. Up-
that can be selected at runtime via the SCHEDULER TYPE            date mode follows the same pattern but sets is prompt -
environment variable.                                             update=True when replacing the entire input sequence.
The KV cache manager is extended with GPU and CPU                 The engine tracks both modes transparently and manages
block pools to support both swapping and recomputation            KV cache invalidation accordingly.
preemption strategies. When a request is preempted via            O UTPUT AND T ELEMETRY. The engine returns En-
swapping, its blocks are transferred to CPU memory and            gineCoreOutput objects containing newly generated
later swapped back when the request resumes. When pre-            tokens, finish reason, and event timestamps. Events
empted via recomputation, blocks are freed and the request        (QUEUED, SCHEDULED, KV ON GPU, PREEMPTED -
recomputes affected tokens upon resumption. The evalua-           SWAP, PREEMPTED RECOMPUTE, FINISHED) enable
tion drivers (crawler and ANNS) implement the streaming           latency analysis and scheduling diagnostics. The scheduler
request lifecycle, submitting initial requests with the is -      algorithm is selected via SCHEDULER TYPE environment
streaming prompt flag, appending or updating inputs               variable (DEFAULT VLLM, FCFS, MCPS, or LCAS).
as chunks arrive (via is prompt update), and signaling
completion with is streaming prompt finished.
Event tracking records key state transitions (QUEUED,             6     E VALUATION
SCHEDULED, KV ON GPU, PREEMPTED SWAP, PRE-                        We evaluate S TREAM 2LLM on two distinct streaming
EMPTED RECOMPUTE, FINISHED) to enable detailed                    workloads: crawler-based context retrieval (append mode)
latency analysis and telemetry for streaming workloads.           and ANNS-based refinement (update mode). Our exper-
                                                                  iments demonstrate that scheduling policies designed for
                                                                  streaming inputs significantly improve responsiveness with-
                                                                  out sacrificing system throughput.
                      Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

  Metric                      Mean     P50      P75      P95
  ANNS (Update Mode, 500 queries)
  Total Tokens per Query  13K     10K           15K      31K
  Retrieval Latency (s)   4.5     3.9           5.3      8.5
  Crawler (Append Mode, 4,322 queries)
  Total Tokens per Query   9.1K    5.8K        11.3K    28.9K
  Retrieval Latency (s)     9.9     9.3         11.6     16.7

      Table 2. Summary of streaming workload characteristics.
                                                                  Figure 6. Distribution of inter-chunk arrival times for ANNS and
                                                                  Crawler workloads (log-scale). ANNS chunks arrive with a median
6.1     Experiment Setup                                          of 36.7 ms, while crawler chunks arrive with a median of 700.7 ms,
                                                                  exhibiting significantly higher variability.
Streaming Workloads. Due to the lack of large-scale, pub-
licly available streaming workloads, we collected two realis-
tic retriever traces with full response and timing information.
These traces are replayed at varying query-per-second (QPS)
rates to emulate moderate load conditions where schedul-
ing decisions are most impactful. Table 2 summarizes their
characteristics.
Update Mode. We use the Fineweb-edu corpus (index: 372
GB, vectors: 279 GB) with SQuAD queries (Rajpurkar
et al., 2016), both encoded using e5-base-v2 (Wang                Figure 7. Distribution of chunks per query. ANNS queries are
et al., 2022). We build a DiskANN index(Jayaram Subra-            heavily skewed toward 1–3 chunks, while crawler queries are
manya et al., 2019) with L2 distance and configure search         concentrated around 6–10 chunks per query.
with beam width W = 8 and list size L = 10000. This
large L increases retrieval accuracy but introduces signifi-      in crawler chunk arrivals creates extended idle periods for
cant latency, creating the high-latency, I/O-bound scenario       individual requests, motivating scheduling policies that can
S TREAM 2LLM targets. We implement AquaPipe’s recall-             dynamically re-prioritize requests as new chunks arrive.
aware prefetching (Yu et al., 2025), which progressively          Figure 7 shows the distribution of chunks per query. ANNS
refines the top-k candidate list and enables early emission       queries are heavily right-skewed, with the majority receiving
of partial results with sufficient recall.                        1–3 chunks (>50% of queries receive ≤3 chunks). Crawler
Append Mode. We use the crawl4ai Python library for               queries follow a broader, roughly unimodal distribution
core web crawling and scraping functionalities, which in-         centered around 6–10 chunks. The combination of more
cludes content pruning and link filtering with the BM25           chunks per query and higher per-chunk variability makes
algorithm to extract content only relevant to the original        the crawler workload substantially more demanding for
query (UncleCode, 2024). Each crawl explores pages up             streaming schedulers.
to depth 2 and streams retrieved content to S TREAM 2LLM
in arrival order. Queries are from OpenAI’s SimpleQA              Hardware and Configuration. Experiments run on
dataset(Wei et al., 2024), which contains 4,327 fact-seeking      NVIDIA H200 (141GB) and NVIDIA H100 (80GB) GPUs.
questions requiring web-based evidence. Our crawler work-         We use Llama-3.1-8B-Instruct as the primary model. Tensor
load applies per-document filtering and deduplication inline      parallelism is set to 2, and GPU memory utilization target
as each page is retrieved, enabling incremental streaming         is 80% (20% buffer to prevent OOM). Token budget per
without a global reranking step.                                  scheduling step varies from 2048 to 8192. S TREAM 2LLM
                                                                  extends capabilities of vLLM inference system.
Chunk Arrival Patterns. The two workloads exhibit fun-
damentally different chunk arrival patterns, which drive the      Methods of Comparison. We compare S TREAM 2LLM
scheduling challenges described in the main paper.                against two baselines: (1) vLLM-NS: default vLLM sched-
                                                                  uler without streaming support, and (2) vLLM-S: default
Figure 6 shows the distribution of inter-chunk arrival times.     vLLM scheduler with streaming support. We also evaluate
ANNS arrivals are tightly concentrated around 36.7 ms (me-        S TREAM 2LLM under its three custom scheduling and pre-
dian), with the bulk of the distribution spanning 1–1,000 ms.     emption policies–FCFS, MCPS, and LCAS–to assess the
Crawler arrivals are 19× slower (median 700.7 ms) and             impact of scheduling strategy on latency and throughput.
span approximately three orders of magnitude, from tens
of milliseconds to over 30 seconds. This high variability         Metrics. We report two key performance metrics: (1) TTFT
                   Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token




Figure 8. TTFT CCDF across load levels for the crawler and ANNS workloads on H200. Streaming achieves up to 10.8–11.0× faster
median latencies than non-streaming on the crawler workload, and up to 2.49–2.63× P95 speedups on the ANNS workload.
(Time-To-First-Token), the time from request arrival to first
generated token, which directly captures user-facing respon-
siveness, and (2) Trace Completion Time, the total wall-
clock time to complete all requests in a workload, reflecting
system throughput and end-to-end performance. All experi-
ments target the prefill instance in a disaggregated serving
configuration; decode latency (TPOT) is handled by a sepa-
rate decode instance and is not evaluated.

6.2   Prefill Latency
Figure 8 shows TTFT performance on H200 across load
levels for Crawler (top) and ANNS (bottom) workloads.
Crawler Workload (Append Mode): Streaming consis-
tently improves TTFT across all loads. The performance gap
between streaming and non-streaming widens with increas-
ing load: at low loads (QPS 0.5–1.0), streaming achieves        Figure 9. Average and P95 time-to-first-token across schedulers at
3.9–4.3× faster median latencies over non-streaming, while      increasing request rates: crawler workload (top, 0.5–4 QPS) and
at QPS 4.0, it delivers 10.8–11.0× faster median latencies      ANNS (bottom, 0.25–2 QPS).
by effectively overlapping retrieval and prefill.
Within the streaming approaches, scheduler differentia-
tion becomes apparent only when the page arrival rate ap-       vides substantial and consistent benefits in update-mode
proaches the prefill rate. At QPS 2, FCFS’s arrival-time or-    workloads across all load levels. Similar to the crawler work-
dering and LCAS’s prioritization of recent page arrivals both   load, at low loads (QPS 0.25–0.5) and high loads (>QPS
achieve 1.44× and 1.32× P95 speedups over the default           2.0), all schedulers perform similarly–prefill rate dominates
streaming baseline respectively, while MCPS’s progress-         at low loads while queuing dominates at high loads. At
based prioritization degrades to 0.73× as recent pages with     QPS 1.0, streaming schedulers converge tightly with 2.49–
fresh content are deprioritized. This effect amplifies at       2.63× P95 speedups over non-streaming, indicating that
QPS 4.0, where FCFS and LCAS reach 3.16× and 2.64×              prefill rate still dominates update arrival rate, and stream-
P95 speedups over the default streaming baseline, whereas       ing architecture provides the dominant benefit regardless of
MCPS falls to 0.71×. Beyond QPS 4.0, GPU utilization            scheduler choice. As load increases to QPS 2.0, scheduler
saturates and queuing delay dominates scheduling decisions,     differentiation emerges: FCFS achieves 2.26× P95 speedup
causing all approaches to converge to similar performance.      over non-streaming, LCAS 1.81×, and MCPS 1.53× over
                                                                non-streaming. MCPS slightly underperforms in this case
ANNS Workload (Update Mode): Streaming also pro-                because accumulated token progress becomes invalid when
                     Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token


Table 3. Scheduler and Eviction Strategy Ablation Study. Each cell shows TTFT speedup vs. Default vLLM non-streaming baseline
(in secs) at different percentiles (P50/P99). red indicates degradation below the non-streaming baseline (<1×). green indicates best
speedup per column. Crawler: 4.0 QPS, 10× delays. ANNS: 2.0 QPS, 30× delays.
                                   Crawler Workload                                            ANNS Workload
                   Recompute               Swap               Cost-Based       Recompute            Swap            Cost-Based
  Scheduler       P50    P99            P50    P99           P50     P99      P50    P99         P50    P99        P50     P99
  vLLM-NS       0.64s      8.97s        0.64s   8.97s    0.64s       8.97s   0.72s    6.56s     0.72s    6.56s    0.72s    6.56s
  vLLM-S        8.59×      0.78×        7.29×   0.66×    8.25×       0.71×   2.53×    0.19×     2.26×    0.18×    2.63×    0.19×
  FCFS          8.77×     10.03×        7.78×   6.69×    8.30×       8.62×   2.70×    1.84×     2.37×    1.26×    2.70×    2.04×
  LCAS          8.61×      9.23×        7.32×   4.80×    7.82×       9.14×   2.38×    1.79×     1.62×    0.97×    2.44×    1.79×
  MCPS          5.92×      0.73×        3.86×   0.48×    4.96×       0.77×   2.20×    0.19×     1.47×    0.22×    2.23×    0.19×


                                                                       produce indistinguishable curves on both workloads, demon-
Table 4. Preemption statistics across workloads. Crawler (4.0 QPS,
                                                                       strating that the TTFT improvements come at no cost to
10× delays) shows higher preemption with balanced swap/recom-
pute. ANNS (2.0 QPS, 30× delays) shows lower frequency with            system-level throughput.
cost-based heavily favoring recompute.
      Policy      Recomp.      Swap             Cost-Based             6.4   Performance under Memory Pressure
                       Crawler Workload                                Our default H200 setup (141GB memory, 80% utiliza-
      vLLM-S          2,448      779      1,735 (17%/83%)              tion) has sufficient memory that eliminates preemption. To
      FCFS            3,552      709      1,575 (21%/79%)              evaluate scheduler behavior under memory pressure, we
      LCAS           12,564    1,049      3,486 (21%/80%)              modified the workloads to saturate the GPU KV cache
      MCPS            3,316      721      1,741 (19%/81%)              pool by increasing chunk delays: a factor of 10× for
                        ANNS Workload                                  the crawler workload (append-mode) and 30× for ANNS
      vLLM-S            130        35       125 (2%/98%)               (update-mode), where the delay multiplier is defined as
                                                                                   total KV tokens capacity
      FCFS              342        50       331 (1%/99%)               avg tokens per query×avg query duration×QP S .
      LCAS              385        48       373 (2%/98%)
      MCPS              215        40      237 (0%/100%)               Scheduler and Eviction Strategy Ablation: We vary
                                                                       scheduler and eviction policy independently on both work-
                                                                       loads. Table 3 shows median (P50) and tail (P99) latency
                                                                       speedups versus the non-streaming baseline. The study
document sets change. All maintain 1.30–1.42× P50 advan-               shows two key findings: (1) DEFAULT vLLM streaming
tage over non-streaming. Streaming incurs significant cache            exhibits catastrophic tail latency degradation under memory
invalidation (Figure 11): more than 10% of requests at all             pressure—at P99, it performs 0.71× (crawler) and 0.19×
loads invalidate over 10,000 tokens across all schedulers.             (ANNS) vs. non-streaming baseline, demonstrating that
Despite this cost, streaming achieves up-to ∼ 2× faster                streaming without proper scheduling is harmful under con-
TTFT than non-streaming, proving the responsiveness gains              tention. (2) Eviction strategy choice creates substantial per-
justify the trade-off. Beyond QPS 2.0, queueing delay dom-             formance variation: in the crawler workload, FCFS achieves
inates scheduling decisions, making cache efficiency gains             10.03× speedup with recompute-only but only 6.69× with
secondary to responsiveness.                                           swap-only at P99, while LCAS shows similar sensitivity
                                                                       (9.23× vs. 4.80×). The cost-based approach balances these
6.3   Latency–Throughput Tradeoff                                      extremes, achieving 8.62× (FCFS) and 9.14× (LCAS). For
                                                                       ANNS, the pattern repeats with smaller absolute speedups
Figure 9 shows average and P95 TTFT as a function of
                                                                       due to update-mode characteristics: FCFS achieves 1.84×
QPS. As QPS increases, TTFT rises across all methods,
                                                                       (recompute) vs. 1.26× (swap) vs. 2.04× (cost-based) at P99.
but streaming schedulers consistently outperform the non-
                                                                       These results confirm that hardware-aware eviction is es-
streaming baseline. In the crawler workload, FCFS and
                                                                       sential for performance under memory pressure, and proper
LCAS maintain the lowest TTFT at high QPS, while MCPS
                                                                       scheduling (FCFS/LCAS) prevents catastrophic tail latency.
degrades. In the ANNS workload, all streaming schedulers
track closely until QPS 2.0, where FCFS pulls ahead.                   Preemption Statistics: Table 4 reports preemption frequen-
                                                                       cies across configurations. The crawler workload triggers
Figure 10 (Appendix) confirms that streaming and schedul-
                                                                       779–12,564 total preemptions depending on scheduler ag-
ing optimizations do not affect aggregate throughput. Trace
                                                                       gressiveness (LCAS highest at 12.5K for recompute-only).
completion times decrease hyperbolically with increasing
                                                                       Cost-based eviction allocates 17–21% to SWAP and 79–83%
QPS, and all scheduler variants (including non-streaming)
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

to RECOMPUTE for crawler, confirming balanced strategy              scheduling architecture supporting both append-mode and
selection. The ANNS workload exhibits lower preemption              update-mode retrieval patterns in concurrent environments.
frequency (35–385 total) with cost-based heavily favoring
RECOMPUTE (98–100%), validating the earlier claim that              LLM Inference Serving. vLLM (Kwon et al., 2023) pro-
update-mode characteristics make recomputation nearly al-           vides PagedAttention for efficient KV cache storage and
ways cost-optimal due to smaller effective KV caches after          supports preemption via recomputation or swapping, but its
context invalidation.                                               scheduler assumes static input sequences. Orca (Yu et al.,
                                                                    2022), Sarathi (Agrawal et al., 2024), and FastServe (Wu
6.5   Overhead Analysis                                             et al., 2023) improve batch efficiency and throughput
                                                                    through iteration-level scheduling, chunked prefills, and pre-
Cache Efficiency in Update Mode. Figure 11 (Appendix)               emptive prioritization, respectively. Mooncake (Qin et al.,
shows the CCDF of tokens invalidated per request across             2024), DistServe (Zhong et al., 2024) and Splitwise (Patel
QPS levels for the ANNS workload. All three streaming               et al., 2024) use disaggregated GPU pools for prefill and
schedulers (FCFS, LCAS, MCPS) exhibit nearly overlap-               decode. These systems assume static input sequences and
ping invalidation curves, indicating that cache invalidation        do not support dynamic updates or cache invalidation when
behavior is driven by the retrieval workload rather than the        sequences change dynamically.
scheduling policy. The non-streaming baseline (vLLM-NS)
shows zero invalidation by design, as it waits for complete         Scheduling and Cache Optimization. Scheduling poli-
retrieval before beginning inference. Invalidation patterns         cies range from FCFS to SJF variants (Patel et al., 2024)
remain stable across load levels: at all QPS values, more           and sophisticated approaches like Sarathi-Serve (Agrawal
than 10% of requests invalidate over 10,000 tokens, reflect-        et al., 2024) that minimize pipeline stalls. However, ex-
ing the iterative document replacement in update mode.              isting policies assume static input sequences and do not
                                                                    account for dynamic priority shifts from streaming context
Scheduling Overhead. Scheduler sorting and budget allo-             arrival. For cache optimization, PagedAttention divides
cation add sub-millisecond overhead per scheduling step.            KV cache into fixed blocks, while RadixAttention (Zheng
With 50 concurrent requests (representative of peak QPS),           et al., 2024) enables fine-grained sharing via radix trees.
sorting latency is 15–16 µs for FCFS/LCAS and 12–13 µs              These optimize memory for static input sequences but lack
for MCPS. Even at 500 requests, P99 latency remains below           mechanisms for cache invalidation when sequences change.
165 µs—negligible compared to prefill computation. The              S TREAM 2LLM addresses this with longest common prefix
one-time offline profiling for hardware-specific cost models        (LCP)-based cache invalidation that preserves valid blocks
takes approximately five minutes per GPU configuration              while selectively invalidating only affected blocks, minimiz-
and is stored as a JSON file, imposing no runtime overhead.         ing recomputation across dynamic retrieval patterns.


7     R ELATED W ORK                                                8    C ONCLUSION
                                                                    S TREAM 2LLM bridges the latency gap in context retrieval
Retrieval–Inference Co-Design. Recent work has explored
                                                                    systems by enabling streaming input processing with con-
overlapping retrieval and inference to reduce end-to-end
                                                                    current requests. Its decoupled scheduling architecture,
latency. AquaPipe (Yu et al., 2025) improves time-to-first-
                                                                    LCP-based cache invalidation, and hardware-aware preemp-
token via recall-aware prefetching of intermediate ANNS
                                                                    tion models extend vLLM to handle dynamically evolving
results, but is limited to single-request settings and does not
                                                                    contexts efficiently. Our evaluation shows that streaming
support scheduling with dynamic priorities. It also relies
                                                                    delivers order-of-magnitude improvements in time-to-first-
on interrupting and restarting prefill when retrieval context
                                                                    token latency without sacrificing throughput. Scheduler
changes, which is not supported in production systems such
                                                                    choice has limited impact when memory is abundant but
as vLLM. PipeRAG (Jiang et al., 2024) uses pipeline paral-
                                                                    becomes critical under pressure, improving append-mode
lelism with flexible retrieval intervals for iterative retrieval-
                                                                    performance and preventing degradation in update-mode
generation, but evaluates with fixed batch sizes and does not
                                                                    workloads. S TREAM 2LLM establishes streaming as a core
address variable sequence lengths in deployments. Notably,
                                                                    architectural primitive for low-latency, high-throughput con-
PipeRAG targets encoder-decoder architectures with cross-
                                                                    text retrieval in large-scale LLM deployments.
attention, which is architecturally incompatible with the
causal self-attention models used by S TREAM 2LLM. Both
systems leave open questions about scheduling policies for          ACKNOWLEDGMENTS
concurrency, adaptive preemption, and cache invalidation
                                                                    This work was supported in part by the National Science
mechanisms across requests with different retrieval patterns.
                                                                    Foundation under grant IIS-2335881.
S TREAM 2LLM addresses these by introducing a two-phase
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

R EFERENCES                                                       Malkov, Y. A. and Yashunin, D. A. Efficient and robust
                                                                   approximate nearest neighbor search using hierarchical
Agrawal, A., Kedia, N., Panwar, A., Mohan, J., Kwatra,
                                                                   navigable small world graphs. IEEE transactions on
  N., Gulavani, B., Tumanov, A., and Ramjee, R. Taming
                                                                   pattern analysis and machine intelligence, 42(4):824–
  throughput-latency tradeoff in llm inference with sarathi-
                                                                   836, 2018.
  serve. In 18th USENIX Symposium on Operating Sys-
  tems Design and Implementation (OSDI 24), pp. 117–134,
                                                                  Patel, P., Choukse, E., Zhang, C., Shah, A., Goiri, Í., Maleki,
  2024.
                                                                    S., and Bianchini, R. Splitwise: Efficient generative llm
Chen, Q., Zhao, B., Wang, H., Li, M., Liu, C., Li, Z., Yang,        inference using phase splitting. In 2024 ACM/IEEE 51st
  M., and Wang, J. Spann: Highly-efficient billion-scale            Annual International Symposium on Computer Architec-
  approximate nearest neighborhood search. Advances in              ture (ISCA), pp. 118–132. IEEE, 2024.
  Neural Information Processing Systems, 34:5199–5212,
  2021.                                                           Qin, R., Li, Z., He, W., Zhang, M., Wu, Y., Zheng,
                                                                    W., and Xu, X. Mooncake: A kvcache-centric disag-
Gao, Y., Xiong, Y., Gao, X., Jia, K., Pan, J., Bi, Y., Dai, Y.,     gregated architecture for llm serving. arXiv preprint
  Sun, J., Wang, M., and Wang, H. Retrieval-augmented               arXiv:2407.00079, 2024.
  generation for large language models: A survey, 2024.
  URL https://arxiv.org/abs/2312.10997.                           Rajpurkar, P., Zhang, J., Lopyrev, K., and Liang, P. SQuAD:
                                                                    100,000+ questions for machine comprehension of text.
Gim, I., Chen, G., Lee, S.-s., Sarda, N., Khandelwal, A.,           In Su, J., Duh, K., and Carreras, X. (eds.), Proceed-
  and Zhong, L. Prompt cache: Modular attention reuse for           ings of the 2016 Conference on Empirical Methods in
  low-latency inference. Proceedings of Machine Learning            Natural Language Processing, pp. 2383–2392, Austin,
  and Systems, 6:325–338, 2024.                                     Texas, November 2016. Association for Computational
                                                                    Linguistics. doi: 10.18653/v1/D16-1264. URL https:
Jayaram Subramanya, S., Devvrit, F., Kadekodi, R., Krish-           //aclanthology.org/D16-1264.
  naswamy, R., and Simhadri, H. V. Diskann: Fast accurate
  billion-point nearest neighbor search on a single node.         UncleCode.   Crawl4ai: Open-source llm friendly
  Advances in neural information processing Systems, 32,           web crawler & scraper. https://github.com/
  2019.                                                             unclecode/crawl4ai, 2024.
Jiang, W., Zhang, S., Han, B., Wang, J., Wang, B., and            Vespa Team.         Perplexity: Show what great
   Kraska, T. Piperag: Fast retrieval-augmented gener-              rag takes.           https://blog.vespa.ai/
   ation via algorithm-system co-design. arXiv preprint             perplexity-show-what-great-rag-takes,
   arXiv:2403.05676, 2024.                                          2024. Accessed: 2025-12-01.
Karsin, B., Santana, R., and Li, X. GPU DiskANN and
                                                                  Wang, L., Yang, N., Huang, X., Jiao, B., Yang, L.,
  Beyond: Accelerating Microsoft Vector Search with
                                                                   Jiang, D., Majumder, R., and Wei, F. Text embeddings
  NVIDIA cuVS. NVIDIA GTC session, 2025. Accessed:
                                                                   by weakly-supervised contrastive pre-training. arXiv
  2026-03-23.
                                                                   preprint arXiv:2212.03533, 2022.
Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,
 C. H., Gonzalez, J., Zhang, H., and Stoica, I. Efficient         Wei, J., Karina, N., Chung, H. W., Jiao, Y. J., Papay, S.,
 memory management for large language model serving                Glaese, A., Schulman, J., and Fedus, W. Measuring short-
 with pagedattention. Proceedings of the 29th Symposium            form factuality in large language models. arXiv preprint
 on Operating Systems Principles, pp. 611–626, 2023. doi:          arXiv:2411.04368, 2024.
 10.1145/3600006.3613165.
                                                                  Wu, B., Zhong, Y., Zhang, Z., Liu, S., Liu, F., Sun, Y.,
Lewis, P., Perez, E., Piktus, A., Petroni, F., Karpukhin, V.,      Huang, G., Liu, X., and Jin, X. Fast distributed infer-
  Goyal, N., Küttler, H., Lewis, M., Yih, W.-t., Rocktäschel,    ence serving for large language models. arXiv preprint
  T., et al. Retrieval-augmented generation for knowledge-         arXiv:2305.05920, 2023.
  intensive nlp tasks. Advances in neural information pro-
  cessing systems, 33:9459–9474, 2020.                            Xu, P., Ping, W., Wu, X., McAfee, L., Zhu, C., Liu, Z., Sub-
                                                                    ramanian, S., Bakhturina, E., Shoeybi, M., and Catanzaro,
Li, M. Introducing AISAQ in Milvus: Billion-Scale Vector            B. Retrieval meets long context large language models.
  Search Just Got 3,200x Cheaper on Memory. Milvus                  In The Twelfth international conference on learning rep-
  blog post, 2025. Accessed: 2026-03-23.                            resentations, 2023.
                   Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

Yu, G.-I., Jeong, J. S., Kim, G.-W., Kim, S., and Chun, B.-G.
  Orca: A distributed serving system for transformer-based
  generative models. 16th USENIX Symposium on Operat-
  ing Systems Design and Implementation (OSDI 22), pp.
  521–538, 2022. URL https://www.usenix.org/
  conference/osdi22/presentation/yu.
Yu, R., Huang, W., Bai, S., Zhou, J., and Wu, F. Aquapipe:
  A quality-aware pipeline for knowledge retrieval and
  large language models. Proceedings of the ACM on Man-
  agement of Data, 3(1):1–26, 2025.
Zheng, L., Yin, L., Xie, Z., Sun, C., Huang, J., Yu, C. H.,
  Cao, S., Kozyrakis, C., Stoica, I., Gonzalez, J. E., et al.
  Sglang: Efficient execution of structured language model
  programs. Advances in neural information processing
  systems, 37:62557–62583, 2024.
Zhong, Y., Liu, S., Chen, J., Hu, J., Zhu, Y., Liu,
  X., Jin, X., and Zhang, H. DistServe: Disaggregat-
  ing prefill and decoding for goodput-optimized large
  language model serving. In 18th USENIX Sympo-
  sium on Operating Systems Design and Implementa-
  tion (OSDI 24), pp. 193–210, Santa Clara, CA, July
  2024. USENIX Association. ISBN 978-1-939133-40-3.
  URL https://www.usenix.org/conference/
  osdi24/presentation/zhong-yinmin.
                    Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

                                                                    itory README.md.

                                                                    B.2     Artifact check-list (meta-information)
                                                                      • Algorithm: vLLM-NS, vLLM-S, S TREAM 2LLM-FCFS,
                                                                        S TREAM 2LLM-LCAS, S TREAM 2LLM-MCPS scheduling;
                                                                        cost-based preemption

                                                                      • Program: Python scripts; modified vLLM 0.8.1 engine

                                                                      • Data set: Web-crawler traces; ANNS pipeline traces;
Figure 10. Trace completion time across QPS levels for both work-       performance-model JSONs
loads. All scheduler variants achieve near-identical completion
times, confirming throughput parity.                                  • Run-time environment: Linux, Python 3.10.9, CUDA,
                                                                        conda, pip

A     A DDITIONAL E VALUATION                                         • Hardware: Artifact-only: any machine. Full re-run: 2×
                                                                        H200 or 2× H100 GPUs
A.1   Cache Invalidation in Update Mode
                                                                      • Metrics: TTFT, trace completion time, preemption counts,
Figure 11 shows the CCDF of tokens invalidated per re-                  scheduler sorting latency
quest across QPS levels for the ANNS workload. All
                                                                      • Output: Figures in figures/, tables in tables/, logs
three S TREAM 2LLM schedulers (S TREAM 2LLM-FCFS,                       in data/run log/
S TREAM 2LLM-LCAS, S TREAM 2LLM-MCPS) exhibit
nearly overlapping invalidation curves, indicating that cache         • How much disk space required (approximately)?: ∼5 GB
invalidation behavior is driven by the retrieval workload
                                                                      • How much time is needed to prepare workflow (approxi-
rather than the scheduling policy. The non-streaming base-              mately)?: ∼15 minutes
line (vLLM-NS) shows zero invalidation by design, as it
waits for complete retrieval before beginning inference. In-          • How much time is needed to complete experiments (ap-
validation patterns remain stable across load levels: at all            proximately)?: ∼5 minutes (artifact-only); ∼48 hours (full
                                                                        re-run)
QPS values, more than 10% of requests invalidate over
10,000 tokens, reflecting the update-mode characteristic of           • Publicly available?: Yes
iterative document replacement.
                                                                      • Code licenses (if publicly available)?: MIT

A.2   Throughput Parity                                               • Data licenses (if publicly available)?: CC-BY-4.0

Figure 10 confirms that streaming and scheduling optimiza-            • Archived (provide DOI)?: https://doi.org/10.
tions do not affect aggregate throughput. Trace completion              5281/zenodo.18906769
times decrease hyperbolically with increasing QPS, and all
scheduler variants (including non-streaming) produce in-            B.3     Description
distinguishable curves—appearing as a single overlapping
line—on both workloads. This demonstrates that the TTFT             B.3.1    How delivered
improvements reported in the main evaluation come at no             The artifact is delivered as a GitHub repository on the
cost to system-level throughput.                                    mlsys artifact branch:

B     A RTIFACT A PPENDIX                                           https://github.com/rajveerb/stream2llm

B.1   Abstract                                                      All large data (run logs, workload traces, performance-
                                                                    model JSONs) is stored in a git submodule hosted on Hug-
This artifact contains the open-source Stream2LLM system:
                                                                    gingFace:
a modified vLLM 0.8.1 engine with streaming input support,
along with all scripts, pre-computed run logs, and workload
                                                                            https://huggingface.co/datasets/
traces needed to reproduce every figure, table, and inline
                                                                             rbachkaniwala3/stream2llm-data
number in the paper. The repository includes two evaluation
paths: (1) an artifact-only path that regenerates all paper
                                                                    B.3.2    Hardware dependencies
artifacts from pre-computed data in approximately five min-
utes without GPU hardware, and (2) a full re-run path that          No GPU is required for artifact-only evaluation. For full
re-executes the experiments end-to-end on NVIDIA GPUs.              experiment re-runs, the paper’s experiments used NVIDIA
For a detailed version of this artifact appendix, see the repos-    2×H200 and 2×H100 GPUs. See README.md for details.
                       Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token




Figure 11. Tokens invalidated per request (CCDF) across QPS levels (0.25–2.0) for the ANNS workload. All S TREAM 2LLM schedulers
show similar cache invalidation behavior. vLLM-NS has zero invalidation as it waits for complete retrieval.

B.3.3    Software dependencies                                    scripts is:
Dependencies are installed in a conda environment
                                                                     • Figure 5 → plot recomp vs swap clean.py
via pip. See README.md for setup instructions and
requirements.txt for the full package list.                          • Figures 6   and  7            →      chunk arrival -
                                                                       characterization.py
B.3.4    Data sets                                                   • Figure 8 → plot ttft ccdf stacked 2x4.py
All data is hosted as a HuggingFace dataset                          • Figure 9 → crawler/plotter utils/plot -
(rbachkaniwala3/stream2llm-data) and fetched                           ttft qps comparison.py    (Crawler    half)
automatically via the git submodule. See the “Data                     and     anns/plotter utils/plot ttft qps -
Organization” section of README.md for details.                        comparison.py (ANNS half)

                                                                     • Figure  10         →       plot trace completion -
B.4     Installation                                                   combined.py
Refer to the README.md.                                              • Figure 11   →           plot tokens invalidated -
                                                                       aggregated.py
B.5     Experiment workflow                                          • Table 3 → compute scheduler improvements.py
B.5.1    Artifact-only (no GPU required)                             • Table 4 → analyze preemptions.py
bash reproduce_artifacts.sh                                          • Scheduler latency      →     benchmark scheduler -
                                                                       latency.py
This generates all figures in figures/ and analysis tables
in tables/ from pre-computed run logs. Takes approxi-             B.7   Experiment customization
mately five minutes.
                                                                     • Scheduler selection: per-scheduler YAML configs in
B.5.2    Full re-run (GPU required)                                    experiments/{crawler,anns}/configs/

The experiment drivers run each scheduler vari-                      • Arrival-time sweeps: supported via the driver script’s
ant (default vllm, fcfs, lcas, mcps) across                            scheduler and compare modes
all arrival-time configurations and write logs to
                                                                     • Hardware adaptation: modify YAML configs and
data/run log/.           See README.md (“Building
                                                                       performance-model JSONs in data/perf model/
Stream2LLM Engine” and “Running Experiments”)
and experiments/README.md for the full list of
experiment configurations and commands.                           See experiments/README.md for full details.

B.6     Evaluation and expected result                            B.8   Methodology

Figures and tables. Generated figures in figures/                 Submission, reviewing, and badging methodology:
should visually match the pre-built reference copies in
figures/reference/. Analysis scripts produce .txt                    • http://cTuning.org/ae/
files in tables/. The mapping from paper artifacts to                  submission-20190109.html
          Stream2LLM: Overlap Context Streaming and Prefill for Reduced Time-to-First-Token

• http://cTuning.org/ae/
  reviewing-20190109.html
• https://www.acm.org/publications/
  policies/artifact-review-badging
