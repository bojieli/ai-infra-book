             WAVE : A S YMBOLIC P YTHON DSL AND C OMPILER FOR H IGH
                         P ERFORMANCE M ACHINE L EARNING


 Harsh Menon 1 Oleksandr Zinenko 2 Gaurav Verma 1 Stanley Winata 1 Ivan Butygin 3 Nithin Meganathan 1
Sanket Pandit 1 William Hatch 1 Surya Jasper 1 Megan Kuo 1 Sahil Faizal 1 Ashay Rane 1 Aurore De Spirlet 2
                                           Martin Paul Lücke 3


                                                        A BSTRACT
        Modern ML models demand ever-greater compute, prompting hardware vendors to add specialized matrix cores to
        their GPUs. While these units unlock high throughput, they impose intricate programming models and addressing
        schemes that are difficult to manage by hand. This paper introduces Wave, a Python-embedded DSL for kernel
        authoring that automates these complex address computations and lets authors focus on core computation. In
        experiments, it matches or surpasses the performance of state-of-the-art kernel DSLs and libraries.

1       I NTRODUCTION                                               ware. Rather than being completely independent, threads
                                                                    in a workgroup are arranged into subgroups, referred to as
Generative Machine Learning (ML) models have seen                   warps in CUDA and waves in HIP1 , that are processed simi-
tremendous success across domains ranging from image                larly to Single Instruction Multiple Data (SIMD) vectors by
generation to natural language processing and agentic ap-           dedicated wide Vector Arithmetic and Logic Units (VALUs).
plications. Their success comes with a significant compu-           Dedicated units for lower-precision matrix computations of-
tational cost, making such models the driving force behind          ten share register and scratchpad memory resources with
the design and evolution of high-performance GPUs and               VALUs and require access by a subgroup (or multiple sub-
specialized accelerators. Extracting maximum performance            groups) simultaneously rather than by individual threads.
from these increasingly sophisticated devices requires one          This poses a programmability challenge in thread-oriented
to effectively leverage increasingly specialized units, such        programming models.
as Nvidia Tensor cores (Choquette et al., 2021; Choquette,
2022), AMD Matrix cores (Sitaraman et al., 2022) or Google          On the other hand, the widespread adoption of gener-
TPU’s systolic arrays (Jouppi et al., 2017) for efficient           ative models led to more integrated hardware-agnostic
matrix multiplication. A missed optimization opportunity            software stacks, such as vLLM (Kwon et al., 2023) and
preventing a computational kernel from using these units,           SGLang (Zheng et al., 2024) inference servers, calling for
falling back to more conventional arithmetic resources, may         hardware-decoupled tools to implement high-performance
result in an order-of-magnitude performance penalty.                kernels to avoid code size and complexity explosion oth-
                                                                    erwise needed to handle specialized units with sizes and
As hardware grows in complexity, it exposes increasingly            configurations varying across hardware generations even
complex programming models required to tap into the full            from the same vendor for a collection of ML models of
performance potential. Historically, GPUs have been pro-            varying size. This requirement is at odds with the low-level
grammed using the Single Instruction Multiple Threads               programming models currently exposed by the hardware.
(SIMT) model, pioneered by CUDA (Luebke, 2008), and
adopted by HIP (ROCm Authors, 2025) and OpenCL (Mun-                Kernel generation Domain-Specific Languages (DSLs) are
shi, 2009). Having allowed us to bootstrap utilization of           often proposed to fill the abstraction gap between lower-
GPUs beyond graphics, the SIMT model does not fully                 level hardware programming models and higher-level pro-
correspond to the execution model of the underlying hard-           gramming models expected by ML frameworks such as
    1
                                                                    PyTorch (Paszke et al., 2019) or software stacks built on
    Advanced Micro Devices, Inc., Santa Clara, California, USA      them. The highly popular Triton DSL (Tillet et al., 2019)
2
 AMD SAS, Paris, France 3 AMD Advanced Micro Devices GmbH,
Aschheim, Germany. Correspondence to: Oleksandr Zinenko
                                                                    features workgroup-level abstractions to effectively lever-
<azinenko@amd.com>, Gaurav Verma <gaurverm@amd.com>.                age workgroup (or shared) memory. Tile DSLs, such as
                                                                    TileLang (Wang et al., 2025) or cuTile (Adelstein Lel-
Proceedings of the 9 th MLSys Conference (Industry Track), Belle-
                                                                       1
vue, WA, USA, 2026. Copyright 2026 by the author(s).                       We may use these terms interchangeably.
          Tracing and Inference           Lowering                 Optimization                Code              MLIR/LLVM
               (section 2)               (section 3)                (section 4)              generation          compilation

      Wave DSL                torch.fx                  torch.fx                  torch.fx                MLIR                 executable

             Fully symbolic                            Shape symbolic, tile sizes numeric                        Fixed shape


Figure 1. Wave compilation and optimization use torch.fx as a substrate and progressively specializes the kernel from symbolic to an
optimized fixed shape.


bach, 2025), focus on data decomposition from full ten-                     metic operations. Activating the matrix cores requires the
sors to workgroup and subgroup levels. However, these                       product operands to be distributed across registers dedicated
languages still tightly couple computational logic with                     to specific threads or stored in the workgroup memory in a
hardware-specific address arithmetic needed for distribu-                   certain format using specialized instructions. Prior to invok-
tion, tiling, and swizzling strategies. This entanglement                   ing the computational instruction, individual threads must
makes kernels difficult to understand, maintain, and adapt                  bring the data to registers, performing non-trivial address
across different hardware targets or problem dimensions.                    computations based on lane, thread and workgroup indices
                                                                            to obtain the offset of the data required by the thread in the
We introduce Wave, a Python-embedded DSL for ML ker-
                                                                            global memory space. This leads to intricate and fragile
nel authoring. Wave adopts a wave-level (subgroup) pro-
                                                                            indexing arithmetic being present in the kernel source code.
gramming model, where address computations are implicit:
rather than explicitly computing memory offsets in the                      A distinct issue sharing the common cause is the lack of
source code, authors specify distribution and tiling con-                   portability, sometimes even within the same generation of
straints separately from the kernel logic. The compiler                     accelerators, as matrix multiplication cores may marshall
derives per-thread addressing expressions using index se-                   different amounts of data with different inter-thread data
quences—triples of (offset, size, stride) expressions that cap-             distribution constraints, such as the AMD CDNA matrix
ture block-strided memory access patterns required by ma-                   cores which support 16x16x16 and 32x32x8 bfloat16
trix cores. Both kernel logic and constraints are expressed                 multiplication, float32 accumulation matrix product con-
symbolically, allowing for the substitution of tile sizes and               figurations (Sitaraman et al., 2022). Both issues are exacer-
hardware parameters without code modification. We demon-                    bated in kernels designed for processing large inputs with
strate that Wave kernels achieve competitive performance                    data tiling and other work distribution techniques, increas-
with Triton and hand-tuned libraries on attention and matrix                ing the complexity of the address computation (Salykov
multiplication for Large Language Models (LLMs).                            et al., 2025) .
Overall, this paper makes the following contributions:                      Wave is a kernel authoring DSL embedded into Python with
                                                                            the programming model based around two principles:
 • Design of a subscript-free Wave DSL for ML workloads
   embedded into Python, primarily targeting AMD matrix                     • Implicit Indexing—the address computations are implic-
   cores.                                                                     itly derived from hardware and target configuration.
 • Compact representation of block-strided layouts compati-                 • Symbolic Mapping—quantities pertaining to address
   ble with matrix and tensor cores via index sequences and                   computations, including tile sizes and mapping configu-
   a set of transformations using those to target the GPU                     ration, are kept in the symbolic form.
   memory subsystem.
                                                                            This model hides address computation from the user and
 • An evaluation of Wave on AMD Instinct MI300, MI325
                                                                            makes it easily portable as symbols representing, e.g., tile
   and Radeon RX9070RT GPUs demonstrating improved
                                                                            sizes or SIMD vector widths, can be substituted with con-
   performance for attention and matrix multiplication ker-
                                                                            crete values late in the compilation process. It complements
   nels relevant to LLMs.
                                                                            the SIMT programming model as well as the block-level pro-
                                                                            gramming model, pioneered by Triton (Tillet et al., 2019),
2     WAVE DSL                                                              both of which require explicit address computation, by dele-
                                                                            gating hardware-specific address generation to a compiler.
2.1    Design Rationale and Programming Model
Modern GPUs and similar accelerators are usually com-                       2.2    Expressing Computation
posed of multiple compute units (CU) consisting of dedi-
cated hardware units for matrix multiplications and arith-                  Computations in Wave are expressed as a function operat-
                                                                            ing on tensors with symbolic shapes, located in registers
                                                                        2
or memory as described in subsection 3.1. Each operation             conditionals. In the remainder of the paper, we focus on
can be seen as implicitly iterating through the elements of          impactful GEMM and attention kernels for brevity.
its operand tensors. The iteration domain of the operation           @wave(...) # omitted for brevity
consists of the dimensions (co-)indexing the corresponding           def fused_moe_gemm(
                                                                         b: Memory[E, N, K, ADDRESS_SPACE_B,f16],
tensors, associated with the symbols used in tensor shapes.              expert_ids: Memory[NUM_BLOCKS,ADDRESS_SPACE_A,i32],
                                                                         ...): # more arguments omitted for brevity
Iteration domain dimensions may be tiled and mapped to                 # Using a value (workgroup ID) computed in the kernel
parallel work- and subgroups using constraints as described            # directly in a subscript via remapping.
                                                                       wid = scalar(WORKGROUP_2, i32)
in subsection 2.3. At most one remaining dimension may                 expert_id = read(expert_ids, mapping=IndexMapping(
                                                                           inputs={NUM_BLOCKS: d0},
correspond to an implicit sequential innermost loop. The                   outputs={NUM_BLOCKS: i},
remaining sequential dimensions, including reduction di-                   dynamic_val_mappings={NUM_BLOCKS: i}),
                                                                         mapping_dynamic_vals=(wid,))
mensions, must be introduced using the explicit iteration              # Using a value read by the kernel in subscripts
construct. This construct is a decorator function taking as            # via a named symbol.
                                                                       set_symbol(IDX, expert_id)
arguments and returning the loop-carried variables, with               b_reg = read(b, mapping=IndexMapping(
                                                                           inputs={E: IDX, N: i, K: j},
initial values provided in the decorator arguments along                   outputs={N: i, K: j})
with the symbol of the dimension being indexed over. Note              # ...

the absence of the explicit loop induction variable: one can
instead construct a tensor containing the current value of           Listing 2. Excerpt from a Mixture-of-Experts Wave kernel
                                                                           demonstrating usage of dynamic values in subscripts.
any symbol when needed (e.g., for masking or filtering).
Listing 1 represents a M ⇥ N ⇥ K matrix product where the
                                                                     2.3    Device Mapping
symbols are used directly in the tensor data type. It reads
the inputs, uses the Wave mma operator to denote matrix              The computation from Listing 1 is mapped to a GPU after
multiplication and accumulation, and defers to the iterate           a multi-level tiling to achieve a blocked matrix product
loop construct to accumulate partial reduction results in the        allowing for efficient execution. The tile sizes and mapping
c_reg register before writing the result back to the memory          decisions are provided by the user in the configuration object
provided for this purpose.                                           consisting of a list of constraints passed into the @wave
@wave(...) # parameters omitted for brevity                          decorator. Listing 3 is an example of such a configuration
def mm(                                                              mapping the computation to a 2D grid of 2D workgroups.
    a: Memory[M, K, ADDRESS_SPACE_0, f16],
    b: Memory[N, K, ADDRESS_SPACE_0, f16],                           Note that constraints in the configuration are allowed to use
    c: Memory[M, N, ADDRESS_SPACE_1, f32]):
  c_reg = Register[M, N, f32](0.0)                                   further symbolic values.
  @iterate(K, init_args=[c_reg])                                     constraints = [
  def loop(acc: Register[M,N,f32])-> Register[M,N,f32]:                  # Specifies that dimensions indexed by M, N respec-
      a_reg = read(a)                                                    # tively are mapped to workgroup indices x and y.
      b_reg = read(b)                                                    WorkgroupConstraint(M, BLOCK_M, 0),
      acc = mma(a_reg, b_reg, acc)                                       WorkgroupConstraint(N, BLOCK_N, 1),
      return acc
  # The name of the loop is also used as its result.                       # Specifies that dimensions indexed by M, N respec-
  write(loop, c)                                                           # tively are further mapped to concurrent waves
                                                                           # with each wave processing a subset of iterations.
                                                                           WaveConstraint(M, BLOCK_M / 2),
Listing 1. Mixed-precision C = A ⇥ B T expressed in Wave.                  WaveConstraint(N, BLOCK_N / 2),
      Symbols M, N, K are used for tensor shapes and                       # Requires the dimension indexed by K to be tiled.
      ADDRESS_SPACE_0 is a symbol allowing one to                          TilingConstraint(K, BLOCK_K),
      parameterize data placement in the memory hierarchy.
                                                                           # Specifies the MMA configuration to use and the
      Address computation is implicit.                                     # subgroup (warp/wave) size.
                                                                           HardwareConstraint(threads_per_wave=64,
                                                                               mma_type=MMAType.F32_16x16x16_F16)
The iteration domain of the write operation is M ⇥ N as              ]
                                                                     @wave(constraints)
these are the dimensions indexing both loop and c. The               def mm(...): #body
iteration domain of both read operations is M ⇥ N ⇥ K
with K being indexed over by the explicit loop. Without              Listing 3. An example of a configuration for the matrix product
further specification either of M or N , or both are expected              kernel above. BLOCK_M, BLOCK_N and BLOCK_K are
                                                                           symbolic values.
to be mapped to concurrent work- and/or subgroups.
Wave is sufficiently expressive to capture performance-              In this listing, the workgroup constraints indicate how the
critical kernels in transformer models including extend atten-       indexing spaces are further mapped to parallel workgroups.
tion (Appendix A.1), persistent GEMM (Appendix A.2), and             The workgroup indexes 0, 1, 2 are equivalent to block-
mixture-of-experts (MoE) (Appendix A.3). It supports sym-            Idx.x, y, z, respectively, in CUDA or HIP. The wave
bolic remapping expressions for tensor subscripts, where             constraint indicates that the iteration space and address
symbols may be bound to values computed within the ker-              space dimensions corresponding to certain symbols are fur-
nel (Listing 2) as well as value-dependent while loops and           ther mapped to concurrent waves (warps) within the work-
                                                                 3
group, with the parameter indicating the number of ele-               the program, respecting the mapping constraints, to match
ments per wave. The constraint system expects only the                matrix core sizes. This process is fully hardware-agnostic.
dimensions that have workgroup constraints to have wave
constraints. In absence of a wave constraint, the size of             3.1   Data Movement and Type Inference
the wave subspace is taken to be equal to that of the work-
group subspace. This can be seen as a second, wave level              Data in Wave resides in one of three address spaces: regis-
of tiling for parallel dimensions. In Listing 3, each work-           ters accessible to arithmetic operations, workgroup (shared)
group consists of four waves (inferred from dividing the              memory or global memory accessible to read/write op-
per-dimension size of the iteration subspace mapped to each           erations for data motion. Wave adopts a hybrid stance: it
workgroup to subspace mapped to each wave) processing a               automates global-to-shared data movement while keeping
BLOCK_M/2 ⇥ BLOCK_N/2 subset of the iteration space.                  register-level operations explicit, so the compiler can in-
                                                                      fer synchronization and coalescing. The user can request
The tiling constraints indicate loop tiling for dimensions that       kernel arguments, initially located in global memory, to
are implemented as sequential, either because they carry de-          be staged through workgroup memory by setting address
pendencies or because they were not mapped to workgroup               space of the type, either directly or by using a parameter
dimensions with concurrent waves. In Listing 3, the sequen-           that will later be substituted. If staging is requested, Wave
tial reduction dimension K is tiled with size BLOCK_K. Wave           will automatically insert a copy from global to workgroup
requires tiling constraints to be specified for all dimensions        memory before the original read that is always required to
that are not associated with workgroups and waves. When               bring data into registers. The newly inserted operations are
actual tiling is not desired, the tile size may be set to the         subject to optimizations from section 4 and may be imple-
expected size of the dimension.                                       mented as asynchronous copies, with or without swizzling,
Finally, the hardware constraint indicates the desired kind           depending on hardware support. The allocation size in work-
of matrix multiply and accumulate (MMA) instruction to                group memory is computed to fit a full data tile deduced
use, in this case the 16 ⇥ 16 ⇥ 16 one, which informs the             from mapping and tiling constraints on the dimensions in-
Wave compiler of the desired distribution of tensor values            dexing the argument, plus 64-bit padding to mitigate bank
across threads. The compiler contains a mapping from                  conflicts. For example, for an M ⇥ K-shaped argument, the
known MMA instructions to distribution index sequences                workgroup constraint (M, BM , 0) and the tiling constraint
described in subsection 3.2.                                          (K, BK ) will lead to a BM ⇥ Bk + 64bit allocation.

The overall mapping process comprises three hierarchical              Following this transformation, Wave compiler infers types
levels of iteration domain tiling: workgroup-level within de-         (symbolic shape and address space) for all program values
vices, wave-level within workgroups, and register-level (im-          using an optimistic forward sparse dataflow analysis with
plicitly determined by MMA layout requirements). There-               the lattice consisting of > and ? states, and an intermediate
fore, block sizes are expected to be non-increasing. Tile             state for each concrete type the join of which is >. This
sizes are expected to be known at compile time, similar               analysis initializes all unknown types to ? and proceeds to
to most ML kernel generators, but are exposed for tuning.             propagate type information using operation-specific transfer
Shapes and other symbolic expressions may be optionally               function: join of operands for elementwise and control flow
substituted with their numeric values to specialize the kernel        operations, projecting out reduction dimensions for MMA
and enable more aggressive optimization. Otherwise, the               and reductions, swap address space in the type for mem-
Wave compiler performs symbolic expression simplification             ory operations. Programs where type inference reaches >,
and emits parametric code. This, in particular, enables it to         which may arise, (e.g., when differently-shaped arguments
generate prefill (symbolic sequence length) and specialized           are supplied to an elementwise operation), are rejected as
decode (sequence length of 1) attention kernels from the              incorrectly typed.
same source. When tile sizes cannot be proven to evenly               After type inference, the types feature symbolic shapes indi-
divide problem shapes, Wave resorts to masking or padding             cating both the overall size of the tensor across all threads
(controlled by a tunable compiler option) to avoid out-of-            and workgroups and the symbols used to index the tensor.
bounds accesses. Partial tiles may be peeled off to avoid             For example, c_reg = Register[M, N, f32] in List-
overhead from masking.                                                ing 1 indicates that c_reg is register-resident with overall
                                                                      size M ⇥ N ; the actual number of registers per thread is
3    L OWERING                                                        determined later based on constraints.

Once compilation options are set, the Wave compiler pro-              3.2   Constructing Address Computations
gressively lowers the program to an executable by inferring
tensor dimensions and addresses. It then tiles and expands            The core component in lowering Wave programming model
                                                                      to SIMT is to idenitfy address expressions for data each
                                                                  4
thread must load into registers before computation, in par-            K ! (iK · BLOCK_K) + 4 · b(Tx mod 64)/16c, 4, 1 while
ticular for MMA. Wave defines an index sequence mapping                the index sequence for the write to C is: M ! (Wx ·
each symbolic dimension S to a triple (OS , NS , DS ) of               BLOCK_M) + (bTx /64c(BLOCK_M/2)), 1, 1), N ! (Wy ·
closed-form algebraic expressions over parameters, work-               BLOCK_N + Ty (BLOCK_N/2), 1, 1).
group/thread identifiers, and loop induction variables. Here
                                                                       Here W refer to workgroup and T to thread x, y, z
OS (offset) is the base logical index; NS (size) is the num-
                                                                       indices respectively and i refers to the induction variable
ber of elements per thread along S (the per thread vector
                                                                       of an explicit loop.⌅ Each additive
                                                                                                   ⇧        term in the offset comes
width); and DS (stride) is the spacing, in element indices,
                                                                       from a constraint: Tx /64 obtains the subgroup index from
between consecutive elements within that per-thread vector.
                                                                       the thread index Tx (subgroup size 64); term Tx mod 16
For each dimension S, the triple compactly represents the
                                                                       selects the relative thread position within the subgroup tile,
set of element indices {O + iD | 0  i < N }.
                                                                       while the index sequence for the write is:
Index sequences can be defined for all dimensions involved
                                                                       The mma operation requires each thread to provide 4 ele-
in any operation though they are most relevant for mem-
                                                                       ments of the input tensor, setting the initial value of D = 4
ory operations. They are obtained using a sparse optimistic
                                                                       for operands along K and for the result along N . These will
dataflow analysis propagating indexing information both
                                                                       be propagated to the corresponding read and write opera-
forward and backward. Specifically, it initializes index se-
                                                                       tions, indicating that they are to access vectors of 4 values.
quences for all operations based on the constraints and then
                                                                       Note that in absence of the mma operation, the kernel would
propagates indexing information from:
                                                                       read and write one element per thread (D = 1) instead of
 • MMA operations and their configuration, forward from                four since no constraint requires otherwise. A hardware
   results and backward from operands;                                 constraint or a parameter of read/ write operations may
 • other reduction operations, similarly;                              specify a different value to force vectorized memory access.
 • memory operations, forward from reads and backward                  Finally, offsets of index sequences are used as indices along
   from writes.                                                        the memory dimension indexed by the corresponding sym-
                                                                       bol in memory-access operations and sizes are used to indi-
The propagation is based on a lattice defined as a pair "prove-
nance, index sequence", (p, I) in addition to the canonical            cate how many elements each access touches. Multiplying
lattice bottom and top states. The provenance indicates                them with strides of the corresponding tensors leads to the
where the index statement originally comes from, and is                required, complex address computation expression required
ordered: pmma > preduce > pmem > ?. This ordering is                   when manually implementing similarly distributed kernels.
a heuristic motivated by order-of-magnitude performance
penalties for not utilizing MMA engines or in-subgroup re-
duction schemes, which are common in modern GPUs. The                  3.3   Expansion
join of two lattices is defined as:
                                                                       After index sequence propagation establishes address com-
                  8                                                    putation patterns, the Wave compiler performs expansion,
                  >
                  > (p1 , I1 I2 )     if p2 = ?
                  >
                  >
                  > (p , I I2 )       if p1 = ?                        converting the wave-level representation into a thread-level
                  < 2 1
   (p1 , I1 )t
                ,
                    (p1 , I1 )        if p1 > p2                       representation. This transformation resolves the mapping
     (p2 , I2 )   >
                  > (p2 , I2 )        if p1 < p2                       from the high-level, wave-oriented operations to the fine-
                  >
                  > (p , I )          if p1 = p2 ^ I1 = I2
                  >
                  : 1 1                                                grained, per-thread operations required by the SIMT exe-
                    >                 otherwise
                                                                       cution model, effectively transitioning from SIMW (Single
 where index sequence composition is defined as I1 I2 =                Instruction Multiple Waves) to SIMD (Single Instruction
{O1 + D1 (O2 + D2 · i2 + N2 D2 i1 ) | 0  i1 < N1 , 0                 Multiple Data) representation.
i2 < N2 }. In other words, this means that the index se-               Prior to expansion, operations such as read, mma, and
quence induced by an MMA overrides that induced by a                   write are expressed as wave-level operations on symbolic
reduction, which in turn overrides that induces by a mem-              tiles, abstracting over the distribution of work across indi-
ory access, and that all of them compose with the index                vidual threads. The underlying hardware, however, requires
sequence initially assigned to the operations.                         each thread within a wave to perform specific operations
The analysis propagates forward by joining the lattices of all         on its assigned subset of data. Expansion creates multiple
operands and backward by joining the lattices of all results           copies of each operation, one for each sub-tile that a thread
until a fix point is reached. If the lattice top is reached, the       must process to cover the wave’s iteration space. For in-
program is rejected as incorrect.                                      stance, for a wave-level mma operation processing a 16 ⇥ 16
                                                                       tile, where each thread must invoke the instruction multiple
Continuing with the example in Listing 1 and Listing 3, the            times to cover a larger workgroup tile, expansion creates
index sequence for the first read from A is M ! (Wx ·                  these multiple per-thread invocations.
BLOCK_M) + (bTx /64c(BLOCK_M/2)) + (Tx mod 16), 1, 1 ,
                                                                   5
Static Resolution of Wave Tile Geometry Although                     4.1   Global Memory Optimizations
Wave maintains symbolic representation throughout most
                                                                     After initial index sequence propagation and expansion/un-
of the compilation pipeline, expansion requires that the fol-
                                                                     rolling, a Wave program typically includes multiple vector
lowing three values be statically known at compile time for
                                                                     or even scalar reads from consecutive locations in global
each dimension S with constraints:
                                                                     memory. The sizes of these reads are most likely driven
 • TS : the tile size from WorkgroupConstraint or                    by the shape of the MMA operations, (e.g., for the running
   TilingConstraint.                                                 example Listing 1 and Listing 3), each operation is expected
 • WS : the number of waves per workgroup along dimen-               to read 4 bfloat16 elements, or 4 ⇥ 16 ⇥ 64 = 4096 bits
   sion S.                                                           per subgroup. However, CDNA3-enabled hardware such as
                                                                     AMD MI300X provides eight 1024-bit memory channels
 • VS : the vector size (elements per thread per operation)
                                                                     for a total of 8192 bits, so such a read would utilize only
   along dimension S.
                                                                     half of the available bandwidth.
The number of copies
                  l for dimension
                          m       S is then computed as:             Global read coarsening transformations increase the num-
dim_scaling(S) = WSTS·VS                                             ber of elements fetched by each read instruction so as to
                                                                     saturate the High-Bandwidth Memory (HBM) bandwidth
This represents the number of times each operation must be
                                                                     and decrease the total number of memory transactions. It
replicated per thread along dimension S. While TS , WS ,
                                                                     proceeds by identifying consecutive reads from the same
and VS must be concrete values for expansion to proceed,
                                                                     global memory-resident tensor such that the total size of data
what is fundamentally fixed is the per-wave tile size TS /WS .
                                                                     read is below the device-specific memory bandwidth and
The system preserves parametricity at higher levels: The
                                                                     cannot be increased further by adding another read. Two
number of workgroups launched remains symbolic. Thus,
                                                                     reads with index sequences (O1 , D1 , N1 ) and (O2 , D2 , N2 )
the kernel maintains fully unrolled inner loops for a fixed
                                                                     associated with the innermost dimension are consecutive if
per-wave tile geometry, while remaining agnostic to varying
                                                                     O2 = O1 + D1 , and they read (D1 + D2 ) · 8 · sizeof(Tel )
input and workgroup tile sizes.
                                                                     bits where Tel is the elemental type of the tensor being
                                                                     read. When all combined reads have Di > 1, (i.e., the
Graph Transformation Expansion employs a depth-first                 innermost dimension is used for vectorized load/store), they
traversal, starting from the leaf nodes of the computation           can be simply
                                                                                 P replaced with a single wider read operation
graph (write nodes). For each node, the compiler generates           with D = i Di and O = O1 . When a non-innermost
all combinations of indices where each dimension S ranges            dimension has Di > 1 and consequently the innermost has
over [0, dim_scaling(S)), and creates a new copy of the              Di = 1, coarsening essentially transposes the read by hav-
operation for each combination. Given the original index             ing each read operation access consecutive elements. In this
sequence (OS , NS , DS ) from subsection 3.2, the offset for         case, exactly Di operations are expected to be combined.
the i-th expanded copy along dimension S (where 0  i <
dim_scaling(S)) is updated to OS0 = OS + i · NS · DS .               Thus coarsened reads would often result in elements re-
For mma operations expanded along the reduction dimension,           quired by different threads to be loaded in a single thread.
accumulation is implemented by chaining the copies: The              For example, loading 8 consecutive elements for the A ma-
accumulator input of the i-th copy is connected to the output        trix from our running example in Listing 1 would bring
of the (i 1)-th copy, creating a sequential dependency that          in 4 elements for thread 0 and 4 elements for thread 16 in
implements the reduction.                                            the registers of the same thread. We therefore immediately
                                                                     store the result of the global load into workgroup memory.
The inputs to each expanded operation are recursively ex-            After a workgroup synchronization, each thread can load
panded in the same manner, creating a fully unrolled com-            the required 4 consecutive elements from workgroup mem-
putation graph where all wave-level abstractions have been           ory instead. This transformation is only enabled for global
lowered to concrete per-thread operations.                           loads that are already stored into the workgroup memory
                                                                     to avoid increasing the workgroup memory usage and thus
4    O PTIMIZATION                                                   decreasing occupancy and thus performance.

We devise a series of GPU-oriented optimizations to im-              4.2   Scheduling Optimization
prove kernel performance, in addition to those already avail-
able in the MLIR (Lattner et al., 2021)/LLVM (Lattner &              Schedule reordering This optimization is a combination
Adve, 2004) infrastructure. These optimizations rely on              of software pipelining (Lam, 1988) with a variation of the
target-specific information, such as the memory hierarchy            “ping-pong” task alternation between subgroups (Shah et al.,
description and the respective latencies of computational            2024). It supports pipelining schemes specialized for matrix
and memory operations.                                               multiplications and attentions as well as a more generalized
                                                                 6
modulo scheduling (Rau & Glaeser, 1981). The transfor-               aids and visualization, while allowing us to naturally express
mation is specifically targeted to concurrent hardware units,        semantically-loaded transformations on Wave operations.
such as matrix cores that can run concurrently to memory             Symbolic expressions for index sequences and parameters
accesses. It starts by identifying MMA operations nested             are encoded using Sympy (Meurer et al., 2017). Control flow
in an explicit loop whose indexing symbol also indexes the           constructs are captured as attributes referencing torch.fx
operands, which is a pre-requirement for software pipelin-           subgraphs.
ing. It then identifies reads and writes surrounding this
                                                                     After tracing, Wave compiler lowers the abstraction level,
operation, along with eventual casts, and ensures there are
                                                                     performing type and index sequence inference, followed
not interfering writes that would violate dependencies. It
                                                                     by graph expansion (section 3) using the torch.fx abstrac-
first applies classical software pipelining by issuing reads
                                                                     tion. Following expansion, tile sizes are fixed and no longer
for a hardware-specific initiation interval k of loop itera-
                                                                     parametric. It then optionally applies high-level optimiza-
tions, before alternating reads for iteration i+k with MMAs
                                                                     tions (section 4) and lowers to MLIR base (arith, vector,
and writes for iteration i and an epilogue consisting only of
                                                                     scf) and vendor-specific (amdgpu) dialects for low-level
the latter. It additionally doubles the amount of subgroups
                                                                     optimization and device-specific code generation as shown
and distributes loads and MMAs between them. Thus when
                                                                     in Figure 1. Fully lowered Intermediate Representation (IR)
a subgroup is stalled waiting for data to arrive, the Compute
                                                                     fixes all parameters and is no longer symbolic in any aspect.
Unit (CU) can rapidly context switch to another subgroup
                                                                     It is finally (JIT-)compiled via LLVM into a binary.
that is ready to perform an MMA.
Workgroup Reordering: Common GPU architectures have                  5.2   Integration
multiple CUs that share the same L2 cache. Direct dis-
tribution of (tiled) data across workgroup may result in             Wave kernels integrate with PyTorch as nn.Module sub-
workgroups executing on units with shared cache access-              classes, adhering to PyTorch’s C++ ABI for zero-copy ten-
ing distant, non-overlapping data regions leading to cache           sor access. A compilation cache amortizes overhead across
thrashing and ultimately decreased memory subsystem per-             repeated invocations. We further integrated Wave as an
formance. Workgroup reordering changes how workgroup                 attention backend for SGLang (Zheng et al., 2024), imple-
data tiles are mapped to actual workgroups to allow for bet-         menting an ABI-conforming adapter layer that eliminates
ter cache locality, (e.g., using stripes of consecutive tiles        data copies while supporting KV cache management. The
or Morton order (Perdacher et al., 2020)). This transforma-          implementation provides extend (Jin et al., 2024), paged
tion can be easily achieved by first defining a closed-form          decode (Kwon et al., 2023), and prefill (Kamath et al., 2025)
expression mapping physical workgroup indices to logical             attention variants for LLM inference.
indices following the desired grouping strategy, referred to
as a reordering constraint, and then substituting this expres-
                                                                     6     E VALUATION
sion instead of physical indices in the index sequences. This        6.1   Experimental Setup
process is purely mechanical thanks to Wave maintaining
symbolic expressions at this stage.                                  We evaluate standalone Wave kernels on the following sys-
                                                                     tems: 1) AMD EPYC 9554 64-Core CPU with eight AMD
                                                                     Instinct MI300x GPUs (MI300), 2) AMD EPYC 9655 96-
5     I MPLEMENTATION                                                Core CPU with eight AMD Instinct MI325x GPUs (MI325)
5.1   Compiler Architecture                                          and 3) AMD Ryzen ThreadRipper PRO 7995WX 96-Core
                                                                     CPU with 2 Radeon RX9070XT GPUs, all running running
The Wave compiler combines the flexibility of the torch.fx           Ubuntu Linux 22.04 and AMD ROCm package 7.0.0-38.
ecosystem (Reed et al., 2022) at the high level with the             We use Wave version 3.7.0 with IREE version 3.7.0 with
robustness of the industry-grade MLIR infrastructure at the          LLVM/MLIR at 8243dfc.
low level.
                                                                     For baselines, we use PyTorch 2.8.0 (Ansel et al., 2024), Tri-
We use the torch.fx tracing compiler to process Wave DSL             ton 3.4.0 (Tillet et al., 2019), the Tile language (Wang et al.,
and produce an initial graph capturing the computational             2025) 0.1.6.post1+5d66d32a and TVM 0.19 (Chen
structure of the kernel. By itself, it supports a handful of         et al., 2018) with Ansor (Zheng et al., 2020) and MetaSched-
nodes, including call_function that allows “calls” to                ule scheduling algorithms.
opaque operators. We leverage this by embedding Wave
operations into the node attribute storage and rely on Python        All measurements are taken using PyTorch profiler sup-
type casting to reconstruct Python classes corresponding to          port, which defers to vendor-provided benchmarking util-
these operations on-the-fly when desired. This preserves             ities. For each kernel, we execute 20 warmup invocations
compatibility with torch.fx tooling, including debugging             and 100 measured invocations We measure the time taken
                                                                     by the kernel on device and report arithmetic means. To
                                                                 7
compare Wave and Triton fairly on attention with differ-
ent layouts across different hardware, we standardized hy-
perparameter tuning. Triton attention kernel evaluated in
this work, autotunes six presets per shape (covering block
sizes, num_warps, waves per execution unit, and related
constraints). We mirrored this by testing six Wave config-
urations per layout, varying block sizes and MMA variant,
then selecting a single machine-specific configuration ap-
plied uniformly across all shapes. Autotuning takes on the
order of single-digit minutes per kernel, depending on kernel
complexity.
                                                                                    (a) BHSD attention throughput.
6.2   Attention Kernel Performance
We evaluated attention kernels extracted from popular
LLMs and diffusion models with the BxHxS_qxS_kxD
shapes as follows: Llama-2-13B 1x40x706x706x128;
Llama-3.1-8B 1x32x642x642x128; Llama-3.3-70B
1x64x642x642x128; Mistral-7B 1x32x706x706x128;
Stable    Diffusion    XL      (SDXL)        cross-attention
2 ⇥ 20 ⇥ 1024 ⇥ 1024 ⇥ 64, where B stands for
batch size, H for head count, D for head size and Sq , Sk
are query and key sequence dimensions. We compared                                  (b) BSHD attention throughput.
against PyTorch (eager) and Triton in prefill mode.
                                                                    Figure 2. Attention throughput on MI300X (higher is better).
We evaluated two data layouts: BHSD (Batch, Heads, Se-
quence, other Dimensions) and BSHD (Batch, Sequence,
Heads, other Dimensions). The former is the default format
for attention query, key and value tensors. The latter layout
transposes head and (both) sequence dimensions to improve
locality for operations across heads. In Wave, it can be
obtained by simply swapping the symbolic indexing dimen-
sions at the function boundary and having them propagated
through the kernel.
For BHSD attention (MI300/MI325), we use the follow-
ing tile sizes: BLOCK_B=1, BLOCK_M=128, BLOCK_N=128,
BLOCK_K2=64. For BSHD attention (MI300/MI325),
we set BLOCK_B=1, BLOCK_H=2, BLOCK_D_KV=64,
BLOCK_N_KV=32, with BLOCK_N_Q=64 (MI300) and 128
(MI325). For matrix multiplication (all hardware), we use                           (a) BHSD attention throughput.
BLOCK_M=128, BLOCK_N=128, BLOCK_K2=64.

For BHSD layout on MI300X (all listed shapes), Wave is 5-
294% (harmonic mean 101.7%) than PyTorch (eager), and
6-152% (harmonic mean 37.8%) faster than Triton. From
the performance counters for LLama-2.1-13b-hf, we see that
Wave trades a small drop in nominal per-CU occupancy ver-
sus Triton (0.566 vs 0.622) for on-chip reuse by using 34 KB
more Local Data Store (CDNA term for workgroup/shared
memory) (LDS), which aligns with the higher end-to-end
throughput observed versus PyTorch and the competitive                              (b) BSHD attention throughput.
performance versus Triton.
                                                                    Figure 3. Attention throughput on MI325x (higher is better).
For BSHD layout on MI325X (all listed shapes), On
MI300X (BHSD layout), Wave is 20–166% (harmonic mean
                                                                8
                (a) BHSD attention throughput.

                                                                     Figure 5. Distribution of speedups for matmul kernels. Wave is
                                                                     predominantly faster than Triton and is faster than PyTorch com-
                                                                     piler in ⇡ 60% of the cases relevant for LLMs.


                                                                     Table 1. Matmul shapes selected from major transformer models
                                                                     based on kernel invocation count and total multiply-accumulate
                                                                     operations, and their operational intensity.
                                                                           Model / Src        M         N       K      Op.Int.
                (b) BSHD attention throughput.
                                                                           Llama-2-13B         706     5120    5120     499.41
                                                                           Llama-3.3-70B       642    28672    8192     544.47
Figure 4. Attention throughput on RX9070XT (higher is better).             Llama-3.1-8B        642    14336    4096     472.66
                                                                           Mistral-7B-v0.3     706    14336    4096     506.47
                                                                           GPT-OSS-20B       20512    5760     2880    1090.74
94.3%) faster than PyTorch (eager), and ranges from 15%                    GPT-OSS-20B       20512    2880     2880     917.08
slower to 74% faster than Triton (harmonic mean 8.2%                       Square             8192    8192     8192     2048.0
faster). For Llama-2-13b-hf, Wave and Triton show similar
core-attention occupancy (approx. 1.0002 waves/active CU
and approx. 0.64 waves/CU), but Wave uses 25 KB LDS                  6.3    Matrix Multiplications
per workgroup while Triton uses none, trading extra on-chip
                                                                     We evaluate 573 out of 626 transposed matrix multiplication
storage for reuse. The PyTorch kernel shows a Mean Occu-
                                                                     (C = AB T ) shapes extracted from LLama 2 and 3, Mistral-
pancy/ActiveCU = 7.46 and Mean Occupancy/CU = 6.83.
                                                                     7B and GPT-OSS-20B LLMs captured in the IREE kernel
It uses more global writes/reads between pipeline stages,
                                                                     benchmark (Nod-AI, 2024) that are not matrix-vector mul-
which degrades end-to-end attention throughput.
                                                                     tiplications, (i.e., M 6= 1) . We complement this set with
We also evaluate BHSD and BSHD layouts on a Radeon                   a 81923 case. All cases are evaluated using float16 pre-
GPU Figure 4. For BHSD, the Wave kernel outperforms                  cision. In most cases, Wave delivers superior performance
PyTorch fused kernels for fp16 attention with Llama-2-13b            compared to both PyTorch (compile) and Triton, with the
shape on a RT9070XT. Although both have a similar num-               distribution shown in Figure 5.
ber of instructions, the Wave IR shows key advantages,
                                                                     We selected a small subset of frequently-invoked kernels,
namely, it uses 5 llvm.amdgcn.ds.bpermute intrinsics
                                                                     presented in Table 1 with high multiply-accumulate counts
for in-register reductions, about 48 LDS loads, and only 6
                                                                     for a deeper investigation. These specific shapes offer broad
barriers. In contrast, PyTorch kernels rely on 1–6 global and
                                                                     case coverage. The target hardware has a FP16 MMA peak
LDS loads and up to 9 barriers, without any ds.bpermute
                                                                     of 940 TFLOP/s and bandwidth of 42 TB/s for HBM, 76
or mfma intrinsics, indicating LDS-based reductions. These
                                                                     TB/s for L2 and 119 TB/s for L1 cache, and 177 TB/s for
differences reduce synchronization overhead and LDS traf-
                                                                     LDS, leading to the machine balance of 22.4, 12.4, 7.9
fic in Wave, leading to higher throughput through radeon-
                                                                     and 5.3 for these parts of the memory subsystem respec-
specific wavefront optimizations. Similarly, for BSHD lay-
                                                                     tively. For example, the 81923 case has the arithmetic inten-
out, Wave performs about 60 LDS loads and 18 stores in
                                                                     sity as shown in Table 1 FLOPs/byte, making it compute-
LDS with minimal global-memory use, yielding an LDS
                                                                     bound. We indeed practically measured its performance at
share of ⇡ 80% compared to only 30–35% in PyTorch
                                                                     900 TFLOP/s against 940 TFLOP/s peak.
fused kernels. In contrast, PyTorch relies on 2–6 global and
LDS loads per fused kernel with up to 8–10 barrier calls             In contrast, the 706 ⇥ 14336 ⇥ 4096 case is bound by L1
and lacks any ds.bpermute or mfma intrinsics, signaling              and L2 bandwidth rather than compute throughput. The
heavier LDS-based synchronization across fused kernels.              achieved arithmetic intensity (approx. 10–20 FLOPs/Byte)
                                                                 9
falls near the L1 ridge point (approx. 12 FLOPs/Byte) and
below the L2 ridge point (approx. 28 FLOPs/Byte). The cor-
responding throughput (approximately 350–400 TFLOP/s)
remains well below the 940 TFLOP/s compute roof, de-
spite having a similar instruction-level structure. Small M
and large N reduce output reuse per tile and increases the
number of global and L2 reloads for B fragments.
We compare throughput of these kernels with PyTorch (com-
pile), Triton, TileLang, TVM Ansor and MetaSchedule.
For Triton, we invoke the gemm_a16w16 from the ROCm
AITER library that defers to a tuned Triton implementation.
For TileLang, we use kernels from the TileLang Benchmark             (a) MI300x: Matmul (AB > ) throughput across frameworks
Suite (Tile-AI, 2024).                                               (higher is better).
As seen in Figure 6a, Wave outperforms Triton and Py-
Torch by 20–30% for larger models (GPT OSS 20B, Llama
3.3 70B) and offers performance comparable to Triton on
smaller models (LLama 2 13B, Llama 3.1 8B and Mistral),
where PyTorch outperforms both by 10–20%, deferring to
optimized libraries. Wave is 2⇥ faster than TileLang on
average and outperforms TVM-based solutions by an order
of magnitude. The latter struggle to converge after over
4096 iterations, or at least one successful compilation con-
figuration and is lacking lower-level optimizations targeting
AMD instruction sets.
Instruction-level analysis           demonstrates      that          (b) MI325x: Matmul (AB > ) throughput across frameworks
Wave successfully utilizes         matrix cores through              (higher is better).
v_mfma_f32_16x16x16_f16 instructions for 16⇥16⇥16
float16 multiply float32 accumulation, as well as effi-              Figure 6. Matmul (AB > ) throughput comparison across frame-
cient data staging via ds_read2_b64/ds_write2_b64                    works. Wave outperforms alternatives for large models and re-
for wide LDS access as well as buffer_load_dwordx4                   mains in the same performance segment as Triton and PyTorch for
and buffer_store_dwordx4 for coalesced vector-                       smaller models.
ized global memory access. For large, well-aligned
shapes such as 20512x5760x2880, the kernels remain
largely branch-free, and only minor predication appears
to handle residual macro-tiles—less than 0.2% of the                 changing target parameters (e.g., threads_per_wave and
workload. In contrast, smaller or irregular shapes (e.g.,            the MFMA variant) and re-tuning, without modifying the
706 ⇥ 14336 ⇥ 4096), exhibit a higher proportion of partial          kernel algorithm. Between Wave and PyTorch, Wave
tiles, increasing inactive lanes and triggering additional           is on par or better for almost all kernels. For GEMM
predicated control flow. Combined with higher Vector                 specifically, Wave is 4–47% faster than PyTorch (har-
General-Purpose Register (VGPR) usage, which restricts               monic mean 13.9%). Porting Wave to Radeon only re-
the number of active waves per compute unit (AMD, 2024),             quired setting threads_per_wave=32, and mma_type
these reduce the effective utilization of both compute and           = MMAType.RDNA4_F32_16x16x16_F16 to adhere to
memory pipelines, particularly for large-tile matmuls. The           the underlying hardware specification.       The variant
assembly inspection shows that Wave makes efficient use              RDNA4_F32_16x16x16_F16 specifies the instrinsic dimen-
of hardware-level primitives such as MFMA pipelines and              sions and data types. Based on our static analysis, Wave
prefetch mechanisms. Nonetheless, residual performance               IR had 32 WMMA calls (vs 16 in PyTorch) which keeps
gaps arise from occupancy and boundary-condition                     a higher compute/memory balance and reduces bandwidth
constraints on irregular shapes, indicating that additional          pressure. WMMA calls are tile-level Wave Matrix Multiply-
low-level tuning and launch-configuration optimization               Accumulate operations that use the GPU’s matrix cores. It
could further improve efficiency.                                    does fewer data-motion ops (64 vector ops vs 168, 44 loads
                                                                     vs 92), further lowering instruction and memory overhead.
On RX9070RT, Figure 7, we ran Wave unchanged at the al-              There are only 2 branches in Wave vs 34 in PyTorch, which
gorithm level to show performance portability. This required         reduces divergence.
                                                                10
                                                                           synchronizations instead of 32), which compounds the over-
                                                                           all throughput drop from 215.6 to 40.1 TFLOPs.

                                                                           7   D ISCUSSION
                                                                           While our evaluation targets the latest generations of AMD
                                                                           GPUs as this platform is underserved by publicly available
                                                                           tooling, Wave is designed with retargetability in mind. By
                                                                           exposing target-specific constants, such as the threads per
                                                                           wave/warp and matrix multiplication configurations, as tun-
Figure 7. Wave Performance (throughput)            portability   to        able parameters, Wave kernels can be adapted to different
RX9070RT for Matmul (AB > )
                                                                           architectures with minimal modification provided MLIR/L-
                                                                           LVM backend support. This is demonstrated by moving
                                                                           from MI300/MI325 (CDNA3 architecture with 64 threads
                                                                           per wave, mma instructions) to RX9070RT (RDNA4 archi-
                                                                           tecture with 32 threads per wave, mfma instructions). It is
                                                                           technically possible to execute wave on Nvidia hardware;
                                                                           however, only the memory optimizations would be rele-
                                                                           vant, since wgmma instructions on the Hopper and Blackwell
                                                                           architectures operate directly on shared memory.
                                                                           Our implicit indexing design enables portability across dif-
                                                                           ferent MMA engines and makes the code more compact.
                                                                           For example, the extend attention kernel would use 15 addi-
Figure 8. Disabling optimizations (section 4) results in systematic        tional statements to compute memory indices without it.
performance degradation.

                                                                           8   R ELATED W ORK
6.4   Impact of Optimizations
                                                                           GPU DSLs span a wide design space in how they ex-
To evaluate the effect of optimizations implemented in Wave,               press indexing, expose/automate parallelism, and separate
we perform an ablation study selectively disabling them for                algorithms from schedules. Triton (Tillet et al., 2019)
the kernels from the previous section. Configuration remains               and Helion (Pytorch, 2025) emphasize block-level control;
unchanged and no re-tuning is performed.                                   Exo (Ikarashi et al., 2025) offers correctness-preserving
As demonstrated in Figure 8, disabling global memory opti-                 rewrites; Lift/Elevate (Lücke et al., 2021) use functional
mizations (subsection 4.1) results in a 2.7–4.4⇥ slowdown                  combinators; and Halide (Ragan-Kelley et al., 2013) /
(harmonic mean 3.0⇥) whereas disabling scheduling opti-                    TVM (Chen et al., 2018) derive loop nests from schedules.
mization (subsection 4.2) results in a 24–35% slowdown                     Wave proposes an alternative: it represents computation and
(harmonic mean 30%). Effects of optimizations are ob-                      distribution with symbolic index sequences that defer con-
served across all cases, with smaller kernels affected more,               crete indices to codegen, enabling uniform reasoning about
4.4⇥ global memory-induced slowdown for LLama-2 13b                        mapping patterns without manual loop transformations.
compared to 2.7⇥ for GPT OSS 20B, since they are strug-                    Rather than an explicit, user-written schedule (as in Halide
gling to hide access latency by the amount of parallelism.                 /TVM or template-driven transforms like Fireiron (Hage-
Assembly-level analysis of LLama-2-13b shows that the ker-                 dorn et al., 2020), Wave composes symbolic constraints and
nel uses 12 buffer_load_dwordx4 instructions for 128-                      propagates dependencies so that fused kernels and execu-
bit loads per thread with global memory optimization and 20                tion order emerge from the composition itself. This makes
buffer_load_dwordx2 for 64-bit loads without it. We ad-                    scheduling declarative and analytical instead of procedural,
ditionally capture TCC_EA0_RDREQ and TCC_EA0_WRREQ                         unifying index reasoning, mapping, and synchronization
performance counters for 32B reads and 64B writes, respec-                 within one abstraction layer.
tively, and calculate memory throughput by dividing the                    On programming model and abstraction, CUDA/HIP ex-
total amount of data read and written by the kernel duration.              pose thread-level control (max flexibility, high complex-
Global memory optimization increases memory bandwidth                      ity), while Triton/Helion operate at block granularity and
from 248 GB/s to 599 GB/s. Inefficient loads also increase                 Halide/TVM/Lift/Elevate work at loop/functional levels.
usage of other storage (244 VGPR instead of 192, 80 LDS-                   Wave targets the wavefront/subgroup-level to align with
accessing instructions instead of 24 with 46 s_waitcnt
                                                                      11
modern GPU execution and tensor-core cooperation. In                 Bik, A., Koanantakool, P., Shpeisman, T., Vasilache, N.,
memory control, existing systems range from automated                  Zheng, B., and Kjolstad, F. Compiler support for sparse
staging (Triton/Helion) to directive-driven (Halide/ TVM)              tensor computations in mlir. ACM Transactions on Ar-
to fully manual (Exo). Wave adopts a hybrid stance: global-            chitecture and Code Optimization (TACO), 19(4):1–25,
to-shared movement is implicit while memory-to-register                2022.
movement is explicit in the code.
                                                                     Chen, T., Moreau, T., Jiang, Z., Zheng, L., Yan, E., Shen, H.,
                                                                       Cowan, M., Wang, L., Hu, Y., Ceze, L., et al. {TVM}: An
9   C ONCLUSION                                                        automated {End-to-End} optimizing compiler for deep
In this work, we introduced Wave, a lightweight and exten-             learning. In 13th USENIX Symposium on Operating Sys-
sible framework for defining and compiling custom GPU                  tems Design and Implementation (OSDI 18), pp. 578–594,
kernels. By abstracting hardware-specific details while re-            2018.
taining fine-grained control, Wave bridges the gap between
                                                                     Choquette, J. Nvidia Hopper GPU: Scaling Performance .
low-level performance tuning and high-level programmabil-
                                                                       In 2022 IEEE Hot Chips 34 Symposium (HCS), pp. 1–46,
ity. We believe Wave can serve as a foundation for exploring
                                                                       Los Alamitos, CA, USA, August 2022. IEEE Computer
new kernel designs and compiler transformations across a
                                                                       Society. doi: 10.1109/HCS55958.2022.9895592. URL
range of workloads. Future efforts will focus on improving
                                                                       https://doi.ieeecomputersociety.org/
interoperability with existing MLIR-based infrastructures
                                                                       10.1109/HCS55958.2022.9895592.
and expanding support for emerging GPU architectures.
Another direction is to extend Wave’s indexing symbols               Choquette, J., Gandhi, W., Giroux, O., Stam, N., and
to express irregular and sparse tensors using dependent-               Krashinsky, R. Nvidia a100 tensor core gpu: Perfor-
iterator semantics (Kjolstad et al., 2017), without changing           mance and innovation. IEEE Micro, 41(2):29–35, 2021.
the control-flow primitives. This can leverage MLIR’s exist-           doi: 10.1109/MM.2021.3061394.
ing sparse tensor compilation (Bik et al., 2022) support.
                                                                     Hagedorn, B., Elliott, A. S., Barthels, H., Bodík, R.,
R EFERENCES                                                            and Grover, V. Fireiron: A scheduling language
                                                                       for high-performance linear algebra on gpus. CoRR,
Adelstein Lelbach, B. cuTile, the new/old kid on the block:            abs/2003.06324, 2020. URL https://arxiv.or
  Python programming models for GPUs. In SciPy 2025,                   g/abs/2003.06324.
  July 2025.
                                                                     Ikarashi, Y., Qian, K., Droubi, S., Reinking, A., Bernstein,
AMD. AMD GPU Architecture and Programming Docu-                        G. L., and Ragan-Kelley, J. Exo 2: Growing a scheduling
 mentation. https://gpuopen.com/amd-gpu-a                              language. In Proceedings of the 30th ACM International
 rchitecture-programming-documentation                                 Conference on Architectural Support for Programming
 /, 2024.                                                              Languages and Operating Systems, Volume 1, pp. 426–
                                                                       444, 2025.
Ansel, J., Yang, E., He, H., Gimelshein, N., Jain, A., Voz-
  nesensky, M., Bao, B., Bell, P., Berard, D., Burovski, E.,         Jin, H., Han, X., Yang, J., Jiang, Z., Liu, Z., Chang, C.-
  Chauhan, G., Chourdia, A., Constable, W., Desmaison,                  Y., Chen, H., and Hu, X. Llm maybe longlm: Self-
 A., DeVito, Z., Ellison, E., Feng, W., Gong, J., Gschwind,             extend llm context window without tuning. arXiv preprint
  M., Hirsh, B., Huang, S., Kalambarkar, K., Kirsch, L.,                arXiv:2401.01325, 2024.
  Lazos, M., Lezcano, M., Liang, Y., Liang, J., Lu, Y.,
  Luk, C. K., Maher, B., Pan, Y., Puhrsch, C., Reso, M.,             Jouppi, N. P., Young, C., Patil, N., Patterson, D., Agrawal,
  Saroufim, M., Siraichi, M. Y., Suk, H., Zhang, S., Suo, M.,          G., Bajwa, R., Bates, S., Bhatia, S., Boden, N., Borchers,
 Tillet, P., Zhao, X., Wang, E., Zhou, K., Zou, R., Wang,              A., Boyle, R., Cantin, P.-l., Chao, C., Clark, C., Coriell,
 X., Mathews, A., Wen, W., Chanan, G., Wu, P., and                     J., Daley, M., Dau, M., Dean, J., Gelb, B., Ghaem-
  Chintala, S. Pytorch 2: Faster machine learning through              maghami, T. V., Gottipati, R., Gulland, W., Hagmann,
  dynamic python bytecode transformation and graph com-                R., Ho, C. R., Hogberg, D., Hu, J., Hundt, R., Hurt,
  pilation. In Proceedings of the 29th ACM International               D., Ibarz, J., Jaffey, A., Jaworski, A., Kaplan, A., Khai-
 Conference on Architectural Support for Programming                   tan, H., Killebrew, D., Koch, A., Kumar, N., Lacy, S.,
 Languages and Operating Systems, Volume 2, ASPLOS                     Laudon, J., Law, J., Le, D., Leary, C., Liu, Z., Lucke,
 ’24, pp. 929–947, New York, NY, USA, 2024. Associa-                   K., Lundin, A., MacKean, G., Maggiore, A., Mahony,
  tion for Computing Machinery. ISBN 9798400703850.                    M., Miller, K., Nagarajan, R., Narayanaswami, R., Ni,
  doi: 10.1145/3620665.3640366. URL https:                             R., Nix, K., Norrie, T., Omernick, M., Penukonda, N.,
 //doi.org/10.1145/3620665.3640366.                                    Phelps, A., Ross, J., Ross, M., Salek, A., Samadiani, E.,
                                                                12
  Severn, C., Sizikov, G., Snelham, M., Souter, J., Stein-            https://doi.org/10.1145/3446804.3446
  berg, D., Swing, A., Tan, M., Thorson, G., Tian, B.,                844.
  Toma, H., Tuttle, E., Vasudevan, V., Walter, R., Wang, W.,
  Wilcox, E., and Yoon, D. H. In-datacenter performance             Luebke, D. Cuda: Scalable parallel programming for high-
  analysis of a tensor processing unit. SIGARCH Com-                  performance scientific computing. In 2008 5th IEEE
  put. Archit. News, 45(2):1–12, June 2017. ISSN 0163-                International Symposium on Biomedical Imaging: From
  5964. doi: 10.1145/3140659.3080246. URL https:                      Nano to Macro, pp. 836–838, 2008. doi: 10.1109/ISBI.2
  //doi.org/10.1145/3140659.3080246.                                  008.4541126.

Kamath, A. K., Prabhu, R., Mohan, J., Peter, S., Ramjee,            Meurer, A., Smith, C. P., Paprocki, M., Čertík, O., Kirpichev,
  R., and Panwar, A. Pod-attention: Unlocking full prefill-          S. B., Rocklin, M., Kumar, A., Ivanov, S., Moore, J. K.,
  decode overlap for faster llm inference. In Proceedings of         Singh, S., et al. Sympy: symbolic computing in python.
  the 30th ACM International Conference on Architectural             PeerJ Computer Science, 3:e103, 2017.
  Support for Programming Languages and Operating Sys-
  tems, Volume 2, pp. 897–912, 2025.                                Munshi, A. The opencl specification. In 2009 IEEE Hot
                                                                     Chips 21 Symposium (HCS), pp. 1–314, 2009. doi: 10.1
Kjolstad, F., Chou, S., Lugato, D., Kamil, S., and Amaras-           109/HOTCHIPS.2009.7478342.
  inghe, S. Taco: A tool to generate tensor algebra kernels.
  In 2017 32nd IEEE/ACM International Conference on Au-             Nod-AI.          IREE Kernel Benchmark,         2024.
  tomated Software Engineering (ASE), pp. 943–948. IEEE,              URL h t t p s : / / g i t h u b . c o m / n o d - a
  2017.                                                               i / i r e e - k e r n e l- b e n c h m a rk.    See
                                                                      iree_kernel_benchmark/gemmbench/problems.py.
Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,
 C. H., Gonzalez, J., Zhang, H., and Stoica, I. Efficient           Paszke, A., Gross, S., Massa, F., Lerer, A., Bradbury, J.,
 memory management for large language model serving                   Chanan, G., Killeen, T., Lin, Z., Gimelshein, N., Antiga,
 with pagedattention. In Proceedings of the 29th Sym-                 L., Desmaison, A., Köpf, A., Yang, E., DeVito, Z., Rai-
 posium on Operating Systems Principles, SOSP ’23, pp.                son, M., Tejani, A., Chilamkurthy, S., Steiner, B., Fang,
 611–626, New York, NY, USA, 2023. Association for                    L., Bai, J., and Chintala, S. PyTorch: An Imperative
 Computing Machinery. ISBN 9798400702297. doi:                        Style, High-Performance Deep Learning Library, Decem-
 10.1145/3600006.3613165. URL https://doi.                            ber 2019. URL http://arxiv.org/abs/1912.0
 org/10.1145/3600006.3613165.                                         1703. arXiv:1912.01703 [cs, stat].
Lam, M. Software pipelining: an effective scheduling tech-
                                                                    Perdacher, M., Plant, C., and Böhm, C. Improved data
  nique for vliw machines. SIGPLAN Not., 23(7):318–328,
                                                                      locality using morton-order curve on the example of lu
  June 1988. ISSN 0362-1340. doi: 10.1145/960116.54022.
                                                                      decomposition. In 2020 IEEE International Conference
  URL https://doi.org/10.1145/960116.5
                                                                      on Big Data (Big Data), pp. 351–360, 2020. doi: 10.110
  4022.
                                                                      9/BigData50022.2020.9378385.
Lattner, C. and Adve, V. Llvm: A compilation framework
  for lifelong program analysis & transformation. In Inter-         Pytorch. Helion. https://github.com/pytorch
  national symposium on code generation and optimization,             /helion, 2025.
  2004. CGO 2004., pp. 75–86. IEEE, 2004.
                                                                    Ragan-Kelley, J., Barnes, C., Adams, A., Paris, S., Durand,
Lattner, C., Amini, M., Bondhugula, U., Cohen, A., Davis,             F., and Amarasinghe, S. Halide: a language and compiler
  A., Pienaar, J., Riddle, R., Shpeisman, T., Vasilache,              for optimizing parallelism, locality, and recomputation in
  N., and Zinenko, O. Mlir: Scaling compiler infras-                  image processing pipelines. Acm Sigplan Notices, 48(6):
  tructure for domain specific computation. In 2021                   519–530, 2013.
  IEEE/ACM International Symposium on Code Gener-
  ation and Optimization (CGO), pp. 2–14, 2021. doi:                Rau, B. R. and Glaeser, C. D. Some scheduling techniques
  10.1109/CGO51591.2021.9370308.                                      and an easily schedulable horizontal architecture for high
                                                                      performance scientific computing. ACM SIGMICRO
Lücke, M., Steuwer, M., and Smith, A. Integrating a func-             Newsletter, 12(4):183–198, 1981.
  tional pattern-based ir into mlir. In Proceedings of the
  30th ACM SIGPLAN International Conference on Com-                 Reed, J., DeVito, Z., He, H., Ussery, A., and Ansel, J. torch.
  piler Construction, CC 2021, pp. 12–22, New York, NY,               fx: Practical program capture and transformation for deep
  USA, 2021. Association for Computing Machinery. ISBN                learning in python. Proceedings of Machine Learning
  9781450383257. doi: 10.1145/3446804.3446844. URL                    and Systems, 4:638–651, 2022.
                                                               13
ROCm Authors.        HIP documentation - AMD                        structured language model programs. In Globerson, A.,
 ROCm™software 7.0.2.     AMD ROCm™Software                         Mackey, L., Belgrave, D., Fan, A., Paquet, U., Tom-
 (online), 2025. URL https://rocm.docs.amd.                         czak, J., and Zhang, C. (eds.), Advances in Neural In-
 com/projects/HIP/en/latest/.                                       formation Processing Systems, volume 37, pp. 62557–
                                                                    62583. Curran Associates, Inc., 2024. URL https:
Salykov, A., Luo, A., Huang, C., and Sun, P. Matrix core            //proceedings.neurips.cc/paper_files
  programming on AMD CDNA™ 3 and CDNA™ 4 ar-                        /paper/2024/file/724be4472168f31ba1c
  chitecture – AMD ROCm blogs. AMD ROCm blogs                       9ac630f15dec8-Paper-Conference.pdf.
  (online), September 2025. URL https://rocm.blo
  gs.amd.com/software-tools-optimizatio
  n/matrix-cores-cdna/README.html.

Shah, J., Bikshandi, G., Zhang, Y., Thakkar, V., Ramani,
  P., and Dao, T. Flashattention-3: Fast and accurate at-
  tention with asynchrony and low-precision. In Glober-
  son, A., Mackey, L., Belgrave, D., Fan, A., Paquet, U.,
  Tomczak, J., and Zhang, C. (eds.), Advances in Neural
  Information Processing Systems, volume 37, pp. 68658–
  68685. Curran Associates, Inc., 2024. URL https:
  //proceedings.neurips.cc/paper_files
  /paper/2024/file/7ede97c3e082c6df10a
  8d6103a2eebd2-Paper-Conference.pdf.

Sitaraman, G., McDougall, D., Oostrum, R. V., Malaya,
   N., Chalmers, N., and O’Reilly, O. AMD matrix cores
  – AMD ROCm™blogs, November 2022. URL https:
  //rocm.blogs.amd.com/software-tools-o
   ptimization/matrix-cores/README.html.

Tile-AI. TileLang Benchmark Suite. https://gith
  ub.com/tile-ai/tilelang-benchmark, 2024.
  Accessed: 2024.

Tillet, P., Kung, H. T., and Cox, D. Triton: an interme-
  diate language and compiler for tiled neural network
  computations. In Proceedings of the 3rd ACM SIG-
  PLAN International Workshop on Machine Learning
  and Programming Languages, pp. 10–19, Phoenix AZ
  USA, June 2019. ACM. ISBN 978-1-4503-6719-6. doi:
  10.1145/3315508.3329973. URL https://dl.acm
  .org/doi/10.1145/3315508.3329973.

Wang, L., Cheng, Y., Shi, Y., Tang, Z., Mo, Z., Xie, W.,
 Ma, L., Xia, Y., Xue, J., Yang, F., et al. Tilelang: A com-
 posable tiled programming model for ai systems. arXiv
 preprint arXiv:2504.17577, 2025.

Zheng, L., Jia, C., Sun, M., Wu, Z., Yu, C. H., Haj-Ali, A.,
  Wang, Y., Yang, J., Zhuo, D., Sen, K., et al. Ansor: Gen-
  erating {High-Performance} tensor programs for deep
  learning. In 14th USENIX symposium on operating sys-
  tems design and implementation (OSDI 20), pp. 863–879,
  2020.

Zheng, L., Yin, L., Xie, Z., Sun, C., Huang, J., Yu, C. H.,
  Cao, S., Kozyrakis, C., Stoica, I., Gonzalez, J. E., Bar-
  rett, C., and Sheng, Y. Sglang: Efficient execution of
                                                               14
