 AXL EARN : M ODULAR , H ARDWARE -AGNOSTIC L ARGE M ODEL T RAINING


            Mark Lee∗ 1 Chang Lan∗ 1 Tom Gunter† 1 John Peebles† 1 Hanzhi Zhou† 1 Kelvin Zou† 1
    Sneha Bangalore 1 Chung-Cheng Chiu 1 Nan Du 1 Xianzhi Du 1 Philipp Dufter 1 Liang He 1 Ruixuan Hou 1
     Haoshuo Huang 1 Dongseong Hwang 1 Xiang Kong 1 Jinhao Lei 1 Tao Lei 1 Meng Li 1 Li Li 1 Jiarui Lu 1
        Zhiyun Lu 1 Yiping Ma 1 David Qiu 1 Vivek Rathod 1 Senyu Tong 1 Zhucheng Tu 1 Chong Wang 1
    Jianyu Wang 1 Yongqiang Wang 1 Zirui Wang 1 Floris Weers 1 Sam Wiseman 1 Guoli Yin 1 Bowen Zhang 1
                       Xiyou Zhou 1 Danyang Zhuo§ 2 Cheng Leong 1 Ruoming Pang‡ 1


                                                            A BSTRACT
         AXLearn is a production system which facilitates scalable and high-performance training of large deep learning
         models. Compared to other state-of-the-art deep learning systems, AXLearn has a unique focus on modularity
         and support for hardware-agnostic training. AXLearn’s internal interfaces between software components follow
         strict encapsulation, allowing different components to be assembled to facilitate rapid model development and
         experimentation on different hardware infrastructure. AXLearn maintains constant complexity as we scale the
         components in the system, compared to linear or quadratic complexity in state-of-the-art training systems. This
         allows integrating features such as Rotary Position Embeddings (RoPE) into AXLearn across hundreds of modules
         with just 10 lines of code, compared to hundreds, as required in other systems. At the same time, AXLearn
         maintains equivalent performance compared to state-of-the-art training systems. We also share our experience in
         the development and operation of AXLearn at Apple.

1        I NTRODUCTION                                               initions and training methods. We call this modularity of
                                                                     the deep learning system. Second, as a large technology
Large-scale deep learning models are now integral to so-             company, we cannot rely on a single hardware vendor—any
ciety—they power chatbots such as ChatGPT and Gem-                   hardware can run into supply issues and vary in pricing. Our
ini (OpenAI, 2022; Google, 2025), enhance video confer-              design goal is to be hardware-agnostic, supporting GPU,
encing (Zoom, 2025), and support modern coding tools (Cur-           TPU, and AWS Trainium. This allows us to train on major
sor, 2025). Modern deep learning systems prioritize perfor-          cloud providers (e.g., AWS, Google Cloud, Azure) as well
mance and scalability to accommodate large models. Nu-               as our on-premises servers.
merous techniques have been explored to this end, including
parallelization strategies (Narayanan et al., 2021; Zheng            To facilitate modularity, the core design decision of
et al., 2022), memory optimizations (Rajbhandari et al.,             AXLearn is to enforce strict encapsulation. While encapsu-
2020; Zhao et al., 2023), and model-specific kernel opti-            lation is a well known principle in object oriented program-
mizations (Dao et al., 2022; Dao, 2023).                             ming, we find that it is often neglected in ML frameworks
                                                                     as almost all existing frameworks rely on subtyping. The
At Apple, we have integrated many AI models into our prod-           prevalence of subtyping can be partially attributed to the
ucts, catering to billions of users worldwide. We have two           lack of formal analysis on how to quantify the modularity
requirements for our deep learning systems besides training          and extensibility of a system, where conventional design
performance and scalability. First, we aim to empower our            principles or “rules of thumb” are non-exhaustive and hard
model engineers to experiment with diverse model archi-              to measure. To this end, we propose a framework for quan-
tectures and training techniques. They should write only a           tifying the complexity of a system by measuring asymptotic
minimal amount of code to configure complex model def-               Lines-of-Code (LoC) changes incurred by the addition of
     Apple 2 Duke University. ∗ Mark Lee and Chang Lan are
     1                                                               a new feature. Such a framework is necessary as the tra-
the first authors. † denotes the core authors. § Danyang Zhuo        ditional mechanistic way of counting LoC can only cap-
contributed to this work as a visiting scholar at Apple. ‡ Ruoming   ture complexity under a frozen snapshot, when extensibility
Pang is the corresponding author.                                    is really about how a system evolves over time. We also
Proceedings of the 9 th MLSys Conference (Industry Track), Belle-
                                                                     show that the framework is consistent with directly counting
vue, WA, USA, 2026. Copyright 2026 by the author(s).                 LoC via the case study of integrating Rotary Positional Em-
                                                                     beddings (RoPE) (Su et al., 2024) and Mixture of Experts
Table 1. Comparing AXLearn with state-of-the-art deep learning systems. We define “partial” as systems that decouple parallelism from
model implementation via device abstractions (e.g., XLA), but in practice do not apply strict encapsulation.

          System            Underlying Framework      Model Agnostic       3D Parallelism   Modular    GPU     TPU     Trainium
       Megatron-LM                PyTorch                                       ✓                       ✓
        DeepSpeed                 PyTorch                    ✓                  ✓                       ✓
      PyTorch FSDP                PyTorch                    ✓                                          ✓
    PyTorch XLA FSDP              PyTorch                    ✓                                          ✓       ✓
        TorchTitan                PyTorch                                        ✓          partial     ✓
          Haiku                     JAX                      ✓                   ✓          partial     ✓       ✓
           Flax                     JAX                      ✓                   ✓          partial     ✓       ✓
           Pax                      JAX                      ✓                   ✓          partial     ✓       ✓
         MaxText                    JAX                                          ✓          partial     ✓       ✓
      AXLearn (Ours)                JAX                      ✓                   ✓            ✓         ✓       ✓         ✓



(MoE) (Shazeer et al., 2017) into AXLearn as compared to            but these optimizations do not directly apply to other hard-
other state-of-the-art deep learning systems. To our knowl-         ware. Apple uses a diverse set of model architectures across
edge, AXLearn is the only training framework that adheres           a range of cloud backends, so we cannot directly use these
strictly to encapsulation—any module is replaceable, includ-        systems.
ing the input pipeline, checkpointer, trainer loop—allowing
                                                                    Over the past few years, we have deployed AXLearn to train
complex features to be implemented without increasing the
                                                                    thousands of models involving hundreds of engineers. The
complexity of the overall system.
                                                                    rapid adoption of AXLearn largely owes to its modularity
To enable hardware-agnostic training, we build AXLearn on           and unique ability to scale on various public clouds, includ-
top of XLA (xla, 2025) and GSPMD (Xu et al., 2021). Our             ing Google Cloud TPU, AWS GPU or Trainium2. We share
native integration with XLA allows parallelism strategies           how our experience evolved our design choices over several
to be automatically generated, but still allows hand-crafting       years. AXLearn is open-sourced under Apache 2.0 license
kernel code for specific accelerators. For instance, on each        at https://github.com/apple/axlearn.
hardware backend, we replace the attention layer with a
custom kernel like FlashAttention (Dao et al., 2022; Dao,            2     M OTIVATION
2023). We believe this design strikes an ideal balance: by
leveraging the XLA ecosystem, we can seamlessly support              2.1    Modularity
multiple hardware accelerators without sacrificing high per-
                                                                     In existing ML systems, neural network layers are imple-
formance. Due to its modular design, AXLearn also enables
                                                                     mented by subtyping: a layer inherits from some base layer,
succinct user configurations to customize the paralleliza-
                                                                     instantiates child layers as instance attributes, and imple-
tion, rematerialization, and quantization strategies, further
                                                                     ments a forward method that handles the layer’s logic.
simplifying the scaling experience.
                                                                     Consider the changes required to replace a feed-forward
Table 1 provides a list of deep learning systems designed            network (FFN) of a Transformer architecture with an MoE
for large models. Besides modularity and hardware require-           layer using subtyping. Taking the example from Deep-
ment, the table indicates whether each system is model-              Speed (DeepSpeed, 2025b), one applies such a change by
agnostic or supports broader parallelism strategies—such as          replacing the instance attribute for the original FFN:
3D parallelism (Narayanan et al., 2021)—to enable efficient
training across diverse model architectures. We summarize            - self.fc3 = nn.Linear(84, 10)
several important observations. Some systems only sup-               + self.fc3 = nn.Linear(84, 84)
                                                                     + self.fc3 = deepspeed.moe.layer.MoE(...)
port specific architectures: for example, MaxText (MaxText           + self.fc4 = nn.Linear(84, 10)
team, 2025) provides LLM implementations but does not
extend easily to custom architectures as its design encour-         At a glance, this seems like a simple 4 LoC change. How-
ages fork-and-modify rather than reusing building blocks.           ever, in practice one would need to subtype self.fc3’s parent
Similarly, TorchTitan (Liang et al., 2024) requires model-          layer to apply such a change. This effectively reduces the
specific parallel plans that traverse the model architecture,       problem of replacing the FFN with MoE, to the problem of
limiting its applicability to architectures not explicitly sup-     replacing the parent layer with the new subtyped layer. By
ported. Other systems are designed for specific backends:           induction, it is easy to see how such a change can compound
for instance, Megatron-LM (Narayanan et al., 2021) has              to changes to multiple modules across the subtype hierar-
carefully designed optimizations for Transformers on GPU,           chy. Indeed, this happens in practice: between DeepSpeed’s
                                                                                    be used to explain why composition should be favored over
        Transformer                Self Attn                      Attn              subtyping. The LoC-complexity(MoE) for DeepSpeed is
                                                                QKV proj            lower-bounded by O(N ), where N denotes the number of
                                                                  ...               modules in the system (§7): we require (1) at least one LoC
                                                                                    change for each attention variant to subtype a forward
                                     FFN                        Linear 1
                                                                                    implementation that replaces FFN with the MoE equivalent;
                                                                Linear 2            (2) at least one LoC change in each ancestor module of an
                            (a) Standard Transformer
                                                                                    attention layer to incorporate the new subtyped layer. In
  Transformer     Self Attn           Attn             Gating            Linear 1   contrast, the LoC-complexity(MoE) in AXLearn is O(1):
                                   QKV proj
                                                                                    the code snippet in §4 can be used to integrate MoE without
                                                       FFN 1             Linear 2
                                                                                    changing any model.
                                      ...               ...              Linear 1
                                                                                    In practice, we can validate the LoC-complexity with con-
                      FFN            MoE               FFN K             Linear 2
                                                                                    crete LoC counts. Indeed, in AXLearn we use the same
                              (b) MoE Transformer                                   10-line snippet to configure MoE in over 1,000 different
                                                                                    experiments1 . On the other hand, if we were to adopt Deep-
Figure 1. Specifying MoE Transformer in AXLearn. Blue compo-                        Speed’s strategy of integrating MoE to our internal codebase,
nents are reused from the specification of standard Transformer. In                 we would incur over 4,000 LoC just to modify different
AXLearn, a user script that defines MoE only needs to specify the                   model variants to support their MoE counterparts.
green parts of the neural network.
                                                                                    2.2   Hardware-Agnostic Training
                                                                                    As one of the largest technology companies, Apple cannot
QwenV2 and QwenV2 MoE implementations (DeepSpeed,                                   feasibly rely on a single hardware platform for all of our
2025a), over 200 LoC are required to apply the MoE layer,                           machine learning workloads. Aside from supply constraints,
not accounting for the MoE layer itself—a far cry from the                          our company is positioned to benefit from not committing to
4 LoC that we may have hoped.                                                       any single platform: Megatron-LM (Narayanan et al., 2021)
Conceptually, MoE is a small change—ideally, one should                             has a vested interest in optimizing for Nvidia GPUs, while
be able to “swap” the FFN directly with an MoE equivalent,                          Haiku (Haiku, 2025), Flax (Heek et al., 2023), Pax (PAX
without incurring the side-effects of propagating changes                           team, 2022), and MaxText (MaxText team, 2025) are mostly
up to ancestors like the Transformer layer. In AXLearn, this                        optimized for Google TPUs.
can be achieved by exploiting the compositional nature of                           Instead, we take the alternative approach of developing
neural networks: by implementing the FFN and MoE layers                             a hardware-agnostic system that can adapt seamlessly to
with compatible input/output interfaces, and encapsulating                          multiple platforms, which allows our engineers to take ad-
all MoE specific details within its layer implementation, we                        vantage of the most cost-effective solution for their training
realize this ideal scenario of treating MoE as a “drop-in”                          workload. For example, while AWS Trainium2 did not exist
replacement.                                                                        when we first began development, AXLearn is one of the
Note that by naively comparing the LoC between implemen-                            first deep learning systems that supports Trainium2 at scale.
tations, the benefits of composition cannot be fully observed.                      While JAX/XLA is a key component to support hardware-
Specifically, the 200 LoC difference between the two im-                            agnostic training, it is not sufficient. XLA often provides
plementations does not account for higher-order effects: if                         reasonable out-of-the-box performance, but additional steps
we consider that a production codebase may instead contain                          are needed to achieve the best performance—including pro-
tens to hundreds of variants of the same model, the 200 LoC                         viding “hints” to the XLA compiler at various points in the
change quickly becomes thousands of LoC.                                            layer graph, using hand-tuned kernels for less mature com-
We instead propose to measure the extensibility of a sys-                           pilation targets like GPU or Trainium2, or customizing the
tem in terms of the asymptotic LoC changes required by                              rematerialization strategy for each workload.
some re-parameterization of its API. When we refer to the
re-parameterization of the API, we essentially pose the ques-                       3     OVERVIEW
tion of how the LoC change when we reconfigure the system
to support a different implementation signature—such as                             AXLearn allows model engineers to rapidly experiment with
adding new functionality like the MoE layer. Note that while                        various model architecture and training methods across dif-
coding style affects absolute Lines of Code (LoC), it is less                           1
                                                                                          An experiment refers to a training job configuration, such as
likely to change the LoC-complexity (the rate of asymptotic                         the model architecture and hyper-parameters.
LoC growth relative to feature changes). This metric can
ferent hardware backends. AXLearn’s developer interface
is a hierarchical configuration of modules. A module can be
viewed abstractly as a node in an object tree. In training, the
root module is typically the trainer itself, with child modules
such as the model, learner, and input, each of which may
have its own children. A module’s definition follows a con-
sistent structure, including a config object that encapsulates
all configurable parameters of the module.
Our view is that for ML researchers, writing configs and
composing modules is much easier than implementing sub-
typed modules from existing ones, a view that is shared by
TorchTitan (Liang et al., 2024). The pure composition ap-
proach allows replacing individual components and makes
it easy to reuse components between teams or 3rd party
libraries. Similar to TorchTitan, our model definition is in-
dependent of any specific parallelization strategy or trainer
loop. However, from the developer experience point of
view, AXLearn has several key differences in how modules
are parameterized. First, AXLearn encourages hierarchical
config composition over config flattening. In the example          Figure 2. AXLearn’s system diagram. The blue components be-
shown in Figure 1, a user script can build on top of a stan-       long to AXLearn.
dard Transformer architecture by defining only the green           checkpointing, monitoring, and fault tolerance.
components, and selectively replacing FFN modules with
MoE equivalents without needing to inspect the details of
the base Transformer architecture. In contrast, TorchTitan         4     AXL EARN C OMPOSER
adopts a monolithic approach where config files “flatten” all      4.1   Modular Configuration
possible configurations, including model, optimizer, check-
pointer, and trainer configs. Flattening has significant ramifi-   Hierarchical Configuration TorchTitan, DeepSpeed, Flax,
cations in the extensibility of TorchTitan. (See §7.) Second,      and most other libraries utilize “flat” config layouts that pro-
AXLearn uses an entirely Python-based interface, which has         vide a bird’s-eye view of all configurations. However, a flat
the benefit of allowing configs to be expressed with Python        layout can become unwieldy as the number of layers grows,
constructs like functions, loops, and recursion; as well as        or when switching implementations of layers (in which case,
the advantage of being able to be directly unit-tested.            one must flatten multiple combinations of configurations).
Figure 2 shows AXLearn’s system diagram and workflow.              In AXLearn, users instead specify training configurations
AXLearn has two key components: (1) AXLearn composer               via composition, which logically forms a tree. As an exam-
and (2) AXLearn runtime. A user typically uses the layer           ple, a Transformer layer’s configuration consists of multiple
library in AXLearn to define a training configuration. Given       child layer configurations:
such a script, the AXLearn composer first materializes a
                                                                   class TransformerLayer(Module):
full JAX program, which includes selecting the appropriate           class Config(Module.Config):
mesh shape for the desired accelerator instance, applying              self_attention: AttentionLayer.Config
sharding annotations for certain layers, performing auto-              feed_forward: FeedForwardLayer.Config
tuning of XLA compilation options for the target hardware,             ...
selecting appropriate attention kernels for the backend, and
applying appropriate rematerialization strategies based on         Child configurations are encapsulated and can be specified
tagged points in the module hierarchy. These annotations           independently of the parent: TransformerLayer’s con-
are crucial for training to run efficiently. The JAX pro-          fig does not directly specify the hyperparameters of its child
gram and compiler options are then passed to the XLA               layers, allowing users to switch between different imple-
compiler to generate the accelerator program (including e.g.       mentations of child layers. For instance, one can decide to
CUDA kernels), which is then orchestrated via the AXLearn          replace feed forward with MoE without changing the
runtime on distributed hardware (e.g., Kubernetes) using           config of TransformerLayer. Further, since layers can
accelerator-specific runtimes (e.g., CUDA runtime). The            be reused between models, the configs are often initially
AXLearn runtime monitors the execution of the accelerator          partially specified.
program and provides additional capabilities like efficient        Note that we have not configured an input dimension on
feed forward; instead, hidden dim is configured to               that users do not need to implement parallelism directly,
be a function of the (as of yet unspecified) input dimen-        but instead specify their desired parallelism strategy via
sion. This allows the parent TransformerLayer to set             configuration. This contrasts with Flax or PyTorch where
input dim when the layer is instantiated:                        sharding is not a native concept in the layer library, and thus
                                                                 code changes may be necessary depending on parallelism
class TransformerLayer(Module):                                  strategy. Users also have granular control over how specific
  def __init__(self, cfg):                                       parameters in specific layers are partitioned.
    # feed_forward uses the same input_dim.
    cfg.feed_forward.set(                                        Memory optimizations AXLearn has several built-in mech-
      input_dim=cfg.input_dim)
                                                                 anisms for optimizing high-bandwidth-memory (HBM) us-
    self._add_child("feed_forward",
      cfg.feed_forward)                                          age which are necessary for large-scale training. First,
                                                                 AXLearn layers natively support configuring rematerializa-
                                                                 tion (a.k.a., activation checkpointing or remat). Depending
By partially defining configs and propagating from parent
                                                                 on the hardware, the remat strategy must be tuned to selec-
to child, configurations often only need to be specified once,
                                                                 tively save or recompute certain activations in the backward
commonly at the layers near the root of the tree. So long
                                                                 pass. In AXLearn, common remat points (such as the atten-
as the parent and child agree on a configuration interface
                                                                 tion QKV projections and output) are “tagged” with names,
(often just input and output dimensions), arbitrarily complex
                                                                 such that users can selectively target remat points to decide
configs can be constructed while keeping layers modular.
                                                                 which activations to save in accelerator memory, offload
Config traversal Because configs are hierarchical and en-        to CPU memory (if supported by hardware), or recompute.
capsulated, one can apply arbitrary modifications by travers-    Users can also employ programmatic remat strategies, such
ing the config tree. We call such traversal a “config mod-       as only saving the output of linear layers. Second, AXLearn
ifier”. The illustrate, the following snippet recursively re-    implements optimizer state offloading to CPU memory. This
places any target config with new cfg:                           is essential for training models with more than hundreds of
                                                                 billions of parameters on certain platforms like TPU v5e
def replace_config(cfg, tgt, new_cfg):
                                                                 where HBM is limited, and where sharding beyond a certain
  def enter_fn(child):                                           point can be inefficient.
    for key, value in child.items():
      if isinstance(value, tgt.Config):                          Hardware-dependent optimizations In order to support
        new_cfg.set(**value.items())                             efficient training, it is necessary to apply target-dependent
        child.set(key, new_cfg)                                  parallelism strategies. AXLearn introduces the concept of
  cfg.visit(enter_fn=enter_fn)
                                                                 “mesh rules” to address this problem. A mesh rule describes
                                                                 a mapping from accelerator types to config modifiers, which
This can be used to apply MoE in the following manner:           allows users to express complex per-target optimizations
                                                                 with succinct and self-contained configs. AXLearn uses
# Replace any FFN with MoE.                                      these rules to automatically apply the appropriate modifiers
replace_config(
  trainer_cfg,                                                   based on the target platform. Appendix A shows a roughly
  target=FeedForwardLayer,                                       10 line code snippet that configures training to use FSDP
  new_cfg=MoELayer.                                              within TPU v5e slices and data-parallel across slices, of-
    default_config().set(...),                                   floading activations from dot products to host memory and
)                                                                enabling INT8 training.

Indeed, this roughly 10-line code snippet is used to apply       While the XLA compiler can fuse most operations to pro-
MoE to over 1,000 experiment configs, without any addi-          duce optimized target-specific code, in some cases we can
tional changes to other modules.                                 achieve better performance with custom kernels. Again
                                                                 owing to the modular nature of AXLearn layers, enabling
4.2   Generating JAX Programs                                    custom kernels only requires simple configuration changes.
                                                                 For instance, AXLearn provides a FlashAttention (Dao et al.,
Config-based parallelism AXLearn natively integrates             2022; Dao, 2023) layer, which can be used as a drop-in re-
parallelism support in every relevant layer for all common       placement for the default attention layer. Like above, this
parallelism strategies, including fully-sharded data paral-      can be expressed as a config modifier in a mesh rule. Behind
lelism (FSDP) (Rajbhandari et al., 2020), pipeline paral-        the scenes, the FlashAttention layer transparently dispatches
lelism (Huang et al., 2019), expert-parallelism (Du et al.,      kernels based on the backend: on GPU, cuDNN (Chetlur
2024), sequence parallelism (Li et al., 2023), and tensor        et al., 2014) is used when possible, falling back to a custom
model parallelism (Narayanan et al., 2021). This means           Pallas (pallas, 2025) kernel for cases like block-sparse at-
Figure 3. Invocation Context. Module invocations push contexts to the stack, which retrieve child states, split PRNG keys, and create
child output collections. Upon returning, contexts are popped, collecting outputs into the parent collection. The context stack can be
programmatically traversed to retrieve shared state, allowing features like tied weights to preserve encapsulation.


tention where cuDNN is not supported; on AWS Trainium,               saves considerable time and resource costs when scaling
the Nki kernel from AWS Neuron Toolkit (AWS, 2025)                   development to large teams.
is used; and on TPU, the SplashAttention Pallas kernel in
JAX (Bradbury et al., 2018) is used.                                 4.3   Maintaining States across Module Boundaries.
We note that efficient hardware-agnostic training is largely         In order to be transformed by JAX primitives such as jit
possible because of several core design choices in AXLearn.          and grad, JAX programs must be purely functional, which
First, AXLearn layers are already sharding and remat aware.          means that they must be stateless. However, neural net-
This allows complex strategies to be expressed with config-          work training is inherently stateful: it is often necessary
uration rather than code. Second, all components are imple-          to store model parameters, maintain pseudo-random num-
mented as strictly encapsulated modules. This allows ex-             ber generators (PRNGs), collect training summaries, and
pressing optimizations like quantization as a replacement of         aggregate outputs. The canonical way to maintain state
DotGeneral layers (XLA, 2025) with their quantization-               in a functional system is for the caller to pass in “side
aware equivalents. Third, configurations are entirely Python-        inputs” (such as layer parameters) along with the main
based. This allows utilizing constructs like recursion to            inputs for the method. The outputs include not only the
traverse and modify configs, which would otherwise not be            method results, but also “side outputs” such as summaries
possible with a DSL.                                                 and state updates. Rather than requiring the user to main-
                                                                     tain these side inputs and outputs, AXLearn introduces an
AOT compilation A core advantage of a compilation-based              abstraction called the InvocationContext, depicted in
approach is that many of the errors that would otherwise be          Figure 3. When a parent module invokes a child module,
encountered in a full-scale distributed run can be checked           an InvocationContext for the child module and state
entirely locally, from the convenience of a single host envi-        is automatically pushed to the stack, which transparently
ronment. AXLearn provides native support for JAX Ahead-              “splits” the PRNG key and creates a new data store for any
of-Time (AOT) compilation, which allows users to analyze             training summaries or outputs saved by the child module.
the memory and FLOPS utilization of a training program               When a child module returns, the context is popped off
without executing a single line of the program, including            the stack, which transparently collects child summaries and
catching errors like OOMs that would otherwise result in             outputs into the parent data store. Analogously to the tradi-
wasted resources. Because the same codepath is used for              tional call stack, user code does not need to be aware of the
AOT and actual training, users can be confident that a pro-          InvocationContext. This allows users to implement
gram that AOT-compiles will run at a larger scale. This              modules in a familiar imperative fashion while retaining the
functional properties demanded by JAX.                          facilitate detection, AXLearn provides several components.
                                                                First, the AXLearn runtime has a configurable “watchdog”
A subtle design decision is that InvocationContexts
                                                                that monitors the step time and hardware utilization of a
contain references to modules, but not vice-versa. This
                                                                host. Upon observing low hardware utilization or abnormal
allows contexts to be accessed outside of the module hi-
                                                                step times, the watchdog can be configured to force a restart
erarchy, including from arbitrary function calls that may
                                                                of the host, alert an on-call for manual intervention, or dump
not have references to a module, allowing deep integration
                                                                stack traces for debugging.
with 3rd party libraries that are not natively aware of the
AXLearn state system (e.g., optax optimizers (Optax Au-         Failure recovery. Efficient recovery is critical to ensure
thors, 2025)); as well as compatibility with codepaths with     high hardware utilization and goodput. Large-scale training
unique execution behavior, such as JAX’s custom vjp             usually involves multiple data-parallel replicas with repli-
backward pass.                                                  cated weights. Upon a failure of a data-parallel replica, the
The design of InvocationContext is a key factor al-             checkpoint can be restored directly from a healthy replica
lowing complex architectures to be well-encapsulated. In        and broadcasted through the fast interconnect to all other
other libraries, users may be required to perform nested        healthy or newly provisioned replicas. Applying a similar
instance attribute access in order to access shared state be-   strategy, persistent compilation caches can be configured to
tween layers. However, this requires that module imple-         eliminate any startup compilation time, as compilation arti-
mentations to be intricately aware of other modules being       facts can be entirely reused across restarts of the same model.
used and where they are in the hierarchy. In AXLearn,           Finally, AXLearn builds on native Kubernetes features to
the system layer can instead transparently traverse the         support slice-level hot-swap. In this case, the AXLearn
InvocationContext hierarchy to retrieve shared states,          scheduler over-provisions spare replicas within the same
which allows module implementations to remain completely        cluster, allowing failed nodes in an ongoing training job to
unaware of other modules in the system.                         be rapidly substituted with healthy nodes. Meanwhile, the
                                                                over-provisioned hardware can still run low-priority jobs to
                                                                reduce resource waste, or be sent for inspection and repair.
5   AXL EARN RUNTIME
Monitoring and profiling.          AXLearn’s runtime pro-       6     U NIFYING T RAINING AND I NFERENCE
vides observability at several layers. At the hardware layer,
                                                                One surprising discovery during the process of building
AXLearn natively integrates with JAX’s profiler to provide
                                                                AXLearn is that we can get an efficient inference en-
insights into inefficiencies introduced by input pipelines,
                                                                gine by reusing a substantial subset of AXLearn compo-
sharding strategies, compiler behavior, or other aspects of
                                                                nents. Though inference performance is not our design goal,
the program. For large scale distributed training, AXLearn
                                                                AXLearn achieves significantly higher performance than a
supports a generic measurement interface that can be used
                                                                state-of-the-art inference engine, vLLM (Kwon et al., 2023),
to record arbitrary events such as the start of training or
                                                                on TPUs. (See §7 for details.)
the start of a step. These events can be used to measure
end-to-end inefficiencies such as those introduced by hard-     AXLearn’s design allows modules to easily be adapted to
ware provisioning or checkpointing recovery, which can be       optimize for decoding. For example, because the attention
captured via metrics like overall job goodput.                  layer’s KV cache is an encapsulated component, an atten-
                                                                tion layer can incorporate inference-friendly cache layouts
Checkpointing. AXLearn’s default checkpointing capabili-        without changing the attention layer. This allows incorporat-
ties are similar to that of orbax (Orbax Authors, 2022), but    ing new inference techniques like continuous batching (Yu
supports multiple cloud storage backends, including AWS         et al., 2022), disaggregated prefill and decode (Zhong et al.,
S3 and Google Cloud Storage (GCS), and provides addi-           2024), and paged KV caches (Kwon et al., 2023) without
tional memory optimizations for large-scale checkpointing.      re-implementing models and layers. While we currently sup-
Checkpoints are saved asynchronously during training to         port TPU, we believe with additional effort we can support
avoid bottlenecks on slow networks, blocking only in rare       unified training and inference on other backends.
cases where the checkpointer is waiting on a prior serializa-
tion to complete. A garbage collector runs in the background
to remove old checkpoints according to a user-configurable      7     E VALUATION
policy to reduce storage costs.                                 7.1   Modularity of Configurations
Failure detection. Failures are inevitable for large-scale      We analyze modularity using LoC-Complexity, motivated
training spanning thousands of chips. A core problem is         in §2. Specifically, we analyze the asymptotic LoC changes
therefore identifying when and where a failure happens. To      required to re-parameterize each system to support RoPE
Table 2. LoC analysis of deep learning systems. N denotes number of modules in a system, and M denotes number of variants of a
feature. LoC Estimates represent changes necessary within each system’s API interfaces to integrate a single variant of RoPE or MoE
under a standard production setting. Note that in AXLearn, 0 LoC changes to existing interfaces are necessary.

          System         LoC-Complexity(RoPE)       LoC-Complexity(MoE)       LoC Estimate (RoPE)     LoC Estimate (MoE)
        Megatron-LM            O(N M )                    O(N )                       400                      20
         DeepSpeed             O(N M )                   O(N M )                      320                    4000
         TorchTitan            O(N M )                   O(N M )                      240                     400
            Flax               O(N M )                     N/A                        600                    N/A
           Praxis              O(N M )                    O(M )                       300                      5
          MaxText              O(N M )                   O(N M )                      200                     300
         AXLearn                O(1)                       O(1)                        0                       0



Table 3. Comparing AXLearn with state-of-art systems on training performance. Systems are benchmarked on identical hardware within
each model–hardware configuration. For example, the first four rows (PyTorch FSDP, Megatron, MaxText, and AXLearn) are evaluated
on a 32 × H100-8 cluster.
            Model               Hardware               System            Iteration Time (s)   MFU   Throughput (tokens/s)
                                                   PyTorch FSDP                  2.6          29.9%        1.6M
                                                   Megatron-LM                   1.7          44.9%        2.5M
                              32 × H100-8
                                                      MaxText                    1.4          54.7%        3.0M
                                                     AXLearn                     1.4          54.2%        3.0M
          Llama2-7B                              PyTorch XLA FSDP                3.5          46.7%        1.2M
                               tpu-v5p-512            MaxText                    2.7          61.6%        1.6M
                                                     AXLearn                     2.5          66.2%        1.7M
                           64 × Trainium2-16         AXLearn                     1.2          24.2%        3.5M
                                                   PyTorch FSDP                 10.6          34.7%        396K
                                                   Megatron-LM                   7.8          47.2%        538K
                              64 × H100-8
                                                      MaxText                    9.4          39.1%        446K
                                                     AXLearn                     9.2          40.0%        456K
         Llama2-70B                              PyTorch XLA FSDP                               OOM
                              tpu-v5p-1024            MaxText                  12.3           64.4%        341K
                                                     AXLearn                   11.6           68.0%        360K
                           64 × Trainium2-16         AXLearn                   11.2           25.0%        374K
                                                      MaxText                  13.0           31.3%        1.3M
                              tpu-v5p-1024
                                                     AXLearn                   12.9           31.6%        1.3M
      Qwen-3 30B-A3B
                                                   Megatron-LM                 4.1            20.2%        4.1M
                              64 × B200-8
                                                     AXLearn                   4.3            19.2%        3.9M



and MoE. When measuring extensibility, we focus on LoC             model) and 10 variants of attention (i.e., kernels, kv cache
changes incurred in existing modules in the system as op-          strategies, etc.). Notation: Let N be the number of modules
posed to the new functionality itself. This allows for a com-      in a system, and M be the number of variants of the feature
parable analysis, and in practice, the same feature across         to be added. For example, MoE has variants of routing or
systems tends to incur similar LoC up to some constant.            gating mechanisms.
Table 2 shows the summary of LoC-Complexities across sys-          AXLearn (ours) In AXLearn, RoPE and MoE are strictly
tems. We omit PyTorch FSDP, which is not an LLM library;           encapsulated. §4 provides a 10-line code snippet to integrate
Haiku, which has no implementation of RoPE or MoE; and             both features into any experiment config. Internally we use
Pax, which is replaced with its layer library Praxis. For a        such a code snippet to configure over 1,000 experiments
concrete evaluation, we also provide LoC estimates based           to use RoPE, MoE, or both. As we scale the number of
on the changes required in a specific production codebase.         modules or RoPE or MoE variants, we require no changes to
While many of the listed systems provide a few sample im-          any existing interfaces, achieving constant LoC-Complexity.
plementations, a production codebase can be orders of mag-
nitude larger. We cannot disclose specific details of the scale    Other Systems All other systems we have studied require
of our internal model architectures and hyper-parameters.          O(N M ) to incorporate RoPE and MoE, with the following
Therefore, we provide estimates under a realistic setting of       exceptions. Like AXLearn, Megatron-LM defines a MoE
a codebase with 20 model variants (e.g., variants of a GPT         layer in place of the standard MLP. However, each MLP
                 40                          0.8                     6                         0.4




                                                    Iteration Time (s)
Iteration Time (s)

                                                                                                       Table 4. Comparing AXLearn with vLLM on inference perfor-
                 30                          0.6                     4                         0.3     mance for Llama 2 models on Time-To-First-Token (TTFT), Time-
                 20                          0.4




                                              MFU
                                                                                               0.2




                                                                                                 MFU
                                                                                                       Per-Output-Token (TPOT), and throughput on TPUs.
                                                                     2
                 10                          0.2                                               0.1      Model      System       TTFT      TPOT       Throughput
                  0                          0.0                     0                         0.0                 vLLM       538.6ms     22.4ms    1117 tokens/s
                                                                            2        84 2768
                      256 512 102420484096                               819     163    3
                                                                                                          7B
                                                                                                                  AXLearn      40.1ms     9.1ms     3125 tokens/s
                            # Chips                                             # Chips                            vLLM          80s     189.8ms     705 tokens/s
                                                                                                         70B
                                                                                                                  AXLearn     150.5ms     28.1ms    1139 tokens/s
                      (a) Model A (70B)                                  (b) Model B (150B)

  Figure 4. AXLearn’s training throughput and MFU when scaling
  the number of TPUs.
                                                                                                       Megatron-LM has stronger performance on H100 and B200
                                                                                                       GPUs over AXLearn because PyTorch currently have finer-
  introduces MoE-specific fields in its signature, so that any                                         grained scheduling capability over XLA. However, using
  module composing a linear submodule incurs at least one                                              XLA allows AXLearn to be hardware-agnostic. This is a
  LoC change for MoE-specific arguments. This scales with                                              trade-off we are willing to take.
  O(N ). Like AXLearn, Praxis uses a “template” composi-                                               To test AXLearn’s scalability, we perform a weak-scaling
  tion approach, which allows LoC-Complexity(MoE) to be                                                study of two production models. Model A is a 70B parame-
  O(M ): each MoE variant incurs a small 5 LoC change. Flax                                            ter model with 4,096 context length, and Model B is a 150B
  has no public example of MoE so we exclude it from our                                               parameter model with 8,192 context length. Fixing the per-
  analysis. See Appendix B for details on the LOC analysis.                                            device batch size, for Model A, scaling from 256 to 4,096
                                                                                                       chips reduces MFU from 63.0% to 52.4%; and for Model
  7.2                 Performance on Different Hardware                                                B, scaling from 8,192 to 32,768 chips reduces MFU from
                                                                                                       40.6% to 37.6%. These numbers demonstrate that AXLearn
  We compare AXLearn training performance with PyTorch
                                                                                                       achieves very close to linear scaling, shown in Figure 4. We
  FSDP, Megatron-LM, and MaxText, which achieve state-of-
                                                                                                       note that MFU for the 150B model is lower due to the need
  the-art performance on GPU and TPU. All comparisons are
                                                                                                       to limit the global batch size at 32,768 chip scale for good
  conducted on identical hardware to ensure fair evaluation—
                                                                                                       training convergence. All scaling experiments for the 150B
  systems sharing a hardware group in Table 3 are bench-
                                                                                                       model has 1/16 per chip sequence length compared to 70B
  marked on the same cluster with the same number of ac-
                                                                                                       model experiments.
  celerators. We use training iteration time, model FLOPS
  utilization (MFU), and throughput to represent a training                                            A key benefit of AXLearn’s modular design is that the same
  system’s performance. We evaluate both dense and MoE                                                 training codebase can serve inference workloads with min-
  models: Llama2 7B and 70B (dense), and Qwen-3 30B-                                                   imal additional effort. We compare the inference perfor-
  A3B (MoE), on four hardware backends: (1) 256/512 H100                                               mance in terms of Time-To-First-Token (TTFT) and Time-
  GPUs (32/64 AWS P5d instances, each with 8 H100 GPUs);                                               Per-Output-Token (TPOT) of Llama2 7B and 70B mod-
  (2) 512 B200 GPUs (64 AWS P6 instances, each with 8                                                  els between AXLearn and vLLM on TPUs. We use the
  B200 GPUs); (3) TPU-v5p-512/1024 (64/128 GCP Cloud                                                   ShareGPT dataset for the prompts. For the 7B model, the
  TPU hosts, each with 4 chips); and (4) 1024 Trainium2 (64                                            benchmark is performed on a Google Cloud TPU v5p-8 VM
  AWS trn2 instances, each with 16 Trainium2 chips). All                                               instance, with maximum input length of 1024 and maximum
  runs use a global batch size of 1024. Note that since Pytorch                                        output length of 256. For the 70B model, the benchmark
  FSDP does not run on TPU, we use Pytorch XLA FSDP as                                                 is on a Google Cloud TPU v6e-8 VM instance2 , with max-
  its closest replacement (PyTorch, 2025). Megatron-LM is a                                            imum input length of 1,800 and maximum output length
  GPU-only system, so we only report results on GPU. None                                              of 256. As Table 4 shows, AXLearn achieves 500x and 6x
  of the baselines support Trainium, so we only report results                                         speedup in TTFT and TPOT over vLLM, respectively. In
  for AXLearn.                                                                                         terms of throughput, AXLearn is 2.8x faster for 7B model
                                                                                                       inference and 1.6x faster for 70B model inference. We note
  Table 3 summarizes the performance results. On TPUs,
                                                                                                       that TPU support for vLLM is still experimental, which
  AXLearn achieves state-of-the-art performance, where Max-
                                                                                                       likely contributes to the performance gap. Our goal is not to
  Text lags slightly behind, likely due to its choices of rema-
                                                                                                       position AXLearn as a specialized inference engine, but to
  terialization. On the other hand, PyTorch XLA FSDP fails
                                                                                                       demonstrate that a modular training framework can achieve
  to run entirely with out-of-memory errors. On H100 GPUs,
                                                                                                       production-grade inference performance with minimal addi-
  AXLearn outperforms PyTorch FSDP due to fine-grained
  control over activation checkpointing and better support for                                            2
                                                                                                            vLLM does not support the 70B model on v5p-8 VM at the
  memory bound operations such as RMSNorm and RoPE.                                                    time the experiments were run.
                       4
 Throughput(Steps/m)
                                Checkpoint                                                competitive performance across AWS and GCP; and is now
                       3        created                                                   natively supported by platforms like Trainium2. Where
                       2                          Job recovery
                                                                                          compilers fall short, we rely on custom kernels to close the
                       1     Failure              started               Checkpoint        performance gap. While AXLearn’s modular design makes
                             detected                                   restored          integrating custom kernels straightforward—as drop-in re-
                       0
                            0         10     20         30         40      50        60   placements via config modifiers—achieving peak hardware
                                                  Time (Minutes)                          utilization on each backend requires backend-specific kernel
                                                                                          implementations, which demands specialized expertise and
Figure 5. AXLearn’s recovery latency under a hardware failure.                            ongoing maintenance as hardware evolves.
                                                                                          Adopting JAX/XLA meant several paradigm shifts. First,
                                                                                          it shifts from imperative to functional programming, which
tional effort.                                                                            posed a challenge as familiar patterns like mutable state
                                                                                          were prohibited. Instead, developers manually specified
7.3                        Failure Recovery Latency                                       parameters as inputs to each module, and bubbled up out-
                                                                                          puts through the call stack. This hurt modularity as it
We measure the speed of recovering from a hardware failure                                required implementations to be intricately aware of state
via analyzing a production training job. Figure 5 shows                                   structure, and hurt usability as users lost the comfort of
training throughput over time on 32,768 TPUs over a 1-                                    the PyTorch imperative style. This motivated the design
hour window that contains a complete event which the job                                  of InvocationContext, which allowed state to be de-
recovers from a hardware failure. Since checkpointing is per-                             coupled, outputs to be collected across the module hier-
formed asynchronously, no training throughput reduction is                                archy, and layer implementations to be implemented in
observed during checkpoint creation. When hardware fails,                                 familiar PyTorch fashion. Second, it shifts focus to the
AXLearn detects the training job has failed immediately                                   cloud. JAX/XLA initially only supported Google Cloud
and initiates slice-level hot-swap, completing the hot-swap                               TPU, which meant that a number of internal components
within 4 minutes. Checkpoint restoration finishes in 9 min-                               could not be directly used. As GCP was in a nascent state
utes after the hot-swap completes. In total, the failure results                          with limited TPU capacity, we knew we could not rely on
in 21 minutes of lost training time, which includes both the                              it entirely, and hoped to leverage our internal compute in-
downtime during hot-swap and checkpoint restoration, as                                   frastructure. To that end, we designed the system to allow
well as the lost progress from training step completed after                              all components of training to be configurable. For example,
the most recent checkpoint before the failure occurred.                                   we initially leveraged Flax’s GCS-compatible checkpointer
                                                                                          to bootstrap the layer library, which we later replaced to
7.4                        Experience of using AXLearn at Apple                           support internal storage backends. This migration was com-
AXLearn’s development began in late 2021 using PyTorch                                    pletely seamless with no changes to user code, which was
with a small team of engineers. At the time, signs of the                                 only possible due to AXLearn’s modular design.
Transformer architecture becoming a de facto choice were                                  As we scaled development, we faced additional barriers.
evident (e.g., BERT (Devlin et al., 2019), T5 (Raffel et al.,                             First, avoiding resource contention was crucial due to lim-
2020), GPT3 (Brown et al., 2020)). Architectural conver-                                  ited TPU capacity within GCP. Aside from leveraging other
gence meant that most layers in many new models can be                                    backends, we realized that a significant portion of resources
reused if implemented modularly. However, due to limited                                  were consumed by jobs with low resource utilization or no
support for automatic parallelization, layer implementations                              progress due to preventable errors. However, XLA com-
ultimately could not be fully modularized—in many cases,                                  pilation inherently operates on device abstractions—many
different parallelization strategies meant that logic had to be                           errors (e.g., OOMs due to suboptimal sharding) can be theo-
rewritten, with inevitable interactions across modules.                                   retically caught from a local machine. We therefore capital-
GSPMD (Xu et al., 2021) (published late 2021) meant that                                  ized on JAX’s compiler-first approach by deeply integrating
communication can instead be injected by a compiler. We                                   AOT-compilation, which allowed users to debug training
chose to deeply integrate with XLA as a bet on the compiler-                              entirely on CPU. This has allowed development to scale
first approach. First, layers can be modularized as sharding                              even with limited capacity. Second, we learned the hard
propagation across the graph can be automatically and trans-                              way that ML testing practices are often insufficient. While
parently handled. Second, we believed eventual adoption                                   it is common to unit test layers, ML experiments heavily
would push hardware providers to optimize compilers on                                    depend on correctness of configs. With a large codebase,
their platforms, which means performance improvements                                     changes to one experiment can inadvertently affect others,
can be achieved without a single LoC. As it turns out, while                              which are difficult to catch with unit or integration testing
JAX/XLA did not initially support GPUs, today it achieves                                 (e.g., subtle changes in training dynamics). To address this
issue, AXLearn introduces the notion of “golden configura-        ability of custom kernels. This reflects current limitations in
tion” tests: key training configs are serialized into human       tensor compilers, which often cannot automatically derive
readable format and committed along with code changes.            highly optimized kernels (e.g., FlashAttention (Dao et al.,
This allows changes to produce consistent and reviewable          2022; Dao, 2023)) from high-level specifications alone. In
diffs, to trigger relevant code-owner reviews, avoid code-        AXLearn, such custom kernels are treated as black-box
contribution-level conflicts, and provide a traceable commit      nodes within the computation graph, ensuring they remain
history of experiments. Lastly, public cloud infrastructure       encapsulated and do not interfere with higher-level config-
can fail in opaque ways. In contrast to an internally man-        uration or composability. A key strength of AXLearn is
aged cluster, we learned that we must account for opaque          that it allows a wide range of machine learning experiments,
failures often out of our control, including hardware fail-       such as exploring new model architectures or training meth-
ures, ICI failures, silent data corruption, kernel panics, file   ods, to leverage these kernels with minimal code changes
system throttling, and more. While we have built many             across different experiments. This helps reduce friction in
layers of resiliency, debugging and mitigating many of these      experimentation while maintaining strong performance.
failures required close collaboration with Google, Amazon,
and Nvidia.                                                       Is AXLearn’s performance on GPUs and Trainium lim-
                                                                  ited by XLA? AXLearn is built on top of XLA, and
Today, AXLearn has grown from a handful of developers             AXLearn therefore inherits its backend performance charac-
training models at million parameter scales, to hundreds of       teristics. In addition to XLA compiler, XLA provides cus-
developers training models at billion-to-trillion parameter       tom calls as an escape hatch to allow experts to write hand-
scales. It actively supports over 10,000 experiments under        tuned kernels that outperform compilers. On GPUs, we
development at a given time, running across tens of differ-       demonstrate that integrating custom kernels (e.g., FlashAt-
ent hardware clusters. Some of the models trained with            tention) enables AXLearn to achieve performance compara-
AXLearn now power features used by over a billion users,          ble to other state-of-the-art training systems. For emerging
including intelligent assistants, multimodal understanding        hardware platforms such as Trainium, the broader ecosys-
and generation, and code intelligence.                            tem support across training frameworks is still evolving,
                                                                  making comprehensive comparisons an ongoing area of
8    D ISCUSSION                                                  investigation. By relying on XLA, we prioritize a hardware-
                                                                  agnostic approach, which we view as a pragmatic and
Tradeoffs between development agility and modularity.             forward-looking design choice.
Training systems inevitably need to balance development
agility and modularity. Directly modifying Python execu-          9    R ELATED W ORK
tion code offers the highest degree of agility, as it allows
arbitrary changes to be introduced quickly. In AXLearn, we        Training systems for large models. High-performance
instead encourage users to configure parameterized modules,       large model training is an active research area. FSDP (Zhao
which can reduce agility in the short term. Our perspective       et al., 2023) and DeepSpeed (Rajbhandari et al., 2020)
is that, across many model architectures, a large number          use sharding to reduce GPU memory usage. Megatron-
of components are structurally similar (e.g., RoPE, MoE).         LM (Narayanan et al., 2021) combines data, tensor-model,
Implementations of these building blocks benefit from being       and pipeline parallelism to train LLMs efficiently on GPUs.
shared across architectures and reused across experiments,        MegaScale (Jiang et al., 2024) further scales LLM training
including those that change hyperparameters such as model         to more than 10,000 GPUs and deals with straggler and fail-
dimension or number of experts. When existing modules             ures in GPU clusters. AXLearn’s focus is complementary
are insufficient, developers can still implement new mod-         to these systems, with the goal of enabling engineers to use
ules in AXLearn and integrate them seamlessly. While no           these techniques with minimal effort.
framework is universally optimal, our model engineers have
conducted extensive experiments exploring a wide range            Software modularity. Maintaining a modular structure
of model architectures and training methods. To date, we          is a standard software engineering practice in any complex
have not encountered cases where AXLearn’s modular de-            computer system, facilitating maintenance and integration
sign constrained the specification of model architectures or      of new features (Bershad et al., 1995; Kohler et al., 2000).
training approaches.                                              Modularity is especially important for a fast-moving ma-
                                                                  chine learning system: at Apple, we iterate on new model
Is AXLearn a complete answer for hardware-agnostic                architectures, hyper-parameters, and training methods every
training? AXLearn enables training to run across different        day. To facilitate this development, configurations must be
hardware platforms. However, similar to other training sys-       separated from system details like parallelism or rematerial-
tems, achieving peak performance still depends on the avail-      ization in the simplest way possible, so that researchers can
focus on model iteration.                                          Wanderman-Milne, S., and Zhang, Q. JAX: composable
                                                                   transformations of Python+NumPy programs, 2018. URL
Configuration systems for distributed workloads. One               http://github.com/jax-ml/jax.
of our contributions is the ability to configure distributed
deep learning execution in a modular and automated manner.       Brown, T. B., Mann, B., Ryder, N., Subbiah, M., Kaplan,
Configuration for distributed systems is a topic beyond deep       J., Dhariwal, P., Neelakantan, A., Shyam, P., Sastry, G.,
learning. Meta’s configuration system (Tang et al., 2015)          Askell, A., Agarwal, S., Herbert-Voss, A., Krueger, G.,
can enable easy A/B testing, similar to how we conduct             Henighan, T., Child, R., Ramesh, A., Ziegler, D. M., Wu,
A/B testing on model accuracy and training performance by          J., Winter, C., Hesse, C., Chen, M., Sigler, E., Litwin, M.,
comparing AXLearn’s configurations. However, our main              Gray, S., Chess, B., Clark, J., Berner, C., McCandlish,
design goal for the configuration system is to enable mod-         S., Radford, A., Sutskever, I., and Amodei, D. Language
ules to be re-parametrized and composed to easily integrate        models are few-shot learners, 2020. URL https://
with new features (e.g., RoPE, MoE).                               arxiv.org/abs/2005.14165.

                                                                 Chetlur, S., Woolley, C., Vandermersch, P., Cohen, J., Tran,
10    C ONCLUSION                                                  J., Catanzaro, B., and Shelhamer, E. cudnn: Efficient
We have developed AXLearn, a modular, hardware-agnostic            primitives for deep learning, 2014. URL https://
large model training system. AXLearn maintains constant            arxiv.org/abs/1410.0759.
complexity as the number of modules scales. Further, our         Cursor. The ai code editor, 2025. URL https://www.
evaluations show that AXLearn has achieved comparable              cursor.com/.
performance compared to other state-of-the-art training sys-
tems. Finally, we shared our development and operation           Dao, T. Flashattention-2: Faster attention with better par-
experiences with AXLearn.                                          allelism and work partitioning, 2023. URL https:
                                                                  //arxiv.org/abs/2307.08691.
ACKNOWLEDGMENTS                                                  Dao, T., Fu, D. Y., Ermon, S., Rudra, A., and Ré, C. Flashat-
We thank the anonymous reviewers and our shepherd for              tention: Fast and memory-efficient exact attention with io-
their helpful feedback on the paper. We thank Yi Wang, Ke          awareness, 2022. URL https://arxiv.org/abs/
Ye, Dong Yin, Muyang Yu, Yi Zhang, and other key mem-              2205.14135.
bers of the Apple Foundation Model team, for their deep
collaboration and valuable feedback in the development of        DeepSpeed.          Model   implementations,
AXLearn. We also thank Benoit Dupin and Daphne Luong               2025a.       URL   https://github.com/
for their leadership support.                                      deepspeedai/DeepSpeed/tree/
                                                                   a21e5b9db68adf25e9fc797d0e67fdb5879f6069/
                                                                   deepspeed/inference/v2/model_
R EFERENCES                                                        implementations.
Xla: Optimizing compiler for machine learning., 2025. URL
                                                                 DeepSpeed. Mixture of experts - deepspeed, 2025b. URL
  https://openxla.org/xla/tf2xla.
                                                                   https://www.deepspeed.ai/tutorials/
AWS. Neuron documentation, 2025. URL https:                        mixture-of-experts/#moe-layer-api.
 //awsdocs-neuron.readthedocs-hosted.
 com/en/v2.21.0.beta/general/nki/api/                            Devlin, J., Chang, M.-W., Lee, K., and Toutanova, K.
 generated/nki.kernels.flash_fwd.html.                             Bert: Pre-training of deep bidirectional transformers
                                                                   for language understanding. In North American Chap-
Bershad, B. N., Savage, S., Pardyak, P., Sirer, E. G., Fi-         ter of the Association for Computational Linguistics,
  uczynski, M. E., Becker, D., Chambers, C., and Eggers,           2019. URL https://api.semanticscholar.
  S. Extensibility safety and performance in the spin op-          org/CorpusID:52967399.
  erating system. In Proceedings of the Fifteenth ACM
  Symposium on Operating Systems Principles, SOSP ’95,           Du, X., Gunter, T., Kong, X., Lee, M., Wang, Z., Zhang,
  pp. 267–283, New York, NY, USA, 1995. Association               A., Du, N., and Pang, R. Revisiting moe and dense
  for Computing Machinery. ISBN 0897917154. doi:                   speed-accuracy comparisons for llm training, 2024. URL
  10.1145/224056.224077. URL https://doi.org/                      https://arxiv.org/abs/2405.15052.
  10.1145/224056.224077.
                                                                 Google. Gemini: Our most intelligent ai models, built
Bradbury, J., Frostig, R., Hawkins, P., Johnson, M. J., Leary,     for the agentic era, 2025. URL https://deepmind.
  C., Maclaurin, D., Necula, G., Paszke, A., VanderPlas, J.,       google/technologies/gemini/.
Haiku. Haiku: Sonnet for jax, 2025. URL https://                     Narayanan, D., Shoeybi, M., Casper, J., LeGresley, P., Pat-
  github.com/google-deepmind/dm-haiku.                                 wary, M., Korthikanti, V., Vainbrand, D., Kashinkunti,
                                                                       P., Bernauer, J., Catanzaro, B., Phanishayee, A., and
Heek, J., Levskaya, A., Oliver, A., Ritter, M., Rondepierre,           Zaharia, M. Efficient large-scale language model train-
  B., Steiner, A., and van Zee, M. Flax: A neural network              ing on gpu clusters using megatron-lm. In Proceed-
  library and ecosystem for JAX, 2023. URL http://                     ings of the International Conference for High Perfor-
  github.com/google/flax.                                              mance Computing, Networking, Storage and Analysis,
Huang, Y., Cheng, Y., Bapna, A., Firat, O., Chen, M. X.,               SC ’21, New York, NY, USA, 2021. Association for
  Chen, D., Lee, H., Ngiam, J., Le, Q. V., Wu, Y., and Chen,           Computing Machinery. ISBN 9781450384421. doi: 10.
  Z. Gpipe: Efficient training of giant neural networks us-           1145/3458817.3476209. URL https://doi.org/
  ing pipeline parallelism. Advances in neural information             10.1145/3458817.3476209.
  processing systems, 32, 2019.                                      OpenAI. Introducing chatgpt, 2022. URL https://
                                                                       openai.com/index/chatgpt/.
Jiang, Z., Lin, H., Zhong, Y., Huang, Q., Chen, Y., Zhang,
   Z., Peng, Y., Li, X., Xie, C., Nong, S., Jia, Y., He, S.,         Optax Authors, 2025. URL https://github.com/
   Chen, H., Bai, Z., Hou, Q., Yan, S., Zhou, D., Sheng, Y.,           google-deepmind/optax.
   Jiang, Z., Xu, H., Wei, H., Zhang, Z., Nie, P., Zou, L.,
   Zhao, S., Xiang, L., Liu, Z., Li, Z., Jia, X., Ye, J., Jin, X.,   Orbax Authors, 2022. URL https://github.com/
   and Liu, X. MegaScale: Scaling large language model                 google/orbax.
   training to more than 10,000 GPUs. In 21st USENIX                 pallas. Pallas: a jax kernel language., 2025.          URL
   Symposium on Networked Systems Design and Implemen-                 https://docs.jax.dev/en/latest/
   tation (NSDI 24), pp. 745–760, Santa Clara, CA, April               pallas/index.html.
   2024. USENIX Association. ISBN 978-1-939133-39-7.
   URL https://www.usenix.org/conference/                            PAX team. Framework to configure and run machine
   nsdi24/presentation/jiang-ziheng.                                   learning experiments on top of jax., 2022. URL http:
                                                                       //github.com/google/paxml.
Kohler, E., Morris, R., Chen, B., Jannotti, J., and Kaashoek,
                                                                     PyTorch.    Fully sharded data parallel in pytorch
  M. F. The click modular router. ACM Trans. Comput.
                                                                       xla, 2025. URL https://pytorch.org/xla/
  Syst., 18(3):263–297, August 2000. ISSN 0734-2071. doi:
                                                                       master/perf/fsdp.html.
  10.1145/354871.354874. URL https://doi.org/
  10.1145/354871.354874.                                             Raffel, C., Shazeer, N., Roberts, A., Lee, K., Narang, S.,
                                                                       Matena, M., Zhou, Y., Li, W., and Liu, P. J. Exploring
Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,                the limits of transfer learning with a unified text-to-text
 C. H., Gonzalez, J., Zhang, H., and Stoica, I. Effi-                  transformer. J. Mach. Learn. Res., 21(1), January 2020.
 cient memory management for large language model                      ISSN 1532-4435.
 serving with pagedattention. In Proceedings of the
 29th Symposium on Operating Systems Principles, SOSP                Rajbhandari, S., Rasley, J., Ruwase, O., and He, Y. Zero:
 ’23, pp. 611–626, New York, NY, USA, 2023. Associa-                   memory optimizations toward training trillion parame-
 tion for Computing Machinery. ISBN 9798400702297.                     ter models. In Proceedings of the International Con-
 doi: 10.1145/3600006.3613165. URL https://doi.                        ference for High Performance Computing, Networking,
 org/10.1145/3600006.3613165.                                          Storage and Analysis, SC ’20. IEEE Press, 2020. ISBN
                                                                       9781728199986.
Li, S., Xue, F., Baranwal, C., Li, Y., and You, Y. Sequence
  parallelism: Long sequence training from system perspec-           Shazeer, N., Mirhoseini, A., Maziarz, K., Davis, A.,
  tive. In ACL, pp. 2391–2404. Association for Computa-                Le, Q. V., Hinton, G. E., and Dean, J.      Outra-
  tional Linguistics, 2023. ISBN 978-1-959429-72-2.                    geously large neural networks: The sparsely-gated
                                                                       mixture-of-experts layer. In ICLR (Poster), 2017.
Liang, W., Liu, T., Wright, L., Constable, W., Gu, A.,                 URL http://dblp.uni-trier.de/db/conf/
  Huang, C.-C., Zhang, I., Feng, W., Huang, H., Wang,                  iclr/iclr2017.html#ShazeerMMDLHD17.
  J., Purandare, S., Nadathur, G., and Idreos, S. Torchtitan:
  One-stop pytorch native solution for production ready              Su, J., Ahmed, M., Lu, Y., Pan, S., Bo, W., and Liu,
  llm pre-training, 2024. URL https://arxiv.org/                       Y. Roformer: Enhanced transformer with rotary po-
  abs/2410.06511.                                                      sition embedding. Neurocomput., 568(C), February
                                                                       2024. ISSN 0925-2312. doi: 10.1016/j.neucom.
MaxText team. Maxtext., 2025. URL https://github.                      2023.127063. URL https://doi.org/10.1016/
 com/AI-Hypercomputer/maxtext.                                         j.neucom.2023.127063.
Tang, C., Kooburat, T., Venkatachalam, P., Chander,             Zoom. Work happy with zoom ai companion, 2025.
  A., Wen, Z., Narayanan, A., Dowell, P., and Karl,               URL https://www.zoom.com/en/products/
  R. Holistic configuration management at facebook.               ai-assistant/.
  In Proceedings of the 25th Symposium on Operat-
  ing Systems Principles, SOSP ’15, pp. 328–343, New
  York, NY, USA, 2015. Association for Computing                A    M ESH RULES
  Machinery. ISBN 9781450338349. doi: 10.1145/
                                                                In AXLearn, users can specify per-target platform configura-
  2815400.2815401. URL https://doi.org/10.
                                                                tion changes using “mesh rules”. These rules are mappings
  1145/2815400.2815401.
                                                                from instance type regular expressions to config modifiers
XLA. Operation semantics - openxla project, 2025. URL           discussed in §4. In the example below, when launching
 https://openxla.org/xla/operation_                             training on TPU v5e, we configure training to use FSDP
 semantics.                                                     within-slices and data-parallel across slices, offloading ac-
                                                                tivations from dot products to host memory and enabling
Xu, Y., Lee, H., Chen, D., Hechtman, B., Huang, Y.,
                                                                INT8 training. On the other hand, when launching training
 Joshi, R., Krikun, M., Lepikhin, D., Ly, A., Maggioni,
                                                                on H100s, we instead switch to 8-way tensor parallelism
  M., Pang, R., Shazeer, N., Wang, S., Wang, T., Wu,
                                                                within node and FSDP across nodes, saving query, key,
 Y., and Chen, Z. Gspmd: General and scalable par-
                                                                value, and output (QKVO) projections to HBM, as well as
  allelization for ml computation graphs, 2021. URL
                                                                enabling FP8 training with delayed scaling.
  https://arxiv.org/abs/2105.04663.
                                                                These configs are all that are necessary to apply per-target
Yu, G.-I., Jeong, J. S., Kim, G.-W., Kim, S., and Chun, B.-     optimizations, allowing users to scale training on different
  G. Orca: A distributed serving system for Transformer-        platforms with ease.
  Based generative models. In 16th USENIX Sympo-
  sium on Operating Systems Design and Implementa-              [("tpu-v5e-256-*",
  tion (OSDI 22), pp. 521–538, Carlsbad, CA, July                 [MeshShapeModifier.default_config().set(
  2022. USENIX Association. ISBN 978-1-939133-28-1.                   mesh_shape=mesh(data=-1, fsdp=256)),
  URL https://www.usenix.org/conference/                            RematSpecModifier.default_config().set(
                                                                      remat_policies={
  osdi22/presentation/yu.                                               "model.decoder.transformer.layer":
Zhao, Y., Gu, A., Varma, R., Luo, L., Huang, C.-C., Xu, M.,               RematSpec(policy=offload_dots)}),
                                                                    INT8ConfigModifier.default_config()]),
  Wright, L., Shojanazeri, H., Ott, M., Shleifer, S., Desmai-   ("gpu-H100-*",
  son, A., Balioglu, C., Damania, P., Nguyen, B., Chauhan,        [MeshShapeModifier.default_config().set(
  G., Hao, Y., Mathews, A., and Li, S. Pytorch fsdp: Expe-            mesh_shape=mesh(fsdp=-1, model=8)),
  riences on scaling fully sharded data parallel. Proc. VLDB        RematSpecModifier.default_config().set(
  Endow., 16(12):3848–3860, August 2023. ISSN 2150-                   remat_policies={
                                                                        "model.decoder.transformer.layer":
  8097. doi: 10.14778/3611540.3611569. URL https:                         RematSpec(policy=save_qkvoflash)}),
  //doi.org/10.14778/3611540.3611569.                               FP8ConfigModifier.default_config().set(
                                                                      fp8_amax_history_length=128)])]
Zheng, L., Li, Z., Zhang, H., Zhuang, Y., Chen, Z., Huang,
  Y., Wang, Y., Xu, Y., Zhuo, D., Xing, E. P., Gonzalez,
  J. E., and Stoica, I. Alpa: Automating inter- and Intra-      B    L O C A NALYSIS
  Operator parallelism for distributed deep learning. In 16th   We provide additional details and rationale of how we de-
  USENIX Symposium on Operating Systems Design and              rived the LoC estimates in Table 2.
  Implementation (OSDI 22), pp. 559–578, Carlsbad, CA,
  July 2022. USENIX Association. ISBN 978-1-939133-             Megatron-LM Megatron-LM’s Transformer implemen-
  28-1.                                                         tation composes TransformerBlockSubmodules.
                                                                However, the RoPE-specific parameters are flattened
Zhong, Y., Liu, S., Chen, J., Hu, J., Zhu, Y., Liu,
                                                                in the init signature of each model implemen-
  X., Jin, X., and Zhang, H. DistServe: Disaggregat-
                                                                tation, e.g.    rotary percent, rotary base,
  ing prefill and decoding for goodput-optimized large
                                                                rotary scaling,      and position embedding
  language model serving. In 18th USENIX Sympo-
                                                                 type in GPTModel. Additionally, these parameters are
  sium on Operating Systems Design and Implementa-
                                                                propagated to submodules like TransformerBlock,
  tion (OSDI 24), pp. 193–210, Santa Clara, CA, July
                                                                Transformer Layer, or Attention.
  2024. USENIX Association. ISBN 978-1-939133-40-3.
  URL https://www.usenix.org/conference/                        This means to integrate a RoPE variant, one potentially
  osdi24/presentation/zhong-yinmin.                             incurs LoC changes to each module in each model imple-
mentation, as at minimum the RoPE parameters must be           RoPE5 .
propagated down an arbitrary number of modules to the
                                                               In addition, the base model implementation propagates this
Attention layer.
                                                               embedding type (and additional RoPE configs) to child lay-
Additionally, each model’s init implementation                 ers like the self attention layer. As a consequence, every at-
includes branching logic to instantiate the RoPE               tention layer implementation, such as DSDenseBlocked
embedding layer variant depending on the de-                   Attention, must first update its init signature to handle
sired position embedding type.            For example,         the input embedding type. It must also update its forward
RotaryEmbedding should be instantiated if the embed-           implementation to apply RoPE based on the embedding
ding type is “rope”, while MultimodalRotaryEmbedd              type.
ing should be instantiated when embedding type is
                                                               In a similar way as Megatron-LM, LoC-complexity(RoPE)
“mrope”. Therefore, if we additionally consider RoPE
                                                               scales quadratically with O(N M ), because we must prop-
variants, the LoC-complexity scales quadratically to
                                                               agate embedding type and RoPE parameters down an arbi-
O(N M ), as in the worst case each module must account
                                                               trary number of modules to the attention layer, and because
for each variant in its init signature if it receives an
                                                               each attention module in the worst case receives and must
embedding type.
                                                               be prepared to handle all possible values of embedding type.
To integrate MoE, Megatron-LM is able to leverage com-
                                                               If we assume conservatively 20 models with at least 6 LoC
position via TransformerBlockSubmodules to spec-
                                                               per model6 , we incur 120 LoC; in addition, with 10 attention
ify a MoE layer in place of the standard MLP. How-
                                                               variants each requiring approximately 20 LoC7 we incur
ever, once again the encapsulation is not applied strictly.
                                                               another 200 LoC.
Each MLP layer implementation introduces an is expert
field in its init signature, which is propagated to the lin-   If the MoE case,            DeepSpeed requires sub-
ear submodules. Any module that uses a linear submod-          classing each model from a custom subclass
ule therefore needs to incur at least one LoC change,          DSMoETransformerModelBase, which requires
which scales with O(N ). Indeed, Megatron-LM itself            in some cases a re-implementation of most methods. For 20
has a number of modules that are impacted, including           model variants, each can incur 100s of LoC—in the case of
ColumnParallelLinear, RowParallelLinear,                       DeepSpeed’s QwenV2MoE, more than 200 LoC—which
Attention, CrossAttention, and more.                           conservatively incurs 4,000 LoC changes.
If we assume the production setting of 20 model variants,      TorchTitan TorchTitan adopts a similar design as Deep-
with each conservatively incurring at least 20 LoC changes     Speed by using a monolithic BaseModelArgs subclass
to integrate RoPE3 , we incur at least 400 LoC. For MoE,       that flattens all configs for each model. In the case of RoPE,
if we assume 10 MLP variants4 , each incurring at least 1      each such config subclass introduces RoPE specific config-
LoC change to integrate support for is expert, we incur        urations like rope theta and rope scaling. Similar
at minimum 10 LoC change. In addition, if we assume at         to prior analysis, such a design already incurs at least O(N )
least 10 modules in the system using a linear submodule        LoC-Complexity as each model adopting RoPE must ac-
(corresponding to each linear variant), each requiring 1 LoC   cordingly modify its config class signature.
change to build module, we incur additional 10 LoC.
                                                               In addition, each model has its own Attention imple-
DeepSpeed       DeepSpeed applies the “config flat-            mentation. For example, the deepseek v3 Attention
tening” methodology discussed in §4.               RoPE        implementation conditions on the value of RoPE configs
configs like rotary dim and rope theta are                     (e.g. rope scaling) to decide which child RoPE layer to
grouped under a monolithic config class, like                  instantiate. In the worst case, each RoPE variant may incur
DeepSpeedInferenceConfig.                 Every model          the same conditional logic for each model implementation
implementation reads the config and overrides the method       that intends to support it. This causes the LoC-Complexity
positional embedding type to indicate whether                  to degrade to O(N M ) if we consider RoPE variants.
RoPE should be enabled for the model by returning a
                                                               With 20 model variants, each incurs 2 LoC on average
specific value of the embedding type. With this design,
                                                               to update its corresponding ModelArgs class to incorpo-
we can already observe that LoC-Complexity(RoPE) must
                                                               rate RoPE configs, as well as 10 LoC for each attention
be at least O(N ), as each model implementation incurs
                                                               implementation. In total, this incurs 240 LoC, although
LoC changes to override the necessary methods to enable
                                                                  5
   3                                                               E.g.,        positional embedding type       and
     Based on the changes required in GPTModel.                positional embedding config
   4
     Based on Megatron-LM’s own Linear variants.                 6
                                                                   To override two properties.
                                                                 7
                                                                   Based on changes in DSDenseBlockedAttention.
we note that this varies greatly between TorchTitan model          However, using a composable config system does
implementations8 .                                                 not guarantee strict encapsulation.          In particular,
                                                                   Praxis flattens a number of RoPE-specific configs (e.g.,
In the MoE case, TorchTitan conditionally instantiates ei-
                                                                   use rotary position emb) into each attention layer
ther a MoE child layer or a standard FeedForward layer,
                                                                   implementation, which means that it incurs at least O(N )
based on the config field moe enabled. Because TorchTi-
                                                                   LoC-Complexity for a single RoPE variant. As it turns
tan implements customized layers for each model (e.g.,
                                                                   out, Praxis attention layers compose the actual RoPE im-
TransformerBlock for Llama, DecoderLayer for
                                                                   plementation itself via defining a RoPE layer “template”
DeepSeekV3), this results in O(N M ) complexity as we
                                                                   rotary position emb tpl. However, because each
must modify the corresponding Transformer layer for each
                                                                   RoPE variant may have different configuration interfaces,
model for each MoE variant.
                                                                   the flattening ultimately means that LoC-Complexity un-
Like with RoPE, we incur at least 10 LoC for each                  der RoPE variants is O(N M ), as each RoPE variant may
ModelArgs (due to more complex configuration of MoE),              nevertheless require modifications to each attention layer.
as well as 10 LoC for each attention layer implementation9 .
                                                                   For 10 attention variants11 , each incurring approximately 30
Over 20 model variants, this results in 400 LoC changes.
                                                                   LoC12 , this incurs at least 300 LoC.
Flax Flax implements the Gemma model via mostly self-
                                                                   MaxText       MaxText builds on top of Flax mod-
contained Transformer modules which are not shared with
                                                                   ules, and follows a similar layer design and analy-
other implementations. To integrate RoPE, the Gemma
                                                                   sis. MaxText’s Attention conditions on configs (like
TransformerConfig is modified to add the RoPE pa-
                                                                   attention type and rope type) to choose the RoPE
rameters; the Gemma Transformer module is modified
                                                                   module. This has undesirable interactions between the at-
to propagate the RoPE parameters from the config to its
                                                                   tention and RoPE variants; in the worst case, Attention
Block submodules; each Block submodule is modified
                                                                   must account for the cross-product of attention and RoPE
so that its init signature can accommodate additional
                                                                   variants. We can already observe this with MaxText’s MLA
RoPE parameters; and each Attention module must
                                                                   implementation; while MLA implemented as a subtype, its
take these RoPE parameters to finally implement the RoPE
                                                                   RoPE logic must also be handled in its parent Attention.
logic. It is easy to see that the LoC-Complexity(RoPE)
                                                                   For each RoPE variant and model, we can expect at least 10
in this case is O(N M ), as for each RoPE variant, one
                                                                   LoC13 , which results in 200 LoC across 20 variants.
would need to correspondingly update RoPE parameters
in the top-level TransformerConfig, and then propa-                In a similar way, MaxText’s MoE implementation details
gate those configs down an arbitrary number of modules             are flattened into each model’s decoder. Each model im-
to the Attention layer implementation. For a single                plements its own DecoderLayer, while a monolithic
variant, the Gemma model incurs at least 30 LoC across             Decoder selects a decoder layer implementation based
TransformerConfig, Transformer, Block, and                         on the name of the model in the config. With 10 LoC per
Attention parameterization modifications, not including            decoder14 , this results in 200 LoC for 20 model variants.
the actual RoPE implementations. For 20 model variants,            MaxText’s trainer also includes MoE-specific logic: each
this results in at least 600 LoC changes.                          loss function uses MoE configs to apply auxiliary losses.
                                                                   With 5 LoC for each loss function15 , across 20 model vari-
Praxis Praxis is the layer library of Pax. It internally           ants this results in additional 100 LoC.
uses fiddle, a config system similar to the one in AXLearn,
which allows it to express certain re-parameterizations
using composition. For example, Praxis uses a “tem-
plate” approach to configure the MoE layer in each Trans-
former layer stack. This “template” is configured along
with several MoE configs (like num experts) in the
StackedTransformerLayer definition, which in the-
ory allows the integration of MoE to scale with O(M ), or
the number of MoE variants. Because some MoE configs are
still flattened, although not to the extent of causing quadratic     11
                                                                         Based on number of attention layers in Praxis
interactions, each MoE variant incurs 5 LoC change10 .               12
                                                                         Based       on      DotProductAttention              and
   8                                                               MultiQueryDotProductAttention
     Based on DeepSeekV3 and Llama4 implementations.                  13
   9                                                                     Based on Attention implementation for “llama3.1”, “yarn”,
     Based on TransformerBlock and DecoderLayer.
  10                                                               etc.
     Based on the number of flattened configs.                        14
                                                                         Based on changes in Decoder.
                                                                      15
                                                                         Based on loss fn and dpo loss fn.
