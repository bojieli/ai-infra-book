                      B LUEPRINT, B OOTSTRAP, AND B RIDGE :
          A S ECURITY L OOK AT NVIDIA GPU C ONFIDENTIAL C OMPUTING


 Zhongshu Gu 1 Enriquillo Valdez 1 Salman Ahmed 1 Julian James Stephen 1 Michael V. Le 1 Hani Jamjoom 1
                                      Shixuan Zhao 2 Zhiqiang Lin 2


                                                           A BSTRACT
        NVIDIA GPU Confidential Computing (GPU-CC) aims to provide secure execution for AI workloads. For end
        users, enabling GPU-CC is seamless and requires no modifications to existing applications. However, this ease
        of adoption relies on a proprietary and highly complex system that is difficult to inspect, creating challenges for
        researchers seeking to understand its architecture and security landscape. In this work, we provide a security look
        at GPU-CC by reconstructing a coherent view of the system. We first examine the system’s blueprint, focusing
        on the specialized architectural engines that support its security mechanisms. We then analyze the bootstrap
        process, which coordinates hardware and software components to establish these protections. Finally, we conduct
        targeted experiments to assess whether, under the GPU-CC threat model, data transfers along different paths
        remain protected across the bridge between trusted CPU and GPU domains. We responsibly disclosed all security
        findings presented in this paper to the NVIDIA Product Security Incident Response Team (PSIRT).

1       I NTRODUCTION                                              stacks to secure communication between the CPU and GPU.
                                                                   For researchers in the machine learning system community,
Confidential computing enables secure outsourcing of sen-          a comprehensive understanding of GPU-CC’s architecture
sitive computations to Trusted Execution Environments              and security landscape is essential. This knowledge allows
(TEEs) on public clouds, protecting the confidentiality and        researchers to critically assess whether the environment sat-
integrity of data while in use. Early efforts focused on           isfies specific security requirements under the stated threat
CPU Confidential Computing (CPU-CC), which secures                 model. However, achieving this depth of insight remains
computations within the CPU package and encrypts sys-              challenging due to three primary obstacles:
tem memory. As modern workloads increasingly rely on
GPUs, driven by the growth of large language and vision            1. Lack of Public Specifications. NVIDIA provides high-
models, NVIDIA introduced GPU Confidential Comput-                 level overviews (Dhanuskodi et al., 2023; NVIDIA, 2025b)
ing (GPU-CC) (Dhanuskodi et al., 2023) starting with the           of GPU-CC for end users, but leaves many technical details
Hopper architecture, integrating support into CUDA and             undefined. For example, GPU-CC-related engines, such as
GPU kernel drivers. GPU-CC extends the trust boundary              the Foundation Security Processor (FSP), GPU System Pro-
from CPUs to GPUs, creating a unified protected domain             cessor (GSP), and Secure Processor (SEC2), only appear in
for AI pipelines. This advancement expands support for             diagrams, but their precise functions, interactions, and roles
secure GPU-accelerated applications and enables collabora-         are largely undocumented. While NVIDIA’s patents (Rogers
tive learning scenarios (Mo et al., 2022; Guo et al., 2024a;       et al., 2023a;b; 2025) provide more technical details than
Quoc & Fetzer, 2021; Eichner et al., 2024; Gu et al., 2019;        white papers, their claims are written in legalistic, abstract
Cheng et al., 2024a) where mutually distrusting parties can        terms to maximize protection and cover a broad range of
jointly execute AI workloads without exposing private data.        possible implementations, making them difficult to interpret.
GPU-CC enables users to deploy AI workloads with no mod-           2. Proprietary Ecosystem. While NVIDIA has open-
ifications to application-level programs or data. Despite this     sourced certain components, such as open GPU kernel mod-
seamless user experience, GPU-CC introduces extensive              ules (NVIDIA, 2025e) and nvTrust (NVIDIA, 2025c), most
changes across the system software, firmware, and hardware         elements related to GPU-CC remain closed, either embed-
    1
                                                                   ded within GPUs or distributed only as binary executables.
     IBM Research 2 The Ohio State University. Correspondence
to: Zhongshu Gu <zgu@us.ibm.com>.                                  3. System Complexity. GPU-CC introduces substantial mod-
                                                                   ifications across system stacks. To preserve compatibility
Proceedings of the 9 th MLSys Conference, Bellevue, WA, USA,       with existing implementations, GPU-CC layers new data
2026. Copyright 2026 by the author(s).
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

protection mechanisms over heterogeneous and legacy com-          2016; Kaplan, 2017; AMD, 2020). CPU-CC protects the
ponents. For example, CPU-GPU data transfers traverse             confidentiality and integrity of computations and data within
multiple distinct data paths, each with its own transmis-         confidential virtual machines (CVMs) by enforcing crypto-
sion mechanisms, data formats, and granularities. This            graphic isolation from the host environment, including the
compatibility-driven design significantly increases imple-        hypervisor, system administrators, and I/O devices, which
mentation complexity and broadens the attack surface, mak-        are considered untrusted or potentially compromised. This
ing it harder to reason about the system’s security guaran-       protection relies on security features such as trusted VM
tees.                                                             management and runtime memory encryption. Each CVM
                                                                  is assigned a unique ephemeral key that is used to encrypt
In this paper, we aim to reconstruct a coherent view of the
                                                                  the memory. The Advanced Encryption Standard (AES)
GPU-CC system and conduct security analysis structured
                                                                  engine in the on-die memory controller handles the encryp-
around three stages: Blueprint, Bootstrap, and Bridge.
                                                                  tion and decryption of data transferred between the CPU
Blueprint (§6). We begin by instrumenting and inter-              and the CVM’s private memory. CPU-CC also supports
cepting inter-component interactions within the GPU-CC            remote attestation to verify the authenticity and integrity of
system. By correlating these experimental observations            the trusted platform.
with fragmented information synthesized from public
                                                                  I/O Protection in CPU-CC. The trust boundary of
sources (Dhanuskodi et al., 2023; NVIDIA, 2025b; Rogers
                                                                  CPU-CC is limited to the CPU and its designated private
et al., 2023a;b; 2025), we uncover a static blueprint of the
                                                                  memory regions. When data leaves this boundary, e.g.,
system, identifying the architectural engines that form the
                                                                  during I/O operations, it must pass through untrusted com-
foundation of GPU-CC’s security design.
                                                                  ponents such as the hypervisor and I/O devices. I/O devices
Bootstrap (§7). Next, we analyze the bootstrap process,           commonly use Direct Memory Access (DMA) to read and
including secure boot, key generation, firewall establish-        write memory without CPU involvement. Since these de-
ment, and device attestation, which bring GPU-CC to a             vices are untrusted, they are not permitted direct access to
trustworthy state. We examine the dependencies between            the CVM’s private memory, which is encrypted with the
hardware and software components and study how secu-              CVM’s key. The general practice is to employ software-
rity mechanisms are orchestrated and enforced during this         based encryption mechanisms, e.g., Transport Layer Secu-
transition.                                                       rity (TLS) is used for network traffic and Linux Unified Key
                                                                  Setup (LUKS) is used for disk encryption. In this model,
Bridge (§8). Finally, we evaluate the security of runtime         I/O-bound data is first encrypted within the CVM’s private
data transfers across the bridge between the trusted CPU          memory, then transferred to a staging buffer. This buffer,
and GPU domains. These transfers traverse the untrusted           which is outside the CVM’s encrypted memory, is accessi-
PCIe interface, making them vulnerable to observation or          ble via DMA by I/O devices. Therefore, the encrypted I/O
manipulation by adversaries controlling the host system. For      data can be securely transmitted to the devices.
each data path, we investigate the protection mechanisms in       Unlike network interface cards or storage disks, GPUs are
place, analyze potential attack surfaces, assess their security   not I/O devices solely responsible for data delivery. In-
implications, and propose possible mitigations. All secu-         stead, they have evolved into general-purpose compute
rity findings presented in this paper have been responsibly       devices composed of numerous Streaming Multiproces-
disclosed to NVIDIA’s Product Security Incident Response          sors (SMs) and equipped with on-package High-Bandwidth
Team (PSIRT).                                                     Memory (HBM). For the GPU to process offloaded code
                                                                  and data, these assets must ultimately be in plaintext within
2    BACKGROUND                                                   the GPU’s device memory at runtime. Meanwhile, the PCIe
                                                                  interconnect between the CPU and GPU is generally consid-
In this section, we provide a brief overview of the evolution     ered untrusted and is vulnerable to snooping and tampering
of confidential computing from CPU to GPU, summarize              attacks. Therefore, extending the trust boundary of confi-
earlier research efforts on enabling confidential comput-         dential computing from CPUs to GPUs requires establishing
ing on GPUs, and discuss the current status of NVIDIA’s           end-to-end secure channels that protect code and data as
GPU-CC technology.                                                they traverse the untrusted interconnects.
CPU-CC Technologies. NVIDIA GPU-CC cannot operate                 Prior Research on GPU-CC. Research efforts to support
independently. It must work in conjunction with virtual           GPU-CC began with systems like Graviton (Volos et al.,
machine (VM)-based CPU-CC, such as Intel Trust Domain             2018) and HIX (Jang et al., 2019). Graviton proposed modi-
Extensions (TDX) (Intel, 2023b; Cheng et al., 2024b) and          fications to the GPU’s command processor, while HIX intro-
AMD Secure Encrypted Virtualization (SEV) (Kaplan et al.,         duced changes to the I/O interconnect. However, due to the
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

proprietary nature of NVIDIA GPUs, Graviton’s approach           visor, and peripheral devices. They may also access host
could only be evaluated via emulation, and HIX lacks pro-        system memory.
tection against hardware-based attacks on PCIe and GPU
memory. To avoid hardware modifications, researchers have        Extended Adversarial Capabilities under GPU-CC. Ad-
also explored software-based GPU-CC approaches. For              versaries may monitor and manipulate the traffic on the CPU-
instance, SAGE (Ivanov et al., 2023) used Intel Software         GPU interconnects. They can interact with GPU configu-
Guard Extensions (SGX) as a local verifier to establish a        rations via in-band tools (e.g., nvTrust (NVIDIA, 2025c))
dynamic root of trust on the GPU, while Honeycomb (Mai           or out-of-band interfaces like the Baseboard Management
et al., 2023) performed static analysis to validate GPU appli-   Controller (BMC). They may flash the GPU’s VBIOS or
cations at load time. Confidential computing has also been       update its firmware, reassign GPUs between CVMs, detach
explored for other accelerators such as IPUs (Vaswani et al.,    and reattach GPUs, or physically remove them from PCIe
2023) and NPUs (Lee et al., 2022), where there is generally      buses.
more flexibility in customizing the hardware. In addition to
x86 systems, GPU-CC has also been studied in the context         Out of Scope. CPU-CC and GPU-CC provide confidential-
of Arm systems (Wang et al., 2024a; Jiang et al., 2022; Deng     ity and integrity guarantees for data in use, but do not ad-
et al., 2022). More broadly, Wang and Oswald (Wang &             dress system availability. Adversaries with control over the
Oswald, 2026) provide a comprehensive survey of recent           host can launch denial of service attacks at will. Addition-
advances in confidential computing for CPU-GPU systems.          ally, sophisticated physical attacks, such as decapsulation
                                                                 to probe internal interconnects or on-package HBM, are
One of the greatest challenges in designing GPU-CC stems         also out of scope. Modern GPUs use advanced multi-layer
from the immutability of commodity GPUs. While mod-              packaging with dense interconnects and protective layers,
ifying hardware and firmware is the most direct path to          making such attacks technically challenging and risky, with
enabling secure execution on GPUs, it is often impracti-         a high chance of permanently damaging the silicon.
cal. However, GPU vendors like NVIDIA possess the flexi-
bility to add confidential computing functionality directly      Attack Surface. As CPU-CC and GPU-CC are integrated
into their hardware/firmware. NVIDIA introduced the first        into a unified security domain, the attack surface expands,
commercial GPU-CC solution (Dhanuskodi et al., 2023)             allowing adversaries to launch targeted strikes from three
as part of its Hopper architecture. Recent research has          primary perspectives:
started to focus on improving the performance of NVIDIA
GPU-CC (Mohan et al., 2024; Tan et al., 2025; Tan & Mi,          1. Attacks on the VM-based CPU-CC. In a typical de-
2024; Zhao et al., 2025; Lee et al., 2025; Wang et al.,          ployment, a physical GPU is passed through to a CVM,
2024b). However, a significant gap remains between us-           which hosts all sensitive code and data prior to GPU of-
ing the GPU-CC system and understanding its inner work-          floading. The CVM also holds the full privileges required
ings, largely due to the closed nature of NVIDIA’s ecosys-       to configure the GPU, making it a critical component of the
tem. This lack of transparency makes it difficult to evaluate    Trusted Computing Base (TCB) within the unified security
whether NVIDIA GPU-CC meets the security requirements            domain. Consequently, any compromise of the CVM ef-
of confidential computing or if its implementation can with-     fectively undermines the integrity of the GPU-CC. Recent
stand various attack vectors.                                    research on VM-based CPU-CC has uncovered a number of
                                                                 security flaws in both AMD SEV-SNP (Cohen et al., 2022;
Our study focuses on GPU-CC in NVIDIA H100 GPUs.                 Schlüter et al., 2024a;b; Zhang et al., 2024a; Schlüter et al.,
Certain new GPU-CC features, such as Multi-GPU sup-              2025; Gast et al., 2025; Schlüter & Shinde, 2025; De Meule-
port and Trusted I/O, are disabled or unavailable on our         meester et al., 2025) and Intel TDX (Aktas et al., 2023;
test platform. We discuss these features conceptually in         Wilke et al., 2024; Rauscher et al., 2025; 2026). These vul-
Appendix §B.                                                     nerabilities span a wide range of attack vectors and require
                                                                 timely firmware and microcode updates to mitigate them
3    T HREAT M ODEL                                              within the CPU-CC.

NVIDIA’s GPU-CC threat model inherits and extends the            2. Attacks on the CPU-GPU Interconnects. This scenario
threat assumptions of VM-based CPU-CC technologies               focuses on the confidentiality and integrity of data in tran-
such as Intel TDX and AMD SEV.                                   sit between the CVM and the GPU. In the threat model,
                                                                 interconnects such as PCIe are treated as untrusted, leav-
Adversarial Capabilities under CPU-CC. Adversaries               ing them susceptible to traffic interception. Specifically,
are assumed to have physical or remote access to the host        data paths involving Remote Procedure Call (RPC), mem-
machine, potentially controlling the boot firmware, System       ory transfers, Unified Virtual Memory (UVM) operations,
Management Mode (SMM), host operating system, hyper-             memory scrubbing, fault delivery, and CUDA operations
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

are at risk. While prior work demonstrated the feasibility        enforcement of security mechanisms (e.g., firewall) by eval-
of reconstructing deep learning models by snooping the            uating the proportion of control registers that are blocked
memory and PCIe buses (Hu et al., 2020; Zhu et al., 2021),        versus those still exposed. For device attestation, we extract
the advent of CPU-CC and GPU-CC, which encrypt traffic            attestation reports and certificate chains by intercepting re-
across the memory and PCIe buses respectively, makes such         lated functions in nvTrust and NVIDIA kernel-mode driver.
side-channel analysis more challenging.                           The certificate hierarchy also helps validate the secure boot
                                                                  sequence.
3. Attacks on the GPU Hardware. Beyond the interconnects,
the GPU itself presents a significant attack surface. This        The Bridge stage corresponds to the runtime execution of
survey (Naghibijouybari et al., 2022) provided a comprehen-       GPU-CC, focusing on the security of data transfers between
sive overview of how vulnerabilities manifest for systems         trusted CPU and GPU domains. Because data flows traverse
integrating hardware accelerators prior to 2022. Recent           multiple distinct data paths with different implementations,
studies have explored NVIDIA GPU-specific security flaws,         we discuss the specific methodology case by case in §8.
including electromagnetic side channels (Zhan et al., 2022),
isolation issues in Multi-Instance GPU (MIG) (Zhang et al.,       5    P LATFORM C ONFIGURATION
2023), cache-based side channels (Zhang et al., 2024b),
and code injection attacks (Guo et al., 2024b). These ex-         Our host system is equipped with dual-socket AMD EPYC
ploits typically assume the adversary and victim share an         9634 84-core processors, with SEV-SNP enabled, and
execution domain with direct GPU access. Consequently,            an eight-GPU NVIDIA H100 SXM5 setup. Each GPU
these attacks become particularly critical if an adversary        has 80 GB of GPU memory, and the VBIOS version is
can successfully breach the CVM, as described in the first        96.00.61.00.01. The host operating system is Ubuntu
scenario.                                                         22.04.5 LTS running the Linux 5.19.0 kernel.
                                                                  At the time of writing, GPU-CC on our host does not sup-
4    M ETHODOLOGY                                                 port the Multi-GPU configuration. Therefore, we passed
                                                                  through a single GPU to a guest CVM for testing. We eval-
We analyze NVIDIA GPU-CC through three stages,
                                                                  uated two CVMs. One guest runs Red Hat Enterprise Linux
Blueprint (§6), Bootstrap (§7), and Bridge (§8), organized
                                                                  9.4 with the Linux 6.1.91 kernel, using NVIDIA driver
according to their chronological order and corresponding
                                                                  version 550.54.15 and CUDA 12.4. The other guest
security properties. Each stage requires different method-
                                                                  runs Ubuntu 22.04 with Linux kernel 6.8.0, NVIDIA
ologies for security analysis.
                                                                  driver version 570.86.15, and CUDA 12.8. Each CVM
The Blueprint stage corresponds to the static state of            is provisioned with 64 GB of system memory.
GPU-CC, focusing on the functionalities and roles of the ar-
chitectural engines. Since these engines operate behind           6    B LUEPRINT: A RCHITECTURAL E NGINES
the GPU boundary and their firmware is closed-source,
their behavior cannot be directly monitored. NVIDIA               Multiple architectural engines have been designed or repur-
patents (Rogers et al., 2023a;b; 2025) provide more techni-       posed to support GPU Confidential Computing (GPU-CC).
cal details than the white paper (Dhanuskodi et al., 2023),       As shown in Figure 1, these engines interact directly with
but the broad, generalized patent claims make them difficult      NVIDIA software stacks, such as the NVIDIA kernel-mode
to interpret. To narrow the scope, we infer engine function-      driver, Unified Virtual Memory (UVM) driver, and CUDA
alities by intercepting communications between NVIDIA             user-mode driver. We outline the key functionalities of each
software stacks and the hardware engines. For the NVIDIA          engine and their roles in GPU-CC.
kernel-mode and UVM drivers, which are open-source, we
instrument the code to track control flows to specific engines.   Foundation Security Processor (FSP). FSP is character-
For closed-source components, such as the CUDA runtime            ized as a RISC-V microcontroller that undergoes a secure
and user-mode driver, we preload modified libraries to indi-      boot early in the hardware chain of trust. On the NVIDIA
rectly observe execution events. We then cross-validate our       Hopper architecture, once the FSP is securely initialized,
findings against patent descriptions to refine the mapping of     the NVIDIA kernel-mode driver transmits several binary im-
engine roles.                                                     ages, specifically the GSP First Mutable Code (GSP-FMC)
                                                                  and the GSP Resource Manager (GSP-RM), across a dedi-
The Bootstrap stage corresponds to the initialization of          cated communication channel. These images are digitally
GPU-CC, during which the system establishes a trustworthy         signed by NVIDIA to ensure integrity and authenticity. The
state. We intercept the launch sequence managed by the            FSP validates the signatures before initializing and launch-
NVIDIA kernel-mode driver to monitor how architectural            ing GSP-RM on the GPU System Processor (GSP).
engines are brought up. We also quantitatively measure the
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

In addition, in the GTC talk (Delignat-Lavaud & Rogers,
2023), FSP was described as responsible for setting up
GPU-CC mode and performing attestation. However,
NVIDIA patents (Rogers et al., 2023a;b; 2025) attribute
these same operations to the Secure Processor (SEC2) en-
gine. Because the GPU-side implementation remains pro-
prietary, the exact technical details are unclear. Neverthe-
less, since both the FSP and SEC2 reside within the trusted
boundary, from a security standpoint, it makes no difference
which engine performs these tasks. The shift likely reflects
a migration of functionality during NVIDIA’s architectural
evolution.

GPU System Processor (GSP). The GSP serves as the                 Figure 1: The software/hardware components in GPU-CC
GPU’s control plane, handling initialization and resource
management tasks. The GSP engine is a RISC-V microcon-
troller equipped with Advanced Encryption Standard (AES)
hardware for encryption and decryption. It communicates
                                                                  attached to the GPU but takes effect only after the next GPU
exclusively with the NVIDIA kernel-mode driver. In the
                                                                  reset.
context of GPU-CC, the GSP serves two main roles:
                                                                  2. Device Attestation. Evidence indicates that SEC2 is re-
1. Key Negotiation. When GPU-CC is enabled, a Security
                                                                  sponsible for generating an attestation report to prove the
Protocol and Data Model (SPDM) session is established
                                                                  authenticity and trustworthiness of the GPU. The Device
before launching the GSP-RM. The GSP hosts an SPDM
                                                                  Identity Key (DIK) is burned into the key fuse during sili-
Responder, and during the handshake process, the NVIDIA
                                                                  con manufacturing, which is unique and immutable. The
kernel-mode driver negotiates a shared master secret with
                                                                  DIK is exclusively accessible by SEC2, preventing access
the SPDM Responder. This secret serves as the foundation
                                                                  from any other entities. The attestation report is signed
for deriving all session keys that protect the data transmitted
                                                                  using an Attestation Key (AK) derived from a hierarchy of
between the confidential virtual machine (CVM) and GPU.
                                                                  cryptographic identities embedded in the silicon.
2. Resource Manager API (RMAPI) Remote Procedure
                                                                  3. Secure Data Transmission. SEC2 engine has session
Call (RPC). Once the GSP-RM is launched, it communi-
                                                                  keys with the CVM to protect the data in transit. The CVM
cates with the NVIDIA kernel-mode driver via the RMAPI
                                                                  may either sign or encrypt the data using the session keys
RPC channel and transfers memory through the CPU-GSP
                                                                  before placing it in an unprotected staging buffer. The SEC2
Direct Memory Access (DMA) channel. Separate session
                                                                  engine then retrieves the data: If it is signed, SEC2 verifies
keys, derived from the master secret, protect data trans-
                                                                  the integrity to detect any tampering during transit. If it
mitted through the bi-directional RPC and DMA channels.
                                                                  is encrypted, SEC2 decrypts the data and stores it in the
Additionally, firmware running within the GSP may sani-
                                                                  GPU’s CPR.
tize all inputs and outputs to prevent unauthorized access or
tampering by the host.                                            4. Memory Scrubbing. Sensitive data within the GPU’s CPR
                                                                  must be erased when no longer needed. The NVIDIA kernel-
Secure Processor (SEC2). SEC2 is a RISC-V microcon-               mode driver can establish a scrubber channel associated
troller specifically designed for GPU-CC. SEC2 can de-            with the SEC2 engine. When physical memory pages are
crypt and verify data, but it cannot encrypt data. Unlike         freed and require scrubbing, commands submitted to the
GSP, SEC2 is accessible to both kernel-mode clients (e.g.,        scrubber channel are signed to prevent tampering. SEC2
NVIDIA kernel-mode driver and UVM driver) and user-               can trigger a soft reset of the GPU, ensuring that before
mode clients (e.g., CUDA user-mode driver). Based on              the GPU memory becomes visible on the system bus, all
the function descriptions in NVIDIA patents (Rogers et al.,       session keys are deleted, and memory is wiped.
2023a;b; 2025), SEC2 provides multiple security function-
alities:                                                          5. Secure Workload Submission. SEC2 can be used for
                                                                  bootstrapping secure workload submission. The CUDA
1. Setup of Compute Protected Region (CPR) and GPU-CC             user-mode driver and UVM driver can allocate secure chan-
Mode. Evidence indicates that SEC2 is responsible for creat-      nels bound to the SEC2 engine, ensuring command integrity.
ing the CPR and transitioning the GPU into GPU-CC mode.           Commands in this channel can be signed using an HMAC
The GPU-CC mode setting can be stored in an EEPROM                session key. SEC2 verifies the integrity of these commands
                                                                  before execution.
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

Copy Engine (CE). CEs manage memory transfers for                CPR via the Base Address Register (BAR), and (4) device
GPUs and include AES hardware for encryption and de-             attestation to establish the trustworthiness of GPU-CC.
cryption. A GPU has one or more logical CEs and physical
CEs. Physical CEs handle data movement, while logical            7.1   Secure Boot of Architectural Engines
CEs manage the control logic for physical CEs. Logical CEs
are accessible to both kernel-mode clients (e.g., NVIDIA         To protect against malicious firmware updates or VBIOS
kernel-mode driver and UVM driver) and user-mode clients         flashing, GPU-CC enforces a secure boot process for archi-
(e.g., CUDA user-mode driver). Clients can allocate secure       tectural engines. All firmware components must be authen-
channels to schedule work with logical CEs, with encryp-         ticated and cryptographically signed by a trusted authority.
tion keys obtained from the NVIDIA kernel-mode driver.           According to the information shared by an NVIDIA engi-
Each logical CE negotiates four keys with the kernel-mode        neer on the developer forum (NVIDIA, 2024), the chain-
driver in CVM: two keys are for user mode and two for ker-       of-trust follows this sequence: CEC EROT (if present) →
nel mode. For each mode, one key protects host-to-device         FSP → GSP → SEC2. Here, CEC EROT refers to Mi-
(h2d) data transfers, while the other protects device-to-host    crochip’s CEC1736 (MICROCHIP, 2025), a real-time con-
(d2h) transfers. All channels bound to the same logical CE       troller, where EROT stands for “External Root of Trust.”
share the same key. Here are the security functionalities of     The CEC1736 plays a critical role as a hardware Root of
CEs:                                                             Trust, anchoring the secure boot process and establishing
1. Data Movement Protection. CE ensures that data copied         the system’s chain-of-trust. Both CEC EROT and FSP’s
from CPR to non-CPR memory is encrypted, while data              Boot ROM (BROM) authenticate FSP firmware. This dual-
transferred from non-CPR to CPR is decrypted and integrity-      layer verification ensures both external and internal integrity
checked. For example, it can retrieve encrypted GPU push-        checks before FSP is fully booted.
buffers or CUDA kernels from unprotected memory, de-
                                                                 GSP Initialization. The initialization of the GSP is a multi-
crypting them into CPR before executing them. Conversely,
                                                                 step, cryptographically secured sequence. This process is
CEs can encrypt and sign data in CPR before moving them
                                                                 orchestrated by the NVIDIA kernel-mode driver and an-
to unprotected memory, such as when the GPU sends en-
                                                                 chored from the FSP. Once the FSP has completed its own
crypted and signed synchronization signals (e.g., tracking
                                                                 secure boot, the driver issues a command to FSP to trigger
semaphores) to notify command completion. Clients can
                                                                 the GSP boot process. The chronological flow is as follows:
then decrypt and verify these signals to proceed. Addition-
ally, they may also support plaintext transfers of CPR-to-       1. Image Extraction and Memory Allocation. The kernel-
CPR or non-CPR-to-non-CPR memory.                                mode driver extracts the GSP-FMC and GSP-RM binary im-
                                                                 ages from their respective archives. The driver then allocates
2. Memory Access Control. CEs, in conjunction with the
                                                                 unprotected system memory to load these images. These
GPU’s Memory Management Unit, enforce access control
                                                                 firmware components are typically bundled with NVIDIA
by restricting compute engines to CPR memory. Once a
                                                                 kernel-mode driver releases, facilitating flexible runtime
compute engine accesses CPR, it is prevented from access-
                                                                 updates to the GSP firmware. During this phase, the driver
ing any other unprotected memory regions. Any attempt to
                                                                 also calculates the total secure internal memory required
access memory outside CPR is blocked, triggering a mem-
                                                                 by the GSP firmware and populates a metadata structure to
ory fault.
                                                                 define the necessary memory region sizes.
3. Replay Attack Prevention. AES’s Initialization Vectors
                                                                 2. Chain of Trust (CoT) Payload Construction. The driver
(IVs) are used to prevent replay attacks. While channels
                                                                 builds a CoT message payload containing the system mem-
on the same CE share the same h2d and d2h keys, each
                                                                 ory address of the GSP-FMC image along with its au-
channel independently maintains its own pair of IVs for
                                                                 thentication fields (cryptographic hashes, public keys, and
h2d and d2h and increments them after each encryption or
                                                                 signatures). This payload is transmitted to the FSP via
decryption. As a result, attempts to replay the old ciphertext
                                                                 Management Component Transport Protocol (MCTP).
would fail due to authentication tag mismatches.
                                                                 3. FSP Authentication and Initialization. The FSP pro-
7    B OOTSTRAP : S ECURITY I NITIALIZATION                      cesses the CoT payload, copies the GSP-FMC image, and
                                                                 verifies its digital signature. Upon successful verification,
This section explains the GPU-CC initialization process,         the GSP-FMC can begin to execute.
covering: (1) secure boot of architectural engines required
                                                                 4. GSP-RM Loading. The GSP-FMC reads the sizing meta-
for GPU-CC, (2) key negotiation and derivation, (3) firewall
                                                                 data (generated in step 1) to allocate the necessary device
establishment to block regular access to GPU registers and
                                                                 memory in the GPU. It then copies the GSP-RM image into
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

                                   Table 1: The complete list of derived keys in GPU-CC

  Key Name                                              Engine    Protected Communication                         Number
  {gsp cpu,cpu gsp} locked rpc                                    RPCs between CPU and GSP                        2x1
  {gsp cpu,cpu gsp} dma                                           Memory transfers between CPU and GSP            2x1
                                                        GSP
  gsp cpu replayable fault                                        Replayable faults sent from GSP to CPU          1
  gsp cpu non replayable fault                                    Non-replayable faults sent from GSP to CPU      1
  cpu sec2 {data,hmac} {user,kernel}                              Channels between CPU and SEC2                   2x2
                                                        SEC2
  cpu sec2 {data,hmac} scrubber                                   Scrubber channels between CPU and SEC2          2x1
  lce{x} {h2d,d2h} {user,kernel}                        CE        Channels between CPU and CE                     8x2x2


the allocated memory region and cryptographically validates
its authenticity and integrity.
5. GSP Engine Launch. The GSP-FMC points the hardware
instruction pointer to the verified GSP-RM’s entry point,
effectively booting the GSP engine. Once active, the GSP
initiates the key negotiation for GPU-CC and establishes
the protected RPC channel for CPU-GSP communication.

The Relationship between GSP and SEC2. Although
NVIDIA’s forum (NVIDIA, 2024) indicates that the GSP is
responsible for starting the SEC2 engine, the precise boot se-
quence for these components remains opaque, as the internal
handoff is concealed within the GPU hardware. However,
analysis of the NVIDIA kernel-mode driver suggests that,
starting with the Blackwell architecture, the GSP can also be
initialized via the SEC2 engine using a process analogous to
the FSP-driven method. This shift likely represents a migra-
                                                                  Figure 2: Classification of register reads via GPU’s BAR0
tion of security functionality, consolidating early-stage trust
management within the SEC2 engine as part of NVIDIA’s
ongoing architectural iteration.
                                                                  and gsp cpu non replayable fault, protect the
                                                                  transmission of replayable and non-replayable fault packets
7.2   Key Negotiation and Derivation
                                                                  from GSP to CPU.
The Security Protocol and Data Model (SPDM) Requester
                                                                  For SEC2, six keys are derived to protect secure work-
within the NVIDIA kernel-mode driver initializes an SPDM
                                                                  load launch channels and scrubber channels. Data keys
session with the SPDM Responder in the GSP. This session
                                                                  are used to encrypt data sent from the CPU to SEC2.
establishes a shared master secret, from which all crypto-
                                                                  HMAC keys are used to sign data for integrity verifica-
graphic keys used in GPU-CC are derived to protect en-
                                                                  tion. Each key is used in either user mode (e.g., by
crypted communications across the untrusted interface.
                                                                  the CUDA user-mode driver) or kernel mode (e.g., by
Table 1 lists all the derived keys used in GPU-CC, catego-        the UVM driver). The cpu sec2 data scrubber and
rized by the engines they are associated with. For each key,      cpu sec2 hmac scrubber keys are used by the scrub-
we indicate the protected communication channel and the           ber channel.
number of keys involved. The key names are designed to be
                                                                  On an H100 GPU, secure data transfers are supported with
self-explanatory, incorporating the engine name, direction,
                                                                  eight logical CEs. Each engine utilizes distinct crypto-
and key type.
                                                                  graphic keys to secure host-to-device and device-to-host
For GSP, the following six keys are derived: the                  transfers across both user and kernel modes. This architec-
gsp cpu locked rpc and cpu gsp locked rpc                         ture results in a total of 32 derived keys.
keys are used to protect RMAPI RPC communication.
The gsp cpu dma and cpu gsp dma keys are used to                  7.3   Firewall Establishment
protect memory transfers between CPU and GSP. Two
additional keys, i.e., gsp cpu replayable fault                   In non-GPU-CC mode, system administrators on the host
                                                                  can freely access and modify the GPU’s control and sta-
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

tus registers, as well as its device memory, through BARs.
These registers configure various GPU settings, while the
device memory may contain sensitive user data provisioned
within the CVM. Since the host, along with its adminis-
trators and software stacks, is no longer considered trusted
in the threat model, BAR-based PCIe access substantially
enlarges the attack surface.
To mitigate this, NVIDIA’s paper (Dhanuskodi et al., 2023)
states that enabling GPU-CC mode activates a PCIe firewall
mechanism called BAR0 Decoupler, which blocks unautho-
rized accesses to the majority of registers through BAR0.
However, a limited subset of registers remains accessible to
support essential GPU management operations.
                                                                   Figure 3: GPU’s device and RIM file certificate chains
A critical concern is determining which registers remain
accessible after GPU-CC is enabled and whether their expo-
sure poses any potential security risks. Unfortunately, the
specifications for NVIDIA GPU control registers are pro-         data transmission between CVMs and GPUs, as discussed
prietary, and no public documentation is available detailing     in detail in §8.
the functionality of each register. Only a limited number of
register fields have been referenced through the open GPU
                                                                 7.4   Device Attestation
kernel modules and nvTrust.
                                                                 The purpose of device attestation is to establish crypto-
Although the precise semantics of individual registers re-
                                                                 graphic proof that a CVM is communicating with a genuine,
main unclear, we quantitatively assess the difference in
                                                                 uncompromised GPU.
register accessibility before and after enabling the GPU-CC
mode. To this end, we developed a scanning program that          Device Certificate Chain. The process begins with each
simulates an adversary probing the entire BAR0 space on a        NVIDIA GPU being provisioned with a unique crypto-
H100 GPU under two modes, i.e., the non-GPU-CC mode              graphic identity: a hardware-fused Device Identity Key
and the GPU-CC mode. The program reads from BAR0                 (DIK) and a corresponding Device Identity Certificate. This
starting at offset 0x0 and advances in 4-byte strides. Given     certificate, anchored to NVIDIA’s Root Certificate Author-
that BAR0 spans 16 MB on a H100 GPU, this results in a           ity (CA), forms the basis of a certificate chain used to estab-
total of 0x400000 read operations.                               lish trust.
We categorize the returned values into three types: (1) Val-     When a verifier running inside a CVM initiates attestation,
ues: non-zero numerical returns, (2) Zeros: reads that return    it first retrieves the device certificate chain. As illustrated in
0x0, and (3) Errors: reads returning error codes in the form     the left part of Figure 3, the chain comprises five certificates:
of 0xbadxxxxx. As shown in Figure 2, in non-GPU-CC               the Attestation Certificate and Device Identity Certificate
mode, 7.94% of the fields return values and 80.25% return        are retrieved from the GPU, while the remaining Provi-
errors. In contrast, in GPU-CC mode, 99.78% of the fields        sioner Certificate, Model Certificate, and Root Certificate
return zero, with only 0.02% (1,042 fields) returning non-       are obtained from the NVIDIA kernel-mode driver. The De-
zero values and 0.19% returning errors. This significant         vice Identity Certificate corresponds to the immutable GSP
zeroing effect is attributed to the activation of the BAR0       BootROM (GSP-BROM) and is signed by the NVIDIA
Decoupler, which hides most control registers.                   Provisioner CA. The Device Identity Certificate uniquely
From an adversarial perspective, the decoupler obfuscates        identifies the device as an authentic NVIDIA GPU. The im-
register visibility by returning zeros, making it difficult to   mutable GSP-BROM starts execution and ensures that the
determine whether a field is simply unmapped or protected.       system begins in a secure state. The GSP-FMC is loaded
However, the small fraction (0.02%) of accessible fields         and authenticated. The GPU mathematically mixes its fused
still raises security concerns. We advocate for transparency     secret with specific firmware measurements to derive the
from NVIDIA regarding the functionality of these exposed         Attestation Key (AK) pair. The private AK is used to sign
registers and their necessity for GPU management.                attestation reports. The Attestation Certificate is signed by
                                                                 the private DIK, establishing a chain-of-trust. It contains the
Beyond register access, the PCIe firewall also blocks host       public AK required to verify the attestation report. The root
access to the Compute Protected Region (CPR) of GPU              of the certificate chain is the Root Certificate, a self-signed
memory. This restriction leads to a rerouting of runtime         certificate that serves as the trust anchor.
                  Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

The verifier replaces the Root Certificate with a local one to
prevent a compromised driver from undermining the certifi-
cate chain. It then verifies the chain in reverse order, starting
with the Device Identity Certificate and proceeding up to
the Root Certificate. During this process, the verifier sends
multiple requests to the NVIDIA Online Certificate Status
Protocol (OCSP) service (NVIDIA, 2025d) to check the
revocation status of each certificate. If the chain passes both
the signature and revocation checks, the verifier proceeds to
request the attestation report.

Attestation Report. Once the certificate chain is validated,
the verifier retrieves the attestation report directly from the
GPU. This report is cryptographically signed by the private
AK. The verifier validates the attestation report’s signature          Figure 4: Data transmission between CVM and GPU
using the Attestation Certificate. An attestation report con-
tains measurement data and an opaque metadata block that
identifies the GPU’s firmware and driver. The measurement
data consists of 64 structured records, each containing a           Data protection relies on the keys negotiated via SPDM
measurement specification, a size field, and a cryptographic        between the NVIDIA kernel-mode driver and the GPU.
hash of a measured component (e.g., firmware, configu-              Different data paths employ different keys, with the full
ration). The opaque data block provides information to              set of derived keys listed in §7.2. As shown in Figure 4,
identify the GPU driver and firmware version.                       staging buffers serve as intermediaries for these transfers.
                                                                    The staging buffer is a shared, unencrypted memory region
Measurements. The verifier extracts the driver and VBIOS            accessible by both the CVM and the GPU. Any data mov-
IDs from the opaque block and retrieves the corresponding           ing between protected regions, such as the CVM’s private
Reference Integrity Manifest (RIM) files from the NVIDIA            memory and the GPU’s CPR, must first be signed and/or
RIM service (NVIDIA, 2025f). The verifier checks the                encrypted using the negotiated key before being placed in
status of the RIM files by validating their schemas and en-         the staging buffer. Upon retrieval, the data is verified and/or
dorsements. Each RIM file includes a certificate chain. The         decrypted using the corresponding key.
verifier appends a local Root RIM Certificate to complete
the chain (the right part of Figure 3) and verify it. Once          By combining code analysis of NVIDIA kernel drivers with
the chain is validated, the leaf certificate, i.e., RIM File        runtime monitoring of data transmission, we identified six
Certificate, is used to verify the signature on the RIM file.       types of data paths used to transfer data between the CVM
To validate the measurements, the verifier compiles a list          and the GPU. Table 2 presents these data paths along with
of golden measurements from the RIM files. It compares              their associated hardware engines. We further evaluated
each measurement in the attestation report with the allowed         the security of each path in terms of confidentiality (C) and
values from the golden list. If a mismatch is found, the veri-      integrity (I). In the table, • indicates a confirmed leakage or
fier concludes that the GPU is not in the expected state. If        integrity violation, ◦ denotes that no issues were observed,
all checks succeed, the verifier reports the attestation result       represents a potential vulnerability with low risk, and ?
and the user can choose to transition the GPU-CC into the           signifies an unknown status due to proprietary components.
READY state.                                                        For each data path type, our security analysis is structured
                                                                    into three parts: (1) Methodology, describing our experi-
8    B RIDGE : S ECURITY A NALYSIS OF DATA                          ments used to uncover the protection mechanism; (2) Ob-
                                                                    servation, explaining how the mechanism operates; and (3)
     T RANSMISSION UNDER GPU-CC
                                                                    Security Insight, highlighting the security implications and
At runtime, data must be transmitted between the CVM’s              mitigations. Due to space constraints, the full analysis is
private memory and the GPU’s CPR over an untrusted PCIe             provided in Appendix §A. Below, we briefly summarize the
interface, which can be intercepted or manipulated by ad-           key security findings for each data path.
versaries on the host. This raises a key security question:
                                                                    1. CPU-GSP Remote Procedure Call (RPC). The NVIDIA
                                                                    kernel-mode driver communicates with the GSP-RM
                                                                    through a physical RMAPI RPC interface. Because this
 Does the data traversing this untrusted interface remain
                                                                    communication occurs via shared system memory, which is
 protected in terms of confidentiality and integrity?
                                                                    visible and malleable to a compromised host, the commands
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

                                Table 2: Classification of security implications by data paths

 Data Paths                         Engines       C    I     Security Findings
 CPU-GSP RPC                        GSP           •          Leakage and possible manipulations of RMAPI RPC invocations
 CPU-GSP Memory Transfers           GSP                ◦     Timing channels for leaking memory transfer sizes
 GPU Memory Faults                  GSP                ◦     Leakage of PUT pointer of shadow buffers
 UVM                                SEC2, CE      •          Unprotected GPFIFO, GPPUT, and semaphores in SEC2 Channel
 Memory Scrubbing                   SEC2          •          Unprotected semaphores in SEC2 Channel
 CUDA                               SEC2, CE      ?     ?    Closed-source CUDA runtime and user-mode driver


and responses must be encrypted. To assess the security of        a potential timing side channel that could leak information
this channel, we intercepted all RMAPI RPC transactions,          about transfer size or activity. We recommend implement-
decoded 1,588 command types using encodings extracted             ing constant-time RPC handling or adding statistical noise
from the NVIDIA Software Development Kit (SDK), and               for CPU-GSP memory transfers to obfuscate these timing
examined the shared staging buffer by dumping its physi-          patterns.
cal memory. Our analysis reveals that only the command
                                                                  3. GPU Memory Faults. Memory faults occur when a
and status payloads are encrypted, while most RPC meta-
                                                                  program accesses out-of-bound memory, dereferences null
data items in the staging buffers remain in plaintext and are
                                                                  pointers, or writes to freed memory. In GPU-CC mode,
exposed to potential attackers. This metadata leakage en-
                                                                  the hardware buffer for logging these faults resides inside
ables attackers to track RMAPI invocation status, violating
                                                                  the GPU’s CPR, making it inaccessible to the CPU. To
confidentiality guarantees. Although payload encryption
                                                                  examine the fault-handling path under GPU-CC, we inten-
with a session key protects RPC command integrity, attack-
                                                                  tionally triggered faults in a customized CUDA program
ers can still modify metadata fields such as readPtr and
                                                                  and instrumented both the NVIDIA kernel-mode and UVM
writePtr, altering the order, repetition, or omission of
                                                                  drivers to observe how fault packets are transferred and
RPC calls and thus may partially undermine execution in-
                                                                  protected. We found that the UVM driver allocates two
tegrity. We recommend encrypting both RPC payloads and
                                                                  shadow buffers in unprotected memory: one for replayable
metadata of command and status queues to provide stronger
                                                                  faults and one for non-replayable faults. The GSP encrypts
protection.
                                                                  fault packets using gsp cpu replayable fault or
2. CPU-GSP Memory Transfers. In the GPU-CC mode,                  gsp cpu non replayable fault keys and copies
BAR2 access to the GPU’s CPR is blocked, so the                   them into the appropriate shadow buffer, then signals the
NVIDIA kernel-mode driver uses CPU-GSP DMA to trans-              CPU via an RPC event so the UVM driver can verify and
fer memory. Because DMA cannot access CVM’s pri-                  decrypt the packet. Unlike non-GPU-CC mode, both fault
vate memory directly, transfers must pass through an un-          types now follow a unified workflow since the GSP con-
protected staging buffer in the shared memory. Mem-               trols the hardware buffers. The only observed leakage is
ory read operations are initiated when the driver issues          that the shadow buffer’s PUT pointer is exposed to the CPU
a MEMMGR MEMORY TRANSFER WITH GSP RPC request,                    via BAR0 and is updated whenever a new fault is logged.
which prompts the GSP to encrypt source data using the            However, since faults are infrequent, the risk of information
gsp cpu dma key. Following encryption, the data is trans-         leakage remains low.
ferred via DMA into a staging buffer for driver-side retrieval
                                                                  4. Unified Virtual Memory (UVM). UVM provides a unified
and decryption. Conversely, for write operations, the driver
                                                                  CPU-GPU address space. But in GPU-CC mode, where the
first encrypts the data using the cpu gsp dma key and
                                                                  GPU cannot access CVM’s private memory and BAR2 ac-
places it in the staging buffer. The driver then invokes the
                                                                  cess to the CPR is blocked, UVM must adopt a new secure
same RPC to signal the GSP, which performs a DMA copy
                                                                  design. GPU-CC introduces a two-phase, multi-channel
and decrypts the data into the CPR. In our analysis, we
                                                                  workflow involving SEC2 Channel, Work Launch Chan-
intercepted all CPU-GSP transfers across a CVM lifecycle
                                                                  nel (WLC), and Launch Confirmation Indicator Channel
(4,394 transfers in total: 453 reads and 3,941 writes), logged
                                                                  (LCIC). Pushbuffers and command queue structures are now
execution times for transfer sizes from 8 to 4,096 bytes, and
                                                                  relocated into the CPR. To examine this new pipeline, we
correlated latency with size to search for timing channels.
                                                                  instrumented the NVIDIA kernel-mode and UVM drivers
The timing distribution is bimodal: for small transfers (8 to
                                                                  and reconstructed the control flow. We observed that the
256 bytes), RPC overhead dominates and size has little ef-
                                                                  SEC2 Channel initializes 16 WLC/LCIC pairs by verifying
fect. By contrast, for large transfers (4,096 bytes), execution
                                                                  the signed static methods, with their schedules configured
times increase substantially, producing a size-dependent
                                                                  through SEC2 pushes. UVM CE pushes are then launched
timing signal observable via the RPC channel. This creates
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

indirectly via WLCs: their decrypt-then-run workflow trans-     with no visibility from the kernel space. Given that both the
fers encrypted pushbuffers into the CPR and decrypts them       CUDA runtime and user-mode driver are closed-source, we
for execution, while encrypted tracking semaphores in stag-     make the following reasoned speculation based on the data
ing memory allow the driver to monitor progress. Our secu-      that require special protection under GPU-CC:
rity analysis shows that SEC2’s command queue structures,
specifically GPPUT, GPFIFO, and tracking semaphores,
remain unprotected in shared memory, which could poten-
                                                                 With GPU-CC enabled, all CUDA-related data transmis-
tially allow attackers to redirect execution toward malicious
                                                                 sion must be protected when passing over the untrusted
pushbuffers. However, the SEC2’s method-signing mech-
                                                                 PCIe interface. (1) Large data transmission, such as user
anism may still prevent modified GPU methods from be-
                                                                 data and CUDA kernel code, must be encrypted with
ing executed. Meanwhile, all critical WLC/LCIC/CE data
                                                                 CE’s session keys, staged in shared buffers, and trans-
structures, including their mutable pushbuffers, command
                                                                 ferred via the CE into the GPU’s CPR for decryption.
queues, and tracking semaphores, remain encrypted in stag-
                                                                 Reverse transfers from GPU’s CPR to system memory
ing buffers or confined to the CPR.
                                                                 should be similarly protected. (2) Smaller but sensitive
5. Memory Scrubbing. Memory scrubbing ensures sensi-             structures, including CUDA kernel arguments and QMD
tive data in GPU memory is securely erased before GPU            launch configuration, can no longer be written in plain-
reuse. In GPU-CC mode, a dedicated scrubber channel              text over PCIe. Instead, they should be encrypted by the
associated with the SEC2 engine is introduced to prevent         CUDA user-mode driver and securely pulled by a GPU
tampering with scrub operations. To study this workflow, we      security engine (e.g., SEC2), which verifies integrity
intercepted SEC2 signing functions and backtracked scrub         and decrypts them within the CPR prior to execution.
triggers within the memory manager. We found that when           (3) Command queue structures (e.g., GPFIFO, GPPUT,
memory pages are freed, the driver creates a pushbuffer          pushbuffers, and tracking semaphores) must either reside
with a series of memset commands, signs them using the           in the CPR or remain encrypted in staging buffers, with
cpu sec2 hmac scrubber key, appends authentication               secure work submission enabled through authenticated
tags, and dispatches the signed pushbuffer to SEC2 engine        and encrypted channels managed by SEC2.
via the scrubber channel. The SEC2 engine validates the sig-
nature before executing the scrub task. Our security analysis
reveals two limitations: (1) scrub pushbuffers are signed but
not encrypted, exposing their GPU methods to attackers on
the host and (2) the tracking semaphores used by the SEC2       Key Takeaway. GPU-CC introduces stricter security re-
engine to signal task completion remain unprotected. These      quirements to protect data transmission. Yet much of the
gaps allow attackers to observe scrub activity or tamper        existing GPU software stack consists of legacy implemen-
with completion signals, suggesting the need for stronger       tations that do not meet these expectations and must be
protection in the scrubber channel.                             updated individually. While recent changes secure most
                                                                bulk command and data transfers, some sensitive metadata,
6. CUDA. CUDA is NVIDIA’s proprietary parallel comput-          timing behavior, and coordination signals remain exposed
ing platform, designed to offload compute-intensive portions    in the unprotected shared memory. These residual leaks
of an application from the sequential CPU to parallel GPU       can enable inference of computational behavior and in some
cores. GPU-CC must protect five categories of data involved     cases even manipulation of operations, creating a partial loss
in CUDA operations: (1) User data (e.g., datasets and model     of integrity. Extending protection to metadata and reducing
weights), (2) CUDA kernel code, (3) CUDA kernel argu-           observable timing differences would better strengthen the
ments, (4) Queue Metadata (QMD) launch configuration            end-to-end pipeline security.
structure, and (5) command queue structures used to trans-
fer data and orchestrate kernel execution. To understand how
GPU-CC secures CUDA’s data transfers, we instrumented           9   C ONCLUSION
the NVIDIA kernel-mode driver, the UVM driver, and the          The NVIDIA GPU-CC system aims to provide a secure
OpenSSL library to capture their interactions with CUDA         execution environment for privacy-critical parallel comput-
programs. Our experiments reveal that the CUDA user-            ing workloads. However, its proprietary ecosystem, lack of
mode driver retrieves two sets of keys via the GetKMB API:      public specifications, and design complexity hinder com-
cpu sec2 {data,hmac} user keys, which are bound                 prehensive security verification. This paper demystifies its
to the SEC2 engine, and lce{x} {h2d,d2h} user keys,             inner workings to establish a foundation for future security
associated with CEs. Analysis indicates that critical tasks,    research. We detail the underlying architectural mecha-
such as kernel execution and data movement, are primarily       nisms and present a series of in-depth experiments to assess
managed within the CUDA runtime and user-mode driver,           security risks and identify potential attack surfaces.
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

ACKNOWLEDGMENTS                                                Eichner, H., Ramage, D., Bonawitz, K., Huba, D., Santoro,
                                                                 T., McLarnon, B., Van Overveldt, T., Fallen, N., Kairouz,
We are grateful to the anonymous reviewers for their feed-       P., Cheu, A., et al. Confidential Federated Computations.
back and suggestions. The authors at The Ohio State Uni-         arXiv preprint arXiv:2404.10764, 2024.
versity were partially supported by NSF awards 2112471,
2207202, and 2348754.                                          Gast, S., Weissteiner, H., Schröder, R. L., and Gruss, D.
                                                                 CounterSEVeillance: Performance-counter attacks on
                                                                 AMD SEV-SNP. In Network and Distributed System
R EFERENCES
                                                                 Security (NDSS) Symposium 2025, 2025.
Aktas, E., Cohen, C., Eads, J., Forshaw, J., and Wilhelm, F.
                                                               Gu, Z., Jamjoom, H., Su, D., Huang, H., Zhang, J., Ma,
  Intel Trust Domain Extensions (TDX) Security Review.
                                                                T., Pendarakis, D., and Molloy, I. Reaching Data Con-
 Google security review, 2023.
                                                                 fidentiality and Model Accountability on the CalTrain.
AMD. AMD SEV-SNP: Strengthening VM Isolation with                In 2019 49th Annual IEEE/IFIP International Confer-
 Integrity Protection and More. AMD, 2020.                       ence on Dependable Systems and Networks (DSN), pp.
                                                                 336–348. IEEE, 2019.
AMD. AMD SEV-TIO: Trusted I/O for Secure Encrypted
                                                               Guo, J., Pietzuch, P., Paverd, A., and Vaswani, K. Trustwor-
 Virtualization. AMD, 2023.
                                                                 thy AI using Confidential Federated Learning: Federated
Cheng, P.-C., Eykholt, K., Gu, Z., Jamjoom, H., Jayaram, K.,     learning and confidential computing are not competing
  Valdez, E., and Verma, A. DeTA: Minimizing Data Leaks          technologies. Queue, 22(2):87–107, 2024a.
  in Federated Learning via Decentralized and Trustworthy      Guo, Y., Zhang, Z., and Yang, J. GPU Memory Exploitation
  Aggregation. In Proceedings of the Nineteenth European         for Fun and Profit. In 33rd USENIX Security Symposium
  Conference on Computer Systems, pp. 219–235, 2024a.           (USENIX Security 24), pp. 4033–4050, 2024b.
Cheng, P.-C., Ozga, W., Valdez, E., Ahmed, S., Gu, Z.,         Hu, X., Liang, L., Li, S., Deng, L., Zuo, P., Ji, Y., Xie,
  Jamjoom, H., Franke, H., and Bottomley, J. Intel TDX          X., Ding, Y., Liu, C., Sherwood, T., et al. DeepSniffer:
  Demystified: A Top-Down Approach. ACM Computing               A DNN Model Extraction Framework Based on Learn-
  Surveys, 56(9):1–33, 2024b.                                    ing Architectural Hints. In Proceedings of the Twenty-
                                                                Fifth International Conference on Architectural Support
Cohen, C., Forshaw, J., Horn, J., and Brand, M. AMD Se-          for Programming Languages and Operating Systems, pp.
  cure Processor for Confidential Computing Security Re-         385–399, 2020.
  view. Technical report, Google Project Zero and Google
  Cloud Security, 2022.                                        Intel.     Intel TDX Connect Architecture Specifi-
                                                                  cation.    https://cdrdv2.intel.com/v1/dl/
De Meulemeester, J., Wilke, L., Oswald, D., Eisenbarth, T.,       getContent/773614, 2023a.
  Verbauwhede, I., and Van Bulck, J. BadRAM: Practical
  memory aliasing attacks on trusted execution environ-        Intel.  Intel Trust Domain Extensions. https:
  ments. In 2025 IEEE Symposium on Security and Privacy          //cdrdv2.intel.com/v1/dl/getContent/
 (SP), pp. 4117–4135. IEEE, 2025.                                 690419, 2023b.
                                                               Ivanov, A., Rothenberger, B., Dethise, A., Canini, M., Hoe-
Delignat-Lavaud, A. and Rogers, P. Hopper Con-
                                                                 fler, T., and Perrig, A. SAGE: Software-based Attestation
  fidential Computing: How it Works under the
                                                                 for GPU Execution. In 2023 USENIX Annual Technical
  Hood.     https://www.nvidia.com/en-us/on-
                                                                 Conference (USENIX ATC 23), pp. 485–499, 2023.
  demand/session/gtcspring23-s51709/,
  2023.                                                        Jang, I., Tang, A., Kim, T., Sethumadhavan, S., and Huh,
                                                                 J. Heterogeneous Isolated Execution for Commodity
Deng, Y., Wang, C., Yu, S., Liu, S., Ning, Z., Leach, K.,        GPUs. In Proceedings of the Twenty-Fourth International
  Li, J., Yan, S., He, Z., Cao, J., et al. StrongBox: A GPU      Conference on Architectural Support for Programming
  TEE on Arm Endpoints. In Proceedings of the 2022 ACM           Languages and Operating Systems, pp. 455–468, 2019.
  SIGSAC Conference on Computer and Communications
  Security, pp. 769–783, 2022.                                 Jiang, J., Qi, J., Shen, T., Chen, X., Zhao, S., Wang, S.,
                                                                  Chen, L., Zhang, G., Luo, X., and Cui, H. CRONUS:
Dhanuskodi, G., Guha, S., Krishnan, V., Manjunatha, A.,           Fault-isolated, Secure and High-performance Heteroge-
  Nertney, R., O’Connor, M., and Rogers, P. Creating the          neous Computing for Trusted Execution Environment.
  First Confidential GPUs. Communications of the ACM,             In 2022 55th IEEE/ACM International Symposium on
  67(1):60–67, 2023.                                              Microarchitecture (MICRO), pp. 124–143. IEEE, 2022.
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

Kaplan, D. Protecting VM Register State with SEV-ES.               modules/blob/main/kernel-open/
 AMD, 2017.                                                        nvidia-uvm/uvm_gpu_non_replayable_
                                                                   faults.c#L38, 2025a.
Kaplan, D., Powell, J., and Woller, T. AMD Memory En-
  cryption. AMD, 2016.                                           NVIDIA.     Deployment Guide for SecureAI.
                                                                  https://docs.nvidia.com/cc-deployment-
Lee, J., Wang, Y., Rajat, R., and Annavaram, M. Charac-
                                                                  guide-tdx.pdf, 2025b.
  terization of GPU TEE Overheads in Distributed Data
  Parallel ML Training. arXiv preprint arXiv:2501.11771,         NVIDIA. nvTrust: Ancillary Software for NVIDIA Trusted
  2025.                                                           Computing Solutions.        https://github.com/
Lee, S., Kim, J., Na, S., Park, J., and Huh, J. TNPU: Support-    NVIDIA/nvtrust, 2025c.
  ing Trusted Execution with Tree-less Integrity Protection
                                                                 NVIDIA.    OCSP Service API Documentation.
  for Neural Processing Unit. In 2022 IEEE International
                                                                  https://docs.attestation.nvidia.com/
  Symposium on High-Performance Computer Architecture
                                                                  OCSP/ocsp_api.html, 2025d.
  (HPCA), pp. 229–243. IEEE, 2022.

Mai, H., Zhao, J., Zheng, H., Zhao, Y., Liu, Z., Gao, M.,        NVIDIA. NVIDIA Linux open GPU kernel module
 Wang, C., Cui, H., Feng, X., and Kozyrakis, C. Honey-            source. https://github.com/NVIDIA/open-
 comb: Secure and Efficient GPU Executions via Static             gpu-kernel-modules, 2025e.
 Validation. In 17th USENIX Symposium on Operating
                                                                 NVIDIA.    RIM Service API Documentation.
 Systems Design and Implementation (OSDI 23), pp. 155–
                                                                  https://docs.nvidia.com/attestation/
 172, 2023.
                                                                  api-docs-rim/latest/index.html, 2025f.
MICROCHIP. CEC1736 Real-Time Platform Root of
 Trust Controller. https://www.microchip.com/                    Quoc, D. L. and Fetzer, C. SecFL: Confidential Federated
 en-us/product/cec1736, 2025.                                      Learning using TEEs. arXiv preprint arXiv:2110.00981,
                                                                   2021.
Mo, F., Haddadi, H., Katevas, K., Marin, E., Perino, D.,
 and Kourtellis, N. PPFL: Enhancing Privacy in Feder-            Rauscher, F., Wilke, L., Weissteiner, H., Eisenbarth, T.,
 ated Learning with Confidential Computing. GetMobile:             and Gruss, D. TDXploit: Novel Techniques for Single-
 Mobile Computing and Communications, 25(4):35–38,                 Stepping and Cache Attacks on Intel TDX. In 34th
 2022.                                                             USENIX Security Symposium (USENIX Security 25), pp.
                                                                   1207–1222, 2025.
Mohan, A., Ye, M., Franke, H., Srivatsa, M., Liu, Z., and
 Gonzalez, N. M. Securing AI Inference in the Cloud:             Rauscher, F., Weissteiner, H., and Gruss, D. TELESCOPE:
 Is CPU-GPU Confidential Computing Ready? In 2024                  TDX Exploit Leaking Encrypted Data using Sibling Core
 IEEE 17th International Conference on Cloud Computing             Performance Counters. In ACM ASIA Conference on
 (CLOUD), pp. 164–175. IEEE, 2024.                                 Computer and Communications Security 2026, 2026.
Naghibijouybari, H., Koruyeh, E. M., and Abu-Ghazaleh, N.        Rogers, P., Overby, M., Venkataraman, V., Cherukuri, N.,
  Microarchitectural Attacks in Heterogeneous Systems: A           Deming, J. L., Dhanuskodi, G., Swoboda, D., Dunning,
  Survey. ACM Computing Surveys, 55(7):1–40, 2022.                 L., Manjunatha, A., Jiricek, A., et al. Confidential
Nertney, R. Remote Attestation for NVIDIA Hopper                   computing using multi-instancing of parallel processors,
  and Blackwell GPUs, CPUs, and Beyond. https:                     September 21 2023a. US Patent App. 18/123,222.
 //www.youtube.com/watch?v=2dZgwowP2_M,
                                                                 Rogers, P., Overby, M., Venkataraman, V., Cherukuri, N.,
  2025.
                                                                   Deming, J. L., Dhanuskodi, G., Swoboda, D., Dunning,
NVIDIA. Are the On-Die Root of Trust and SEC2                      L., Manjunatha, A., Jiricek, A., et al. Confidential com-
 security microcontroller physically the same thing?               puting using parallel processors with code and data pro-
 https://forums.developer.nvidia.com/                              tection, September 21 2023b. US Patent App. 18/185,654.
 t/are-the-on-die-root-of-trust-
 and-sec2-security-microcontroller-                              Rogers, P. J., Overby, M., Woodmansee, M. A., Venkatara-
 physically-the-same-thing/307330, 2024.                           man, V., Cherukuri, N., Dhanuskodi, G., Swoboda, D. F.,
                                                                   Dunning, L. B., Hairgrove, M., Guha, S., et al. Implement-
NVIDIA.    open-gpu-kernel-modules. https:                         ing trusted executing environments across multiple pro-
 //github.com/NVIDIA/open-gpu-kernel-                              cessor devices, February 4 2025. US Patent 12,219,057.
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

Schlüter, B. and Shinde, S. RMPocalypse: How a Catch-22        Wilke, L., Sieck, F., and Eisenbarth, T. TDXdown: Single-
  Breaks AMD SEV-SNP. In Proceedings of the 2025 ACM             Stepping and Instruction Counting Attacks against Intel
  SIGSAC Conference on Computer and Communications               TDX. In Proceedings of the 2024 on ACM SIGSAC Con-
  Security, pp. 3840–3854, 2025.                                 ference on Computer and Communications Security, pp.
                                                                 79–93, 2024.
Schlüter, B., Sridhara, S., Bertschi, A., and Shinde, S. We-
  See: Using Malicious #VC Interrupts to Break AMD              Zhan, Z., Zhang, Z., Liang, S., Yao, F., and Koutsoukos,
  SEV-SNP. In 2024 IEEE Symposium on Security and                 X. Graphics Peeping Unit: Exploiting EM Side-Channel
  Privacy (SP), pp. 4220–4238. IEEE, 2024a.                       Information of GPUs to Eavesdrop on Your Neighbors.
                                                                  In 2022 IEEE Symposium on Security and Privacy (SP),
Schlüter, B., Sridhara, S., Kuhne, M., Bertschi, A., and         pp. 1440–1457. IEEE, 2022.
  Shinde, S. HECKLER: Breaking Confidential VMs with
  Malicious Interrupts. In 33rd USENIX Security Sympo-          Zhang, R., Gerlach, L., Weber, D., Hetterich, L., Lü, Y.,
  sium (USENIX Security 24), pp. 3459–3476, 2024b.                Kogler, A., and Schwarz, M. CacheWarp: Software-
                                                                  based Fault Injection using Selective State Reset. In 33rd
Schlüter, B., Wech, C., and Shinde, S. Heracles: Chosen          USENIX Security Symposium (USENIX Security 24), pp.
  Plaintext Attack on AMD SEV-SNP. In Proceedings of              1135–1151, 2024a.
  the 2025 ACM SIGSAC Conference on Computer and
  Communications Security, pp. 3810–3824, 2025.                 Zhang, Z., Allen, T., Yao, F., Gao, X., and Ge, R. TunneLs
                                                                  for Bootlegging: Fully Reverse-Engineering GPU TLBs
Tan, Y. and Mi, Z. Performance Analysis and Optimization          for Challenging Isolation Guarantees of NVIDIA MIG.
  of Nvidia H100 Confidential Computing for AI Work-              In Proceedings of the 2023 ACM SIGSAC Conference on
  loads. In 2024 IEEE International Symposium on Parallel         Computer and Communications Security, pp. 960–974,
  and Distributed Processing with Applications (ISPA), pp.        2023.
  1426–1432. IEEE, 2024.
                                                                Zhang, Z., Cai, K., Guo, Y., Yao, F., and Gao, X. Invali-
Tan, Y., Tan, C., Mi, Z., and Chen, H. PipeLLM: Fast              date+Compare: A Timer-Free GPU Cache Attack Prim-
  and Confidential Large Language Model Services with             itive. In 33rd USENIX Security Symposium (USENIX
  Speculative Pipelined Encryption. In Proceedings of the         Security 24), pp. 2101–2118, 2024b.
  30th ACM International Conference on Architectural Sup-
  port for Programming Languages and Operating Systems,         Zhao, S., Gu, Z., Ahmed, S., Valdez, E., Jamjoom, H., and
  Volume 1, pp. 843–857, 2025.                                    Lin, Z. GPU Travelling: Efficient Confidential Collabo-
                                                                  rative Training with TEE-Enabled GPUs. In Proceedings
Vaswani, K., Volos, S., Fournet, C., Diaz, A. N., Gordon,         of the 2025 on ACM SIGSAC Conference on Computer
  K., Vembu, B., Webster, S., Chisnall, D., Kulkarni, S.,         and Communications Security, 2025.
  Cunningham, G., et al. Confidential Computing within
  an AI Accelerator. In 2023 USENIX Annual Technical            Zhu, Y., Cheng, Y., Zhou, H., and Lu, Y. Hermes Attack:
  Conference (USENIX ATC 23), pp. 501–518, 2023.                  Steal DNN Models with Lossless Inference Accuracy. In
                                                                  30th USENIX Security Symposium (USENIX Security 21),
Volos, S., Vaswani, K., and Bruno, R. Graviton: Trusted           2021.
  Execution Environments on GPUs. In 13th USENIX Sym-
  posium on Operating Systems Design and Implementation
  (OSDI 18), pp. 681–696, 2018.                                 A     S ECURITY A NALYSIS OF DATA PATHS
Wang, C., Zhang, F., Deng, Y., Leach, K., Cao, J., Ning, Z.,    A.1   CPU-GSP RPC
 Yan, S., and He, Z. CAGE: Complementing Arm CCA
 with GPU Extensions. In Network and Distributed System         Methodology. The NVIDIA kernel-mode driver communi-
 Security (NDSS) Symposium, volume 2024, 2024a.                 cates with the GSP Resource Manager (GSP-RM) via the
                                                                physical Resource Manager API (RMAPI) Remote Proce-
Wang, Q. and Oswald, D. Confidential Computing on Het-          dure Call (RPC) interface over a bi-directional channel. The
 erogeneous CPU-GPU Systems: Survey and Future Di-              driver sends commands to the GPU System Processor (GSP)
 rections. ACM Computing Surveys, 2026.                         for offloading specific tasks, while the GSP responds with
                                                                execution status and GPU events to notify the driver. Since
Wang, Y., Rajat, R., Lee, J., Tang, T., and Annavaram, M.       the RPC occurs over an untrusted medium that attackers
 Fastrack: Fast IO for Secure ML using GPU TEEs. arXiv          on the host can examine and interpose, all data transmitted
 preprint arXiv:2410.15240, 2024b.                              through the RPC channels must be encrypted.
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

We intercepted all physical RMAPI invocations from the
NVIDIA kernel-mode driver and captured their correspond-
ing responses from the GSP. Each command type follows
a specific encoding, automatically generated by NVIDIA’s
FINN tool. To better understand the semantics of each RPC
invocation, we extracted the encodings of 1,588 RMAPI
commands from NVIDIA’s Software Development Kit
(SDK) and replaced them in our captured log.

Observation. The RPC infrastructure is initialized during
the GSP engine construction phase. A shared memory re-
gion is allocated in unprotected system memory for commu-
nication, consisting of a physical address table, a command
queue, and a status queue. The physical address table,
which holds the physical addresses of all pages in the shared
region, informs the GPU of the physical memory layout.
The command queue stores commands and their parameters
sent from the driver to the GSP, while the status queue con-
tains status information returned from the GSP to the driver.   Figure 5: Information leaks in RMAPI RPC: By traversing
The locations of these structures are communicated to the       the memory addresses within the physical address table,
GSP during initialization.                                      which is allocated in unprotected system memory, adver-
                                                                saries can identify the plaintext metadata of queue and ele-
To send a command, the driver first sets up the com-            ment headers.
mand header with a sequence number and element count.
The header also includes an Additional Authenticated
Data (AAD) buffer, an authentication tag, and a check-
sum, but it remains in plaintext. The driver then en-           physical address, followed by one entry for the command
crypts the command payload in private memory using the          queue’s header, 63 entries for command queue elements,
cpu gsp locked rpc key and computes a checksum                  one entry for the status queue’s header, and 63 entries for
over both the header and the encrypted payload, recording       status queue elements. If adversaries gain access to the
the value in the header. The header and encrypted payload       physical address table, they can easily locate all elements in
are then copied into the command queue in the staging buffer.   the command/status queues. For example, the second entry
The GSP retrieves the command, verifies the checksum and        (0x000000016921f000) points to the command/status
the integrity of the encrypted payload, decrypts it using the   queue’s header, where the readPtr and writePtr fields
same key, and executes the command.                             indicate the next elements to read and write. These fields
                                                                can then be used as indices in the address table to locate
To send a status update or notify the driver of an event, the
                                                                active elements in queues.
GSP encrypts the status using the gsp cpu locked rpc
key and copies the status header along with the encrypted       For instance, the third entry (0x0000000169297000)
payload into the status queue in the staging buffer. The        points to a specific queue element. Each element con-
driver retrieves the data from the status queue, copies it to   tains a header with fields such as authTagBuffer,
private memory, and verifies the checksum and the integrity     aadBuffer, checkSum, seqNum, and elemCount,
of the encrypted payload. Then it decrypts the status payload   followed by the encrypted RPC payload. The plaintext
and processes it accordingly.                                   header can also reveal significant information about the cur-
                                                                rently active element. For example, if the elemCount has
Security Insight. It is important to note that only the com-    a value greater than one, it can be used to infer the RPC
mand and status RPC payloads are encrypted in the un-           type with high accuracy.
protected staging buffers, while the physical address table,
queue headers, and queue element headers remain in plain-       A key challenge is how to locate the physical address table
text and are exposed to potential attackers on the host. Fig-   within the large physical memory space of a confidential
ure 5 illustrates these data structures by dumping memory       virtual machine (CVM). The vulnerability arises because
from their corresponding physical addresses in the Kernel-      the first entry in the address table stores its own physical ad-
based Virtual Machine (KVM) hypervisor.                         dress (e.g., 0x000000016921e000 in Figure 5). Given
                                                                that the address table is page-aligned, adversaries can per-
The physical address table consists of 129 entries (Figure 5    form a memory scan in 4096-byte strides (the typical page
shows the first four). The first entry stores the table’s own   size) across the entire physical memory. If a 64-bit physical
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing




                                                    (a) Read data from GSP




                                                     (b) Write data to GSP

                                 Figure 6: Timing channels in CPU-GSP memory transfers


address found during the scan equals the value stored at         A.2    CPU-GSP Memory Transfers
that location, the attacker can uniquely identify the starting
address of the physical address table, potentially exposing      Methodology. Once we enable the GPU Confidential Com-
critical memory mappings.                                        puting (GPU-CC) mode, the BAR2 access to the GPU mem-
                                                                 ory is blocked. Reads and writes to data within the Compute
Leakage of RPC metadata enables adversaries to monitor
                                                                 Protected Region (CPR) should use alternative routes. For
RMAPI invocation status, thereby may weaken confiden-
                                                                 the memory transfers between CVM and GSP, the driver
tiality guarantees. While the RPC payload is protected by
                                                                 uses the CPU-GSP Direct Memory Access (DMA). As
the session key, which prevents direct compromise of its
                                                                 DMA cannot access the private memory in the CVM di-
integrity, attackers can still manipulate the readPtr and
                                                                 rectly, the driver needs to allocate and map a staging buffer
writePtr fields in the metadata to alter the active ele-
                                                                 in the unprotected memory, which is not protected by the
ments. This can change the order, repetition, or omission of
                                                                 CVM’s key.
RPC calls, thus may partially undermine execution integrity.
To mitigate both leakage and manipulation, we recommend          In our experiment, we intercepted all memory transfers
encrypting not only the RPC payload but also the metadata        between the CVM and GSP and recorded their execution
of the command and status queues.                                time throughout the lifecycle of a CVM. Our goal is to
                                                                 investigate how the source or destination aperture and the
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

size of memory transfers impact execution time and whether         variations, we recommend implementing constant-time exe-
the potential presence of timing channels can be exploited.        cution for RPC invocations or introducing statistical noise
                                                                   to obfuscate timing patterns.
Observation. To read data from the GPU’s CPR, the driver
sends a MEMMGR MEMORY TRANSFER WITH GSP RPC                        A.3   GPU Memory Faults
request with parameters specifying the memory size, the
source in the GPU’s CPR, and the destination in a staging          Methodology. In GPU-CC mode, handling GPU memory
buffer located in unprotected system memory. The pro-              faults presents unique challenges due to the stricter memory
tection for transmitting this RPC request itself has been          access control. Typically, memory faults occur when a pro-
discussed in §A.1. Upon receiving the request, the GSP en-         gram attempts to access out-of-bound memory, dereference
crypts the source data in the CPR using the gsp cpu dma            null pointers, or write to freed memory. In the non-GPU-CC
key and copies it via DMA into the unprotected staging             mode, both the CPU-side drivers and GPU have access to
buffer. The driver then transfers the data to private memory       a shared hardware buffer, allowing the GPU to log fault
and decrypts it using the same key.                                packets directly into the buffer, which the driver can then
To write data to the GPU’s CPR, the driver first encrypts the      retrieve and handle. However, in GPU-CC mode, this hard-
data in the CVM’s private memory using the cpu gsp dma             ware buffer is placed within the GPU’s CPR, rendering it
key and copies it to the unprotected staging buffer. The           inaccessible to the CPU. As a result, a new mechanism is re-
driver then sends the memory transfer request via the same         quired to securely transfer fault packets through an untrusted
RPC, specifying the memory size, the source in the staging         interface.
buffer, and the destination in the CPR. Upon receiving             To analyze this memory fault workflow in GPU-CC mode,
the command, the GSP copies the data via DMA from the              we developed a CUDA program designed to deliberately
unprotected staging buffer, decrypts it using the same key,        trigger memory faults. By instrumenting the NVIDIA
and writes it to the target address in the CPR.                    kernel-mode driver and Unified Virtual Memory (UVM)
                                                                   driver, we intercepted RPC invocations used for setting up
Security Insight. We recorded the execution time for 4,394         the staging buffers to deliver the fault packets and monitored
memory transfers between the CPU and GSP throughout an             the use of cryptographic keys related to memory faults.
entire CVM lifecycle, consisting of 453 read operations and
3,941 write operations. Our goal is to analyze whether the         Observation. To meet the memory access control require-
transfer size impacts execution time.                              ments in GPU-CC, the UVM driver requests the NVIDIA
In Figure 6, we first illustrate the relationship between mem-     kernel-mode driver to allocate two shadow buffers, one for
ory transfer sizes and execution time for both read and write      replayable memory faults and another for non-replayable
operations in the two plots on the left. We can observe that       memory faults. These shadow buffers reside in unprotected
large memory transfers, specifically those of 4,096 bytes,         staging buffers, enabling the GSP to write directly to them.
demonstrate a distinct shift in execution time compared to         The NVIDIA kernel-mode driver registers these shadow
smaller transfers ranging from 8 to 256 bytes.                     buffers with the GSP through the RMAPI RPC, informing
                                                                   the GSP of their structures. The GSP then encrypts the
To visualize this statistical shift more effectively, we repre-    fault packets using the gsp cpu replayable fault
sent the data as histograms in the right-side plots of Figure 6.   or gsp cpu non replayable fault key before copy-
In these histograms, the horizontal axis denotes execution         ing them into the shadow buffers. Once the fault packets are
time while the vertical axis indicates the count of memory         stored, the GSP sends a MMU FAULT QUEUED event via
transfers within specific intervals. Distinct colors are used      the RPC return path, notifying the driver. The UVM driver
to identify the varying transfer sizes. The histograms reveal      monitors this event and schedules an interrupt service rou-
that both read and write operations exhibit a bimodal distri-      tine to retrieve the encrypted fault packets from the shadow
bution of execution times. For small transfer sizes (8 to 256      buffers. Each fault packet includes an authentication tag
bytes), the execution time is largely dominated by the inher-      and a valid field (used as AAD). The UVM driver uses the
ent cost of the RPC itself, making the effect of memory size       corresponding key to verify and decrypt incoming packets,
relatively insignificant. However, for larger transfers (4,096     then parses them to handle memory faults effectively while
bytes, represented by blue bars), a noticeable shift occurs,       preserving data confidentiality and integrity.
with execution times increasing significantly compared to
smaller transfers.                                                 Security Insight. According to the comment (NVIDIA,
                                                                   2025a) in the UVM driver’s source code, replayable faults
To mitigate potential timing side-channel attacks, where
                                                                   originate from the Streaming Multiprocessors (SMs), while
adversaries monitoring the RPC channel in untrusted mem-
                                                                   non-replayable faults come from other engines, e.g., Copy
ory could infer sensitive information from execution time
                                                                   Engines (CEs). In non-GPU-CC mode, these two types
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

of faults follow different handling paths. The UVM driver      longer be directly accessed or modified by either the CPU
exclusively manages replayable faults and owns the hard-       or GPU, as shared memory is untrusted. To mitigate the
ware buffer that stores them. In contrast, handling non-       risk of compromise or leakage, these components require
replayable faults requires interaction with the NVIDIA         additional security measures while residing in unprotected
kernel-mode driver via a specific shadow buffer since only     staging buffer.
the kernel-mode driver can access the hardware buffer for
                                                               The implementation of UVM in GPU-CC is significantly
non-replayable faults.
                                                               more complex, involving multiple phases and recursive inter-
However, in GPU-CC mode, where the GSP owns the hard-          actions. To better understand its execution, we instrumented
ware buffer and the CPU lacks direct access, both replayable   both the NVIDIA kernel-mode driver and the UVM driver,
and non-replayable faults rely on shadow buffers to store      restructuring the control flow into a clear temporal sequence.
encrypted fault packets. Therefore, their handling paths be-   Our primary focus is analyzing the interactions between
come similar in GPU-CC mode. Additionally, the GSP-RM          different engines, such as SEC2 and CEs, and examining
needs to update the shadow buffer’s PUT pointer whenever       how their derived keys protect specific target components
a new fault packet is placed. NVIDIA repurposes the access     within this new memory management model.
counter registers mapped in BAR0 to expose this pointer
to the CPU. This means that adversaries can also observe       Observation. Three specialized channels have been intro-
this value on the host. Given that memory faults occur in-     duced in UVM for GPU-CC: the SEC2 Channel, the Work
frequently, the risk of exposing the PUT pointer remains       Launch Channel (WLC), and the Launch Confirmation In-
low.                                                           dicator Channel (LCIC).
                                                               1. SEC2 Channel: The SEC2 Channel is the first to be
A.4   Unified Virtual Memory (UVM)                             created, serving as the foundation for bootstrapping se-
                                                               cure workload submission. Its primary role is to verify
Methodology. NVIDIA’s UVM system provides a unified            and set up the WLC and LCIC. There is only one SEC2
address space across system memory and GPU memory,             Channel, which is associated with the SEC2 engine and
enabling seamless memory access by managing memory             uses two cryptographic keys: cpu sec2 hmac kernel
migrations, page faults, and coherence automatically.          and cpu sec2 data kernel, which are used for sign-
In the non-GPU-CC mode, the UVM driver communicates            ing methods and encrypting payloads respectively.
with CEs through UVM channels to conduct memory oper-          2. Work Launch Channel (WLC): WLCs are responsi-
ations such as page copies or Translation Lookaside Buffer     ble for indirectly launching UVM workloads. They pro-
(TLB) invalidations. Each UVM channel owns specific re-        cess encrypted UVM CE pushes submitted by the UVM
gions of memory called pushbuffers, which are allocated        driver, verify their integrity, and dispatch them to selected
in system memory and mapped for both CPU and GPU               CEs for execution. The SEC2 Channel initializes 16
access. The driver writes sequences of GPU methods into        WLCs, corresponding to the maximum number of concur-
these pushbuffers to prepare workloads for execution. A        rent pushes. Each WLC is associated with a logical CE
GPFIFO operates as a circular ring buffer that contains        with two keys: lce{x} h2d kernel for host-to-device
pointers to these pushbuffers. When the UVM driver sub-        encryption and lce{x} d2h kernel for device-to-host
mits work, it populates a pushbuffer with methods and adds     encryption, where {x} denotes the index of the associated
a pointer to the GPFIFO. The driver then increments the        logical CE.
GPPUT pointer and triggers the doorbell to notify the CE
of the pending task. Subsequently, the CE fetches and ex-      3. Launch Confirmation Indicator Channel (LCIC): Similar
ecutes the methods from the GPFIFO before updating the         to WLCs, LCICs are also initialized by the SEC2 Channel.
GPGET pointer to reflect the current status. To synchronize    Each LCIC is paired with a corresponding WLC, sharing the
these asynchronous operations, each UVM channel has a          same logical CE association, resulting in a total of 16 LCICs.
tracking semaphore. The last method in every pushbuffer is     The LCIC is designed to track the execution progress of its
a command to increment this semaphore, which the UVM           paired WLC.
driver polls to confirm the successful completion of all GPU   The workflow can be divided into two phases:
methods.
                                                               Phase I: Setup of WLC/LCIC via SEC2 Channel. The
In the GPU-CC mode, the GPU cannot access the private          SEC2 Channel acts as the trust anchor for bootstrapping
memory of the CVM and BAR2 access to the GPU’s CPR             WLCs and LCICs. SEC2’s GPPUT pointer, GPFIFO ring
is also disabled. Consequently, key components essential       buffer, and pushbuffers are allocated in an unprotected
for UVM operations, such as the pushbuffers, GPFIFO ring       staging buffer and remain unencrypted. However, the
buffers, GPPUT pointers, and tracking semaphores, can no       methods in the SEC2 pushbuffers are signed with the
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing




Figure 7: The interactions among Secure Processor (SEC2) Channel, Work Launch Channel (WLC), and Launch Confirma-
tion Indicator Channel (LCIC)


cpu sec2 hmac kernel key. Thus, the SEC2 engine                 Phase II: Indirect Launch of UVM CE Pushes via WLC.
can verify the integrity of the methods to detect any tamper-   Once the WLCs/LCICs are fully initialized, they can be
ing.                                                            selected at runtime to indirectly launch a UVM CE push. A
                                                                UVM CE push’s pushbuffer contains methods for executing
With GPU-CC enabled, the GPPUT, GPFIFO, and push-
                                                                a specific UVM task, e.g., writing page table entries for an
buffers of WLCs/LCICs must be placed in GPU’s CPR to
                                                                address range.
protect them from unauthorized access. The UVM driver
populates an SEC2 push to set up these data structures for      To execute a UVM CE push, the UVM driver selects a
WLCs/LCICs. This process repeats for all 16 WLCs and            WLC and encrypts the scheduled UVM CE push’s push-
LCICs. At this stage, the WLCs/LCICs are launched but not       buffer using the lce{x} h2d kernel key of that WLC.
yet operational. The next step is to set up their schedules.    The encrypted pushbuffer is placed in the unprotected stag-
                                                                ing buffer. A corresponding run push for the WLC is
The WLC’s schedule consists of two alternating pushes:
                                                                generated based on the parameters of the UVM CE push,
(1) decrypt push, responsible for decrypting the next
                                                                encrypted, and also stored in the staging buffer. The
run push and placing it into CPR. The decrypt push
                                                                decrypt push, pre-loaded during Phase I, executes first
is static and remains unchanged. (2) run push, executing
                                                                to decrypt the run push into CPR. Once decrypted, the
GPU methods to launch a UVM CE pushbuffer that con-
                                                                run push begins execution by decrypting the UVM CE
tains the payloads for the UVM operations, and advances
                                                                push’s pushbuffer, setting up the GPFIFO and GPPUT
the LCIC’s GPPUT by one step for synchronization. The
                                                                within CPR, and triggering it to run on another logical CE.
run push is mutable, as its parameters must be dynami-
                                                                The UVM CE push uses the lce{y} h2d kernel and
cally updated to accommodate different UVM CE pushes.
                                                                lce{y} d2h kernel keys tied to the newly attached log-
The LCIC schedule is entirely static and simply advances        ical CE, where {y} denotes the engine index. Finally, the
the WLC’s GPPUT by two steps ahead, ensuring that the           run push signals the LCIC to advance the WLC’s GPPUT,
WLC always starts from the decrypt push in the next             preparing it for the next execution cycle.
cycle.
                                                                Throughout this process, WLC, LCIC, and UVM CE up-
Finally, the static parts (marked blue in Figure 7) of the      date their respective tracking semaphores, which are then
WLC/LCIC schedules are uploaded and their GPFIFO en-            encrypted and stored in the unprotected staging buffer.
tries are updated by the SEC2 push. Once this is complete,      The UVM driver continuously monitors these encrypted
the WLCs/LCICs are fully operational and ready for accept-      semaphores to track execution progress. The decryp-
ing UVM CE pushes.                                              tion of tracking semaphores is handled by different keys:
                                                                lce{x} d2h kernel for the WLC/LCIC pushes and
                                                                lce{y} d2h kernel for the UVM CE push.
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

Security Insight. The UVM system relies on the signed             deeper understanding into the mechanism underlying GPU’s
push of the SEC2 Channel to bootstrap WLCs/LCICs. The             memory scrubbing.
SEC2 push is not encrypted since it only consists of pre-
defined methods without embedded secrets. However, the            Observation. The NVIDIA kernel-mode driver establishes
GPFIFO, GPPUT, and tracking semaphores of the SEC2                a scrubber channel linked to the SEC2 engine to handle
Channel reside in an unprotected staging buffer and are           memory scrubbing tasks securely. When the GPU’s memory
neither signed nor encrypted. Adversaries might exploit           manager needs to free certain memory pages, it populates a
this by manipulating the GPFIFO and GPPUT to redirect             pushbuffer with a sequence of methods for a secure memset
the execution to a crafted pushbuffer. However, since the         operation using SEC2. These methods are then submitted
methods themselves are signed, the SEC2 engine can still          to the scrubber channel.
detect and prevent tampered methods from being executed.          To securely free large GPU memory allocations, the mem-
Therefore, we consider such an attack to compromise confi-        ory manager chops the massive request into smaller chunks,
dentiality by exposing the fields in the SEC2 Channel and to      generating cryptographically signed methods for each. To
partially undermine execution integrity. To mitigate this risk,   ensure integrity, the methods in the pushbuffers are signed
we recommend encrypting these fields in SEC2 Channel as           using the cpu sec2 hmac scrubber key, and their
well.                                                             HMAC digests are stored in the authentication tag buffers.
The critical data structures for WLC, LCIC, and CE                The SEC2 engine verifies the signature and zeros out the
command queues, such as GPFIFO and GPPUT, are se-                 physical memory, firing an internal semaphore after every
curely placed in CPR. The pushbuffers, including WLC’s            single chunk to notify the driver that it can safely recycle
run push and UVM CE pushes, are encrypted while in                that specific authentication tag’s memory slot for the next
the unprotected staging buffer before being transferred into      command. On the very last chunk of the loop, a completion
CPR. Unlike the SEC2 push, these buffers are mutable and          semaphore is appended, forcing the hardware to flush all
contain sensitive information, including memory addresses         physical caches and notify that the entire block has been
and UVM operations. Therefore, they require encryption            destroyed.
when transmitted through an untrusted interface. Addition-
ally, the semaphores used for signaling the UVM driver are        Security Insight. The earlier design of the NVIDIA kernel-
also encrypted while in unprotected memory before being           mode driver reused the cpu sec2 hmac kernel
passed to CVM’s private memory, ensuring confidentiality          key for signing methods during memory scrub-
and integrity for retrieving the execution status of command      bing operations. While in the latest driver versions,
queues.                                                           NVIDIA introduced two dedicated scrubber keys, i.e.,
                                                                  cpu sec2 {hmac,data} scrubber, for handling
A.5   Memory Scrubbing                                            memory scrubbing tasks. It is worth noting that the
                                                                  cpu sec2 hmac scrubber key is now used for signing
Methodology. Memory scrubbing is used for deliberate              methods, while the cpu sec2 data scrubber key
overwriting of memory contents to clear out sensitive infor-      exists but remains unused. We make the following reasoned
mation. This prevents the recovery of confidential data,          speculation about the key usage:
which is particularly important in secure environments             Given that the pushbuffers and the authentication tag
where old data could otherwise be exploited by unautho-            buffers in the scrubber channel are currently allocated
rized users. Typically, the NVIDIA kernel-mode driver              in the staging buffers, which are not encrypted, the
establishes a scrubber channel with the CE, where it sub-          cpu sec2 data scrubber key may eventually be
mits GPU methods for memory scrubbing, allowing the CE             used to protect these data structures in the future.
to execute the task. However, when GPU-CC is enabled, a
dedicated and secure channel is required to submit mem-
ory scrubbing methods to prevent tampering through the            Additionally, the SEC2 engine only supports decryption and
untrusted interface. Instead of using CE, SEC2 is used for        does not provide encryption capabilities. Notably, while
memory scrubbing when GPU-CC is enabled, as it supports           there are cpu sec2 * keys for securing data sent to SEC2,
signed push.                                                      there are no corresponding sec2 cpu * keys for protecting
                                                                  data from SEC2 back to the driver. This means that the
In our experiment, we intercepted the signing function invo-      semaphores used to notify the driver remain unprotected.
cations in the SEC2 utility responsible for signing methods       The rationale behind SEC2’s lack of encryption support is
in the scrubber channel. We then backtracked the call trace       unclear and needs further clarification from NVIDIA. This
to identify the origin of memory scrubbing requests within        could present a security risk, as attackers on the host could
the GPU’s memory manager. This approach provided a                observe the unencrypted pushbuffers and authentication tag
                                                                  buffers (though they cannot tamper with commands due
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

to integrity verification) and manipulate the unprotected       movement by invoking the GPU’s CE to transfer data over
semaphores.                                                     the PCIe bus using DMA.
                                                                (2) CUDA Kernel Code. This consists of the compiled
A.6   CUDA                                                      Streaming Assembler (SASS) instructions executed by the
                                                                GPU’s SMs. During CUDA context initialization, the
Methodology. Since the CUDA user-mode driver is not
                                                                CUDA user-mode driver uses the CE to copy the CUDA
open-source, its execution flow in GPU-CC remains opaque.
                                                                kernel code into GPU’s device memory. At kernel launch,
To analyze its behavior, we intercepted its interactions with
                                                                the memory address of the kernel code is embedded in the
external components that we could instrument. Our ap-
                                                                Queue Metadata (QMD), enabling the hardware to locate
proach involved developing a sample CUDA application that
                                                                and fetch the instructions.
performed operations such as GPU memory allocation, host-
to-device data transfers, and CUDA kernel launches. How-        (3) CUDA Kernel Arguments. Kernel arguments are the
ever, instead of using the CUDA runtime APIs, we directly       parameters passed from the CPU to a CUDA kernel func-
invoked the low-level CUDA user-mode driver APIs to gain        tion. They include scalar values, dynamic dimensions, and
finer control over execution. For instance, we compiled the     pointers to user data. At launch time, these arguments are
CUDA kernels into Parallel Thread Execution (PTX) code          packed and copied into shared memory accessible by both
and launched them via cuLaunchKernel.                           the CPU and GPU. They are then transferred to Constant
                                                                Bank 0, which is backed by a dedicated constant cache in
To further dissect communication flows, we instrumented
                                                                the SM.
the NVIDIA kernel-mode driver and UVM driver, capturing
their interactions with the CUDA user-mode driver. Addi-        (4) Queue Metadata (QMD). The QMD is a bit-packed
tionally, drawing insights from PipeLLM (Tan et al., 2025),     launch configuration structure that encodes grid and block
which identified OpenSSL APIs as being used for encryp-         dimensions, register and memory requirements, pointers to
tion, decryption, and MAC verification by CUDA, we also         kernel code and arguments, and hardware state controls. It is
instrumented relevant OpenSSL functions. By preloading a        copied into shared memory and read by the GPU’s command
customized version of the OpenSSL library, we intercepted       processor to configure the SMs for kernel launching.
all cryptographic events invoked by the CUDA user-mode
                                                                (5) Command Queue Structures. The CUDA user-mode
driver, allowing us to observe how security mechanisms are
                                                                driver prepares GPU methods in pushbuffers and links them
enforced.
                                                                into corresponding GPFIFO command queues. These in-
Observation. By intercepting the corresponding OpenSSL          clude queues targeting the CEs for transferring user data and
functions, we are able to observe the key strings used by       kernel code, as well as queues targeting GPU’s command
the CUDA user-mode driver and map them to the keys de-          processor to initiate kernel execution.
rived from the master secret negotiated during the Security
                                                                Security Insight. The CUDA user-mode driver has limited
Protocol and Data Model (SPDM) session. Our analysis
                                                                interactions with the NVIDIA kernel-mode driver, primarily
reveals that the CUDA user-mode driver retrieves two cate-
                                                                occurring during: CUDA API/context initialization, kernel
gories of keys from the NVIDIA kernel-mode driver through
                                                                channel creation and key retrieval, and memory cleanup.
the GetKMB API: (1) cpu sec2 {data,hmac} user
                                                                However, the most critical operations related to data trans-
keys, associated with the SEC2 engine and likely used
                                                                mission and CUDA kernel launch remain self-contained
for encrypted data transfer and integrity verification. (2)
                                                                within the proprietary CUDA runtime and user-mode driver,
lce{x} {h2d,d2h} user keys, tied to specific logical
                                                                making them less observable. Given the limited informa-
CEs and facilitate encrypted data transfers between CPU
                                                                tion available, we make the following reasoned speculation
and GPU’s CEs. It is worth noting that, since CUDA oper-
                                                                regarding the types of data that should be protected during
ates in user space, all these keys are post-fixed with user,
                                                                CUDA execution:
indicating their privilege level.
Based on our understanding of CUDA’s execution lifecycle         User Data and CUDA Kernel Code. User data and
and CPU-GPU data movement patterns, we consider that             CUDA kernel code are typically transferred via the
the following five categories of data require specialized        GPU’s CE using DMA due to their large size. With
handling under GPU-CC:                                           GPU-CC enabled, however, the CE cannot directly ac-
                                                                 cess private memory within a CVM. Instead, both user
(1) User Data. This refers to the actual data, e.g., training    data and kernel code must first be encrypted using a ses-
datasets and model weights, on which the CUDA kernel             sion key (e.g., the lce{x} h2d user key) and placed
operates. The cudaMemcpy* runtime APIs manage data               in a staging buffer. The CE may then transfer the en-
                Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

 crypted data via DMA into the GPU’s CPR, where it is          paper (Dhanuskodi et al., 2023) discusses Multi-GPU sup-
 decrypted. Conversely, data transferred from the CPR          port, the initial software releases only allowed a confidential
 back to system memory must be encrypted with the ses-         GPU to be passed through to a single CVM. Recent software
 sion key (e.g., lce{x} d2h user key) before being             releases have added support for Multi-GPU passthrough un-
 written to the staging buffer.                                der the Protected PCIe mode, which requires updates to
                                                               the VBIOS, NVIDIA GPU driver, and CUDA1 . According
 CUDA Kernel Arguments and QMD. Under GPU-CC,                  to NVIDIA’s OC3 presentation (Nertney, 2025), Protected
 the conventional approach, where the CPU directly writes      PCIe mode only supports encrypted data transfers over the
 plaintext kernel arguments and QMD to the GPU via             PCIe interface. Inter-GPU data transfers over NVLinks and
 PCIe Base Address Register (BAR), is disallowed to            NVSwitches are not encrypted due to performance consid-
 defend against an untrusted hypervisor and PCIe bus. In-      erations. NVLink encryption is expected to be introduced
 stead, a pull-based model can be adopted in which the         in the Blackwell architecture.
 GPU retrieves cryptographically protected data. Specifi-
 cally, the CUDA user-mode driver may encrypt the kernel       Supporting GPU-CC in a Multi-GPU setting introduces new
 arguments and QMD using a pre-established session key         requirements for securely transferring data across GPUs.
 (derived from SPDM key negotiation) and place the ci-         Specifically, peer-to-peer keys must be established to pro-
 phertext in the staging buffer. A GPU security engine         tect memory transfers between each pair of GPUs. A GPU
 (e.g., SEC2) may initiate a memory transfer to fetch the      cannot directly access another GPU’s CPR, as the interface
 encrypted data over PCIe, verify its integrity, and decrypt   between them is considered untrusted. Although CPR oc-
 it securely within the GPU’s CPR just prior to kernel         cupies the majority of GPU memory, a small unprotected
 execution.                                                    portion remains available and can serve as a staging buffer,
                                                               which is accessible to other GPUs. To transfer data securely,
 Command Queue Structures. Command queue com-                  the sending GPU must first encrypt the data and write it to
 ponents, including GPFIFO, GPPUT, pushbuffers, and            the staging buffer. The receiving GPU can then copy the
 tracking semaphores, must either reside within the GPU’s      encrypted data from the buffer and decrypt it into its own
 CPR or remain encrypted when stored in staging buffers.       CPR.
 A secure work submission mechanism, similar to that
 provided by SEC2 in the UVM driver, may be employed.          B.2   Trusted I/O Support
 For instance, the cpu sec2 hmac user key may be               One limitation of the current GPU-CC architecture is that
 used to authenticate SEC2 push’s methods, while the           the GPU cannot directly access the private memory of a
 cpu sec2 data user key may encrypt data sent to               CVM. Instead, data must be transferred through a staging
 the SEC2 engine. Work submission may then proceed             buffer, introducing additional software-based encryption and
 indirectly through SEC2 or other secure channels, such        decryption overhead. This not only complicates software
 as WLC, which itself is securely bootstrapped by SEC2.        development, particularly when determining which data
                                                               objects to protect, but also increases the risk of leaving
B     F UTURE D IRECTIONS                                      some data unprotected during transmission.

Here, we discuss the security features expected in upcoming    A promising direction to address this limitation is the devel-
GPU-CC releases, along with the architectural and software     opment of Trusted I/O for confidential computing. Trusted
changes required to support them.                              I/O requires architectural support from both CPUs and
                                                               GPUs. On the CPU side, technologies like Intel TDX Con-
B.1   Multi-GPU Support                                        nect (Intel, 2023a) and AMD SEV-TIO (AMD, 2023) aim
                                                               to enable this capability. On the GPU side, NVIDIA’s Black-
As AI models continue to grow in size, a single GPU’s mem-     well architecture is expected to introduce Trusted I/O sup-
ory often cannot accommodate the full model or dataset,        port as well2 .
necessitating Multi-GPU support for both inference and             1
training tasks. On our NVIDIA H100 SXM5 platform,                    Protected PCIe mode requires, at a minimum, VBIOS ver-
                                                               sion 96.00.BC.00.01, NVIDIA GPU driver version 570, and
each H100 GPU is connected to all four NVSwitches via          CUDA version 12.8.
4th-generation NVLinks, enabling high-bandwidth commu-             2
                                                                     Based on the release notes of NVIDIA kernel driver 590, both
nication between GPUs.                                         Hopper and Blackwell currently only support encrypted data trans-
                                                               fers through staging buffers, which is the mechanism described
In GPU-CC’s threat model, adversaries capable of inter-        in §8. The initial support for PCIe link encryption and device au-
cepting traffic at the NVSwitches or along the NVLinks         thentication has recently been upstreamed to Linux kernel version
between GPUs and NVSwitches may be able to observe             6.19 (February 2026). Additional Trusted I/O support is planned
or tamper with inter-GPU data transfers. While NVIDIA’s        for the future kernel release cycles.
                 Blueprint, Bootstrap, and Bridge: A Security Look at NVIDIA GPU Confidential Computing

Intel and AMD’s Trusted I/O technologies rely on a combi-          AK Attestation Key
nation of standardized protocols and trusted components to         BAR Base Address Register
ensure secure device integration and communication within          BMC Baseboard Management Controller
confidential computing environments. Protocols such as             BROM Boot ROM
TEE Device Interface Security Protocol (TDISP), Security           CA Certificate Authority
Protocol and Data Model (SPDM), and Integrity and Data             CE Copy Engine
Encryption (IDE) are central to this infrastructure. TDISP         CPU-CC CPU Confidential Computing
governs the device interface lifecycle, while SPDM is used         CoT Chain of Trust
for device authentication and establishing secure communi-         CPR Compute Protected Region
cation channels. IDE provides encryption for PCIe traffic,         CVM confidential virtual machine
securing both control and data paths.                              DIK Device Identity Key
                                                                   DMA Direct Memory Access
Trusted components include the TEE Security Manager
                                                                   DSM Device Security Manager
(TSM) and the Device Security Manager (DSM). The TSM
                                                                   FSP Foundation Security Processor
is responsible for defining and enforcing security policies
                                                                   GPU-CC GPU Confidential Computing
and managing secure communication with devices. The
                                                                   GSP GPU System Processor
DSM, a trusted logical component within the device, works
                                                                   GSP-BROM GSP BootROM
in conjunction with the TSM to establish secure channels.
                                                                   GSP-FMC GSP First Mutable Code
Device attestation is supported through SPDM and the               GSP-RM GSP Resource Manager
Reference Integrity Manifest (RIM), which validate device          HBM High-Bandwidth Memory
identity and firmware integrity. When a device is assigned to      IDE Integrity and Data Encryption
a CVM, the host initiates attestation by communicating with        IV Initialization Vector
the device’s DSM using SPDM. The TSM verifies the iden-            KVM Kernel-based Virtual Machine
tity and measurements of the device, checks them against           LCIC Launch Confirmation Indicator Channel
security policies, and passes the evidence to the CVM for          LUKS Linux Unified Key Setup
final evaluation. If the device is verified, it is admitted into   MCTP Management Component Transport Protocol
the trusted environment.                                           MIG Multi-Instance GPU
                                                                   OCSP Online Certificate Status Protocol
Once a device is trusted, it can perform DMA to private
                                                                   PSIRT Product Security Incident Response Team
guest memory. In Intel TDX Connect, this access requires
                                                                   PTX Parallel Thread Execution
the device to be included in the CVM’s Trusted Comput-
                                                                   QMD Queue Metadata
ing Base (TCB). The TDX module manages translation of
                                                                   RIM Reference Integrity Manifest
device transactions to private memory, with the IOMMU
                                                                   RMAPI Resource Manager API
enforcing these mappings and ensuring access is restricted
                                                                   RPC Remote Procedure Call
to assigned devices. Secure PCIe transactions use IDE
                                                                   SASS Streaming Assembler
Transaction Layer Packets (TLPs) marked with a T-bit to
                                                                   SDK Software Development Kit
signal trusted communication. In AMD SEV-TIO, DMA
                                                                   SDT Secure Device Table
access is permitted after the guest establishes trust in the de-
                                                                   SEC2 Secure Processor
vice. The IOMMU maintains a Secure Device Table (SDT)
                                                                   SEV Secure Encrypted Virtualization
that tracks the security attributes of devices and enforces
                                                                   SGX Software Guard Extensions
access control accordingly.
                                                                   SM Streaming Multiprocessor
Overall, both platforms ensure a secure and authenticated          SMM System Management Mode
path for data and control between hosts, devices, and guest        SPDM Security Protocol and Data Model
CVMs, building trust through layered verification and se-          TCB Trusted Computing Base
cure protocol enforcement.                                         TDISP TEE Device Interface Security Protocol
                                                                   TDX Trust Domain Extensions
C    L IST OF ACRONYMS                                             TEE Trusted Execution Environment
                                                                   TLB Translation Lookaside Buffer
GPU-CC uses a large number of acronyms. For clarity, we            TLP Transaction Layer Packet
include a glossary of acronyms with cross-references to            TLS Transport Layer Security
assist readers in navigating the paper.                            TSM TEE Security Manager
                                                                   UVM Unified Virtual Memory
AAD Additional Authenticated Data                                  VM virtual machine
AES Advanced Encryption Standard                                   WLC Work Launch Channel
