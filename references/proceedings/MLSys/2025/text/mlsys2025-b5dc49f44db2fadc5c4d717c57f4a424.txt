O PTIMIZING LLM Q UERIES IN R ELATIONAL DATA A NALYTICS W ORKLOADS


             Shu Liu * 1 Asim Biswal * 1 Amog Kamsetty 1 Audrey Cheng 1 Luis Gaspar Schroeder 1 2
            Liana Patel 3 Shiyi Cao 1 Xiangxi Mo 1 Ion Stoica 1 Joseph E. Gonzalez 1 Matei Zaharia 1


                                                       A BSTRACT
     Batch data analytics is a growing application for Large Language Models (LLMs). LLMs enable users to perform
     a wide range of natural language tasks, such as classification, entity extraction, and translation, over large datasets.
     However, LLM inference is highly costly and slow: for example, an NVIDIA L4 GPU running Llama3-8B can
     only process 6 KB of text per second, taking about a month to handle 15 GB of data; processing a similar amount
     of data costs around $18K on OpenAI’s GPT-4o. In this paper, we propose novel techniques that can significantly
     reduce the cost of LLM calls for relational data analytics workloads. Our key contribution is developing efficient
     algorithms for reordering the rows and the fields within each row of an input table to maximize key-value (KV)
     cache reuse when performing LLM serving. As such, our approach can be easily applied to existing analytics
     systems and serving platforms. Our evaluation shows that our solution can yield up to 3.4× improvement in job
     completion time on a benchmark of diverse LLM-based queries using Llama 3 models. Our solution also achieves
     a 32% cost savings under OpenAI and Anthropic pricing models.

1    I NTRODUCTION                                                 tion, entity extraction, summarization, and translation (dat).
                                                                   Going forward, we will refer to queries that invoke LLMs
One of the most popular applications of large language             over relational data as LLM queries.
model (LLM) batch inference is data analytics. A grow-
ing number of analytics platforms now support LLM in-              Unfortunately, applying LLMs to real-world datasets (which
vocations for complex analytical tasks. For instance,              can contain millions of rows) incurs significant computa-
leading database vendors, such as AWS Redshift (aws),              tional and monetary costs. Accordingly, there has been
Databricks (dat), and Google BigQuery (goo), have inte-            growing research on LLM inference optimization. In par-
grated LLM functionality into their SQL APIs. Similarly,           ticular, recent work (Kwon et al., 2023; Zheng et al., 2023;
DataFrame libraries and programming frameworks (Chase,             Ye et al., 2024; Juravsky et al., 2024; Gim et al., 2024)
2022; Patel et al., 2024) offer LLM support for querying           leverages prompt caching, a technique that stores the atten-
relational (table-based) data. With these new APIs, users          tion states of frequently reused prompt segments in GPU
can write queries like the following:                              memory, known as key-value (KV) cache (Vaswani et al.,
                                                                   2023). Reusing cached state whenever a similar prefix of
    SELECT user_id, request,                                       prompts appears again can significantly reduce inference
    ,→    support_response,                                        latency (Zheng et al., 2023). In addition, prompt reuse also
       LLM('Did {support_response} address                         brings economic benefits. Recently, providers like OpenAI,
       ,→   {request}?', support_response,
       ,→   request) AS success
                                                                   Anthropic, and Google Gemini (OpenAI; ant, 2024; gem,
    FROM customer_tickets                                          2024) have introduced prompt caching as a service, charg-
    WHERE support_response <> NULL                                 ing 2–10× less for cached prompts. Therefore, maximizing
                                                                   prefix hits in the prompt KV cache is crucial for reducing
where the LLM is invoked for each row in the customer              both LLM request time and monetary costs.
ticket table to analyze whether the customer service requests      However, simply invoking LLMs over relational data within
are effectively addressed. Increasingly, analysts wish to          analytical engines and connecting to a backend inference
leverage LLMs in such queries for tasks including classifica-      server with a prompt cache often results in low cache hit
  *                                                                rates. This approach fails to exploit relational workloads to
    Equal contribution 1 UC Berkeley 2 Technical University of
Munich 3 Stanford University. Corresponding author: Shu Liu        fully maximize cache reuse.
<lshu@berkeley.edu>.                                               In this work, we identify and present solutions to optimize
                   th
Proceedings of the 6 MLSys Conference, Santa Clara, CA, USA,       relational data analytics workloads for offline LLM infer-
2025. Copyright 2025 by the author(s).                             ence. In particular, given an LLM query, we propose re-
                              Optimizing LLM Queries in Relational Data Analytics Workloads

quest reordering at the row and field granularity of the             • We identify significant opportunities to speed up LLM-
relational data. Our key insight is that, with oracular knowl-         based batch data analytics through reordering rows and
edge of all requests to be sent to the LLM, we can reorder             fields of input tables.
both the requests and the fields inside each request to in-
crease the number of cache hits. In real datasets, there can         • We introduce an optimal reordering algorithm (OPHR)
be many sharing opportunities across rows and fields. For              that maximizes prefix sharing but with exponential
example, joining feature tables, referencing popular items,            complexity. We propose an efficient greedy algorithm
or repeating similar context in RAG queries (Lewis et al.,             (GGR) that approximates OPHR by leveraging func-
2021). These common patterns lead to repeating values in               tional dependencies and table statistics. We show that
different fields, leaving rooms for significantly improving            a fixed field ordering can yield as much as m (number
cache hit rates by optimizing request order and format.                of fields) times worse cache hits than our solution.

Finding the optimal ordering of requests is challenging due          • We present an LLM query benchmark consisting of 16
to the exponential number of choices to order the fields and           queries and 7 real-world datasets to represent a range
rows of data in a query. For a table with n rows and m                 of retrieval and processing tasks. Our evaluation with
fields, there are n! × (m!)n potential orderings. One way to           Llama3-8B and 70B shows up to a 3.4× speedup in
reduce this search space is to apply the same field ordering           end-to-end query latency compared to naive orderings.
across all rows. However, as we show in Sec 3.2, this can              With OpenAI and Anthropic prefix cache pricing mod-
reduce the prefix hit count by up to a factor of m compared            els, our techniques reduce costs by up to 32%.
to reordering fields on a more fine-grained, per-row basis.
To support per-row field reordering, we introduce Optimal        2    BACKGROUND AND M OTIVATION
Prefix Hit Recursion (OPHR), an algorithm that divides           This section provides a brief overview of the inference pro-
the table into smaller subtables and reorders each subtable      cess and the key components of the LLM architecture.
to maximize the prefix hits. While OPHR achieves high
hit rates, its complexity is exponential, which makes it im-     LLM inference. LLMs are made up of autoregressive Trans-
practical for large datasets. To address this challenge, we      former models (Vaswani et al., 2023), which generate text
propose Greedy Group Recursion (GGR), an approximate             token by token until a termination token or a length limit is
algorithm that leverages functional dependencies (such as        reached. LLM inference consists of two stages: (i) the pre-
primary and foreign key relationships from the data schema)      fill stage, where the model processes the input prompts, and
and table statistics, which are readily available in many        (ii) the decoding stage, where it generates output sequen-
databases and analytics systems, to reduce the search space.     tially, as each token depends on all previously generated
In particular, functional dependencies help identify corre-      tokens through a chain of conditional probabilities. LLM
lated fields, reducing the number of fields that need to be      inference engines (e.g., vLLM (Kwon et al., 2023), TGI
reordered at each step, thus decreasing the solver runtime. In   (Huggingface, 2023), TensorRT-LLM (NVIDIA, 2023b))
addition, GGR leverages the cardinality and length statistics    typically batch requests continuously (Yu et al., 2022) to
to efficiently approximate the greedy objective.                 improve throughput. The intermediate computed state for
                                                                 all tokens involved is stored in memory. This token state is
We implement our techniques in Apache Spark (Zaharia             cached as key and value vectors in the key-value (KV) cache,
et al., 2012) and use vLLM (Kwon et al., 2023) as the            consuming up to 800KB per token for a 13B Model (Kwon
model serving backend. Due to the lack of standard work-         et al., 2023). A typical request (involving 2,000 tokens)
loads in this area, we build a benchmark suite of 16 LLM         can require up to 1.6 GB of memory. Despite batching (up
queries of different types, spanning selection, projection,      to 32 requests), inference remains compute-intensive, with
multi-LLM invocations, and retrieval-augmented genera-           current speeds limited to 2,000 tokens/s per GPU, making
tion (RAG) queries (Lewis et al., 2021). We evaluate             LLM performance a bottleneck for many analytical tasks.
these queries on recommendation and question-answering
datasets such as Amazon Product Reviews, Rotten Toma-            Prompt KV cache. Efficient KV cache management is
toes Movies, BIRD, Stanford Question Answering Dataset,          critical for high LLM serving throughput. Recent work
Public Domain MusicXML, RateBeer Reviews, and Fact               improves cache utilization by reusing tokens across requests
Extraction and VERification datasets (He & McAuley, 2016;        with shared prefixes (Zheng et al., 2023). For example, if
Pang & Lee, 2005; Li et al., 2024; Rajpurkar et al., 2016;       two requests share a prefix in prompts, the first will already
Long et al., 2024; Thorne et al., 2018). Our techniques          have performed some computation on the input tokens and
show 1.5 – 3.4× speed-up in end-to-end query latency and         cached results in the KV cache during the prefill phase.
reduce costs by up to 32% on proprietary model APIs, while       The subsequent request can then reuse these cached values,
preserving query semantics. In summary, our contributions        avoiding redundant computation of the shared tokens.
are as follows:                                                  Improving KV cache hit for analytics workloads. Real-
                              Optimizing LLM Queries in Relational Data Analytics Workloads

world relational databases often exhibit diverse repetitive      summarization task.
data patterns. Columnar storage systems like C-Store and
                                                                 Objective The goal of request scheduling is to maximize
Parquet (Stonebraker et al., 2018) exploit repeated val-
                                                                 the prefix hit count by optimizing the order of fields and rows
ues across fields for compression, while techniques like
                                                                 of an input table with n rows and m fields. Each row may
run-length encoding (RLE), multi-relational data mining,
                                                                 have a different field order. We represent a request schedule
and correlation analysis (Lemire & Kaser, 2011; Džeroski,
                                                                 as a list of tuples L, where each tuple in L represents a
2003; Ilyas et al., 2004) leverage diverse data relationships
                                                                 row in the table, and the tuple elements contain the field
to optimize query execution. Relational queries also cre-
                                                                 values. We adjust the row order by rearranging the tuples in
ate data groupings based on access patterns. Techniques
                                                                 L, and adjust the field order for that row by rearranging the
such as database cracking and multi-dimensional clustering
                                                                 elements within each tuple. We pass each tuple alongside
(MDC) (Idreos et al., 2007; Chen et al., 2012), including
                                                                 the user question to form an input request to the LLM.
Delta Lake Z-order (Armbrust et al., 2020), reorganize data
based on query patterns to optimize performance.                 We define the prefix hit count (PHC) of L as the number
                                                                 of consecutive field cell values shared with the previous
These structural repetitive patterns present an opportunity
                                                                 row starting from the first cell, summing over all n rows.
for prefix KV cache sharing in an LLM query. In our setting,
                                                                 Each cell value must exactly match the corresponding cell
an LLM is invoked once per row in a relational table, result-
                                                                 of the previous row (cannot be a substring), and cell values
ing in a batch of model requests from a single LLM query.
                                                                 past the first must match consecutively (must be a prefix).
Since the full table structure and content are known in ad-
                                                                 Formally, a cell in the list of tuples is denoted as L[r][f ],
vance, we can reorder these requests to maximize shared
                                                                 indicating the value in tuple r at position f . Then, the PHC
prefixes and reduce redundant computation during inference.
                                                                 for a list of tuples L with n rows and m fields is given by:
Our goal is to maximize the prefix hit count – the sum of the                                    n
length of token prefixes reused from the KV cache.
                                                                                                X
                                                                                    PHC(L) =        hit(L, r)               (1)
Our Approach: Request Reordering. We leverage table                                             r=1

information to enhance the KV cache hit rate. Specifically,      Here, the function hit(L, r) represents the prefix hit count
we introduce algorithms that reorder requests of an LLM          for a single row r in L. For simplicity, we assume that the
query and fields within each request to maximize prefix          input list is sorted. For each row r, the function checks if
sharing. Our algorithm leverages functional dependencies         the value in each field f matches any previously seen value
and table statistics to reduce runtime while finding near-       in the same field of the previous row r − 1. If all previ-
optimal orderings that maximize prefix hit count.                ous fields match, the hit count is the sum of the squares of
                                                                 the lengths of the values in those fields until a mismatch oc-
3     P ROBLEM S ETUP                                            curs. The squared lengths reflect the quadratic complexity of
                                                                 token processing in LLM inference, where each token com-
This section introduces the problem setup of maximizing
                                                                 putation depends on every preceding token and increases
prefix hits in the prompt cache (Sec 3.1) and highlights cases
                                                                 computational cost quadratically with input length.
where naive fixed field ordering can result in significantly
                                                                                       Pc
lower hit rates (Sec 3.2).                                                                 f =1 len(L[r][f ])
                                                                                                              2
                                                                                                                   if ∀f ≤ c,
                                                                                       
                                                                                       
3.1   Setup and Objective
                                                                                                                  L[r][f ] =
                                                                   hit(L, r) = max
                                                                                0≤c<m                             L[r − 1][f ]
In this work, we consider a generic LLM operator that
                                                                                       
                                                                                       
                                                                                         0
                                                                                       
                                                                                                                   otherwise
takes the text of the prompt as well as a set of expressions
                                                                                                                              (2)
listing one or more fields {T.a, T.b, T.c} or {T.∗} of the
table T . This simple design can be easily implemented           To simplify the design, we make two assumptions. First, we
in most analytics systems and enables us to dynamically          make a common assumption that at least one tuple (row) can
reorder fields within these expressions to optimize for cache    fit into the KV cache to allow reuse. Second, we assume that
efficiency. Consider the following example query:                a cell value only counts as a hit if it exactly matches a previ-
                                                                 ously seen value – substring matches are not allowed. This
    SELECT LLM("Summarize: ", pr.*)                              is a reasonable assumption in relational databases, where
    FROM (
        SELECT review, rating, description
                                                                 exact value repetition is common and extensively leveraged
        FROM reviews r JOIN product p ON                         by storage optimization techniques like run-length encod-
        ,→  r.asin = p.asin                                      ing (Lemire & Kaser, 2011). Column-oriented storage sys-
    ) AS pr                                                      tems such as C-Store and Parquet (Stonebraker et al., 2018)
                                                                 also benefit from many exact repetitions in columnar data.
This query sends a list of rows, each with fields review,        These assumptions simplify design and, as shown in Sec. 6,
rating, and description from table pr to the LLM for a           demonstrate good real-world performance.
                                     Optimizing LLM Queries in Relational Data Analytics Workloads

                                                                                                                       A Better Ordering

                                                                                     Fixed Field Ordering              Field1 Field2 Field3
          Fixed Field Ordering                A Better Ordering                                             𝒙 rows {    G1
                                                                                     Field1 Field2 Field3
         Field1   Field2    Fieldm                                                                                     Field2 Field1 Field3
                                           Field2     Fieldm    Field1    𝒙 rows {     G1
                                                                                                            𝒙 rows {    G2
                                                                                             G2
𝑛 rows                                                                                                                 Field3 Field1 Field2
                   G2         Gm            G2             Gm                                        G3
                                                                                                            𝒙 rows {    G3

                  PHC = 0                     PHC = (n-1)(m-1)                         PHC = (𝒙-1)                       PHC = 3(𝒙-1)

                  (a) Distinct Values in the First Field                        (b) Group of Identical Values in each Field, m = 3

Figure 1. Case Study of Fixed Field Ordering: Comparing the PHC of a fixed field ordering to a better ordering in two scenarios. Green
boxes denote cache hits; red boxes indicate cache misses. A box labeled Gi signifies consecutive rows share the same values in Field i;
otherwise, assume values are distinct. Fig 1a shows fixed field ordering can be (n − 1)(m − 1) worse in terms of PHC compared to an
optimized ordering. Fig 1b shows fixed field ordering can be m times worse in PHC compared to an optimized ordering, where m = 3.
3.2   Case Study: Fixed Field Ordering                                   In the above scenario, PHC improvements from optimized
                                                                         field ordering can reach m times that of a fixed field ordering.
Relational data typically uses a fixed field order across rows,
                                                                         For example, there can be multiple (instead of just one) such
which can lead to lower hit rates in real-world databases
                                                                         groups in each field. If each field contains roughly the same
with diverse data patterns (Sec 2). In fact, we show that
                                                                         number of such groups, dynamic reordering for different
using a fixed order can reduce the hit rate by up to m times
                                                                         rows can achieve as much as an m-fold improvement in PHC
compared to a per-row field reordering. To illustrate this,
                                                                         over fixed field ordering. Under the OpenAI pricing model,
we begin with a simple example and extend it to show the
                                                                         which charges half price for cached prompts, optimizing
potential impact of a naive fixed field ordering on prefix
                                                                         field order for a table with nine fields could yield 42% in
hit counts (PHC). First, consider a table T with n rows
                                                                         cost savings compared to fixed field ordering, assuming
and m fields arranged in an arbitrary (default) order. For
                                                                         fixed ordering has a 10% hit rate (e.g., (x−1)n    = 10). This
simplicity, we assume each value is of length one. In many
                                                                         example highlights the benefits of a more complex field
cases, certain fields of an input table may contain highly
                                                                         reordering mechanism for different rows on PHC.
unique values, like timestamps or IDs. In the worst case,
suppose the first field of the table contains only unique
values (Fig 1a), and the remaining m − 1 fields contain the
                                                                         4     R ECURSIVE R EQUEST R EORDERING
same value across all rows. This ordering yields 0 PHC. A                We now introduce our algorithms that re-arrange fields to
more optimized ordering (Fig 1a) will place the other m − 1              maximize prefix sharing in the KV cache. We present an
fields first, yielding a PHC of (n − 1) × (m − 1). Each of               optimal recursive reordering algorithm that maximizes PHC
the n − 1 rows has a hit after the initial cold miss, and the            (Sec 4.1) and introduce a greedy algorithm that efficiently
length of each hit is m − 1.                                             approximates the optimal algorithm (Sec 4.2).
Now consider a scenario where the table contains groups of               4.1   Optimal Prefix Hit Recursion (OPHR)
consecutive rows with identical values (not necessarily in
the same field). Suppose each field i has one such group with            Our Optimal Prefix Hit Maximization (OPHR) algorithm is
x consecutive rows of the same value, with other n − x rows              a recursive algorithm that finds the optimal PHC for a given
having distinct values, where n is the number of rows. We                table T by considering all possible ways to split the table
denote the group appearing in the Fieldi as Gi , so we have              into a group of cells with the same value and two sub-tables.
G1, ..., Gm groups, where m is the number of fields. Now,                The algorithm takes as input a table T and computes the
consider a scenario where groups in consecutive fields are               optimal PHC S along with a reordered list of tuples L. If
non-overlapping across rows, as shown in Fig 1b. With fixed              T only has one row or field, OPHR computes PHC and
field reordering, the PHC of this structure is limited to x − 1          trivially returns the sorted T .
no matter which field is prioritized. By contrast, a better              In the recursive case, for each field c in T , the algorithm
ordering would rearrange the field order for different rows              identifies all distinct values v in the field and the rows Rv
to prioritize groups with shared values. Fig 1b references a             for which the field value is v. For each distinct value v, the
table with 3x rows and 3 fields. A naive fixed field ordering            table is split into two sub-tables: one of T excluding rows
for all rows will result in misses on two groups, each with              Rv and one of Rv excluding field c. PHC for the currently
x rows in Field2 and Field3 . However, a better ordering                 selected value v is calculated as the sum of the PHC of the
will pick different Fieldj to prioritize for different rows,             sub-tables and the PHC contribution of v. OPHR evaluates
resulting in a 3 times higher hit rate of 3(x − 1).
                                  Optimizing LLM Queries in Relational Data Analytics Workloads

Algorithm 1 Greedy Group Recursion (GGR)                                                       Max Group Value
 1: Input: Table T , Functional Dependency F D                                                                            b
 2: Output: Prefix Hit Count S, Reordered List of Tuples L                  b   v             v         b
                                                                            b                           b                 b
                                                                                v             v
                                                                            b                           b                 b
 3: function H IT C OUNT(v, c, T, F D)                                          v             v
                                                                                v             v
 4:   Rv ← {i | T [i, c] = v}
 5:   inferred cols ← {c′ | (c, c′ ) ∈ F D}P                         a                        a                       a
                                                  len(T [r,c′ ])     a                        a                       a
      tot len = len(v)2 + c′ ∈inferred cols r∈Rv|Rv |
                          P
 6:
 7:   return tot len × (|Rv | − 1), [c] + inferred cols
 8: end function                                                   Figure 2. GGR picks the group with the maximum hit count at each
                                                                   step and calculates PHC as the sum of PHC of the elected group
 9: function GGR(T , F D)                                          values (yellow box), the sub-table T excluding rows Rv (green
10:   if |T |rows = 1 then                                         box), and the sub-table of rows Rv excluding the field where the
11:      return 0, [T [1]]                                         value is located in (blue box).
12:   end if
13:   if |T |colsP= 1 then                                         optimally computes PHC for both sub-tables because it is
14:      S ← v∈distinct(T [,1]) H IT C OUNT(v, 1, T )              optimal for tables with fewer rows and fields. The PHC for
15:      Return S, sort([T [i] | i ∈ 1 . . . |T |rows ])           T is the sum of PHC for TA and TB , plus the contribution
16:   end if
                                                                   of v. When the distinct value v is used to partition the table,
17:   max HC, b v, b c, b cols ← −1, None, None, []
18:   for c ∈ columns(T ), v ∈ distinct(T [, c]) do                its full contribution to the PHC is captured. If the table were
19:      HC, cols ← H IT C OUNT(v, c, T, F D)                      not split based on distinct values, this contribution could
20:      if HC > max HC then                                       be fragmented or lost due to non-contiguous groupings,
21:         max HC, b v, b c, b cols = HC, v, c, cols              leading to suboptimal PHC. Thus, the OPHR algorithm
22:      end if
                                                                   ensures optimal reordering by selecting the best from all
23:   end for
24:   R v ← {i | T [i, b c] = b v}                                 possible configurations.
25:   A HC, L A ← GGR(T [rows \ R v, cols], F D)
26:   B HC, L B ← GGR(T [R v, cols \ b cols], F D)                 4.2     Greedy Group Recursion (GGR) Algorithm
27:   C HC, ← H IT C OUNT(b v, b c, T, F D)
28:   S ← A HC + B HC + C HC                                       Due to the computational complexity of the OPHR, we
29:   L ← [[b v] + LA [i] | i ∈ 1 . . . |R v|] + L B               propose a Greedy Group Recursion (GGR) algorithm (Al-
30:   return S, L                                                  gorithm 1) that approximates OPHR. The GGR algorithm
31: end function                                                   takes an input table T and returns the PHC S along with a
                                                                   reordered list of tuples L. It has the same base case as the
32: return GGR(T , F D)
                                                                   OPHR algorithm if T only has one row or one field. At a
                                                                   high level, the GGR algorithm recursively selects the value
                                                                   bv with the maximum prefix hit count (lines 3-8) at each
all possible groups of distinct values in each field and selects   recursion step (lines 17-23) rather than iterating through all
the value that yields the maximum PHC.                             possible distinct values in the entire table. It then prioritizes
Notably, the OPHR algorithm has exponential complexity             the field bc where this bv is in, splits the table into groups of
with respect to the number of rows and fields due to its           cells of the same values and recurses on the two sub-tables
recursive nature and the combinatorial explosion of possi-         (lines 24-26) and calculates the total PHC as the sum of PHC
ble distinct value groupings (we present a more efficient          of the subtables and contributions of bv (line 28) similar to
algorithm in Sec 4.2).                                             the OPHR algorithm.
Optimality Proof In the base case, the OPHR algorithm              Since GGR does not iterate through all possible distinct
trivially computes the best PHC: for the single row case,          values but instead selects the one that gives the highest hit
the PHC is 0; for the single field case, the PHC is the sum        count at each step, the number of recursive calls is signif-
of the squared lengths of distinct values multiplied by their      icantly reduced (i.e. the maximum depth of recursion is
occurrences minus one, which accounts for the initial miss         O(min(n, m)), where the algorithm reduces dimensions of
when a value is seen the first time. Next, we prove optimality     the table at each recursive step). However, at each recursive
by induction. For the inductive case, assume that the OPHR         step, the cost of scanning to determine distinct values can
algorithm is optimal for any table with k ≤ n rows and             result in quadratic complexity in terms of table size.
l ≤ m fields. For a table T with n + 1 rows and m + 1
                                                                   4.2.1    Functional Dependencies
fields, the algorithm iterates through each field c. For each
distinct value v in field c, we split T into two sub-tables:       We leverage functional dependencies to reduce the number
TA (rows not containing v), and TB (rows containing v but          of fields the GGR algorithm needs to consider at each recur-
excluding field c). Based on the inductive hypothesis, OPHR        sion step. This insight helps improve both the approximation
                                Optimizing LLM Queries in Relational Data Analytics Workloads

and efficiency of the algorithm, bringing it closer to the op-      5     I MPLEMENTATION
timal solution without the need for extensive backtracking
as in the OPHR algorithm. A functional dependency (FD)              We implement our algorithms in approximately 1.3K lines
is a constraint between two sets of attributes in a relation        of Python code and evaluate them with PySpark (Armbrust
from the data. For example, let R be a relation schema              et al., 2015), which is backed by Apache Spark (Zaharia
and let X and Y be nonempty sets of attributes in R. We             et al., 2012) – a widely adopted large-scale data processing
define an instance r of R that satisfies the FD X ↔ Y if            engine in industry. The LLM operator implements the actual
for every pair of tuples t1 and t2 in r: if t1 .X = t2 .X then      LLM inference by calling a configurable LLM endpoint. We
t1 .Y = t2 .Y and vice versa. In our GGR algorithm, FDs             implement this function as a UDF in PySpark. It takes in
help narrow down the fields that must be considered at each         a system prompt, a query prompt, and a single row of data
recursion step. Specifically, when a value v in field f is          as input (Appendix C). The row and field orders are input
selected for a given row, all fields functionally dependent         based on the ordering returned by the reordering function.
on f are ordered directly besides f in the final ordering           The operator is also responsible for prompt construction.
for that row (lines 5-6). As an example, if R(A, B, C) is           Specifically, it converts the user-provided question and the
a table with attributes (fields) A, B, C where we have an           table row values into a prompt that an LLM can parse. We
FD A ↔ C, field C is not in consideration in our recursive          use JSON formatting to encode row values to indicate the
steps when A has already been included in the prefix.               relationship between field names and values to the LLM.

4.2.2   Table Statistics                                            6     E VALUATION
To further reduce the algorithm runtime, we introduce an            In this section, we evaluate the effectiveness of our opti-
early stopping mechanism that halts recursion by specific           mizations within a constructed benchmark suite of queries.
recursion depth (row-wise sub-table recursion, column-wise          We aim to answer the following questions:
sub-table recursion) or when a threshold HITCOUNT score
calculated using table statistics is not exceeded. These statis-        1. How does our request reordering optimization impact
tics are generally widely available, such as the number of                 query latency and costs across different LLM query
unique entries (i.e., cardinality) and the distribution of length          types and datasets?
of values for each field. With this information, our GGR                2. How does the request reordering algorithm influence
algorithm estimates a HITCOUNT score for each field c with                 LLM accuracy for different models?
HITCOUNT(C) = avg(len(c))2 . This score denotes the ex-                 3. What is our algorithm solver time, and how does that
pected contribution of a field to the PHC, accounting for                  compare to end-to-end query latency?
the average length of the values and their frequency. Us-
ing these statistics, the algorithm can prioritize fields more
                                                                    6.1     Evaluation Benchmark
likely to contribute to the PHC. Additionally, we can further
improve the quality of the solution by establishing a fixed         Given the lack of standard benchmarks for LLM queries,
field ordering for the subtables using table statistics once        we construct a benchmark suite to represent real-world data
the recursion stops. Early termination and falling back to          retrieval and processing tasks (Sec 6.1.1). We define a range
table statistics allows GGR to avoid scanning the table and         of query types (Sec 6.1.2) over datasets from various sources
performing recursion on real-world workloads.                       to assess the impact of LLMs in relational analytics.
4.2.3   Achieving Optimal PHC                                       6.1.1     Datasets
While our GGR approximates the OPHR algorithm, it can
achieve optimal PHC in certain cases. When the table has            Dataset     nrows    nfields inputavg      outputavg      Query Type
only one row or one field, GGR matches OPHR by construc-            Movies     15000       8       276       {2, 29, 16, 2}   T1-T4
tion. When functional dependencies are accurate and cover           Products   14890       8       377      {3, 107, 62, 2}   T1-T4
all the fields of a table, GGR can also identify the optimal         BIRD      14920       4       765          {2, 43}       T1, T2
solution. For instance, if one field A functionally determines       PDMX      10000      57      738           {2, 72}       T1, T2
                                                                      Beer     28479       8       156          {2, 38}       T1, T2
all other fields, then GGR prioritize groups of values in A         SQuAD      22665       5      1047            11          T5
due to the accumulated HITCOUNT score (line 3 in Algo-              FEVER      19929       5      1302             3          T5
rithm 1), capturing key correlations early and producing the
optimal reordering. However, when fields tie in HITCOUNT,           Table 1. Datasets: nrows and nfields denote the number of rows and
GGR may be suboptimal, as it lacks the exhaustive search            fields, respectively. inputavg and outputavg represent average input
used by OPHR to resolve such ties. We show more empiri-             and output token lengths. Query Type is detailed in Sec 6.1.2.
cal results in real-world datasets comparing PHC between            Since inputavg remains consistent across query types, we report a
GGR and OPHR in Appendix D.1.                                       single overall average, while outputavg varies, with each bracketed
                                                                    value corresponding to a specific query type.
                               Optimizing LLM Queries in Relational Data Analytics Workloads

We build our benchmark suite on 7 commonly used rec-              6.1.3    Evaluation Setup
ommendation and natural language processing datasets,
                                                                  Metrics We evaluate end-to-end query latency for each
shown in Table 1. These datasets vary in the number
                                                                  LLM query. We also measure the monetary cost of us-
of rows, fields, average input/output token lengths, and
                                                                  ing OpenAI and Anthropic endpoints. Additionally, we
appropriate query types (Sec 6.1.2). The datasets in-
                                                                  hand-label a subset of the LLM filter queries to evaluate the
clude Rotten Tomatoes Movie Reviews (Movies) (Pang &
                                                                  reordering implications for query accuracy.
Lee, 2005), Amazon Product Reviews (Products) (He &
                                                                  Models We run setups shown in Table 1 using Meta Llama-
McAuley, 2016), BIRD (Li et al., 2024)1 , Public Domain
                                                                  3-8B-Instruct (lla, 2024). For RAG queries, we use Alibaba-
MusicXML (PDMX) (Long et al., 2024), RateBeer Reviews
                                                                  NLP/gte-base-en-v1.5 (Li et al., 2023) to embed the context
(Beer) (McAuley et al., 2012), Stanford Question Answer-
                                                                  and use Facebook Similarity Search Library (FAISS) (John-
ing Dataset (SQuAD) (Rajpurkar et al., 2016), and Fact
                                                                  son et al., 2019) for context retrieval. We also run Llama-
Extraction and Verification (FEVER) (Thorne et al., 2018).
                                                                  3-70B-Instruct (lla, 2024) for LLM Filter queries. For cost
Details on the fields are in the Appendix B.
                                                                  results, we evaluate with OpenAI GPT-4o-mini and An-
6.1.2   LLM Queries                                               thropic Claude 3.5 Sonnet.
                                                                  Hardware We evaluate Llama-3-8B-Instruct on a single
Our evaluation consists of 16 queries across 5 query types
                                                                  NVIDIA L4 GPU (GCP g2-standard-4) with 24GB of GPU
corresponding to different real-world use cases, as shown
                                                                  Memory. We also run a larger model Llama-3-70B-Instruct
in Table 1. We discuss each query type below and provide
                                                                  on 8xL4 GPUs (GCP g2-standard-48). For OpenAI and
details on queries for each dataset in Appendix A and B.
                                                                  Anthropic cost experiments, we utilize their API endpoints.
(T1) LLM filter. Filter queries mimic SQL WHERE clauses           Baselines Our algorithm (Cache (GGR)) is compared
and use LLMs to categorize data. This query type illustrates      against two baselines: one without prompt caching (No
typical use cases in sentiment analysis, categorization, and      Cache) and another with caching enabled but without re-
content filtering. Given their binary or categorical focus,       ordering (Cache (Original)). We do not evaluate the optimal
these queries often yield short outputs (e.g., ”Yes” or ”No”).    prefix hit recursion algorithm (Sec 4.1) as it is infeasible
We construct five filter queries spanning all datasets except     over large tables (e.g., solving a 10-row table takes sev-
for SQuAD and FEVER.                                              eral minutes). The algorithm runtime far exceeds the LLM
(T2) LLM projection. Projection queries use LLMs to sum-          inference time for larger tables for the optimal algorithm.
marize or interpret specific table field(s), similar to a SQL
SELECT statement. These tasks typically produce longer            6.2     End-to-End Benchmark Results
outputs due to the descriptive nature of the results. We con-     Overview. Fig 3 and Fig 4 show the end-to-end latency
struct five projection queries spanning all datasets except       results of our techniques on LLM filter, projection, multi-
SQuAD and FEVER.                                                  LLM invocation, aggregation, and RAG queries with the
(T3) Multi-LLM invocation. Multi-LLM queries involve              Llama-3-8B-Instruct model on a single L4. Our evaluation
sequential LLM calls (e.g., a filter followed by a projection),   shows that our approach can achieve 1.5 to 3.4× speedup
supporting tasks like multi-step data processing and combin-      over Cache (Original) and 1.8 to 3.8× speedup over No
ing insights. Output lengths vary by task but generally mix       Cache across 16 queries. We discuss the evaluation for each
short and long responses. We construct two example multi-         query type in detail as below.
LLM invocation queries on Movies and Products datasets.           LLM filter. This query type uses an LLM operator to filter
(T4) LLM aggregation. Aggregation queries incorporate             rows, often producing concise outputs of only a few tokens
LLM outputs into aggregate functions, like averaging senti-       (see Table 1). Examples include question-answering tasks
ment scores given by LLMs for individual reviews. These           limited to ’Yes’ or ’No’ responses, or sentiment labels like
tasks usually generate concise numeric outputs for analysis       ’Positive,’ ’Negative,’ or ’Neutral.’ We construct five such
(e.g., ratings of 1 to 5), resulting in shorter output lengths    queries on the datasets shown in Fig 3a. Our Cache (GGR)
similar to filter queries. We construct two example aggrega-      approach achieves a 2.1 – 3.8× speed-up over No Cache by
tion queries on Movies and Products datasets.                     caching repeated prefixes from system prompts and input
(T5) Retrieval-augmented generation (RAG). RAG queries            data. Cache (Original) with prompt caching enabled can
involve fetching external knowledge as context, such as           achieve a modest speedup of 1.03 – 1.9× over No Cache by
retrieving relevant document segments before generating           reusing instruction prompts and repeated values from the
answers. We evaluate FEVER and SQuAD datasets, fetch-             default input table. For queries with short decode stages,
ing 4 contexts for FEVER and 5 contexts for SQuAD for             the primary benefit of prompt caching is the saved prefill
question answering.                                               computations. Our Cache (GGR) algorithm further reduces
   1
     We use Posts and Comments table joined by PostID from the    end-to-end latency by 1.8 – 3.0× over Cache (Original)
BIRD dataset.                                                     through reordering rows and fields in the input table to
                                                   Optimizing LLM Queries in Relational Data Analytics Workloads

                              No Cache           Cache (Original)            Cache (GGR)                                        No Cache                   Cache (Original)             Cache (GGR)
              6000
                                                                                                          10000                                                                    1.9x
                                                                                                                                                                                       1.8x
                                                                                                                                                                                              1.8x
Runtime (s)




                                                 2.7x 2.6x                                                                                                                                        1.7x




                                                                                            Runtime (s)
              4000                                                                                         7500
                                                                                                                                                3.7x3.4x
                                                               2.1x                                                                  2.6x2.4x
                                                                      1.8x                                 5000                                             1.9x1.9x
                                   3.8x                                       3.8x
              2000   3.8x                 3.0x                                                                                                                          2.4x
                            2.5x                                                     2.0x                  2500          3.3x2.4x                                           1.5x

                 0    Movies        Products        BIRD            PDMX         Beer                            0       Movies Products BIRD                PDMX         Beer      FEVER SQuAD

                                          (a) Filter Queries                                                                        (b) Projection and RAG Queries
 Figure 3. End-to-end Result (Filter, Selection, RAG): Our optimizations Cache (GGR) achieve 1.5 – 3.4× speed-up in end-to-end runtime
 over caching without reordering (Cache (Original)), and 1.8 – 3.8× over No Cache baseline.

  maximize prefix reuse.                                                                                                            No Cache               Cache (Original)             Cache (GGR)
                                                                                                                 6000
                                                                                                                                                2.8x
 Most review datasets, such as Movies, Products, and BIRD,
                                                                                                                                                    2.2x




                                                                                                   Runtime (s)
 contain highly distinct values in the first few default fields                                                  4000
 due to the joining of reviews with metadata tables. For in-
 stance, these tables often begin with a review content                                                                     2.7x                                                        3.7x
                                                                                                                 2000
                                                                                                                                1.7x                                   3.5x                 2.8x
 field. Our algorithm prioritizes fields with repeated values,                                                                                                             2.5x
 like description and product title, leading to a                                                                    0
 57 – 74% increase in prefix hit rates and a 2.5 – 3× speed-up                                                            Movies (T3) Products (T3) Movies (T4) Products (T4)
 over the original ordering. PDMX is a dataset containing 57                                   Figure 4. End-to-end Result (Multi-LLM Invocation, Aggrega-
 fields with many unique, lengthy text entries. In this dataset,                               tion): Our optimizations Cache (GGR) achieve 1.7 - 2.8× speed-up
 our algorithm raises the hit rate from an initial 12% to 57%,                                 over Cache (Original), and 2.7 - 3.7× speed-up over No Cache.
 resulting in a 1.8× reduction in end-to-end latency. This
 lower speed-up is due to the nature of long input and 43%                                            Method Movies Prods. BIRD PDMX Beer FEVER SQuAD
 of cache miss from this dataset even for Cache (GGR). The                                            Original 35%                        27%       10%         12% 50%              11%           11%
 Beer dataset contains some duplicated values in early fields                                         GGR      86%                        83%       85%         57% 80%              67%           70%
 like review/profileName and Cache (Original) can
 achieve an initial hit rate of 50%. Cache (GGR) can further                                   Table 2. PHR (%) of LLM Filter and RAG queries for Original and
 increase the hit rate by an additional 30% to reach 80% and                                   GGR, which achieves 30 – 75% higher hit rates.
 achieve a 2× speedup.
 LLM projection. This query type applies the LLM to the
                                                                                               a 2.7× and 2.8× speedup over the No Cache baseline for
 selected data for a specific task, producing longer outputs
                                                                                               Movies and Products, respectively. Compared to Cache
 ranging from 29 to 107 tokens (see Table 1). For example,
                                                                                               (Original), Cache (GGR) attains a speedup of 1.7× and
 LLMs can be used to summarize the positive aspects of
                                                                                               2.2×. The relative speedup compared to Cache (Original)
 movies leading to favorable ratings in the Movies dataset.
                                                                                               reduces for both datasets compared to Filter and Projection
 As shown in Fig 3b, for datasets except for SQuAD and
                                                                                               queries. This is because the first LLM invocation for filter-
 FEVER (i.e. RAG queries), Cache (GGR) achieves 2.4×
                                                                                               ing is over distinct reviews for sentiment analysis, so Cache
 to 3.7× speed-up over No Cache, and 1.5× to 3.4× speed-
                                                                                               (Original) and Cache (GGR) performance will be similar,
 up over Cache (Original). Notice that as the output token
                                                                                               reducing the overall benefits. For Movies, this number re-
 length increases, query execution time across all baselines
                                                                                               duces from 2.5× to 1.7× as the first invocation accounts
 also grows. In cases where the decode stage dominates,
                                                                                               for nearly half the query time; while for Products, the sec-
 benefits from prefill caching are less pronounced, leading
                                                                                               ond invocation on Projection dominates runtime due to long
 to smaller relative performance gains than with LLM Filter
                                                                                               decode output length (i.e., around 107), so we can still see
 queries with shorter output length. However, for datasets
                                                                                               2.2× speed-up over Cache (Original).
 like BIRD and PDMX, which contain long strings, prompt
                                                                                               LLM aggregation. This query type uses AVG operator to
 caching saves memory during the decode stage, making the
                                                                                               aggregate the sentiment score on the reviews column with
 speedup more noticeable with longer decode times.
                                                                                               additional columns provided as context. We achieve a 3.5×
 Multi-LLM invocation. This query type combines Filter and
                                                                                               speed-up in the Movies dataset and a 3.7× speed-up in
 Selection operations, beginning with an initial LLM filter
                                                                                               the Products dataset over the No Cache baseline. We also
 (e.g., selecting positive reviews), followed by an LLM sum-
                                                                                               achieve 2.5× speed-up on Movies and 2.8× speed-up on
 marization of the filtered table. Applied to the Movies and
                                                                                               Products over Cache (Original). The results of this query
 Products datasets, as shown in Fig 4, Cache (GGR) achieves
                                                                                               type are similar to filtering query results, as the average
                                          Optimizing LLM Queries in Relational Data Analytics Workloads

                               Cache (Original)       Cache (GGR)          Dataset Model Method PHR (%) Cost ($) Savings (%)
                15000                                                                      Original  0.0 0.81         -
                                           2.6x                                    4o-mini
                                                                                            GGR     62.2 0.55       32%
  Runtime (s)


                10000                                                      FEVER
                                                   1.9x                            Sonnet
                                                                                           Original  0.0 5.49         -
                                 3.3x                                                       GGR     30.6 4.33       21%
                 5000   3.2x                               2.2x
                                                                          Table 3. OpenAI and Anthropic Costs: cache hit rate (HR%), cost,
                    0                                                     and savings comparison of GGR over Original for GPT-4o-mini
                        Movies Products    BIRD    PDMX     Beer
                                                                          and Claude 3.5 Sonnet in FEVER.
Figure 5. Cache (GGR) is able to achieve 1.9 – 3.3× speed-up over
Cache (Original) for filter queries on Llama3-70B.                                             PHR (%)         Est. Cost Savings (%)
                                                                           Dataset
output length is similar.                                                                Original      GGR OpenAI          Anthropic
RAG. This query is performed on a table of questions and                   Movies          34.6        85.7      31            73
the top four to five supporting evidence items extracted from              Products        26.7        83.3      33            73
the FEVER and SQuAD datasets. Cache (GGR) achieves                         BIRD            10.4        84.8      39            79
                                                                           PDMX            11.8        56.6      24            48
a 1.9× speed-up on both FEVER and SQuAD over the
                                                                           Beer            49.9        80.1      20            55
No Cache baseline. We also achieve a 1.8× speed-up on                      FEVER           11.2        67.4      30            60
FEVER and 1.7× on SQuAD over Cache (Original). In                          SQuAD           11.0        69.7      31            63
this experiment, we embed all supporting contexts for a
                                                                          Table 4. Estimated cost savings: across datasets using PHR from
question/claim into a vector index. We perform a K-nearest
                                                                          Sec 6.2 and OpenAI and Anthropic’s pricing model.
neighbor search on the vector index for each question to
fetch relevant contexts. At runtime, we apply our GGR                     Since both OpenAI and Anthropic require a minimum prefix
algorithm to the table of questions and contexts to maximize              length of 1,024 tokens for caching, we duplicate each field
cache hits. Cache (GGR) can achieve 56 – 59% prefix                       value five times, approximating a more realistic dataset
hit rate improvements over Cache (Original), as multiple                  with detailed conversations and descriptions. We select the
questions might share similar contexts, and Cache (GGR)                   FEVER dataset for its long input length and use 1000 rows
can rearrange contexts to maximize prefix reuse.                          from this dataset. For Anthropic experiments, we specify
Results on Different Model Sizes Fig 5 shows the evalu-                   cache write for only the first 1,024 tokens per request as
ation of our Cache (GGR) method compared with Cache                       a conservative assumption, as Anthropic does not support
(original) on filtering queries, using Llama-3-70B-Instruct               automatic prefix detection.
with 70B parameters. We run this model on an 8×L4 in-
                                                                          We evaluate GGR reordering on two tables submitted to the
stance with tensor parallelism and measure the end-to-end
                                                                          OpenAI and Anthropic APIs (each row is a request): one
query latency. Cache (GGR) achieves 1.9× to 3.3× speed-
                                                                          reordered with GGR and one in the original row and field
up under this setup, showing a trend similar compared to the
                                                                          order. Table 3 shows that GGR achieves 32% cost savings
Llama-3-8B model. We evaluate the larger model accuracy
                                                                          with GPT-4o-mini and 21% savings with Claude 3.5 Sonnet.
on LLM Filter queries in Sec 6.4. We also show results for
                                                                          The hit rate in OpenAI for GGR-reordered table is 62.2%,
the smaller 1B model in Appendix D.2.
                                                                          closely matching the hit rate (i.e., 67%) measured from our
6.3             Cost Savings on Proprietary API Endpoints                 previous experiment in Table 2. Original ordering receives
                                                                          no cached tokens with 0% cache hits, as the shared prefix
This section evaluates the cost efficiency of our GGR al-                 does not meet the 1,024-token minimum. The Anthropic
gorithm with closed models that support prompt caching.                   cache hit rate is around 30.6%, two times lower than the
For OpenAI, cached prompts are offered at a 50% discount                  OpenAI hit rate due to our conservative caching threshold.
compared to uncached prompts. Anthropic beta prompt
caching (ant, 2024) requires users to manually specify                    Assume that in the future, automatic prefix caching is en-
prompts to cache. Writing to the cache costs 25% more                     abled and prompts can be cached at arbitrary token lengths.
than the base input token price for any given model while                 We use the hit rate numbers collected from our previous
using cached content costs only 10% of the base rate. We                  experiments in Table 2 to simulate cost-saving ratios achiev-
evaluate OpenAI GPT-4o-mini and Anthropic Claude 3.5                      able by GGR, compared to the original unordered algorithm.
Sonnet, using their pricing models in our cost calculations.23            GGR yields 20 to 39% cost savings under the OpenAI pric-
                                                                          ing model and up to 79% cost savings with Anthropic.
      2
      GPT-4o-mini charges $0.075/1M tokens for cached tokens
versus $0.15/1M tokens for uncached tokens.                               6.4   Impact of Reordering on Accuracy
    3
      Claude 3.5 Sonnet standard input tokens are priced at $3 per
million tokens, cache writes at $3.75 per million, and cache reads        As GGR order alters the input prompt to the LLM, we as-
at $0.30 per million tokens.                                              sess the impact this has on query accuracy using LLM Filter
                                                Optimizing LLM Queries in Relational Data Analytics Workloads

                              Original          GGR                                           Original          GGR                                           Original          GGR
           100%                                                            100%                                                           100%




                                                                Accuracy




                                                                                                                               Accuracy
Accuracy




           80%                                                             80%                                                            80%
                                                       +14.2%
           60%                                                             60%                                                            60%
                  +3%   -1%   +0%        +1%    -6%                               +4%   +1%   +1%        -1%    -3%    +1.7%                     -3%    -2%    -1%       +4%    -3%    -2.4%
              Movies Products BIRD       PDMX   Beer   FEVER                  Movies Products BIRD       PDMX   Beer   FEVER                 Movies Products BIRD        PDMX   Beer   FEVER

                  (a) Meta-Llama-3-8B-Instruct                                    (b) Meta-Llama-3-70B-Instruct                                        (c) OpenAI GPT-4o
 Figure 6. Accuracy of original v.s. GGR ordering: we perform statistical bootstrapping to get a distribution of exact match accuracy
 measurements across 10,000 runs. The numbers indicate the difference in the median accuracy of GGR compared to the original ordering.

                                                                                                     7      R ELATED W ORK
                         Solver Time (s)
       Movies Products BIRD PDMX Beer FEVER SQuAD                                                    Our optimizations build on recent work in LLM inference
        3.3     4.5     1.2    12.6    8.0 5.6 4.5                                                   as well as prior work integrating machine learning and data
                                                                                                     management. We describe several major related areas below.
Table 5. GGR Solver time (s): GGR runs under 15 seconds for
                                                                                                     Inference-optimized systems. There has been a recent
datasets with up to 30K rows and 57 fields.
                                                                                                     rise of dedicated systems for LLM inference, including
 queries (Sec 6.1.2) with constrained output. We also eval-                                          FasterTransformer (NVIDIA, 2023a), Orca (Yu et al., 2022),
 uate a RAG query of FEVER, excluding SQuAD due to                                                   vLLM (Kwon et al., 2023), and SGLang (Zheng et al., 2023).
 its open-ended questions. FEVER includes ground-truth la-                                           Many systems already explore developing memory-efficient
 bels for all records, while 100 rows from other datasets are                                        GPU kernels that perform inference while leveraging shared
 manually labeled. Using statistical boostrapping (Wilcox,                                           prefixes. SGLang’s RadixAttention (Zheng et al., 2023),
 2003), we perform 10K runs, sampling with replacement on                                            Hydragen (Juravsky et al., 2024), and Cascade Inference
 each run to obtain a distribution of accuracy results. Accu-                                        (Ye et al., 2024) all implement optimized kernels. Our work
 racy experiments are conducted with Llama-3-8B-Instruct,                                            builds upon prior work investigating high-throughput LLM
 Llama-3-70B-Instruct, and GPT-4o models, measured as                                                inference and prefix caching for model serving. In addition,
 the percentage of exact matches between the LLM output                                              we leverage full workload information from batch queries
 and the ground truth labels.                                                                        to further improve performance in relational workloads.
 In Fig 6, we plot the accuracy distributions across the boot-                                       LLMs in Relational Data Analytics Many systems support
 strap runs and the relative difference in median accuracy                                           calling LLMs as operators on relational data, spanning from
 of GGR versus original ordering. The accuracy distribu-                                             production database vendors like Databricks (dat), Google
 tion of GGR ordering is within 5% accuracy of the original                                          BigQuery (goo) and AWS Redshift (aws) to programming
 ordering, with the only exception being FEVER with Llama-                                           frameworks like LOTUS (Patel et al., 2024). While these
 3-8B, where the ordering with GGR performs 14.2% better                                             works provide APIs for running LLMs over relational data,
 than the original. This is due to the GGR algorithm places                                          they do not explore how reordering data can optimize KV
 the “claim” field at the end of the prompt instead of at the                                        cache hits. There is also a line of work (Kang et al., 2017;
 beginning, which Llama3-8B prefers. However, the same                                               Lu et al., 2018) that explores using cheaper models for
 behavior does not hold for the larger models. Overall, we                                           approximate query generation. This orthogonal direction is
 can see that larger models like Llama-3-70B and GPT-4o are                                          not considered in our paper scope, as our work specifically
 within 5% of accuracy difference compared with original                                             focuses on calling LLMs as functions from inside a regular,
 ordering and are more robust to field reordering.                                                   given SQL query.

 6.5          Algorithm Overhead                                                                     8      C ONCLUSION
 Latency Table 5 shows the average overheads of GGR                                                  In this paper, we introduce techniques to optimize LLM in-
 across datasets, using a row recursion depth of four and                                            vocations in relational data analytics workloads. By leverag-
 column recursion depth of two, or an early stopping thresh-                                         ing workload information coupled with observations about
 old of 0.1M hit count. In all cases, GGR runs in under 15                                           the LLM inference process, we can significantly improve
 seconds – less than 0.01% of LLM query runtimes.                                                    end-to-end query performance and reduce costs without af-
 Memory GGR only requires the input table T (n rows, m                                               fecting query semantics. Our technique achieves up to 3.4×
 columns) touched by the query to be loaded into memory.                                             decreases in end-to-end query latency with Llama-3-8B and
 Recursive splitting reduces table size at each step, keeping                                        Llama-3-70B and also achieves up to 32% cost savings
 total memory usage at O(n × m), aside from minimal stack                                            under OpenAI and Anthropic pricing models.
 and temporary variable overhead.
                              Optimizing LLM Queries in Relational Data Analytics Workloads

ACKNOWLEDGEMENT                                                 Chen, T., Zhang, N. L., Liu, T., Poon, K. M., and Wang, Y.
                                                                  Model-based multidimensional clustering of categorical
We thank Soujanya Ponnapalli for helpful discussions and          data. Artificial Intelligence, 176(1):2246–2269, 2012.
feedback, and Jelani Nelson for academic advising. This
research was supported by gifts from Accenture, AMD,            Džeroski, S. Multi-relational data mining: an introduction.
Anyscale, Broadcom Inc., Google, IBM, Intel, Intesa San-          ACM SIGKDD Explorations Newsletter, 5(1):1–16, 2003.
paolo, Lambda, Mibura Inc, Samsung SDS, and SAP.
                                                                Gim, I., Chen, G., seob Lee, S., Sarda, N., Khandelwal,
                                                                  A., and Zhong, L. Prompt cache: Modular attention
R EFERENCES                                                       reuse for low-latency inference, 2024. URL https:
Large Language Models for sentiment analysis with Ama-            //arxiv.org/abs/2311.04934.
  zon Redshift ML (Preview) — Amazon Web Services
 — aws.amazon.com. https://aws.amazon.com/                      He, R. and McAuley, J. Ups and downs: Modeling
  blogs/big-data/large-language-models-                           the visual evolution of fashion trends with one-class
  for-sentiment-analysis-with-amazon-                             collaborative filtering. In Proceedings of the 25th In-
  redshift-ml-preview/. [Accessed 01-03-2024].                    ternational Conference on World Wide Web, WWW
                                                                 ’16, pp. 507–517, Republic and Canton of Geneva,
AI Functions on Databricks — docs.databricks.com.                 CHE, 2016. International World Wide Web Confer-
  https://docs.databricks.com/en/large-                           ences Steering Committee. ISBN 9781450341431. doi:
  language-models/ai-functions.html. [Ac-                        10.1145/2872427.2883037. URL https://doi.org/
  cessed 01-03-2024].                                             10.1145/2872427.2883037.
LLM with Vertex AI only using SQL queries in BigQuery           Huggingface. Text Generation Inference, 2023. URL
 — Google Cloud Blog — cloud.google.com. https:                   https://huggingface.co/docs/text-
  //cloud.google.com/blog/products/ai-                            generation-inference/en/index.
  machine-learning/llm-with-vertex-ai-
  only-using-sql-queries-in-bigquery.                           Idreos, S., Kersten, M. L., Manegold, S., et al. Database
  [Accessed 01-03-2024].                                          cracking. In CIDR, volume 7, pp. 68–78, 2007.
Prompt caching with claude.      https://                       Ilyas, I. F., Markl, V., Haas, P., Brown, P., and Aboulnaga,
  www.anthropic.com/news/prompt-caching,                           A. Cords: Automatic discovery of correlations and soft
  2024.                                                            functional dependencies. In Proceedings of the 2004
                                                                   ACM SIGMOD international conference on Management
Context caching. https://ai.google.dev/gemini-
                                                                   of data, pp. 647–658, 2004.
  api/docs/caching?lang=python, 2024.
Apr 2024. URL https://ai.meta.com/blog/meta-                    Johnson, J., Douze, M., and Jégou, H. Billion-scale similar-
  llama-3/.                                                       ity search with GPUs. IEEE Transactions on Big Data, 7
                                                                  (3):535–547, 2019.
Armbrust, M., Xin, R. S., Lian, C., Huai, Y., Liu,
  D., Bradley, J. K., Meng, X., Kaftan, T., Franklin,           Juravsky, J., Brown, B., Ehrlich, R., Fu, D. Y., Ré, C., and
  M. J., Ghodsi, A., and Zaharia, M.      Spark sql:              Mirhoseini, A. Hydragen: High-throughput llm inference
  Relational data processing in spark.  In Proceed-               with shared prefixes, 2024.
  ings of the 2015 ACM SIGMOD International Con-
                                                                Kang, D., Emmons, J., Abuzaid, F., Bailis, P., and Zaharia,
  ference on Management of Data, SIGMOD ’15, pp.
                                                                  M. Noscope: optimizing neural network queries over
  1383–1394, New York, NY, USA, 2015. Association
                                                                  video at scale. Proc. VLDB Endow., 10(11):1586–1597,
  for Computing Machinery. ISBN 9781450327589. doi:
                                                                  aug 2017. ISSN 2150-8097.
  10.1145/2723372.2742797. URL https://doi.org/
  10.1145/2723372.2742797.                                      Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,
Armbrust, M., Das, T., Sun, L., Yavuz, B., Zhu, S., Murthy,      C. H., Gonzalez, J., Zhang, H., and Stoica, I. Efficient
  M., Torres, J., van Hovell, H., Ionescu, A., Łuszczak, A.,     memory management for large language model serving
  et al. Delta lake: high-performance acid table storage over    with pagedattention. In Proceedings of the 29th Sym-
  cloud object stores. Proceedings of the VLDB Endowment,        posium on Operating Systems Principles, SOSP ’23, pp.
  13(12):3411–3424, 2020.                                        611–626, New York, NY, USA, 2023. Association for
                                                                 Computing Machinery. ISBN 9798400702297. doi:
Chase, H. LangChain, October 2022. URL https://                  10.1145/3600006.3613165. URL https://doi.org/
  github.com/langchain-ai/langchain.                             10.1145/3600006.3613165.
                                Optimizing LLM Queries in Relational Data Analytics Workloads

Lemire, D. and Kaser, O. Reordering columns for smaller in-         Stonebraker, M., Abadi, D. J., Batkin, A., Chen, X., Cher-
  dexes. Information Sciences, 181(12):2550–2570, 2011.               niack, M., Ferreira, M., Lau, E., Lin, A., Madden, S.,
                                                                      O’Neil, E., et al. C-store: a column-oriented dbms.
Lewis, P., Perez, E., Piktus, A., Petroni, F., Karpukhin,             In Making Databases Work: the Pragmatic Wisdom of
  V., Goyal, N., Küttler, H., Lewis, M., tau Yih, W.,                Michael Stonebraker, pp. 491–518. 2018.
  Rocktäschel, T., Riedel, S., and Kiela, D. Retrieval-
  augmented generation for knowledge-intensive nlp tasks,           Thorne, J., Vlachos, A., Christodoulopoulos, C., and Mittal,
  2021.                                                               A. Fever: a large-scale dataset for fact extraction and
                                                                      verification, 2018. URL https://arxiv.org/abs/
Li, J., Hui, B., Qu, G., Yang, J., Li, B., Li, B., Wang, B., Qin,     1803.05355.
   B., Geng, R., Huo, N., et al. Can llm already serve as a
   database interface? a big bench for large-scale database         Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones,
   grounded text-to-sqls. Advances in Neural Information              L., Gomez, A. N., Kaiser, L., and Polosukhin, I. Attention
  Processing Systems, 36, 2024.                                       is all you need, 2023.

Li, Z., Zhang, X., Zhang, Y., Long, D., Xie, P., and Zhang,         Wilcox, R. R. Bootstrap confidence interval. In Applying
  M. Towards general text embeddings with multi-stage                Contemporary Statistical Techniques. Academic Press,
  contrastive learning. arXiv preprint arXiv:2308.03281,             2003.
  2023.
                                                                    Ye, Z., Lai, R., Lu, B.-R., Lin, C.-Y., Zheng, S., Chen, L.,
Long, P., Novack, Z., Berg-Kirkpatrick, T., and McAuley,              Chen, T., and Ceze, L. Cascade inference: Memory band-
  J. Pdmx: A large-scale public domain musicxml dataset               width efficient shared prefix batch decoding, February
  for symbolic music processing, 2024. URL https:                     2024. URL https://flashinfer.ai/2024/02/
  //arxiv.org/abs/2409.10831.                                         02/cascade-inference.html.

Lu, Y., Chowdhery, A., Kandula, S., and Chaudhuri, S. Ac-           Yu, G.-I., Jeong, J. S., Kim, G.-W., Kim, S., and
  celerating machine learning inference with probabilistic            Chun, B.-G. Orca: A distributed serving system
  predicates. In Proceedings of the 2018 International                for Transformer-Based generative models. In 16th
  Conference on Management of Data, SIGMOD ’18, pp.                   USENIX Symposium on Operating Systems Design and
  1493–1508, New York, NY, USA, 2018. Association for                 Implementation (OSDI 22), pp. 521–538, Carlsbad,
  Computing Machinery. ISBN 9781450347037.                            CA, July 2022. USENIX Association. ISBN 978-
                                                                      1-939133-28-1. URL https://www.usenix.org/
McAuley, J., Leskovec, J., and Jurafsky, D. Learning at-              conference/osdi22/presentation/yu.
 titudes and attributes from multi-aspect reviews, 2012.
 URL https://arxiv.org/abs/1210.3926.                               Zaharia, M., Chowdhury, M., Das, T., Dave, A., Ma, J.,
                                                                      McCauly, M., Franklin, M. J., Shenker, S., and Stoica,
NVIDIA. Faster Transformer, 2023a. URL https://                       I. Resilient distributed datasets: A {Fault-Tolerant} ab-
 github.com/NVIDIA/FasterTransformer.                                 straction for {In-Memory} cluster computing. In 9th
                                                                      USENIX symposium on networked systems design and
NVIDIA. TensorRT LLM, 2023b. URL https://                             implementation (NSDI 12), pp. 15–28, 2012.
 github.com/NVIDIA/TensorRT-LLM.
                                                                    Zheng, L., Yin, L., Xie, Z., Huang, J., Sun, C., Yu, C. H.,
OpenAI.    Pricing — openai.com.     https://                         Cao, S., Kozyrakis, C., Stoica, I., Gonzalez, J. E., Barrett,
  openai.com/pricing. [Accessed 01-03-2024].                          C., and Sheng, Y. Efficiently programming large language
                                                                      models using sglang, 2023.
Pang, B. and Lee, L. Seeing stars: Exploiting class relation-
  ships for sentiment categorization with respect to rating
  scales. In Proceedings of the ACL, 2005.
                                                                    A    Q UERY E XAMPLES
Patel, L., Jha, S., Guestrin, C., and Zaharia, M. Lotus:
                                                                    Our benchmark suite incorporates a broad range of query
  Enabling semantic queries with llms over tables of un-
                                                                    types. We show examples of each query type as follows.
  structured and structured data, 2024. URL https:
  //arxiv.org/abs/2407.11418.                                       LLM filter. This query type leverages LLM for filtering
                                                                    data within a WHERE clause. The LLM processes and ana-
Rajpurkar, P., Zhang, J., Lopyrev, K., and Liang, P. Squad:         lyzes information to meet some specified criteria, such as
  100,000+ questions for machine comprehension of text,             identifying whether a movie is suitable for kids. This query
  2016.                                                             type illustrates typical use cases in sentiment analysis and
                              Optimizing LLM Queries in Relational Data Analytics Workloads

content filtering, which are important for application tasks,   these scores to calculate an average sentiment for overall
such as customer feedback analysis and content moderation.      customer feedback. This query type is essential for tasks
                                                                that need to extract insights from complex textual data.
  SELECT t.movietitle
  FROM MOVIES
  WHERE LLM(                                                        SELECT AVG(
      'Given the following fields,                                      LLM(
      ,→  determine whether the movie is                                     'Rate sentiment in numerical
      ,→  suitable for kids. Answer ONLY                                     ,→  values from 1 (bad) to 5
      ,→  with "Yes" or "No".',                                              ,→  (good).',
      movieinfo,                                                             reviewcontent, movieinfo
      reviewcontent,                                                    )
      reviewtype,                                                   ) AS AverageScore
      movietitle                                                    FROM MOVIES
  ) = 'Yes'
                                                                Retrieval-augmented generation (RAG). This query type
LLM projection. This query type makes calls to an LLM           leverages external knowledge bases for enhanced LLM pro-
within a SELECT statement to process information from           cessing, enriching LLM queries with a broader context.
specified database column(s). It reflects common tasks in       It simulates use cases where queries need to pull in rele-
data analytics in which the LLM is used for summarization       vant information from external sources, such as document
and interpretation based on certain data attributes.            databases or knowledge graphs, to provide comprehensive
                                                                answers.
  SELECT LLM(
      'Given the following information,
      ,→  summarize good qualities in                               SELECT LLM(
      ,→  this movie that led to a                                      'Given a question and four
      ,→  favorable rating.',                                           ,→  supporting contexts, answer the
      reviewcontent, movieinfo                                          ,→  provided question.',
  )                                                                     ,→  VectorDB.search(question, k=4),
  FROM MOVIES                                                           ,→  question)
                                                                    FROM FEVER

Multi-LLM invocation. This query type involves multiple
LLM calls in different parts of the query and addresses sce-    B    DATASET I NFORMATION
narios in which several layers of data processing or analysis
                                                                We detail the fields and functional dependencies (FDs) used
are required. It represents advanced analytical tasks, such
                                                                for each dataset as follows.
as combining different data insights.

  SELECT LLM(                                                       MOVIES
      'Given the information about a
      ,→  movie, summarize the good                                 columns:
      ,→  qualities that led to a                                   genres, movieinfo, movietitle,
      ,→  favorable rating.',                                       productioncompany, reviewcontent,
      reviewtype,                                                   reviewtype, rottentomatoeslink,
      reviewcontent,                                                topcritic
      movieinfo,
      genres                                                        FDs:
  )                                                                 movieinfo, movietitle,
  FROM MOVIES                                                       rottentomatoeslink
  WHERE LLM(
      'Given the following review, answer
      ,→  whether the sentiment is
                                                                    PRODUCTS
      ,→  "POSITIVE" or "NEGATIVE".
      ,→  Respond ONLY with "POSITIVE" or                           columns:
      ,→  "NEGATIVE", in all caps.',                                description, id, parent_asin,
      reviewcontent                                                 product_title, rating, review_title,
  ) = 'NEGATIVE'                                                    text, verified_purchase
LLM aggregation. This query type incorporates an AVG
operator that incorporates LLM outputs into further query           FDs:
processing. For example, one could use LLMs to assign               parent_asin, product_title
sentiment scores to individual reviews and then aggregate
                  Optimizing LLM Queries in Relational Data Analytics Workloads

BIRD                                                  SQuAD
columns:                                              columns:
Body, PostDate, PostId, Text                          question, context1, context2,
                                                      context3, context4, context5
FDs:
Body, PostId                                          FDs: []


PDMX                                              C    P ROMPTS
columns:                                          We detail the system and user prompts for each query type
artistname, bestarrangement, bestpath,            and dataset as follows.
bestuniquearrangement, composername,
complexity, genre, grooveconsistency,                 System Prompt
groups, hasannotations, hascustomaudio,
hascustomvideo, haslyrics, hasmetadata,               You are a data analyst. Use the provided JSON data
                                                      to answer the user query based on the specified
haspaywall, id, isbestarrangement,                    fields. Respond with only the answer,
isbestpath, isbestuniquearrangement,                  no extra formatting.
isdraft, isofficial, isoriginal,
isuserpro, isuserpublisher, isuserstaff,              Answer the below query:
                                                      {QUERY}
license, licenseurl, metadata,
nannotations, ncomments, nfavorites,                  Given the following data:
nlyrics, notesperbar, nnotes, nratings,               {fields}
ntracks, ntokens, nviews, path,
pitchclassentropy, postdate, postid,
                                                      User Prompt - LLM Aggregation
publisher, rating, scaleconsistency,
songlength, songlengthbars,
                                                      MOVIES: Given the following fields of a movie
songlengthbeats, songlengthseconds,                   description and a user review, assign a sentiment
songname, subsetall, subsetdeduplicated,              score for the review out of 5. Answer with ONLY a
subsetrated, subsetrateddeduplicated,                 single integer between 1 (bad) and 5 (good).
subtitle, tags, text, title, tracks,
                                                      PRODUCTS: Given the following fields of a product
version                                               description and a user review, assign a sentiment
                                                      score for the review out of 5. Answer with ONLY a
                                                      single integer between 1 (bad) and 5 (good).
FDs:
[metadata, path],                                     User Prompt - Multi-LLM Invocation
[hasannotations, hasmetadata, isdraft,
isofficial, isuserpublisher, subsetall                MOVIES/PRODUCTS: Given the following review, answer
]                                                     whether the sentiment associated is 'POSITIVE' or
                                                      'NEGATIVE'. Answer in all caps with ONLY 'POSITIVE'
                                                      or 'NEGATIVE':


BEER                                                  User Prompt - LLM Filter

columns:                                              MOVIES: Given the following fields, answer in one
                                                      word, 'Yes' or 'No', whether the movie would be
beer/beerId, beer/name, beer/style,                   suitable for kids. Answer with ONLY 'Yes' or 'No'.
review/appearance, review/overall,
review/palate, review/profileName,                    PRODUCTS: Given the following fields determine if
review/taste, review/time                             the review speaks positively ('POSITIVE'),
                                                      negatively ('NEGATIVE'), or netural ('NEUTRAL')
                                                      about the product. Answer only 'POSITIVE',
FDs:                                                  'NEGATIVE', or 'NEUTRAL', nothing else.
[beer/beerId, beer/name]
                                                      BIRD: Given the following fields related to posts
                                                      in an online codebase community, answer whether the
                                                      post is related to statistics. Answer with only
FEVER                                                 'YES' or 'NO'.

                                                      PDMX: Based on following fields, answer 'YES' or
-- FEVER --                                           'NO' if any of the song information references a
columns:                                              specific individual. Answer only 'YES' or 'NO',
claim, evidence1, evidence2,                          nothing else.
evidence3, evidence4
                                                      BEER: Based on the beer descriptions, does this
                                                      beer have European origin? Answer 'YES' if it does
FDs: []                                               or 'NO' if it doesn't.
                              Optimizing LLM Queries in Relational Data Analytics Workloads

      User Prompt - LLM Projection                                 Dataset               PHR (%)           Solver Runtime (s)
                                                                                 OPHR GGR           Diff   OPHR      GGR
      MOVIES: Given information including movie
      descriptions and critic reviews, summarize the good          Movies-50      80.6     80.6     0%      2556      0.05
      qualities in this movie that led to a favorable
      rating. (also used in multi-invocation)
                                                                   Products-25    19.7     18.5    -1.2%     357      0.06
                                                                   BIRD-50        77.5     76.2    -1.3%    0.43      0.05
      PRODUCTS: Given the following fields related to              PDMX-25        29.4     28.6    -0.8%     822      0.05
      amazon products, summarize the product, then answer          Fever-50        7.3     6.9     -0.4%     110      0.23
      whether the product description is consistent with
      the quality expressed in the review. (also used
                                                                   Beer-10        25.7     25.6    -0.1%    1269      0.08
      in multi-invocation)                                         SQuAD-10       34.0     34.0     0%       1.6      0.05

      BIRD: Given the following fields related to posts
      in an online codebase community, summarize how the
                                                                Table 6. Comparison of Prefix Hit Rate (PHR) and solver runtime
      comment Text related to the post body.                    across datasets. GGR achieves near-optimal PHR while being
                                                                orders of magnitude faster than OPHR.
      PDMX: Given the following fields, provide an
      overview on the music type, and analyze the given
      scores. Give exactly 50 words of summary.
                                                                D.2   Results of Smaller Model
      BEER: Given the following fields, provide an
      high-level overview on the beer and review in a
      20 words paragraph.                                       To analyze the impact of using a smaller model, we run the
                                                                Filter Query described in Fig. 3a with the Llama-3.2-1B
                                                                model, using the same setup as with Llama-3 8B (i.e., single
      User Prompt - RAG                                         L4 instance), and compare the prefix hit rate and end-to-
                                                                end query execution time of GGR with the default vLLM
      FEVER: You are given 4 pieces of evidence as              baseline (i.e. Cache Original). The results are reported in
      {evidence1}, {evidence2}, {evidence3}, and
      {evidence4}. You are also given a claim as {claim}.       Table 7.
      Answer SUPPORTS if the pieces of evidence support
      the given {claim}, REFUTES if the evidence refutes
      the given {claim}, or NOT ENOUGH INFO if there is               Metric                  BIRD         Movies PDMX
      not enough information to answer. Your answer
      should just be SUPPORTS, REFUTES, or NOT ENOUGH                 Runtime (orig/GGR)          1.5×     1.3×     1.3×
      INFO and nothing else.                                          Orig PHR (%)                10.41    29.32    11.97
                                                                      GGR PHR (%)                 83.99    82.10    56.00
      SQuAD: Given a question and supporting contexts,
      answer the provided question.                                   Metric                 Products      BEER
                                                                      Runtime (orig/GGR)          1.4×     1.2×
                                                                      Orig PHR (%)                24.06    47.98
                                                                      GGR PHR (%)                 82.10    73.93
D      A BLATIONS
We present two sets of ablation experiments: one comparing      Table 7. Cache runtime ratio and prefix hit rate (PHR) (%) com-
                                                                parison between original and GGR ordering for Llama-3.2-1B.
the prefix hit rate (PHR) between GGR and an optimal
oracle, and another examining the impact of using a smaller
LLM model.                                                      We observe similar prefix hit rates with Llama-3.2-1B com-
                                                                pared to our previous 8B model runs. This consistency
D.1     PHR of GGR v.s. OPHR                                    arises from the effectiveness of GGR field reordering, which
                                                                converts non-reusable field contents (0 hits) into reusable
OPHR is a very expensive brute-force oracle algorithm that      prefixes within the cache. We also observe that under the
iterates through all possible combinations of value groups      same GPU instance setup (e.g., L4 with 24 GB memory),
and calculates the prefix hit count. In our empirical evalua-   larger models like Llama-8B (7.6 GB) exhibit larger relative
tion, it is impractical to run on larger datasets.              performance gains from GGR compared to smaller models
                                                                like Llama-1B (1.8 GB), despite seeing similar prefix hit
Thus, we test the first (10, 25, 50, 100, 200) rows for each
                                                                rates. This is because prefix caching benefits from reducing
dataset and terminate OPHR runs exceeding 2 hours, report-
                                                                computational overhead on shared prefixes and enabling
ing the result of the successful run with the most rows. For
                                                                larger batch sizes for LLM generation by reducing memory
PDMX, we reduce 57 columns to 10 to enable runs on even
                                                                usage through sharing. For smaller models, the availability
as few as 10 rows. The PHR (prefix hit rate) and solver
                                                                of ample GPU memory diminishes the relative impact of
runtime in seconds across datasets are reported in Table 6,
                                                                prefix caching, as larger batch sizes can be achieved without
with the dataset labeled as {dataset}-{#rows}.
                                                                relying on caching. But for larger models, or when there is
We can see that on these small samples of the datasets, our     less available GPU space, prefix caching benefits become
algorithm (GGR) achieves within 2% of the optimal, but          more pronounced.
can be up to hours faster on solver runtime.
