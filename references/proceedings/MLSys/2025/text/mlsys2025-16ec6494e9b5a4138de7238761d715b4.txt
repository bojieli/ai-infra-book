L EAN ATTENTION : H ARDWARE -AWARE S CALABLE ATTENTION M ECHANISM
             FOR THE D ECODE -P HASE OF T RANSFORMERS



           Rya Sanovar 1 Srikant Bharadwaj 1 Renee St. Amant 1 Victor Rühle 1 Saravan Rajmohan 1


                                                        A BSTRACT
     Transformer-based large language models are memory hungry and incur significant inference latencies even
     on cutting edge AI-accelerators, such as GPUs. Specifically, the time and memory complexity of the attention
     operation is quadratic in terms of the total context length, i.e., prompt and output tokens.
     To that end, we propose LeanAttention, a scalable, hardware-efficient, “exact” attention acceleration mechanism
     for the decode-phase of transformer-based models. LeanAttention enables scaling the attention mechanism for the
     challenging case of long context lengths by re-designing the attention execution flow for the decode-phase. As a
     result, we achieve an average of 1.73x speedup in attention execution compared to FlashDecoding, with up to
     2.18x speedup for 256k context length.

1    I NTRODUCTION                                                in a range of applications (e.g. RAG involving numerous
                                                                  long documents) that improve relevance, coherence, and
Transformer-based (Vaswani et al., 2017) language mod-            user experience.
els (Achiam et al., 2023; Touvron et al., 2023; Zhang et al.,
2022; Li et al., 2023; Chowdhery et al., 2023) have revolu-       To mitigate LLM scalability challenges, mechanisms like
tionized the field of natural language processing (NLP) and       FlashAttention (Dao et al., 2022) and FlashAttention-
found applications across diverse domains (Spataro & Inc.;        2/3 (Dao, 2023; Shah et al., 2024) have been developed.
Inc.). These powerful models, fueled by massive amounts           FlashAttention brings IO-awareness to attention computa-
of data and sophisticated architectures, have become indis-       tion by reducing slow reads and writes to and from the
pensable tools for tasks such as machine translation (Kenton      GPU’s global memory (Ivanov et al., 2021). Instead, it com-
& Toutanova, 2019), question answering (OpenAI), text             putes attention in the faster shared memory using a tiling
generation (OpenAI), and sentiment analysis.                      strategy. It allows for parallelization over batch size and
                                                                  number of heads. FlashAttention-2 builds on FlashAtten-
The core of the transformer architecture is the powerful          tion to further optimize attention computation by enabling
component of self-attention. However, execution of the            parallelization over input sequence length (or query length).
self-attention mechanism is slow and suffers from a large
memory footprint, especially when dealing with long con-          While these optimizations provide significant improvements,
texts. A standard implementation of self-attention has            these mechanisms only give performance benefits for a sub-
quadratic time and memory complexity with respect to to-          set of problem sizes (i.e. query length, context length, batch
tal sequence length, which leads to scalability challenges        size, and number of heads). Their utilization of underlying
as model sizes (Brown et al., 2020) and supported context         hardware resources is mostly optimized for problem sizes
lengths increase (Anthropic; Zhang et al., 2024; Liu et al.,      encountered in the prefill-phase of transformer-based mod-
2023). Despite these scalability challenges, we see a trend       els, and often results in critically low hardware utilization for
of state-of-the-art models supporting greater and greater         problem sizes found in the decode-phase (see Figure 1). By
context lengths, with some production models supporting           overlooking the distinct behavior of attention during the de-
contexts as long as hundreds of thousands of tokens. Support      code phase versus the prefill phase, these mechanisms miss
for long contexts can improve a model’s utility by enabling       out on potential performance gains that could be achieved
an increasingly rich context, which is particularly beneficial    by efficiently exploiting the parallelization capabilities of
     1
                                                                  the underlying hardware.
     Microsoft.     Correspondence to:          Rya Sanovar
<ryasanovar6@gmail.com>,           Srikant         Bharadwaj      In decoder-only transformer models, the inference pro-
<srikant.bharadwaj@microsoft.com>.                                cess for a single request involves multiple forward passes
                                                                  through the model where output tokens are generated se-
Proceedings of the 8 th MLSys Conference, Santa Clara, CA, USA,
2025. Copyright 2025 by the author(s).
                                                                  quentially (Kim et al., 2023). This inference procedure
                                                          LeanAttention

inherently comprises of two distinct computational phases
due to the practice of reusing (caching) the key-value ten-
sors of the previously computed tokens (Pope et al., 2023).
The first phase is the prefill-phase (also known as prompt-
computation phase) where attention is computed of the
entire input prompt against itself to generate the first out-
put token. It’s followed by the decode-phase, also known
as the token-generation phase, which executes in an auto-
regressive manner (Vaswani et al., 2017). The current token
computes attention against itself and the entire cached con-
text (kv-cache) of previous tokens in the sequence to produce
the next token. With the push towards longer contexts, this
cached context length can get extremely long, exceeding
more than hundreds of thousands of tokens in length (Li
et al., 2024; Zhang et al., 2024; Fu et al., 2024; Liu et al.,
2023). Despite state-of-the-art batching techniques (Yu
et al., 2022b) and attention partitioning mechanisms (Dao
et al., 2022; Dao, 2023; Dao et al.; Ye et al., 2024), the lack
of a smart parallelized execution of attention along this long
context makes the decode-phase slow, bound by memory
bandwidth (Williams et al., 2009) and capacity (Kim et al.,
2023).
Efficient parallelization of attention over the context di-
mension is highly necessary. Although mechanisms like
FlashDecoding (Dao et al.) and FlashInfer (Ye et al., 2024)
enable parallelization over context length, they do it via the
fixed-split partitioning strategy which provides the hardware
with imbalanced loads and as a consequence suffers from
hardware resource under-utilization that slows down atten-         Figure 1. Attention execution schedule of FlashAttention-2 (Dao,
tion computation. Further, attention optimizations (Agrawal        2023), FlashDecoding (Dao et al.) (fixed-split), and LeanAttention
et al., 2024) are increasingly relying on batching requests        across a hypothetical 5 SM GPU for 2 heads. LeanAttention splits
of unequal context lengths to improve overall throughput,          the context into optimal LeanTiles (shown with 5 tiles per head).
but suffer from similar inefficiencies due of the partitioning           LeanAttention that always provides equal compute
strategies adopted by FlashAttention and FlashDecoding.                  loads to every compute unit in the hardware system (as
To address these limitations, we introduce LeanAttention,                shown in Figure 1) for any problem size, thus giving
a generalized exact-attention mechanism that enables effi-               near 100% hardware occupancy and delivering speedup
cient parallelization across all problem size dimensions and             irrespective of problem size and hardware architecture
provides the hardware with equalized loads by virtue of its              (subsection 4.3).
streaming nature. It ensures perfect quantization efficiency,          • Expatiate LeanAttention’s versatility and generalizabil-
i.e., 100% GPU occupancy for all problem sizes, delivers                 ity, where FlashAttention-2 and FlashDecoding can be
a runtime speedup during attention for long contexts, and                recovered as special cases of it. (subsection 4.3)
scales efficiently in multi-GPU scenarios with tensor paral-
lelism.                                                            LeanAttention results in an average of 1.73x latency
Overall, our contributions are as follows:                         speedup over FlashDecoding for the decode phase of
                                                                   transformer-based models and up to 2.18x speedup for 256k
   • Identify the limitations of state-of-the-art attention exe-   context length, while maintaining a near 100% GPU occu-
     cution optimizations on GPUs during the decode-phase          pancy irrespective of problem size.
     of transformer-based models. (subsection 3.2)
   • Identify the associative nature of the softmax re-            2    BACKGROUND
     scaling operator that enables it to function as a reduc-
     tive operator.                                                In this section, we provide the required background on Stan-
   • Leverage this crucial associativity property to employ        dard Attention (Vaswani et al., 2017) and FlashAttention-2
     a stream-K style (Osama et al., 2023) partitioning in         (Dao, 2023).
                                                       LeanAttention

2.1   Standard Attention                                                                              Operation Dimension
                                                                       Operation           Type
                                                                                                       Prefill     Decode
For a given input tensor with dimensions of batch size B,            query × key         MatMul      N ×d×N 1×d×N
query length Nq , key/value sequence length (also known                sof tmax          EleWise      N ×N         1×N
as context length) Nk , and hidden dimension D, multi-            attn score × value     MatMul      N ×N ×d 1×N ×d
head attention typically splits attention computation into
h number of heads along the hidden dimension, with each         Table 1. Operations in self-attention. Matrix multiplications are
head responsible for computing attention independently for      described in the M × N × K format.
a head dimension of size d = D/h.
The query and context lengths may not always be
equal (Pope et al., 2023). For instance, the prefill-phase      P matrices, FlashAttention employs kernel fusion of the
of generative decoder-only transformers such as GPT-            three operations shown in Equation 1: query × key Mat-
4 (Achiam et al., 2023) or Phi-2 (Li et al., 2023) has se-      Mul, softmax and attn score × value MatMul, effectively
quence lengths Nq = Nk = N , but in their decode-phase          avoiding intermediate global memory reads and writes. To
the context length increments by 1 after every autoregres-      this end, it employs the tiling strategy.
sive step of decode generation, while the query length (for     By utilizing the online softmax algorithm (Milakov &
a given batch instance) is the singular token that was gen-     Gimelshein, 2018), FlashAttention only requires a single
erated in the previous n-th time step, i.e., Nq = 1 and         pass over an entire row of tokens to compute their softmax,
Nk = N + n.                                                     bypassing the issue of a priori knowledge in standard atten-
The query matrix Q ∈ RNq ×d and key K and value V ma-           tion. This helps leverage the tiling strategy which partitions
trices ∈ RNk ×d are inputs to the following equation which      the attention output matrix O into independent output tiles
is computed independently by different batch instances          (i.e. the computation of an output tile is independent of the
and heads. The output matrix O ∈ RNq ×d is obtained             computation of other output tiles). A grid of cooperative
in essentially three steps as shown in Equation 1. Table 1      thread arrays (CTAs)1 is launched, each computing a given
summarises the three operations involved in self-attention      output tile of the output matrix O. The input matrices Q, K
along with their corresponding dimensions in both prefill       and V are partitioned into smaller tiles too. While comput-
and decode-phase at time step n = 0.                            ing the output tile corresponding to a given query tile, the
                                                                necessary key/value tiles are brought into shared memory in
                                                                a sequential manner and the output tile is iteratively updated
                                S
       S = QK T , P = sof tmax( √ ), O = P V             (1)    and corrected by the right scaling factors. This on-chip
                                 d                              updation avoids the need of storing the intermediate S and
                                                                P matrices in global memory. In addition to parallelizing
Standard attention implementation involves computing the        computation over batches and heads like FlashAttention,
large intermediate matrices, namely the attention score ma-     FlashAttention-2 further parallelizes over the query length
trix S ∈ RNq ×Nk and the softmax matrix P ∈ RNq ×Nk             dimension, as the attention computation of output tiles along
and storing them in global memory. These intermediate           this length is also independent. This results in a 2x speedup
matrices need to be stored in the global memory because         over FlashAttention.
the computation of the softmax matrix P requires a priori
knowledge of all tokens in a given row. Specifically, the       Thus, the tiling strategy ensures that the extra global mem-
row-wise maximum and exponential sum of tokens in a row         ory space required by FlashAttention-2 is O(Nq ) (needed to
need to be computed beforehand to calculate the softmax-ed      store the scaling factors for the backward pass), an impres-
value of each element in the row.                               sive improvement in memory footprint over the O(Nq ×Nk )
                                                                in traditional attention. The additional parallelism over
The computational complexity of standard attention is on        query length helps it reach 50-70% of peak theoretical
the order of O(Nq Nk d), with the two matrix multiplications    FLOPS/s and increases hardware occupancy in the prefill
(MatMul’s) contributing to the majority of it. Due to slow      phase. FlashAttention-2 was augmented to FlashAttention-
global memory access speeds, storing and retrieving these       3 (Shah et al., 2024), which is specifically fine-tuned for
intermediate matrices (Ivanov et al., 2021) results in long     execution on Hopper GPU’s (Luo et al., 2024) to exploit
latencies and incurs a large memory footprint, both in the      its low-precision and asynchronous hardware capabilities.
order of O(Nq Nk ).                                             Its optimizations are orthogonal to this work. Other related
                                                                techniques such as Ring Attention (Liu et al., 2023) and
2.2   Flash Attention-2                                         Striped Attention (Brandon et al., 2023), optimize inter-
To mitigate the memory footprint and access over-                   1
                                                                      Blocks of GPU threads are coscheduled in CTAs, which virtu-
heads (Ivanov et al., 2021) associated with storing the S and   alize the hardware’s streaming multiprocessor cores (SMs)
                                                         LeanAttention

GPU occupancy and are also orthogonal to the technique
presented in this work.

3     C HALLENGES I N T HE D ECODE P HASE
Prior to outlining our methodology for LeanAttention, to set
the stage for our approach, we delve into some of the chal-
lenges encountered in the decode phase of LLM inference,          Figure 2. Timeshare of decode attention compared to other stages
as well as the limitations of FlashAttention-2 optimizations      for different prompt sizes with 8:1 token ratio for Phi-3 Medium
in the decode phase.                                              model with single batch size.

3.1   Time Spent in Decode Phase
As we’ve discussed, modern generative LLM inference com-
prises of two computationally distinct phases: the prefill
phase followed by the decode phase. In the prefill phase,
self-attention is computed for the entire input prompt. The
query length Nq in this phase is the same as the context          Figure 3. Utilization of various resources on a single Nvidia-A100-
length Nk , i.e., (Nq = Nk = N ). Whereas, the decode             80GB GPU in LeanAttention compared to FlashDecoding at
phase begins generating each subsequent output token in           Heads=56 and BS=1 measured using Nsight Compute.
auto-regressive iterations. For each decode iteration, the
                                                                  and decode during attention. Even with a prompt input to
query length is a single token Nq = 1, and the context
                                                                  output token ratio of 8:1, more than 50% of the processing
length Nk could be very long, in the order of more than
                                                                  time is consumed by the decode phase, taking up to nearly
thousands of tokens depending on the auto-regressive step
                                                                  80% of the timeshare for longer prompt sizes. Additionally,
and input query length.
                                                                  other layers of the decode phase such as QKV and FFN
Figure 2 depicts the processing time breakdown of prefill         layers can be optimized using state-of-the-art MatMul par-
and decode, with their further breakdown into time spent in       titioning techniques like Stream-K. These operations are
the attention layer and the rest of the layers in a transformer   also typically quantized to lower data formats such as INT8
block (i.e., Q/K/V activation layers, feed-forward linear         to further enhance their efficiency. As a result, the decode
layers etc).                                                      attention operation can constitute up to 50-60% of the total
                                                                  duration of inference as shown.
While the large matrix multiplications found in the linear
layers of the prefill phase are heavily optimized (the entire
prefill phase taking up only 10% of the timeshare even for        3.2    Limitations of FlashAttention-2 for Decode
a high prompt:output ratio), the decode phase presents a          In both the prefill and decode phase, FlashAttention-2 tra-
different challenge. During the decode phase, where the           verses the context length dimension (Nk ) sequentially, i.e.,
query length is only 1 token long, linear layers perform          it updates the attention output for a given query tile by bring-
matrix multiplications on very narrow matrices which do           ing in the key/value tiles into shared memory in a sequential
not provide enough work to occupy the GPU. MatMul par-            manner. While FlashAttention-2 does parallelize over query
titioning strategies like Stream-K (Osama et al., 2023) can       length (Nq ) to increase SM occupancy, this additional mode
be leveraged to efficiently partition these narrow matrices       of parallelism has limited parallelization capacity in the
and accelerate their computation, preventing the linear lay-      decode phase where the query is a single token (Nq = 1).
ers from becoming a bottleneck during decode. However,            Not parallelizing attention computation along context length
the attention layer, with the existing attention partitioning     makes FlashAttention-2 (Dao, 2023) suffer from extremely
techniques (Dao et al., 2022; Dao, 2023; Dao et al.; Ye et al.,   low SM occupancy during decode as depicted in Figure 1.
2024) experiences longer latencies along with significant         This means that at any given point in time, the number of
under-utilization of hardware resources during this phase.        CTAs in flight on the GPU is directly proportional to the
This makes leveraging efficient parallelism along context         number of query tiles, and, therefore, to the query length-
length (Nk ) during attention a crucial aspect in increasing      regardless of the context length.
SM occupancy and reducing decode phase processing time.
                                                                  More explicitly, for a single batch instance, the maximum
As the number of output tokens generated increases, the con-      number of heads for state-of-the-art LLMs barely occupy the
text length becomes longer and thus the proportion of time        compute resources of modern hardware architecture systems
spent in decode relative to prefill becomes larger. Figure 2      during the decode phase where query length Nq = 1. For ex-
depicts this imbalance in processing time spent in prefill        ample, for a model with 128 heads, its decode phase would
                                                        LeanAttention

suffer from severe under-utilization of an 8 GPU A100 sys-       FlashDecoding++ (Hong et al., 2024) achieves speedup over
tem that has 864 compute cores at its disposal. Unlike the       FlashDecoding by approximating the softmax operation to
prefill phase, decode phase can offer parallelization only       remove the sequential dependencies it creates in attention.
across batch size and number of heads for FlashAttention-2.      Notably, this approach compromises on accuracy and its
                                                                 implementation is limited to certain model architectures.
Processor occupancy in FlashAttention-2 could be improved
                                                                 FlashDecoding++, as well as other techniques that focus
by increasing the batch size or number of heads, the other
                                                                 on softmax approximations to achieve speedup (like ConS-
two modes of parallelization it addresses. Intuitively, having
                                                                 max (Liu et al., 2024) and Softermax (Stevens et al., 2021)),
larger batch sizes in the decode phase could provide enough
                                                                 can be seamlessly integrated into LeanAttention.
work to every compute resource to fully occupy the GPU,
but this introduces other challenges and limitations. Due to     Despite these improvements, fixed-split used in these mech-
increasingly large model sizes, the need to independently        anisms (Dao et al.; Ye et al., 2024; Hong et al., 2024) is a
cache KV context for every batch instance would likely           non-optimal load balancing strategy. While this method of
exceed the memory capacity of the hardware system. More-         partitioning would improve speedup and occupy the GPU
over, scheduling overheads (Yu et al., 2022a) for efficiently    well for some attention workloads, it’s an inefficient strategy
batching queries along with the challenges of batching low       to adopt for the entire problem space because often results
SLA queries would increase inference latency and challenge       in partially full waves of attention computation that suffer
utilization.                                                     from quantization inefficiencies, i.e. low GPU occupancy
                                                                 due to imbalanced loads, and loses out on performance gains
Without having to resort to larger batch sizes as the sole
                                                                 it could get from the idle resources otherwise (depicted in
solution to resolving the GPU occupancy issue (which is
                                                                 Figure 1). While increasing the number of splits could help
limited by available memory capacity), the large context
                                                                 occupy the GPU better, it would result in reduction over-
length in the decode phase would benefit from partitioning
                                                                 heads that scale with the split factor and would allocate
its workload across different SMs efficiently. This motivates
                                                                 minimal work to each SM making it an inefficient use of
the need for smarter attention decomposition techniques
                                                                 register space.
which can efficiently distribute the workload across the
cores without resorting to larger batch sizes.                   While fixed-split partitioning along the context length in
                                                                 FlashDecoding does occupy a larger number of compute
3.3   Limitations of Related Work                                resources on the GPU compared to vanilla FlashAttention-
                                                                 2, its GPU occupancy varies greatly with problem size,
FlashDecoding, which is FlashAttention-2 with fixed-split        split factor and the number of compute units in the hard-
partitioning, has recently been proposed (Dao et al.; Hong       ware system as shown in Figure 3, making it unlikely for
et al., 2024; git), where attention computation is also parti-   FlashDecoding and its variants to reach perfect quantization
tioned along context length Nk . FlashInfer (Ye et al., 2024)    efficiency for all problem sizes and hardware systems. More-
implements an identical fixed-split partitioning of attention    over, the problem of quantization inefficiencies with these
for single-requests in the decode phase. For the case of         mechanisms would be particularly exacerbated in the com-
batched-requests in decode, FlashInfer implements an op-         mon cases of processing a batch of requests of heterogenous
timized version of PagedAttention for efficient KV cache         context lengths (Agrawal et al., 2024). In contrast, LeanAt-
storing and fetching.                                            tention, with its stream-K-style decomposition (discussed
Fixed-split is a general matrix multiplication decomposition     in section 4), will always provide well-balanced loads to
scheme that we briefly describe here. Given a MatMul com-        every compute unit in the hardware system to reach near
putation problem with matrices A (M × K) and B (N × K)           100% GPU occupancy for all problem sizes and hardware
to obtain a matrix C (M × N ) where C = AB T , to op-            architectures, making it perfectly adept at handling batched
timize concurrent computation, the fixed-split mechanism         requests of unequally sized contexts.
partitions the K-mode of the A and B matrices into s batches
based on a fixed splitting factor s provided dynamically at      4    L EAN ATTENTION
run time. This launches s times the CTAs as launched with-
out fixed-split, which are computing partial products of the     LeanAttention is an optimized scalable attention execution
output tiles of the C matrix concurrently. Fixed-split uti-      mechanism. It provides extensive parallelism across all
lizes the associativity of addition in the inner product of a    modes of the attention tensor, with well-balanced compu-
MatMul to later reduce or “fix-up” the partially computed        tation workloads to each CTA ensuring close to 100% SM
C matrices and produce the final C matrix. The concur-           occupancy while delivering a runtime speedup in attention
rency from fixed-split reduces latency and simultaneously        execution.
increases hardware occupancy at the cost of an additional        First, we identify that the associative property of softmax re-
reduction at the end.
                                                            LeanAttention

                                                                     duction to obtain one row vector of the attention score ma-
                                                                     trix S, of the form S(x) S(y) consisting of some un-
                                                                                                                                         (x)
                                                                     equal length vectors S(x) , S(y) where S(x) ∈ R1×Bc and
                                                                                    (y)                                     (x)
                                                                     S(y) ∈ R1×Bc , where 1 is the query length and Bc and
                                                                        (y)
                                                                     Bc are the unequal context lengths. The vectors S(x) and
                                                                     S(y) were computed from Q × (K(x) )T and Q × (K(y) )T
                                                                     (illustrated in Figure 4). Note that to generalize this proce-
                                                                     dure for blocks of any size, the context length of K(x) and
                                                                                  (x)       (y)
                                                                     K(y) are Bc and Bc and are not necessarily equal.
                                                                     The attention computation is split into two key parts. The
                                                                     first part involves calculation of an “un-scaled” version of
                                                                     O(i) (where i is either x or y) along with statistics m(i) and
                                                                     ℓ(i) :
                                                                                                                   (i)
                                                                                 S(i) = Q(K(i) )T ∈ R1×Bc
                                                                                 m(i) = rowmax(S(i)(i)
                                                                                                       ) ∈ R1×1
Figure 4. Illustrative diagram showing LeanAttention’s partition-                 (i)             S −m(i)
                                                                                 ℓ = rowsum(e              ) ∈ R1×1
ing strategy with two differently sized work volumes of a head                                                    (i)

assigned to different CTAs. The un-scaled outputs are indepen-                   A = exp(S − m ) ∈ R1×Bc
                                                                                   (i)        (i)      (i)
                                                                                   (i)    (i) (i)      1×d
                                                                                 Õ = A V ∈ R
dently computed and re-scaled later in a reduction operation. Note
that this can be generalized to any arbitrary-sized work volume
split.
                                                                     Softmax Re-scaling Operation. The second part involves
                                                                     re-scaling the “un-scaled” outputs O(i) using the previously
                                                                     computed statistics m(i) and ℓ(i) .
scaling enables us to treat it as a reduction operator along the     We define the softmax re-scaling operation f (x, y) for two
context length dimension and allows us to split the workload         intermediate outputs O(x) and O(y) as follows:
(i.e. KV tensors) of a single head into unequally sized blocks
if and when needed (described in subsection 4.1), while              m(x,y) = max(m(x) , m(y) )
                                                                                 (x)     (x,y)            (y)   (x,y)
maintaining the lossless exactness of attention.                     ℓ(x,y) = em −m            ℓ(x) + em −m           ℓ(y)
                                                                                           (x)    (x,y)                    (y) (x,y)
Second, we identify the smallest optimal granularity of de-          f (x, y) = diag(em −m              )Õ(x) + diag(em −m          )Õ(y)
                                                                                   (x,y)
composition in attention computation, termed as LeanTile             f (x, y) = Õ
(subsection 4.2), which can be linearly mapped on the hard-          O(x,y) = diag(ℓ(x,y) )−1 f (x, y)
ware resources in a flexible style akin to stream-k decom-
position of matrix multiplications subsection 4.3). Multiple         Proof of Associativity The associative nature of softmax
such LeanTiles belonging to either single or multiple atten-         re-scaling f (x, y) allows us to reduce intermediate outputs
tion outputs will constitute a workload assigned to a CTA.           produced from key/value vectors of different lengths in
                                                                     LeanAttention. We shall briefly prove that f (f (x, y), z) =
4.1   Softmax Re-scaling as Reduction                                f (x, f (y, z)) = f (x, y, z), where: f (x, y) = Õ(x,y) ,
                                                                     f (y, z) = Õ(y,z) and f (x, y, z) = Õ(x,y,z) .
LeanAttention’s decomposition results in splits of work for
a given SM that are not always equal in size, i.e., the K/V          Proving that f (f (x, y), z) = f (x, y, z):
tensors of a given query tile are not dispatched in same-sized                  f (x, y) = Õ(x,y)
                                                                                                       (x,y)
blocks to different SMs (unlike FlashDecoding (Dao et al.),                                                    −m((x,y),z)
                                                                            f (f (x, y), z) = diag(em                             )Õ(x,y)
FlashInfer (Ye et al., 2024)).                                                                         (z)
                                                                                                             −m((x,y),z)
                                                                                           + diag(em                        )Õ(z)
To reduce these partial attention outputs that result from dif-                                        (x,y)        (x,y,z)
                                                                                                               −m
ferently sized blocks, we use a softmax re-scaling operator.                               = diag(em                          )Õ(x,y)
This requires us to identify softmax re-scaling’s associativity                            + diag(em
                                                                                                       (z)
                                                                                                             −m(x,y,z)
                                                                                                                          )Õ(z)
property that allows it to correctly reduce blocks of unequal                                          (x,y)
                                                                                                               −m(x,y,z)
sizes, i.e., application of softmax re-scaling as a reduction                              = diag(em                          )
operator will give the same exact attention output with no                                              (x)
                                                                                                              −m (x,y)
                                                                                           × (diag(em                     )Õ(x)
loss in accuracy, regardless of the way the work might be                                              (y)
                                                                                                             −m(x,y)
split, whether in same-sized blocks or arbitrary differently                               + diag(em                     )Õ(y) )
sized blocks.                                                                                          (z)
                                                                                                             −m(x,y,z)
                                                                                           + diag(em                      )Õ(z)
Without loss of generality, we describe this process of re-
                                                                LeanAttention
                                  (x)
                                        −m(x,y,z)
                      = diag(em                        )Õ(x)           equally among all CTAs using a pre-determined optimal tile
                                  (y)
                                        −m   (x,y,z)                    size. It does this by rolling out the inner mode iterations of
                     + diag(em                         )Õ(y)           all output tiles and appending them to form a linear map-
                                  (z)      (x,y,z)
                                        −m
                     + diag(em                         )Õ(z)           ping. With the given grid size, it divides this total work into
                                                                        buckets demarcated appropriately such that each CTA has
                      = Õ(x,y,z) = f (x, y, z)
                                                                        equal amount of MAC operations to perform. The grid size
                                                                        is fixed for a given tile size as LeanAttention provides equal
Therefore, f (f (x, y), z) = f (x, y, z) and similarly                  work to all SMs. For example, for a tile of 256 tokens, two
ℓ((x,y),z) = ℓ(x,y,z) . For brevity, we omit the proof of               CTAs can be co-executed in a single wave with the available
f (x, f (y, z)) = f (x, y, z), but it can be deduced in a simi-         shared memory of A100 GPU. This would result in a total
lar manner.                                                             grid size of 108(N umSM s) × 2 = 216. Number of tiles
                                                                        to be computed by each CTA can be calculated as follows:
This associativity of softmax re-scaling is leveraged in
LeanAttention to concurrently calculate the “partial” out-                                   BatchSize × N umHeads × ContextLen
                                                                         T ilesP erCT A =                                          (2)
puts produced from unequally sized KV blocks and then                                       T ileSize × N umSM s × M axCT AsP erSM
“reduce” them to obtain exact attention.                                LeanAttention extends Stream-K style of linear mapping of
                                                                        iterations by rolling out LeanTile iterations in a similar fash-
4.2   LeanTile                                                          ion, assigning equal number of LeanTiles to every CTA as
To enable us to efficiently distribute the work of computing            shown in Figure 1. Each CTA’s range of LeanTile iterations
the attention output tiles, we define the smallest granularity          is mapped contiguously into a batch size →heads →context
of a KV block as a LeanTile. A single LeanTile iteration                length linearization, crossing the head and query boundary
computes “local attention” across a subset of tokens along              as it may. Should a given CTA’s starting and/or ending
the Nk dimension to generate an un-scaled attention output              LeanTile not coincide with the head’s boundary, it must con-
as shown in the grey box in Figure 5. Algorithm 2 (in                   solidate its partial output with those of the other CTA(s) also
Appendix B) depicts the detailed subroutine for computing               covering that head’s output tile. In our implementation of
the partial attention outputs for a sequence of LeanTiles.              LeanAttention, each attention output tile is consolidated by
This LeanTile() subroutine is called when computing each                the CTA that performed that output’s first LeanTile (called
partial output tile in a CTA launched in LeanAttention, as              as a host block). Before it can do so, it must accumulate
will be discussed later (Algorithm 1).                                  the un-scaled output tensors from the other CTA(s) (shown
                                                                        in Figure 1) in temporary global storage. The negligible
To efficiently split attention into smaller tiles, it is necessary      synchronization overhead of the original stream-K imple-
to identify the smallest tile size capable of achieving the             mentation also extends to LeanAttention, thus leading to
highest compute efficiency. LeanTile size depends on the                near 100% occupancy of SMs (not tensor core utilization)
computational power and memory access costs and, thus,                  during the execution of a single CTA. Note that the tem-
are fixed for a particular hardware architecture. After an              porary global storage overhead is minimal in the case of
extensive empirical sweep through various sizes for a Lean-             decode-phase where the output tensors are of dimensions
Tile, we found a tile size granularity of 256 and 128 tokens            1 × head dim, where head dim is typically in the range of
along the Nk dimension to be the most optimal for a head                64 to 256.
size of 64 and 128 respectively for FP16→32 problems
while experimenting on an A100 GPU (Choquette et al.,                   Since we distribute the overall attention problem into opti-
2021; Jia & Van Sandt, 2021). This optimal size can simi-               mal LeanTiles, we achieve a near 100% quantization effi-
larly be identified for other head dimensions and hardware              ciency irrespective of problem size (context length). This
architectures.                                                          cohesive implementation of parallel computation and re-
                                                                        duction happens in a single kernel launch in LeanAtten-
                                                                        tion, avoiding the reduction kernel launch overheads that
4.3   Decomposition and Mapping of LeanTiles
                                                                        FlashDecoding suffers from. A difference in Stream-K de-
Finally, LeanAttention uses a stream-K (Osama et al., 2023)             composition in LeanAttention is in the reduction or “fix-up”
style decomposition and mapping of these LeanTiles to                   phase. While Stream-K for MatMuls has addition as its
deliver efficient execution of attention.                               reductive operator, LeanAttention has softmax re-scaling as
                                                                        its reductive operator.
Stream-K Decomposition. Stream-K is a parallel decompo-                 Naturally, some CTAs will be computing LeanTile iterations
sition technique for dense matrix-matrix multiplication on              of more than one independent output tile. In such cases,
GPUs. Stream-K partitioning addresses the inefficiencies in             Stream-K’s equalized partitioning makes LeanAttention
fixed-split by dividing the total workload (MAC operations)             more adept for problem sizes which would not occupy
                                                           LeanAttention




Figure 5. Control and dataflow of a single CTA in LeanAttention utilizing various hardware resources. The tensors are loaded to shared
memory in a tiled manner. At the end of a head’s computation, a reduction is performed if it is a host CTA, else the partial un-scaled
results are written to memory before moving on to the next head.

the hardware well if executed using its counterparts,
FlashAttention-2 and FlashDecoding. To enable such a
smooth transition between tiles, the input tensor view is also
different in LeanAttention compared to FlashAttention-2.
This requires a constant stride moving between different
heads as we transition from the LeanTile of one head
to another requiring Q/K/V tensors be of the shape                   Figure 6. Illustrative diagram of LeanAttention’s optimal distribu-
(batch size, heads, query/ctx length, head dim)                      tion of work in the ragged batching case. Each SM receives equal
compared to FlashAttention-2’s requirement of                        amount of LeanTiles.
(batch size, query/ctx length, heads, head dim).
                                                                     tributed evenly across the grid, ensuring each CTA receives
With this execution design, we must point out that LeanAt-           the same number of LeanTiles to process. The range of
tention behaves as a versatile attention partitioning mecha-         LeanTile iterations assigned to each CTA is mapped con-
nism which generalizes to FlashAttention-2 when the num-             tiguously in a Heads → TotalContextLength linearization
ber of output tiles is equal to grid size, and generalizes to        and partial outputs for each head are consolidated in the
FlashDecoding when grid size is an even multiple of number           same manner as in the non-ragged case.
of output tiles. Finally, for all other cases (most common)
LeanAttention efficiently distributes the work across the            4.4   Execution Flow
compute resources available in the system. Thus, LeanAt-
tention will either always perform better or the same as             Algorithm 1 details LeanAttention’s execution. For a fixed
FlashAttention-2 and FlashDecoding.                                  grid size G, CTAs are launched and given equal amount
                                                                     of LeanTile iterations to work with (Line 7). Each CTA
Lean Ragged Batching.          For the special case of               computes the LeanTile() subroutine for every distinct output
dealing with unequal context lengths within a batch                  tile that comes under its boundaries (Line 16). Figure 5
of requests, the number of LeanTiles per request be-                 shows the execution flow of a single CTA computing partial
comes unique, resulting in a total workload that is                  and final attention outputs for the assigned heads.
smaller than the non-ragged case.        To account for
this difference, ragged KV tensor inputs to LeanAtten-               The unique reduction phase of LeanAttention, which is char-
tion are first prepared with unpadded dimensions of                  acterized by its softmax re-scaling operator, is performed by
(N umHeads, T otalContextLength, HeadDim), where                     the host CTA block (Lines 24-40). A host CTA (Line 17) is
T otalContextLength is the sum of all distinct context               the CTA responsible for computing the first ever LeanTile
lengths within the batch. The batch dimension is elimi-              for a given output tile, and it behaves as the reducing CTA
nated, and both batch indices and the true context lengths           during parallel reduction of partial un-scaled outputs.
of requests are tracked through pointers to a cumulative             All non-host CTAs will share their partials through a store
sequence length array for each input tensor. These point-            to global memory and signal their arrival (Lines 20-23). On
ers have size (BatchSize + 1) each, introducing minimal              the other hand, a host block which is also a non-finishing
memory overhead.                                                     block (Lines 24-25), needs to wait for other contributing
With the total workload of LeanTiles correctly determined,           peer CTA blocks to signal their completion (Line 28) and
Lean ragged batching functions identically to the non-               then proceed to carry out the reduction (Lines 29-35).
ragged case as shown in Figure 6. The workload is dis-               A CTA that’s computing the attention for a head exclusively
                                                         LeanAttention

completes all the LeanTile iterations for its output tile in a    5         E VALUATION M ETHODOLOGY
single CTA and can directly store its results from the register
file to global memory (Line 38-39) without any need for           Implementation. We implement LeanAttention using the
further reduction.                                                CUTE abstractions (CuTe, a;c;b) provided by Nvidia’s CUT-
                                                                  LASS library (Thakkar et al., 2023). For comparative
                                                                  measurements we utilize FlashAttention-2’s implementa-
                                                                  tion of FlashDecoding as it is available on their Github
Algorithm 1 Lean Attention                                        repository (git)2 and FlashInfer’s implementation from their
 1: shared O[Tm , d]                                              Github repository (Ye et al., 2024)3 . For the end-to-end
 2: shared m[Tm , 1]                                              inference results we use Llama, Mistral and Phi-3 models as
 3: shared l[Tm , 1]                                              available in the HuggingFace Transfomers repository (Wolf
 4: Number of output tiles: Cm = ⌈Nq /Tm ⌉                        et al., 2019) and modify them to allow execution via LeanAt-
 5: Number of iterations for each output tile: Cn =               tention wherever necessary. Note that optimizations such
    ⌈Nk /Tn ⌉                                                     as FlashAttention-3 (Shah et al., 2024) are orthogonal to
 6: Total number of iterations: I = Cm Cn                         this work and targeted specifically for H100s. The core
 7: Number of iterations per CTA: IG = I/G                        computation of LeanAttention can adopt FlashAttention-3’s
 8: fork CTAg in G do                                             optimizations for further benefits on Hopper GPUs (Luo
 9: cta start = g IG and cta end = cta start + IG                 et al., 2024).
10: for iter = cta start to cta end do
11:    Index of current output tile: tile idx = iter / Cn         System. We benchmark the attention mechanisms on a
12:    tile iter = tile idx ×Cn                                   Nvidia-A100-80GB-GPU (Choquette et al., 2021) system
13:    tile iter end = tile iter + Cn                             with up to 8 GPUs. We measure runtime using a single GPU
14:    local iter = iter - tile iter                              as well as 8xGPUs for larger models and context lengths. A
15:    local iter end = min(tile iter end, cta end) - tile iter   single A100 GPU consists of 108 SMs with an 80GB HBM
16:    O, m, l = LeanTile(tile idx, local iter, local iter end)   global memory. To demonstrate LeanAttention’s versatility
17:    host-block if: iter == tile iter                           across hardware architectures we benchmark it similarly on
18:    finishing-block if: cta end >= tile iter end               a single Nvidia-H100-SXM-80GB-GPU (Luo et al., 2024)
19:    if !(host-block) then                                      which has 132 streaming multiprocessors and an 80GB
20:       StorePartials(Op[g], O)                                 HBM global memory.
21:       StorePartials(mp[g], m)                                 Batching. The evaluations assume the same context length
22:       StorePartials(lp[g], l)                                 for all queries in a batch working in tandem with batch-
23:       Signal(flags[g])                                        ing techniques such as Orca (Yu et al., 2022a). However,
24:    else                                                       LeanAttention supports varied context length execution in-
25:       if !(finishing-block) then                              cluding heterogeneous batching such as prefill queries with
26:          last cta = tile iter end / Cn                        decode.
27:          for cta = (g + 1) to last cta do
28:              Wait(flags[cta])                                 Attention Mechanisms. In addition to FlashDecoding
29:              mcta = LoadPartials(mp[cta])                     (FD) (Dao et al.), we also benchmark FlashInfer (FI) (Ye
30:              lcta = LoadPartials(lp[cta])                     et al., 2024) for comparison against LeanAttention (LA).
31:              Octa = LoadPartials(Op[cta])
32:              mnew = max(mcta , m)                             6         E VALUATION R ESULTS
                                   new             new
33:              lnew = emcta −m lcta + em−m l
34:
                                     new
                 Onew = emcta −mi Octa + em−mi O
                                                     new
                                                                  In this section, we evaluate the impact of LeanAttention at
35:              Update m = mnew       , l = linew                the attention operation-level as well as end-to-end inference
                                  i
36:          end for                                              performance-level.
37:       end if
38:       Write O = diag(l)−1 O to GMEM.                          6.1       Benchmarking Attention - Decode-Phase
39:       Write L = m + log(l) to GMEM.                           We benchmark the runtime of just the attention operation
40:    end if                                                     at varying context lengths, number of attention heads, head
41:    iter = tile iter end                                       dimensions (64: default and 128), and inference batch sizes
42: end for
                                                                        2
43: join                                                               Version 2.5.6
                                                                        3
                                                                       Version 0.1.6 - Note that we increase the number of heads in
                                                                  the single batch decoding API to simulate batch size as the batch
                                                                  API performance is too low
                                                             LeanAttention




Figure 7. Speedup of LA over state-of-the-art attention execution mechanisms at different context lengths, batch sizes and attention heads
with head dimension = 64 on a single Nvidia-A100-80GB GPU.
                                                                       large model sizes, delivering more than 2x speedup over FD
                                                                       when there’s more than 24 heads in the model for a long
                                                                       256k context at batch size = 4. This shows that LA is able
                                                                       to scale well for both small and large model sizes.
                                                                       Effect of Batching. Figure 7(c) shows the performance
                                                                       improvement of LA at varying batch sizes. As expected, we
                                                                       observe that LA gives comparable speedup to FD and FI.
                                                                       This is because both FD and FI are able to employ a higher
                                                                       number of splits at smaller batch sizes to occupy all the SMs
Figure 8. Speedup of LA compared to state-of-the-art Attention         in the GPU. However, as batch size increases, FD selects
execution mechanisms at varying context lengths, at a fixed batch      fewer splits per head. For instance, FD doesn’t split at
size and attention heads with head dimension = 64 on a single          batch sizes over 4 in Figure 7(c) because the total number of
Nvidia-H100-SXM-80GB GPU.                                              heads in the batch exceed the number of SMs available in the
                                                                       system. As a result, it behaves like vanilla FlashAttention-2,
on a single Nvidia A100-80GB GPU and a single Nvidia                   missing out on potential performance gains by leaving some
H100-SXM-80GB GPU.                                                     SMs idle in its final partially full wave. Consequently, LA
Increasing Context Length. Figure 7(a) shows the speedup               achieves more than 1.5x speedup compared to FD through
of different attention mechanisms for a model with 32 heads            its stream-K-ed decomposition. A comprehensive analysis
with batch size = 4 on a single A100 GPU. LA delivers                  on ragged batching is also detailed in Appendix A.
close to 2x speedup compared to FD even at smaller context             Overall, we benchmarked the system on more than 1,000
lengths, reaching up to 2.18x speedup as the context lengths           samples of varying batch sizes, context lengths and attention
grows to 256k tokens. When context lengths exceed 16k,                 heads. On an A100 GPU, we observed an average speedup
we observe more than 1.46x speedup over FI. Note that                  of 1.73x over FD (Max: 2.18x for 56 heads, batch size 2,
while FI implements the fixed split mechanism like FD,                 256k context). On an H100 GPU, we recorded an average
it shows better performance over it due to efficient data              speedup of 1.52x over FD (Max: 2.53x for 48 heads, batch
movement and optimized tile size selection. Repeating a                size 6, 64k context).
similar exercise on an H100 GPU, we observe the speedups
of LA at batch size = 6 and 48 heads as shown in Figure 8.             Multi-GPU Execution. Repeating a similar benchmarking
LA delivers over 2x speedup even at a 4k context length,               process on an 8xA100 GPU system, we vary the context
reaching upto a maximum of 2.52x speedup at a 64k context              lengths from 1k to 1M, with 256 heads at a batch size of 4
length which more or less plateaus at the context lengths              as shown in Figure 9(a). LeanAttention reaches a speedup
increase.                                                              of more than 2x even at smaller contexts. This is because
                                                                       parallelizing only over the batch and heads (total heads
Increasing Attention Heads. Figure 7(b) shows the                      = 256×4 = 1024) does not provide sufficient work for each
speedup delivered by LA over FD and FI for models with                 SM (total SMs = 8 × 108 = 864) as 864 − (1024 − 864) =
an increasing number of heads. LA delivers comparable                  704 SMs remain idle in the last wave. Furthermore, FD
speedups to FD and FI at smaller model sizes. With fewer               behaves identically to vanilla FlashAttention-2, opting for
heads, FD and FI’s fixed-split mechanism can distribute the            a split factor of 1. In contrast, LeanAttention computes
workload as evenly as LA. However, as the number of heads              attention in fully quantized waves for all problem sizes.
increases, FD/FI resorts to fewer splits per head, resulting
in partially filled waves of attention on the SMs. In contrast,        To observe this effect in greater detail, we evaluate across
LA maintains even workload distribution at both small and              a varying number of heads in Figure 9(b) with a context
                                                           LeanAttention




Figure 9. Speedup of LA over state-of-the-art attention execution mechanisms at different context lengths, batch sizes, attention heads
with head dimension = 64 on an 8x Nvidia-A100-80GB system.
                                                                      delivers a speedup of 3.5x compared to FD at 128k context
                                                                      length. Even at smaller context lengths of 8k tokens, we
                                                                      observed an improved performance of 1.34x over FD.

                                                                      6.2   End-to-End Inference Performance
                                                                     We measure the end-to-end inference runtime using Phi-3
                                                                     Medium model (with 40 heads) as shown in Figure 11 at
                                                                     different prompt sizes with a prompt to output token ratio
                                                                     of 8:1. This includes the prefill stage latency as well as the
                                                                     total runtime of decode phase. LeanAttention offers a 1.12x
                                                                     speedup with Phi-3 Medium as compared to FlashDecod-
                                                                     ing for first 1k output tokens. However, LA offers higher
Figure 10. Speedup offered by LA for decode attention across         speedups as the output tokens increase beyond 16k deliver-
models, batch sizes, context lengths integrated with ONNXRT          ing an average of 1.73x speedup compared to FD. As we
(Note that ONNXRT has an older version of FlashDecode)               note, the inference-level runtime improvement delivered
                                                                     by LA will depend heavily on the number of heads, total
                                                                     context length, batch size etc.

                                                                      7     C ONCLUSION
                                                                     The attention mechanism in transformer-based language
                                                                     models is a slow and memory hungry process. State-of-the-
                                                                     art optimization kernels (Dao et al.; Ye et al., 2024; Dao,
                                                                     2023) have cleverly addressed this challenge but fail to adapt
Figure 11. End-to-End Speedup of LA compared to FD in ON-            to the computationally distinct phases of inference at long
NXRT running Phi-3 Medium model at different context lengths,        context lengths. To address this, we propose LeanAttention,
batch size = 1, prompt size : output tokens = 8 : 1                  a generalized exact attention execution mechanism that en-
                                                                     sures lower runtimes and almost 100% hardware occupancy
length of 256k and batch size = 4. We observe comparable             during attention irrespective of problem size. By leveraging
speedups to FD/FI at a smaller number of heads (64, 160).            the associative property of softmax re-scaling, we employ a
This is because at these dimensions, fixed-split is able to          “Stream-K”-style partitioning of attention that provides the
produce enough splits to occupy most of the SMs. We can              hardware with well-balanced loads.
also clearly see that FD resorts to vanilla execution when
we increase the number of heads from 160 to 512. LA, on               Our measurements indicate that LeanAttention delivers an
the other hand scales well as we increase the number of               average speedup of 1.73x over FlashDecoding, with up to
heads, showcasing its hardware-aware scalable execution               2.18x speedup for a 256k context size.
algorithm. In Figure 9(c), as we increase the batch size from
1 to 32, we can see that LA starts to outperform FD/FI as             ACKNOWLEDGEMENTS
batch size increases.
                                                                     We thank Daniel Madrigal Diaz at Microsoft for their help
Effect of Head Dimension. Figure 10 shows the speedup                with LeanAttention’s end-to-end inference benchmarking
offered by LA for models with a LLAMA-3, Mistral and                 setup during internal demos. We also thank the ONNX
Phi-3-like config with a head dimension of 128. We utilize           team for their help with integrating LeanAttention into ON-
a 128-token wide LeanTile for decomposition instead of               NXRuntime’s main repository.
256. We observe a similar trend in performance, where LA
                                                      LeanAttention

R EFERENCES                                                    Dao, T. Flashattention-2: Faster attention with bet-
                                                                 ter parallelism and work partitioning. arXiv preprint
GitHub - Dao-AILab/flash-attention: Fast and memory-
                                                                 arXiv:2307.08691, 2023.
  efficient exact attention — github.com. https://
  github.com/Dao-AILab/flash-attention.                        Dao, T., Haziza, D., Massa, F., and Sizov, G. Flashde-
  [Accessed 19-04-2024].                                         coding:    Stanford CRFM — crfm.stanford.edu.
                                                                 https://crfm.stanford.edu/2023/10/12/
Achiam, J., Adler, S., Agarwal, S., Ahmad, L., Akkaya, I.,       flashdecoding.html. [Accessed 22-04-2024].
  Aleman, F. L., Almeida, D., Altenschmidt, J., Altman, S.,
  Anadkat, S., et al. Gpt-4 technical report. arXiv preprint   Dao, T., Fu, D., Ermon, S., Rudra, A., and Ré, C. Flashat-
  arXiv:2303.08774, 2023.                                        tention: Fast and memory-efficient exact attention with
                                                                 io-awareness. Advances in Neural Information Process-
Agrawal, A., Kedia, N., Panwar, A., Mohan, J., Kwatra, N.,       ing Systems, 35:16344–16359, 2022.
  Gulavani, B. S., Tumanov, A., and Ramjee, R. Tam-
  ing throughput-latency tradeoff in llm inference with        Fu, Y., Panda, R., Niu, X., Yue, X., Hajishirzi, H., Kim,
  sarathi-serve, 2024. URL https://arxiv.org/                    Y., and Peng, H. Data engineering for scaling language
  abs/2403.02310.                                                models to 128k context. arXiv preprint arXiv:2402.10171,
                                                                 2024.
Anthropic.   Introducing the next generation of
  claude. https://www.anthropic.com/news/                      Hong, K., Dai, G., Xu, J., Mao, Q., Li, X., Liu, J., Dong,
  claude-3-family. [Accessed 19-04-2024].                       Y., Wang, Y., et al. Flashdecoding++: Faster large lan-
                                                                 guage model inference with asynchronization, flat gemm
Brandon, W., Nrusimha, A., Qian, K., Ankner, Z., Jin, T.,        optimization, and heuristics. Proceedings of Machine
  Song, Z., and Ragan-Kelley, J. Striped attention: Faster      Learning and Systems, 6:148–161, 2024.
  ring attention for causal transformers. arXiv preprint
  arXiv:2311.09431, 2023.                                      Inc., G.        An important next step on our
                                                                 AI    journey   —   blog.google.     https:
Brown, T., Mann, B., Ryder, N., Subbiah, M., Kaplan, J. D.,      //blog.google/technology/ai/
  Dhariwal, P., Neelakantan, A., Shyam, P., Sastry, G.,          bard-google-ai-search-updates/.         [Ac-
  Askell, A., et al. Language models are few-shot learners.      cessed 31-03-2024].
  Advances in neural information processing systems, 33:
  1877–1901, 2020.                                             Ivanov, A., Dryden, N., Ben-Nun, T., Li, S., and Hoefler, T.
                                                                 Data movement is all you need: A case study on optimiz-
Choquette, J., Lee, E., Krashinsky, R., Balan, V., and           ing transformers. Proceedings of Machine Learning and
  Khailany, B. 3.2 the a100 datacenter gpu and ampere            Systems, 3:711–732, 2021.
  architecture. In 2021 IEEE International Solid-State Cir-
                                                               Jia, Z. and Van Sandt, P. Dissecting the ampere gpu ar-
  cuits Conference (ISSCC), volume 64, pp. 48–50. IEEE,
                                                                  chitecture via microbenchmarking. In GPU Technology
  2021.
                                                                  Conference, 2021.
Chowdhery, A., Narang, S., Devlin, J., Bosma, M., Mishra,      Kenton, J. D. M.-W. C. and Toutanova, L. K. Bert: Pre-
  G., Roberts, A., Barham, P., Chung, H. W., Sutton, C.,         training of deep bidirectional transformers for language
  Gehrmann, S., et al. Palm: Scaling language modeling           understanding. In Proceedings of naacL-HLT, volume 1,
  with pathways. Journal of Machine Learning Research,           pp. 2, 2019.
  24(240):1–113, 2023.
                                                               Kim, S., Hooper, C., Wattanawong, T., Kang, M., Yan, R.,
CuTe. Cute layouts. https://github.com/NVIDIA/                   Genc, H., Dinh, G., Huang, Q., Keutzer, K., Mahoney,
  cutlass/blob/main/media/docs/cute/01_                          M. W., et al. Full stack optimization of transformer infer-
  layout.md, a. [Accessed 19-04-2024].                           ence: a survey. arXiv preprint arXiv:2302.14017, 2023.
CuTe. Cute’s support for matrix multiply-accumulate            Langley, P. Crafting papers on machine learning. In Langley,
  instructions. https://github.com/NVIDIA/                       P. (ed.), Proceedings of the 17th International Conference
  cutlass/blob/main/media/docs/cute/0t_                          on Machine Learning (ICML 2000), pp. 1207–1216, Stan-
  mma_atom.md, b. [Accessed 19-04-2024].                         ford, CA, 2000. Morgan Kaufmann.

CuTe. Cute tensors. https://github.com/NVIDIA/                 Li, T., Zhang, G., Do, Q. D., Yue, X., and Chen, W. Long-
  cutlass/blob/main/media/docs/cute/03_                          context llms struggle with long in-context learning. arXiv
  tensor.md., c. [Accessed 19-04-2024].                          preprint arXiv:2404.02060, 2024.
                                                         LeanAttention

Li, Y., Bubeck, S., Eldan, R., Del Giorno, A., Gunasekar,         Thakkar, V., Ramani, P., Cecka, C., Shivam, A., Lu, H.,
  S., and Lee, Y. T. Textbooks are all you need ii: phi-1.5         Yan, E., Kosaian, J., Hoemmen, M., Wu, H., Kerr, A.,
  technical report. arXiv preprint arXiv:2309.05463, 2023.          Nicely, M., Merrill, D., Blasig, D., Qiao, F., Majcher, P.,
                                                                    Springer, P., Hohnerbach, M., Wang, J., and Gupta, M.
Liu, H., Zaharia, M., and Abbeel, P. Ring attention with            CUTLASS, January 2023. URL https://github.
  blockwise transformers for near-infinite context. arXiv           com/NVIDIA/cutlass.
  preprint arXiv:2310.01889, 2023.
                                                                  Touvron, H., Martin, L., Stone, K., Albert, P., Almahairi,
Liu, S., Tao, G., Zou, Y., Chow, D., Fan, Z., Lei, K., Pan,         A., Babaei, Y., Bashlykov, N., Batra, S., Bhargava, P.,
  B., Sylvester, D., Kielian, G., and Saligane, M. (eds.).          Bhosale, S., et al. Llama 2: Open foundation and fine-
  ConSmax: Hardware-Friendly Alternative Softmax with               tuned chat models. arXiv preprint arXiv:2307.09288,
  Learnable Parameters, 2024. URL https://arxiv.                    2023.
  org/abs/2402.10930.
                                                                  Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones,
Luo, W., Fan, R., Li, Z., Du, D., Wang, Q., and Chu, X.             L., Gomez, A. N., Kaiser, Ł., and Polosukhin, I. At-
  Benchmarking and dissecting the nvidia hopper gpu ar-             tention is all you need. Advances in neural information
  chitecture, 2024. URL https://arxiv.org/abs/                      processing systems, 30, 2017.
  2402.13499.
                                                                  Williams, S., Waterman, A., and Patterson, D. Roofline:
Milakov, M. and Gimelshein, N. Online normalizer cal-              an insightful visual performance model for multicore
 culation for softmax. arXiv preprint arXiv:1805.02867,            architectures. Communications of the ACM, 52(4):65–76,
 2018.                                                             2009.
OpenAI. Introducing ChatGPT — openai.com. https:                  Wolf, T., Debut, L., Sanh, V., Chaumond, J., Delangue, C.,
 //openai.com/blog/chatgpt. [Accessed 01-04-                       Moi, A., Cistac, P., Rault, T., Louf, R., Funtowicz, M.,
  2024].                                                           et al. Huggingface’s transformers: State-of-the-art natural
                                                                   language processing. arXiv preprint arXiv:1910.03771,
Osama, M., Merrill, D., Cecka, C., Garland, M., and Owens,         2019.
  J. D. Stream-k: Work-centric parallel decomposition for
  dense matrix-matrix multiplication on the gpu. In Pro-          Ye, Z., Chen, L., Lai, R., Zhao, Y., Zheng, S., Shao, J., Hou,
  ceedings of the 28th ACM SIGPLAN Annual Symposium                 B., Jin, H., Zuo, Y., Yin, L., Chen, T., and Ceze, L. Ac-
  on Principles and Practice of Parallel Programming, pp.           celerating self-attentions for llm serving with flashinfer,
  429–431, 2023.                                                    February 2024. URL https://flashinfer.ai/
                                                                    2024/02/02/introduce-flashinfer.html.
Pope, R., Douglas, S., Chowdhery, A., Devlin, J., Bradbury,
  J., Heek, J., Xiao, K., Agrawal, S., and Dean, J. Efficiently   Yu, G.-I., Jeong, J. S., Kim, G.-W., Kim, S., and Chun, B.-
  scaling transformer inference. Proceedings of Machine             G. Orca: A distributed serving system for Transformer-
  Learning and Systems, 5, 2023.                                    Based generative models. In 16th USENIX Sympo-
                                                                    sium on Operating Systems Design and Implementa-
Shah, J., Bikshandi, G., Zhang, Y., Thakkar, V., Ramani, P.,        tion (OSDI 22), pp. 521–538, Carlsbad, CA, July
  and Dao, T. Flashattention-3: Fast and accurate attention         2022a. USENIX Association. ISBN 978-1-939133-28-1.
  with asynchrony and low-precision, 2024. URL https:               URL https://www.usenix.org/conference/
  //arxiv.org/abs/2407.08608.                                       osdi22/presentation/yu.
Spataro, J. and Inc., M. Introducing Microsoft 365                Yu, G.-I., Jeong, J. S., Kim, G.-W., Kim, S., and Chun, B.-
  Copilot – your copilot for work - The Official                    G. Orca: A distributed serving system for {Transformer-
  Microsoft Blog — blogs.microsoft.com.    https:                   Based} generative models. In 16th USENIX Symposium
  //blogs.microsoft.com/blog/2023/03/                               on Operating Systems Design and Implementation (OSDI
  16/introducing-microsoft-365-copilot/                             22), pp. 521–538, 2022b.
 -your-copilot-for-work/. [Accessed 31-03-
  2024].                                                          Zhang, P., Liu, Z., Xiao, S., Shao, N., Ye, Q., and Dou,
                                                                    Z. Soaring from 4k to 400k: Extending llm’s context
Stevens, J. R., Venkatesan, R., Dai, S., Khailany, B., and          with activation beacon. arXiv preprint arXiv:2401.03462,
  Raghunathan, A. Softermax: Hardware/software co-                  2024.
  design of an efficient softmax for transformers. In 2021
  58th ACM/IEEE Design Automation Conference (DAC),               Zhang, S., Roller, S., Goyal, N., Artetxe, M., Chen, M.,
  pp. 469–474. IEEE, 2021.                                          Chen, S., Dewan, C., Diab, M., Li, X., Lin, X. V.,
                                                   LeanAttention

et al. Opt: Open pre-trained transformer language models.
arXiv preprint arXiv:2205.01068, 2022.
                                                           LeanAttention

A     R AGGED BATCHING IN D ECODE .
Ragged Batching in Decode. For the purpose of our evaluations, we quantify the heterogeneity of a ragged batch as the
ratio of average context length to the maximum context length present in the batch. Figure 12 shows the speedup of LA over
FD. We observe that as the heterogeneity of batch increases, LA delivers a higher speedup because of better distribution of
work across SMs.




Figure 12. Speedup offered by LA over FD at different batch sizes with heterogeneous context lengths. Batch context ratio(%) shows the
ratio of average context length over maximum context length
                                                 LeanAttention

B   L EAN T ILE A LGORITHM

Algorithm 2 LeanTile() for a sequence of lean tile iterations.
 1: function LeanTile(tile idx, iter begin, iter end)
 2: shared Oacc [Tm , d]
 3: shared Qf [Tm , d]
 4: shared Kf [Tn , d]
 5: shared Vf [Tn , d]
 6: shared m[Tm , 1]
 7: shared l[Tm , 1]
 8: Initialize Oacc to (0)Tm ×d ∈ RTm ×d in SMEM.
 9: Initialize m to (−∞)Tm ×1 and l to (0)Tm ×1 ∈ RTm ×1 in SMEM.
10: mm = Tm × (tile idx / 1)
11: nn = d× (tile idx % 1)
12: Perform lean tile iterations for this output tile.
13: for iter = iter begin to iter end do
14:    kk = iter × Tn
15:    load fragments from GMEM to SMEM
16:    Qf = LoadF ragment(Q, mm, nn)
17:    Kf = LoadF ragment(K, nn, kk)
18:    Vf = LoadF ragment(V, nn, kk)
19:    Compute on chip:
20:    Sf = Qf Kf where Sf ∈ RTm ×Tn
21:    mnew = max(m, rowmax(Sf ))
22:    Pf = exp(Sf − mnew ) where Pf ∈ RTm ×Tn
                      new
23:    lnew = em−m l + rowsum(Pf )
                                      new
24:    Oacc = Pf Vf + diag(em−m )Oacc
25:    l = lnew , m = mnew
26: end for
27: return Oacc , l, m
28: end function
                                                          LeanAttention

C    E NERGY C ONSUMPTION E VALUATION
Fixed-split partitioning results in imbalanced workloads across the SMs, leaving many of them idle during the final stages
of computation. This inefficiency makes fixed-split attention mechanisms energy-inefficient. As shown in Figure 13, the
disparity in energy consumption between FlashDecoding, FlashInfer, and LeanAttention increases as context lengths grow
over 128k. LeanAttention, with its well-balanced load partitioning strategy, ensures more consistent utilization of SMs,
making it significantly more energy-efficient.




Figure 13. Ratio of Energy consumed by attention kernel to energy consumed by FlashDecoding kernel of different attention mechanisms
for batch size = 1, number of heads = 56, head dimension = 64 on a single Nvidia-A100-80GB GPU as measured using NVML APIs.
                                                         LeanAttention

D    R EDUCTION OVERHEAD A NALYSIS
We have noticed negligible reduction overheads in our experiments (when reducing 8 or less partial outputs per host CTA)
as detailed in Table 2, with the total latency being dominated by the LeanTile computation within attention.
Reduction overheads might be more conspicuous in cases with very few heads (around 3-4 heads) or very short contexts
(<1k), where more splits per head would be required to fully occupy the processor. To mitigate this, LeanAttention
strategically optimizes grid size (i.e., the number of CTAs launched) to ensure that each CTA processes at least two
LeanTiles. As grid size increases, so does the number of splits per head which consequently increases overheads from
reduction. Thus, for shorter contexts where total number of LeanTiles are not enough to cover the entire processor width,
LeanAttention chooses a smaller, more efficient grid size to maintain performance. This prevents an unnecessary increase in
splits, effectively reducing reduction overheads while still leveraging parallelism across context length for speedups.
Since most open-sourced model configurations have greater than 12 heads, we rarely ever exceed 8 splits per head with
LeanAttention in our experiments.

                                         Number of Splits      Reduction Overhead %
                                               4                       0.41
                                               8                       2.85
                                              16                       6.84
                                              32                       16.47

                 Table 2. Reduction overhead as a percentage of timeshare for different number of splits per head.
