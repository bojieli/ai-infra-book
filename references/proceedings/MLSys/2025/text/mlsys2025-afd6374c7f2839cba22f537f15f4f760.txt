                        E FFICIENT LLM I NFERENCE USING
               DYNAMIC I NPUT P RUNING AND C ACHE -AWARE M ASKING


      Marco Federici* 1 Davide Belli* 1 Mart van Baalen 1 Amir Jalalirad 1 Andrii Skliar 1 Bence Major 1
                                       Markus Nagel 1 Paul Whatmough 1


                                                        A BSTRACT
     While mobile devices provide ever more compute power, improvements in DRAM bandwidth are much slower.
     This is unfortunate for large language model (LLM) token generation, which is heavily memory-bound. Previous
     work has proposed to leverage natural dynamic activation sparsity in ReLU-activated LLMs to reduce effective
     DRAM bandwidth per token. However, more recent LLMs use SwiGLU instead of ReLU, which results in little
     inherent sparsity. While SwiGLU activations can be pruned based on magnitude, the resulting sparsity patterns
     are difficult to predict, rendering previous approaches ineffective. To circumvent this issue, our work introduces
     Dynamic Input Pruning (DIP): a predictor-free dynamic sparsification approach, which preserves accuracy with
     minimal fine-tuning. DIP can further use lightweight LoRA adapters to regain some performance lost during
     sparsification. Lastly, we describe a novel cache-aware masking strategy, which considers the cache state and
     activation magnitude to further increase cache hit rate, improving LLM token rate on mobile devices. DIP
     outperforms other methods in terms of accuracy, memory and throughput trade-offs across simulated hardware
     settings. On Phi-3-Medium, DIP achieves a 46% reduction in memory and 40% increase in throughput with
     < 0.1 loss in perplexity when compared to streaming the dense model from Flash. The open source code
     for HW simulator, methods, and experiments in this paper is available at https://github.com/Qualcomm-AI-
     research/dynamic-sparsity.

1    I NTRODUCTION

Specialized neural processing unit (NPU) accelerators for
AI have become standard in recent smartphone SoCs. These
processors enable efficient neural network inference on-
device, opening the way for powerful on-device AI appli-
cations. However, as shown in Figure 2, memory size has
not improved at the same pace as processing power (Jhang
et al., 2021; Kwon et al., 2023; Gholami et al., 2024). This
memory bottleneck is especially evident for LLMs, which
grew to billions and trillions of parameters, and for which       Figure 1. Overview of Cache-Aware Dynamic Input Pruning for a
all weights must be accessed to generate each individual          vector-matrix multiplication. The Cache-Aware masking compo-
                                                                  nent (DIP-CA) outputs a column sparsity mask based on current
output token.
                                                                  input activations and weights currently cached in DRAM. The
For instance, a model with 14B parameters like Phi-3-             subset of required weights are retrieved from DRAM cache, if
                                                                  available, or loaded from Flash in case of cache misses.
  *
    Equal contribution 1 Qualcomm AI Research. Qualcomm AI
Research is an initiative of Qualcomm Technologies, Inc. Corre-
spondence to: Marco Federici <mfederic@qti.qualcomm.com>,
Davide Belli <dbelli@qti.qualcomm.com>, Mart van                  Medium quantized at INT4 precision occupies approxi-
Baalen     <mart@qti.qualcomm.com>,         Markus       Nagel    mately 7GB, which approaches the average smartphone to-
<markusn@qti.qualcomm.com>.                                       tal DRAM capacity (Counterpoint, 2021; Grossi & Lorenz,
Proceedings of the 8 th MLSys Conference, Santa Clara, CA, USA,   2024).
2025. Copyright 2025 by the author(s).                            But, in fact, the OS and active applications already occupy
    Snapdragon branded products are products of Qualcomm
Technologies, Inc. and/or its subsidiaries.                       a significant portion of the total DRAM capacity, typically
                                                                  leaving a few GBs free for an on-device LLM.
                       Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

                                                                     in ReLU LLMs (see Figure 6). This issue is circumvented in
                                                                     previous works (Mirzadeh et al., 2023; Song et al., 2024a;b)
                                                                     by replacing SiLU activations with ReLU activations, and
                                                                     fine-tuning the LLM to adapt to the architecture change.
                                                                     However, this fine-tuning step is expensive, often requiring
                                                                     billions of tokens of fine-tuning data to achieve reasonable
                                                                     accuracy. In this work we introduce Dynamic Input Pruning
                                                                     as a predictor-free alternative to dynamic sparsity that does
                                                                     not rely on expensive re-training. We also show that small
                                                                     LoRA adapters (Hu et al., 2021) can be used to mitigate the
                                                                     performance loss due to dynamic pruning.
                                                                     The high natural sparsity rates of ReLU LLMs (up to 90%)
                                                                     have two implications: 1) for each token, only a few ac-
Figure 2. Comparing the largest LLMs by year of release (Zhao
                                                                     tive parameters need to be loaded from Flash, and 2) active
et al., 2023) against NPU and DRAM trends in recent iPhone de-
                                                                     parameters from multiple previous tokens can be cached,
vices2 . An exponential fit highlights the linear increase in DRAM
size opposed to the exponential increases in NPU processing power    leading to higher cache hit rate and, as a result, reduced flash
and model sizes.                                                     access. SwiGLU LLMs, however, can be pruned dynami-
                                                                     cally only up to approximately 50% before model accuracy
                                                                     is significantly impacted (see Section 3). Hence, to reach
To relax the DRAM capacity pressure, it has been previ-              reasonable token generation rates at lower sparsity levels,
ously proposed to load a model directly from flash, instead          we introduce a cache-aware variant of dynamic input prun-
of first copying it in DRAM (Alizadeh et al., 2023; Xue              ing, which we visualize in Figure 1. This variant takes into
et al., 2024). By leveraging dynamic sparsity and intro-             account the current state of the cache when deciding which
ducing DRAM caching the latency overhead of loading a                parameters to use. By biasing the parameter selection to-
model from flash can be mitigated significantly. Dynamic             wards the ones that are already in cache, we can increase
sparsity (Mirzadeh et al., 2023; Lee et al., 2024; Akhauri           cache-hit rate and hence LLM throughput, while minimally
et al., 2024) exploits the fact that all operations during to-       affecting model accuracy.
ken generation are matrix-vector multiplications, and the
                                                                     The main contributions in this paper are three-fold:
fact that in LLMs with ReLU activations, vectors contain
many zero elements. For elements in the activation vec-
tors that are zero, we can skip loading the corresponding                1. We analyze the limitation of methods like DejaVu (Liu
columns in the following layer. If sparsity predictors such                 et al., 2023) and “LLM in a Flash” (Alizadeh et al.,
as those introduced in DejaVu (Liu et al., 2023) are used to                2023) in modeling activation sparsity for modern non-
predict which elements in the activation vectors will be zero,              ReLU LLMs, demonstrating the impracticality of train-
the weights that generate zero-valued activation elements                   ing predictors for SwiGLU-based models (Section 3).
can also be ignored in the preceding layer. Additionally,
a DRAM cache is used to cache active parameters from                     2. We propose a predictor-free method to dynamically
previous tokens. When a DRAM cache is used, parameters                      prune MLP layers which outperforms previous static
only need to be loaded from flash in case of a cache miss,                  and dynamic pruning methods on the latest LLMs
i.e., when an active parameter is not already in cache from                 across various sparsity levels. Additionally, we show
previous tokens. These approaches allow even Mixtral8x7B                    that a lightweight LoRA component can further reduce
to run on mobile devices at reasonable token generation                     the accuracy lost with sparsification. (Section 4).
rates (Xue et al., 2024).
                                                                         3. We implement a cache-aware masking strategy which
Prior approaches rely on predictors that can anticipate the                 modifies the sparsity mask by prioritizing cached
natural dynamic activation sparsity. As we will show in                     weights, achieving better trade-offs between model
more detail in Section 3, SwiGLU MLP used in most mod-                      accuracy and latency at different DRAM constraints
ern LLMs (Jiang et al., 2023; Bai et al., 2023; Abdin et al.,               and hardware settings (Section 5).
2024; Dubey et al., 2024) have little to no natural activation
sparsity. While these activations can still be pruned based
on magnitude, it turns out to be much harder to predict              2     BACKGROUND AND R ELATED W ORK
which activations should be pruned in SwiGLU LLMs than
                                                                     This section first reviews quantization and static sparsity,
   2
     Data collected from Wikipedia (2024a). Values are averaged      two alternative approaches to reduce model size, and later
in case of multiple model releases per year.                         discusses prior works employing dynamic sparsity.
                        Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

              Mistral-7B               Turbosparse-Mistral             Recent approaches (Mirzadeh et al., 2023; Alizadeh et al.,
                                                                       2023; Song et al., 2024b) aim to reduce the number of MLP
                                                                       parameters loaded at inference time by exploiting the sparse
                                                                       activation pattern of the intermediate ReLU activations. Liu
                                                                       et al. (2023) (DejaVu) further showed that, in LLM with
                                                                       ReLU activations such as OPT (Zhang et al., 2022) and
                                                                       Llama v1 (Roumeliotis et al., 2023), even small predictors
                                                                       can accurately determine active units with moderate train-
                                                                       ing and memory overhead. Whenever the predictions are
Figure 3. GLU Activation Magnitude distribution for Layer 31           correct, the DejaVu approach does not introduce any approx-
Mistral-7B (left), and the corresponding re-trained ReLU-fied Tur-     imation error since only rows and column corresponding to
bosparse Mistral (right).                                              zero entries are removed.

        Global                 Per-Layer          Per-Token            Other recent work extended the approach from DejaVu aim-
       Threshold               Threshold       Threshold (topK)        ing to reduce system latency and memory overhead. Akhauri
                                                                       et al. (2024) replaced the individual predictors with a single,
                                                                       global predictor to determine the active units. Alizadeh et al.
                                                                       (2023) added an LFU cache on the MLP parameters and
                                                                       introduced weight bundling to reduce memory overhead.
                                                                       Song et al. (2023) proposed identifying most active neurons
                                                                       offline and pre-loading them on GPU for faster access, while
                                                                       loading the remaining neurons on-demand based on predic-
                                                                       tor outputs. Xue et al. (2024) builds an engine to optimize
Figure 4. Layer activation density for different GLU threshold-
ing strategies at 50% MLP target density on a Mistral-7B model.        LLM inference given certain input hardware specifications.
Darker shaded region indicate the standard deviation, while lighter    Interestingly, this work shows that neuron pre-loading is not
shading covers minimum and maximum values. Setting a threshold         necessary to achieve high system throughput.
for each layer or each token result in similar perplexity. The dense
                                                                       Most dynamic sparsity approaches inherently rely on
model perplexity is 5.249.
                                                                       the sparse activation pattern induced by the ReLU non-
                                                                       linearity, however, the vast majority of recent LLM archi-
Static Pruning and Quantization Pruning and quanti-                    tectures3 (Jiang et al., 2023; Abdin et al., 2024; Dubey et al.,
zation techniques have been extensively explored to min-               2024) use non-linearities that do not naturally induce many
imize the memory footprint of neural networks (Kuzmin                  hard zeros (cf. Figure 3). While ReLU-fication, the process
et al., 2023). Unstructured pruning typically involves re-             of replacing the original network activations with ReLU acti-
moving individual weight parameters, whereas structured                vations, has shown promising results (Mirzadeh et al., 2023;
pruning eliminates entire weight structures (van der Oud-              Song et al., 2024b), though on the other hand it requires
eraa et al., 2023; Frantar & Alistarh, 2023; Sun et al., 2023).        a computationally demanding retraining procedure which
Alternative approximation strategies involve low bit-width             may fail to replicate the original performance (SparseLLM,
quantization (Nagel et al., 2021; Frantar et al., 2022; van            2023; Lee et al., 2024).
Baalen et al., 2024; Huang et al., 2024) and low-rank repre-           For this reason, in the following sections we investigate the
sentations (Zhang & Papyan, 2024). All prior techniques,               effectiveness of dynamic sparsity strategies when applied
however, tend to degrade performance, which can be par-                directly to modern LLMs.
tially mitigated through model fine-tuning. This fine-tuning
process necessitates a representative dataset and significant
computational resources, and it is therefore prohibitive for           3    DYNAMIC S PARSITY ON M ODERN LLM S
certain applications. Another downside of static pruning is            Modern LLMs consist of alternating sequences of attention
its limited flexibility, as the set of retained weights is fixed       and MLP blocks. In most cases, the majority of network
for all activations, while the optimal set of weights may vary         weights resides in the latter component. This became even
across different tokens.                                               more pronounced with the introduction of Group Query
                                                                       Attention (Roumeliotis et al., 2023), reducing the number of
Dynamic Sparsity Recent work proposed dynamic spar-                    parameters in attention layers. MLP architectures in recent
sity methods (Mirzadeh et al., 2023; Liu et al., 2023; Lee             LLMs (Jiang et al., 2023; Abdin et al., 2024; Dubey et al.,
et al., 2024; Akhauri et al., 2024) to select active weights              3
based on the current inputs, and thus overcoming part of the                All the open-source LLMs benchmarked in Wang et al. (2024)
                                                                       use SwiGLU non-linearities.
limitations in static methods.
                        Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

      (a) GLU pruning                   (b) Gate pruning             (c) Predictive GLU pruning          (d) Input pruning (ours)




Figure 5. Diagrams describing the sparsity schemes for GLU pruning, Gate pruning, Predictive pruning and Input pruning. Activations
with larger magnitude are visualized in darker colors, while dark blue indicates weights used for computation for the current input.


2024) frequently consist of a Gated non-Linear Unit (GLU)           the current activations of each layer dynamically. Note that
layer, which maps the activations into high-dimensional             the latter corresponds to simply keeping the top-K largest
feature space followed by a linear down projection:                 magnitude values for each token. In all cases the thresholds
                                                                    are chosen to ensure 50% of activations are kept on average.
               MLP(x) = W d GLU(x)                          (1)
                                                                    We find that a global threshold performs poorly compared
               GLU(x) = W u x          (W g x)              (2)
                                                                    to a per-layer or per-token threshold. This is due to the fact
in which W u , W g , W d will be referenced as up, gate,            that activation magnitude varies considerably per layer and
and down weight matrices, respectively. The non-linearity           thus a fixed threshold results in too aggressive pruning for
  often consists of a SiLU function (Dauphin et al., 2017;          initial layers. Lastly, we find that the per-token pruning
Elfwing et al., 2018; Naveed et al., 2023).                         strategy performs comparably to the per-layer strategy, but
                                                                    with the additional benefit of pruning a constant number of
We define as follows the sparse version of an MLP:                  activations for each token. For this reason, we only consider
                          ⇣                   ⌘                     top-K pruning in the remainder of this paper.
         ]
         MLP(x)    =W fd W  f ux      (Wf g x) ,            (3)
                                                                    3.2   Strategies for Dynamic Sparsity
where the tilde indicates that the original weight matrices
are replaced with a sparse version, which may result in an          We now consider several dynamic weight sparsification
approximation error.                                                schemes that exploit dynamic magnitude-based activation
As discussed in Section 2, SwiGLU MLPs exhibit no natural           pruning. Figure 5a-c shows the different schemes for GLU
activation sparsity, and existing methods to circumvent this        LLMs described in the remainder of this section.
are expensive and time-consuming. In the remainder of this
section we will first show that non-sparse activations can          GLU pruning (Figure 5a) When values of GLU(x) are
still be pruned based on magnitude. We then discuss how             pruned, the corresponding columns of W d can also be
this can be used to dynamically prune weights. Lastly, we           pruned as they will be multiplied with a zero:
                                                                                   (
demonstrate that predictor-based approaches such as those                            0      if i 2
                                                                                                 / topK (|GLU(x)|)
introduced by DejaVu (Liu et al., 2023) and its derivatives               f  d
                                                                          W:,i =                                   .  (4)
                                                                                     W:,i o.w.
                                                                                        d
fail for dynamic, magnitude-based sparsity.
                                                                    Note that, instead of top-K pruning, other thresholding meth-
3.1   Magnitude-based Dynamic Pruning                               ods (such as those presented in Section 3.1) can be used
Since the number of hard zeros in SwiGLU architectures              instead.
is negligible, to enable higher sparsity we first relax the         This strategy can sparsify only one of the three large MLP
condition of removing only zeros, and instead prune values          matrices, reaching at most 33% MLP sparsity.
that are smaller than a predetermined threshold t.
In Figure 4, we compare the effect of three GLU thresh-             Gate pruning (Figure 5b) Assuming that most of the
olding strategies on Mistral-7B layer activations in terms          small entries are determined by the non-linearity , one
of WikiText-2 perplexity: a global threshold, i.e., a fixed         could first perform a dense matrix multiplication to deter-
threshold t is used to prune the activations in all layers;         mine (W g x), then sparsify both W u and W d based on
a per layer threshold, i.e. a fixed threshold which is set          the small (partial) activations:
                                                                                      (
independently per layer based on the CDF of activations
over a small calibration set, but shared over different tokens;        f ,W
                                                                       W  u  f , = 0, 0 if i 2
                                                                                 d                  / topK (| (W g x)|)
                                                                                                                        . (5)
                                                                         :,i    i
and a per-token threshold, which is based on the CDF of                                 W:,i , Wi o.w.
                                                                                            u     d
                      Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

Similarly, we can define Up Pruning as the equivalent strat-                             MMLU (5-shots)
egy in which the activations W u x are used to prune W g
and W d , respectively. In contrast to GLU pruning, Gate
(and Up) pruning can prune up to 66% of the MLP weights.
However, this pruning strategy may be sub-optimal since
the network parameters are pruned based on partial GLU
activations.

Predictive GLU pruning (Figure 5c) The same predic-
tive pruning strategy introduced by DejaVu can be extended
to GLU LLMs by training a small predictor to identify low-       Figure 6. Comparison of the effectiveness of GLU sparsification
magnitude GLU activations:                                       methods on Mistral-7B (left) and its ReLU-fied counterpart (right).
                    (                                            The predictor training procedure, architecture, and training data is

  Wf ,W
      u  f ,W
           g  f = 0,0,0 if i 2
                 d                  / topK(pred(x))
                                                      . (6)
                                                                 equivalent across the two LLMs.
     :,i  :,i   i              g
                         u
                      W:,i , W:,i , Wid o.w.
                                                                 ing activation magnitude is a much harder task in SwiGLU
Here pred(x) indicates the logits of a predictor trained to      networks. We hypothesize that this phenomenon is due to
identify the smallest activations using cross-entropy loss.      the interplay of various aspects. First, since SwiGLU acti-
Predictive GLU pruning methods can sparsify all MLP              vations are less sparse, predictor mistakes are more likely
weight matrices, potentially resulting in higher sparsity lev-   to prune relevant activations. Secondly, the activation pat-
els at the same model accuracy. However, as we will show         tern in GLU MLPs is less predictable than the one in the
next, it is extremely challenging to train predictors for GLU    ReLU-fied counterparts. We conjecture that this is because
pruning.                                                         predicting ReLU zeros is essentially sign prediction for a
                                                                 linear projection, a relatively easy task, whereas predicting
3.3   The challenge of predicting GLU activations                magnitude is a more challenging task due to the interplay
                                                                 between the gating components.
As demonstrated in Figure 6 (blue line), GLU pruning with
topK thresholding allows us to prune large portions of acti-     Due to the inherent limitations of GLU pruning and the chal-
vations with a minor impact on downstream performance.           lenges of predicting complex SwiGLU activation patterns
On SwiGLU networks, such as Mistral-7B, it is possible to        with small predictors, we consider a simple alternative ap-
prune up to 50% of the activations before observing a 1%         proach that aims to sparsify all three MLP matrices without
decrease in MMLU performance. However, the correspond-           requiring any additional predictor architecture.
ing ReLU-fied network, TurboSparse-Mistral, is much more
sparsifiable, and GLU pruning can reach more than 90%            4    DYNAMIC I NPUT P RUNING
activation sparsity before observing the same performance
deterioration. This showcases that SwiGLU activations are        In this section we introduce the Dynamic Input Pruning
inherently more sensitive to pruning.                            (DIP) strategy, which addresses the limitations of existing
                                                                 MLP dynamic pruning strategies on SwiGLU networks. The
Without any predictive mechanism, the effectiveness of           DIP method, visualized in Figure 5d, hinges on removing
GLU pruning is further hindered by the fact that only one of     small activations in both the input of each MLP block and
three MLP matrices is sparse (Figure 5a). We therefore stud-     the intermediate GLU block using per-token topK thresh-
ied the effect of predictive GLU pruning, which employs          olding, and thus requires no separate predictors. DIP is
predictors trained with cross-entropy loss to identify small     defined as:
GLU activations. Consistently with Liu et al. (2023), after                       (
training we prune the GLU activations corresponding to the          fu , W f = 0, 0
                                                                              g                   if i 2
                                                                                                       / topK (|x|)
                                                                    W  :,i   :,i              g                           (7)
smallest outputs of the predictor pred(x) on a per-token                            W:,i , W:,i o.w.
                                                                                       u

basis. The results are visualized in Figure 6 (orange line),                      (                      ⇣           ⌘
                                                                                    0      if i 2
                                                                                                /  topK    | ]
                                                                                                             GLU(x)|
which compares the effectiveness of predictive pruning on                  f =
                                                                           W  d
                                                                             :,i                                       . (8)
SwiGLU and ReLU-fied counterparts for the same set of                                  d
                                                                                    W:,i   o.w.
predictors and training procedure.
MMLU accuracy at 50% activation sparsity drops by almost         ]
                                                                 GLU(x)   corresponds to the GLU activations obtained using
10% on Mistral-7B, while the performance drop is negligi-        f u and W
                                                                 W         f g instead of W u and W g . Note that we can
ble on the ReLU-fied counterpart. The wider gap between          use different levels of sparsity for the up, gate and down
GLU pruning and GLU predictive pruning hints that predict-       layers. The optimal proportion of active units is empirically
                          Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

                                                                         5.1   Cache eviction policies
                                                                         A cache management system can be employed to optimize
                                                                         the decision of which parameters to evict, when the cache
                                                                         is full and we want to allocate new weights. Commonly
                                                                         used cache eviction policies include Least Recently Used
                                                                         (LRU) and Least Frequently Used (LFU). In LRU, the item
                                                                         that was last used farthest in the past is evicted. In LFU, a
                                                                         counter is kept (for the current session or across sessions) to
Figure 7. Diagram for the Cache-aware Dynamic Input Pruning              track usage frequency and evict the most rarely used item.
method applied to an individual linear layer. DIP-CA uses cache
                                                                         An optimal cache eviction policy can be implemented if
state and input activations to output a sparsity mask. Flash reads are
                                                                         future DRAM reads were known in advance. Belady (1966)
performed to load necessary weights missing from cache (marked
in red), and unused weights are evicted if the DRAM is full. The         describes a greedy oracle algorithm to maximize the cache
sparse weight matrix is then used to process the current activations.    hit-rate over a known, fixed sequence of memory reads.
                                                                         Based on this optimal algorithm, previous work (Lykouris
                                                                         & Vassilvitskii, 2021; Liu et al., 2022; Sadek & Elias, 2024)
determined as a function of the overall target MLP sparsity              explores methods to use estimators (Jain & Lin, 2016; Shah
(see Appendix B.1 for details).                                          et al., 2022) or learnable ML models based on cache history
                                                                         to predict the future cache usage. While these methods
When compared to Predictive GLU pruning, Dynamic Input                   outperforms simpler caching eviction policies, they require
Pruning trades off the error caused by the predictor for                 an additional training step, and their performance is upper-
approximation error introduced by the use of an approximate              bounded by the optimal Belady caching algorithm.
gating layer.
                                                                         Caching methods for dynamic sparsity have also been em-
                                                                         ployed, with the eviction policy used to determine which
Light-weight LoRA fine-tuning To compensate for the                      parameters should be kept in DRAM to increase the cache
approximation error caused by the subsequent pruning of                  hit rate and therefore minimize latency. For example, Song
multiple MLP activations, similarly to contemporary litera-              et al. (2023) use a static cache by preloading the most active
ture (Ma et al., 2023; Sun et al., 2023; Lee et al., 2024), we           neurons, Alizadeh et al. (2023) introduce an LRU eviction
add fine-tuned LoRA adapters to the sparsified up, gate and              policy to prune unused weights, and Xue et al. (2024) pro-
down MLP matrices. To ensure that the LoRA adapters do                   pose neuron bundling over multiple weight matrices.
not lead to any additional overhead, each adapter is applied
before column selection:                                                 5.2   Cache-aware neuron sparsity

                    W:,i          ! (W + AB):,i ,                 (9)    Differently from other caching applications, the current
                    |{z}            |   {z    }                          choice of sparsity mask is not a hard constraint. We argue
                original column     LoRA adapted column                  that suboptimal masks can be preferred to increase the sys-
                                                                         tem throughput while sacrificing some model accuracy. We
in which A and B refer to low-rank learnable matrices.                   thus propose a cache-aware sparsification method to strike
This allows us to fuse the adapters to the original matrices             a better trade-off between accuracy and latency. This is
after fine-tuning to avoid any memory overhead.                          implemented as a re-weighting mechanism before selecting
                                                                         active neurons, which introduces a penalty multiplier term
5    I MPROVED C ACHING IN DYNAMIC                                       on the neurons activation magnitude:
     S PARSITY                                                                             x(t)   c(t 1) + (1       c(t 1) )
                                                                                  s(t) =                                       ,   (10)
An effective dynamic sparsity method is not enough to guar-                                            x(t) 1
antee good LLM inference latency on a memory-constrained
device. The number of Flash reads, which bottlenecks the                 where x(t) refers to the current input activations, c(t 1) is
overall throughput, largely depends on the cache eviction                a binary mask representing which neurons are currently
policy employed to dynamically load and unload weights in                loaded in cache, and is a hyper-parameter to penalize
DRAM during inference. In this section we discuss exist-                 the selection of non-cached neurons. The resulting scores
ing cache eviction policies and present a novel cache-aware              s(t) are used instead of the original magnitude for dynamic
masking strategy for dynamic sparsity. Our approach allows               pruning as discussed in Section 4. The normalization term in
us to reach better accuracy-latency trade-offs with respect              the denominator ensures that the re-weighting mechanisms
to solely regulating the sparsity level.                                 is not sensitive to changes in the dynamic range of the
                       Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

Algorithm 1 Dynamic Input Pruning with Cache-Aware               HW Simulator In order to accelerate the evaluation pro-
re-weighting for a single linear layer.                          cedure, we implement a software tool to simulate the data
function DIP-CA x(t) , c(t 1) , W, , k :                         transfers between Flash, DRAM and processing units, and
                                                                 then compute the latency incurred in these processes. We
             x(t)   c(t 1) + (1      c(t 1) )                    also implement simulators for different cache eviction poli-
    s(t) =                                         . Eq.10       cies on DRAM. This allows us to efficiently estimate the
                  x(t) 1
     (t)      (t)                                                impact of different dynamic sparsity solutions on system la-
    m = topK s , k
                                                                 tency, without having to implement or modify a full system
    c(t) = cache update c(t 1) , m(t)                            on-device. As inference in the token generation scenario
                                                                 is bound by DRAM and Flash accesses (Jhang et al., 2021;
    f (t) = W
    W               m(t)                                         Kwon et al., 2023; Gholami et al., 2024), we do not simu-
           f (t) , c(t)                                          late NPU inference times, which can be largely parallelized
    return W
                                                                 or are negligible when compared to the more expensive
                                                                 memory operations. In Appendix A we include a detailed
                                                                 description of the simulator, including motivation for the
                                                                 selected parameters.
activations over different tokens.
                                                                 Unless otherwise stated, results throughout the paper are
Our implementation choice is motivated by the observation        based on simulations for Apple A18 with DRAM I/O speed
that for each layer and token, few neurons fire very strongly,   of 60 GB/s, and Flash read speed of 1 GB/s. We also conduct
while activations for most neurons are orders of magnitude       an ablation study on different choices for DRAM size and
lower (see Figure 10, left), and have less impact on the layer   Flash read speed. Results are reported in Appendix D.
outputs. The penalty term is thus set high enough to allow re-
ranking of the activations in the intermediate regime, but low
enough to not impact the strongest activations even if not in    Methods and Models We evaluate the proposed approach
cache. We empirically find that a simple LFU eviction policy     against the Gate Pruning, Up Pruning, and GLU Pruning
with cache-aware masking results in better latency-accuracy      methods presented in Section 2. We further consider CATS
trade-offs even compared against the oracle algorithm from       (Lee et al., 2024) and DejaVu (Liu et al., 2023): two recent
Belady (1966) without re-weighting (see Section 6.4).            dynamic sparsity solutions. Following the original approach,
                                                                 each predictor consist of a neural network of 1000 hidden
We name DIP-CA the Cache-Aware variant of Dynamic
                                                                 units, which is trained for at most 20 epochs on activations
Input Pruning, and show it outperforms DIP in terms of
                                                                 collected from a calibration set using cross entropy loss.
throughput at all perplexity levels. Figure 7 visualizes the
                                                                 For SwiGLU LLMs, the binary targets are generated by
DIP-CA procedure, which is described in detail in Algo-
                                                                 selecting the 10% largest activations for each token. We
rithm 1 for a single linear layer.
                                                                 further compare Dynamic Input Pruning with unstructured
                                                                 and semi-structured (2:4, 4:8) SparseGPT pruning (Frantar
6     E VALUATION                                                & Alistarh, 2023) and quantization methods GPTQ (Frantar
                                                                 et al., 2022) and GPTVQ (van Baalen et al., 2024).
6.1    Experimental setup
                                                                 In DIP, we empirically calibrate the proportion of active
A three-way trade-off in KPIs We observe how meth-
                                                                 units for a target MLP sparsity as described in Appendix B.1.
ods for efficient LLM inference must be evaluated based
                                                                 For DIP-CA, we tune the cache-aware re-weighting hyper-
on three different metrics: 1) model accuracy, 2) memory
                                                                 parameter on WikiText-2 validation set and fix it to = 0.2
footprint and 3) inference throughput. Depending on the tar-
                                                                 for all experiments. We include an ablation showing the
get application, a different trade-off between these metrics
                                                                 impact of changes in . Following previous work (Hu et al.,
can be considered optimal. To simplify the experimental
                                                                 2021; Lee et al., 2024), we train LoRA adapters with rank 32
analysis, we evaluate at most two metrics at a time, while ig-
                                                                 for a total of 1000 iterations using a knowledge distillation
noring or fixing the remaining ones. We first compare model
                                                                 loss to match the original (dense) model logits. The training
performance at a fixed sparsity rate, that is at the same con-
                                                                 procedure is consistent for DIP and CATS models. The
straint in terms of memory footprint. The model accuracy is
                                                                 adapters are applied to all the up, down and gate matrices
reported in terms of token perplexity or 5-shot accuracy on
                                                                 for DIP, while only up and down are adapted for CATS.
downstream tasks. We then evaluate the throughput achiev-
able at different operating points in terms of both memory       We evaluate each method on four recent SwiGLU LLMs
footprint and loss in model accuracy. We include in Ap-          with different dimensionalities: Phi-3-Medium and Phi-3-
pendix ablations on hardware specifications to show how          Mini (Abdin et al., 2024), Llama 3 8B (Dubey et al., 2024),
different methods perform in various use-cases.                  Mistral 7B (Jiang et al., 2023).
                        Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking


Table 1. Experimental results for dynamic sparsity methods at 50% MLP sparsity. ⇤ The MLP density estimation ignores the overhead
introduced by the predictors or additional data-structures. GLU pruning is not included since at most 33% sparsity can be achieved.

                                             WikiText-2 (Perplexity #)                          MMLU (5-shot accuracy ")
                                  Phi3Med     Phi3Mini     Llama8B       Mistral7B    Phi3Med     Phi3Mini   Llama8B       Mistral7B
  Dense                             4.29         6.01         6.14          5.25        78.14      70.62       65.30         62.68
  GLU Pruning (oracle)              4.45         6.10         6.30          5.29        77.98      70.24       64.77         62.09
  SparseGPT (unstructured)⇤         5.67         7.44         7.71          5.81        73.97      62.15       57.72         58.16
  SparseGPT (2:4)⇤                  6.87         9.84        10.05          7.31        68.07      53.39       43.69         51.15
  SparseGPT (4:8)⇤                  6.33         8.43         8.64          6.41        70.34      57.98       50.78         54.64
  Gate Pruning                     11.28        14.83        29.26         96.37        66.08      47.90       27.23         40.39
  Up Pruning                        7.41        10.94        13.93          7.13        70.68      56.82       46.34         50.09
  DejaVu⇤                           6.15         8.76         9.43          6.24        68.99      54.86       49.02         54.34
  CATS                              8.34        10.83        28.24         60.48        71.08      55.64       27.34         38.96
  CATS+LoRA                         5.54         7.44        16.53          9.18        72.23      60.16       30.52         44.84
  DIP                               5.52         7.04         7.38          5.61        75.54      66.51       60.38         59.66
  DIP+LoRA                          5.01         6.79         7.26          5.57        75.89      66.57       60.94         59.58


                                                                         & Alistarh, 2023) unstructured and structured pruning, at
                                                                         least 1 extra bit of information for each weight is needed to
                                                                         indicate whether the weight is pruned (Kuzmin et al., 2024).
                                                                         The memory overhead in this case would be 6.25% and 25%
                                                                         for 16-bit and 4-bit quantized models, respectively. We ob-
                                                                         serve how DIP outperforms both static and dynamic pruning
                                                                         methods at all sparsity levels, even excluding this overhead.
                                                                         Noticeably, we can run Phi-3-Medium in 70% of its full
                                                                         memory footprint, at the cost of only 0.20 perplexity and
                                                                         0.01% accuracy with respect to the dense model. In com-
                                                                         parison, CATS (Lee et al., 2024) incurs in a 0.50 increase
Figure 8. Pareto-curves for WikiText-2 perplexity (left) and 5-shot      in perplexity and 0.88% decrease in accuracy, while for
MMLU accuracy (right) vs MLP density on Phi-3-Medium. The                DejaVu (Liu et al., 2023) that is 1.03 higher perplexity and
density estimation ignores overhead in DejaVu and SparseGPT.             4.34% lower accuracy. We include in Appendix C additional
                                                                         results for Phi-3-Mini, Llama-v3-8B, and Mistral-7B.
                                                                         In Table 1 we report more extensive numerical results on
Datasets We use the SlimPajama (Soboleva et al., 2023)
                                                                         four modern LLMs. We consider 50% MLP sparsity for
dataset as calibration datasets to train predictors and fine-
                                                                         each method, and report the perplexity on WikiText-2 as
tune LoRA adapters. We evaluate all methods on token per-
                                                                         well as the 5-shot accuracy on the MMLU benchmark. Re-
plexity for language modeling on WikiText-2 test set with
                                                                         sults for different operating points can be found in Table 3
sequence length 2048, and on 5-shot accuracy for down-
                                                                         and Table 4 provided in Appendix C, which also includes
stream tasks on the MMLU benchmark (Hendrycks et al.,
                                                                         evaluations on a broader range of tasks.
2020) using the LM Evaluation Harness (Gao et al., 2024).
                                                                         Static pruning methods (Frantar & Alistarh, 2023), partic-
6.2   Results                                                            ularly in the semi-structured setup, show a clear gap in
                                                                         accuracy with respect to the dense model. Gate Pruning and
Memory footprint We firstly compare DIP against re-                      Up Pruning are simple baselines introduced in Section 3.
cent static and dynamic pruning methods. In Figure 8 we                  Pruning activations based on the output of the gate activa-
show the trade-off between model performance and MLP                     tions (W g x) (Gate Pruning) severely affects performance
density obtained at varying sparsity levels. Note that cer-              on LLama-v3-8B and Mistral-7B. Noticeably, pruning gate
tain methods require additional parameters to achieve the                layers based on the output of the up component W u x (Up
sparsification, but we do not account for this in the visual-            Pruning) instead gives better performance across all tested
izations, as the resulting overhead depends on the bit-width             architectures. Still, both approaches are far from the ideal
choice for quantization. DejaVu (Liu et al., 2023) requires              results achievable with an oracle for GLU Pruning. This ora-
sparsity predictors which amount up to 15% of the number                 cle shows that, with perfect predictions, model performance
of parameters for the dense MLP. For SparseGPT (Frantar
                        Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking


Table 2. Comparison of throughput for dynamic sparsity methods.
We report the highest throughput achieved at a 0.2 and 0.5 increase
in perplexity on WikiText-2 over the dense model. The available
DRAM capacity is set to fit approximately 50% of each dense
model. All models are 4-bit quantized.

                  Phi3Med    Phi3Mini     Llama8B     Mistral7B
 Model size       7.4 GB       2.4 GB      4.3 GB      3.9 GB
 DRAM size        4.0 GB       1.5 GB      2.5 GB      2.0 GB
                             Throughput [tok/s] (")
 Dense              0.29        1.15        0.57        0.54
                      Throughput [tok/s] @ + 0.2 PPL (")              Figure 9. Comparison against quantization and pruning, integra-
                                                                      tion of DIP with 4 bit BQ and 3 bit VQ models on Phi-3-Medium.
 GLU Pruning        0.41        1.59        0.78        0.80
 Up Pruning         0.43        1.69        0.76        1.03
 CATS               0.40        1.54        0.73        0.69
 DIP                0.43        1.69        0.73        0.98          the sub-optimality of Gate Pruning described in Table 1. By
 DIP-CA             0.45        1.77        0.78        1.08          itself, DIP reaches similar or marginally worse throughput
                      Throughput [tok/s] @ + 0.5 PPL (")              than Up Pruning. Finally, we assess the benefits of applying
                                                                      Cache-Aware masking to DIP. For all models and perplexity
 GLU Pruning        0.45        1.63        0.84        0.87
 Up Pruning         0.52        1.98        0.87        1.32          operating points, DIP-CA outperforms previous methods.
 CATS               0.47        1.84        0.83        0.77          Noticeably, DIP-CA enables running Phi-3-Medium with
 DIP                0.50        1.98        0.82        1.23          46% lower DRAM footprint and 40%, 55% and 93% faster
 DIP-CA             0.56        2.09        0.89        1.39          throughput at a perplexity loss of 0.1, 0.2 and 0.5, respec-
                                                                      tively. Similar improvements are observed for Phi-3-Mini,
                                                                      LLama-v3-8B and Mistral-7B.
would be marginally worse than the dense baseline.
CATS (Lee et al., 2024) is an instance of per-layer thresh-           6.3   Comparison and compatibility with quantization
old pruning on the gate activations. Comparably with Gate                   and static pruning
Pruning, the method struggles with the Llama-v3-8B and                Quantization is an alternative approach to improve model
Mistral-7B models. We expect that modifying the CATS                  efficiency. We compare DIP with Blockwise Quantization
approach to use up layer activation might improve its effec-          (BQ) (Frantar et al., 2022) and Vector Quantization (VQ)
tiveness. Introducing LoRA adapters over CATS is neces-               (van Baalen et al., 2024) at 2, 3 and 4 bits, as well as static
sary to boost performance. In contrast, our proposed DIP              pruning with SparseGPT (Frantar & Alistarh, 2023). Note
method without adapters outperforms CATS+LoRA as well                 that in these results we do account for 1 bit of overhead
as previous baselines on all models and evaluation scenar-            per weight introduced by the pruning mask. In Figure 9
ios. DIP+LoRA further improves overall results, with more             we report results on the comparison and combination of
pronounced benefits on perplexity scores.                             DIP with quantization. DIP applied to 4 bit BQ models
                                                                      compares favorably against both pruning and quantization
Model throughput In Table 2 we investigate what                       approaches, reaching 15% lower perplexity than SparseGPT
throughput can be achieved with different sparsity meth-              given 4GB of available DRAM. At higher quantization lev-
ods and models. For this analysis, all models are quantized           els, BQ4+DIP outperforms more aggressive BQ quantiza-
to INT4 using GPTQ (Frantar et al., 2022), and all methods            tion, which suggests that increased dynamic sparsity is pre-
rely on an LFU cache allocated as explained in Section 6.1.           ferred over decreasing quantization bit-width. VQ3+DIP
We consider a use-case where approximately half of the                improves over BQ4+DIP at higher sparsity, similarly out-
dense model can fit in DRAM and include the selected                  performing 2-bit VQ, and showing that DIP can be consid-
DRAM sizes compared against the model sizes in the first              ered as complementary to quantization methods to enhance
part of the table. To numerically evaluate the trade-off be-          model efficiency.
tween model accuracy and efficiency, we report the highest
throughput achieved by each method under fixed operating
                                                                      6.4   Cache eviction policies
points in perplexity loss. We first notice that GLU Prun-
ing underperforms Up Pruning, as GLU Pruning needs to                 Cache-aware masking By analyzing the distribution of
load the full gate and up layers to compute GLU activations,          GLU activations in Phi-3-Medium (see Figure 10, left), we
which do not fit entirely in DRAM. CATS (Lee et al., 2024)            observe that a small percentage of activations are orders
falls short from the previous methods, possibly because of            of magnitude larger than the rest, and will have a bigger
                       Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking




Figure 10. (Left) Distribution of normalized GLU activations for
Phi-3-Medium layers. (Right) Effects of changing the DIP-CA         Figure 11. Comparison between cache eviction policies and cache-
hyper-parameter on throughput at different levels of perplexity.    aware masking. LFU with DIP-CA outperforms all policies includ-
                                                                    ing Belady’s oracle throughput at a given perplexity threshold.


impact in determining the layer output. On the other hand,          7    C ONCLUSION
activations between the 30th and 80th percentiles are within
one order of magnitude, which means they will have a rela-          This paper addresses the challenge of efficient LLM in-
tively similar contribution to the linear layer output. DIP-CA      ference on DRAM constrained mobile devices. Existing
exploits this behavior by re-weighting the scores for acti-         dynamic sparsity approaches are not effective for modern
vations depending on whether they are currently available           LLMs which use SwiGLU instead of ReLU, and hence do
in cache. This effectively re-orders the activation scores          not exhibit natural sparsity. Instead, we introduce a novel
before the TopK selection. In Figure 10 (right), we show the        dynamic input pruning (DIP) solution which removes the
effect of changing the hyper-parameter in DIP-CA. For all           need for predictors and requires only minimal fine-tuning.
perplexity levels, ranges between 0.1 and 0.3 result in op-         DIP can be further aided by a lightweight LoRA adapter to
timal throughput. We include an example by comparing two            recover part of the accuracy lost during sparsification. Fi-
models achieving the same throughput. The DIP instance              nally, we introduced a cache-ware variant (DIP-CA), which
with = 1 (in blue) has a perplexity of 5.14, while the DIP-         significantly enhances model throughput and reduces mem-
CA counterpart with = 0.2 reaches lower perplexity of               ory footprint. Specifically, we achieved a 46% reduction
5.0. Since the activation distributions are comparable across       in memory usage and a 40% increase in throughput with
layers (with first and last layers having slightly longer tails),   respect to dense Phi-3-Medium, with a minimal impact on
we use a fixed value of for all layers.                             perplexity. Underscoring these results, we show extensive
                                                                    comparisons against pruning and quantization baselines,
                                                                    as well as ablation studies considering different hardware
Comparison with cache eviction policy We compare in                 specifications.
Figure 11 different caching policies to handle allocation
and eviction of weights in DRAM. Without DRAM caching,              Future work will focus on further refining our sparsity tech-
inference inference throughput is severly degraded, as the          niques and exploring their applicability to a broader range
MLP weights need to be loaded from Flash for each token.            of models and hardware configurations, including on-device
LFU and LRU policies regulate the eviction of parameters            simulation of model performance.
once the DRAM cache is full, by considering how often or
recently a neuron in the cache has been used. Results are           R EFERENCES
very similar, with LFU being marginally better. We then ob-
                                                                    Abdin, M., Jacobs, S. A., Awan, A. A., Aneja, J., Awadallah,
serve how even the oracle Belady cache eviction policy does
                                                                      A., Awadalla, H., Bach, N., Bahree, A., Bakhtiari, A.,
not yield much improvement over practical policies. The
                                                                      Behl, H., et al. Phi-3 technical report: A highly capable
throughput for all caching eviction policies overlap under
                                                                      language model locally on your phone. arXiv preprint
5.25 perplexity, corresponding to the sparsity level under
                                                                      arXiv:2404.14219, 2024.
which the parameter size for the active neurons becomes
larger than the DRAM size allocated for caching. In this            Akhauri, Y., AbouElhamayed, A. F., Dotzel, J., Zhang, Z.,
case, the cache will always contain current neurons, or part          Rush, A. M., Huda, S., and Abdelfattah, M. S. Shad-
of them. In contrast, DIP-CA with a simple LFU policies               owllm: Predictor-based contextual sparsity for large lan-
largely outperforms DIP with all cache eviction policies,             guage models. arXiv preprint arXiv:2406.16635, 2024.
even including the optimal Belady algorithm. The improve-
ment also holds at lower sparsity regimes, where part of the        Alizadeh, K., Mirzadeh, I., Belenko, D., Khatamifard, K.,
active neurons are directly loaded to the processing unit, as         Cho, M., Del Mundo, C. C., Rastegari, M., and Fara-
there is no remaining space to allocate them in cache.                jtabar, M. Llm in a flash: Efficient large language
                      Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

  model inference with limited memory. arXiv preprint              07 2024. URL https://zenodo.org/records/
  arXiv:2312.11514, 2023.                                          12608602.

Bai, J., Bai, S., Chu, Y., Cui, Z., Dang, K., Deng, X., Fan,     Gholami, A., Yao, Z., Kim, S., Hooper, C., Mahoney, M. W.,
  Y., Ge, W., Han, Y., Huang, F., et al. Qwen technical            and Keutzer, K. Ai and memory wall. IEEE Micro, 2024.
  report. arXiv preprint arXiv:2309.16609, 2023.
                                                                 Grossi, T. and Lorenz, J.     Smartphone memory:
Belady, L. A. A study of replacement algorithms for a              Gen ai upgrades to drive spike in dram demand,
  virtual-storage computer. IBM Systems journal, 5(2):             2024.     URL https://www.yolegroup.com/
  78–101, 1966.                                                    technology-outlook/smartphone-memory-
                                                                   gen-ai-upgrades-to-drive-spike-in-
Counterpoint. Smartphones beat dram drum to meet
                                                                   dram-demand.
  performance demand, 2021.      URL https://
  www.counterpointresearch.com/insights/                         Hendrycks, D., Burns, C., Basart, S., Zou, A., Mazeika,
  smartphones-dram-trends-2019-2020.                               M., Song, D., and Steinhardt, J. Measuring mas-
Danielson, S. How much ram should a phone have,                    sive multitask language understanding. arXiv preprint
  2023. URL https://robots.net/tech/how-                           arXiv:2009.03300, 2020.
  much-ram-should-a-phone-have.                                  Hu, E. J., Shen, Y., Wallis, P., Allen-Zhu, Z., Li, Y., Wang,
Dauphin, Y. N., Fan, A., Auli, M., and Grangier, D. Lan-           S., Wang, L., and Chen, W. Lora: Low-rank adaptation of
  guage modeling with gated convolutional networks. In             large language models. arXiv preprint arXiv:2106.09685,
  International conference on machine learning, pp. 933–           2021.
  941. PMLR, 2017.
                                                                 Huang, W., Liu, Y., Qin, H., Li, Y., Zhang, S., Liu,
Dubey, A., Jauhri, A., Pandey, A., Kadian, A., Al-Dahle,          X., Magno, M., and Qi, X. Billm: Pushing the limit
 A., Letman, A., Mathur, A., Schelten, A., Yang, A., Fan,          of post-training quantization for llms. arXiv preprint
 A., et al. The llama 3 herd of models. arXiv preprint             arXiv:2402.04291, 2024.
  arXiv:2407.21783, 2024.
                                                                 Jain, A. and Lin, C. Back to the future: Leveraging be-
Elfwing, S., Uchibe, E., and Doya, K. Sigmoid-weighted              lady’s algorithm for improved cache replacement. ACM
  linear units for neural network function approximation           SIGARCH Computer Architecture News, 44(3):78–89,
  in reinforcement learning. Neural networks, 107:3–11,             2016.
  2018.
                                                                 Jhang, C.-J., Xue, C.-X., Hung, J.-M., Chang, F.-C., and
Federici, M., Belli, D., van Baalen, M., Jalalirad, A.,            Chang, M.-F. Challenges and trends of sram-based
  Skliar, A., Major, B., Nagel, M., and Whatmough,                 computing-in-memory for ai edge devices. IEEE Trans-
  P.     Code artifact for efficient llm inference us-             actions on Circuits and Systems I: Regular Papers, 68:
  ing dynamic input pruning and cache-aware masking,               1773–1786, 2021.
  March 2025. URL https://doi.org/10.5281/
  zenodo.15088634.                                               Jiang, A. Q., Sablayrolles, A., Mensch, A., Bamford, C.,
                                                                    Chaplot, D. S., Casas, D. d. l., Bressand, F., Lengyel, G.,
Frantar, E. and Alistarh, D. Sparsegpt: Massive language            Lample, G., Saulnier, L., et al. Mistral 7b. arXiv preprint
  models can be accurately pruned in one-shot. In Inter-            arXiv:2310.06825, 2023.
  national Conference on Machine Learning, pp. 10323–
  10337. PMLR, 2023.                                             Kuzmin, A., Nagel, M., Van Baalen, M., Behboodi, A., and
                                                                   Blankevoort, T. Pruning vs quantization: Which is better?
Frantar, E., Ashkboos, S., Hoefler, T., and Alistarh, D. Gptq:    Advances in neural information processing systems, 36:
  Accurate post-training quantization for generative pre-          62414–62427, 2023.
  trained transformers. arXiv preprint arXiv:2210.17323,
  2022.                                                          Kuzmin, A., Nagel, M., Van Baalen, M., Behboodi, A., and
                                                                   Blankevoort, T. Pruning vs quantization: which is better?
Gao, L., Tow, J., Abbasi, B., Biderman, S., Black, S., DiPofi,    Advances in neural information processing systems, 36,
  A., Foster, C., Golding, L., Hsu, J., Le Noac’h, A., Li,         2024.
  H., McDonell, K., Muennighoff, N., Ociepa, C., Phang,
  J., Reynolds, L., Schoelkopf, H., Skowron, A., Sutawika,       Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu,
  L., Tang, E., Thite, A., Wang, B., Wang, K., and Zou,           C. H., Gonzalez, J., Zhang, H., and Stoica, I. Efficient
  A. A framework for few-shot language model evaluation,          memory management for large language model serving
                      Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

  with pagedattention. In Proceedings of the 29th Sym-            Shah, I., Jain, A., and Lin, C. Effective mimicry of belady’s
  posium on Operating Systems Principles, pp. 611–626,              min policy. In 2022 IEEE International Symposium on
  2023.                                                             High-Performance Computer Architecture (HPCA), pp.
                                                                    558–572. IEEE, 2022.
Labaran, A. Apple nvme vs ufs 4.0 storage, 2023.
  URL      https://www.allroundreview.com/                        Soboleva, D., Al-Khateeb, F., Myers, R., Steeves,
  practical-differences-between-the-                                J. R., Hestness, J., and Dey, N. SlimPajama: A
  apple-nvme-vs-ufs-40-storage.                                     627B token cleaned and deduplicated version of
                                                                    RedPajama.        https://cerebras.ai/blog/
Lee, J.-Y., Lee, D., Zhang, G., Tiwari, M., and Mirhoseini,
                                                                    slimpajama-a-627b-token-cleaned-and-
  A. Cats: Contextually-aware thresholding for sparsity in
                                                                    deduplicated-version-of-redpajama,
  large language models. arXiv preprint arXiv:2404.08763,
                                                                    June 2023.     URL https://huggingface.co/
  2024.
                                                                    datasets/cerebras/SlimPajama-627B.
Liu, W., Cui, J., Li, T., Liu, J., and Yang, L. T. A space-
                                                                  Song, C., Han, X., Zhang, Z., Hu, S., Shi, X., Li, K., Chen,
  efficient fair cache scheme based on machine learning
                                                                    C., Liu, Z., Li, G., Yang, T., et al. Prosparse: Intro-
  for nvme ssds. IEEE Transactions on Parallel and Dis-
                                                                    ducing and enhancing intrinsic activation sparsity within
  tributed Systems, 34(1):383–399, 2022.
                                                                    large language models. arXiv preprint arXiv:2402.13516,
Liu, Z., Wang, J., Dao, T., Zhou, T., Yuan, B., Song, Z.,           2024a.
  Shrivastava, A., Zhang, C., Tian, Y., Re, C., et al. Deja
  vu: Contextual sparsity for efficient llms at inference time.   Song, Y., Mi, Z., Xie, H., and Chen, H. Powerinfer: Fast
  In International Conference on Machine Learning, pp.              large language model serving with a consumer-grade gpu.
  22137–22176. PMLR, 2023.                                          arXiv preprint arXiv:2312.12456, 2023.

Lykouris, T. and Vassilvitskii, S. Competitive caching with       Song, Y., Xie, H., Zhang, Z., Wen, B., Ma, L., Mi, Z.,
  machine learned advice. Journal of the ACM (JACM), 68             and Chen, H. Turbo sparse: Achieving llm sota perfor-
  (4):1–25, 2021.                                                   mance with minimal activated parameters. arXiv preprint
                                                                    arXiv:2406.05955, 2024b.
Ma, X., Fang, G., and Wang, X. Llm-pruner: On the struc-
 tural pruning of large language models. Advances in              SparseLLM, M. Sparse language models with relu activa-
 neural information processing systems, 36:21702–21720,             tions. https://huggingface.co/SparseLLM/
 2023.                                                              ReluLLaMA-7B, 2023. Blogpost.

Mirzadeh, I., Alizadeh, K., Mehta, S., Del Mundo, C. C.,          Sun, M., Liu, Z., Bair, A., and Kolter, J. Z. A simple and
 Tuzel, O., Samei, G., Rastegari, M., and Farajtabar,               effective pruning approach for large language models.
 M. Relu strikes back: Exploiting activation sparsity in            arXiv preprint arXiv:2306.11695, 2023.
 large language models. arXiv preprint arXiv:2310.04564,          van Baalen, M., Kuzmin, A., Nagel, M., Couperus, P., Bas-
 2023.                                                              toul, C., Mahurin, E., Blankevoort, T., and Whatmough,
Nagel, M., Fournarakis, M., Amjad, R. A., Bondarenko,               P. Gptvq: The blessing of dimensionality for llm quanti-
 Y., Van Baalen, M., and Blankevoort, T. A white pa-                zation. arXiv preprint arXiv:2402.15319, 2024.
  per on neural network quantization. arXiv preprint
                                                                  van der Ouderaa, T. F., Nagel, M., Van Baalen, M., Asano,
  arXiv:2106.08295, 2021.
                                                                    Y. M., and Blankevoort, T. The llm surgeon. arXiv
Naveed, H., Khan, A. U., Qiu, S., Saqib, M., Anwar, S.,             preprint arXiv:2312.17244, 2023.
  Usman, M., Akhtar, N., Barnes, N., and Mian, A. A
                                                                  Wang, Y., Ma, X., Zhang, G., Ni, Y., Chandra, A., Guo, S.,
  comprehensive overview of large language models. arXiv
                                                                   Ren, W., Arulraj, A., He, X., Jiang, Z., et al. Mmlu-pro:
  preprint arXiv:2307.06435, 2023.
                                                                   A more robust and challenging multi-task language un-
Roumeliotis, K., Tselikas, N., and Nasiopoulos, D. Llama           derstanding benchmark. In The Thirty-eight Conference
  2: Early adopters’ utilization of meta’s new open-source         on Neural Information Processing Systems Datasets and
  pretrained model. preprints2023 2023072 142. Eng.).              Benchmarks Track, 2024.
  DOI, 10, 2023.
                                                                  Wikipedia.     Apple silicon — Wikipedia, the
Sadek, K. A. A. and Elias, M. Algorithms for caching and           free encyclopedia, 2024a.     URL https:
  mts with reduced number of predictions. In The Twelfth           //en.wikipedia.org/w/index.php?title=
  International Conference on Learning Representations,            Apple_silicon&oldid=1250932871. [Online;
  2024.                                                            accessed 14-October-2024].
                      Efficient LLM Inference using Dynamic Input Pruning and Cache-Aware Masking

Wikipedia. List of qualcomm snapdragon systems on chips
 — Wikipedia, the free encyclopedia, 2024b. URL https:
 //en.wikipedia.org/w/index.php?title=
 List_of_Qualcomm_Snapdragon_systems_
 on_chips&oldid=1250680585. [Online; accessed
 15-October-2024].
Wikipedia. Flash memory — Wikipedia, the free encyclo-
 pedia, 2024. URL https://en.wikipedia.org/
 w/index.php?title=Flash_memory&oldid=
 1251065401. [Online; accessed 31-October-2024].
Xue, Z., Song, Y., Mi, Z., Chen, L., Xia, Y., and Chen, H.
  Powerinfer-2: Fast large language model inference on a
  smartphone. arXiv preprint arXiv:2406.06282, 2024.
Zhang, S. and Papyan, V. Oats: Outlier-aware pruning
  through sparse and low rank decomposition. arXiv
  preprint arXiv:2409.13652, 2024.
Zhang, S., Roller, S., Goyal, N., Artetxe, M., Chen, M.,
  Chen, S., Dewan, C., Diab, M., Li, X., Lin, X. V.,
  et al. Opt: Open pre-trained transformer language models.
  arXiv preprint arXiv:2205.01068, 2022.
Zhao, W. X., Zhou, K., Li, J., Tang, T., Wang, X., Hou, Y.,
  Min, Y., Zhang, B., Zhang, J., Dong, Z., et al. A survey of
  large language models. arXiv preprint arXiv:2303.18223,
  2023.
