    L IGHTWEIGHT S OFTWARE K ERNELS AND H ARDWARE E XTENSIONS FOR
    E FFICIENT S PARSE D EEP N EURAL N ETWORKS ON M ICROCONTROLLERS


         Francesco Daghero 1 Daniele Jahier Pagliari 1 Francesco Conti 2 Luca Benini 2 3 Massimo Poncino 1
                                                 Alessio Burrello 1


                                                           A BSTRACT
        The acceleration of pruned Deep Neural Networks (DNNs) on edge devices such as Microcontrollers (MCUs) is a
        challenging task, given the tight area- and power-constraints of these devices. In this work, we propose a three-fold
        contribution to address this problem. First, we design a set of optimized software kernels for N:M pruned layers,
        targeting ultra-low-power, multicore RISC-V MCUs, which are up to 2.1× and 3.4× faster than their dense
        counterparts at 1:8 and 1:16 sparsity, respectively. Then, we implement a lightweight Instruction-Set Architecture
        (ISA) extension to accelerate the indirect load and non-zero indices decompression operations required by our
        kernels, obtaining up to 1.9× extra speedup, at the cost of a 5% area overhead. Lastly, we extend an open-source
        DNN compiler to utilize our sparse kernels for complete networks, showing speedups of 3.21× and 1.81× on a
        ResNet18 and a Vision Transformer (ViT), with less than 1.5% accuracy drop compared to a dense baseline.

1       I NTRODUCTION                                               for sparsity, especially at low pruning ratios (Yu et al.,
                                                                    2017). In the State-of-the-Art (SotA), this issue is tack-
The execution of Deep Neural Networks (DNNs) on extreme             led by a combination of model-, software- and hardware-
edge devices, such as IoT end-nodes based on Microcon-              level countermeasures. At the DNN model level, structured
trollers (MCUs), has become increasingly popular (Wang              or semi-structured pruning forces specific patterns in the
et al., 2020). Local execution enables smart capabilities in        positions of non-zero (NZ) weights, simplifying memory
these devices while avoiding the costly transmission of raw         access and indices storage. A popular example is N:M prun-
data, with advantages in latency predictability, data privacy,      ing, in which exactly N weights are NZ, in every group of
and energy efficiency (Sze et al., 2017; Shi et al., 2016).         M (Zhou et al., 2021). Several solutions for accelerating
However, since edge devices operate on tight memory and             sparse workloads have been proposed at lower levels of the
power constraints, DNNs need to be extensively optimized            stack, ranging from optimized software kernels to custom
before they can be deployed on MCUs. Techniques such                hardware. The latter includes complete accelerators (Chen
as neural architecture search (Wu et al., 2019), quantiza-          et al., 2019; Peltekis et al., 2024), functional units within
tion (Wu et al., 2020; Nagel et al., 2021), and pruning (Yu         a CPU pipeline (Jeong et al., 2023), or tensor core exten-
et al., 2017; Trommer et al., 2021) aim at reducing DNNs’           sions, such as the one featured by the NVIDIA A100 GPU
memory occupation and computational requirements while              family (NVIDIA, 2024).
limiting accuracy drops.
                                                                    Extending sparsity support to MCUs, however, is not trivial,
In particular, weight pruning removes (i.e., sets to zero) the      as their tight area and power constraints often do not allow
least relevant weights of a DNN, with potential benefits            the additional overhead of a complete accelerator or com-
for both memory and computation, as operations involv-              plex core modifications. The few existing works (Scheffler
ing zeroed-out weights can be skipped (Yu et al., 2017).            et al., 2023) target unstructured sparsity, not exploiting the
However, sparse workloads have less regular memory ac-              advantages of constrained pruning patterns such as N:M. As
cesses and lower arithmetic intensity than their dense coun-        a consequence, they require the availability of uncommon
terparts, leading to lower-than-expected performance gains          HW features, such as Streaming Semantic Registers (SSRs),
when leveraging HW/SW stacks not explicitly designed                which contribute significantly to the total area of the system.
    1
     Politecnico di Torino, Turin, Italy 2 University of Bologna,   At the same time, purely software solutions are complex
Bologna, Italy 3 ETH Zurich, Zurich, Switzerland. Correspondence    to implement on MCUs, which feature simple ISAs with
to: Francesco Daghero <francesco.daghero@polito.it>.                limited vectorial capabilities.
Proceedings of the 8 th MLSys Conference, Santa Clara, CA, USA,     This work tries to overcome this gap by proposing a solu-
2025. Copyright 2025 by the author(s).                              tion for the acceleration of semi-structured N:M sparsity on
      Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

MCUs. Specifically, our contributions are the following:           Pruning Patterns: Fig. 1 depicts three common pruning
                                                                   patterns proposed in the literature. Unstructured (i.e., uncon-
                                                                   strained) pruning leads to the highest compression ratio for a
    • We design a set of software kernels for 1:4, 1:8, and
                                                                   given accuracy target. However, accelerating unconstrained
      1:16 sparsity on MCUs, targeting both convolutions
                                                                   sparse workloads is challenging: skipping computations
      (Conv) and fully-connected (FC) layers. With software
                                                                   requires performing multiple indirect memory loads with an
      only, we achieve speedups ranging from 1.1× to 1.85×
                                                                   irregular pattern, significantly impacting arithmetic density,
      for Conv and from 1.02× to 3.4× for FC with respect
                                                                   as well as cache/scratchpad hit rates. Therefore, for non-
      to the best dense library available, on the RISC-V MCU
                                                                   extreme sparsity ratios, layers with unstructured sparsity are
      presented in (Rossi et al., 2021), featuring an 8-core
                                                                   often even slower than their dense counterparts (Yu et al.,
      Parallel-Ultra-Low-Power (PULP) cluster.
                                                                   2017).

    • We then extend the ISA of our target with a new,             Structured pruning introduces constraints on the location of
      lightweight instruction, aimed at improving the effi-        the NZ elements, yielding more regular data, at the cost of
      cacy of our sparse kernels. Specifically, we accelerate      lower accuracy for the same sparsity (Li et al., 2022b; Tan
      the activations decimation (i.e., the selection of activa-   et al., 2022; Guo et al., 2020). For instance, block-wise spar-
      tions corresponding to NZ weights), enabling efficient       sity (Li et al., 2022b) preserves dense groups of NZ elements
      use of the available Single Instruction Multiple Data        of dimension A × B to increase L1 memory and register
      (SIMD) instructions. Implementing this extension in-         utilization, but forcing such coarse-grained patterns usually
      curs an area overhead of 5.0%, but allows our kernels’       yields large accuracy drops (Li et al., 2022b) Extremizing
      to obtain up to 1.9× extra speedup.                          this concept, feature or channel pruning (Li et al., 2022a),
                                                                   not shown in the figure, eliminates entire rows of the weight
                                                                   matrix, producing a result that is equivalent to a dense (but
    • Lastly, we instrument a DNN compiler (Hamdi et al.,          smaller) weight tensor, thus removing all the difficulties of
      2024) to use our sparse kernels. On two complete             sparsity, but worsening performance even more.
      DNNs, a Convolutional Neural Network (CNN) and
      a Vision Transformer (ViT), we achieve a latency re-         The N:M approach represents a middle-ground between un-
      duction of 1.31× and 1.43×, respectively, using 1:4          structured and block-wise sparsity (often referred to as semi-
      sparsity, with no accuracy loss. At 1:16 sparsity, with      structured), enforcing the constraint of N non-zero elements
      an accuracy drop <1.5%, we show speedups of 3.21×            in every group of M values. This scheme facilitates load bal-
      for the CNN and 1.81× for the ViT.                           ancing and parallelism at all levels (multicore, SIMD, etc),
                                                                   as equally-sized tensor portions comprising a multiple of M
                                                                   elements always require the same amount of computation,
2      BACKGROUND                                                  while also ensuring a partial locality in activation accesses.
2.1     DNN Pruning                                                NVIDIA GPUs have added hardware support for this format
                                                                   starting from the A100 series (NVIDIA, 2020), although
Several works have shown how large portions of weights             limited to 2:4 or 1:2 patterns, depending on the data type.
can be pruned with limited impact on the prediction quality
of DNNs (Ma et al., 2023; Park et al., 2016). Optimized
                                                                   Sparse Data Structures: Several formats have been intro-
implementations of pruned models leverage compressed
                                                                   duced in the literature to store the NZ elements of a sparse
representations for the sparse weights matrices, storing only
                                                                   tensor and the corresponding indices with different memory
NZ elements and the corresponding coordinates. Only the
                                                                   versus decoding overhead tradeoffs. The simplest COOrdi-
activations corresponding to NZ weights are loaded and
                                                                   nates (COO) format stores a sparse matrix as three arrays,
processed at inference time, while computations associated
                                                                   containing the NZ elements and their (row, column) posi-
with zeroed weights are skipped.
                                                                   tion. While not requiring any extra computation to obtain
Our work focuses on optimizing the execution of an already         the coordinates, this format has a non-negligible memory
pruned DNN. Therefore, it is orthogonal to the specific prun-      overhead: using 8-bit integer values and 16-bit indices, the
ing strategy, i.e., the method used to select what and when        minimum sparsity required to balance the memory overhead
to prune (for example, using weights magnitude or 1st/2nd          is 75%. The Compressed Sparse Rows (CSR) format also
order loss approximations). We refer readers to (Hoefler           uses three one-dimensional arrays containing the NZ ele-
et al., 2021) for a comprehensive survey on this topic. In         ments, the number of elements per row, and the column
the rest of this section, we focus instead on the most com-        indices. In practice, CSR compresses the row indexes of
mon pruning patterns (i.e., constraints on the position of         the COO format, trading off speed for memory efficiency.
NZ weights to improve sparse layers’ efficiency); then, we         However, its memory overhead is still non-negligible, re-
detail common data structures for sparse tensors.                  quiring more than 50% sparsity to be advantageous on 8-bit
      Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers




                               Figure 1. Pruning patterns and indices compressions with 75% sparsity.


quantized values.                                                   3    R ELATED W ORKS
The N:M format, shown in the center of Figure 1, stores              Many works in the literature have focused on accelerat-
sparse data in two matrices. The first contains the NZ values       ing sparse DNN workloads. Some of them target high-end
and has dimensions (R0 , C M N ) . The second stores the rel-
                            0
                                                                    CPUs/GPUs, either introducing novel software kernels to
ative indices of the NZ elements within each M-sized block,         exploit existing HW or proposing HW extensions to achieve
compressed in ⌈log2 (M )⌉ bits, usually rounded to the near-        higher speed-ups. For instance, the authors of (Wang, 2021;
est larger power-of-two (e.g., M=8 uses 4 bits). This format        Castro et al., 2023; Jeong et al., 2023) exploit graph-level
enables memory-efficient storage even at low sparsity ratios,       optimizations, custom sparsity formats and new instruc-
such as 1:2, but requires additional unpacking operations to        tions to achieve speedups of up to 4x w.r.t. dense baselines.
extract the compressed indices.                                     Other works design complete HW accelerators, achieving
                                                                    impressive speed-ups at the cost of significant area over-
2.2     IoT Edge Nodes                                              heads (Chen et al., 2019; Peltekis et al., 2024; Han et al.,
                                                                    2016).
The compute platforms in IoT edge nodes are tradition-
ally centered around low-power and low-cost MCUs. How-              While less explored, some articles also target the execution
ever, in recent years, these devices have been progressively        of sparse DNNs on MCUs. In (Yu et al., 2017), the authors
equipped with more advanced features to support the ef-             propose pruning groups of weights with the same dimension
ficient local execution of advanced applications such as            as the target’s SIMD width, so that, at inference time, SIMD
Digital Signal Processing (DSP) and DNNs. Accordingly,              instructions can still be utilized. This is a particular instance
it is not uncommon for modern MCUs to include multiple              of blockwise pruning with dimensions A × B = 1 × SIMD-
processing elements and specialized instructions such as            width. They benchmark their custom kernels on an ARM
SIMD. At the same time, since energy and area costs re-             Cortex M4 MCU with 2-way fixed-point SIMD instructions,
main critical, these devices often give up cache memories in        obtaining maximum speedups of 1.38× on a ConvNet at
exchange for a multi-level hierarchy of software-controlled         59.95% sparsity and of 3.51× on a LeNet-5 at 93.28% spar-
scratchpads, where data transfers between levels are handled        sity, among CNNs. Additionally, they achieve a speed-up
through Direct Memory Access (DMA) controllers. Com-                of 9.17× at 93.07% sparsity on a LeNet300, composed
panies like STM (STMicroelectronics, 2024), NXP (NXP,               only of FC layers. However, on the tested MCU, weights
2024) and GreenWaves (GreenWaves, 2024) have already                are directly loaded from Flash memory, with high latency.
commercialized such architectures.                                  Therefore, the benefits of skipped loads due to SIMD-aware
                                                                    pruning hide the overheads of blockwise sparse processing,
In this work, we target the Vega PULP SoC (Rossi et al.,
                                                                    “idealizing” the speedups (absolute inference times would
2021), which features 10 RISC-V cores and is the blueprint
                                                                    remain very high and bottlenecked by memory accesses).
architecture for commercial products such as GreenWaves’
                                                                    Moreover, as mentioned in Sec. 2, blockwise sparsity can
GAP9 (GreenWaves, 2024). In Vega, one core acts as the
                                                                    lead to large accuracy drops on more complex DNNs and
Fabric Controller (FC), managing peripherals and orches-
                                                                    datasets (Li et al., 2022b) (LeNet is tested on MNIST, while
trating the entire workload; one controls the DMA set-up;
                                                                    ConvNet reaches 81.86% accuracy on CIFAR10).
the other 8 are organized as a cluster to speed up computa-
tion. All cores feature an extended ISA including SIMD dot          The work of (Trommer et al., 2021) focuses on unstructured
product instructions on 8-bit integers, loads with automatic        sparsity, proposing a linear encoding for CSR indices to re-
post increment, and hardware loops to accelerate DSP and            duce their memory overhead in exchange for extra decoding
DNN workloads. The SoC features a 128 kB L1 data mem-               operations. They benchmark their approach on an ARM
ory, shared between all cluster cores, a 1.6 MB L2 main             Cortex-M55, using three DS-CNN variants and comparing
memory (comprising an MRAM memory, which we do not                  with two dense baselines derived from CMSIS-NN (Lai
exploit), and 16 MB of external L3 HyperRAM memory.                 et al., 2018), as well as with the storage of relative indices
                                                                    (RI) in CSR. They achieve a maximum reduction of cycles
                                                                    on the largest DS-CNN of 41.7% compared to the original
    Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

CMSIS-NN library and 6.7% compared to hand-optimized
                                                                                 Table 1. Layer dimensions notation
kernels, with 90% weights sparsity on point-wise Convo-                 Tensors                  Dimensions                 Abbreviations
lutions, and FC layers. They also show a 5.21× memory                   Input (I)          rows, columns, channels             IX/IY/C
                                                                       Output (O)          rows, columns, channels            OX/OY/K
reduction on the pruned layers, with a memory footprint                Weights (W)   filter height/width, channels in/out    FX/FY/C/K
only 3.5% higher than the CSR’s RI storage. Speedups com-              Activations              Padding/Stride                   P/S
pared to optimized dense kernels are relatively low due to
the large decoding overhead.
The authors of (Titopoulos et al., 2023) implement an            way to accelerate N:M sparsity on MCUs, either through
ISA extension to accelerate N:M sparsity on a 64-bit             SW or through an area-inexpensive HW extension, without
RISC-V core paired with a 16-lane 512-bit vector engine.         relying on ad-hoc accelerators. The aim is to unlock a
Their new instruction, called IndexMAC, allows low-cost          new set of Pareto-optimal trade-offs in the space of model
indirect access to row portions of the dense activation          accuracy vs. latency reduction vs. area cost, bridging the
matrix, pre-loaded in a vector register file. Moreover,          gap between the significant speedups offered by dedicated
they introduce a set of optimized kernels performing row-        HW accelerators and classic MCUs without HW and SW
wise matrix multiplication with this instruction, achieving      extensions.
1.82×/2.14×/1.92× speed-ups over a SW-only sparse base-          This section describes our sparse convolutional (Sec 4.1)
line on ResNet50, Densenet121, and InceptionV3 at 75%            and FC (Sec. 4.2) kernels, focusing first on SW-only ver-
sparsity. Differently from our work, the deployment target       sions and then showing how those can be enhanced with a
is a high-end RISC-V core, leading to significantly different    lightweight ISA extension. We detail the HW that imple-
design choices. First, MCUs do not include a dedicated           ments our new instruction in Sec. 4.3. Our kernels operate
vector register file and have limited options for data pre-      on 8-bit data, already stored in L1 memory. To execute
loading, given their small L1 memory size. Second, in their      full networks, we integrate them in the DNN compiler of
work, NZ indices are stored uncompressed, limiting the           (Hamdi et al., 2024), as described in Sec. 4.4. We focus
memory reduction in exchange for increased throughput.           on semi-structured N:M sparsity, given the demonstrated
Finally, their vector engine supports 32-bit element vec-        benefits over an unstructured approach with a limited loss
tors and scalar-by-vector operations, while we target 8-bit      in accuracy (Zhou et al., 2021). Namely, we support 1:4,
integer SIMD without any scalar-by-vector instruction.           1:8, and 1:16 formats, as increasing sparsity further would
In (Scheffler et al., 2023), the authors introduce a RISC-       lead to a too high accuracy loss, while reducing it would
V ISA extension to accelerate computations with unstruc-         not lead to any latency benefit. While we target the SoC
tured sparsity, supporting both CSR and Compressed Sparse        of (Rossi et al., 2021), our SW-only kernels are general
Fiber (CSF) formats. They extend RI5CY cores with Sparse         enough to be used on all MCUs from the PULP family, i.e.,
Stream Semantic Registers (SSSRs), which transparently           RISC-V-based multi-core MCUs with hierarchical scratch-
load/store data based on a stream of indices, making them        pad memories (Gautschi et al., 2017), and are written in
available to other instructions (e.g., dot products). This in-   plain C code. Table 1 reports the notation used in the rest of
creases FPU utilization by getting rid of explicit load/store    the paper for layer hyper-parameters.
instructions. While extremely effective (5x speedup at           In all our kernels, we use the format shown in Fig. 1 and
95.7% sparsity on GEMMs ), SSSRs are targeted for float-         detailed in Sec. 2 to store N:M sparse weights. Namely, only
ing point kernels and are not easily extensible to support       NZ weights are stored, together with their indices within
low-precision integer SIMD calculations. Moreover, SSSRs         the corresponding M-sized block, compressed on 4-bits
are complex circuits whose area overhead (≈20 to 31 kGE          (for 1:8 and 1:16 sparsity) or 2-bits (for 1:4). Thus, 1:4
depending on the configuration) is already significant (20-      sparsity leads to a weight memory reduction of 68.75%, 1:8
31%) when considering an FPU-equipped RI5CY with its             of 81.25%, and 1:16 of 90.62% (with int8 weights). Notice
102 kGE (Schuiki et al., 2020), as baseline. When compared       that, in the same conditions, the CSR format requires storing
                                                                                                  Y ×C
to an FPU-less RI5CY, the overhead increases to as much as       K row pointers and K×F X×F    M       column indices with a
44%. Lastly, CSR and CSF formats require the storage of          minimum precision of 16-bit (for reasonably sized layers),
NZ indices at high precision, thus resulting in significantly    leading to less than 25% compression at 75% sparsity (i.e.,
higher memory overheads w.r.t. the N:M format.                   the equivalent of the 1:4 format).

4    L IGHTWEIGHT K ERNELS FOR N:M                               4.1     Convolutional Kernel Library
     S PARSE DNN S ON MCU S                                      4.1.1    Dense Baseline
  As outlined in Section 1, the novelty of our work lies in      The dense 8-bit kernels utilized as a baseline in our work are
being the first, to the best of our knowledge, to propose a      taken from the SotA PULP-NN library, described in (Garo-
   Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers




Figure 2. Inner loop of the PULP-NN dense convolutional kernel.        Figure 3. Inner loop of our sparse convolutional kernel.


falo et al., 2020). Fig. 2 visualizes their internal loop: after
                                                                   linear increase in the im2col buffer memory overhead. This,
performing a partial im2col transformation that reorganizes
                                                                   in turn, would limit the usability of our kernels for layers
2 spatially contiguous input patches into 1-dimensional ar-
                                                                   with a large number of input channels or big filters, given
rays, the kernel loads the weights of 4 convolutional filters
                                                                   that the im2col array must necessarily fit in L1.
and iterates to produce a total of 8 outputs, (2 different
spatial positions, and 4 output channels). The dataflow is         The key difficulty in implementing kernels for N:M pruning
output-stationary; all output channels relative to the same        is optimizing the loading of input activations corresponding
spatial positions are produced before moving to the next           to NZ weights in the innermost loop. We explored three
ones. The detailed loop order is shown in the bottom-left of       alternatives:
the figure. The outermost loops over OX and OY are par-
                                                                   1) DMA-based copy: modify the DMA calls used to move
allelized on the platform’s cores. Furthermore, the MACs
                                                                   data from L2 to L1 memory to move, for each output chan-
of the innermost loop are performed using 4x8-bit SIMD
                                                                   nel, only activations corresponding to NZ weights (bypass-
dot product instructions available in the RISC-V XpulpV2
                                                                   ing the im2col entirely). However, this would eliminate
ISA extension (Gautschi et al., 2017). Notice that the partial
                                                                   the advantages of DMA burst transfers, leading to highly
im2col operation leads to an additional L1 memory require-
                                                                   inefficient loads.
ment of FX×FY×C×2×N CORES elements.
                                                                   2) Sparse Im2col: modify the im2col step to fill the 1-d
This kernel achieves a theoretical peak performance in the
                                                                   buffers only with activations corresponding to NZ weights.
innermost loop of 2.28 8-bit MACs/instruction/core (32
                                                                   However, there would be no reuse opportunity for these
MACs with 14 instructions, i.e., six load words and 8 SIMD
                                                                   buffers, as different output channels do not share the NZ
dot-products). However, it cannot be directly extended for
                                                                   indices. The im2col would therefore become a part of the
sparse layers, as the inner loop unrolling assumes that the
                                                                   innermost loop, being repeated for every output channel,
input activations used to generate all 4 output channels
                                                                   with a consequent explosion of innermost loop instructions.
are identical, which enables reusing the im2col buffers.
This is no longer true for N:M sparse layers. Therefore,           3) Decimate Im2col: keep the im2col step unchanged while
given the impossibility of implementing such unrolling for         adding a decimate step in the innermost loop, that selects
sparse kernels, we also consider a dense variant with 1x2          only the elements corresponding to NZ weights from the
unrolling, i.e., computing a single output channel per inter-      im2col buffer, for each different output channel. Compared
nal loop iteration, that reaches a lower theoretical peak of       to the previous option, the main difference is that the ad-
1.6 MACs/instruction/core. The internal loop of this variant       dresses of activations corresponding to NZ weights have to
is shown on the left of Fig. 4.                                    be computed relative to the im2col buffer, and not to the
                                                                   original input tensor. In other words, they are computed
4.1.2   Software-only Sparse Kernel                                as i×M + o, where i identifies the correct block of M
                                                                   elements in the im2col buffer, and o is the relative offset of
The SW-only versions of our sparse kernels only exploit
                                                                   the NZ weight within the block. Conversely, to load activa-
instructions from the XpulpV2 extension, already available
                                                                   tions directly from the input tensor, we should consider its
in (Rossi et al., 2021). They have the same data flow and
                                                                   spatial size (IY, IX) and the center position of each patch to
data stationarity of the dense baseline and use an innermost
                                                                   compute the correct address, considering corner cases for
loop unrolling factor of 1x2, i.e., over two consecutive input
                                                                   different stride and padding combinations, leading to a high
patches and one weight filter. The single output channel
                                                                   number of indexing operations in the innermost loop.
per iteration is due to the reasons mentioned above. Instead,
further increasing the unrolling factor over the input patches     Fig. 3 visualizes our sparse convolutions, which use the
would improve offsets and weights reuse but would lead to a        latter strategy (Decimate Im2col). Fig. 4 (center) details the
   Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers




Figure 4. Innermost iteration of the dense matmul kernel (left), 1:8 / 1:16 sparse kernel with no custom instructions (center), and 1:8 /
1:16 sparse kernel with the xDecimate instruction (right).


corresponding innermost loop execution. Here, OFFSETS                 we design a new instruction, xDecimate, to load an 8-
is the array whose elements store the relative indices of NZ          bit element from a buffer into a register, given an address
elements in each M-sized block, on 4-bit (for 1:8 and 1:16            and an offset, essentially merging the extractOffset
sparsity) or 2-bit (for 1:4); the extractOffset function              function and the load byte operation of the SW-only ker-
uses shift and mask operations to unpack the correct index            nel. Thanks to it, we reduce the instructions to fill the input
from each byte-sized element of the array. vB1 and vB2,               registers to 8, and the total instructions in the innermost
the two 32-bit registers that store input activations, are filled     loop from 22 (or 23) to 12, regardless of the sparsity level,
using the four unpacked indices pos<i>. Weights loading               as shown in Fig. 4 (right). Thus, we reach a peak of 0.66
and dot products are identical to the dense baseline.                 MACs/instruction/core, corresponding to 2.64, 5.28, and
                                                                      10.56 equivalent dense MACs/intruction/core, respectively
Each iteration of the 1:8 and 1:16 kernels performs 8 MACs
                                                                      at 1:4, 1:8 and 1:16 sparsity.
and 22 instructions, leading to a peak performance of 0.36
MACs/instructions/core. The 1:4 version has all the four              The syntax of the instruction is the following: xdecimate
indices required to fill a 32-bit word stored in a single             rd, rs1, rs2, where rd is the register in which ex-
8-bit OFFSETS element, but requires more complex un-                  tracted data will be stored, while the two source registers
packing. Overall, it requires 23 instructions (2 more mask-           contain the starting address of the im2col buffer (rs1) and
ings, one less load) per iteration, leading to a peak of 0.35         the packed NZ offsets (rs2). At a high level, each execution
MACs/instruction/core. Considering the number of theoret-             of the instruction loads one activation byte in rd, comput-
ical dense MACs executed, i.e., multiplying the effective             ing the starting address of the target M-sized block in the
number of MACs by the sparsity factor M, we obtain peak               im2col buffer, and adding the correct offset to it. It does so
performances of 1.4, 2.88, and 5.76 MACs/instruction/core.            by combining rs1 and rs2, with the value stored in one
                                                                      control-status-registers (csr1 ). The csr is auto-incremented
Notably, although our implementation is partly target-
                                                                      at every xDecimate operation so that consecutive calls
specific, the same design choices performed for our kernels
                                                                      to xDecimate automatically point to the correct data. A
can be extended to support other MCUs, without relying
                                                                      specific instruction, xDecimate.clear, is used to reset
on features like on-board DMA or the XPulpV2 ISA exten-
                                                                      it to 0 at the end of the loop over the output channels.
sion. For instance, ARM Cortex-M architectures leverage
CMSIS-NN (Lai et al., 2018) kernels and SIMD operations               To account for the innermost loop unrolling (over two
for dense convolutions, thus very similar kernel routines can         im2col buffers), the M-sized block’s base address and the
be designed to add support for N:M sparsity.                          offset in the destination register are updated only once every
                                                                      two xDecimate executions. Instead, the rs2 bits from
4.1.3   ISA-extended Sparse Kernel                                    which the NZ offset is unpacked are updated every time.
                                                                      This requires duplicating each NZ index in the OFFSETS
The main bottleneck of the SW-based sparse kernels lies
                                                                      array, with a consequent memory overhead. However, it
in the indices unpacking and vector packing operations; 19
                                                                      allows us to accommodate, with a single instruction, also
instructions are required to load the two activation regis-
                                                                          1
ters, vB1 and vB2 (9 for computing indices, 8 for loading                  We use a lowercase acronym to avoid confusion with CSR,
data, 2 for updating addresses). To tackle this limitation,            the Compressed Sparse Row format.
      Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers




Figure 5. Innermost iteration of the dense FC kernel (left), 1:8 SW-only sparse kernel (center), and 1:8 ISA-extended sparse kernel (right).
the case of FC layers, as detailed in Sec. 4.2.3. Notably, the
overhead is acceptable given the low bit-width of indices;
we still obtain weight memory savings of 62.5% for 1:4
sparsity, 75% for 1:8, and 87.5% for 1:16. The hardware
implementation of xDecimate is detailed in Sec. 4.3.

4.2     Fully-Connected Kernel Library
4.2.1     Dense Baseline
                                                                        Figure 6. For FC layers, NZ OFFSETS are reorganized offline
The innermost loop of our dense baseline for FC layers                  alternating 2 output channels. Activations are then loaded in two
(again taken from PULP-NN), is shown in Fig. 5 (left). It is            buffers vB1 and vB2 (example for 1:4 sparsity).
unrolled by a factor of 2 over the K dimension, as FC layers
have no opportunities for weight reuse. The efficiency peak
for this kernel is 1.6 MACs/instruction/core. Multi-core                o1,chi , o1,chi+1 , etc.). In the kernel’s inner loop, we load NZ
parallelization is performed on the K dimension.                        weights for channels i and i+1 in two 4x8-bit registers (vA1
                                                                        and vA2), before calling xDecimate eight times to load
4.2.2     Software-only Sparse Kernel                                   8 activations from a single im2col buffer, alternating vB1
                                                                        and vB2 as destinations. As a result, vB1 will eventually
We use the same innermost loop as for convolutions, unpack-             contain the activations corresponding to the i-th channel’s
ing the indices of four NZ elements and then using them for             NZ weights, and vB2 those corresponding to the i + 1-th
dot products. The pseudo-code is shown in Fig. 5 (center).              channel, allowing to perform 8 MACs. In other words, we
Differently from the dense baseline, we do not unroll over              essentially unroll over two output channels (K = 2) despite
two output channels, given that each of them requires dif-              not having input reuse opportunities, with the objective of
ferent input data. The theoretical peak performance is 0.25             maintaining the same exact instruction originally designed
MACs/instruction/core. Considering the equivalent dense                 for Convolutions. Fig. 6 shows the complete flow. The
MACs, we obtain 1.0, 2.0, and 4.0 MACs/instruction/core                 theoretical peak performance obtained with this kernel is
for 1:4, 1:8, and 1:16 sparsity. Notice that the 1:4 SW-                0.61 dense equivalent MACs/instruction/core, which trans-
only sparse kernel does not even reach sufficient theoretical           lates to 2.44, 4.88, and 9.76 MACs/instruction/core, always
performance to outperform the dense baseline.                           outperforming the dense baseline.

4.2.3     ISA-extended Sparse Kernel                                    4.3        xDecimate Hardware Implementation
The innermost loop of our ISA-extended sparse FC ker-                   We prototyped the functionality of the xDecimate
nel is shown in Fig. 5 (right). To make our HW extension                instruction at the register-transfer level (RTL) inside
lightweight, we exploit the same xDecimate instruction                  the open-source RI5CY/CV32E40P processor 2 used
designed for Convolutions. As mentioned in Sec. 4.1.3, the              in (Rossi et al., 2021). The RTL is available open-source at
instruction updates the M-sized block’s base address, and               https://github.com/eml-eda/cv32e40x deci
the offset in rd only once every two executions. For FC                 mate. RI5CY implements the RV32IMC ISA with the
layers, which differently from Conv are not unrolled over               addition of the Xpulpv2 extension, which includes
two im2col buffers, we do not duplicate the NZ offsets,                 hardware loops, load/store with post-increment, SIMD
but rather, we reorganize them (offline). Namely, we con-               dot-product instructions, etc.      We selected a RISC-V
struct the OFFSETS array by alternating the offsets of NZ               processor for implementing our xDecimate instruction,
elements in two consecutive channels (i.e., o0,chi , o0,chi+1 ,
                                                                              2
                                                                                  https://github.com/openhwgroup/cv32e40p
   Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

                                                                 memory response and written into the rd register in the po-
                                                                 sition selected by the csr (again right-shifted to account for
                                                                 unrolling). The updated rd is propagated back to RI5CY’s
                                                                 RF. Then, the csr is automatically incremented to prepare
                                                                 for the next execution. Formally:

                                                                  rd[(csr[2 : 1] ∗ 8 + 7) : (csr[2 : 1] ∗ 8)] ← MEM[addr]
                                                                  csr ← csr + 1


                                                                 The XFU controller also checks for data dependencies be-
                                                                 tween consecutive xDecimate instructions to enable for-
                                                                 warding the value of rd from the WB stage.

Figure 7. Detail of xDecimate eXtension Functional Unit (XFU)    4.4   Integration in the MATCH Compiler
micro-architecture prototyped inside the RI5CY processor.
                                                                 To run end-to-end neural networks and support layer tiling,
                                                                 we integrate our sparse kernels into MATCH (Hamdi et al.,
leveraging the inherent extensibility of the ISA. However,       2024), an extension to Apache TVM for heterogeneous
in principle, our hardware extension could also be adapted       SoCs. We added three main features to MATCH to support
to other architectures, such as ARM.                             our sparse kernels efficiently:
We modified the processor by adding an eXtension Func-           1) Modified Pattern Recognition: the first compilation step
tional Unit (XFU), whose microarchitecture is shown in           of MATCH associates DNN graph patterns to known accel-
Fig. 7, which encompasses three of the four stages of            eration targets based on a HW-specific pattern table. Starting
RI5CY’s pipeline: Instruction Decode (ID), Execute (EX),         from the existing PULP target, we added new patterns that
and Write-Back (WB). Differently from regular RISC-V             extend the ones already present for convolutional/FC layers,
loads, the xDecimate instruction employs an R-type en-           with an additional constraint on the weights’ values, check-
coding with two source registers rs1, rs2 and one destina-       ing the NZ weights positions and recognizing 1:4, 1:8, or
tion register rd; all three registers are simultaneously read    1:16 sparsity formats.
from the register file (RF, not shown in Fig. 7) in the ID
stage, exploiting RI5CY’s 3-read port RF, which is also nec-     2) Tiling for Sparse Kernels: the integration of N:M sparse
essary for the Xpulpv2 extension. A lightweight decoder          kernels requires tiling both the NZ weight tensors and the
identifies which flavor of xDecimate (i.e., which sparsity       associated NZ indices. Our modified tiling engine optimizes
format) is being used.                                           the tile layout by taking into account both the reduced di-
                                                                 mension of the weights and the overhead due to the indices.
The EX stage implements the extension’s main functionality,      To do so, we simply changed the number of bits associated
which for 1:8 and 1:16 sparsity, can be summarized as:           with each weight. As an example, considering 1:4 sparsity,
                                                                 we need 12 bits to store each NZ weight (8 bits for the value,
     o ← rs2[(csr[2 : 0] ∗ 4 + 3) : (csr[2 : 0] ∗ 4)]            4 bits for the replicated weight offset). Since the other 3
     addr ← rs1 + M ∗ csr[15 : 1] + o                            weights are zero, this is equivalent to having 3-bit per dense-
                                                                 equivalent weight. Thanks to this modification to the tiling
                                                                 engine, we are able to tile sparse layers better, obtaining
The csr is used to select the appropriate bits to unpack         additional speed-ups due to better L1 utilization.
the NZ offset from the 32-bit word loaded in rs2. The
                                                                 3) Weights Memory Storage: To further enhance perfor-
csr is also used to compute the starting address of the
                                                                 mance, we modify the memory layout handling in MATCH
current M-sized block. For this, it is right-shifted by one
                                                                 to better support the simultaneous transfer of the compressed
position, thus ensuring that two consecutive xDecimate
                                                                 weights and their corresponding indices. In our approach,
calls point to the same block (thus accounting for our inner
                                                                 the weights and indices are stored in L2 memory in an inter-
loop unrolling). Both offsets are added to the base address
                                                                 leaved fashion. Specifically, if a layer is tiled on K/2 out-
loaded in rs1, and the result is propagated as a memory
                                                                 put channels, MATCH stores the corresponding half of the
request through RI5CY’s load/store unit. The instruction
                                                                 weights, followed by the corresponding indices, so that both
for 1:4 sparsity is similar, just using 4 csr LSBs instead of
                                                                 can be transferred with a single DMA transaction. Once the
3, given that rs2 contains 16 2-bit wide offsets in this case.
                                                                 first tile has been processed, the second half of weights and
In the WB stage, the interesting byte is extracted from the      indices are fetched in the same manner.
      Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers




Figure 8. Single-layer results. Bars are grouped by input channels/features (C). Numbers report speedups over the dense 1x2 baseline.

5      R ESULTS                                                      for convolutions and C ∈ [256, 512, 1024, 2048] for FC
                                                                     layers. Fig. 8 shows the performance of 1:4, 1:8, and 1:16
5.1     Experimental Setup                                           sparse kernels, without (SW) our with (ISA) xDecimate,
We benchmark our proposed kernels and two end-to-end                 as well as the results of the two dense baselines, 1x2 and
networks using the GVSoC virtual platform (Bruschi et al.,           PULP-NN (FC layers have a single baseline, given that also
2021) to simulate Vega (Rossi et al., 2021). The HW ex-              PULP-NN uses 1x2 unrolling).
tension has been implemented in SystemVerilog RTL and                As expected from the inner loop instructions analysis of
synthesized using Synopsys Design Compiler targeting the             Sec. 4 the 1:4 SW-only Convolution performs worse than the
same 22nm technology as (Rossi et al., 2021) with 200 MHz            dense versions (+23% cycles on average, w.r.t. the 1x2 base-
target clock frequency in worst-case operating conditions            line). At the other extreme, the 1:16 SW-only kernel obtains
(slow-slow process, 0.72 V operating voltage, 125 C tem-             an average speedup of 2.6× and 1.85× compared to the 1x2
perature). The two DNNs are a ResNet18 (He et al., 2016),            and the PULP-NN baselines. Notice that the lower speed-up
trained on Cifar100, and a Vision Transformer (Dosovitskiy           compared to the theoretical one obtained comparing inner
et al., 2020) (ViT-Small), trained on a rescaled 224x224             loop instructions (3.6× vs the 1x2 baseline) is mainly due
version of Cifar10. Both have been trained for 200 epochs            to the im2col step, which is identical in sparse and dense
using the combined training and pruning scheme detailed              kernels and reduces the overall MACs/cycle performance.
in (Zhou et al., 2021). For ResNet, we apply N:M pruning             ISA-extended kernels further improve performance, making
to 3x3 convolutions, leaving pointwise layers dense. For             1:4 sparsity advantageous too, with an average speedup of
the ViT, we sparsify only the FC layers of the feed-forward          1.50× and 1.12× over 1x2 and PULP-NN. At 1:8 sparsity,
block. Note that these layers are part of many other rele-           the speedups grow to 2.4× and 1.74×, and at 1:16 sparsity,
vant transformer-based architectures, such as (Devlin et al.,        to 3.9× and 2.78×, respectively. For all kernels, perfor-
2019; Raffel et al., 2020). As a consequence, our approach           mance tends to improve with increasing C; the improvement
is easily transferable to other architectures, where compa-          is more marked for sparser layers, given that our kernels
rable savings to our benchmarks on ViT can be expected.              accelerate the innermost matrix multiplication loop, whose
Note also that both the task and the training of the pruned          relative importance over the constant im2col phase grows
networks are orthogonal to our work, making our approach             with C. At C=256, given the high occupation of the im2col
usable also on more complex datasets.                                buffer, L1 tiles for Conv layers over spatial dimensions start
After training, we quantize all networks to 8 bits using             to be very small. Thus, performance reduces slightly.
Brevitas (Pappalardo, 2023). All individual kernels and              For FC layers, SW-only sparse kernels outperform the base-
end-to-end ResNets have been deployed using the modi-                lines even at 1:4 sparsity, albeit with modest latency reduc-
fied MATCH compiler. For ViTs, given the lack of support             tions (2% on average). This is particularly evident on larger
for attention layers in MATCH, we computed the latency               geometries (e.g., C = 2048), where the speedup reaches
layer-by-layer, using Deeploy (Scherer et al., 2024), an al-         1.2×. Although there is no theoretical gain in the inner loop,
ternative compiler for the same HW, for attention layers,            this improvement derives from the fewer weight loads re-
and MATCH for sparse/dense feed-forward layers. The re-              quired, given the lower memory footprint of sparse weights.
ported MACs/cycle values always refer to dense-equivalent            This contribution is less evident (or absent) for convolu-
operations.                                                          tions, where the latency of weights transfers from L2 to
                                                                     L1 is hidden using double-buffering (Hamdi et al., 2024).
5.2     Single Layers Benchmarking                                   For memory-bound FC layers, instead, these transfers are
We first benchmark both convolutional and FC layers, vary-           one of the dominant components of the overall latency. At
ing the input channels/neurons dimension while keeping               higher sparsity ratios (1:8 and 1:16), the average speedups
other parameters fixed; we set the output channels/neurons           increase to 1.6× and 2.3×, respectively, with peaks up to
K = 256, and for convolutions, we also use IX/IY = OX/OY             3.4×. ISA-extended kernels yield more significant improve-
= 8, FX/FY = 3, S = 1, P = 1. We vary C ∈ [32, 64, 128, 256]         ments, achieving an average 1.8× speedup at 1:4 sparsity
      Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers


                                    Table 2. End-to-end models results on the (Rossi et al., 2021) platform.
                                                                    SW-only kernels                          ISA-extended kernels
 Model            Dataset        Sparsity     Acc.[%] MAC/cyc. Cyc.[M] Mem.[MB] MAC/cyc. Cyc.[M] Mem.[MB]
                                 Dense        95.59      4.65           975.23       21.59            -           -          -
                                 1:4          95.73      4.80           944.17       11.86            6.66        681.19     11.86
 ViT              CIFAR10
                                 1:8          95.02      6.31           718.86       10.09            7.48        606.99     10.09
                                 1:16         95.17      7.59           598.04       8.76             8.40        540.23     8.76
                                 Dense 1x2 75.28         8.33           66.63        11.22            -           -          -
                                 PULP-NN 75.28           11.17          49.71        11.22            -           -          -
 ResNet18         CIFAR100
                                 1:4          75.78      8.11           68.44        3.66             14.74       37.67      4.35
                                 1:8          75.63      14.78          37.57        2.29             23.12       24.01      2.98
                                 1:16         73.79      25.85          21.48        1.26             35.87       15.48      1.6

                                                                        98% of the total MACs. Training the models with 1:4 and
          Table 3. Comparison with the state-of-the-art.
                                                                        1:8 sparsity, we obtained even higher accuracy than the
      Benchmark       Spars.          Speedup Area[%]                   dense network, while 1:16 yields a slight performance drop
      LeNet 1              93.28%             3.51         -            of 1.49%. In terms of latency, similarly to the result ob-
      ConvNet 1            59.9%              1.38         -            tained on single layers, the model that leverages the 1:4
      LeNet300 1           93.07%             9.17         -            SW-only kernels is outperformed by both dense baselines
      DS-CNN 2             90%                1.71         -
                                                                        (1x2 and PULP-NN), achieving 1.03× and 1.38× higher la-
      ResNet503            75%                1.82∗        n.a.
      DenseNet3            75%                2.14∗        n.a.         tency, although it reduces the memory footprint from 11.22
      InceptionV33         75%                1.92∗        n.a.         MB to 3.66 MB. SW-only sparse models outperform their
      spMV4                95.7%              5∗           44           dense counterparts at 1:8 and 1:16 sparsity. With the ISA
      ResNet18-SW 87.5-93.75% 1.77-3.10 -                               extension, all sparse ResNets achieve speedups against both
      ResNet18-ISA 75-93.75%                  1.77-4.31 5               baselines. The most compressed model that does not incur
      1
        (Yu et al., 2017), 2 (Trommer et al., 2021), 3 (Titopoulos      accuracy drops uses 1:8 sparsity, reaching 0.35% higher
        et al., 2023), 4 (Scheffler et al., 2023), ∗ speedup compared   accuracy, 2.07× lower latency, and 3.77× lower memory
        to SW-only sparse baseline                                      footprint compared to the best dense option, i.e., PULP-NN.
with a peak of 2.3× for C = 2048. At 1:8 and 1:16 sparsity,             Notice that ResNet versions using xDecimate require
the average speedups are 2.2× and 2.9×, respectively.                   slightly more memory than SW-only ones due to the repli-
                                                                        cation of NZ indices in Conv kernels (see Sec. 4.1.3).
5.3     End-to-end DNNs Benchmarking                                    While our benchmark DNNs are relatively small, compati-
Table 2 reports profiling results of end-to-end models. We              bly with edge applications’ constraints, we underline that
also report the accuracy obtained by our sparse models to               even greater speedups would be achieved on bigger models,
demonstrate that the considered sparsity patterns are appli-            given the combined effect of a more aggressive pruning
cable to real-world applications, leading in most cases to              applicable to them and the lower impact of overhead effects
null or small degradations.                                             (e.g., external loop management) on bigger layers, in which
                                                                        most of the complexity resides in the internal convolutional
In the ViT model, the sparsified FC layers account for 65%              kernel loops that we optimize.
of the model’s parameters and 60% of the operations. De-
spite the relevant parameters reduction, even with the most             5.4   SoTA Comparison
aggressive pruning pattern (1:16), sparsifying these layers
impacts the accuracy minimally, with a drop of 0.42%. On                Table 3 compares our work with the SotA on sparse accel-
the other hand, in terms of latency, all sparsified models,             eration for MCUs. We report the benchmark DNNs, the
both with and without the new xDecimate operation, out-                 sparsity level, the speedup obtained w.r.t. a dense execution,
perform the dense baseline. Using SW-only kernels, we                   and the area overhead, if present. Concerning our results,
achieve speedups of 1.03×, 1.36×, and 1.63×, respectively,              we report those on ResNet since, to our knowledge, no SotA
at 1:4, 1:8, and 1:16 sparsity. ISA-extended kernels outper-            work considered N:M pruning for transformers on MCUs.
form the dense models even further, achieving speedups of
                                                                        The authors of (Yu et al., 2017) report speedups from 1.31×
1.43×, 1.61×, and 1.81×. Overall, our most compressed
                                                                        to 9.17×. At a high sparsity (≥90%), they achieve 3.21×
model (1:16) achieves 95.17% accuracy with a 2.34× lower
                                                                        speed-up over a dense LeNet CNN and 9.17× on the FC-
memory footprint compared to the dense counterpart and a
                                                                        only LeNet300. As anticipated in Sec. 3, the high speed-
1.81× latency reduction when using our ISA extension.
                                                                        up on the LeNet300 is caused by the high-latency loads
Concerning ResNet18, the sparsified convolutions (all but               from Flash memory. Since FC models are memory-bound,
the pointwise) account for 97% of the total parameters and              strongly reducing the number of loads almost linearly trans-
    Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

lates to latency savings. Conversely, on more compute-            R EFERENCES
bound CNNs, the load latency impact is lower, and our
                                                                  Bruschi, N., Haugou, G., Tagliavini, G., Conti, F., Benini, L.,
ISA-extended solution achieves better speed-ups for both
                                                                    and Rossi, D. GVSoC: A Highly Configurable, Fast and
aggressive (4.31× vs 3.51× at ≥90% sparsity) and moder-
                                                                    Accurate Full-Platform Simulator for RISC-V based IoT
ate pruning (1.77× vs 1.38× for 75% vs 59.9% sparsity).
                                                                    Processors. In 2021 IEEE 39th International Conference
With SW-only kernels, we achieve comparable results.
                                                                    on Computer Design (ICCD), pp. 409–416, 2021.
The Large DS-CNN from (Trommer et al., 2021), at 90%
sparsity, achieves 1.71× speed-up compared to dense unopti-       Castro, R. L., Ivanov, A., Andrade, D., Ben-Nun, T.,
mized CMSIS-NN kernels. Similarly, at 87.5% sparsity, we            Fraguela, B. B., and Hoefler, T. VENOM: A Vectorized
obtain 1.77×/2.77× speed-ups with the SW and ISA kernels            N:M Format for Unleashing the Power of Sparse Tensor
compared to the 1x2 baseline. Note also that, with the N:M          Cores. In Proceedings of the International Conference for
format, we obtain a memory reduction of 79.59/73.44%,               High Performance Computing, Networking, Storage and
compared to the 74.45% obtained by (Trommer et al., 2021),          Analysis, SC ’23, New York, NY, USA, 2023. Association
with similar levels of sparsity (87.5% vs 90%).                     for Computing Machinery. ISBN 9798400701092.

In (Titopoulos et al., 2023), the authors report speedups         Chen, Y.-H., Yang, T.-J., Emer, J., and Sze, V. Eyeriss v2: A
of 1.82/1.92/2.14× on different convolutional networks at           flexible accelerator for emerging deep neural networks on
75% sparsity, comparing their ISA-enhanced results to SW-           mobile devices. IEEE Journal on Emerging and Selected
only sparse kernels. On our ResNet18 model at iso-sparsity,         Topics in Circuits and Systems, 9(2):292–308, 2019.
thanks to xDecimate, we achieve a 1.82× speedup w.r.t.
the SW-only version, despite the fact that our HW extension,      Devlin, J., Chang, M.-W., Lee, K., and Toutanova, K. Bert:
being tailored for ultra-low-power edge devices, is much            Pre-training of deep bidirectional transformers for lan-
more lightweight than the one in (Titopoulos et al., 2023),         guage understanding. In Proceedings of the 2019 confer-
which leverages a large vector RF on a more powerful core           ence of the North American chapter of the association for
(and whose area overhead is not reported).                          computational linguistics: human language technologies,
                                                                    volume 1 (long and short papers), pp. 4171–4186, 2019.
In (Scheffler et al., 2023), a 5× speedup over SW-only
sparse kernels for GEMM is reported, at 95.7% sparsity,           Dosovitskiy, A., Beyer, L., Kolesnikov, A., Weissenborn,
higher than the 1.39×@93.75% sparsity (i.e. 1:16) that we           D., Zhai, X., Unterthiner, T., Dehghani, M., Minderer, M.,
achieve with our ISA extension vs the SW-only ResNet18.             Heigold, G., Gelly, S., et al. An image is worth 16x16
However, note that for such extreme values a sparsity, an          words: Transformers for image recognition at scale. arXiv
apparently small difference corresponds to executing 6.25%          preprint arXiv:2010.11929, 2020.
of the original MACs (in our case), vs 4.3% (i.e. 1.45×
fewer) in (Scheffler et al., 2023). Furthermore, the HW           Garofalo, A., Rusci, M., Conti, F., Rossi, D., and Benini,
support required to achieve such an acceleration leads to           L. PULP-NN: Accelerating quantized neural networks
an area overhead of 44% w.r.t. an FPU-less core like ours,          on parallel ultra-low-power RISC-V processors. Philo-
versus our 5%.                                                      sophical Transactions of the Royal Society A, 378(2164):
                                                                    20190155, 2020.

6    C ONCLUSIONS                                                 Gautschi, M., Schiavone, P. D., Traber, A., Loi, I., Pullini,
                                                                    A., Rossi, D., Flamand, E., Gürkaynak, F. K., and Benini,
We have introduced a set of efficient kernels N:M pruning on        L. Near-Threshold RISC-V Core With DSP Extensions
MCUs of the PULP family, and a lightweight extension of             for Scalable IoT Endpoint Devices. IEEE Trans. Very
the XPulpV2 ISA, to further speed them up. On the platform          Large Scale Integr. Syst., 25(10):2700–2713, October
described in (Rossi et al., 2021), and targeting a ViT and a        2017. ISSN 1063-8210.
ResNet18, we have achieved end-to-end latency reductions
of 1.81× and 3.21×, with less than 0.5/1.5% accuracy drop         GreenWaves.          GAP9.             https://
on CIFAR10/CIFAR100 respectively. Our future work will              greenwaves-technologies.com/gap9_
study the impact of variable sparsity patterns (e.g., per-layer     processor/, 2024. [Accessed 10-06-2024].
or per-channel) on latency and accuracy, considering both
the pure software kernels and the xDecimate-enhanced              Guo, C., Hsueh, B. Y., Leng, J., Qiu, Y., Guan, Y., Wang,
ones. Further, we will prototype our hardware extension             Z., Jia, X., Li, X., Guo, M., and Zhu, Y. Accelerating
on FPGA to enable an estimation of the energy savings               sparse DNN models without hardware-support via tile-
achieved by our kernels, which can show further advantages         wise sparsity. In SC20: International Conference for
in the reduced off-chip memory accesses.                           High Performance Computing, Networking, Storage and
                                                                   Analysis, pp. 1–15. IEEE, 2020.
   Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

Hamdi, M. A., Daghero, F., Sarda, G. M., Van Delm, J.,            NVIDIA. TensorRT. https://developer.nvidia.
  Symons, A., Benini, L., Verhelst, M., Pagliari, D. J.,           com/tensorrt/, 2024. [Accessed 10-06-2024].
  and Burrello, A. MATCH: Model-Aware TVM-based
  Compilation for Heterogeneous Edge Devices. arXiv               NXP.           LPC4300.                 https:
  preprint arXiv:2410.08855, 2024.                                 //www.nxp.com/products/
                                                                   processors-and-microcontrollers/
Han, S., Liu, X., Mao, H., Pu, J., Pedram, A., Horowitz,           arm-microcontrollers/
  M. A., and Dally, W. J. EIE: Efficient Inference Engine on       general-purpose-mcus/
  Compressed Deep Neural Network. In 2016 ACM/IEEE                 lpc4300-arm-cortex-m4-m0:MC_
  43rd Annual International Symposium on Computer Ar-              1403790133078#/, 2024. [Accessed 10-06-2024].
  chitecture (ISCA), pp. 243–254, 2016.
He, K., Zhang, X., Ren, S., and Sun, J. Deep residual learn-      Pappalardo, A. Xilinx/brevitas, 2023. URL https://
  ing for image recognition. In Proceedings of the IEEE             doi.org/10.5281/zenodo.3333552.
  conference on computer vision and pattern recognition,
                                                                  Park, J., Li, S., Wen, W., Tang, P. T. P., Li, H., Chen, Y., and
  pp. 770–778, 2016.
                                                                    Dubey, P. Faster cnns with direct sparse convolutions and
Hoefler, T., Alistarh, D., Ben-Nun, T., Dryden, N., and             guided pruning. arXiv preprint arXiv:1608.01409, 2016.
  Peste, A. Sparsity in deep learning: Pruning and growth
  for efficient inference and training in neural networks.        Peltekis, C., Titopoulos, V., Nicopoulos, C., and Dimi-
 Journal of Machine Learning Research, 22(241):1–124,               trakopoulos, G. DeMM: A Decoupled Matrix Multi-
  2021.                                                             plication Engine Supporting Relaxed Structured Sparsity.
                                                                    IEEE Computer Architecture Letters, 23(1):17–20, 2024.
Jeong, G., Damani, S., Bambhaniya, A. R., Qin, E., Hughes,
  C. J., Subramoney, S., Kim, H., and Krishna, T. VEG-            Raffel, C., Shazeer, N., Roberts, A., Lee, K., Narang, S.,
  ETA: Vertically-Integrated Extensions for Sparse/Dense            Matena, M., Zhou, Y., Li, W., and Liu, P. J. Exploring
  GEMM Tile Acceleration on CPUs. In 2023 IEEE In-                  the limits of transfer learning with a unified text-to-text
  ternational Symposium on High-Performance Computer                transformer. Journal of machine learning research, 21
  Architecture (HPCA), pp. 259–272. IEEE, 2023.                     (140):1–67, 2020.
Lai, L., Suda, N., and Chandra, V. CMSIS-NN: Efficient            Rossi, D., Conti, F., Eggiman, M., Di Mauro, A., Tagliavini,
  neural network kernels for Arm Cortex-M CPUs. arXiv               G., Mach, S., Guermandi, M., Pullini, A., Loi, I., Chen, J.,
  preprint arXiv:1801.06601, 2018.                                  et al. Vega: A ten-core SoC for IoT endnodes with DNN
Li, G., Ma, X., Wang, X., Yue, H., Li, J., Liu, L., Feng,           acceleration and cognitive wake-up from MRAM-based
  X., and Xue, J. Optimizing deep neural networks on in-            state-retentive sleep mode. IEEE Journal of Solid-State
  telligent edge accelerators via flexible-rate filter pruning.     Circuits, 57(1):127–139, 2021.
  Journal of Systems Architecture, 124:102431, 2022a.
                                                                  Scheffler, P., Zaruba, F., Schuiki, F., Hoefler, T., and Benini,
Li, S., Osawa, K., and Hoefler, T. Efficient quantized sparse       L. Sparse Stream Semantic Registers: A Lightweight ISA
   matrix operations on tensor cores. In SC22: International        Extension Accelerating General Sparse Linear Algebra.
  Conference for High Performance Computing, Network-               IEEE Transactions on Parallel and Distributed Systems,
   ing, Storage and Analysis, pp. 1–15. IEEE, 2022b.                2023.
Ma, X., Fang, G., and Wang, X. LLM-Pruner: On the                 Scherer, M., Macan, L., Jung, V., Wiese, P., Bompani, L.,
 structural pruning of large language models. Advances in           Burrello, A., Conti, F., and Benini, L. Deeploy: Enabling
 neural information processing systems, 36:21702–21720,             Energy-Efficient Deployment of Small Language Mod-
 2023.                                                              els On Heterogeneous Microcontrollers. arXiv preprint
Nagel, M., Fournarakis, M., Amjad, R. A., Bondarenko,               arXiv:2408.04413, 2024.
 Y., Van Baalen, M., and Blankevoort, T. A white pa-
  per on neural network quantization. arXiv preprint              Schuiki, F., Zaruba, F., Hoefler, T., and Benini, L. Stream
  arXiv:2106.08295, 2021.                                           semantic registers: A lightweight RISC-V ISA extension
                                                                    achieving full compute utilization in single-issue cores.
NVIDIA.        A100 tensor core gpu architec-                       IEEE Transactions on Computers, 70(2):212–227, 2020.
 ture.          https://images.nvidia.com/
 aem-dam/en-zz/Solutions/data-center/                             Shi, W., Cao, J., Zhang, Q., Li, Y., and Xu, L. Edge com-
 nvidia-ampere-architecture-whitepaper.                             puting: Vision and challenges. IEEE internet of things
 pdf, 2020. [Accessed 10-06-2024].                                  journal, 3(5):637–646, 2016.
   Lightweight Software Kernels and Hardware Extensions for Efficient Sparse Deep Neural Networks on Microcontrollers

STMicroelectronics.                STM32H7.
  https://www.st.com/en/
  microcontrollers-microprocessors/
  stm32h7-series.html, 2024. [Accessed 10-06-
  2024].
Sze, V., Chen, Y.-H., Yang, T.-J., and Emer, J. S. Efficient
  processing of deep neural networks: A tutorial and survey.
  Proceedings of the IEEE, 105(12):2295–2329, 2017.
Tan, Y., Han, K., Zhao, K., Yu, X., Du, Z., Chen, Y., Wang,
  Y., and Yao, J. Accelerating Sparse Convolution with
  Column Vector-Wise Sparsity. In Koyejo, S., Mohamed,
  S., Agarwal, A., Belgrave, D., Cho, K., and Oh, A. (eds.),
  Advances in Neural Information Processing Systems, vol-
  ume 35, pp. 30307–30317. Curran Associates, Inc., 2022.
Titopoulos, V., Alexandridis, K., Peltekis, C., Nicopoulos,
  C., and Dimitrakopoulos, G. Indexmac: A custom risc-v
  vector instruction to accelerate structured-sparse matrix
  multiplications. arXiv preprint arXiv:2311.07241, 2023.
Trommer, E., Waschneck, B., and Kumar, A. dcsr: a
  memory-efficient sparse matrix representation for parallel
  neural network inference. In 2021 IEEE/ACM Interna-
  tional Conference On Computer Aided Design (ICCAD),
  pp. 1–9. IEEE, 2021.
Wang, F., Zhang, M., Wang, X., Ma, X., and Liu, J. Deep
 learning for edge computing applications: A state-of-the-
 art survey. IEEE Access, 8:58322–58336, 2020.
Wang, Z. SparseDNN: Fast sparse deep learning inference
 on CPUs. arXiv preprint arXiv:2101.07948, 2021.
Wu, B., Dai, X., Zhang, P., Wang, Y., Sun, F., Wu, Y., Tian,
 Y., Vajda, P., Jia, Y., and Keutzer, K. FBNet: Hardware-
 Aware Efficient ConvNet Design via Differentiable Neu-
 ral Architecture Search. In Proceedings of the IEEE/CVF
 conference on computer vision and pattern recognition,
 pp. 10734–10742, 2019.
Wu, H., Judd, P., Zhang, X., Isaev, M., and Micikevi-
 cius, P. Integer quantization for deep learning infer-
 ence: Principles and empirical evaluation. arXiv preprint
 arXiv:2004.09602, 2020.

Yu, J., Lukefahr, A., Palframan, D., Dasika, G., Das, R.,
  and Mahlke, S. Scalpel: Customizing DNN pruning to
  the underlying hardware parallelism. ACM SIGARCH
  Computer Architecture News, 45(2):548–560, 2017.

Zhou, A., Ma, Y., Zhu, J., Liu, J., Zhang, Z., Yuan, K., Sun,
  W., and Li, H. Learning N: M Fine-grained Structured
  Sparse Neural Networks From Scratch. arXiv preprint
  arXiv:2102.04010, 2021.
