# Frozen protocol — 2026-09-09, before GPU execution

Scope: cached DeepSeek-V4-Flash-0731 revision 7872f01b1d1fe23eabc4c98b48bffcef5a386062, layer 0 experts [0,1,7,42,128,255], mapped in that order to local IDs 0..5. No full model, router, real activation distribution, retrieval quality, other operators, or performance ranking. Inputs and routing are synthetic. Source snapshots and SHA256 are recorded before importing the installed function. No framework patching.

Read header/index and exact selected byte ranges only; preserve every selected key, original shape/dtype/offset/shard/raw SHA256 and payload. Weight is I8 packed low nibble even K, high nibble odd K; native scale F8_E8M0 [N,K/32], group of 32 consecutive K values. No swizzle, transpose, inverse, or Marlin repack. Assemble w13=[w1;w3] along output rows, w2 unchanged; stack six experts. GPU scales use Torch's native E8M0 view→FP32; independent reference checks the byte mapping explicitly. Save assembled packed weights, FP32 scales, inputs, IDs, weights, outputs and CPU references/stages.

CPU reference: explicit E2M1 table [0,.5,1,1.5,2,3,4,6,-0,-.5,-1,-1.5,-2,-3,-4,-6], E8M0 scalar table 2**(b-127) for b=0..254 and NaN for 255. FP64 dequantization and matrix multiplication. BF16 round-to-nearest-even implemented independently with integer bit operations at GEMV1, activated SwiGLU, GEMV2, masked-down, routing-weight cast, weighted product, reduced output, final routed factor. SwiGLU gate upper-only clamp, up symmetric clamp, scalar SiLU and multiply independently in FP64; original activation arithmetic is FP32, expected tiny boundary differences are measured. BF16 reduction accumulates in FP32 in the original; exact FP64 sum then BF16 for the reference (six BF16 terms have ample FP32 precision at these scales). Original slot GEMV accumulates FP32, reference accumulates FP64.

Fixed criteria per case: all finite; EVERY output element abs(y-ref) <= 0.02*abs(ref) + 0.002*RMS(ref); relative-L2 <= 0.01. The RMS absolute floor accommodates BF16 cancellation, relative allowance covers multiple stored BF16 stages. Zero-reference cases require exact zero. Report every error and failing index, max absolute, RMS-normalized max, relative-L2, quantiles and count. No threshold relaxation after observing failures. Offloader, slot permutation, zero routes, and padded versus zero routing controls additionally require bitwise output identity where specified.

Cases: smoke first using real expert slices H=128,I=64, M=1,topk6; pass required before full shape. Full H=4096,I=2048, M=1 and 8, topk6; NumPy PCG64 seed 20260909, Gaussian std=.01 cast BF16. IDs (slot+token)%6; weights (1..6)/21, cyclic shift per token, FP32 then original BF16 cast. Main clamp=10, factor=1.5 from config. M8 controls: six one-hot route selections (one per local expert), all-zero routing, -1 invalid slot versus the same route zeroed, slot reversal, unclamped case, forced-active clamp=.005 (same small input) to exercise clamp asymmetry. Controls are bounded, no random weights. Each direct run calls installed mxfp4_moe_forward_triton unmodified. Optional one Torch profiler call on the main M8 input, no timing comparisons. Official OffloaderV1 wraps a four-parameter small module whose forward calls that same original function; verify parameters are CPU and same-input outputs are bitwise equal. Capture exception if unsupported.

Resource controls: before GPU init nvidia-smi free >=4096 MiB; one Python GPU process, four CPU affinity cores and 4 library threads. Torch allocator cap 2 GiB (room for context within 3 GiB); supervisor samples own process-tree RSS and GPU PID memory, aborts only its own child if >8 GiB RSS or >3 GiB GPU. Sampling is not an instantaneous hardware quota; report measured peaks and allocator peak. All caches/temp files redirected inside this experiment. No other process stopped. Local artifacts <=500 MiB. Exit first, transfer, then standalone analysis. Full FP64 dequantized matrices are transient, never saved; reference reproducible from packed files.
