# 文件后端契约实测

复制原模型运行产生的一页真实BF16 KV（2,359,296字节）。在七个独立目录中调用安装的HiCacheFile原函数，验证完整、缺失、截断2字节、翻转1位、追加5字节、model_name变化、损坏文件上再次set。记录返回、异常、目标张量及文件哈希。使用CPU张量，不启动模型，不修改原存储。

此为后端API边界实验，不替代端到端请求故障行为。异常前目标张量可能已写入；单个原语未校验内容不等于高层永远没有其他验证。模型身份只测试此参数影响文件命名，不推断adapter/布局等完整缓存身份是否安全。
