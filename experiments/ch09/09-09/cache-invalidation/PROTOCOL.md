# 原生路由器与缓存清除

同版本原生cache_aware路由器与两个Qwen实例，三轮独立路由器，每轮四次相同3136-token实际Agent输入：冷、热、清两实例缓存后、再次热。两实例清缓存通过实际API确认，在途请求为零；保留路由器进程。核对worker原生日志归属、响应真实缓存数与输出。此为显式清除，不冒充自然容量淘汰、事件丢失或跨进程恢复。若清除后仍去原实例但缓存为零，只证明亲和选择不足以保证物理命中，不直接证明内部索引未更新。每轮请求按因果顺序，非随机性能实验。
