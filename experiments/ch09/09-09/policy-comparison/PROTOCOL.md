# 三规则实际对照

复用completion-prediction已冻结模型及SHA，不调参。两实例同GPU、相同目标及背景，预算8/32/128 × 缓存/队列/预测三策略 × 两轮，共18组54生成调用。每轮九条件seed911随机交错，每组清缓存预热；记录决策、实际队列与输出。比较目标完成和两任务全部完成，分别报告，不将命中或目标最短等同整机吞吐。前轮结果已知，非盲测；两轮不推断稳定显著性。仅客户端选路，不当原生Router功能。
