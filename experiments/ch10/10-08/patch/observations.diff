--- a/verl/workers/engine/fsdp/transformer_impl.py
+++ b/verl/workers/engine/fsdp/transformer_impl.py
@@ -787,6 +787,8 @@
         Returns:
             grad_norm (float): Norm of gradients before clipping.
         """
+        from observe import optimizer as _observe_optimizer
+        _observe_optimizer(self, "before")
         assert self.optimizer_config.clip_grad is not None
 
         # getattr fallback: some subclasses (e.g. VeOmniEngine) bypass FSDPEngine.__init__.
@@ -826,6 +828,7 @@
 
             invalidate_all_scales(self.module)
 
+        _observe_optimizer(self, "after")
         return grad_norm.item()
 
     def lr_scheduler_step(self):
--- a/verl/trainer/ppo/ray_trainer.py
+++ b/verl/trainer/ppo/ray_trainer.py
@@ -661,6 +661,8 @@
 
             test_batch = test_batch.union(test_output_gen_batch)
             test_batch.meta_info["validate"] = True
+            from observe import batch as _observe_validation
+            _observe_validation(test_batch, self.global_steps, stage="validation")
 
             # Store original inputs
             input_ids = test_batch.batch["prompts"]
@@ -1673,6 +1675,8 @@
                             norm_adv_by_std_in_grpo=norm_adv_by_std_in_grpo,
                             config=self.config.algorithm,
                         )
+                    from observe import batch as _observe_batch
+                    _observe_batch(batch, self.global_steps)
                     # update critic
                     if self.use_critic:
                         with marked_timer("update_critic", timing_raw, color="pink"):
@@ -1799,6 +1803,8 @@
                 )
 
                 # TODO: make a canonical logger that supports various backend
+                from observe import metrics as _observe_metrics
+                _observe_metrics(metrics, self.global_steps)
                 logger.log(data=metrics, step=self.global_steps)
 
                 progress_bar.update(1)
--- a/verl/workers/rollout/vllm_rollout/utils.py
+++ b/verl/workers/rollout/vllm_rollout/utils.py
@@ -396,6 +396,8 @@
                     for model in self._iter_all_models():
                         if peft_config is None:
                             model.load_weights(param_updates)
+                            from observe import receiver as _observe_receiver
+                            _observe_receiver(model, param_updates)
                         else:
                             names = {n for n, _ in model.named_parameters(remove_duplicate=False)}
                             names.update(n for n, _ in model.named_buffers())
@@ -421,7 +423,7 @@
         local_rank = _resolve_vllm_weight_sync_local_rank(self.local_rank, parallel_config)
         trainer_rank_base = os.environ.get("VERL_ZMQ_BASE_TRAINER_RANK")
         trainer_rank = int(trainer_rank_base) + local_rank if trainer_rank_base is not None else local_rank
-        return f"ipc:///tmp/rl-colocate-zmq-{job_id}-replica-{replica_rank}-rank-{trainer_rank}.sock"
+        return f"ipc://{os.environ['VERLRL_IPC']}/rl-colocate-zmq-{job_id}-replica-{replica_rank}-rank-{trainer_rank}.sock"
 
     def update_verl_delta_weights(self, update_info: dict) -> None:
         """Add this worker's IPC endpoint and forward the delta update to vLLM."""
--- a/verl/workers/rollout/vllm_rollout/vllm_rollout.py
+++ b/verl/workers/rollout/vllm_rollout/vllm_rollout.py
@@ -138,7 +138,7 @@
         # stale socket file) cannot collide on the shared /tmp namespace.
         local_rank = self.rollout_rank % local_world_size
         job_id = ray.get_runtime_context().get_job_id()
-        self.zmq_handle = f"ipc:///tmp/rl-colocate-zmq-{job_id}-replica-{self.replica_rank}-rank-{local_rank}.sock"
+        self.zmq_handle = f"ipc://{os.environ['VERLRL_IPC']}/rl-colocate-zmq-{job_id}-replica-{self.replica_rank}-rank-{local_rank}.sock"
 
         self.use_shm = not is_support_ipc()
         self._delta_weight_transfer_engine_initialized = False
@@ -238,7 +238,8 @@
             bucket_size_mb=bucket_size_mb,
             use_shm=self.use_shm,
         )
-        await sender.async_send_weights(weights)
+        from observe import sender as _observe_sender
+        await sender.async_send_weights(_observe_sender(weights, global_steps))
 
         if future is not None:
             await future
