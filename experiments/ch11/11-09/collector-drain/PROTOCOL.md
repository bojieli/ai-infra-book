# 持续新到达下的真实Collector积压

固定contrib0.133.0、file_storage/fsync=true、100请求队列与单消费者。120个事件计划20次/s立即投递、实际时间保留，每事件2048字节padding（不是整个OTLP请求大小）。前2秒后端503，之后每次请求分别额外服务等待10/80ms；真实HTTP/文件队列/SQLite提交，不用模拟队列代替。两轮，轮内条件seed1109打乱。服务等待是受控软件延迟，不冒充物理带宽。

核对所有120事件逐字内容、重复、实际发送偏差及最后提交；从源端dispatch到后端提交重建未完成事件数（包含传输/队列/在途，不称Collector内部队列长度）。后端恢复后第一次旧积压清空和停发后最终排空分别报告。无队列容量/32/64MiB预算复算，无GPU与外部资料修改。
