# 11-5 超时与资源释放预定方案

固定600000次串行SHA256，先独立取得完整校验结果。三轮各线程等待10ms超时、独立子进程等待10ms后终止并回收、10s宽限额复验。超时从worker已启动后开始，不含排队/创建。记录真实CPU工作与完成哈希、反馈时刻和实际完成/退出观察时刻。只终止本程序创建的受控子进程，无外部服务或GPU。该工作是确定性验证载荷，不是模型生成的CUDA代码；不冒充DistRS/verl运行或批次调度收益。
