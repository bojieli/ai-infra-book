# 主复核补充：离线分析开销的计时窗口

原封存文件保持不变。`checks.json.runtime.process_cpu_s` 来自 `getrusage(RUSAGE_SELF)` 的进程累计 user+system CPU，包含解释器启动及导入；`wall_s` 从 `analyze.py` 的 `main()` 内设置 `begin=time.monotonic()` 开始。两者起点不同，不能相除估分析器占核，也不能用相减推等待。原记录0.100523 CPU秒与0.064268墙钟秒因此不是同一窗口的利用率对照。

这只澄清本次离线分析程序自己的开销字段，不改变11-01原始Agent的逐模型/工具区间CPU差值、墙钟、RSS、质量或资源选择。主agent已独立核对其12轮原始模型墙钟和8.995476951589808秒、工具墙钟和0.27552744932472706秒、模型区间控制器CPU和0.1571458099999994秒、765个实际输出token，与报告一致。原8文件manifest和228个外部源SHA均通过；本补充不在原manifest中。
