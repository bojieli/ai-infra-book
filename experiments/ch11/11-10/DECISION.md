# 实验 11-10｜资源池的扩容选择与验证计划

**选择：先验证模型服务路径及输出质量，再决定是否试增 GPU 服务能力；目前不作采购数量或扩容收益承诺。** 本页完成已封存轨迹的实验性证据部分，未实施扩容，成本、SLO 和相同负载反事实仍待验证。原题的一页估算要求保留，预算计算复用 C65。

**基准负载。** [11-01 真实 Agent](../11-01/README.md)的一次区间合并修复：固定 Qwen3-8B 快照 `b968826d…1218`、BF16、vLLM 0.23.0、Torch 2.11.0+cu130，关闭思考、贪心、每轮上限 1200 token，APC 开启、串行最多 12 轮。规范、初始代码、提示、token IDs、逐轮文件 SHA 和六例检查均可追溯。最终 2/6、未 finish，首次合格结果时间为空，失败消耗保留。

| 资源 | 本次可观测证据 | 选择与待验证点 |
|---|---|---|
| CPU | 循环 9.357789 s；模型区间 8.995477 s（96.13%），该区间控制器 CPU 0.157146 s；工具墙钟 0.275527 s、控制器 CPU 0.075647 s、已回收子进程 CPU 0.179064 s | 不优先扩 CPU。没有完整进程树忙核、CPU 排队或节流记录，不能断言 CPU 扩容收益为零；先测工具排队与核配额。 |
| GPU | 真实本地模型请求占主要墙钟；没有 GPU 利用率、显存峰值或内核争用字段 | 优先补模型服务时间归因和质量门槛；主机侧等待不证明 GPU 饱和。增加 GPU 是否降低同质量任务延迟待测。 |
| 内存 | 控制器采样峰值 1298.469 MiB，模型区间内持续驻留；不含活模型 worker 或测试子进程 RSS | 先测整个进程树及内存压力。驻留不是内存不足，不能把该值乘环境数估预算，也不能据此选大内存机器。 |
| API 容量 | 本地 AsyncLLMEngine；12 次已记录引擎排队合计 0.198 ms，单次最大 0.032 ms；无外部 API 配额/429/限流证据 | 只增加未被调用的外部 API 配额不会改变这条本地调用路径。生产 API 容量仍未知；Collector 排空证据不等于模型 API 容量。 |
| 预热池 | [11-06](../11-06/README.md)另一条旧轨迹的真实工具重放中，按需/常驻工具累计延迟中位为 0.667/0.165 s，采样峰 RSS 为 36.39/72.88 MiB | 有依据安排小预热池验证。模型间隔是历史等待重放，不能移植其收益到本次 Agent；不以最短时间选择采购。 |

**估算接口。** 已测阶段时间、CPU、RSS、请求用量见 [summary.json](summary.json)；货币成本、预算与 SLO 字段均为 `null`。复用 C65 的[固定教学输入和结果](../../../calculations/results/retry-paths-book.md)及[机器可读结果](../../../calculations/results/retry-paths-book.json)时，路径和 SHA 由 [sources.lock.json](sources.lock.json)固定。它们是假想教学参数，未接入本轨迹，不是当前报价；本批未重算或复制其表格，也不填不存在的扩容估算。

**下一轮接受条件（计划，未执行）。** 先登记时限 L、按时且质量达标率目标 P、最小有用改善 δ 和预算 B；按 [README](README.md)冻结同一任务/模型/工具/到达轨迹，每次只改变一个资源。先做质量与计时预检，再按随机 AB/BA 配对比较基线与干预；失败、超时、清理和全部重试都入账。原六例必须全部通过，独立扩展检查另列。只有达到预先登记的质量/SLO、改善的置信区间满足 δ、且 C65 owner 接入真实价格后满足 B，才接受扩容。当前没有成功任务分母或 matched-workload 扩容记录，不宣称已达 SLO。
