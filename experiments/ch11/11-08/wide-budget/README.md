# 11-8 放宽生成上限后的诊断实测

状态：两次新请求完成，11-8整体仍为partial。上一轮thinking总上限100/1000均未结束思考；本次保持同Qwen3-8B快照、157-token输入、APCoff、贪心解码与六例检查，将总生成上限放宽到4000。旧结果已知，是诊断性扩展，不是盲测；没有修改旧数据或模型代码。

| 轮次 | 实际输出token | 结束 | 首段回答文本秒 | 模型＋原检查秒 | 原六例 |
|---|---:|---|---:|---:|---:|
| 0 | 2524 | stop | 28.154454 | 29.130693 | 4/6 |
| 1 | 2524 | stop | 28.222125 | 29.207976 | 4/6 |

两次完整输出及代码相同，思考结束后才给出JSON。代码修复了嵌套区间终点，但仍原地排序输入，违反“不修改输入”的要求。独立重叠图oracle检查1013个输入并检查输出别名，两次均仅450/1013通过；更广检查没有改变原六例的判定口径。

这次证明1000上限截断不是唯一失败来源：放宽到4000能够自然结束，仍没有得到合格修复。不能以自然结束、长思考或首段文本出现当作任务成功；verified_usable_s继续为空。首token约17–20ms与约28秒后才出现回答文本相差很大，但这些都不是首个通过验证的结果。补充1013例检查另行执行，其时间不混入表内原六例检查时间。

## 独立复现

run.py/fixture.py自带同任务和工具检查。按上级相同专用vLLM环境，在无results的独立目录运行：

    python run.py --model /local/path/to/the/fixed/Qwen3-8B/snapshot

check_code.py为已有独立检查器的本目录副本，使用连通分量oracle而非模型同样的排序折叠；从来没有放进模型工作目录或提示。Linux环境执行 python check_all.py，子进程CPU/地址空间限额及5秒父等待保留，完整stdout和失败输入记录于results/independent-checks.json。初始六例结果与独立检查分开，不能按更多检查事后改写原结果。

python3 analyze.py离线核验正式执行源码、输入/用量/事件、生成文件、原测试及独立检查源码哈希和1013条计数。run.log、原始思考/答案、token IDs和工作目录全部保存；manifest.json封存本目录文件。gpu-after.txt核对原四项GPU服务仍在，正式引擎已关闭。

这是单个已知任务的两次重复，不推广模型总体质量；全部八次尝试仍无合格候选，费用无依据继续未知。其他模型、缓存/排队和实际费用路由仍待。不调用calculations；最终跨session实验与论文复核尚未开始。
