# 同任务思考开关与总输出预算

同一Qwen3-8B区间合并修复任务、相同测试标准，thinking开关和总输出预算三条件：thinking开启100/1000，关闭1000。注意max_tokens限制全部生成token，不是独立的思考token预算；不冒充C64的100/1000思考token手算。两轮随机条件顺序seed1108，APC关闭，串行六请求。每次新工作目录，不将前次答案送回。允许自然结束，保留全部token IDs、输出事件、完整回答、终止原因和工具检查。

记录第一输出token、出现回答文本、模型结束和测试完成，前两者不直接等同可用正确结果。短预算若截断或没有JSON就作为失败保留，长预算也不假定成功。运行前任务及旧模型表现已知，不称新基准。实际费用无服务商价格依据则为空，不以本地token数虚构账单；缓存/排队与多模型路由范围另待。calculations不执行。
