# 11-6 真实工具进程预热协议

固定3-4原始12轮动作、写入字节和模型阶段时长；模型阶段只按已采集时长等待，不重新请求模型。实际执行全部工具；每轮结果和文件SHA必须与原记录一致。两类工具进程file/test，均分配16MiB零初始化控制缓冲（物理驻留以实采RSS为准），用标准Python导入和管道提供服务。这不是容器或虚拟机。

比较resident两个进程持续保留；demand每轮实际调用后创建并在完成后销毁；predict-50ms与predict-fullgap分别提前50ms或整个模型等待阶段启动预测类型进程，正式动作仅在调用发生后发送。预测规则固定：初始预测file，此后根据过去已经完成的工具类型转移预测；未见过转移则预测上一类型。不读取未来动作选择预测。错预测销毁，创建实际类型。预测只准备进程，不提前读写或执行测试。

3重复×4条件，seed1106打乱顺序。同一个工作空间按原始初始文件重置。记录launch、ready、call、send、reply、destroy，采样ps当前worker RSS约50ms加ps耗时，并保留每个worker高水位RSS。PS采样仅覆盖工具常驻worker，不含短命测试子进程、控制器或进程创建前后缺口；积分是采样驻留估计，不能称总平台内存。

原Linux RLIMIT_AS在Mac preexec实际失败，compatibility-probe.json保留报错。正式实验明确移除此项，保留CPU 2秒与subprocess 4秒超时，其余AST校验和工具语义保持。此变化不隐去任何工具失败。比较的是原轨迹工具结果一致性，原Agent任务仍未通过最终质量检查。
