你是用户授权的独立 Codex CLI worker。任务：定位现有 V4 六专家预检数值失败的阶段，执行一个有界真实 GPU 诊断，绝不放宽原协议。唯一可写 experiments/ch02/02-05/expert-stages/ 及 experiments/parallel-workers/v4stages/status.md（和远端对应实验目录）。先读 expert-preflight/README.md、PROTOCOL.md、run.py、reference.py、results/sources/mxfp4_moe_sm120_triton.py 和真实结果。旧目录全部只读，禁止重写旧失败或封存。其他正文/inventory/PROGRESS/research/references/calculations只读，calculations不得执行/复制/修改，不联系 owner。不得再派生 worker，不提交 git。
现有实际 M8 有10/32768输出元素超固定门槛，单专家均通过；原因尚未定位，不能预先宣称舍入足以解释。用相同已保存输入/真实六专家 packed权重/路由，不新生成更容易的输入。优先进程内观测未修改原 mxfp4_moe_forward_triton 函数局部张量（例如 sys.settrace Python return 获取 locals，正确同步复制所有需要的中间张量；不要改共享源码或替换被测算法）。先核对具体局部变量生存期，采集 GEMV1/activation/GEMV2/weighted product/reduce各实际边界，保存原函数输出与旧输出逐位比较，若 autotune造成不同必须如实记录。保存实际选择 autotune config/源SHA，比较每阶段原 CPU FP64参考；再用 GPU实际上一阶段值独立 CPU重算下一阶段，分辨误差传播与本阶段新误差。解包参考不调用被测GPU内核；FP32对照若有解释力可另列，但不能替代原FP64门槛。必要的对照以定位旧10个失败为限，不跑完整模型/完整旧15用例，禁止patch共享环境。
指定远端Python /home/ubuntu/ai-infra-book-experiments/tools/sglang0513-venv/bin/python，Torch2.11cu130/SG.5.13.post1。真实原始权重在远端 expert-preflight/results/，无需下载模型。单GPU进程≤3GiB，4CPU线程，RSS≤8GiB，启动前确认空闲≥4GiB，监控/退出可复用旧launch设计但独立路径，不停止其他服务。新增本地raw≤350MiB。原始执行和传输都退出后才分析，记录每个失败元素阶段来源、完整证据及无法确定部分，运行成功不等于数值通过。中文README、standalone脚本、原始输入来源SHA、输出/阶段/分析/环境/资源/退出/完整manifest。先写status，完成交主agent统一审核；不做全书最终跨session审计。
