用户最新要求：失败记录不要保留，只留最后成功记录。主agent已阅读failureindex的25组93文件候选，授权你执行清理。输入 experiments/parallel-workers/failureindex/candidates.json 和final.md只读。独占可写范围：候选targets所属实验目录及其清单列出的直接文档/manifest/analyzer依赖（仅实验目录，不改outlines/inventory/PROGRESS/根README），以及experiments/parallel-workers/cleanup_history/。不触碰calculations、不运行GPU/模型/正式实验、不接触ch02/02-05或ch10/10-08（另worker负责）。
逐项核实精确文件身份与最终成功证据后删除25组中明确启动/依赖/配置调试失败产物；不要整目录误删清单protected_or_deferred中的科学结果、有效请求、故障注入、正式重复。清单仅93个精确文件，若目录内出现额外文件需检查不直接rmtree。用户优先于封存不变规则，删除后同步重写README到最终成功配置/结果，移除失败历史和死链接，必要时修analyzer对已删日志的依赖但不改科学验收门槛。重新生成受影响manifest保持原格式，处理嵌套manifest从内到外，验证所有保留科学raw哈希未变，写简洁清理结果counts.json；不要把失败内容复制到自己的报告。
远端镜像若能从README/脚本明确对应路径，先只读比对精确候选SHA，匹配才删除那些文件，并同步最终文档/manifest；路径不确定不要猜删、报告给root。等待实际传输结束才报告。本任务应一次完成可清理部分并明确待项，不需要任何用户确认。最终输出删除数量、保留证据校验、修改文档与封存数量。不要修改其他worker的目录或实际服务。
