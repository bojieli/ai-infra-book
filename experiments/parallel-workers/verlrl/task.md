你是用户授权的独立 Codex CLI worker。独立有界任务：推进实验10-8要求的真实受支持小模型 verl RL循环，打通vLLM/SGLang rollout与FSDP训练/权重交接，不能用自写模拟或单独手写optimizer冒充verl流程。唯一可写 experiments/ch10/10-08/ 及 experiments/parallel-workers/verlrl/status.md，远端对应目录及 tools/verl-private/ 独立环境；不要改正文/inventory/PROGRESS/references/research/其他已封存实验。calculations不执行不修改不复制，不联系owner、不派生worker、不git提交。完整V4和论文最终跨session审计不属于本有界任务。
先只读原inventory/SCOPE-REVIEW、outlines/extensions/10-训练系统.md原实验10-8与既有references/framework-history/2026-09-08/weight-handoff的固定verl commit/source记录。当前固定文档只能作版本线索，必须用官方repo/文档核对你实际选定版本的依赖/入口与单GPU支持。技术浏览只用官方原始源；冻结commit/版本，不假定当前vLLM0.23兼容旧verl。主agent已确认共享环境：/home/ubuntu/vllm023-venv是vLLM.23/Torch2.11cu130/Transformers5.12.1；tools/sglang0513-venv是SG.5.13.post1/Torch2.11cu130/Transformers5.8.1。都不得修改，必要新私有venv/checkout隔离依赖。RTX SM120，CUDA13.0私有工具链 /home/ubuntu/ai-infra-book-experiments/tools/flashinfer-cuda130/nvidia/cu13；现SG环境CCCL include可只读复用，但不要混入另一版runtime头。先明确可运行配置再装大依赖，避免不可支持组合。
资源：与agentquality的≤24GiBGPU作业并行。你所有训练/rollout/Ray子进程合计GPU采样上限8GiB，CPU核12-15/库线程4，RSS总≤12GiB，系统MemAvailable≥16GiB；运行前GPU空闲≥10GiB。只处理你自己PID树，禁止停止原五GPU服务或任何其他计算任务（近期OpenROAD大量占RAM，不碰）。如果单GPU受支持路径不能在此上限运行，写具体已验证条件和资源需求，继续准备可复核入口，不伪造执行或自动借更多GPU；主agent后续调度资源。GPU运行每批≤20分钟，watchdog要可终止自己的完整Ray/vLLM/训练进程，保留监控/退出/清理；不能全局ray stop杀别人的Ray。私有Ray address/session/port/temp目录明确，避免使用已有集群。
模型优先现有缓存的小型Qwen0.5/0.6B；若没有，允许在远端独立目录下载≤2GiB公开原始小模型，固定revision/全文件SHA，不复制回Mac。远端新增依赖+模型≤25GiB，先检查磁盘；Mac新增交付≤150MiB（本地仅18GiB空闲，不下载大wheel或模型）。不反复下载已有8B等权重，不拿dummy/random模型替代实际trained小模型。先核对单GPUFSDP/verl路径是否可用；不要重演10-5用CPU自造scheduler替代官方CUDA的错误。
若支持，先写PROTOCOL：一套明确小任务（例如可验证短算术）固定prompts/seed/rollout temperature、response上限、reward计算、训练配置。真实rollout→保存实际tokens/logprobs/奖励→verl advantage/loss→FSDP真实backward/optimizer→受支持权重同步→再次rollout，至少两个实际更新步。小批次即可，记录退化情况（全同reward/零advantage/零梯度等），不能为让曲线好看筛选样本。不要声称两步提高模型质量；重点是实际阶段和版本/身份/状态流。若数据全零导致没更新，忠实记录并按预登记的非零对照检查，不临时修改奖励制造成功。保存前后权重摘要、至少代表性参数/梯度/optimizer状态或独立可复核张量，证明真实更新而非仅计步；rollout引擎接收到哪版权重需要具体机制/日志或张量hash，不凭API返回推定。
使用实际verl提供的版本支持路径；若需改其共享源码不允许，私有checkout实验adapter可做最小观测但不能自行替代核心流程；任何patch保存diff与原SHA、清楚限制。原论文恢复/抢占/异步/归一化扩展本批不强凑：先保证最小真实循环，后续另列未完成范围。仅保存实际阶段时间，初始化/JIT/共享GPU影响分列，不声称隔离性能或V4/H20/H100/B200训练。
输出standalone配置/运行入口、原始训练/rollout/奖励/权重版本/资源与退出日志、离线analyze及必要图QA、中文README和完整manifest。模型作业与传输exit后再分析；源码/依赖安装失败也保存，不用syntax通过证明训练运行。先写status持续更新，完成交主agent统一审核回填。若受支持组合暂无法实跑，给具体证据和下一步，而非宣称10-8完成。
