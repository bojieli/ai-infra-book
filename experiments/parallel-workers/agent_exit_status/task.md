你负责原实验11-9的真实Agent工具退出语义对照，独占新目录experiments/ch11/11-09/exit-status/、对应RTX新目录及experiments/parallel-workers/agent_exit_status/。其他全部只读，尤其calculations不触碰；不改共享模型/环境/原服务、正文inventory/PROGRESS。不停止任何现有服务，不启动新GPU引擎。
背景：quality-feedback已真实ABBA46请求全部2/6，原fixture CHECKER输出passed=false但subprocess returncode=0。请先只读源代码和原始记录确认。这是合理的独立工具协议控制，不预言质量改善。复用RTX现有OpenRealtime vLLM服务127.0.0.1:8000（原PID3613078/3614304）通过localhost HTTP。先GET /v1/models、只读核对启动配置/模型revision，确认服务当前仍为Qwen3-VL-30B-A3B-Instruct-FP8。若服务不同/不可用，先交准备与具体状态给root，不启动替代引擎、不调用付费外部API。不修改或reset共享APC，不影响其他任务；只能说本次同服务ABBA对照，不能当原独立部署时间复现。
在任何模型请求前冻结PROTOCOL：四次ABBA baseline/proper-exit/proper-exit/baseline，同原fixture INITIAL/SPEC/六例、原initial-messages/system，独立空workspace，greedy/seed固定/最多12轮/每轮1200生成token，max一请求在途，总模型任务<=20分钟。只改变工具run_tests在发现原六例passed=false时的实际wrapper退出码：子进程内运行原check、保留同stdout/stderr，并根据原passed显式sys.exit(1)，baseline真实仍exit0。不要伪造退出字段，不加修复代码提示、不把holdout反馈给模型、不调整原测试或阈值。模型调用通过标准HTTP实际chat completions，保持两臂请求参数相同，记录完整原始request/response/usage/finish/messages/代码/工具原始结果及进程码。保留全部四次正式运行和科学负结果；被成功重跑替代的启动/接口调试失败记录清理，只封存最后成功运行。
AST与限制复用fixture，生成代码仅受限Linux子进程（CPU2s/AS512MiB/wall5s/file1MiB），controller CPU8-11/线程1，RAM<=2GiB；不执行模型生成shell。实际fixture/checker需要独立携带，standalone，不依赖其他实验文件运行。输出finish与六例合格分开；实际模型若产生非JSON/length严格记录，不自动抽取/宽松成功。独立1013holdout复用原check_code.py（同SHA）在全部尝试结束后实际受限运行，额外alias标准分列，不提供给模型。不做根据结果追加样本或更换提示。
监控仅自己的CPU进程树；现有GPU服务只读记录PID/显存和背景，不能把该service总GPU资源计作本任务独占；HTTP客户端无服务器token IDs时明确null不可伪造。API不可提供内核/调度时间则只报告客户端请求区间；没有货币报价则费用null，不声称速度提升。检查所有最终原始数据，产README/analysis和manifest（不包含启动调试失败），主agent随后独立复核。先完整准备并真实执行可用服务路径，任务完成后退出。
