准备原2-5的完整V4真实检索实验（不是截断层替代）。独占新目录experiments/ch02/02-05/full-model-run/及parallel-workers/v4_full_prepare/。只读其他实验尤其native-layer-probe最终attempt05、runtime-preflight/config、full-model-readiness；calculations绝不触碰。当前verl仍运行，禁止启动GPU模型、禁止停止任何进程/服务，禁止重复下载已有V4权重；你的子任务是完整可审查的执行包与当前余量证据，主agent在RL结束后调度实际执行。
先核对cached DeepSeek-V4-Flash-0731 revision7872f01b1d1fe23eabc4c98b48bffcef5a386062、43层、48分片、155GiB左右磁盘载荷的实际metadata，不重算另一任务模型账，不哈希整个大模型/复制权重。复用成功原生SG0.5.13.post1+Torch2.11cu130私有环境、官方OffloaderV1、CUDA13.0/CCCL overlay/私有TVM与TileLang缓存，保留43原层和完整config，不带json模型截断override。只读核对原tokenizer/chat template的真实用法，准备no-thinking的自然文本检索，不凭模板记忆写错。原expert-preflight数值门槛M8有10/32768失败，必须在协议说明尚未获得严格数值放行，本次是实际模型输出/检索观察，不能宣称全数值正确。
准备冻结可自动评分的检索题：至少两种输入长度（实际token长度记录，目标约512/2048），每个长度两种隐藏信息位置，各题唯一答案；同内容固定变体，不拿合成合法token当文本质量。可加简单无检索校准但各质量口径分开；固定贪心和输出预算，全部结果包括截断/格式错误保留，不按结果择题重跑。输入题干、答案、完整prompt token IDs、输出IDs/text/finish、引擎真实ready/池配置/实际资源/版本全保存。完整模型初始化和请求时间分开，共享状态和CPUoffload开销单列。
资源方案使用已提出CPUoffload110GiB作为一个待验候选，不能称最低需求。主agent可在必要时按用户授权结束原vLLM/Fish服务释放GPU，但你不能操作进程，只读输出当前MemAvailable/关键进程RSS/anon和GPU PID显存，用于root决定。原OpenROAD永不碰。每个候选必须有自身新session watchdog（全进程树含reparent、唯一环境token+birth确认，GPU/RSS/MemAvail阈值、超时、终止仅自身），只运行新output目录。建议初始context4096/tokenpool4096/SWA ratio1.0，具体需按native成功配置与实际源码核查；不把静态比例当实测显存。不改共享依赖/原checkpoint，输出目录缓存私有。准备脚本不执行GPU，CPU依赖/ServerArgs/tokenizer解析可做，严格标为准备/解析不是实验完成。
最终交付README标明待root实际执行，PROTOCOL、run/launch/reproduce和只读源码/资源核对结果，不需要无意义mock测试，不启动任何GPU训练/推理。不保存被成功解析替代的调试失败记录，只保留最终可运行配置及必要复现资料。不要更改正文/inventory/PROGRESS，不联系其他session。完成准备即退出并告诉root运行命令、实际依赖、建议资源阈值及不确定项。
