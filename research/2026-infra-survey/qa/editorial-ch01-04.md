# 第 1–4 章归属与压缩建议

2026-09-09，纯编辑审读；没有搜索、下载或新增资料，没有修改大纲、计算结果、实验原件或 Git。已读四章主 Markdown 全文和 `outlines/structure.md`；其他章节仅核对小节标题，以给出明确落点。下列八项针对主纲的呈现与归属，**移动的是说明和引用，原计算/实验目录保留，已完成和未完成状态不改写**。暂不重新编号实验；跨章引用可沿用原编号及原路径，定稿时统一处理。

**交付前并发更新：** 根任务已落实 2↔3 修订：实验 2-8 改为给定长度的资源核算，2.6.5 改为“给定请求下的模型比较”，3.1.2 直接负责轨迹统计，不再反向依赖第二章；图 2-8 改为请求计算/状态增量，原六面板留作第三章素材。七段细节记录已移到第 2/3/4/9 章对应扩写，扩写题干/图注同步，根任务报告 `verify_outline` 通过。**下文第 2 项作为已落实的决定记录；第 3 项主体已落实，只核查实验 2-9 是否仍提前要求设备/链路选型，不重复移动原段落。** 第 1 项针对 3.1.1 的矩阵重讲，区别于已修改的 3.1.2。

原 283/285 行引导在最初审读时已经写明第三章主讲轨迹；当时未对齐的是题干、图和第三章反向引用。下文旧行号和原段落专为说明取舍依据，不声称这些问题在交付时仍全部存在。

最初审读时，主要问题是第二章末尾提前承担请求画像、缓存服务和多卡部署，第三章又回头计算单次执行；第一章的后续补充则提前进入访存并发和 TP/PP 细算。以下按优先级给出可直接落实的决定。

## 1. 第 3 章 3.1.1 直接使用第 2 章的需求表，不再走一遍矩阵

**原位置：** `03-推理与训练负载.md:17–31`，3.1.1“沿 Qwen3-8B 和 V4-Flash……把整段输入代入实际矩阵”，实验 3-1 又扫描长度、batch 与命中并计算下界。对应第 2 章 2.2.1–2.2.4（51–71 行）、2.3.1（87–89 行）和实验 2-4（145 行），单次执行工作已完整展开。

**决定：** 3.1.1 压成一次执行与请求时间线之间的接头。保留 prefill 产生首输出、后续 G−1 次 decode，以及每一阶段输入/输出状态；只引用第二章既有矩阵与载荷数。实验 3-1 改作“将已知需求表放入两个时段”：例如相同总输入/输出量、不同到达和阶段组成，算每个时段积累的计算需求与状态，不再次扫描全模型矩阵，不在第 4 章之前重建真实硬件 Roofline。

**必须保留的推导：** `P/S/G/B → 各阶段工作与状态 → 时间上的需求叠加`；保持“prefill 常偏计算、低 batch decode 常偏带宽”是由形状检验的起点，不能当绝对分类。图 3-1 改画阶段时序与资源需求，图 2-2 继续独占矩阵尺寸和数据流。无需新增实验文件。

## 2.〔已落实〕将 Chat/Agent 画像归入 3.1.2/3.1.3，取消反向依赖

**原位置：** 第 2 章 2.6.4（281–295 行）虽说“轨迹统计与实验在第 3 章主讲”，仍包含 C14-recorded-p95、实验 2-8 和完整记录分析；2.6.5 的 C14-chat-agent-figure、图 2-8（319–323 行）继续画画像。第 3 章 3.1.2 第 37 行则写“复用第 2.6 节的统计”，使最新结构规定的归属反了过来。

**决定：** 第二章末尾只留当前 283 行的交付段，列明下一章需要新输入、历史、返回长度、命中位置和复用间隔。把 C14-recorded-p95、实验 2-8、295 行画像结论并入 3.1.2 的实验 3-2 资料入口；把同一图中的 Agent 时序、工具等待和 reasoning 标记并入 3.1.3 的实验 3-4。图 2-8 不再独立占用第二章正文：Chat 分布由图 3-2 承担，Agent 时序由图 3-3 承担，引用现成 `calculations/figures/chat-agent/`，不重画或删除原件。第三章第 37 行改为“使用本节的固定轨迹”。

**必须保留的推导：** 相同均值仍可能有不同尾部、输入/输出相关性及到达突发；工具等待与 KV 保留是不同资源量。p95 小样本退化成最大值、返回 ID 不等于有用输出、声明 KV 驻留不等于实测页寿命，这三项限制放在对应图注/实验说明，正文不再重复三遍。

## 3.〔主体已落实〕拆开 2.6.5 的结构比较、硬件匹配和缓存取回

**原位置：** 第 2 章 2.6.5（297–317 行）在同一小节混入 R22-four-model-request、C14-sealed-chat-resource、C14-request-hardware-bridge、C14-v4-mtp-forward、C14-trace-cache-lifecycle 及实验 2-9，已经跨越第 2、3、4、8、9 章职责。

**决定与落点：**

- 第二章保留 R22 的**固定 token 几何下需求比较**，放在 2.6 收束处：同一 S/P/G/B，分别列矩阵、常驻状态、更新与访问，明确相同 token 数不证明同文本/质量。正文采用 Qwen3-8B 与 V4-Flash 两行，Pro/K3 和压缩边界变体保留原链接与第二章扩写资料。实验 2-9 只完成这张需求表及质量条件，不继续做实际硬件排名。
- C14-sealed-chat-resource（303 行）移至 3.1.2，作为上一项画像向模型账的唯一桥接；原请求数、EOS 和可证调用次数边界保留。
- C14-request-hardware-bridge（305 行）移至 4.5.1，接在 H06-projection 后作为延伸。保留“FP4 存储不等于 FP4 执行峰值”和共享 Tensor 资源不能简单同时取满的反例；完整四模型分类表留计算材料。
- C14-trace-cache-lifecycle（309 行）移至 9.5.1“缓存层次与访问方式”。第 3 章只交付复用间隔和需保留字节；第 8 章引用本地保留/重算，第 9 章才计算 `取回字节 / 可用间隔`、lookup/H2D 后剩余窗口及远端收益，不能在第二章先完成池化选型。
- C14-v4-mtp-forward（307 行）是单次模型结构，移回 2.6.3，正文只保留一次 MTP 新增矩阵/状态及“没有由此获得接受率”的边界。草稿验证和回滚仍在 8.3。

**必须保留的推导：** `G 个返回 token → G−1 次额外 decode → 最终 S+P+G−1 个已处理位置`，以及“模型的需求”与“硬件能供多少”分两步接合。现有 R22、C14 结果不合并改写成一套已经通过质量/运行验证的全链路。

## 4. 第二章保留模型尺寸的容量直觉，把八卡放置完整推导交给第六章

**原位置：** 2.6.1/2.6.2（227–273 行）。标题讨论模型规模和层数/宽度，但 C13-llama70-placement、qwen235-placement、dense-local-quantization、qwen235-eight-way、granularity-selection 已展开逐 rank、TP/PP/EP、复制、通信与完整组合扫描。实验 2-6 第 213 行也提前要求“本机、多设备和 CPU/GPU 放置”。

**决定：** 保留用户要求的 8B/30B/70B/235B“甜点”讨论，正文用一笔 `权重 + 状态 + 工作区 ≤ 可用容量` 说明这是条件式适配；8B BF16 权重约 16 GB，不能等价于 24 GB 卡已经能满足任意并发。层数/宽度保留一组等参或近等参形状反例，专家颗粒度保留相同激活计算、不同专家访问集合的反例。

将 245 行的 70B TP/PP/DP 对照放到 6.2.2/6.2.3；249、253 行的 Qwen235B TP8/EP8 及 KV 完整头复制放到 6.3.1/6.3.2；251 行分片后量化与 263 行含 fabric 的选择移至第六章对应扩写资料。精确低位格式和部署可用性由 8.4.1 接回。图 2-7 正文保留架构形状伴图与一张简化容量边界；目前基于 TP8、TP2/EP4 的完整八卡曲线在第六章使用，原图不删。

**必须保留的推导：** MoE 全部专家常驻、只激活少数专家；四个 KV 头不能无复制切成八个完整头的事实，可在第二章一句预告，第六章才算副本和逐卡占用。实验 2-7 核心范围缩成单设备必要容量和结构变化；八卡子题保留为第六章现有练习的来源。实验 2-6 的设备放置尾句相应改成“将 token–expert 分布交给第 6/9 章”。

## 5. 第一章完成 Roofline 方法，第四章补足“为什么供给达不到规格”

**原位置：** 第 1 章 1.3.2（83–95 行）的 70B/H100 方法已经完整；1.3.3 的 Mess/在途请求/C03-memory-concurrency（101–103 行）又提前引入真实 Qwen KV 与带宽校准。第 4 章 4.3.3 第 115 行再次“承接第 1 章的访存并发算例”；4.5.1（173–185 行）仍可扩成第二套 Roofline 教程和硬件登记汇报。

**决定：** 1.3.2 原有 `70 GB / 3.35 TB/s ≈ 20.9 ms`、算术强度和 `min(F,B×I)` 保留，明确教学模型和完整 token 时间边界。1.3.3 保留实验 1-4“哪项遗漏会改变选择”的实测反例，但仅一句指出独立访问不足也可能达不到带宽，不在这里再展开一个 Qwen 1.125 GiB 案例。

C03-memory-concurrency 的完整在途请求推算移到 4.3.3 第 115 行处，形成该节的新判断：`在途字节 ≥ 目标吞吐 × 工作点延迟`，所以升级接口并不保证得到相同比例服务收益。第 4 章 4.5.1 引用第一章公式，保留 H06-projection 的同一 Q 投影从 B1 到 B256 的瓶颈翻转；H01-H06 与 H05-field-audit 只保留一个会改变结果的精度反例。151 配置/471 峰值记录、CLI 拒绝规则和完整来源审查移到 `extensions/04…#detail-4.5` 与现有硬件资料链接。

**必须保留的推导：** 第一章教资源下界怎样组合，第四章逐一校准精度、累加、资源共享和持续供数；未公开峰值留空。第四章不重讲 Jeff Dean 历史表，不把寄存器/SM 数直接当在途事务数。双方原实测和计算记录均保留。

## 6. 第一章 UB 保留研究经历，把当代 TP8 对 TP4×PP2 算例移到第六章

**原位置：** 第 1 章 1.6.1（171–185 行，定位标题及 C05-modern-scope），八卡/跨服务器开篇之后，C05-modern-scope 已列 Qwen3-32B、完整头、KV 末位置、跨机 payload、两次启动、逐 rank 预算和带宽选择翻转。此时第二章尚未定义这些张量，第六章尚未推出 TP/PP。

**决定：** 1.6.1 仅保留同一任务的数据/状态跨设备边界图，以及“聚合容量会引入交接”的问题。实验 1-7 用给定容量和已经标好的交换边辨认限制，不要求读者先做 TP/PP 算法。C05-modern-scope 的完整计算和翻转点移到 6.2.3“流水线并行与组合方案”，由 6.5.5 的 UB 历史讨论引用这一**当代教学算例**。图 1-6 简化为物理组织示意，具体通信时序在第六章绘制。

**必须保留的推导：** 1.6.2 的时间线完整保留：统一互联研究先于 GPT-3，2020 年是需求被更广泛理解和投入增加的转折。第一章仍保留 TPU、SmartNIC、UB 三个历史案例及实验 1-8；当年 C05 历史输入缺口继续标未闭合，不拿现代 Qwen 数字补写历史动机。

## 7. 第三章训练只走一条完整工作链，压缩 V4 子图验算清单

**原位置：** 3.2.1 的 121–139 行，从 C18-dense-training-matrix、C21-training-nonmatrix 到八类 C19-V4 子图/优化器结果，占据了“预训练、中期训练与 SFT”小节主体；3.2.2 第 159 行 C18-rl-supply 又提前展开独立池/共享池、actor/learner 配比和跨批流水。

**决定：** 3.2.1 正文保留 Qwen3-8B 的代表层：前向、dX、dW 三次矩阵工作及保存状态，接一笔 `431.37 TFLOPs 对粗估 402.59 TFLOPs` 的同场景反例；说明长上下文注意力、输出头和优化器使 6ND 不够。V4 压缩器、online 状态 VJP、重复 ID scatter、mHC、Muon 的逐子图结果全部保留在第三章扩写资料与原计算链接；正文只用一句“新结构还会新增这些工作，当前覆盖并非全模型训练”。第十章按训练状态/执行需要引用相关结果，避免将八张部分账再次铺成八段正文。

3.2.2 保留 C18-rl-cycle 的生成→验证/评分→接受样本更新→权重交接，尤其拒收样本仍耗生成工作。C18-rl-supply 移到 10.5.3“采样、教师与学习的资源配比”，沿全书已指定的固定 verl 配方核对依赖；第三章不把 V4 教学阶段图写成该系统已经被复现。实验 3-7 主纲交付阶段工作和字节，资源池选择为第十章延伸引用。

**必须保留的推导：** `同一批有效样本 → 各阶段实际处理数量 → 计算/状态/交接需求`，以及训练/推理长度的 G−1 边界。3.2.3 Scaling Law 的真实拟合、留出检验和生命周期选择保持独立完整，不因压缩子图清单而退回“只讲 6ND”。

## 8. 两章章尾的多模态列表收进 3.1.4，保留工作单位差异

**原位置：** 第 2 章 327 行“多模态理解之外，补一组生成模型短例”，以及第 3 章 227–229 行“生成式多模态短例/C77-C80-generation-loops”，目前都位于所有编号小节之后、章末设计决定之前；两处重复列 Omni、Fish、Qwen-Image/FLUX、H3/Wan，形成新增目录的感觉。第 3 章 3.1.4（85–107 行）已经有 E/P/D、EC/KV 和实时响应的归属。

**决定：** 第二章末尾这段移入模型扩写资料，只在第二章资源表旁留一句“生成循环与非文本工作单位在 3.1.4 定义”。第三章章尾两段并入 3.1.4，正文压成一个音频循环和一个迭代生成反例：音频码本步不等于文本 token；50 去噪步含 true CFG 时可能对应 100 次 DiT 前向，必须按实际调用计数。其余模型、94/168 个 codec 算子、帧对齐和各配置的完整矩阵继续留 `generative-multimodal-models.md` 及扩写资料，不新增小节或图。

**必须保留的推导：** 请求依赖图的节点可以带重复次数，整请求工作为各实际阶段及循环的求和；不能将任意生成服务都压成纯文本 prefill/decode 或 tokens/s。现有 Qwen3-VL 的文件/EC/KV 三种字节和第 12 章图片精修、语音、截图循环的边界继续保留，第三章不展开网络放置优化。

## 审读快照

下表是 2026-09-09 10:41:41 UTC 核对的文件 SHA；小节标题和标记名同时给出，执行移动前应检查并发变化。行号来自本轮主纲文本，不假定此后文件未变。

| 文件 | SHA-256 | mtime（UTC） |
| --- | --- | --- |
| `outlines/structure.md` | `0e46d2df1a93ee2afa89c4a6bb265edd18ad705ad9d681d09ed64a87095b4d28` | 2026-09-09 10:35:52 |
| `outlines/01-初识 AI Infra.md` | `045b90fe8aa4c7e19f66611ab5cb5c158829e6980de81db7d41671fad8161407` | 2026-09-09 10:38:57 |
| `outlines/02-模型架构.md` | `c3853bb539ed5c3cd51e77fdef5033b97342977253617dfe16f83f1472fcd961` | 2026-09-09 10:38:57 |
| `outlines/03-推理与训练负载.md` | `cc26eca3a9a79e66b0d6d57bfc5ad1d9491214a0e99b922f0a636fa59eaab08b` | 2026-09-09 10:38:57 |
| `outlines/04-加速器架构.md` | `b218a75e8f65a6a9ec255aee9c597f88e551586000ac5e7984b88956c5696702` | 2026-09-09 10:38:57 |

上述决定落实后，前四章的交付依次为：**方法和历史动机 → 单次模型需求 → 时间上的任务需求 → 真实硬件供给**。保留代表配置、决定性的数字和限制；通过引用继续使用已有计算与实验，不以删正文为由删除证据。

交付前再次局部核对：第 2 章 SHA `da11e4be94bcdaba5b938b819c2ef562e588097aaff86cb24a755256900352d5`，第 3 章 SHA `9ee4dc0a77c664ee4a3a1a0b734b8c9c2e043a4d0dba8da86b47fc17759b513c`（2026-09-09T10:47:32.545337+00:00）。第二章新标题与实验 2-8 已对齐；实验 2-9 仍有“代入第 1 章的容量、Roofline 和链路模型”的原句，是第 3 项仅存的主纲范围建议。

## 根任务落实更新

本轮已完成第 2 项，并完成第 3 项的实验 2-9 范围修订：正文先比较 Qwen3-8B 与 V4-Flash 的模型需求，设备与链路推断留给后章，Pro/K3 为迁移练习。第 4 项主体已落实：第二章 2.6.2 的十三段计算记录完整移入第 2、5、6 章对应扩写小节，正文保留尺寸容量直觉与一组专家形状对照；实验 2-7 与图 2-7 的题意已同步。实验 2-6 的原题范围尚需后续检查。本轮未完成第 1、5、6、7、8 项，不据此称前四章全部验收。

两轮迁移共二十段记录，逐段保留原文本与来源链接，仅按目标目录调整相对链接；计算项目和原始实验文件未改。网页已同步，verify_outline 检查通过：13 章、70 节、240 小节、117 实验、105 配图、39 核心练习。此检查证明结构与引用，不证明全部数值、实验或全书论证已验收。主纲已有并发编辑，当前章节改动仍在工作区合并收敛，不把其余未提交工作一并提交。
