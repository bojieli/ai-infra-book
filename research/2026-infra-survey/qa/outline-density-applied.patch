--- a/outlines/extensions/08-单实例推理.md
+++ b/outlines/extensions/08-单实例推理.md
@@ -44,7 +44,7 @@
 
 用 Qwen3-8B 的 151,936 词表和 64 请求算一批约 1.159 MiB 的位掩码，再给 CPU 每行工作与 GPU 前向时间，找采样前的汇合点。字节不多不代表 CPU 开销可忽略；当前填充路径也不总按线程数并行。实验 8-2 用实际 trace 校准，[计算与版本条件](../../case-studies/structured-generation.md)供扩写查用。
 
-连续批处理还会改变同一请求遇到的矩阵形状和归约切分。接回第 5 章的数值对照，用 SGLang 的确定性路径和 vLLM batch invariance 说明为何温度设零仍未必复现；固定归约、请求种子与兼容后端各解决什么问题，又牺牲哪些执行选择。比较[历史与当前版本](../../case-studies/rl-state-and-reproducibility.md)，完整实验放在 11-9，与跨引擎 logprob 和 Routing Replay 连起来。
+连续批处理还会改变同一请求遇到的矩阵形状和归约切分。接回第 5 章的数值对照，用 SGLang 的确定性路径和 vLLM batch invariance 说明为何温度设零仍未必复现；固定归约、请求种子与兼容后端各解决什么问题，又牺牲哪些执行选择。比较[历史与当前版本](../../case-studies/rl-state-and-reproducibility.md)，本节保留同一请求随 batch／归约路径变化的局部数值对照；跨引擎 logprob 与 MoE 路由重放接第 10.5.4 节及实验 10-9，按各自模型与执行条件验证。
 
 <a id="detail-8.1.4"></a>
 
--- a/outlines/09-分布式推理.md
+++ b/outlines/09-分布式推理.md
@@ -114,6 +114,8 @@
 
 **已复算（C48-pd-af-handoff）：** [PD／AF交接账](../calculations/results/pd-af-handoff-book.md)复算32层教学模型：8K BF16 KV为1GiB，AF单步双向激活0.5MiB、64次消息。有效25GB/s、每hop启动5us，串行通信PD42.954673ms、AF0.340972ms；相同1GiB分成64次的对照只因启动多315us。启动增到1ms时AF64.020972ms大于PD43.949673ms，不能仅按载荷判断开销。[双端host staging](../calculations/results/pd-af-handoff-staged.md)逐消息串行D2H→网络→H2D，各接口各算一次，双端GPU与host完整消息槽分列。[官方Qwen8](../calculations/results/pd-af-handoff-qwen8.md)按36层得到1.125GiB KV、单步576KiB激活／72次；[Qwen235](../calculations/results/pd-af-handoff-qwen235.md)另计B=4、128步。运行 `python3 calculations/calc.py pd-af-handoff --format md`，用 `--inputs` JSON修改长度、batch、步数、带宽与启动。PD一次请求快照与AF指定decode调用是不同交接范围，不能据此断言部署优劣；未含计算、排队、格式转换、MoE跨专家dispatch和流水重叠。
 
+另以 vLLM AFD Plugin（2026 年 7 月实验性发布）展示跨服务 AF 的实际接口，核对上述流水分析的实现条件。其已披露模型和 GPU／NPU connector 各有阶段、图执行与微批限制；公开强制均衡及缩层测试不能充当自然路由下完整 V4／K3 的质量或线上性能结论。
+
 > **实验 9-4 ★〔核心〕：PD 与 AF 的交接频次**
 >
 > 为什么同样带宽下 PD 和 AF 表现不同？ 先用相同字节数控制比较启动开销，再回到两项具体教学算例：PD 一次交接 1 GiB KV；AF 在 32 层中双向交接 4096 维、BF16 的单 token 激活，合计 0.5 MiB、64 次方向性交接。手画依赖时序并扫描每次启动延迟，加入实际分层与路由后重算。
@@ -125,8 +127,6 @@
 > 由实验 9-4 生成并排时间线。
 
 ### 9.3.4 大主存与少量 GPU 的服务条件
-
-另以 vLLM AFD Plugin（2026 年 7 月实验性发布）展示跨服务 AF 的实际接口，接回上一小节的流水分析。其已披露模型和 GPU／NPU connector 各有阶段、图执行与微批限制；公开强制均衡及缩层测试不能充当自然路由下完整 V4／K3 的质量或线上性能结论。
 
 沿固定 KTransformers 文档还原 V4-Flash 的单 RTX 5090＋至少 200 GB 主存，以及 Kimi K2 Q4_K_M 的约 600 GB 主存＋14 GB 显存案例。先从完整权重放在哪里解释容量，再计算 CPU 实际专家工作、GPU attention、NUMA 与交接。Kimi K2 的证据不替代 K3；单卡与八卡 MTP 成绩分开引用。
 
--- a/outlines/extensions/09-分布式推理.md
+++ b/outlines/extensions/09-分布式推理.md
@@ -134,6 +134,8 @@
 
 用简化的 32 层、单 token、宽度 4,096、每元素 2 byte 算例：每层完整激活送出一次、合并结果返回一次，每步总载荷 0.5 MiB，却有 64 次有方向的交接。先算 `64 × 启动与同步时间 + 载荷 / 带宽`，再代入 Qwen3／V4-Flash 的真实层数、专家路径与切分位置修正。将同机 PCIe 换成跨机网络，判断新增启动是否已经超过期限，再考虑压缩、流水和聚合；不把少字节直接理解成低开销。
 
+另以 vLLM AFD Plugin（2026 年 7 月实验性发布）展示跨服务 AF 的实际接口，核对上述流水分析的实现条件。其已披露模型和 GPU／NPU connector 各有阶段、图执行与微批限制；公开强制均衡及缩层测试不能充当自然路由下完整 V4／K3 的质量或线上性能结论。
+
 > **实验 9-4 ★：PD 与 AF 的交接频次**
 >
 > 为什么同样带宽下 PD 和 AF 表现不同？ 先用相同字节数控制比较启动开销，再回到两项具体教学算例：PD 一次交接 1 GiB KV；AF 在 32 层中双向交接 4096 维、BF16 的单 token 激活，合计 0.5 MiB、64 次方向性交接。手画依赖时序并扫描每次启动延迟，加入实际分层与路由后重算。
@@ -147,8 +149,6 @@
 <a id="detail-9.3.4"></a>
 
 ### 9.3.4 大主存与少量 GPU 的服务条件
-
-另以 vLLM AFD Plugin（2026 年 7 月实验性发布）展示跨服务 AF 的实际接口，接回上一小节的流水分析。其已披露模型和 GPU／NPU connector 各有阶段、图执行与微批限制；公开强制均衡及缩层测试不能充当自然路由下完整 V4／K3 的质量或线上性能结论。
 
 沿固定 KTransformers 文档还原 V4-Flash 的单 RTX 5090＋至少 200 GB 主存，以及 Kimi K2 Q4_K_M 的约 600 GB 主存＋14 GB 显存案例。先从完整权重放在哪里解释容量，再计算 CPU 实际专家工作、GPU attention、NUMA 与交接。Kimi K2 的证据不替代 K3；单卡与八卡 MTP 成绩分开引用。
 
--- a/outlines/08-单实例推理.md
+++ b/outlines/08-单实例推理.md
@@ -218,9 +218,9 @@
 
 ### 8.4.1 权重位宽与真实驻留容量
 
-沿 Ollama 的容量规划变化校准模型：2024 年所读实现先预测是否装得下，2025 年新引擎按 fit／allocate／commit 的反馈调整层放置；2026 年固定主线的普通模型又转交 llama-server，启动前预测与 MLX 路径分别处理。用[固定版本](../references/framework-history/2026-09-08/overlap-placement/README.md)说明为什么要记录实际 runner、上下文、并发、图缓冲和分配结果，不能将某次公告推广成所有版本都不再 OOM。
-
-结合 v0.30 的 GGUF 路径看实际放置：当前 runner 可自动选择 GPU 层，多模态 projector 还可能单独留在 CPU。内存报告中的 100% GPU 不等于利用率，也不说明图像阶段的耗时；将 mmap 映射、实际驻留与重打包副本分开计量。[版本与读取范围](../references/framework-history/2026-09-08/offload-execution/README.md)供实验复核。
+用 Qwen3-235B-A22B 的固定 GGUF 分片与 M2 Max 96 GB，先逐项算权重、量化元数据、KV、工作区与系统预留，再改变上下文和并发。文件名的 2 bit 不等于全模型平均位宽，统一内存也不能把 CPU 与 GPU 容量相加。
+
+沿第 5.5.1 确认的实际 runner，对照容量预测、实际分配与 CPU／GPU 放置，分开计量 mmap 映射、实际驻留和重打包副本。内存报告中的 100% GPU 不等于利用率；版本演进与支持条件见[本节扩写](extensions/08-单实例推理.md#detail-8.4.1)，供实验复核。
 
 ### 8.4.2 完整模型与任务质量
 
