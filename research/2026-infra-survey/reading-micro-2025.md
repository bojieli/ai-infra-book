# MICRO 2025 阅读与取舍

123 篇主日程论文已匹配出版 DOI；13 篇完整原始摘要已筛，5 篇正文候选、4 篇备查、4 篇排除，110 篇摘要待补。13 份代表公开稿共 208 页，另存一份 15 页早期版本；下载页数不等于已读正文。后续已选读 StreamTensor 物理页 3–14、LLM.265 页 4–13，共 22 页；两项方法进入既有算例，没有移用论文性能比值。首轮摘要取舍保留在筛选表，另三篇候选正文待核。

[归档入口](../../references/proceedings/MICRO/2025/README.md)保存原件、版本和获取缺口；[筛选表](screening-micro-2025.tsv)与[摘要证据](../../references/proceedings/MICRO/2025/public-abstracts.json)保留逐项理由。落点使用当前草案 22，候选先与既有案例比较，不增加论文介绍小节。

| 日程序号与论文 | 取舍 | 比较理由与待核条件 |
| --- | --- | --- |
| 1 Stratum | 正文候选 | 由专家访问倾斜决定分层驻留，但 Mono3D 内部延迟、容量和热约束需要正文核对；与既有 AF 和近存计算案例比较，不能把新存储技术当作现成 GPU 功能。 |
| 2 Kelle | 备查 | eDRAM 的容量密度同时带来刷新能耗，KV 淘汰、重算和刷新控制可作容量之外的代价背景；保留质量与专用芯片条件，不扩展端边云章节。 |
| 14 StreamTensor | 采用既有算例 | 选读 3–14 页，补 5.2.3／实验 5-2 的累计生产、消费与独立重排缓冲：48＋32 KiB 超过 64 KiB 预算。时间格是教学输入；FPGA、格式、动态回退与测量限制另记。 |
| 25 Modern GPU Cores | 正文候选 | 微基准与编译器依赖管理可用来检验模型假设；已取得正式题名的机构稿，待读具体硬件代际、测量与预测范围，不采用早期稿误差或把逆向推断当作厂商规格。 |
| 30 LLM.265 | 采用既有算例 | 选读 4–13 页，补 8.4.3／实验 8-7 的预编码权重、在线激活与解压交点；既有视频引擎、ASAP7 设计和集群分析模型分开。未采用论文吞吐作 Qwen3 实测，也不移用质量结果。 |
| 36 TRRIP | 备查 | CPU 指令缓存的编译器提示可作软硬协同背景；移动程序的前端瓶颈不同于当前 LLM 算子，现有案例优先，不据通用移动应用结果增加推理优化。 |
| 38 Distributed-HISQ | 排除 | 分布式量子控制的同步精度和保真度有专门语义，不直接对应 GPU 集合通信或 RL 任务同步；不展开量子控制系统。 |
| 39 Accurate Leakage Speculation | 排除 | 量子纠错中的泄漏状态推断与校正电路超出 AI Infra 主线；不将 leakage speculation 混为模型推测解码。 |
| 76 RISC-V instruction subsets | 排除 | 面向柔性电子的指令子集芯片强调工艺、面积与可弯曲性，不是本书模型服务的端侧算力配置；只保留筛选记录。 |
| 77 ReGate | 正文候选 | 不同 NPU 部件的空闲模式决定软硬件各负责哪种断电控制；待读唤醒代价、空闲区间与模拟功耗模型，可能校准第 4 章能耗推算，不宣称所有芯片都有摘要中的静态功耗占比。 |
| 94 rhoHammer | 排除 | DRAM 扰动漏洞及攻击技术超出模型服务性能主线，不转化成内存预取优化实验。 |
| 102 Label Propagation task locality | 备查 | 任务就绪不等于应该立刻放到任意空闲核；局部性提示与利用率取舍已有 AI 放置案例，FPGA 加速的细粒度任务调度暂留背景，不当作集群调度器已有功能。 |
| 118 Delegato | 备查 | 原子操作的位置改变 cache line 迁移与串行点；保留作为 chiplet 通信背景，gem5 CPU 工作负载不直接证明 GPU 或模型路由性能，不扩展原子指令专题。 |

[流式交接](../../case-studies/stream-order-and-buffer.md)和[压缩传输](../../case-studies/tensor-codec-and-transfer.md)保留可复算的选择；完整配置、论文条件与设备能力表留在配套材料。没有新增小节、实验或图号。GPU 核心分析、Stratum 与 ReGate 的正文仍待核，只在提供不同判断时采用；历史模型不替代 Qwen3、V4、K3 的贯穿配置。

本轮同步修正了[框架覆盖表](framework-coverage.md)的当前落点：图执行接 5.4→8.1.5→9.6.2，缓存路由接 9.5.3／实验 9-9，RL 闭环接 10.5→11.2／11.3。多模态仍由第 3 章先定义 E→P→D，第 12 章再改变网络条件和位置；没有恢复旧第 8 章作为推理章节前置的安排。

七张实际查看的页面核对分栏、作者、版本与摘要物理页码。Label Propagation 的摘要在第 2 页；Stratum 摘要跨栏但不包含夹入的版权脚注；StreamTensor 引用格式已从摘要范围剔除。两份机构摘要与对应 PDF 是同一论文的重复证据，早期 GPU 稿不重复计篇。论文脚本、模拟器与硬件实验均未执行。

本次正文比较另查看六张页面。StreamTensor 的 FPGA／GPU 量化格式不一致，编译时间不等于完整 RTL 工具链耗时；LLM.265 的能耗列存在尚未解释的计量口径，图 14 的误差名称也有图文差异，均未采用相关数值。官方设备表核对 A100／H100 缺少 NVENC，避免由架构代际误推编码能力。新例子的字节、收益交点和队列轨迹由独立计算核验；未运行 GPU、模型或论文代码。
