--- draft11

+++ draft14

@@ -1,4 +1,4 @@

 《深入理解 AI Infra》骨架
-Book skeleton · draft 11
+Book skeleton · draft 14
 《深入理解 AI Infra》
 量化分析与系统设计
@@ -16,4 +16,8 @@

 恰好位于最易朽的那一层
 。写作时会本能地想让它们当主角，因为那是别人写不出来的。但正因如此，它们只能当证据。这个取舍反直觉，且很难在写作过程中持续坚持，所以立为原则。
+推论：中心不仅要耐久，还必须少。
+H&P 一千多页、六个版本，真正留存下来的大约只有五条（用测量而非主张论证、Amdahl 定律、局部性、关注常见情形、性能方程）。
+若能点出三到五条 2040 年仍然成立的东西，那就是本书的内核，其余全是可修订内容。
+并且要按「这本书会经历一次重写」来设计结构——H&P 的量化纲领在 Dennard scaling 终结时就被大幅重写过，这种体制切换约十五到二十年一次。
 主线：一个能算出来的汇率
 Agent 书用的是
@@ -76,5 +80,5 @@

 全书统一，每章照用。这是平行结构从版面下沉到
 推理层
-——比固定九格更深一层的统一，也是「学会推导」这个目标唯一可操作的抓手。
+——比固定八格更深一层的统一，也是「学会推导」这个目标唯一可操作的抓手。
 写下负载的需求。
 多少 FLOPs、多少字节、多少次同步、延迟目标是多少。工具是第 2–4 章的负载特征刻画。
@@ -245,5 +249,7 @@

 前向、反向与优化器三段；显存四分解；
 单卡容量为何不足
-；可切分维度的枚举（只列举，不评价）；RL 的 rollout–train 循环形态。
+；可切分维度的枚举（数据、张量、流水、
+序列／上下文
+、专家——只列举，不评价）；RL 的 rollout–train 循环形态。
 交付 6ND 推出的「卡·天」量级，同样不依赖硬件参数
 4090 那篇的训练算力推导
@@ -279,21 +285,48 @@

 8
 机间：网络与集合通信
-RDMA、拥塞控制、拓扑；all-to-all 为何是代价最高的原语；东西向／同步／大块／无损与互联网数据中心南北向／突发／容错的对照；光模块可靠性与故障率随规模的增长
+RDMA、拥塞控制、拓扑；all-to-all 为何是代价最高的原语；
+集合通信库这一层
+（算法选择、消息切分、通信 kernel 占用 SM 而与计算争抢、GPU 发起通信）——
+一次集合通信的代价，有多少来自网络、多少来自库
+；东西向／同步／大块／无损与互联网数据中心南北向／突发／容错的对照；光模块可靠性与故障率随规模的增长
 《网络的新黄金时代（一）》+ MP-RDMA、StaR、FastWake、1Pipe
 第三部分 · 系统设计与优化（五步第 3–5 步）
 9
-推理系统优化（一）：单请求代价
-KV 管理、
+单实例推理：单请求代价与并发调度
+KV 管理与 PagedAttention；
 量化与稀疏
-、投机解码、长上下文
-—
-10
-推理系统优化（二）：并发服务与调度
-continuous batching、chunked prefill、PagedAttention、prefix cache 复用（含跨租户的安全边界）、P/D 分离、EP 服务；排队论、SLO 与尾延迟；
+——权重、激活、KV 三处换掉的
+不是同一样东西
+（容量与带宽／算力与融合边界／并发上限）；权重与 KV 的分层卸载；投机解码——
+判据有两个：接受率，以及被吃掉的 batch 容量
+；长上下文与上下文并行。并发一侧：continuous batching、chunked prefill、prefix cache 复用（含跨租户的安全边界）；
+调度即重叠
+——调度器不只决定谁进 batch，还决定微批如何重叠（TBO）以把通信藏进计算；排队论、SLO 与尾延迟；
 Agent 负载改变 prefill／decode 配比，缓存命中率成为主导变量
 Pine：数十万次真实通话
+10
+分布式推理：P/D 分离与大规模 EP
+P/D 分离（含
+两侧 fabric 需求本就不同
+，回扣「延迟反转」一节）；大规模 EP 的形状——
+attention 走 DP、FFN 走 EP
+，以及为何只能如此；
+三种不均衡，各自的定价
+：专家负载倾斜（EPLB——冗余专家与动态重排，
+用显存与权重搬运换等待
+）、实例间与 DP 内的请求路由（
+用信息换等待
+——缓存亲和与队列深度的取舍）、prefill／decode 配比漂移；跨实例 KV 池化（即「按存储层切」，第四种切法）；
+推理侧容错
+——大规模 EP 把推理也变成同步巨型进程，单卡故障的爆炸半径是整个域，
+而推理有 SLO、无 checkpoint 可回滚
+—（实测见待核清单第一、二条）
 11
 训练系统：并行策略与 RL 基础设施
-DP／TP／PP／EP 的通信量推导与选择（第 4 章枚举的维度在此被定价与取舍）、MFU、混合精度、checkpoint 与存储、容错——
+DP／TP／PP／EP／CP 的通信量推导与选择（第 4 章枚举的维度在此被定价与取舍）、
+重叠是第六个维度
+（微批与流水线重叠、双向流水，
+把气泡换成显存
+）、专家负载均衡在训练侧的形态（辅助损失与无辅助损失两条路）、MFU、混合精度、checkpoint 与存储、容错——
 万卡同步训练是单一巨型进程，单点故障重新出现
 ；RL 训推一体的权重同步与资源配比
@@ -323,7 +356,9 @@

 （云「一分为多」，AI「多合为一」）→ 第 7 章
 公平调度让位于批组织效率
-→ 第 10 章
+→ 第 9 章
 失去分布式系统的容错红利
-：万卡同步训练把「多台廉价机器」变回「单台昂贵机器」，单点故障重现 → 第 8、11 章与终章第二个未解问题
+：万卡同步训练把「多台廉价机器」变回「单台昂贵机器」，单点故障重现；
+大规模 EP 让推理侧也重演这一幕
+，且推理有 SLO、没有 checkpoint 可回滚 → 第 8、10、11 章与终章第二个未解问题
 利用率语义改变
 ：云出售分数利用率与超卖，AI 要求专属集群上 MFU 趋近 1 → 第 11 章
@@ -378,5 +413,7 @@

 第 5 环 · 一阶估算与现实差 20–50×，而这才是重点
 估算 TPOT 下界 ≈ 毫秒量级  |  真实服务 TPOT ≈ 20–50 ms
-差距藏在 decode 期个位数百分比的 MFU、每专家有效 batch 太小、all-to-all 的实测延迟、调度与抢占。
+差距藏在 decode 期个位数百分比的 MFU、每专家有效 batch 太小
+且分布不均
+、all-to-all 的实测延迟、调度、抢占与请求路由。
 这应该是全书最大的一次「估算 → 实测 → 解释差距」。
 它还导出一个必须讲清的区分：
@@ -452,9 +489,13 @@

 每卡每次要发出几百条极小的消息
 。
-于是支配完成时间的是两件事：
+于是支配完成时间的是三件事：
 每消息固定开销 × 几百
-（Jetty、零序列化在压的正是这个），和
+（Jetty、零序列化在压的正是这个）；
 尾延迟 × 几百次采样
-——集合通信在最后一条消息到达时才结束，向几百个对端各取一个样本，拿的是分布的尾巴。
+——集合通信在最后一条消息到达时才结束，向几百个对端各取一个样本，拿的是分布的尾巴；以及
+专家负载倾斜
+——路由从不均匀，热专家所在的卡最后才算完，而集合通信等的就是最后那一个。
+第三项与前两项性质不同：它不是网络的问题，是负载的问题。
+因此它也不在第 7、8 章解决，而在第 10 章解决（EPLB：冗余专家与动态重排，用显存与权重搬运换等待）。这个区分很重要——若不把「网络的尾」与「负载的尾」分开测，那张核心图表会把两笔账算到一起，而它们的治法完全不同。
 多级交换的伤害在这里：不是平均延迟多 2 μs，是多几个排队点、尾巴变肥。
 这也解释了上一节那个 20–50× 的差距——差距不在跳数，在尾部与每消息开销。
@@ -479,4 +520,9 @@

 「大模型 + 小 batch + 低 TPOT」
 。batch 大了，decode 就从延迟敏感滑向带宽敏感，多级交换不成问题；batch 小了才暴露尾延迟。
+由此得到一个把第 9、10 章串起来的看法：
+投机解码与微批重叠（TBO）都在把 decode 往带宽敏感那一侧推
+——前者用推测出来的 token 把 batch 填厚，后者用另一个微批把空隙填上。
+它们不只是吞吐优化，也是把系统主动移出约束角的手段。
+而这也解释了为什么投机解码的真实代价是 batch 容量：它花的正是约束角里最稀缺的那样东西。
 而 Agent 与推理模型正把我们往约束角里推
 ——串行多轮调用要求低 TPOT，低 TPOT 又通常意味着不能靠堆 batch 来摊。这正是 Agent 书后记提出的「实时交互」问题在基础设施侧的对应物。
@@ -523,4 +569,11 @@

 由此得到一条可检验未来架构的定律：
 一个序列架构要参赛，必须先满足「训练时可并行」；在此约束之内，剩下的自由度才由建模质量决定。
+而硬件彩票有复活赛。
+被主赛道淘汰的架构可以换一个身份回来——扩散语言模型正以
+草稿器
+的形态进入投机解码（DFlash），因为草稿不必满足质量约束，只需满足
+接受率
+，而它恰好赢在「一次前向出一整块」这一条上。同一个架构，在「作主模型」与「作草稿」两组约束下参加的是两场不同的比赛。
+这既是第二问的最新证据，也说明代价模型筛掉的东西不一定死透。
 架构特征的出生证明
 GQA / MQA
@@ -698,5 +751,5 @@

 缺的不是内容，是主线。四篇已发表的长文加上 Agent 书的两章，已覆盖骨架的大半——但它们分属四种文体（vision talk、BOTE 分析、回忆录＋白皮书、学术论文），
 直接拼装会重演 Agent 书「十章十个写法」的毛病
-，必须全部重写进五步推导与固定九格。
+，必须全部重写进五步推导与固定八格。
 已发表
 供给哪章
@@ -729,5 +782,5 @@

 接成教学实验，而不只是论文附件
 Agent 书 ch2 / ch8
-2 · 3 · 10 · 11
+2 · 3 · 9 · 11
 KV Cache 的上层视角、RL rollout infra
 从「怎么用」翻到「怎么承载」
@@ -786,10 +839,13 @@

 同样并行度置于机间，每 token 多付多少？此步为何会使 TPOT 崩溃？
 9
-单请求优化：量化、长上下文、投机解码
-降低每参数字节数可回收多少？投机解码在何种接受率之上方才划算？
+单实例：量化、投机解码、并发调度与 SLO
+降低每参数字节数可回收多少？投机解码在何种接受率之上划算——
+而它真正的代价是被吃掉的 batch 容量
+？
+P99 约束下尚余多少可用吞吐——估算在此首次失效，回扣第 5 章给出的边界。
 10
-并发服务：P/D 分离、prefix cache、SLO 约束
+拆成多实例：P/D 分离、大规模 EP、三种不均衡
 $/1M token 落于何区间？Agent 负载的缓存命中率使其移动多少？
-P99 约束下尚余多少可用吞吐——估算在此首次失效，回扣第 5 章给出的边界。
+专家倾斜与请求不均各吃掉多少——这是「等待」一项在服务层的全部内容。
 11
 训练优化：并行策略选择、RL
@@ -809,5 +865,5 @@

 分层与缓存
 CPU cache、页缓存
-KV cache 分层卸载、prefix cache
+KV cache 分层卸载、权重 offload、prefix cache
 Memory-Level Cache（Redis +21~45%）
 重算换搬运
@@ -821,19 +877,29 @@

 重叠与流水
 双缓冲、乱序执行
-计算-通信重叠、continuous batching
+计算-通信重叠、
+微批重叠（TBO／双向流水）
+、continuous batching
 KV-Direct 用乱序执行隐藏 PCIe 延迟
 批量与分摊
 大页、批量 I/O
-动态 batching、chunked prefill
+动态 batching、chunked prefill、
+专家与请求的负载均衡
+（
+摊的是不均衡，不是固定开销
+）
 KV-Direct 的客户端批处理
 解耦与池化
 存算分离
-P/D 分离、内存池化、统一编址
+P/D 分离、
+跨实例 KV 池化
+、内存池化、统一编址
 UB 的事务／传输层解耦
 横切关注点两个，不占章：成本（$／token，所有设计的目标函数）与故障（万卡集群的日常，所有设计的约束）。沿用 Agent 书处理「安全」的方式。
-每章内部：固定九格
+每章内部：固定八格
 Agent 书十章十个写法。本书以同一骨架写十二章——
 平行结构本身即教科书感的来源
-。九格是五步推导在版面上的展开；后三格分别承载多架构、协同设计与习题，使这三条横向线索不必各自占章。
+。八格是五步推导在版面上的展开；后三格分别承载多架构与协同设计、以及历史视角，使这几条横向线索不必各自占章。
+八格之后直接是习题
+（回溯推导与反事实分析），习题不占格。并非每章每一格都要单立标题——有些格一段话即可。
 一个问题，不是一个主题。
 「为什么 8 张卡的吞吐不是 1 张卡的 8 倍？」比「张量并行」好。
@@ -844,15 +910,17 @@

 因此生效的模式。
 指回第 1 章命名的六个，说明为什么是这几个（五步第 4 步）。
-真实系统怎么做的。
-vLLM／SGLang／Megatron／CloudMatrix 的具体选择。
+真实系统与各架构的选择。
+vLLM／SGLang／Megatron／CloudMatrix 的具体做法，以及本层约束下 GPU / 昇腾 / TPU 各自选了什么、分别在赌什么（多架构以此分布到全书，不另设章）。
 手算例子更新 + 新瓶颈。
 一阶估算 → 公开实测 → 差距解释（五步第 5 步）。
 误差本身是知识。
-各架构如何回答本层的问题。
-本层约束下 GPU / 昇腾 / TPU 各自的选择，以及它们分别在赌什么（多架构以此分布到全书，不另设章）。
 本层约束在模型架构上的印记。
 这一层催生了哪些模型结构——KV 字节那一层催生 GQA/MLA，卡间通信催生 MoE 的专家粒度，串行深度正在催生更宽更浅。
-哪些结论会过时 · 回溯推导 · 反事实分析。
-区分依赖当前相对代价的结论与不依赖的，再给习题。
+历史视角。
+本层今天的形态是怎么来的：关键的分叉、被淘汰的方案、以及为什么。
+这一格让书成为记录而非手册
+——H&P 的技术内容早已过时，它的历史部分仍在被读。
+耐久性的判断也落在这一格
+——不另设「哪些结论会过时」一节：反复出现的是原理，只出现一次的是当期产物，历史本身就是最好的证据。
 习题主体是推导题，不是代码
 读者没有 8 卡机，但
@@ -881,7 +949,19 @@

 切、EP 沿
 参数
-切——同一把刀「按资源画像切分」。读者应能自己找出第四种切法
+切——同一把刀「按资源画像切分」。读者应能自己找出第四种切法（
+答案：按存储层切——跨实例 KV 池化
+）
 一行带过
-：Unsloth 等属第 6 章材料的应用，未引入新兑换
+：Unsloth 等属第 6 章材料的应用，未引入新兑换。
+投机解码的算法竞赛同理
+——DFlash（块扩散草稿器，一次前向出一整块，把草稿自身的
+串行深度
+压到 1）与 DSpark（半自回归 + 置信度调度，
+不再无差别验证整块
+）各占一行；正文只讲那两条不会过时的判据：
+接受率
+与
+被吃掉的 batch 容量
+。这两个技术恰好各打中一条，是这条政策最好的示范
 提及的格式固定为四栏一行：
 技术 / 换掉了什么 / 代价 / 何时划算
@@ -894,6 +974,6 @@

 正文只写符号。
 硬件参数集中到附录表 + 仓库 YAML，出了新卡书不用改。
-每章末「会过时」小节。
-反复训练读者分辨什么会变、什么不会。
+历史视角一格承担耐久性判断。
+不另设「会过时」小节——反复出现的是原理，只出现一次的是当期产物。
 配套估算工具。
 输入模型规模、硬件、并行策略，输出各层估算与瓶颈定位。它会像 4090 那篇一样自己传播，而且抄不走。
@@ -910,4 +990,13 @@

 整条推算唯一的空洞
 。只有分布能证伪「延迟：一个待重算的反转」那一节；平均值证伪不了。如果能测，它应该是终章的核心图表
+专家负载倾斜的实测分布
+——热专家负载比（max/mean）随 batch、EP 度与路由策略怎么变；EPLB 前后 all-to-all 完成时间的变化
+10 · 12
+「尾延迟」一节的第三个生产者。
+必须与第一条一起测
+：不分开「网络的尾」与「负载的尾」，终章那张核心图表就会把两笔账算到一起，而两者的治法完全不同
+大规模 EP 服务下单卡故障的爆炸半径与实测恢复时间
+10
+「推理侧也失去容错红利」这个论断的证据。决定它在第 10 章是一格还是一节
 CloudMatrix384 的 fabric 是几级（1 级还是 2 级 Clos）
 7 · 12
@@ -956,7 +1045,24 @@

 十章十个写法
 带日期的可证伪预测 + 仓库预测记录与复核
-草案 11 · 供讨论。相对草案 10：立「写作第一原则」——最耐久的东西放在中心，最独特的东西当作证据；并记下它对应的具体诱惑（超节点与制程材料最独特，也最易朽）。
+草案 14 · 供讨论。相对草案 13：
+第三部分的推理两章沿「单实例／跨实例」重划
+——第 9 章收拢单请求代价与单实例并发调度，第 10 章独立为「分布式推理：P/D 分离与大规模 EP」，容纳原先缺席的四项：
+专家负载均衡、请求路由与 DP 内均衡、跨实例 KV 池化、推理侧容错
+。
+仍是 12 章
+，且与第二部分的「片上 → 卡间 → 机间」形成同一种由内向外的推进。
+另补五处缺口：第 4、11 章的可切分维度补入
+上下文并行
+；第 8 章补入
+集合通信库这一层
+（一次集合通信的代价有多少来自网络、多少来自库）；第 9、11 章把
+重叠列为调度／并行的一个维度
+而非一种技巧；第 9 章的量化按「权重／激活／KV 各换掉什么不同」三分；终章「尾延迟」由两个生产者改为
+三个
+——加入
+专家负载倾斜
+，并据此在待核清单增列两条（倾斜的实测分布、EP 域的故障爆炸半径）。
 下一步
 ：先写第 1 章导论与第 5 章的等效 FLOP 代价表。并行推进待核清单第一条（all-to-all 的延迟
 分布
-）——它同时是终章的空缺、「延迟反转」一节的判据，也是本书能否拿出自己的实测数据的第一块试金石。
+）——它同时是终章的空缺、「延迟反转」一节的判据，也是本书能否拿出自己实测数据的第一块试金石。
