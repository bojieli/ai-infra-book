# H07 独立验收审查

最新合入验收：73条availability证据位置及H05两份新原件已合入。重跑得到96份硬件原件、84个目录来源全部验证通过，0悬空引用、0缺失locator。14项差异已登记，包括H100 NVL同SKU的3938与3900 GB/s数值精度差异。H01/H04旧绑定通过guard逆向恢复与正向全表回放后重绑，历史保留；证据见 `rebinding-evidence.json`。正式H07验收见 `../../inventory/h07-source-review.json`。以下为合入前审查过程，原73项补丁缺口现已关闭。

本轮检查当前151设备、471峰值，不修改硬件目录、来源锁、CLI或计划。`audit.py` 可重跑，`audit.json` 绑定硬件目录和来源锁的 SHA；父代理合并其他字段后需重跑以更新绑定。

当前94份硬件来源原件全部通过 SHA-256 和字节数验证。目录实际引用82个来源，递归检查所有 `source_id` 均在公共锁中且属于对应设备。现有 `hardware.catalog()` 也通过。来源作者身份沿 H01/H02/H03/H04 已完成的厂商原件审查，不把本次哈希或域名检查冒称重新阅读了全部94份原件。`cloudmatrix384-v3` 是作者研究材料，未被当前设备引用；不能因为分组叫hardware就当SKU数据表。BFASCEND合作伙伴目录仍在排除清单，未被设备引用。

具体剩余项已经压缩为以下可验收交付：

| 项目 | 现状与交付 | 验收 |
|---|---|---|
| 官方原件与字段指针 | 94原件、82目录来源，0悬空或跨设备引用 | 重跑 `python calculations/research/h07-review/audit.py` |
| 交付状态的字段位置 | 73个availability对象有source_id但无独立locator；其中68个仅说明历史配置有文档，5个说明M5Ultra官方上市公告 | 合并 `availability-locator-patch.json`，保留所有原状态/日期；再检查73个缺位归零 |
| 旧新规格差异登记 | 13项绑定原件 SHA、差异和处理方法 | 使用 `version-differences.json`；其中配置差异/取整/地域页面差异不冒称时间版本变化 |
| 价格、交付、实测分离 | 公共目录无价格、无已测性能字段；有官方公告及历史配置状态 | `commercial-measurement-separation.json` 独立保留一个官方起售价例及上市时间，不映射成每SKU报价，不宣称已交付；实测未知 |
| 来源作者排除 | 合作伙伴目录排除、作者论文不作设备spec | 保持排除；不拿官方域名代替作者审查 |

H07无需为了勾选而补造全部SKU现价、交货期、实测吞吐，也不以H05尚未知精度字段无限阻塞。其可完成范围是：当前目录所使用的规格证据可追溯，已知差异登记，不同证据类型不互相冒充。合并73条纯证据位置补丁、复核其位置语义并重跑绑定后，可按该范围验收H07。若原计划意图新增真实采购报价或硬件性能实验，则应另列明确地区/日期/配置/测量协议的任务；当前未发生采购或实测。

注意 `hardware.catalog()` 当前未专项校验availability，递归独立审查已覆盖此处。补丁只增加位置及语义范围，不修改数值。常规历史配置availability的位置复用同一已核内存配置出处，不能以此证明当前零售有货；M3Ultra历史512GB使用其独立配置表；M5Ultra公告使用Pricing and Availability第一条。
