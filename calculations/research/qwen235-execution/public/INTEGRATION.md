# Independent public candidate

The module uses normal infra_calc imports and calculate(**result['scenario']) replays. Copy public/src/infra_calc/topics/qwen235_execution.py and its portable test to matching locations. Add a dedicated CLI/topic group and use markdown(result); do not use this as a full request latency result. book.append.json has seven flat scenarios, including explicit capacity failures. Sources are existing pinned public config/implementation/index records; no new sources or weights downloaded. Mathematical/source boundaries are in ../CONTRACT.md. Shared files are unchanged.
