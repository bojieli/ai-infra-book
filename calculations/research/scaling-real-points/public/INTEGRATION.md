# Public integration candidate

Copy src/infra_calc/topics/real_scaling_fit.py, tests/test_real_scaling_fit.py, configs/scaling-real-points/{data.json}, configs/scaling-real-points.lock.json and sources/scaling-real-points into the corresponding calculations directories. scaling_boundary_fit.py here is a byte-identical dependency snapshot of the parent's module; integrate the parent's canonical version once. The data topic imports public scaling_law and scaling_boundary_fit normally and contains no research imports or dynamic Python loading.

Register topic real_scaling_fit, calculate() and markdown(result), and append the one flat scenario in book.append.json under the existing scenario-group convention. No per-scenario numeric knobs: this is a fixed source-backed real-data reproduction. Output has calculation/scenario, source hashes, the complete eight records, original positive-law fit, four prespecified sensitivities and separate boundary diagnostic. The parent owns experiment-lifecycle integration.

records() first verifies every necessary source/data SHA and byte count, then safely parses literal notebook dictionaries using ast.literal_eval (never executes notebook code), reconstructs author N/D/loss, verifies fixed holdout, final-log matching loss and tokenizer/validation path/split controls, derives Appendix S N from logged h/l, and checks each available script budget. Full original official logs and notebook are included; no model weights or irrelevant downstream benchmark files are included. The older strict 33-row protocol stays archived in the parent research directory and is not required by the public CLI.

Run candidate: PYTHONDONTWRITEBYTECODE=1 python calculations/research/scaling-real-points/public/replay.py. Portable tests: PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s calculations/research/scaling-real-points/public/tests -v. example.md is the dedicated complete report, example.json the complete ledger, bindings.json binds delivery files. The replay checks public vs research primary and all sensitivity laws exactly.
