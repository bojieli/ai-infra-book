作者公开数据中的八个可核查 C4 验证点，提供了一个真实的小规模拟合例子。按事先固定的 N≥20 亿留出两个模型，其余六点拟合 L=E+A(N/10⁹)^−α+B(D/10¹⁰)^−β，得到 α=0.10、β=0.45，留出 RMSE 约 0.0193 nats/token。将作者报告参数换成论文架构公式估计、将可核查的 token 坐标换成脚本预算后，留出 RMSE 约在 0.0192–0.0226 之间。这些是同一官方 C4 validation 总体上的不同有限样本，不能假定误差独立，也没有足够证据给出置信区间；参数与数据量仍是报告坐标，脚本预算也不等于实测 token 计数。这个结果展示从真实点、固定留出到敏感性检查的计算流程，并不复现整篇论文的规律或证明普适最优训练配比。
