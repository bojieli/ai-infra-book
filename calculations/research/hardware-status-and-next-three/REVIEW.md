# Hardware acceptance consistency and three remaining foundational calculations

Read-only review of the current checkout. No shared files changed; this is not a new official-source semantic audit. Replay `PYTHONDONTWRITEBYTECODE=1 python calculations/research/hardware-status-and-next-three/check.py` for the structural and acceptance binding checks in verification.json.

## H01–H07: accepted scope remains consistent; reader-facing state is stale

`calculations/PLAN.md:57` through `:63` check all seven items. Current `hardware.catalog()` successfully validates every device and reads/verifies its referenced official archive. `audit_catalog(catalog())` exactly equals `results/hardware-audit.json`: 151 devices, 471 peaks, 198 floating-point denominator-eligible peaks, zero recorded values lacking field source, and 156 unrecorded memory/bandwidth/power fields. These 156 are only the structural audit's three device fields; they are not H05's 1,375 reviewed unknown peak-condition fields. Eligibility also is not a count of runnable models or known measured performance.

H05 and H07 acceptance hardware SHA equals current `6a2451972d01cb5f08a61cac759cd4a1339cf97297a1a0464e3f173174b6550a`; all 97 hardware-source rows canonically hash to the accepted `f22e2119ba0ac30d6615d30143f96214991f65187bc7f4a4898c263f9d6bcbeb`. Model-only additions to sources.lock do not invalidate that hardware subset. H01/H02/H03/H04 retain their named-family/SKU/scope acceptance evidence, while the later H05/H07 bindings cover the final integrated catalog. This check does not substitute SHA equality for re-reading each manufacturer's claim.

| Work package | Current scope/evidence | Assessment |
|---|---|---|
| H01 | PLAN:57; inventory/h01-source-review.json | Named data-center families/forms accepted; unavailable H20/A800 details remain bounded unknown, not a reason to re-open indefinitely. |
| H02 | PLAN:58; inventory/h02-review.json | Original seven RTX SKUs accepted; Server conditions remain explicit unknown. |
| H03 | PLAN:59; inventory/h03-source-review.json | 22 Huawei profiles, chip/Cube/Vector/system distinctions retained. |
| H04 | PLAN:60; inventory/h04-source-review.json | 97 Apple profiles accepted; undisclosed GPU peaks remain unknown. |
| H05 | PLAN:61; inventory/h05-source-review.json | Final hardware/source subset binding matches; complete review does not mean every condition numerically disclosed. |
| H06 | PLAN:62; src/infra_calc/hardware.py:234 | Precision/execution/sparsity admission is distinct from workload mapping. Current schema/catalog validation passes. This limited check does not re-run all prior rejection tests. |
| H07 | PLAN:63; inventory/h07-source-review.json | Final hardware/source subset binding matches and official archive reads pass. No price/measurement inference. |

Concrete metadata drift: `configs/hardware.json:3` still says `in_progress`; `:14043` carries the old pending_families list, including `:14049` “互联方向/片上资源/时钟功率的完整字段审查”. `hardware.py:230` renders this as “尚待补齐”, and `:295` propagates it into audit output even though the finite review was accepted. `outlines/04-加速器架构.md:169` likewise still says the full model/spec differences continue to be checked. The A800 Active note at hardware.json:6201 also predates later 80GB profiles. Minimal later cleanup: separate `review_status=accepted_current_scope` from `known_numerical_gaps`, and map old broad pending prose to the actual bounded unknowns. Do not silently replace unknown numbers or uncheck H01–H07. No cleanup was performed in this read-only task.

## 1. C81/C67: raw screenshot pixels to the already-accounted vision encoder

Evidence: `PLAN.md:201` explicitly leaves CPU preprocessing; `PLAN.md:167` leaves resize. `vision_encoding.py:151` excludes decode/resize/pixel normalization, and `vl_request.py:174` starts downstream of them. All patch projection, vision layers, merger/DeepStack, language prefill/decode and static position bridge already exist and must not be recounted. The exact public preprocessing configuration is `sources/multimodal-cache/qwen3-vl4-preprocessor.json`: patch16, temporal2, merge2, mean/std0.5; actual processor type is Qwen2VLImageProcessorFast, so a generic PIL resize formula is not enough.

Independent executable task: add a fixed-backend `vision_preprocess` candidate. First lock the official processor implementation and its actual resize/normalize helpers at a known revision. Input explicit decoded RGB H×W uint8 (PNG/JPEG decoding is a separate optional codec stage, never guessed), preserve exact smart_resize rounding/min/max-area rules; count rescale/normalize scalar operations, actual pad/temporal repeat/transpose/reshape materializations and semantic bytes. Establish whether crop/resize antialiasing is enabled and its selected implementation; unknown primitive internals stay unknown, while known elementwise work cannot be free. Output the exact tensor/grid_thw contract consumed by existing vision_encoding. Four cases: already-aligned square, non-square requiring resize, minimum-area enlargement, maximum-area reduction. Compare values/shapes against the pinned official CPU implementation and verify that attaching preprocessing changes neither the existing encoder nor language matrix totals. This closes one actual missing stage, not all video/ComputerUse/runtime aspects of C81.

## 2. C22: stage-level heterogeneous resource bounds beyond single-projection/ideal decode

Evidence: `PLAN.md:97`; the core exercise in `outlines/04-加速器架构.md:175–177` asks Qwen3-8B and V4-Flash, prefill/decode, three architecture families and independent matrix/bandwidth/non-matrix resource changes. Existing `projection.py:36–42` computes one GEMM max(F/P,V/B). `batch_reuse.py:67–73` explicitly excludes intermediate activations, conversions, tile rereads and non-matrix work; it only accepts the dense Qwen validator. `decode_budget.py:1–3` explicitly uses nominal 2NB rather than a real architecture. These are useful existing results, not a completed heterogeneous full-path model. H06 source/precision admission is already done and should be reused.

Independent executable task: implement a finite per-stage resource ledger, first consuming existing Qwen8 forward operations and then the already-public V4 inference forward (not the active V4 training work). Map each matrix to matching hardware input/accumulator/unit/sparsity; keep vector/exp/reciprocal/normalization/conversion work in separate resource classes. Use explicit effective rates where official matching supply is absent, mark them assumptions, and leave a complete bound unavailable when a required rate is unknown. Compare sum-of-sequential-stage maxima with max-of-total-work as distinct bounds; optionally use an explicitly declared dependency DAG for overlap. Reuse official capacity and conditional memory traffic, never label semantic interface bytes as measured HBM. Deliver small/large batch prefill/decode at two contexts, plus hypothetical one-resource changes and official NVIDIA/Apple/Huawei profiles with unavailable cells preserved. Tests: operation-work conservation, no INT/TF32/sparse substitution, unavailable resource propagation, capacity failure, and a two-stage counterexample where global max is strictly weaker than stagewise serial max. This completes a bounded core exercise calculation without demanding unavailable vendor peaks or measured runtimes.

## 3. C55: actual forward/backward pipeline schedules and activation lifetime

Evidence: `PLAN.md:149` is unfulfilled. `outlines/10-训练系统.md:127` calls for fill/drain versus 1F1B; `:133` specifies Qwen3-8B's official 36 layers, four stages of nine layers, actual forward/backward imbalance, activation, total batch and completion time. Current `pipeline_schedule.py:1` is explicitly an inference FIFO pipeline; `:49` computes a one-token hidden transfer and schedules feedback, not gradient dependencies or saved training activations. Dense training_matrix/training_nonmatrix/training_state now provide usable component work and states; they are not a GPipe/1F1B scheduler. Do not repeat the active V4 training task.

Independent executable task: implement `training_pipeline_schedule` for fixed Qwen8 PP4, initially GPipe and non-interleaved synchronous 1F1B. Inputs: number of microbatches, tokens/microbatch, per-stage forward/backward service times or explicit effective rates, directional activation/gradient transfer service and buffering, and an explicit saved-activation/recompute policy. Build forward and backward dependencies separately, serialize shared compute and link resources, keep every microbatch's required activations live until its backward use, and enforce a single optimizer update only after all gradients are ready. Report useful compute/bubble/exposed transfer, total B, step makespan and per-stage peak—not a steady-state microbatch formula alone. Sweep M=1/4/8/16, include a slow stage and a shared-link case. Tests: every forward/backward exactly once, no backward-before-forward, no link/compute overbooking, activation release at last use, balanced analytical schedule agreement and M=1 identity between policies. The original long-sequence/MoE extensions stay scoped future work, not silently erased.

## Already completed or active work intentionally excluded

The old chapter03-next-gaps real-data fit/lifecycle item has since been implemented; it is not a new missing item. Dense training non-matrix work also exists, V4 training is currently active, and the original 132 reasoning candidates have already been imported by strategy_record_cost (outline03:50). A new generic importer or another nominal Roofline example would repeat work. The three tasks above add actual missing computational stages and have independent bounded tests; no implementation was started in this read-only review.
