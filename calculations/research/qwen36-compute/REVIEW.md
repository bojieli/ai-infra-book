# Candidate generalized Qwen3.5-family forward calculation

Integration files: `qwen35_forward.py`, `qwen35_reference_steps.py`. Copy into public `src/infra_calc/topics`; root can add model-to-source-path routing around keyword parameters.

`calculate(..., model='qwen3.6-35b-a3b', evidence_root='sources/qwen3.6-35b-a3b', mask_source='sources/qwen3.6-35b-a3b/transformers/src/transformers/masking_utils.py')` uses existing checksum-verified source readers. No globals are monkeypatched. All layer counts, projection and state dimensions, router/expert sizes, rotary slices, and selected statement interfaces derive from text_config. Supplement receives explicit text_config and header-derived gate parameter operand widths.

Pinned Transformers cbc1651a032b923da7f4b44b3d0e6f68e6ba6b55 source checked: modeling_qwen3_5_moe.py lines301–436 selects chunk64, two unit-triangular solves for non-export, six state GEMMs and sequential state updates;438 recurrent branch;504–648 DeltaNet dimensions, convolution/cache branching, explicit A_log.float and a.float, Q/K repetition;749–827 gated query projection, partial RoPE, eager selectable attention and output gate;882–911 FP32 router/topk renormalization and shared expert;944–1000 every layer uses sparse MoE. cache_utils.py1051–1112 establishes initial conv padding, cache-history concatenation, record_past alias/unknown history behavior, and recurrent copy. Rotary source190–213 builds three axes and actual interleaved slice lengths. No Transformers runtime or numerical checkpoint inference claim.

Deliberate legacy correction requested by root: 397B headers store A_log F32 but dt_bias BF16. Previous ledger charged/described both as FP32. Correct dt_bias operand bytes lowers linear.decay_and_beta weight_read_bytes by128 per layer and total summary.weight_read_bytes by5760 per forward. Supplement linear.a_dt_softplus_decay_multiply tensor_input_bytes drops128 per layer; total tensor_interface_read_bytes and its summary alias each drop5760. Notes/assumption text now describes verified dtypes. All other serialized results are unchanged across8 scenarios; matrix/scalar/special work, state, inventory and checkpoint bytes unchanged. 35B both gate parameters areBF16, so explicit A_log.float cast adds64 read/128 write bytes per linear layer.

Verification: `python calculations/research/qwen36-compute/check_candidate.py` compares all old fields apart from precisely asserted corrections across8 scenarios, checks new independent dimension formulas across4 scenarios, then checks full35B verified-source calculations across8 scenarios including prefill8192, cold1, tail65, prefix, B2 decode, record_past initial and unknown retained history. Full35B header matching asserts693 weights,34,660,610,688 base-text parameters and69,321,221,376 checkpoint bytes.

Existing calculation identifier/Markdown heading remain Qwen3.5-family names for root to adapt without old default drift. Candidate retains selected reference scope and full_forward_exact=False; operator bytes and statement interfaces overlap and must not be summed into HBM traffic.
