{
  "decision": "accept_coordinate_and_kv_math; tighten_setup_scope_before_integration",
  "required_finite_changes": [
    {
      "scope": "direct_model_decode_position_add",
      "options": ["Add per-consumed-token arange8bytes and repeat_delta8bytes semantic outputs; explicit device-to-copy condition", "Keep addition-only subtotal and explicitly list arange/repeat/to as omitted setup stages"],
      "must_not_claim": "three additions and24outputbytes are the complete direct decode position setup"
    },
    {
      "scope": "text_only_fresh_direct_model",
      "options": ["Restrict charged get_rope_index graph to requests with images", "Expose explicit standalone helper invocation mode separately"],
      "must_not_claim": "fresh text-only direct model calls get_rope_index"
    }
  ],
  "schema_assumption_to_make_explicit": "int64 input/position IDs, batch1, static images T1, no mask/prefix cache; semantic bytes not allocator/HBM"
}
