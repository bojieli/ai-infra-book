# Public candidate integration

Copy the normally imported topic and portable tests to matching public paths. Add six flat scenarios from book.append.json under a dedicated expert-granularity group, and use markdown(result) to retain every matrix, route, destination and rank byte field. calculate(**result['scenario']) replays directly. Source subset is existing public Qwen data, not a new download; parent bindings include exact dependency SHAs.

Keep baseline and untrained variants visibly distinct. Expert budget equality differs from total-model equality because router size changes. No quality/Tensor Core utilization/runtime recommendation is supported. Retain necessary-only capacity scope and full runtime/peak nulls. Original C13/Experiment2-7 remains broader than this finite expert-granularity subaccount. No shared modifications performed.
