# Flash MTP one-call candidate

This is one explicit source MTPBlock call, with a supplied target feature, not base model generation or demonstrated speculative speedup. Fixed source constructs its MTP at layer43 with ratio0. h is the Block42 output before the base HC head/final norm. Caller supplies which token IDs correspond to those features; the candidate does not infer deployed next-token alignment or recover that h by executing the base43-layer model.

Default B1/T1/start0 matrix work is1,796,997,120 FLOPs, including1,059,061,760 for the shared last-position vocabulary head. MTP-only parameters are6,610,048,891, independently conserved against all1575 official MTP keys and actual header shapes. MTP tensor payload is3,593,787,756 bytes; this excludes shared embedding/head residency and is not an allocator peak. Shared head arithmetic still occurs each call.

The newly charged outer path includes two separate projections, per-stream hnorm, enorm, broadcast addition, two block norms and final MTP norm. The reused one-layer HC ledger includes its final HC-head exactly once. Attention and experts are restricted to exactly this one ratio0/scoring-route layer by isolated function-global copies; shared module globals/configs are never changed. No hash table, compressor or indexer is added. The functions' one virtual layer is explicitly mapped to official43. Existing base-only embedding repeat field and misleading MTP-exclusion wording are adjusted in returned component records.

Nine source FP8 Linear invocations are separately charged for activation quantization/scale application and known interfaces: e_proj/h_proj, four attention projections excluding grouped wo_a, and three shared-expert projections. Their matrix FLOPs already appear in the effective total and are not added twice. Routed experts preserve the existing FP4-packed toFP8 multiply and scale account. The grouped wo_a source requests BF16 despite FP8 checkpoint payload; loading conversion/extra runtime storage remains unresolved. Reused sparse attention includes its known rectangular kernel and softmax accounting, distinguished from effective matrix work. Special operations remain separate; no hardware peak is assigned.

State is independent MTP KV, with source ring capacity128 and head width512. Fresh long prefill writes only the last128 rows to persistent ring but attention consumes the full new KV tensor; positive-start one-token decode writes one ring slot. Default ModelArgs max_seq_len4096/max_batch_size4 are extracted from the locked source when missing in JSON. Frequency table and KV registered allocation are separate declared objects, not a combined runtime peak. Positive-start chunk calls are rejected until separately supported.

Four tests cover independent closed matrix formulas at first-call/B2 multi-token/window overflow/history, exact header/key/parameter conservation, ring sizes, last-only vocabulary head, unchanged shared config and invalid inputs. Four frozen scenarios expose the same cases and replay exactly. These are source formula and shape tests, not full FP8/FP4 numerical execution of pretrained MTP weights. Unknown data movement, setup/temporary casts, caller feature alignment, accepted samples, verification/rollback, actual calls and latency remain explicit. No full MTP training forward/backward claim is made.

Run `/Users/boj/miniconda3/bin/python -m unittest discover -s calculations/research/v4-mtp-forward -p test_mtp.py -v`. `freeze.py` regenerates only this candidate's results/source subset/bindings. Public files are untouched.

Root follow-up: routed act_quant special operations were missing from the aggregate. Added valid-cell abs/reduction+floor/clamp/cast and per-group round/E8M0 counts, with an independent top-k/width test. Matrix and scalar totals are unchanged. Five tests now pass. Markdown now includes readable summary, outer matrices, special operations and ring state before retaining the full ledger.
