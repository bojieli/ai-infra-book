# 专家颗粒度选择条件候选

固定已有coarse64与fine256-k16冻结结果，同16token、TP2/EP4、8192保留位置、BF16、2GiB工作区。从每rank逐专家gate/up/down形状重新汇总有效矩阵与每调用input/weight/output逻辑接口，容量采用已有全部驻留账。

| 项目 | coarse64 | fine256-k16 |
|---|---:|---:|
| 最大rank必要容量 bytes | 67708148736 | 67855997952 |
| 最大rank专家矩阵 FLOPs | 56774098944 | 56774098944 |
| 最大rank GEMM操作数 bytes | 56824922112 | 56935809024 |
| 总消息 bytes | 108017280 | 108017280 |

容量处于[67708148736,67855997952)时仅粗粒度通过已声明必要条件。两者消息量相同，共同wire带宽只能给相同加项，不能导致排序翻转。较多较小专家增加重复输入操作数；不能仅用相同总参数推断相同访问量。

声明串行子账为max-rank专家FLOPs/有效计算率 + max-rank操作数字节/有效操作数服务率 + 总wire bytes/共享fabric服务率。默认粗计算100TF/s、操作数3TB/s、fabric100GB/s时，细计算率需严格大于7372800000000000000/59431 FLOPs/s才能在该子账胜出。不是官方峰值、实测HBM或完整请求时间。

四专项测试通过：独立闭式专家FLOPs、router参数导致的容量差、容量±1B、速率门槛上下整数与共同wire速率不改变门槛、重放及输入拒绝。原两个冻结输入SHA写入结果。候选仍未公开接入，S13未据此关闭：router GEMM和BF16操作数服务已纳入，FP32 softmax/top-k/归一化工作已显式保留但速率未知，需要补所选阶段差异服务的条件输入；其余算术、真实访存/队列和质量不可当零。下一步核对逐阶段共用/差异工作，并做独立审核。

本轮对照固定modeling_qwen3_moe.py的gate→FP32 softmax→topk→norm_topk_prob→cast路径。采用声明每TP/EP rank重复router的执行策略，每rank router FLOPs为2×94×16×4096×E，复制总数与原场景守恒；router操作数按BF16 GEMM input/weight/output计，后续FP32选择不混入其字节。更新门槛包含独立router有效计算率和操作数服务率。完整router选择、专家非线性等差异成本仍未知，结果不作为完整阶段/请求排序。

已进一步把未计耗时纳入条件而非假定为零：定义每方案U为所选串行比较剩余阶段成本，则细粒度胜出的充要条件为U_fine−U_coarse < T_coarse,accounted−T_fine,accounted；等号并列。两个U默认null，不返回条件最快方案。仅显式给定两者后，先按必要容量筛选，再按串行总和比较；两者容量均失败返回空列表。该模型仍不证明实测时间或等质量，但有限的条件选择数学现在覆盖剩余成本变量，不再因无法测量所有primitive而无法表达选择条件。

五专项tests通过，新增剩余差额门槛两侧、单边缺输入和两者容量失败、非负输入检查。四个可读场景展示默认未知、细计算率150TF/s以及细方案额外1us/100us的选择变化。尚待独立审核、固定输入校验与公共CLI/章节接入，S13不提前勾选。

根独立审核已完成：另一套闭式重建两变体全部rank的专家权重/操作数、必要容量及router加项，精确分数核四场景串行成本、差额与有条件选择；4个私有输入破坏反例核SHA拒绝，共44项通过。现固定两上游结果、官方Qwen3 MoE源码与235B配置的SHA，变更会显式拒绝，不能只记录新hash然后继续套旧结论。原5专项tests通过，4冻结结果重放无变化。独立证据见相邻granularity-selection-independent；公共接入仍待完成。
