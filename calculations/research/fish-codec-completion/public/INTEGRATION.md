# Public migration candidate

Copy public/src/infra_calc/topics/fish_wave_export.py and public/tests/test_fish_wave_export.py to their public paths after review. calculate AST matches root candidate (including corrected unconditional final-column slicing scope); only dedicated markdown is added. Four portable tests passed via isolated public module injection. book.append.json contains3flat scenario rows; add a new fish_wave_export book group and CLI entry rather than modifying Fish AR/codec math.

Reuse existing generative-audio-analysis sources.lock.json; no new public source download required. Underlying codec_operations is identical existing Fish codec stage and must be referenced/replaced, not added twice. New wrapper_stages own only code clone/check/cat/host-transfer/cast interfaces. example.md is the dedicated human-readable report; bindings.json records SHA and calculate AST equivalence. Existing source indexes and IO/runtime unknown boundaries remain visible.
