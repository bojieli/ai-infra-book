# Public candidate

Copy src/infra_calc/topics/architecture_tile_work.py and portable test to matching public paths. Add book.append.json's four flat scenarios under a dedicated topic. calculate returns directly replayable scenario; markdown renders all matrices and scope. Uses normal public imports; no runtime research path. Candidate and dependency SHAs are in parent bindings. Returned provenance comes from the existing Qwen8 architecture source.

Do not label valid/padded arithmetic ratio as real Tensor Core utilization or conditional matrix service as end-to-end latency. Full-forward runtime/utilization remain null. Configured rectangular attention overhead and tile-tail work are separate. The two near-budget architectures are untrained; no new checkpoint, quantization schedule or official kernel claim is introduced.
