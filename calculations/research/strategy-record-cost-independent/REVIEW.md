# Independent archived strategy-cost review

Verdict: no numerical or protocol-replay blocker found for this fixed archive. Two author tests pass, and check.py independently verifies all132 candidates,72 groups,88 truncations and18 locked originals. No source/request rerun, GPU experiment, shared edit or posterior answer repair was performed. results.json binds the reviewed module SHA.

The three protocols distinguish thinking1024, thinking4096 and the subsequently added no-thinking4096 diagnostic. Thinking requires an actual </think> delimiter followed by a whole JSON object containing only integer count; no-thinking parses the whole output. Booleans, extra keys and prose/code fences are not accepted. The candidate matches these parser rules. Valid-value plurality uses first occurrence to resolve ties, not correctness; synthetic tie/majority checks supplement the archive, which has no successful groups. Ground truth is independently recomputed by exhaustively enumerating all subset inclusion masks, not by repeating the candidate's dynamic program.

The adaptive one-candidate rule applies only to tasks with at most eight elements; other adaptive and serial cases have two independent attempts, and parallel cases have two overlapping client intervals. We check those expected counts, serial order, parallel overlap and selection_start<=selected_at<=scored_at directly against the archive. Input/output token IDs, final streamed token counts and every candidate's parsed value match. No failed or truncated attempt is dropped. All strategies have zero successful groups, so total output equals failed-group output and both cost-per-success measures correctly remain null.

Client request elapsed intervals are not CPU utilization or GPU execution time. Parallel elapsed sums exceed group wall sums; the latter already includes validation and selection/scoring. The candidate correctly retains separate columns rather than adding them into a larger wall time. Different thinking/template/resource conditions must remain separate batches. Returned token IDs do not establish useful answer tokens, autoregressive forward counts or actual KV residence. Fee, GPU seconds and KV peak remain null because no matching rate/device measurements were supplied.

One scope limit should remain explicit when integrating: this is verification of SHA-bound existing records, not a general schema validator for arbitrary future inputs. It does not independently enforce every archived analyzer check (for example the selected_at subinterval is not separately checked inside group_cost); our review verifies the current bound records against those conditions. Altered records must receive a fresh source/control audit, not merely a regenerated lock.

The public move must preserve a stable path for the sources lock and original experiment files; the current module locates a local sources.lock.json next to itself. The main agent owns that integration detail. Keep all18 source records, three-batch distinction and null unknowns visible in the dedicated report. This does not establish a universally superior strategy, equal quality or production SLO benefit.

Public integration follow-up: strict_value, selected_index and group_cost ASTs match the reviewed candidate exactly. All three public batch results equal the independent candidate byte-for-value as Python objects. The public module uses PROJECT and its relocated config/source lock, so the candidate-local path concern is resolved. public-check.json binds the reviewed public module SHA; schema/summary/renderer additions do not change the archived accounting.
