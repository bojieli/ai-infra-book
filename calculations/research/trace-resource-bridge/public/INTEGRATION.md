# Public candidate: original sealed Chat lengths → Qwen8

Copy the normally imported topic and portable tests to matching public paths. Copy source files from copy-manifest.json into calculations/ matching project_target paths; the module uses PROJECT/sources/trace-resource-bridge and verifies its local sources.lock.json. No global source-lock edits or new download timestamps are invented. Raw captured originals are copied with their pre-existing source-index SHA; derived profile identity is also locked. The runtime does not read research directories. Candidate tests redirect only SOURCE_ROOT to the staged exact source files before installation.

Two flat scenarios: unknown_steps (default, known logical prefill mapping only) and returned_ids_serial_policy (explicit conventional generation-step assumption over the same original records). Use dedicated markdown(result) to retain all raw-field mappings and forward ledgers. calculate(**result['scenario']) replays. No model requests are executed.

The source group is specifically four original Chat captures; no replay or teaching request trace is substituted. Returned IDs include EOS and do not prove actual model scheduler steps. All observed_model_forward_calls fields remain null even in the conditional serial scenario. Tool timing remains null/not applicable to these Chat records. Existing wall times are not compute time or throughput predictions. See ../CONTRACT.md for the evidence table and finite minimum logging needed to reconstruct actual execution. Candidate closes the observed-length→logical-resource substitution, not runtime trace completeness or whole T05/C14.
