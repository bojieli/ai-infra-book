# Independent experiment6-10 cohort audit

No correctness blocker found. `python calculations/research/supernode-cohort-cost/check.py` passes1701 independent integer-tick scheduling scenarios,54 model/concurrency/deadline/failure composite scenes,27 capacity-invalid candidate exclusions, and10 invalid inputs. Check results and selections are saved in check-result.json. No candidate or public file was edited.

The independent simulator advances one integer millisecond, settles completed stages first, commits completed requests before same-time failures, applies the failure/recovery event, then starts eligible queued work. This differs structurally from the candidate's per-attempt stage loop. The checker compares complete request and attempt traces, stage names/FLOPs, partial-stage records, completion times, abandoned work, and fault-use flags.

Boundary grid covers durations[3], [3,2], [3,2,1];1/3/7 requests;1/2/4 replicas; no failure and every integer failure time0..19; recovery0/1/5. At fault0, recovery precedes the first attempt. At a nonfinal stage-end fault, that stage is completed and counted as abandoned completed-stage FLOPs, while the whole uncommitted request restarts. At complete-request time, the response commits and is never restarted; a subsequent queued request waits for recovery without creating a spurious failed attempt. Post-cohort failure causes no restart or external fee. Partial interrupted stage work remainsnull rather than being inferred from elapsed-time fractions. All these behaviors match.

For54 public-model composite scenes, the checker reconstructs round-robin scheduling, all8-card reserved horizon, external recovery fee, valid deadline counts, ceil(3N/4) eligibility, valid-request denominators, winner ties, and empty eligibility independently. Required valid counts are1/3/6 for cohorts1/4/8. Reservation cost is8×horizon_ms/1000 credits, including idle/recovery/replay time once; the fee is added once only when the modeled workload uses recovery. Zero-valid candidates retain null per-valid cost and cannot enter selection.

Every candidate has8 placement-card rows, TP×replicas=8, and resident=weight+KV+workspace. Capacity status exactly matches all cards fitting24,000,000,000bytes;27 invalid entries have no execution/cost/SLO result and never enter eligibility. Summed physical final KV independently equals replicas × bytes_per_position ×(256+8−1), using147,456bytes for Qwen3-8B and262,144bytes for Qwen3-32B. Prefill produces the first output; seven decode stages produce the remaining outputs. At TP2/4/8 these models do not require duplicate KV heads.

Scope is internally consistent: stage service durations,32B multiplier, per-card capacity/workspace, prices, fault times and recovery behavior are declared teaching inputs. Model matrix work is a separate source-backed ledger and does not establish those durations. Cohort concurrency is queued burst size, not tensor batch. No partial streaming responses count as deadline successes. Full-request restart with one affected replica is the declared protocol; no broader real recovery or transport guarantee is inferred. Cost is the specified reservation-plus-fee boundary, not measured TCO. Keep these conditions visible with any winning candidate.
