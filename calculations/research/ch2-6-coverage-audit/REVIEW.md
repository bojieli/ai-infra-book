# 第2章2.6原要求覆盖审计

本审计逐句读取当前正文2.6.1–5、对应扩写、实验2-7/8/9与图2-7/8，再对照inventory历史文本及实际公共代码、book场景和JSON。共拆24个有限要求：8项complete、14项partial、2项missing。状态按本条明确范围判定，不能据8项完成勾选整个C13/C14。完整原句、行号、函数、场景和SHA见mapping.json。没有修改公共文件。

这里的complete可以是原文要求的“明确假设下条件计算”；不强制将每个教学格式变成真实量化checkpoint或把所有unknown补为实测。partial保留原任务，不把已有子模块等同整实验。missing指缺该种实际计算/证据，现有邻近模块不能替代。

## C13已实现部分应予承认

四档代表Qwen8、Dense32、DeepSeek官方蒸馏Llama70和Qwen235，24/48/80 decimal GB单卡与八卡的BF16/8/4-bit条件容量已有。单卡10场景、Dense八卡54场景、235八卡24场景均有实际JSON。local-K重分组、逐行尾部/scale、高精度部分、全部专家驻留、完整KV头复制、PP不均匀层、workspace输入和最差rank并发都已在代码中，70B或低位多卡不应再列missing。32B是合法的30–32B档代表；正文另有30B MoE解释，不能任意把本档扩大为所有30B模型。

Dense近似等参变体也已经完成有限核心：24/36/48层、3072/4096/5120宽、FFN补偿和Q/KV头变化，参数误差/严格等参标志、矩阵/状态/串行依赖与条件容量翻转均输出。不能因为没有质量实验，就否认这些算术已完成；也不能把未训练变体称等质量。

## C13真正下一缺口

1. **专家粒度是明确missing计算。** architecture_variants仅Dense；新qwen235_execution改变的是同E128/top-k8/F配置下的路由与放置。改变直方图不能替代改变E/F/top-k。下一计算应从真实235配置声明E/F预算变体，含router参数变化、对齐差额与完整所有权，固定同token批给每专家M、去重owner通信、加权合并、容量和最忙rank条件。保留质量unknown。
2. **宽度到tile工作/服务条件仍partial。** 已有实际矩阵形状，但没有在这一组深窄/浅宽变体上指定kernel tile或有效服务率，不可声称已证Tensor Core利用或经济点。可只选一对变体，给tile补齐和条件速率不等式，不需要穷举硬件。
3. **原句八专家卡与TP8头复制是场景连接缺口。** 当前235冻结放置场景最大TP4/EP4。此次只读调用现有calculate(tp=8,ep=1)和(tp=1,ep=8)均成功，逐rank KV/专家范围已写入mapping；因此无需新公式，只补两条固定场景及正文数值即可。
4. 图2-7仍为配图计划。容量表已存在，但不能把表自动当成已交付容量曲线和深宽/多专家SVG。

## C14已实现与剩余

实验2-8固定夹具已实现阶段分类、输入/返回ID、工具动作/等待、两种命中加权和复用间隔；公共workload_profiles又补中位/p95。44条9组不是44条独立自然生产请求，报告正确保留重放身份。等均值208、不同方差2433.5/4410的离线对照已完成；不能要求重新生成真实队列来否定其声明对照。

实验2-9四模型逻辑请求账已经保留原名单Qwen8、V4Flash/Pro、K3，统一S/P/G/B与G−1，含跨压缩边界、V4顺序恢复前缀/中间head、状态/访问、K3冲突和unknown分类。此子项complete，不能再叫“尚未累计完整decode”；但同token几何不是相同文本或质量。

仍缺如下有限连接：

- 将一组封存轨迹的已知字段真正代入资源账；当前画像和教学request_trace是两个入口，正文也明确逐请求资源代入仍待。
- 在一个具体请求/硬件条件下，将四模型已知阶段账接合法精度分母、容量与链路下界。通用Roofline模块的存在不等于本实验已执行。不得将特殊操作和未知kernel内部当零，也不必为求排名补造速率。
- 固定任务质量记录及能改变选择的最小实验是另一明确missing。原Agent任务检查不构成四模型同任务质量对照。
- V4/K3 MTP的报告训练层、发布结构和后端实际启用三方对照仍partial。Checkpoint/optimizer含MTP分账与通用speculative教学预算，不是两个模型真实MTP forward/verify的执行连接。
- HBM/host/remote取回和画像命中率各有模块；尚未以同一轨迹连接真实或声明状态寿命。图2-8可以保留教学预留区间，但不能填成实测逐block寿命。

## 原文与inventory处理

mapping同时保留18条相关inventory记录。inventory是早期快照，正文随后增加了70B、Dense低位、235执行和请求总账；本次绑定当前文件SHA，未据旧清单误判。PLAN只用于确认工作包编号，不作为完成证据。正文在2.6之后另附生成多模态案例链接，本审计不将其所有下游任务并入C13/C14，也不扩展为全书覆盖审计。

建议先做专家E/F预算变体并补TP8/EP8两现成场景；随后选一组固定轨迹和两个模型进行质量约束的资源选择实验。两者都能闭合原文明确要求，而不是继续泛化扩展模型架构清单。
