# Root review of the Flash MTP candidate

The root reviewed the candidate against the archived official MTPBlock constructor and forward, including shared embedding/head assignment and the last-position vocabulary projection in ParallelHead. No new upstream version is claimed. The source-defined call consumes caller-supplied features; this is not evidence of an enabled serving drafter or a measured speculative speedup.

The executable independent check validates the frozen candidate/dependency hashes, 24 boundary scenarios (batch 1/2/4; fresh lengths 1/2/127/128/129; positive positions 127/128/4095), independent matrix closed forms, outer projection row counts, last-only shared head, ring write/valid sizes, one-layer ownership, null latency, four frozen replays and unchanged public helper outputs after alternating candidate calls. All 203 checks pass. The existing four candidate tests also pass with zero skips.

For B=1, T=1, start=0, effective matrix work is 1,796,997,120 FLOPs, including 1,059,061,760 for the shared vocabulary projection. Outer embedding/hidden projections contribute 2 B T H² (1 + hc_mult), with hc_mult=4; the hidden projection has four times as many rows as the embedding projection. There is exactly one source layer 43, with ratio 0, and no compressor/indexer work.

This review establishes the matrix formulas, selected ring-state accounting and helper isolation. It does not independently validate every scalar/special-operation subtotal or all 1,575 checkpoint tensor interpretations; candidate tests and header conservation checks are evidence for those parts, not an independent full audit. Those details, readable reporting and public CLI/reproduction integration remain to be completed before claiming the book requirement is closed. Known interface bytes are partial operands, not complete physical HBM traffic. Actual feature alignment, draft call counts, verification/rollback, accepted-token counts and latency remain unknown.

Follow-up correction: inspecting archived act_quant_kernel and Expert.forward exposed missing routed-expert activation special operations in the candidate aggregate. The candidate now includes six categories from valid routed cells/groups, with 127 absmax reduction comparisons plus one floor comparison per group. B1/T1 adds 61,440 abs, 61,440 max/floor comparisons, 122,880 clamp comparisons, 480 round-scale calls, 61,440 FP8 cast elements and 480 E8M0 cast elements. These counts are not padded GPU instructions. The matrix/scalar totals are unchanged. Five tests pass after the correction, and all 203 independent formula/hash/replay/isolation checks pass against the refreshed candidate. Current calculate.py SHA256 is 6f1b98cf43fd3fb7aa3f8a7034c69b0b4b68ff61acffaa892424324a6ae528e4. Remaining full scalar/header review and public integration are still open.

Raw header and scalar composition audit completed in check_components.py: independently reads the pinned index and each selected shard header, verifies every payload against dtype size, identifies packed routed weights by source key role, excludes scale tensors from parameters, and compares all tensor records and aggregate counts. Adds independent MoE, two-branch mHC plus final head, outer norms/broadcast, FP8 scale/quantization and routed scale/quantization formulas over four scenarios. All 3,377 combined checks pass. This composition audit reuses the existing attention arithmetic subledger; it does not claim fresh numerical execution of that kernel. No new matrix/scalar discrepancy found. Readable Markdown now includes internal attention/expert projection rows and mHC operation rows. Five candidate tests still pass. Public integration remains pending.
