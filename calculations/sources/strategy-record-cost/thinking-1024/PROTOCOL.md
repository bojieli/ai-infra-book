# 3-3 运行前协议

四道固定子集计数题由穷举与DP交叉验证，仅用于任务正确性评分，不是书中calculations数量级工作。Qwen3-8B固定快照、thinking开启，每候选总生成上限1024（不是独立reasoning配额），温度0.6/top-p0.95，按trial与候选索引固定seed。两轮、四题、三个策略，随机组顺序固定seed303。串行两个独立候选（不是反馈续推）；并行在同一GPU引擎同时提交两个候选；自适应在运行前按元素数<=8一个候选，否则串行两个。24组44次请求。不得读取真值决定重试或分配预算。

最终回答必须在thinking结束后为严格JSON整数count。有效候选多数投票，平票取候选索引较小者；全无效判失败。正确答案只在选完后评分，不做oracle挑选。失败和截断均保留。串并行相同上限不是相同实际token；自适应节省候选但未必同质量。保存完整输入/输出token、思考文本、客户端事件、引擎metrics和选择/验证时间。逻辑token状态不冒充真实KV块驻留，未采集硬件峰值；费用未知为null。共享GPU、两轮四题无法给普适质量/生产SLO结论。串并行调度可能导致输出差异，即使seed相同也要逐项核对。
